var jsxRuntime = { exports: {} };
var reactJsxRuntime_production = {};
var hasRequiredReactJsxRuntime_production;
function requireReactJsxRuntime_production() {
  if (hasRequiredReactJsxRuntime_production) return reactJsxRuntime_production;
  hasRequiredReactJsxRuntime_production = 1;
  var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.transitional.element"), REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
  function jsxProd(type, config, maybeKey) {
    var key = null;
    void 0 !== maybeKey && (key = "" + maybeKey);
    void 0 !== config.key && (key = "" + config.key);
    if ("key" in config) {
      maybeKey = {};
      for (var propName in config)
        "key" !== propName && (maybeKey[propName] = config[propName]);
    } else maybeKey = config;
    config = maybeKey.ref;
    return {
      $$typeof: REACT_ELEMENT_TYPE,
      type,
      key,
      ref: void 0 !== config ? config : null,
      props: maybeKey
    };
  }
  reactJsxRuntime_production.Fragment = REACT_FRAGMENT_TYPE;
  reactJsxRuntime_production.jsx = jsxProd;
  reactJsxRuntime_production.jsxs = jsxProd;
  return reactJsxRuntime_production;
}
var hasRequiredJsxRuntime;
function requireJsxRuntime() {
  if (hasRequiredJsxRuntime) return jsxRuntime.exports;
  hasRequiredJsxRuntime = 1;
  {
    jsxRuntime.exports = requireReactJsxRuntime_production();
  }
  return jsxRuntime.exports;
}
var jsxRuntimeExports = requireJsxRuntime();
var react = { exports: {} };
var react_production = {};
var hasRequiredReact_production;
function requireReact_production() {
  if (hasRequiredReact_production) return react_production;
  hasRequiredReact_production = 1;
  var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense"), REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo"), REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = /* @__PURE__ */ Symbol.for("react.activity"), MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
  function getIteratorFn(maybeIterable) {
    if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
    maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
    return "function" === typeof maybeIterable ? maybeIterable : null;
  }
  var ReactNoopUpdateQueue = {
    isMounted: function() {
      return false;
    },
    enqueueForceUpdate: function() {
    },
    enqueueReplaceState: function() {
    },
    enqueueSetState: function() {
    }
  }, assign = Object.assign, emptyObject = {};
  function Component(props, context, updater) {
    this.props = props;
    this.context = context;
    this.refs = emptyObject;
    this.updater = updater || ReactNoopUpdateQueue;
  }
  Component.prototype.isReactComponent = {};
  Component.prototype.setState = function(partialState, callback) {
    if ("object" !== typeof partialState && "function" !== typeof partialState && null != partialState)
      throw Error(
        "takes an object of state variables to update or a function which returns an object of state variables."
      );
    this.updater.enqueueSetState(this, partialState, callback, "setState");
  };
  Component.prototype.forceUpdate = function(callback) {
    this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
  };
  function ComponentDummy() {
  }
  ComponentDummy.prototype = Component.prototype;
  function PureComponent(props, context, updater) {
    this.props = props;
    this.context = context;
    this.refs = emptyObject;
    this.updater = updater || ReactNoopUpdateQueue;
  }
  var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
  pureComponentPrototype.constructor = PureComponent;
  assign(pureComponentPrototype, Component.prototype);
  pureComponentPrototype.isPureReactComponent = true;
  var isArrayImpl = Array.isArray;
  function noop() {
  }
  var ReactSharedInternals = { H: null, A: null, T: null, S: null }, hasOwnProperty = Object.prototype.hasOwnProperty;
  function ReactElement(type, key, props) {
    var refProp = props.ref;
    return {
      $$typeof: REACT_ELEMENT_TYPE,
      type,
      key,
      ref: void 0 !== refProp ? refProp : null,
      props
    };
  }
  function cloneAndReplaceKey(oldElement, newKey) {
    return ReactElement(oldElement.type, newKey, oldElement.props);
  }
  function isValidElement(object) {
    return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
  }
  function escape(key) {
    var escaperLookup = { "=": "=0", ":": "=2" };
    return "$" + key.replace(/[=:]/g, function(match) {
      return escaperLookup[match];
    });
  }
  var userProvidedKeyEscapeRegex = /\/+/g;
  function getElementKey(element, index) {
    return "object" === typeof element && null !== element && null != element.key ? escape("" + element.key) : index.toString(36);
  }
  function resolveThenable(thenable) {
    switch (thenable.status) {
      case "fulfilled":
        return thenable.value;
      case "rejected":
        throw thenable.reason;
      default:
        switch ("string" === typeof thenable.status ? thenable.then(noop, noop) : (thenable.status = "pending", thenable.then(
          function(fulfilledValue) {
            "pending" === thenable.status && (thenable.status = "fulfilled", thenable.value = fulfilledValue);
          },
          function(error) {
            "pending" === thenable.status && (thenable.status = "rejected", thenable.reason = error);
          }
        )), thenable.status) {
          case "fulfilled":
            return thenable.value;
          case "rejected":
            throw thenable.reason;
        }
    }
    throw thenable;
  }
  function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
    var type = typeof children;
    if ("undefined" === type || "boolean" === type) children = null;
    var invokeCallback = false;
    if (null === children) invokeCallback = true;
    else
      switch (type) {
        case "bigint":
        case "string":
        case "number":
          invokeCallback = true;
          break;
        case "object":
          switch (children.$$typeof) {
            case REACT_ELEMENT_TYPE:
            case REACT_PORTAL_TYPE:
              invokeCallback = true;
              break;
            case REACT_LAZY_TYPE:
              return invokeCallback = children._init, mapIntoArray(
                invokeCallback(children._payload),
                array,
                escapedPrefix,
                nameSoFar,
                callback
              );
          }
      }
    if (invokeCallback)
      return callback = callback(children), invokeCallback = "" === nameSoFar ? "." + getElementKey(children, 0) : nameSoFar, isArrayImpl(callback) ? (escapedPrefix = "", null != invokeCallback && (escapedPrefix = invokeCallback.replace(userProvidedKeyEscapeRegex, "$&/") + "/"), mapIntoArray(callback, array, escapedPrefix, "", function(c) {
        return c;
      })) : null != callback && (isValidElement(callback) && (callback = cloneAndReplaceKey(
        callback,
        escapedPrefix + (null == callback.key || children && children.key === callback.key ? "" : ("" + callback.key).replace(
          userProvidedKeyEscapeRegex,
          "$&/"
        ) + "/") + invokeCallback
      )), array.push(callback)), 1;
    invokeCallback = 0;
    var nextNamePrefix = "" === nameSoFar ? "." : nameSoFar + ":";
    if (isArrayImpl(children))
      for (var i = 0; i < children.length; i++)
        nameSoFar = children[i], type = nextNamePrefix + getElementKey(nameSoFar, i), invokeCallback += mapIntoArray(
          nameSoFar,
          array,
          escapedPrefix,
          type,
          callback
        );
    else if (i = getIteratorFn(children), "function" === typeof i)
      for (children = i.call(children), i = 0; !(nameSoFar = children.next()).done; )
        nameSoFar = nameSoFar.value, type = nextNamePrefix + getElementKey(nameSoFar, i++), invokeCallback += mapIntoArray(
          nameSoFar,
          array,
          escapedPrefix,
          type,
          callback
        );
    else if ("object" === type) {
      if ("function" === typeof children.then)
        return mapIntoArray(
          resolveThenable(children),
          array,
          escapedPrefix,
          nameSoFar,
          callback
        );
      array = String(children);
      throw Error(
        "Objects are not valid as a React child (found: " + ("[object Object]" === array ? "object with keys {" + Object.keys(children).join(", ") + "}" : array) + "). If you meant to render a collection of children, use an array instead."
      );
    }
    return invokeCallback;
  }
  function mapChildren(children, func, context) {
    if (null == children) return children;
    var result = [], count = 0;
    mapIntoArray(children, result, "", "", function(child) {
      return func.call(context, child, count++);
    });
    return result;
  }
  function lazyInitializer(payload) {
    if (-1 === payload._status) {
      var ctor = payload._result;
      ctor = ctor();
      ctor.then(
        function(moduleObject) {
          if (0 === payload._status || -1 === payload._status)
            payload._status = 1, payload._result = moduleObject;
        },
        function(error) {
          if (0 === payload._status || -1 === payload._status)
            payload._status = 2, payload._result = error;
        }
      );
      -1 === payload._status && (payload._status = 0, payload._result = ctor);
    }
    if (1 === payload._status) return payload._result.default;
    throw payload._result;
  }
  var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
    if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
      var event = new window.ErrorEvent("error", {
        bubbles: true,
        cancelable: true,
        message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
        error
      });
      if (!window.dispatchEvent(event)) return;
    } else if ("object" === typeof process && "function" === typeof process.emit) {
      process.emit("uncaughtException", error);
      return;
    }
    console.error(error);
  }, Children = {
    map: mapChildren,
    forEach: function(children, forEachFunc, forEachContext) {
      mapChildren(
        children,
        function() {
          forEachFunc.apply(this, arguments);
        },
        forEachContext
      );
    },
    count: function(children) {
      var n = 0;
      mapChildren(children, function() {
        n++;
      });
      return n;
    },
    toArray: function(children) {
      return mapChildren(children, function(child) {
        return child;
      }) || [];
    },
    only: function(children) {
      if (!isValidElement(children))
        throw Error(
          "React.Children.only expected to receive a single React element child."
        );
      return children;
    }
  };
  react_production.Activity = REACT_ACTIVITY_TYPE;
  react_production.Children = Children;
  react_production.Component = Component;
  react_production.Fragment = REACT_FRAGMENT_TYPE;
  react_production.Profiler = REACT_PROFILER_TYPE;
  react_production.PureComponent = PureComponent;
  react_production.StrictMode = REACT_STRICT_MODE_TYPE;
  react_production.Suspense = REACT_SUSPENSE_TYPE;
  react_production.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = ReactSharedInternals;
  react_production.__COMPILER_RUNTIME = {
    __proto__: null,
    c: function(size) {
      return ReactSharedInternals.H.useMemoCache(size);
    }
  };
  react_production.cache = function(fn) {
    return function() {
      return fn.apply(null, arguments);
    };
  };
  react_production.cacheSignal = function() {
    return null;
  };
  react_production.cloneElement = function(element, config, children) {
    if (null === element || void 0 === element)
      throw Error(
        "The argument must be a React element, but you passed " + element + "."
      );
    var props = assign({}, element.props), key = element.key;
    if (null != config)
      for (propName in void 0 !== config.key && (key = "" + config.key), config)
        !hasOwnProperty.call(config, propName) || "key" === propName || "__self" === propName || "__source" === propName || "ref" === propName && void 0 === config.ref || (props[propName] = config[propName]);
    var propName = arguments.length - 2;
    if (1 === propName) props.children = children;
    else if (1 < propName) {
      for (var childArray = Array(propName), i = 0; i < propName; i++)
        childArray[i] = arguments[i + 2];
      props.children = childArray;
    }
    return ReactElement(element.type, key, props);
  };
  react_production.createContext = function(defaultValue) {
    defaultValue = {
      $$typeof: REACT_CONTEXT_TYPE,
      _currentValue: defaultValue,
      _currentValue2: defaultValue,
      _threadCount: 0,
      Provider: null,
      Consumer: null
    };
    defaultValue.Provider = defaultValue;
    defaultValue.Consumer = {
      $$typeof: REACT_CONSUMER_TYPE,
      _context: defaultValue
    };
    return defaultValue;
  };
  react_production.createElement = function(type, config, children) {
    var propName, props = {}, key = null;
    if (null != config)
      for (propName in void 0 !== config.key && (key = "" + config.key), config)
        hasOwnProperty.call(config, propName) && "key" !== propName && "__self" !== propName && "__source" !== propName && (props[propName] = config[propName]);
    var childrenLength = arguments.length - 2;
    if (1 === childrenLength) props.children = children;
    else if (1 < childrenLength) {
      for (var childArray = Array(childrenLength), i = 0; i < childrenLength; i++)
        childArray[i] = arguments[i + 2];
      props.children = childArray;
    }
    if (type && type.defaultProps)
      for (propName in childrenLength = type.defaultProps, childrenLength)
        void 0 === props[propName] && (props[propName] = childrenLength[propName]);
    return ReactElement(type, key, props);
  };
  react_production.createRef = function() {
    return { current: null };
  };
  react_production.forwardRef = function(render2) {
    return { $$typeof: REACT_FORWARD_REF_TYPE, render: render2 };
  };
  react_production.isValidElement = isValidElement;
  react_production.lazy = function(ctor) {
    return {
      $$typeof: REACT_LAZY_TYPE,
      _payload: { _status: -1, _result: ctor },
      _init: lazyInitializer
    };
  };
  react_production.memo = function(type, compare) {
    return {
      $$typeof: REACT_MEMO_TYPE,
      type,
      compare: void 0 === compare ? null : compare
    };
  };
  react_production.startTransition = function(scope) {
    var prevTransition = ReactSharedInternals.T, currentTransition = {};
    ReactSharedInternals.T = currentTransition;
    try {
      var returnValue = scope(), onStartTransitionFinish = ReactSharedInternals.S;
      null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
      "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && returnValue.then(noop, reportGlobalError);
    } catch (error) {
      reportGlobalError(error);
    } finally {
      null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
    }
  };
  react_production.unstable_useCacheRefresh = function() {
    return ReactSharedInternals.H.useCacheRefresh();
  };
  react_production.use = function(usable) {
    return ReactSharedInternals.H.use(usable);
  };
  react_production.useActionState = function(action, initialState, permalink) {
    return ReactSharedInternals.H.useActionState(action, initialState, permalink);
  };
  react_production.useCallback = function(callback, deps) {
    return ReactSharedInternals.H.useCallback(callback, deps);
  };
  react_production.useContext = function(Context) {
    return ReactSharedInternals.H.useContext(Context);
  };
  react_production.useDebugValue = function() {
  };
  react_production.useDeferredValue = function(value, initialValue) {
    return ReactSharedInternals.H.useDeferredValue(value, initialValue);
  };
  react_production.useEffect = function(create, deps) {
    return ReactSharedInternals.H.useEffect(create, deps);
  };
  react_production.useEffectEvent = function(callback) {
    return ReactSharedInternals.H.useEffectEvent(callback);
  };
  react_production.useId = function() {
    return ReactSharedInternals.H.useId();
  };
  react_production.useImperativeHandle = function(ref, create, deps) {
    return ReactSharedInternals.H.useImperativeHandle(ref, create, deps);
  };
  react_production.useInsertionEffect = function(create, deps) {
    return ReactSharedInternals.H.useInsertionEffect(create, deps);
  };
  react_production.useLayoutEffect = function(create, deps) {
    return ReactSharedInternals.H.useLayoutEffect(create, deps);
  };
  react_production.useMemo = function(create, deps) {
    return ReactSharedInternals.H.useMemo(create, deps);
  };
  react_production.useOptimistic = function(passthrough, reducer) {
    return ReactSharedInternals.H.useOptimistic(passthrough, reducer);
  };
  react_production.useReducer = function(reducer, initialArg, init) {
    return ReactSharedInternals.H.useReducer(reducer, initialArg, init);
  };
  react_production.useRef = function(initialValue) {
    return ReactSharedInternals.H.useRef(initialValue);
  };
  react_production.useState = function(initialState) {
    return ReactSharedInternals.H.useState(initialState);
  };
  react_production.useSyncExternalStore = function(subscribe, getSnapshot, getServerSnapshot) {
    return ReactSharedInternals.H.useSyncExternalStore(
      subscribe,
      getSnapshot,
      getServerSnapshot
    );
  };
  react_production.useTransition = function() {
    return ReactSharedInternals.H.useTransition();
  };
  react_production.version = "19.2.7";
  return react_production;
}
var hasRequiredReact;
function requireReact() {
  if (hasRequiredReact) return react.exports;
  hasRequiredReact = 1;
  {
    react.exports = requireReact_production();
  }
  return react.exports;
}
var reactExports = requireReact();
var client = { exports: {} };
var reactDomClient_production = {};
var scheduler = { exports: {} };
var scheduler_production = {};
var hasRequiredScheduler_production;
function requireScheduler_production() {
  if (hasRequiredScheduler_production) return scheduler_production;
  hasRequiredScheduler_production = 1;
  (function(exports) {
    function push(heap, node) {
      var index = heap.length;
      heap.push(node);
      a: for (; 0 < index; ) {
        var parentIndex = index - 1 >>> 1, parent = heap[parentIndex];
        if (0 < compare(parent, node))
          heap[parentIndex] = node, heap[index] = parent, index = parentIndex;
        else break a;
      }
    }
    function peek(heap) {
      return 0 === heap.length ? null : heap[0];
    }
    function pop(heap) {
      if (0 === heap.length) return null;
      var first = heap[0], last = heap.pop();
      if (last !== first) {
        heap[0] = last;
        a: for (var index = 0, length = heap.length, halfLength = length >>> 1; index < halfLength; ) {
          var leftIndex = 2 * (index + 1) - 1, left = heap[leftIndex], rightIndex = leftIndex + 1, right = heap[rightIndex];
          if (0 > compare(left, last))
            rightIndex < length && 0 > compare(right, left) ? (heap[index] = right, heap[rightIndex] = last, index = rightIndex) : (heap[index] = left, heap[leftIndex] = last, index = leftIndex);
          else if (rightIndex < length && 0 > compare(right, last))
            heap[index] = right, heap[rightIndex] = last, index = rightIndex;
          else break a;
        }
      }
      return first;
    }
    function compare(a, b) {
      var diff = a.sortIndex - b.sortIndex;
      return 0 !== diff ? diff : a.id - b.id;
    }
    exports.unstable_now = void 0;
    if ("object" === typeof performance && "function" === typeof performance.now) {
      var localPerformance = performance;
      exports.unstable_now = function() {
        return localPerformance.now();
      };
    } else {
      var localDate = Date, initialTime = localDate.now();
      exports.unstable_now = function() {
        return localDate.now() - initialTime;
      };
    }
    var taskQueue = [], timerQueue = [], taskIdCounter = 1, currentTask = null, currentPriorityLevel = 3, isPerformingWork = false, isHostCallbackScheduled = false, isHostTimeoutScheduled = false, needsPaint = false, localSetTimeout = "function" === typeof setTimeout ? setTimeout : null, localClearTimeout = "function" === typeof clearTimeout ? clearTimeout : null, localSetImmediate = "undefined" !== typeof setImmediate ? setImmediate : null;
    function advanceTimers(currentTime) {
      for (var timer = peek(timerQueue); null !== timer; ) {
        if (null === timer.callback) pop(timerQueue);
        else if (timer.startTime <= currentTime)
          pop(timerQueue), timer.sortIndex = timer.expirationTime, push(taskQueue, timer);
        else break;
        timer = peek(timerQueue);
      }
    }
    function handleTimeout(currentTime) {
      isHostTimeoutScheduled = false;
      advanceTimers(currentTime);
      if (!isHostCallbackScheduled)
        if (null !== peek(taskQueue))
          isHostCallbackScheduled = true, isMessageLoopRunning || (isMessageLoopRunning = true, schedulePerformWorkUntilDeadline());
        else {
          var firstTimer = peek(timerQueue);
          null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
        }
    }
    var isMessageLoopRunning = false, taskTimeoutID = -1, frameInterval = 5, startTime = -1;
    function shouldYieldToHost() {
      return needsPaint ? true : exports.unstable_now() - startTime < frameInterval ? false : true;
    }
    function performWorkUntilDeadline() {
      needsPaint = false;
      if (isMessageLoopRunning) {
        var currentTime = exports.unstable_now();
        startTime = currentTime;
        var hasMoreWork = true;
        try {
          a: {
            isHostCallbackScheduled = false;
            isHostTimeoutScheduled && (isHostTimeoutScheduled = false, localClearTimeout(taskTimeoutID), taskTimeoutID = -1);
            isPerformingWork = true;
            var previousPriorityLevel = currentPriorityLevel;
            try {
              b: {
                advanceTimers(currentTime);
                for (currentTask = peek(taskQueue); null !== currentTask && !(currentTask.expirationTime > currentTime && shouldYieldToHost()); ) {
                  var callback = currentTask.callback;
                  if ("function" === typeof callback) {
                    currentTask.callback = null;
                    currentPriorityLevel = currentTask.priorityLevel;
                    var continuationCallback = callback(
                      currentTask.expirationTime <= currentTime
                    );
                    currentTime = exports.unstable_now();
                    if ("function" === typeof continuationCallback) {
                      currentTask.callback = continuationCallback;
                      advanceTimers(currentTime);
                      hasMoreWork = true;
                      break b;
                    }
                    currentTask === peek(taskQueue) && pop(taskQueue);
                    advanceTimers(currentTime);
                  } else pop(taskQueue);
                  currentTask = peek(taskQueue);
                }
                if (null !== currentTask) hasMoreWork = true;
                else {
                  var firstTimer = peek(timerQueue);
                  null !== firstTimer && requestHostTimeout(
                    handleTimeout,
                    firstTimer.startTime - currentTime
                  );
                  hasMoreWork = false;
                }
              }
              break a;
            } finally {
              currentTask = null, currentPriorityLevel = previousPriorityLevel, isPerformingWork = false;
            }
            hasMoreWork = void 0;
          }
        } finally {
          hasMoreWork ? schedulePerformWorkUntilDeadline() : isMessageLoopRunning = false;
        }
      }
    }
    var schedulePerformWorkUntilDeadline;
    if ("function" === typeof localSetImmediate)
      schedulePerformWorkUntilDeadline = function() {
        localSetImmediate(performWorkUntilDeadline);
      };
    else if ("undefined" !== typeof MessageChannel) {
      var channel = new MessageChannel(), port = channel.port2;
      channel.port1.onmessage = performWorkUntilDeadline;
      schedulePerformWorkUntilDeadline = function() {
        port.postMessage(null);
      };
    } else
      schedulePerformWorkUntilDeadline = function() {
        localSetTimeout(performWorkUntilDeadline, 0);
      };
    function requestHostTimeout(callback, ms) {
      taskTimeoutID = localSetTimeout(function() {
        callback(exports.unstable_now());
      }, ms);
    }
    exports.unstable_IdlePriority = 5;
    exports.unstable_ImmediatePriority = 1;
    exports.unstable_LowPriority = 4;
    exports.unstable_NormalPriority = 3;
    exports.unstable_Profiling = null;
    exports.unstable_UserBlockingPriority = 2;
    exports.unstable_cancelCallback = function(task) {
      task.callback = null;
    };
    exports.unstable_forceFrameRate = function(fps) {
      0 > fps || 125 < fps ? console.error(
        "forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"
      ) : frameInterval = 0 < fps ? Math.floor(1e3 / fps) : 5;
    };
    exports.unstable_getCurrentPriorityLevel = function() {
      return currentPriorityLevel;
    };
    exports.unstable_next = function(eventHandler) {
      switch (currentPriorityLevel) {
        case 1:
        case 2:
        case 3:
          var priorityLevel = 3;
          break;
        default:
          priorityLevel = currentPriorityLevel;
      }
      var previousPriorityLevel = currentPriorityLevel;
      currentPriorityLevel = priorityLevel;
      try {
        return eventHandler();
      } finally {
        currentPriorityLevel = previousPriorityLevel;
      }
    };
    exports.unstable_requestPaint = function() {
      needsPaint = true;
    };
    exports.unstable_runWithPriority = function(priorityLevel, eventHandler) {
      switch (priorityLevel) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
          break;
        default:
          priorityLevel = 3;
      }
      var previousPriorityLevel = currentPriorityLevel;
      currentPriorityLevel = priorityLevel;
      try {
        return eventHandler();
      } finally {
        currentPriorityLevel = previousPriorityLevel;
      }
    };
    exports.unstable_scheduleCallback = function(priorityLevel, callback, options) {
      var currentTime = exports.unstable_now();
      "object" === typeof options && null !== options ? (options = options.delay, options = "number" === typeof options && 0 < options ? currentTime + options : currentTime) : options = currentTime;
      switch (priorityLevel) {
        case 1:
          var timeout = -1;
          break;
        case 2:
          timeout = 250;
          break;
        case 5:
          timeout = 1073741823;
          break;
        case 4:
          timeout = 1e4;
          break;
        default:
          timeout = 5e3;
      }
      timeout = options + timeout;
      priorityLevel = {
        id: taskIdCounter++,
        callback,
        priorityLevel,
        startTime: options,
        expirationTime: timeout,
        sortIndex: -1
      };
      options > currentTime ? (priorityLevel.sortIndex = options, push(timerQueue, priorityLevel), null === peek(taskQueue) && priorityLevel === peek(timerQueue) && (isHostTimeoutScheduled ? (localClearTimeout(taskTimeoutID), taskTimeoutID = -1) : isHostTimeoutScheduled = true, requestHostTimeout(handleTimeout, options - currentTime))) : (priorityLevel.sortIndex = timeout, push(taskQueue, priorityLevel), isHostCallbackScheduled || isPerformingWork || (isHostCallbackScheduled = true, isMessageLoopRunning || (isMessageLoopRunning = true, schedulePerformWorkUntilDeadline())));
      return priorityLevel;
    };
    exports.unstable_shouldYield = shouldYieldToHost;
    exports.unstable_wrapCallback = function(callback) {
      var parentPriorityLevel = currentPriorityLevel;
      return function() {
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = parentPriorityLevel;
        try {
          return callback.apply(this, arguments);
        } finally {
          currentPriorityLevel = previousPriorityLevel;
        }
      };
    };
  })(scheduler_production);
  return scheduler_production;
}
var hasRequiredScheduler;
function requireScheduler() {
  if (hasRequiredScheduler) return scheduler.exports;
  hasRequiredScheduler = 1;
  {
    scheduler.exports = requireScheduler_production();
  }
  return scheduler.exports;
}
var reactDom = { exports: {} };
var reactDom_production = {};
var hasRequiredReactDom_production;
function requireReactDom_production() {
  if (hasRequiredReactDom_production) return reactDom_production;
  hasRequiredReactDom_production = 1;
  var React = requireReact();
  function formatProdErrorMessage(code) {
    var url = "https://react.dev/errors/" + code;
    if (1 < arguments.length) {
      url += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var i = 2; i < arguments.length; i++)
        url += "&args[]=" + encodeURIComponent(arguments[i]);
    }
    return "Minified React error #" + code + "; visit " + url + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function noop() {
  }
  var Internals = {
    d: {
      f: noop,
      r: function() {
        throw Error(formatProdErrorMessage(522));
      },
      D: noop,
      C: noop,
      L: noop,
      m: noop,
      X: noop,
      S: noop,
      M: noop
    },
    p: 0,
    findDOMNode: null
  }, REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
  function createPortal$1(children, containerInfo, implementation) {
    var key = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null;
    return {
      $$typeof: REACT_PORTAL_TYPE,
      key: null == key ? null : "" + key,
      children,
      containerInfo,
      implementation
    };
  }
  var ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
  function getCrossOriginStringAs(as, input) {
    if ("font" === as) return "";
    if ("string" === typeof input)
      return "use-credentials" === input ? input : "";
  }
  reactDom_production.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = Internals;
  reactDom_production.createPortal = function(children, container) {
    var key = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null;
    if (!container || 1 !== container.nodeType && 9 !== container.nodeType && 11 !== container.nodeType)
      throw Error(formatProdErrorMessage(299));
    return createPortal$1(children, container, null, key);
  };
  reactDom_production.flushSync = function(fn) {
    var previousTransition = ReactSharedInternals.T, previousUpdatePriority = Internals.p;
    try {
      if (ReactSharedInternals.T = null, Internals.p = 2, fn) return fn();
    } finally {
      ReactSharedInternals.T = previousTransition, Internals.p = previousUpdatePriority, Internals.d.f();
    }
  };
  reactDom_production.preconnect = function(href, options) {
    "string" === typeof href && (options ? (options = options.crossOrigin, options = "string" === typeof options ? "use-credentials" === options ? options : "" : void 0) : options = null, Internals.d.C(href, options));
  };
  reactDom_production.prefetchDNS = function(href) {
    "string" === typeof href && Internals.d.D(href);
  };
  reactDom_production.preinit = function(href, options) {
    if ("string" === typeof href && options && "string" === typeof options.as) {
      var as = options.as, crossOrigin = getCrossOriginStringAs(as, options.crossOrigin), integrity = "string" === typeof options.integrity ? options.integrity : void 0, fetchPriority = "string" === typeof options.fetchPriority ? options.fetchPriority : void 0;
      "style" === as ? Internals.d.S(
        href,
        "string" === typeof options.precedence ? options.precedence : void 0,
        {
          crossOrigin,
          integrity,
          fetchPriority
        }
      ) : "script" === as && Internals.d.X(href, {
        crossOrigin,
        integrity,
        fetchPriority,
        nonce: "string" === typeof options.nonce ? options.nonce : void 0
      });
    }
  };
  reactDom_production.preinitModule = function(href, options) {
    if ("string" === typeof href)
      if ("object" === typeof options && null !== options) {
        if (null == options.as || "script" === options.as) {
          var crossOrigin = getCrossOriginStringAs(
            options.as,
            options.crossOrigin
          );
          Internals.d.M(href, {
            crossOrigin,
            integrity: "string" === typeof options.integrity ? options.integrity : void 0,
            nonce: "string" === typeof options.nonce ? options.nonce : void 0
          });
        }
      } else null == options && Internals.d.M(href);
  };
  reactDom_production.preload = function(href, options) {
    if ("string" === typeof href && "object" === typeof options && null !== options && "string" === typeof options.as) {
      var as = options.as, crossOrigin = getCrossOriginStringAs(as, options.crossOrigin);
      Internals.d.L(href, as, {
        crossOrigin,
        integrity: "string" === typeof options.integrity ? options.integrity : void 0,
        nonce: "string" === typeof options.nonce ? options.nonce : void 0,
        type: "string" === typeof options.type ? options.type : void 0,
        fetchPriority: "string" === typeof options.fetchPriority ? options.fetchPriority : void 0,
        referrerPolicy: "string" === typeof options.referrerPolicy ? options.referrerPolicy : void 0,
        imageSrcSet: "string" === typeof options.imageSrcSet ? options.imageSrcSet : void 0,
        imageSizes: "string" === typeof options.imageSizes ? options.imageSizes : void 0,
        media: "string" === typeof options.media ? options.media : void 0
      });
    }
  };
  reactDom_production.preloadModule = function(href, options) {
    if ("string" === typeof href)
      if (options) {
        var crossOrigin = getCrossOriginStringAs(options.as, options.crossOrigin);
        Internals.d.m(href, {
          as: "string" === typeof options.as && "script" !== options.as ? options.as : void 0,
          crossOrigin,
          integrity: "string" === typeof options.integrity ? options.integrity : void 0
        });
      } else Internals.d.m(href);
  };
  reactDom_production.requestFormReset = function(form) {
    Internals.d.r(form);
  };
  reactDom_production.unstable_batchedUpdates = function(fn, a) {
    return fn(a);
  };
  reactDom_production.useFormState = function(action, initialState, permalink) {
    return ReactSharedInternals.H.useFormState(action, initialState, permalink);
  };
  reactDom_production.useFormStatus = function() {
    return ReactSharedInternals.H.useHostTransitionStatus();
  };
  reactDom_production.version = "19.2.7";
  return reactDom_production;
}
var hasRequiredReactDom;
function requireReactDom() {
  if (hasRequiredReactDom) return reactDom.exports;
  hasRequiredReactDom = 1;
  function checkDCE() {
    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
      return;
    }
    try {
      __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
    } catch (err) {
      console.error(err);
    }
  }
  {
    checkDCE();
    reactDom.exports = requireReactDom_production();
  }
  return reactDom.exports;
}
var hasRequiredReactDomClient_production;
function requireReactDomClient_production() {
  if (hasRequiredReactDomClient_production) return reactDomClient_production;
  hasRequiredReactDomClient_production = 1;
  var Scheduler = requireScheduler(), React = requireReact(), ReactDOM = requireReactDom();
  function formatProdErrorMessage(code) {
    var url = "https://react.dev/errors/" + code;
    if (1 < arguments.length) {
      url += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var i = 2; i < arguments.length; i++)
        url += "&args[]=" + encodeURIComponent(arguments[i]);
    }
    return "Minified React error #" + code + "; visit " + url + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function isValidContainer(node) {
    return !(!node || 1 !== node.nodeType && 9 !== node.nodeType && 11 !== node.nodeType);
  }
  function getNearestMountedFiber(fiber) {
    var node = fiber, nearestMounted = fiber;
    if (fiber.alternate) for (; node.return; ) node = node.return;
    else {
      fiber = node;
      do
        node = fiber, 0 !== (node.flags & 4098) && (nearestMounted = node.return), fiber = node.return;
      while (fiber);
    }
    return 3 === node.tag ? nearestMounted : null;
  }
  function getSuspenseInstanceFromFiber(fiber) {
    if (13 === fiber.tag) {
      var suspenseState = fiber.memoizedState;
      null === suspenseState && (fiber = fiber.alternate, null !== fiber && (suspenseState = fiber.memoizedState));
      if (null !== suspenseState) return suspenseState.dehydrated;
    }
    return null;
  }
  function getActivityInstanceFromFiber(fiber) {
    if (31 === fiber.tag) {
      var activityState = fiber.memoizedState;
      null === activityState && (fiber = fiber.alternate, null !== fiber && (activityState = fiber.memoizedState));
      if (null !== activityState) return activityState.dehydrated;
    }
    return null;
  }
  function assertIsMounted(fiber) {
    if (getNearestMountedFiber(fiber) !== fiber)
      throw Error(formatProdErrorMessage(188));
  }
  function findCurrentFiberUsingSlowPath(fiber) {
    var alternate = fiber.alternate;
    if (!alternate) {
      alternate = getNearestMountedFiber(fiber);
      if (null === alternate) throw Error(formatProdErrorMessage(188));
      return alternate !== fiber ? null : fiber;
    }
    for (var a = fiber, b = alternate; ; ) {
      var parentA = a.return;
      if (null === parentA) break;
      var parentB = parentA.alternate;
      if (null === parentB) {
        b = parentA.return;
        if (null !== b) {
          a = b;
          continue;
        }
        break;
      }
      if (parentA.child === parentB.child) {
        for (parentB = parentA.child; parentB; ) {
          if (parentB === a) return assertIsMounted(parentA), fiber;
          if (parentB === b) return assertIsMounted(parentA), alternate;
          parentB = parentB.sibling;
        }
        throw Error(formatProdErrorMessage(188));
      }
      if (a.return !== b.return) a = parentA, b = parentB;
      else {
        for (var didFindChild = false, child$0 = parentA.child; child$0; ) {
          if (child$0 === a) {
            didFindChild = true;
            a = parentA;
            b = parentB;
            break;
          }
          if (child$0 === b) {
            didFindChild = true;
            b = parentA;
            a = parentB;
            break;
          }
          child$0 = child$0.sibling;
        }
        if (!didFindChild) {
          for (child$0 = parentB.child; child$0; ) {
            if (child$0 === a) {
              didFindChild = true;
              a = parentB;
              b = parentA;
              break;
            }
            if (child$0 === b) {
              didFindChild = true;
              b = parentB;
              a = parentA;
              break;
            }
            child$0 = child$0.sibling;
          }
          if (!didFindChild) throw Error(formatProdErrorMessage(189));
        }
      }
      if (a.alternate !== b) throw Error(formatProdErrorMessage(190));
    }
    if (3 !== a.tag) throw Error(formatProdErrorMessage(188));
    return a.stateNode.current === a ? fiber : alternate;
  }
  function findCurrentHostFiberImpl(node) {
    var tag = node.tag;
    if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return node;
    for (node = node.child; null !== node; ) {
      tag = findCurrentHostFiberImpl(node);
      if (null !== tag) return tag;
      node = node.sibling;
    }
    return null;
  }
  var assign = Object.assign, REACT_LEGACY_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.element"), REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = /* @__PURE__ */ Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo"), REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy");
  var REACT_ACTIVITY_TYPE = /* @__PURE__ */ Symbol.for("react.activity");
  var REACT_MEMO_CACHE_SENTINEL = /* @__PURE__ */ Symbol.for("react.memo_cache_sentinel");
  var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
  function getIteratorFn(maybeIterable) {
    if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
    maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
    return "function" === typeof maybeIterable ? maybeIterable : null;
  }
  var REACT_CLIENT_REFERENCE = /* @__PURE__ */ Symbol.for("react.client.reference");
  function getComponentNameFromType(type) {
    if (null == type) return null;
    if ("function" === typeof type)
      return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
    if ("string" === typeof type) return type;
    switch (type) {
      case REACT_FRAGMENT_TYPE:
        return "Fragment";
      case REACT_PROFILER_TYPE:
        return "Profiler";
      case REACT_STRICT_MODE_TYPE:
        return "StrictMode";
      case REACT_SUSPENSE_TYPE:
        return "Suspense";
      case REACT_SUSPENSE_LIST_TYPE:
        return "SuspenseList";
      case REACT_ACTIVITY_TYPE:
        return "Activity";
    }
    if ("object" === typeof type)
      switch (type.$$typeof) {
        case REACT_PORTAL_TYPE:
          return "Portal";
        case REACT_CONTEXT_TYPE:
          return type.displayName || "Context";
        case REACT_CONSUMER_TYPE:
          return (type._context.displayName || "Context") + ".Consumer";
        case REACT_FORWARD_REF_TYPE:
          var innerType = type.render;
          type = type.displayName;
          type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
          return type;
        case REACT_MEMO_TYPE:
          return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
        case REACT_LAZY_TYPE:
          innerType = type._payload;
          type = type._init;
          try {
            return getComponentNameFromType(type(innerType));
          } catch (x) {
          }
      }
    return null;
  }
  var isArrayImpl = Array.isArray, ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, ReactDOMSharedInternals = ReactDOM.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, sharedNotPendingObject = {
    pending: false,
    data: null,
    method: null,
    action: null
  }, valueStack = [], index = -1;
  function createCursor(defaultValue) {
    return { current: defaultValue };
  }
  function pop(cursor) {
    0 > index || (cursor.current = valueStack[index], valueStack[index] = null, index--);
  }
  function push(cursor, value) {
    index++;
    valueStack[index] = cursor.current;
    cursor.current = value;
  }
  var contextStackCursor = createCursor(null), contextFiberStackCursor = createCursor(null), rootInstanceStackCursor = createCursor(null), hostTransitionProviderCursor = createCursor(null);
  function pushHostContainer(fiber, nextRootInstance) {
    push(rootInstanceStackCursor, nextRootInstance);
    push(contextFiberStackCursor, fiber);
    push(contextStackCursor, null);
    switch (nextRootInstance.nodeType) {
      case 9:
      case 11:
        fiber = (fiber = nextRootInstance.documentElement) ? (fiber = fiber.namespaceURI) ? getOwnHostContext(fiber) : 0 : 0;
        break;
      default:
        if (fiber = nextRootInstance.tagName, nextRootInstance = nextRootInstance.namespaceURI)
          nextRootInstance = getOwnHostContext(nextRootInstance), fiber = getChildHostContextProd(nextRootInstance, fiber);
        else
          switch (fiber) {
            case "svg":
              fiber = 1;
              break;
            case "math":
              fiber = 2;
              break;
            default:
              fiber = 0;
          }
    }
    pop(contextStackCursor);
    push(contextStackCursor, fiber);
  }
  function popHostContainer() {
    pop(contextStackCursor);
    pop(contextFiberStackCursor);
    pop(rootInstanceStackCursor);
  }
  function pushHostContext(fiber) {
    null !== fiber.memoizedState && push(hostTransitionProviderCursor, fiber);
    var context = contextStackCursor.current;
    var JSCompiler_inline_result = getChildHostContextProd(context, fiber.type);
    context !== JSCompiler_inline_result && (push(contextFiberStackCursor, fiber), push(contextStackCursor, JSCompiler_inline_result));
  }
  function popHostContext(fiber) {
    contextFiberStackCursor.current === fiber && (pop(contextStackCursor), pop(contextFiberStackCursor));
    hostTransitionProviderCursor.current === fiber && (pop(hostTransitionProviderCursor), HostTransitionContext._currentValue = sharedNotPendingObject);
  }
  var prefix, suffix;
  function describeBuiltInComponentFrame(name) {
    if (void 0 === prefix)
      try {
        throw Error();
      } catch (x) {
        var match = x.stack.trim().match(/\n( *(at )?)/);
        prefix = match && match[1] || "";
        suffix = -1 < x.stack.indexOf("\n    at") ? " (<anonymous>)" : -1 < x.stack.indexOf("@") ? "@unknown:0:0" : "";
      }
    return "\n" + prefix + name + suffix;
  }
  var reentry = false;
  function describeNativeComponentFrame(fn, construct) {
    if (!fn || reentry) return "";
    reentry = true;
    var previousPrepareStackTrace = Error.prepareStackTrace;
    Error.prepareStackTrace = void 0;
    try {
      var RunInRootFrame = {
        DetermineComponentFrameRoot: function() {
          try {
            if (construct) {
              var Fake = function() {
                throw Error();
              };
              Object.defineProperty(Fake.prototype, "props", {
                set: function() {
                  throw Error();
                }
              });
              if ("object" === typeof Reflect && Reflect.construct) {
                try {
                  Reflect.construct(Fake, []);
                } catch (x) {
                  var control = x;
                }
                Reflect.construct(fn, [], Fake);
              } else {
                try {
                  Fake.call();
                } catch (x$1) {
                  control = x$1;
                }
                fn.call(Fake.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (x$2) {
                control = x$2;
              }
              (Fake = fn()) && "function" === typeof Fake.catch && Fake.catch(function() {
              });
            }
          } catch (sample) {
            if (sample && control && "string" === typeof sample.stack)
              return [sample.stack, control.stack];
          }
          return [null, null];
        }
      };
      RunInRootFrame.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
      var namePropDescriptor = Object.getOwnPropertyDescriptor(
        RunInRootFrame.DetermineComponentFrameRoot,
        "name"
      );
      namePropDescriptor && namePropDescriptor.configurable && Object.defineProperty(
        RunInRootFrame.DetermineComponentFrameRoot,
        "name",
        { value: "DetermineComponentFrameRoot" }
      );
      var _RunInRootFrame$Deter = RunInRootFrame.DetermineComponentFrameRoot(), sampleStack = _RunInRootFrame$Deter[0], controlStack = _RunInRootFrame$Deter[1];
      if (sampleStack && controlStack) {
        var sampleLines = sampleStack.split("\n"), controlLines = controlStack.split("\n");
        for (namePropDescriptor = RunInRootFrame = 0; RunInRootFrame < sampleLines.length && !sampleLines[RunInRootFrame].includes("DetermineComponentFrameRoot"); )
          RunInRootFrame++;
        for (; namePropDescriptor < controlLines.length && !controlLines[namePropDescriptor].includes(
          "DetermineComponentFrameRoot"
        ); )
          namePropDescriptor++;
        if (RunInRootFrame === sampleLines.length || namePropDescriptor === controlLines.length)
          for (RunInRootFrame = sampleLines.length - 1, namePropDescriptor = controlLines.length - 1; 1 <= RunInRootFrame && 0 <= namePropDescriptor && sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]; )
            namePropDescriptor--;
        for (; 1 <= RunInRootFrame && 0 <= namePropDescriptor; RunInRootFrame--, namePropDescriptor--)
          if (sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]) {
            if (1 !== RunInRootFrame || 1 !== namePropDescriptor) {
              do
                if (RunInRootFrame--, namePropDescriptor--, 0 > namePropDescriptor || sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]) {
                  var frame = "\n" + sampleLines[RunInRootFrame].replace(" at new ", " at ");
                  fn.displayName && frame.includes("<anonymous>") && (frame = frame.replace("<anonymous>", fn.displayName));
                  return frame;
                }
              while (1 <= RunInRootFrame && 0 <= namePropDescriptor);
            }
            break;
          }
      }
    } finally {
      reentry = false, Error.prepareStackTrace = previousPrepareStackTrace;
    }
    return (previousPrepareStackTrace = fn ? fn.displayName || fn.name : "") ? describeBuiltInComponentFrame(previousPrepareStackTrace) : "";
  }
  function describeFiber(fiber, childFiber) {
    switch (fiber.tag) {
      case 26:
      case 27:
      case 5:
        return describeBuiltInComponentFrame(fiber.type);
      case 16:
        return describeBuiltInComponentFrame("Lazy");
      case 13:
        return fiber.child !== childFiber && null !== childFiber ? describeBuiltInComponentFrame("Suspense Fallback") : describeBuiltInComponentFrame("Suspense");
      case 19:
        return describeBuiltInComponentFrame("SuspenseList");
      case 0:
      case 15:
        return describeNativeComponentFrame(fiber.type, false);
      case 11:
        return describeNativeComponentFrame(fiber.type.render, false);
      case 1:
        return describeNativeComponentFrame(fiber.type, true);
      case 31:
        return describeBuiltInComponentFrame("Activity");
      default:
        return "";
    }
  }
  function getStackByFiberInDevAndProd(workInProgress2) {
    try {
      var info = "", previous = null;
      do
        info += describeFiber(workInProgress2, previous), previous = workInProgress2, workInProgress2 = workInProgress2.return;
      while (workInProgress2);
      return info;
    } catch (x) {
      return "\nError generating stack: " + x.message + "\n" + x.stack;
    }
  }
  var hasOwnProperty = Object.prototype.hasOwnProperty, scheduleCallback$3 = Scheduler.unstable_scheduleCallback, cancelCallback$1 = Scheduler.unstable_cancelCallback, shouldYield = Scheduler.unstable_shouldYield, requestPaint = Scheduler.unstable_requestPaint, now = Scheduler.unstable_now, getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel, ImmediatePriority = Scheduler.unstable_ImmediatePriority, UserBlockingPriority = Scheduler.unstable_UserBlockingPriority, NormalPriority$1 = Scheduler.unstable_NormalPriority, LowPriority = Scheduler.unstable_LowPriority, IdlePriority = Scheduler.unstable_IdlePriority, log$1 = Scheduler.log, unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue, rendererID = null, injectedHook = null;
  function setIsStrictModeForDevtools(newIsStrictMode) {
    "function" === typeof log$1 && unstable_setDisableYieldValue(newIsStrictMode);
    if (injectedHook && "function" === typeof injectedHook.setStrictMode)
      try {
        injectedHook.setStrictMode(rendererID, newIsStrictMode);
      } catch (err) {
      }
  }
  var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback, log = Math.log, LN2 = Math.LN2;
  function clz32Fallback(x) {
    x >>>= 0;
    return 0 === x ? 32 : 31 - (log(x) / LN2 | 0) | 0;
  }
  var nextTransitionUpdateLane = 256, nextTransitionDeferredLane = 262144, nextRetryLane = 4194304;
  function getHighestPriorityLanes(lanes) {
    var pendingSyncLanes = lanes & 42;
    if (0 !== pendingSyncLanes) return pendingSyncLanes;
    switch (lanes & -lanes) {
      case 1:
        return 1;
      case 2:
        return 2;
      case 4:
        return 4;
      case 8:
        return 8;
      case 16:
        return 16;
      case 32:
        return 32;
      case 64:
        return 64;
      case 128:
        return 128;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
        return lanes & 261888;
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return lanes & 3932160;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return lanes & 62914560;
      case 67108864:
        return 67108864;
      case 134217728:
        return 134217728;
      case 268435456:
        return 268435456;
      case 536870912:
        return 536870912;
      case 1073741824:
        return 0;
      default:
        return lanes;
    }
  }
  function getNextLanes(root3, wipLanes, rootHasPendingCommit) {
    var pendingLanes = root3.pendingLanes;
    if (0 === pendingLanes) return 0;
    var nextLanes = 0, suspendedLanes = root3.suspendedLanes, pingedLanes = root3.pingedLanes;
    root3 = root3.warmLanes;
    var nonIdlePendingLanes = pendingLanes & 134217727;
    0 !== nonIdlePendingLanes ? (pendingLanes = nonIdlePendingLanes & ~suspendedLanes, 0 !== pendingLanes ? nextLanes = getHighestPriorityLanes(pendingLanes) : (pingedLanes &= nonIdlePendingLanes, 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = nonIdlePendingLanes & ~root3, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))))) : (nonIdlePendingLanes = pendingLanes & ~suspendedLanes, 0 !== nonIdlePendingLanes ? nextLanes = getHighestPriorityLanes(nonIdlePendingLanes) : 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = pendingLanes & ~root3, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))));
    return 0 === nextLanes ? 0 : 0 !== wipLanes && wipLanes !== nextLanes && 0 === (wipLanes & suspendedLanes) && (suspendedLanes = nextLanes & -nextLanes, rootHasPendingCommit = wipLanes & -wipLanes, suspendedLanes >= rootHasPendingCommit || 32 === suspendedLanes && 0 !== (rootHasPendingCommit & 4194048)) ? wipLanes : nextLanes;
  }
  function checkIfRootIsPrerendering(root3, renderLanes2) {
    return 0 === (root3.pendingLanes & ~(root3.suspendedLanes & ~root3.pingedLanes) & renderLanes2);
  }
  function computeExpirationTime(lane, currentTime) {
    switch (lane) {
      case 1:
      case 2:
      case 4:
      case 8:
      case 64:
        return currentTime + 250;
      case 16:
      case 32:
      case 128:
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return currentTime + 5e3;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return -1;
      case 67108864:
      case 134217728:
      case 268435456:
      case 536870912:
      case 1073741824:
        return -1;
      default:
        return -1;
    }
  }
  function claimNextRetryLane() {
    var lane = nextRetryLane;
    nextRetryLane <<= 1;
    0 === (nextRetryLane & 62914560) && (nextRetryLane = 4194304);
    return lane;
  }
  function createLaneMap(initial) {
    for (var laneMap = [], i = 0; 31 > i; i++) laneMap.push(initial);
    return laneMap;
  }
  function markRootUpdated$1(root3, updateLane) {
    root3.pendingLanes |= updateLane;
    268435456 !== updateLane && (root3.suspendedLanes = 0, root3.pingedLanes = 0, root3.warmLanes = 0);
  }
  function markRootFinished(root3, finishedLanes, remainingLanes, spawnedLane, updatedLanes, suspendedRetryLanes) {
    var previouslyPendingLanes = root3.pendingLanes;
    root3.pendingLanes = remainingLanes;
    root3.suspendedLanes = 0;
    root3.pingedLanes = 0;
    root3.warmLanes = 0;
    root3.expiredLanes &= remainingLanes;
    root3.entangledLanes &= remainingLanes;
    root3.errorRecoveryDisabledLanes &= remainingLanes;
    root3.shellSuspendCounter = 0;
    var entanglements = root3.entanglements, expirationTimes = root3.expirationTimes, hiddenUpdates = root3.hiddenUpdates;
    for (remainingLanes = previouslyPendingLanes & ~remainingLanes; 0 < remainingLanes; ) {
      var index$7 = 31 - clz32(remainingLanes), lane = 1 << index$7;
      entanglements[index$7] = 0;
      expirationTimes[index$7] = -1;
      var hiddenUpdatesForLane = hiddenUpdates[index$7];
      if (null !== hiddenUpdatesForLane)
        for (hiddenUpdates[index$7] = null, index$7 = 0; index$7 < hiddenUpdatesForLane.length; index$7++) {
          var update = hiddenUpdatesForLane[index$7];
          null !== update && (update.lane &= -536870913);
        }
      remainingLanes &= ~lane;
    }
    0 !== spawnedLane && markSpawnedDeferredLane(root3, spawnedLane, 0);
    0 !== suspendedRetryLanes && 0 === updatedLanes && 0 !== root3.tag && (root3.suspendedLanes |= suspendedRetryLanes & ~(previouslyPendingLanes & ~finishedLanes));
  }
  function markSpawnedDeferredLane(root3, spawnedLane, entangledLanes) {
    root3.pendingLanes |= spawnedLane;
    root3.suspendedLanes &= ~spawnedLane;
    var spawnedLaneIndex = 31 - clz32(spawnedLane);
    root3.entangledLanes |= spawnedLane;
    root3.entanglements[spawnedLaneIndex] = root3.entanglements[spawnedLaneIndex] | 1073741824 | entangledLanes & 261930;
  }
  function markRootEntangled(root3, entangledLanes) {
    var rootEntangledLanes = root3.entangledLanes |= entangledLanes;
    for (root3 = root3.entanglements; rootEntangledLanes; ) {
      var index$8 = 31 - clz32(rootEntangledLanes), lane = 1 << index$8;
      lane & entangledLanes | root3[index$8] & entangledLanes && (root3[index$8] |= entangledLanes);
      rootEntangledLanes &= ~lane;
    }
  }
  function getBumpedLaneForHydration(root3, renderLanes2) {
    var renderLane = renderLanes2 & -renderLanes2;
    renderLane = 0 !== (renderLane & 42) ? 1 : getBumpedLaneForHydrationByLane(renderLane);
    return 0 !== (renderLane & (root3.suspendedLanes | renderLanes2)) ? 0 : renderLane;
  }
  function getBumpedLaneForHydrationByLane(lane) {
    switch (lane) {
      case 2:
        lane = 1;
        break;
      case 8:
        lane = 4;
        break;
      case 32:
        lane = 16;
        break;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        lane = 128;
        break;
      case 268435456:
        lane = 134217728;
        break;
      default:
        lane = 0;
    }
    return lane;
  }
  function lanesToEventPriority(lanes) {
    lanes &= -lanes;
    return 2 < lanes ? 8 < lanes ? 0 !== (lanes & 134217727) ? 32 : 268435456 : 8 : 2;
  }
  function resolveUpdatePriority() {
    var updatePriority = ReactDOMSharedInternals.p;
    if (0 !== updatePriority) return updatePriority;
    updatePriority = window.event;
    return void 0 === updatePriority ? 32 : getEventPriority(updatePriority.type);
  }
  function runWithPriority(priority, fn) {
    var previousPriority = ReactDOMSharedInternals.p;
    try {
      return ReactDOMSharedInternals.p = priority, fn();
    } finally {
      ReactDOMSharedInternals.p = previousPriority;
    }
  }
  var randomKey = Math.random().toString(36).slice(2), internalInstanceKey = "__reactFiber$" + randomKey, internalPropsKey = "__reactProps$" + randomKey, internalContainerInstanceKey = "__reactContainer$" + randomKey, internalEventHandlersKey = "__reactEvents$" + randomKey, internalEventHandlerListenersKey = "__reactListeners$" + randomKey, internalEventHandlesSetKey = "__reactHandles$" + randomKey, internalRootNodeResourcesKey = "__reactResources$" + randomKey, internalHoistableMarker = "__reactMarker$" + randomKey;
  function detachDeletedInstance(node) {
    delete node[internalInstanceKey];
    delete node[internalPropsKey];
    delete node[internalEventHandlersKey];
    delete node[internalEventHandlerListenersKey];
    delete node[internalEventHandlesSetKey];
  }
  function getClosestInstanceFromNode(targetNode) {
    var targetInst = targetNode[internalInstanceKey];
    if (targetInst) return targetInst;
    for (var parentNode = targetNode.parentNode; parentNode; ) {
      if (targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey]) {
        parentNode = targetInst.alternate;
        if (null !== targetInst.child || null !== parentNode && null !== parentNode.child)
          for (targetNode = getParentHydrationBoundary(targetNode); null !== targetNode; ) {
            if (parentNode = targetNode[internalInstanceKey]) return parentNode;
            targetNode = getParentHydrationBoundary(targetNode);
          }
        return targetInst;
      }
      targetNode = parentNode;
      parentNode = targetNode.parentNode;
    }
    return null;
  }
  function getInstanceFromNode(node) {
    if (node = node[internalInstanceKey] || node[internalContainerInstanceKey]) {
      var tag = node.tag;
      if (5 === tag || 6 === tag || 13 === tag || 31 === tag || 26 === tag || 27 === tag || 3 === tag)
        return node;
    }
    return null;
  }
  function getNodeFromInstance(inst) {
    var tag = inst.tag;
    if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return inst.stateNode;
    throw Error(formatProdErrorMessage(33));
  }
  function getResourcesFromRoot(root3) {
    var resources = root3[internalRootNodeResourcesKey];
    resources || (resources = root3[internalRootNodeResourcesKey] = { hoistableStyles: /* @__PURE__ */ new Map(), hoistableScripts: /* @__PURE__ */ new Map() });
    return resources;
  }
  function markNodeAsHoistable(node) {
    node[internalHoistableMarker] = true;
  }
  var allNativeEvents = /* @__PURE__ */ new Set(), registrationNameDependencies = {};
  function registerTwoPhaseEvent(registrationName, dependencies) {
    registerDirectEvent(registrationName, dependencies);
    registerDirectEvent(registrationName + "Capture", dependencies);
  }
  function registerDirectEvent(registrationName, dependencies) {
    registrationNameDependencies[registrationName] = dependencies;
    for (registrationName = 0; registrationName < dependencies.length; registrationName++)
      allNativeEvents.add(dependencies[registrationName]);
  }
  var VALID_ATTRIBUTE_NAME_REGEX = RegExp(
    "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
  ), illegalAttributeNameCache = {}, validatedAttributeNameCache = {};
  function isAttributeNameSafe(attributeName) {
    if (hasOwnProperty.call(validatedAttributeNameCache, attributeName))
      return true;
    if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) return false;
    if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName))
      return validatedAttributeNameCache[attributeName] = true;
    illegalAttributeNameCache[attributeName] = true;
    return false;
  }
  function setValueForAttribute(node, name, value) {
    if (isAttributeNameSafe(name))
      if (null === value) node.removeAttribute(name);
      else {
        switch (typeof value) {
          case "undefined":
          case "function":
          case "symbol":
            node.removeAttribute(name);
            return;
          case "boolean":
            var prefix$10 = name.toLowerCase().slice(0, 5);
            if ("data-" !== prefix$10 && "aria-" !== prefix$10) {
              node.removeAttribute(name);
              return;
            }
        }
        node.setAttribute(name, "" + value);
      }
  }
  function setValueForKnownAttribute(node, name, value) {
    if (null === value) node.removeAttribute(name);
    else {
      switch (typeof value) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          node.removeAttribute(name);
          return;
      }
      node.setAttribute(name, "" + value);
    }
  }
  function setValueForNamespacedAttribute(node, namespace, name, value) {
    if (null === value) node.removeAttribute(name);
    else {
      switch (typeof value) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          node.removeAttribute(name);
          return;
      }
      node.setAttributeNS(namespace, name, "" + value);
    }
  }
  function getToStringValue(value) {
    switch (typeof value) {
      case "bigint":
      case "boolean":
      case "number":
      case "string":
      case "undefined":
        return value;
      case "object":
        return value;
      default:
        return "";
    }
  }
  function isCheckable(elem) {
    var type = elem.type;
    return (elem = elem.nodeName) && "input" === elem.toLowerCase() && ("checkbox" === type || "radio" === type);
  }
  function trackValueOnNode(node, valueField, currentValue) {
    var descriptor = Object.getOwnPropertyDescriptor(
      node.constructor.prototype,
      valueField
    );
    if (!node.hasOwnProperty(valueField) && "undefined" !== typeof descriptor && "function" === typeof descriptor.get && "function" === typeof descriptor.set) {
      var get = descriptor.get, set = descriptor.set;
      Object.defineProperty(node, valueField, {
        configurable: true,
        get: function() {
          return get.call(this);
        },
        set: function(value) {
          currentValue = "" + value;
          set.call(this, value);
        }
      });
      Object.defineProperty(node, valueField, {
        enumerable: descriptor.enumerable
      });
      return {
        getValue: function() {
          return currentValue;
        },
        setValue: function(value) {
          currentValue = "" + value;
        },
        stopTracking: function() {
          node._valueTracker = null;
          delete node[valueField];
        }
      };
    }
  }
  function track(node) {
    if (!node._valueTracker) {
      var valueField = isCheckable(node) ? "checked" : "value";
      node._valueTracker = trackValueOnNode(
        node,
        valueField,
        "" + node[valueField]
      );
    }
  }
  function updateValueIfChanged(node) {
    if (!node) return false;
    var tracker = node._valueTracker;
    if (!tracker) return true;
    var lastValue = tracker.getValue();
    var value = "";
    node && (value = isCheckable(node) ? node.checked ? "true" : "false" : node.value);
    node = value;
    return node !== lastValue ? (tracker.setValue(node), true) : false;
  }
  function getActiveElement(doc) {
    doc = doc || ("undefined" !== typeof document ? document : void 0);
    if ("undefined" === typeof doc) return null;
    try {
      return doc.activeElement || doc.body;
    } catch (e) {
      return doc.body;
    }
  }
  var escapeSelectorAttributeValueInsideDoubleQuotesRegex = /[\n"\\]/g;
  function escapeSelectorAttributeValueInsideDoubleQuotes(value) {
    return value.replace(
      escapeSelectorAttributeValueInsideDoubleQuotesRegex,
      function(ch) {
        return "\\" + ch.charCodeAt(0).toString(16) + " ";
      }
    );
  }
  function updateInput(element, value, defaultValue, lastDefaultValue, checked, defaultChecked, type, name) {
    element.name = "";
    null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type ? element.type = type : element.removeAttribute("type");
    if (null != value)
      if ("number" === type) {
        if (0 === value && "" === element.value || element.value != value)
          element.value = "" + getToStringValue(value);
      } else
        element.value !== "" + getToStringValue(value) && (element.value = "" + getToStringValue(value));
    else
      "submit" !== type && "reset" !== type || element.removeAttribute("value");
    null != value ? setDefaultValue(element, type, getToStringValue(value)) : null != defaultValue ? setDefaultValue(element, type, getToStringValue(defaultValue)) : null != lastDefaultValue && element.removeAttribute("value");
    null == checked && null != defaultChecked && (element.defaultChecked = !!defaultChecked);
    null != checked && (element.checked = checked && "function" !== typeof checked && "symbol" !== typeof checked);
    null != name && "function" !== typeof name && "symbol" !== typeof name && "boolean" !== typeof name ? element.name = "" + getToStringValue(name) : element.removeAttribute("name");
  }
  function initInput(element, value, defaultValue, checked, defaultChecked, type, name, isHydrating2) {
    null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type && (element.type = type);
    if (null != value || null != defaultValue) {
      if (!("submit" !== type && "reset" !== type || void 0 !== value && null !== value)) {
        track(element);
        return;
      }
      defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
      value = null != value ? "" + getToStringValue(value) : defaultValue;
      isHydrating2 || value === element.value || (element.value = value);
      element.defaultValue = value;
    }
    checked = null != checked ? checked : defaultChecked;
    checked = "function" !== typeof checked && "symbol" !== typeof checked && !!checked;
    element.checked = isHydrating2 ? element.checked : !!checked;
    element.defaultChecked = !!checked;
    null != name && "function" !== typeof name && "symbol" !== typeof name && "boolean" !== typeof name && (element.name = name);
    track(element);
  }
  function setDefaultValue(node, type, value) {
    "number" === type && getActiveElement(node.ownerDocument) === node || node.defaultValue === "" + value || (node.defaultValue = "" + value);
  }
  function updateOptions(node, multiple, propValue, setDefaultSelected) {
    node = node.options;
    if (multiple) {
      multiple = {};
      for (var i = 0; i < propValue.length; i++)
        multiple["$" + propValue[i]] = true;
      for (propValue = 0; propValue < node.length; propValue++)
        i = multiple.hasOwnProperty("$" + node[propValue].value), node[propValue].selected !== i && (node[propValue].selected = i), i && setDefaultSelected && (node[propValue].defaultSelected = true);
    } else {
      propValue = "" + getToStringValue(propValue);
      multiple = null;
      for (i = 0; i < node.length; i++) {
        if (node[i].value === propValue) {
          node[i].selected = true;
          setDefaultSelected && (node[i].defaultSelected = true);
          return;
        }
        null !== multiple || node[i].disabled || (multiple = node[i]);
      }
      null !== multiple && (multiple.selected = true);
    }
  }
  function updateTextarea(element, value, defaultValue) {
    if (null != value && (value = "" + getToStringValue(value), value !== element.value && (element.value = value), null == defaultValue)) {
      element.defaultValue !== value && (element.defaultValue = value);
      return;
    }
    element.defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
  }
  function initTextarea(element, value, defaultValue, children) {
    if (null == value) {
      if (null != children) {
        if (null != defaultValue) throw Error(formatProdErrorMessage(92));
        if (isArrayImpl(children)) {
          if (1 < children.length) throw Error(formatProdErrorMessage(93));
          children = children[0];
        }
        defaultValue = children;
      }
      null == defaultValue && (defaultValue = "");
      value = defaultValue;
    }
    defaultValue = getToStringValue(value);
    element.defaultValue = defaultValue;
    children = element.textContent;
    children === defaultValue && "" !== children && null !== children && (element.value = children);
    track(element);
  }
  function setTextContent(node, text) {
    if (text) {
      var firstChild = node.firstChild;
      if (firstChild && firstChild === node.lastChild && 3 === firstChild.nodeType) {
        firstChild.nodeValue = text;
        return;
      }
    }
    node.textContent = text;
  }
  var unitlessNumbers = new Set(
    "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
      " "
    )
  );
  function setValueForStyle(style2, styleName, value) {
    var isCustomProperty = 0 === styleName.indexOf("--");
    null == value || "boolean" === typeof value || "" === value ? isCustomProperty ? style2.setProperty(styleName, "") : "float" === styleName ? style2.cssFloat = "" : style2[styleName] = "" : isCustomProperty ? style2.setProperty(styleName, value) : "number" !== typeof value || 0 === value || unitlessNumbers.has(styleName) ? "float" === styleName ? style2.cssFloat = value : style2[styleName] = ("" + value).trim() : style2[styleName] = value + "px";
  }
  function setValueForStyles(node, styles, prevStyles) {
    if (null != styles && "object" !== typeof styles)
      throw Error(formatProdErrorMessage(62));
    node = node.style;
    if (null != prevStyles) {
      for (var styleName in prevStyles)
        !prevStyles.hasOwnProperty(styleName) || null != styles && styles.hasOwnProperty(styleName) || (0 === styleName.indexOf("--") ? node.setProperty(styleName, "") : "float" === styleName ? node.cssFloat = "" : node[styleName] = "");
      for (var styleName$16 in styles)
        styleName = styles[styleName$16], styles.hasOwnProperty(styleName$16) && prevStyles[styleName$16] !== styleName && setValueForStyle(node, styleName$16, styleName);
    } else
      for (var styleName$17 in styles)
        styles.hasOwnProperty(styleName$17) && setValueForStyle(node, styleName$17, styles[styleName$17]);
  }
  function isCustomElement(tagName) {
    if (-1 === tagName.indexOf("-")) return false;
    switch (tagName) {
      case "annotation-xml":
      case "color-profile":
      case "font-face":
      case "font-face-src":
      case "font-face-uri":
      case "font-face-format":
      case "font-face-name":
      case "missing-glyph":
        return false;
      default:
        return true;
    }
  }
  var aliases = /* @__PURE__ */ new Map([
    ["acceptCharset", "accept-charset"],
    ["htmlFor", "for"],
    ["httpEquiv", "http-equiv"],
    ["crossOrigin", "crossorigin"],
    ["accentHeight", "accent-height"],
    ["alignmentBaseline", "alignment-baseline"],
    ["arabicForm", "arabic-form"],
    ["baselineShift", "baseline-shift"],
    ["capHeight", "cap-height"],
    ["clipPath", "clip-path"],
    ["clipRule", "clip-rule"],
    ["colorInterpolation", "color-interpolation"],
    ["colorInterpolationFilters", "color-interpolation-filters"],
    ["colorProfile", "color-profile"],
    ["colorRendering", "color-rendering"],
    ["dominantBaseline", "dominant-baseline"],
    ["enableBackground", "enable-background"],
    ["fillOpacity", "fill-opacity"],
    ["fillRule", "fill-rule"],
    ["floodColor", "flood-color"],
    ["floodOpacity", "flood-opacity"],
    ["fontFamily", "font-family"],
    ["fontSize", "font-size"],
    ["fontSizeAdjust", "font-size-adjust"],
    ["fontStretch", "font-stretch"],
    ["fontStyle", "font-style"],
    ["fontVariant", "font-variant"],
    ["fontWeight", "font-weight"],
    ["glyphName", "glyph-name"],
    ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
    ["glyphOrientationVertical", "glyph-orientation-vertical"],
    ["horizAdvX", "horiz-adv-x"],
    ["horizOriginX", "horiz-origin-x"],
    ["imageRendering", "image-rendering"],
    ["letterSpacing", "letter-spacing"],
    ["lightingColor", "lighting-color"],
    ["markerEnd", "marker-end"],
    ["markerMid", "marker-mid"],
    ["markerStart", "marker-start"],
    ["overlinePosition", "overline-position"],
    ["overlineThickness", "overline-thickness"],
    ["paintOrder", "paint-order"],
    ["panose-1", "panose-1"],
    ["pointerEvents", "pointer-events"],
    ["renderingIntent", "rendering-intent"],
    ["shapeRendering", "shape-rendering"],
    ["stopColor", "stop-color"],
    ["stopOpacity", "stop-opacity"],
    ["strikethroughPosition", "strikethrough-position"],
    ["strikethroughThickness", "strikethrough-thickness"],
    ["strokeDasharray", "stroke-dasharray"],
    ["strokeDashoffset", "stroke-dashoffset"],
    ["strokeLinecap", "stroke-linecap"],
    ["strokeLinejoin", "stroke-linejoin"],
    ["strokeMiterlimit", "stroke-miterlimit"],
    ["strokeOpacity", "stroke-opacity"],
    ["strokeWidth", "stroke-width"],
    ["textAnchor", "text-anchor"],
    ["textDecoration", "text-decoration"],
    ["textRendering", "text-rendering"],
    ["transformOrigin", "transform-origin"],
    ["underlinePosition", "underline-position"],
    ["underlineThickness", "underline-thickness"],
    ["unicodeBidi", "unicode-bidi"],
    ["unicodeRange", "unicode-range"],
    ["unitsPerEm", "units-per-em"],
    ["vAlphabetic", "v-alphabetic"],
    ["vHanging", "v-hanging"],
    ["vIdeographic", "v-ideographic"],
    ["vMathematical", "v-mathematical"],
    ["vectorEffect", "vector-effect"],
    ["vertAdvY", "vert-adv-y"],
    ["vertOriginX", "vert-origin-x"],
    ["vertOriginY", "vert-origin-y"],
    ["wordSpacing", "word-spacing"],
    ["writingMode", "writing-mode"],
    ["xmlnsXlink", "xmlns:xlink"],
    ["xHeight", "x-height"]
  ]), isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
  function sanitizeURL(url) {
    return isJavaScriptProtocol.test("" + url) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : url;
  }
  function noop$1() {
  }
  var currentReplayingEvent = null;
  function getEventTarget(nativeEvent) {
    nativeEvent = nativeEvent.target || nativeEvent.srcElement || window;
    nativeEvent.correspondingUseElement && (nativeEvent = nativeEvent.correspondingUseElement);
    return 3 === nativeEvent.nodeType ? nativeEvent.parentNode : nativeEvent;
  }
  var restoreTarget = null, restoreQueue = null;
  function restoreStateOfTarget(target) {
    var internalInstance = getInstanceFromNode(target);
    if (internalInstance && (target = internalInstance.stateNode)) {
      var props = target[internalPropsKey] || null;
      a: switch (target = internalInstance.stateNode, internalInstance.type) {
        case "input":
          updateInput(
            target,
            props.value,
            props.defaultValue,
            props.defaultValue,
            props.checked,
            props.defaultChecked,
            props.type,
            props.name
          );
          internalInstance = props.name;
          if ("radio" === props.type && null != internalInstance) {
            for (props = target; props.parentNode; ) props = props.parentNode;
            props = props.querySelectorAll(
              'input[name="' + escapeSelectorAttributeValueInsideDoubleQuotes(
                "" + internalInstance
              ) + '"][type="radio"]'
            );
            for (internalInstance = 0; internalInstance < props.length; internalInstance++) {
              var otherNode = props[internalInstance];
              if (otherNode !== target && otherNode.form === target.form) {
                var otherProps = otherNode[internalPropsKey] || null;
                if (!otherProps) throw Error(formatProdErrorMessage(90));
                updateInput(
                  otherNode,
                  otherProps.value,
                  otherProps.defaultValue,
                  otherProps.defaultValue,
                  otherProps.checked,
                  otherProps.defaultChecked,
                  otherProps.type,
                  otherProps.name
                );
              }
            }
            for (internalInstance = 0; internalInstance < props.length; internalInstance++)
              otherNode = props[internalInstance], otherNode.form === target.form && updateValueIfChanged(otherNode);
          }
          break a;
        case "textarea":
          updateTextarea(target, props.value, props.defaultValue);
          break a;
        case "select":
          internalInstance = props.value, null != internalInstance && updateOptions(target, !!props.multiple, internalInstance, false);
      }
    }
  }
  var isInsideEventHandler = false;
  function batchedUpdates$1(fn, a, b) {
    if (isInsideEventHandler) return fn(a, b);
    isInsideEventHandler = true;
    try {
      var JSCompiler_inline_result = fn(a);
      return JSCompiler_inline_result;
    } finally {
      if (isInsideEventHandler = false, null !== restoreTarget || null !== restoreQueue) {
        if (flushSyncWork$1(), restoreTarget && (a = restoreTarget, fn = restoreQueue, restoreQueue = restoreTarget = null, restoreStateOfTarget(a), fn))
          for (a = 0; a < fn.length; a++) restoreStateOfTarget(fn[a]);
      }
    }
  }
  function getListener(inst, registrationName) {
    var stateNode = inst.stateNode;
    if (null === stateNode) return null;
    var props = stateNode[internalPropsKey] || null;
    if (null === props) return null;
    stateNode = props[registrationName];
    a: switch (registrationName) {
      case "onClick":
      case "onClickCapture":
      case "onDoubleClick":
      case "onDoubleClickCapture":
      case "onMouseDown":
      case "onMouseDownCapture":
      case "onMouseMove":
      case "onMouseMoveCapture":
      case "onMouseUp":
      case "onMouseUpCapture":
      case "onMouseEnter":
        (props = !props.disabled) || (inst = inst.type, props = !("button" === inst || "input" === inst || "select" === inst || "textarea" === inst));
        inst = !props;
        break a;
      default:
        inst = false;
    }
    if (inst) return null;
    if (stateNode && "function" !== typeof stateNode)
      throw Error(
        formatProdErrorMessage(231, registrationName, typeof stateNode)
      );
    return stateNode;
  }
  var canUseDOM = !("undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement), passiveBrowserEventsSupported = false;
  if (canUseDOM)
    try {
      var options = {};
      Object.defineProperty(options, "passive", {
        get: function() {
          passiveBrowserEventsSupported = true;
        }
      });
      window.addEventListener("test", options, options);
      window.removeEventListener("test", options, options);
    } catch (e) {
      passiveBrowserEventsSupported = false;
    }
  var root2 = null, startText = null, fallbackText = null;
  function getData() {
    if (fallbackText) return fallbackText;
    var start, startValue = startText, startLength = startValue.length, end, endValue = "value" in root2 ? root2.value : root2.textContent, endLength = endValue.length;
    for (start = 0; start < startLength && startValue[start] === endValue[start]; start++) ;
    var minEnd = startLength - start;
    for (end = 1; end <= minEnd && startValue[startLength - end] === endValue[endLength - end]; end++) ;
    return fallbackText = endValue.slice(start, 1 < end ? 1 - end : void 0);
  }
  function getEventCharCode(nativeEvent) {
    var keyCode = nativeEvent.keyCode;
    "charCode" in nativeEvent ? (nativeEvent = nativeEvent.charCode, 0 === nativeEvent && 13 === keyCode && (nativeEvent = 13)) : nativeEvent = keyCode;
    10 === nativeEvent && (nativeEvent = 13);
    return 32 <= nativeEvent || 13 === nativeEvent ? nativeEvent : 0;
  }
  function functionThatReturnsTrue() {
    return true;
  }
  function functionThatReturnsFalse() {
    return false;
  }
  function createSyntheticEvent(Interface) {
    function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
      this._reactName = reactName;
      this._targetInst = targetInst;
      this.type = reactEventType;
      this.nativeEvent = nativeEvent;
      this.target = nativeEventTarget;
      this.currentTarget = null;
      for (var propName in Interface)
        Interface.hasOwnProperty(propName) && (reactName = Interface[propName], this[propName] = reactName ? reactName(nativeEvent) : nativeEvent[propName]);
      this.isDefaultPrevented = (null != nativeEvent.defaultPrevented ? nativeEvent.defaultPrevented : false === nativeEvent.returnValue) ? functionThatReturnsTrue : functionThatReturnsFalse;
      this.isPropagationStopped = functionThatReturnsFalse;
      return this;
    }
    assign(SyntheticBaseEvent.prototype, {
      preventDefault: function() {
        this.defaultPrevented = true;
        var event = this.nativeEvent;
        event && (event.preventDefault ? event.preventDefault() : "unknown" !== typeof event.returnValue && (event.returnValue = false), this.isDefaultPrevented = functionThatReturnsTrue);
      },
      stopPropagation: function() {
        var event = this.nativeEvent;
        event && (event.stopPropagation ? event.stopPropagation() : "unknown" !== typeof event.cancelBubble && (event.cancelBubble = true), this.isPropagationStopped = functionThatReturnsTrue);
      },
      persist: function() {
      },
      isPersistent: functionThatReturnsTrue
    });
    return SyntheticBaseEvent;
  }
  var EventInterface = {
    eventPhase: 0,
    bubbles: 0,
    cancelable: 0,
    timeStamp: function(event) {
      return event.timeStamp || Date.now();
    },
    defaultPrevented: 0,
    isTrusted: 0
  }, SyntheticEvent = createSyntheticEvent(EventInterface), UIEventInterface = assign({}, EventInterface, { view: 0, detail: 0 }), SyntheticUIEvent = createSyntheticEvent(UIEventInterface), lastMovementX, lastMovementY, lastMouseEvent, MouseEventInterface = assign({}, UIEventInterface, {
    screenX: 0,
    screenY: 0,
    clientX: 0,
    clientY: 0,
    pageX: 0,
    pageY: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    getModifierState: getEventModifierState,
    button: 0,
    buttons: 0,
    relatedTarget: function(event) {
      return void 0 === event.relatedTarget ? event.fromElement === event.srcElement ? event.toElement : event.fromElement : event.relatedTarget;
    },
    movementX: function(event) {
      if ("movementX" in event) return event.movementX;
      event !== lastMouseEvent && (lastMouseEvent && "mousemove" === event.type ? (lastMovementX = event.screenX - lastMouseEvent.screenX, lastMovementY = event.screenY - lastMouseEvent.screenY) : lastMovementY = lastMovementX = 0, lastMouseEvent = event);
      return lastMovementX;
    },
    movementY: function(event) {
      return "movementY" in event ? event.movementY : lastMovementY;
    }
  }), SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface), DragEventInterface = assign({}, MouseEventInterface, { dataTransfer: 0 }), SyntheticDragEvent = createSyntheticEvent(DragEventInterface), FocusEventInterface = assign({}, UIEventInterface, { relatedTarget: 0 }), SyntheticFocusEvent = createSyntheticEvent(FocusEventInterface), AnimationEventInterface = assign({}, EventInterface, {
    animationName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), SyntheticAnimationEvent = createSyntheticEvent(AnimationEventInterface), ClipboardEventInterface = assign({}, EventInterface, {
    clipboardData: function(event) {
      return "clipboardData" in event ? event.clipboardData : window.clipboardData;
    }
  }), SyntheticClipboardEvent = createSyntheticEvent(ClipboardEventInterface), CompositionEventInterface = assign({}, EventInterface, { data: 0 }), SyntheticCompositionEvent = createSyntheticEvent(CompositionEventInterface), normalizeKey = {
    Esc: "Escape",
    Spacebar: " ",
    Left: "ArrowLeft",
    Up: "ArrowUp",
    Right: "ArrowRight",
    Down: "ArrowDown",
    Del: "Delete",
    Win: "OS",
    Menu: "ContextMenu",
    Apps: "ContextMenu",
    Scroll: "ScrollLock",
    MozPrintableKey: "Unidentified"
  }, translateToKey = {
    8: "Backspace",
    9: "Tab",
    12: "Clear",
    13: "Enter",
    16: "Shift",
    17: "Control",
    18: "Alt",
    19: "Pause",
    20: "CapsLock",
    27: "Escape",
    32: " ",
    33: "PageUp",
    34: "PageDown",
    35: "End",
    36: "Home",
    37: "ArrowLeft",
    38: "ArrowUp",
    39: "ArrowRight",
    40: "ArrowDown",
    45: "Insert",
    46: "Delete",
    112: "F1",
    113: "F2",
    114: "F3",
    115: "F4",
    116: "F5",
    117: "F6",
    118: "F7",
    119: "F8",
    120: "F9",
    121: "F10",
    122: "F11",
    123: "F12",
    144: "NumLock",
    145: "ScrollLock",
    224: "Meta"
  }, modifierKeyToProp = {
    Alt: "altKey",
    Control: "ctrlKey",
    Meta: "metaKey",
    Shift: "shiftKey"
  };
  function modifierStateGetter(keyArg) {
    var nativeEvent = this.nativeEvent;
    return nativeEvent.getModifierState ? nativeEvent.getModifierState(keyArg) : (keyArg = modifierKeyToProp[keyArg]) ? !!nativeEvent[keyArg] : false;
  }
  function getEventModifierState() {
    return modifierStateGetter;
  }
  var KeyboardEventInterface = assign({}, UIEventInterface, {
    key: function(nativeEvent) {
      if (nativeEvent.key) {
        var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
        if ("Unidentified" !== key) return key;
      }
      return "keypress" === nativeEvent.type ? (nativeEvent = getEventCharCode(nativeEvent), 13 === nativeEvent ? "Enter" : String.fromCharCode(nativeEvent)) : "keydown" === nativeEvent.type || "keyup" === nativeEvent.type ? translateToKey[nativeEvent.keyCode] || "Unidentified" : "";
    },
    code: 0,
    location: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    repeat: 0,
    locale: 0,
    getModifierState: getEventModifierState,
    charCode: function(event) {
      return "keypress" === event.type ? getEventCharCode(event) : 0;
    },
    keyCode: function(event) {
      return "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
    },
    which: function(event) {
      return "keypress" === event.type ? getEventCharCode(event) : "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
    }
  }), SyntheticKeyboardEvent = createSyntheticEvent(KeyboardEventInterface), PointerEventInterface = assign({}, MouseEventInterface, {
    pointerId: 0,
    width: 0,
    height: 0,
    pressure: 0,
    tangentialPressure: 0,
    tiltX: 0,
    tiltY: 0,
    twist: 0,
    pointerType: 0,
    isPrimary: 0
  }), SyntheticPointerEvent = createSyntheticEvent(PointerEventInterface), TouchEventInterface = assign({}, UIEventInterface, {
    touches: 0,
    targetTouches: 0,
    changedTouches: 0,
    altKey: 0,
    metaKey: 0,
    ctrlKey: 0,
    shiftKey: 0,
    getModifierState: getEventModifierState
  }), SyntheticTouchEvent = createSyntheticEvent(TouchEventInterface), TransitionEventInterface = assign({}, EventInterface, {
    propertyName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), SyntheticTransitionEvent = createSyntheticEvent(TransitionEventInterface), WheelEventInterface = assign({}, MouseEventInterface, {
    deltaX: function(event) {
      return "deltaX" in event ? event.deltaX : "wheelDeltaX" in event ? -event.wheelDeltaX : 0;
    },
    deltaY: function(event) {
      return "deltaY" in event ? event.deltaY : "wheelDeltaY" in event ? -event.wheelDeltaY : "wheelDelta" in event ? -event.wheelDelta : 0;
    },
    deltaZ: 0,
    deltaMode: 0
  }), SyntheticWheelEvent = createSyntheticEvent(WheelEventInterface), ToggleEventInterface = assign({}, EventInterface, {
    newState: 0,
    oldState: 0
  }), SyntheticToggleEvent = createSyntheticEvent(ToggleEventInterface), END_KEYCODES = [9, 13, 27, 32], canUseCompositionEvent = canUseDOM && "CompositionEvent" in window, documentMode = null;
  canUseDOM && "documentMode" in document && (documentMode = document.documentMode);
  var canUseTextInputEvent = canUseDOM && "TextEvent" in window && !documentMode, useFallbackCompositionData = canUseDOM && (!canUseCompositionEvent || documentMode && 8 < documentMode && 11 >= documentMode), SPACEBAR_CHAR = String.fromCharCode(32), hasSpaceKeypress = false;
  function isFallbackCompositionEnd(domEventName, nativeEvent) {
    switch (domEventName) {
      case "keyup":
        return -1 !== END_KEYCODES.indexOf(nativeEvent.keyCode);
      case "keydown":
        return 229 !== nativeEvent.keyCode;
      case "keypress":
      case "mousedown":
      case "focusout":
        return true;
      default:
        return false;
    }
  }
  function getDataFromCustomEvent(nativeEvent) {
    nativeEvent = nativeEvent.detail;
    return "object" === typeof nativeEvent && "data" in nativeEvent ? nativeEvent.data : null;
  }
  var isComposing = false;
  function getNativeBeforeInputChars(domEventName, nativeEvent) {
    switch (domEventName) {
      case "compositionend":
        return getDataFromCustomEvent(nativeEvent);
      case "keypress":
        if (32 !== nativeEvent.which) return null;
        hasSpaceKeypress = true;
        return SPACEBAR_CHAR;
      case "textInput":
        return domEventName = nativeEvent.data, domEventName === SPACEBAR_CHAR && hasSpaceKeypress ? null : domEventName;
      default:
        return null;
    }
  }
  function getFallbackBeforeInputChars(domEventName, nativeEvent) {
    if (isComposing)
      return "compositionend" === domEventName || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent) ? (domEventName = getData(), fallbackText = startText = root2 = null, isComposing = false, domEventName) : null;
    switch (domEventName) {
      case "paste":
        return null;
      case "keypress":
        if (!(nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) || nativeEvent.ctrlKey && nativeEvent.altKey) {
          if (nativeEvent.char && 1 < nativeEvent.char.length)
            return nativeEvent.char;
          if (nativeEvent.which) return String.fromCharCode(nativeEvent.which);
        }
        return null;
      case "compositionend":
        return useFallbackCompositionData && "ko" !== nativeEvent.locale ? null : nativeEvent.data;
      default:
        return null;
    }
  }
  var supportedInputTypes = {
    color: true,
    date: true,
    datetime: true,
    "datetime-local": true,
    email: true,
    month: true,
    number: true,
    password: true,
    range: true,
    search: true,
    tel: true,
    text: true,
    time: true,
    url: true,
    week: true
  };
  function isTextInputElement(elem) {
    var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
    return "input" === nodeName ? !!supportedInputTypes[elem.type] : "textarea" === nodeName ? true : false;
  }
  function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
    restoreTarget ? restoreQueue ? restoreQueue.push(target) : restoreQueue = [target] : restoreTarget = target;
    inst = accumulateTwoPhaseListeners(inst, "onChange");
    0 < inst.length && (nativeEvent = new SyntheticEvent(
      "onChange",
      "change",
      null,
      nativeEvent,
      target
    ), dispatchQueue.push({ event: nativeEvent, listeners: inst }));
  }
  var activeElement$1 = null, activeElementInst$1 = null;
  function runEventInBatch(dispatchQueue) {
    processDispatchQueue(dispatchQueue, 0);
  }
  function getInstIfValueChanged(targetInst) {
    var targetNode = getNodeFromInstance(targetInst);
    if (updateValueIfChanged(targetNode)) return targetInst;
  }
  function getTargetInstForChangeEvent(domEventName, targetInst) {
    if ("change" === domEventName) return targetInst;
  }
  var isInputEventSupported = false;
  if (canUseDOM) {
    var JSCompiler_inline_result$jscomp$286;
    if (canUseDOM) {
      var isSupported$jscomp$inline_427 = "oninput" in document;
      if (!isSupported$jscomp$inline_427) {
        var element$jscomp$inline_428 = document.createElement("div");
        element$jscomp$inline_428.setAttribute("oninput", "return;");
        isSupported$jscomp$inline_427 = "function" === typeof element$jscomp$inline_428.oninput;
      }
      JSCompiler_inline_result$jscomp$286 = isSupported$jscomp$inline_427;
    } else JSCompiler_inline_result$jscomp$286 = false;
    isInputEventSupported = JSCompiler_inline_result$jscomp$286 && (!document.documentMode || 9 < document.documentMode);
  }
  function stopWatchingForValueChange() {
    activeElement$1 && (activeElement$1.detachEvent("onpropertychange", handlePropertyChange), activeElementInst$1 = activeElement$1 = null);
  }
  function handlePropertyChange(nativeEvent) {
    if ("value" === nativeEvent.propertyName && getInstIfValueChanged(activeElementInst$1)) {
      var dispatchQueue = [];
      createAndAccumulateChangeEvent(
        dispatchQueue,
        activeElementInst$1,
        nativeEvent,
        getEventTarget(nativeEvent)
      );
      batchedUpdates$1(runEventInBatch, dispatchQueue);
    }
  }
  function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
    "focusin" === domEventName ? (stopWatchingForValueChange(), activeElement$1 = target, activeElementInst$1 = targetInst, activeElement$1.attachEvent("onpropertychange", handlePropertyChange)) : "focusout" === domEventName && stopWatchingForValueChange();
  }
  function getTargetInstForInputEventPolyfill(domEventName) {
    if ("selectionchange" === domEventName || "keyup" === domEventName || "keydown" === domEventName)
      return getInstIfValueChanged(activeElementInst$1);
  }
  function getTargetInstForClickEvent(domEventName, targetInst) {
    if ("click" === domEventName) return getInstIfValueChanged(targetInst);
  }
  function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
    if ("input" === domEventName || "change" === domEventName)
      return getInstIfValueChanged(targetInst);
  }
  function is(x, y) {
    return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
  }
  var objectIs = "function" === typeof Object.is ? Object.is : is;
  function shallowEqual(objA, objB) {
    if (objectIs(objA, objB)) return true;
    if ("object" !== typeof objA || null === objA || "object" !== typeof objB || null === objB)
      return false;
    var keysA = Object.keys(objA), keysB = Object.keys(objB);
    if (keysA.length !== keysB.length) return false;
    for (keysB = 0; keysB < keysA.length; keysB++) {
      var currentKey = keysA[keysB];
      if (!hasOwnProperty.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey]))
        return false;
    }
    return true;
  }
  function getLeafNode(node) {
    for (; node && node.firstChild; ) node = node.firstChild;
    return node;
  }
  function getNodeForCharacterOffset(root3, offset) {
    var node = getLeafNode(root3);
    root3 = 0;
    for (var nodeEnd; node; ) {
      if (3 === node.nodeType) {
        nodeEnd = root3 + node.textContent.length;
        if (root3 <= offset && nodeEnd >= offset)
          return { node, offset: offset - root3 };
        root3 = nodeEnd;
      }
      a: {
        for (; node; ) {
          if (node.nextSibling) {
            node = node.nextSibling;
            break a;
          }
          node = node.parentNode;
        }
        node = void 0;
      }
      node = getLeafNode(node);
    }
  }
  function containsNode(outerNode, innerNode) {
    return outerNode && innerNode ? outerNode === innerNode ? true : outerNode && 3 === outerNode.nodeType ? false : innerNode && 3 === innerNode.nodeType ? containsNode(outerNode, innerNode.parentNode) : "contains" in outerNode ? outerNode.contains(innerNode) : outerNode.compareDocumentPosition ? !!(outerNode.compareDocumentPosition(innerNode) & 16) : false : false;
  }
  function getActiveElementDeep(containerInfo) {
    containerInfo = null != containerInfo && null != containerInfo.ownerDocument && null != containerInfo.ownerDocument.defaultView ? containerInfo.ownerDocument.defaultView : window;
    for (var element = getActiveElement(containerInfo.document); element instanceof containerInfo.HTMLIFrameElement; ) {
      try {
        var JSCompiler_inline_result = "string" === typeof element.contentWindow.location.href;
      } catch (err) {
        JSCompiler_inline_result = false;
      }
      if (JSCompiler_inline_result) containerInfo = element.contentWindow;
      else break;
      element = getActiveElement(containerInfo.document);
    }
    return element;
  }
  function hasSelectionCapabilities(elem) {
    var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
    return nodeName && ("input" === nodeName && ("text" === elem.type || "search" === elem.type || "tel" === elem.type || "url" === elem.type || "password" === elem.type) || "textarea" === nodeName || "true" === elem.contentEditable);
  }
  var skipSelectionChangeEvent = canUseDOM && "documentMode" in document && 11 >= document.documentMode, activeElement = null, activeElementInst = null, lastSelection = null, mouseDown = false;
  function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
    var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : 9 === nativeEventTarget.nodeType ? nativeEventTarget : nativeEventTarget.ownerDocument;
    mouseDown || null == activeElement || activeElement !== getActiveElement(doc) || (doc = activeElement, "selectionStart" in doc && hasSelectionCapabilities(doc) ? doc = { start: doc.selectionStart, end: doc.selectionEnd } : (doc = (doc.ownerDocument && doc.ownerDocument.defaultView || window).getSelection(), doc = {
      anchorNode: doc.anchorNode,
      anchorOffset: doc.anchorOffset,
      focusNode: doc.focusNode,
      focusOffset: doc.focusOffset
    }), lastSelection && shallowEqual(lastSelection, doc) || (lastSelection = doc, doc = accumulateTwoPhaseListeners(activeElementInst, "onSelect"), 0 < doc.length && (nativeEvent = new SyntheticEvent(
      "onSelect",
      "select",
      null,
      nativeEvent,
      nativeEventTarget
    ), dispatchQueue.push({ event: nativeEvent, listeners: doc }), nativeEvent.target = activeElement)));
  }
  function makePrefixMap(styleProp, eventName) {
    var prefixes = {};
    prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
    prefixes["Webkit" + styleProp] = "webkit" + eventName;
    prefixes["Moz" + styleProp] = "moz" + eventName;
    return prefixes;
  }
  var vendorPrefixes = {
    animationend: makePrefixMap("Animation", "AnimationEnd"),
    animationiteration: makePrefixMap("Animation", "AnimationIteration"),
    animationstart: makePrefixMap("Animation", "AnimationStart"),
    transitionrun: makePrefixMap("Transition", "TransitionRun"),
    transitionstart: makePrefixMap("Transition", "TransitionStart"),
    transitioncancel: makePrefixMap("Transition", "TransitionCancel"),
    transitionend: makePrefixMap("Transition", "TransitionEnd")
  }, prefixedEventNames = {}, style = {};
  canUseDOM && (style = document.createElement("div").style, "AnimationEvent" in window || (delete vendorPrefixes.animationend.animation, delete vendorPrefixes.animationiteration.animation, delete vendorPrefixes.animationstart.animation), "TransitionEvent" in window || delete vendorPrefixes.transitionend.transition);
  function getVendorPrefixedEventName(eventName) {
    if (prefixedEventNames[eventName]) return prefixedEventNames[eventName];
    if (!vendorPrefixes[eventName]) return eventName;
    var prefixMap = vendorPrefixes[eventName], styleProp;
    for (styleProp in prefixMap)
      if (prefixMap.hasOwnProperty(styleProp) && styleProp in style)
        return prefixedEventNames[eventName] = prefixMap[styleProp];
    return eventName;
  }
  var ANIMATION_END = getVendorPrefixedEventName("animationend"), ANIMATION_ITERATION = getVendorPrefixedEventName("animationiteration"), ANIMATION_START = getVendorPrefixedEventName("animationstart"), TRANSITION_RUN = getVendorPrefixedEventName("transitionrun"), TRANSITION_START = getVendorPrefixedEventName("transitionstart"), TRANSITION_CANCEL = getVendorPrefixedEventName("transitioncancel"), TRANSITION_END = getVendorPrefixedEventName("transitionend"), topLevelEventsToReactNames = /* @__PURE__ */ new Map(), simpleEventPluginEvents = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(
    " "
  );
  simpleEventPluginEvents.push("scrollEnd");
  function registerSimpleEvent(domEventName, reactName) {
    topLevelEventsToReactNames.set(domEventName, reactName);
    registerTwoPhaseEvent(reactName, [domEventName]);
  }
  var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
    if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
      var event = new window.ErrorEvent("error", {
        bubbles: true,
        cancelable: true,
        message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
        error
      });
      if (!window.dispatchEvent(event)) return;
    } else if ("object" === typeof process && "function" === typeof process.emit) {
      process.emit("uncaughtException", error);
      return;
    }
    console.error(error);
  }, concurrentQueues = [], concurrentQueuesIndex = 0, concurrentlyUpdatedLanes = 0;
  function finishQueueingConcurrentUpdates() {
    for (var endIndex = concurrentQueuesIndex, i = concurrentlyUpdatedLanes = concurrentQueuesIndex = 0; i < endIndex; ) {
      var fiber = concurrentQueues[i];
      concurrentQueues[i++] = null;
      var queue = concurrentQueues[i];
      concurrentQueues[i++] = null;
      var update = concurrentQueues[i];
      concurrentQueues[i++] = null;
      var lane = concurrentQueues[i];
      concurrentQueues[i++] = null;
      if (null !== queue && null !== update) {
        var pending = queue.pending;
        null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
        queue.pending = update;
      }
      0 !== lane && markUpdateLaneFromFiberToRoot(fiber, update, lane);
    }
  }
  function enqueueUpdate$1(fiber, queue, update, lane) {
    concurrentQueues[concurrentQueuesIndex++] = fiber;
    concurrentQueues[concurrentQueuesIndex++] = queue;
    concurrentQueues[concurrentQueuesIndex++] = update;
    concurrentQueues[concurrentQueuesIndex++] = lane;
    concurrentlyUpdatedLanes |= lane;
    fiber.lanes |= lane;
    fiber = fiber.alternate;
    null !== fiber && (fiber.lanes |= lane);
  }
  function enqueueConcurrentHookUpdate(fiber, queue, update, lane) {
    enqueueUpdate$1(fiber, queue, update, lane);
    return getRootForUpdatedFiber(fiber);
  }
  function enqueueConcurrentRenderForLane(fiber, lane) {
    enqueueUpdate$1(fiber, null, null, lane);
    return getRootForUpdatedFiber(fiber);
  }
  function markUpdateLaneFromFiberToRoot(sourceFiber, update, lane) {
    sourceFiber.lanes |= lane;
    var alternate = sourceFiber.alternate;
    null !== alternate && (alternate.lanes |= lane);
    for (var isHidden = false, parent = sourceFiber.return; null !== parent; )
      parent.childLanes |= lane, alternate = parent.alternate, null !== alternate && (alternate.childLanes |= lane), 22 === parent.tag && (sourceFiber = parent.stateNode, null === sourceFiber || sourceFiber._visibility & 1 || (isHidden = true)), sourceFiber = parent, parent = parent.return;
    return 3 === sourceFiber.tag ? (parent = sourceFiber.stateNode, isHidden && null !== update && (isHidden = 31 - clz32(lane), sourceFiber = parent.hiddenUpdates, alternate = sourceFiber[isHidden], null === alternate ? sourceFiber[isHidden] = [update] : alternate.push(update), update.lane = lane | 536870912), parent) : null;
  }
  function getRootForUpdatedFiber(sourceFiber) {
    if (50 < nestedUpdateCount)
      throw nestedUpdateCount = 0, rootWithNestedUpdates = null, Error(formatProdErrorMessage(185));
    for (var parent = sourceFiber.return; null !== parent; )
      sourceFiber = parent, parent = sourceFiber.return;
    return 3 === sourceFiber.tag ? sourceFiber.stateNode : null;
  }
  var emptyContextObject = {};
  function FiberNode(tag, pendingProps, key, mode) {
    this.tag = tag;
    this.key = key;
    this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null;
    this.index = 0;
    this.refCleanup = this.ref = null;
    this.pendingProps = pendingProps;
    this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null;
    this.mode = mode;
    this.subtreeFlags = this.flags = 0;
    this.deletions = null;
    this.childLanes = this.lanes = 0;
    this.alternate = null;
  }
  function createFiberImplClass(tag, pendingProps, key, mode) {
    return new FiberNode(tag, pendingProps, key, mode);
  }
  function shouldConstruct(Component) {
    Component = Component.prototype;
    return !(!Component || !Component.isReactComponent);
  }
  function createWorkInProgress(current, pendingProps) {
    var workInProgress2 = current.alternate;
    null === workInProgress2 ? (workInProgress2 = createFiberImplClass(
      current.tag,
      pendingProps,
      current.key,
      current.mode
    ), workInProgress2.elementType = current.elementType, workInProgress2.type = current.type, workInProgress2.stateNode = current.stateNode, workInProgress2.alternate = current, current.alternate = workInProgress2) : (workInProgress2.pendingProps = pendingProps, workInProgress2.type = current.type, workInProgress2.flags = 0, workInProgress2.subtreeFlags = 0, workInProgress2.deletions = null);
    workInProgress2.flags = current.flags & 65011712;
    workInProgress2.childLanes = current.childLanes;
    workInProgress2.lanes = current.lanes;
    workInProgress2.child = current.child;
    workInProgress2.memoizedProps = current.memoizedProps;
    workInProgress2.memoizedState = current.memoizedState;
    workInProgress2.updateQueue = current.updateQueue;
    pendingProps = current.dependencies;
    workInProgress2.dependencies = null === pendingProps ? null : { lanes: pendingProps.lanes, firstContext: pendingProps.firstContext };
    workInProgress2.sibling = current.sibling;
    workInProgress2.index = current.index;
    workInProgress2.ref = current.ref;
    workInProgress2.refCleanup = current.refCleanup;
    return workInProgress2;
  }
  function resetWorkInProgress(workInProgress2, renderLanes2) {
    workInProgress2.flags &= 65011714;
    var current = workInProgress2.alternate;
    null === current ? (workInProgress2.childLanes = 0, workInProgress2.lanes = renderLanes2, workInProgress2.child = null, workInProgress2.subtreeFlags = 0, workInProgress2.memoizedProps = null, workInProgress2.memoizedState = null, workInProgress2.updateQueue = null, workInProgress2.dependencies = null, workInProgress2.stateNode = null) : (workInProgress2.childLanes = current.childLanes, workInProgress2.lanes = current.lanes, workInProgress2.child = current.child, workInProgress2.subtreeFlags = 0, workInProgress2.deletions = null, workInProgress2.memoizedProps = current.memoizedProps, workInProgress2.memoizedState = current.memoizedState, workInProgress2.updateQueue = current.updateQueue, workInProgress2.type = current.type, renderLanes2 = current.dependencies, workInProgress2.dependencies = null === renderLanes2 ? null : {
      lanes: renderLanes2.lanes,
      firstContext: renderLanes2.firstContext
    });
    return workInProgress2;
  }
  function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
    var fiberTag = 0;
    owner = type;
    if ("function" === typeof type) shouldConstruct(type) && (fiberTag = 1);
    else if ("string" === typeof type)
      fiberTag = isHostHoistableType(
        type,
        pendingProps,
        contextStackCursor.current
      ) ? 26 : "html" === type || "head" === type || "body" === type ? 27 : 5;
    else
      a: switch (type) {
        case REACT_ACTIVITY_TYPE:
          return type = createFiberImplClass(31, pendingProps, key, mode), type.elementType = REACT_ACTIVITY_TYPE, type.lanes = lanes, type;
        case REACT_FRAGMENT_TYPE:
          return createFiberFromFragment(pendingProps.children, mode, lanes, key);
        case REACT_STRICT_MODE_TYPE:
          fiberTag = 8;
          mode |= 24;
          break;
        case REACT_PROFILER_TYPE:
          return type = createFiberImplClass(12, pendingProps, key, mode | 2), type.elementType = REACT_PROFILER_TYPE, type.lanes = lanes, type;
        case REACT_SUSPENSE_TYPE:
          return type = createFiberImplClass(13, pendingProps, key, mode), type.elementType = REACT_SUSPENSE_TYPE, type.lanes = lanes, type;
        case REACT_SUSPENSE_LIST_TYPE:
          return type = createFiberImplClass(19, pendingProps, key, mode), type.elementType = REACT_SUSPENSE_LIST_TYPE, type.lanes = lanes, type;
        default:
          if ("object" === typeof type && null !== type)
            switch (type.$$typeof) {
              case REACT_CONTEXT_TYPE:
                fiberTag = 10;
                break a;
              case REACT_CONSUMER_TYPE:
                fiberTag = 9;
                break a;
              case REACT_FORWARD_REF_TYPE:
                fiberTag = 11;
                break a;
              case REACT_MEMO_TYPE:
                fiberTag = 14;
                break a;
              case REACT_LAZY_TYPE:
                fiberTag = 16;
                owner = null;
                break a;
            }
          fiberTag = 29;
          pendingProps = Error(
            formatProdErrorMessage(130, null === type ? "null" : typeof type, "")
          );
          owner = null;
      }
    key = createFiberImplClass(fiberTag, pendingProps, key, mode);
    key.elementType = type;
    key.type = owner;
    key.lanes = lanes;
    return key;
  }
  function createFiberFromFragment(elements, mode, lanes, key) {
    elements = createFiberImplClass(7, elements, key, mode);
    elements.lanes = lanes;
    return elements;
  }
  function createFiberFromText(content2, mode, lanes) {
    content2 = createFiberImplClass(6, content2, null, mode);
    content2.lanes = lanes;
    return content2;
  }
  function createFiberFromDehydratedFragment(dehydratedNode) {
    var fiber = createFiberImplClass(18, null, null, 0);
    fiber.stateNode = dehydratedNode;
    return fiber;
  }
  function createFiberFromPortal(portal, mode, lanes) {
    mode = createFiberImplClass(
      4,
      null !== portal.children ? portal.children : [],
      portal.key,
      mode
    );
    mode.lanes = lanes;
    mode.stateNode = {
      containerInfo: portal.containerInfo,
      pendingChildren: null,
      implementation: portal.implementation
    };
    return mode;
  }
  var CapturedStacks = /* @__PURE__ */ new WeakMap();
  function createCapturedValueAtFiber(value, source) {
    if ("object" === typeof value && null !== value) {
      var existing = CapturedStacks.get(value);
      if (void 0 !== existing) return existing;
      source = {
        value,
        source,
        stack: getStackByFiberInDevAndProd(source)
      };
      CapturedStacks.set(value, source);
      return source;
    }
    return {
      value,
      source,
      stack: getStackByFiberInDevAndProd(source)
    };
  }
  var forkStack = [], forkStackIndex = 0, treeForkProvider = null, treeForkCount = 0, idStack = [], idStackIndex = 0, treeContextProvider = null, treeContextId = 1, treeContextOverflow = "";
  function pushTreeFork(workInProgress2, totalChildren) {
    forkStack[forkStackIndex++] = treeForkCount;
    forkStack[forkStackIndex++] = treeForkProvider;
    treeForkProvider = workInProgress2;
    treeForkCount = totalChildren;
  }
  function pushTreeId(workInProgress2, totalChildren, index2) {
    idStack[idStackIndex++] = treeContextId;
    idStack[idStackIndex++] = treeContextOverflow;
    idStack[idStackIndex++] = treeContextProvider;
    treeContextProvider = workInProgress2;
    var baseIdWithLeadingBit = treeContextId;
    workInProgress2 = treeContextOverflow;
    var baseLength = 32 - clz32(baseIdWithLeadingBit) - 1;
    baseIdWithLeadingBit &= ~(1 << baseLength);
    index2 += 1;
    var length = 32 - clz32(totalChildren) + baseLength;
    if (30 < length) {
      var numberOfOverflowBits = baseLength - baseLength % 5;
      length = (baseIdWithLeadingBit & (1 << numberOfOverflowBits) - 1).toString(32);
      baseIdWithLeadingBit >>= numberOfOverflowBits;
      baseLength -= numberOfOverflowBits;
      treeContextId = 1 << 32 - clz32(totalChildren) + baseLength | index2 << baseLength | baseIdWithLeadingBit;
      treeContextOverflow = length + workInProgress2;
    } else
      treeContextId = 1 << length | index2 << baseLength | baseIdWithLeadingBit, treeContextOverflow = workInProgress2;
  }
  function pushMaterializedTreeId(workInProgress2) {
    null !== workInProgress2.return && (pushTreeFork(workInProgress2, 1), pushTreeId(workInProgress2, 1, 0));
  }
  function popTreeContext(workInProgress2) {
    for (; workInProgress2 === treeForkProvider; )
      treeForkProvider = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null, treeForkCount = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null;
    for (; workInProgress2 === treeContextProvider; )
      treeContextProvider = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextOverflow = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextId = idStack[--idStackIndex], idStack[idStackIndex] = null;
  }
  function restoreSuspendedTreeContext(workInProgress2, suspendedContext) {
    idStack[idStackIndex++] = treeContextId;
    idStack[idStackIndex++] = treeContextOverflow;
    idStack[idStackIndex++] = treeContextProvider;
    treeContextId = suspendedContext.id;
    treeContextOverflow = suspendedContext.overflow;
    treeContextProvider = workInProgress2;
  }
  var hydrationParentFiber = null, nextHydratableInstance = null, isHydrating = false, hydrationErrors = null, rootOrSingletonContext = false, HydrationMismatchException = Error(formatProdErrorMessage(519));
  function throwOnHydrationMismatch(fiber) {
    var error = Error(
      formatProdErrorMessage(
        418,
        1 < arguments.length && void 0 !== arguments[1] && arguments[1] ? "text" : "HTML",
        ""
      )
    );
    queueHydrationError(createCapturedValueAtFiber(error, fiber));
    throw HydrationMismatchException;
  }
  function prepareToHydrateHostInstance(fiber) {
    var instance = fiber.stateNode, type = fiber.type, props = fiber.memoizedProps;
    instance[internalInstanceKey] = fiber;
    instance[internalPropsKey] = props;
    switch (type) {
      case "dialog":
        listenToNonDelegatedEvent("cancel", instance);
        listenToNonDelegatedEvent("close", instance);
        break;
      case "iframe":
      case "object":
      case "embed":
        listenToNonDelegatedEvent("load", instance);
        break;
      case "video":
      case "audio":
        for (type = 0; type < mediaEventTypes.length; type++)
          listenToNonDelegatedEvent(mediaEventTypes[type], instance);
        break;
      case "source":
        listenToNonDelegatedEvent("error", instance);
        break;
      case "img":
      case "image":
      case "link":
        listenToNonDelegatedEvent("error", instance);
        listenToNonDelegatedEvent("load", instance);
        break;
      case "details":
        listenToNonDelegatedEvent("toggle", instance);
        break;
      case "input":
        listenToNonDelegatedEvent("invalid", instance);
        initInput(
          instance,
          props.value,
          props.defaultValue,
          props.checked,
          props.defaultChecked,
          props.type,
          props.name,
          true
        );
        break;
      case "select":
        listenToNonDelegatedEvent("invalid", instance);
        break;
      case "textarea":
        listenToNonDelegatedEvent("invalid", instance), initTextarea(instance, props.value, props.defaultValue, props.children);
    }
    type = props.children;
    "string" !== typeof type && "number" !== typeof type && "bigint" !== typeof type || instance.textContent === "" + type || true === props.suppressHydrationWarning || checkForUnmatchedText(instance.textContent, type) ? (null != props.popover && (listenToNonDelegatedEvent("beforetoggle", instance), listenToNonDelegatedEvent("toggle", instance)), null != props.onScroll && listenToNonDelegatedEvent("scroll", instance), null != props.onScrollEnd && listenToNonDelegatedEvent("scrollend", instance), null != props.onClick && (instance.onclick = noop$1), instance = true) : instance = false;
    instance || throwOnHydrationMismatch(fiber, true);
  }
  function popToNextHostParent(fiber) {
    for (hydrationParentFiber = fiber.return; hydrationParentFiber; )
      switch (hydrationParentFiber.tag) {
        case 5:
        case 31:
        case 13:
          rootOrSingletonContext = false;
          return;
        case 27:
        case 3:
          rootOrSingletonContext = true;
          return;
        default:
          hydrationParentFiber = hydrationParentFiber.return;
      }
  }
  function popHydrationState(fiber) {
    if (fiber !== hydrationParentFiber) return false;
    if (!isHydrating) return popToNextHostParent(fiber), isHydrating = true, false;
    var tag = fiber.tag, JSCompiler_temp;
    if (JSCompiler_temp = 3 !== tag && 27 !== tag) {
      if (JSCompiler_temp = 5 === tag)
        JSCompiler_temp = fiber.type, JSCompiler_temp = !("form" !== JSCompiler_temp && "button" !== JSCompiler_temp) || shouldSetTextContent(fiber.type, fiber.memoizedProps);
      JSCompiler_temp = !JSCompiler_temp;
    }
    JSCompiler_temp && nextHydratableInstance && throwOnHydrationMismatch(fiber);
    popToNextHostParent(fiber);
    if (13 === tag) {
      fiber = fiber.memoizedState;
      fiber = null !== fiber ? fiber.dehydrated : null;
      if (!fiber) throw Error(formatProdErrorMessage(317));
      nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
    } else if (31 === tag) {
      fiber = fiber.memoizedState;
      fiber = null !== fiber ? fiber.dehydrated : null;
      if (!fiber) throw Error(formatProdErrorMessage(317));
      nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
    } else
      27 === tag ? (tag = nextHydratableInstance, isSingletonScope(fiber.type) ? (fiber = previousHydratableOnEnteringScopedSingleton, previousHydratableOnEnteringScopedSingleton = null, nextHydratableInstance = fiber) : nextHydratableInstance = tag) : nextHydratableInstance = hydrationParentFiber ? getNextHydratable(fiber.stateNode.nextSibling) : null;
    return true;
  }
  function resetHydrationState() {
    nextHydratableInstance = hydrationParentFiber = null;
    isHydrating = false;
  }
  function upgradeHydrationErrorsToRecoverable() {
    var queuedErrors = hydrationErrors;
    null !== queuedErrors && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = queuedErrors : workInProgressRootRecoverableErrors.push.apply(
      workInProgressRootRecoverableErrors,
      queuedErrors
    ), hydrationErrors = null);
    return queuedErrors;
  }
  function queueHydrationError(error) {
    null === hydrationErrors ? hydrationErrors = [error] : hydrationErrors.push(error);
  }
  var valueCursor = createCursor(null), currentlyRenderingFiber$1 = null, lastContextDependency = null;
  function pushProvider(providerFiber, context, nextValue) {
    push(valueCursor, context._currentValue);
    context._currentValue = nextValue;
  }
  function popProvider(context) {
    context._currentValue = valueCursor.current;
    pop(valueCursor);
  }
  function scheduleContextWorkOnParentPath(parent, renderLanes2, propagationRoot) {
    for (; null !== parent; ) {
      var alternate = parent.alternate;
      (parent.childLanes & renderLanes2) !== renderLanes2 ? (parent.childLanes |= renderLanes2, null !== alternate && (alternate.childLanes |= renderLanes2)) : null !== alternate && (alternate.childLanes & renderLanes2) !== renderLanes2 && (alternate.childLanes |= renderLanes2);
      if (parent === propagationRoot) break;
      parent = parent.return;
    }
  }
  function propagateContextChanges(workInProgress2, contexts, renderLanes2, forcePropagateEntireTree) {
    var fiber = workInProgress2.child;
    null !== fiber && (fiber.return = workInProgress2);
    for (; null !== fiber; ) {
      var list = fiber.dependencies;
      if (null !== list) {
        var nextFiber = fiber.child;
        list = list.firstContext;
        a: for (; null !== list; ) {
          var dependency = list;
          list = fiber;
          for (var i = 0; i < contexts.length; i++)
            if (dependency.context === contexts[i]) {
              list.lanes |= renderLanes2;
              dependency = list.alternate;
              null !== dependency && (dependency.lanes |= renderLanes2);
              scheduleContextWorkOnParentPath(
                list.return,
                renderLanes2,
                workInProgress2
              );
              forcePropagateEntireTree || (nextFiber = null);
              break a;
            }
          list = dependency.next;
        }
      } else if (18 === fiber.tag) {
        nextFiber = fiber.return;
        if (null === nextFiber) throw Error(formatProdErrorMessage(341));
        nextFiber.lanes |= renderLanes2;
        list = nextFiber.alternate;
        null !== list && (list.lanes |= renderLanes2);
        scheduleContextWorkOnParentPath(nextFiber, renderLanes2, workInProgress2);
        nextFiber = null;
      } else nextFiber = fiber.child;
      if (null !== nextFiber) nextFiber.return = fiber;
      else
        for (nextFiber = fiber; null !== nextFiber; ) {
          if (nextFiber === workInProgress2) {
            nextFiber = null;
            break;
          }
          fiber = nextFiber.sibling;
          if (null !== fiber) {
            fiber.return = nextFiber.return;
            nextFiber = fiber;
            break;
          }
          nextFiber = nextFiber.return;
        }
      fiber = nextFiber;
    }
  }
  function propagateParentContextChanges(current, workInProgress2, renderLanes2, forcePropagateEntireTree) {
    current = null;
    for (var parent = workInProgress2, isInsidePropagationBailout = false; null !== parent; ) {
      if (!isInsidePropagationBailout) {
        if (0 !== (parent.flags & 524288)) isInsidePropagationBailout = true;
        else if (0 !== (parent.flags & 262144)) break;
      }
      if (10 === parent.tag) {
        var currentParent = parent.alternate;
        if (null === currentParent) throw Error(formatProdErrorMessage(387));
        currentParent = currentParent.memoizedProps;
        if (null !== currentParent) {
          var context = parent.type;
          objectIs(parent.pendingProps.value, currentParent.value) || (null !== current ? current.push(context) : current = [context]);
        }
      } else if (parent === hostTransitionProviderCursor.current) {
        currentParent = parent.alternate;
        if (null === currentParent) throw Error(formatProdErrorMessage(387));
        currentParent.memoizedState.memoizedState !== parent.memoizedState.memoizedState && (null !== current ? current.push(HostTransitionContext) : current = [HostTransitionContext]);
      }
      parent = parent.return;
    }
    null !== current && propagateContextChanges(
      workInProgress2,
      current,
      renderLanes2,
      forcePropagateEntireTree
    );
    workInProgress2.flags |= 262144;
  }
  function checkIfContextChanged(currentDependencies) {
    for (currentDependencies = currentDependencies.firstContext; null !== currentDependencies; ) {
      if (!objectIs(
        currentDependencies.context._currentValue,
        currentDependencies.memoizedValue
      ))
        return true;
      currentDependencies = currentDependencies.next;
    }
    return false;
  }
  function prepareToReadContext(workInProgress2) {
    currentlyRenderingFiber$1 = workInProgress2;
    lastContextDependency = null;
    workInProgress2 = workInProgress2.dependencies;
    null !== workInProgress2 && (workInProgress2.firstContext = null);
  }
  function readContext(context) {
    return readContextForConsumer(currentlyRenderingFiber$1, context);
  }
  function readContextDuringReconciliation(consumer, context) {
    null === currentlyRenderingFiber$1 && prepareToReadContext(consumer);
    return readContextForConsumer(consumer, context);
  }
  function readContextForConsumer(consumer, context) {
    var value = context._currentValue;
    context = { context, memoizedValue: value, next: null };
    if (null === lastContextDependency) {
      if (null === consumer) throw Error(formatProdErrorMessage(308));
      lastContextDependency = context;
      consumer.dependencies = { lanes: 0, firstContext: context };
      consumer.flags |= 524288;
    } else lastContextDependency = lastContextDependency.next = context;
    return value;
  }
  var AbortControllerLocal = "undefined" !== typeof AbortController ? AbortController : function() {
    var listeners = [], signal = this.signal = {
      aborted: false,
      addEventListener: function(type, listener) {
        listeners.push(listener);
      }
    };
    this.abort = function() {
      signal.aborted = true;
      listeners.forEach(function(listener) {
        return listener();
      });
    };
  }, scheduleCallback$2 = Scheduler.unstable_scheduleCallback, NormalPriority = Scheduler.unstable_NormalPriority, CacheContext = {
    $$typeof: REACT_CONTEXT_TYPE,
    Consumer: null,
    Provider: null,
    _currentValue: null,
    _currentValue2: null,
    _threadCount: 0
  };
  function createCache() {
    return {
      controller: new AbortControllerLocal(),
      data: /* @__PURE__ */ new Map(),
      refCount: 0
    };
  }
  function releaseCache(cache) {
    cache.refCount--;
    0 === cache.refCount && scheduleCallback$2(NormalPriority, function() {
      cache.controller.abort();
    });
  }
  var currentEntangledListeners = null, currentEntangledPendingCount = 0, currentEntangledLane = 0, currentEntangledActionThenable = null;
  function entangleAsyncAction(transition, thenable) {
    if (null === currentEntangledListeners) {
      var entangledListeners = currentEntangledListeners = [];
      currentEntangledPendingCount = 0;
      currentEntangledLane = requestTransitionLane();
      currentEntangledActionThenable = {
        status: "pending",
        value: void 0,
        then: function(resolve) {
          entangledListeners.push(resolve);
        }
      };
    }
    currentEntangledPendingCount++;
    thenable.then(pingEngtangledActionScope, pingEngtangledActionScope);
    return thenable;
  }
  function pingEngtangledActionScope() {
    if (0 === --currentEntangledPendingCount && null !== currentEntangledListeners) {
      null !== currentEntangledActionThenable && (currentEntangledActionThenable.status = "fulfilled");
      var listeners = currentEntangledListeners;
      currentEntangledListeners = null;
      currentEntangledLane = 0;
      currentEntangledActionThenable = null;
      for (var i = 0; i < listeners.length; i++) (0, listeners[i])();
    }
  }
  function chainThenableValue(thenable, result) {
    var listeners = [], thenableWithOverride = {
      status: "pending",
      value: null,
      reason: null,
      then: function(resolve) {
        listeners.push(resolve);
      }
    };
    thenable.then(
      function() {
        thenableWithOverride.status = "fulfilled";
        thenableWithOverride.value = result;
        for (var i = 0; i < listeners.length; i++) (0, listeners[i])(result);
      },
      function(error) {
        thenableWithOverride.status = "rejected";
        thenableWithOverride.reason = error;
        for (error = 0; error < listeners.length; error++)
          (0, listeners[error])(void 0);
      }
    );
    return thenableWithOverride;
  }
  var prevOnStartTransitionFinish = ReactSharedInternals.S;
  ReactSharedInternals.S = function(transition, returnValue) {
    globalMostRecentTransitionTime = now();
    "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && entangleAsyncAction(transition, returnValue);
    null !== prevOnStartTransitionFinish && prevOnStartTransitionFinish(transition, returnValue);
  };
  var resumedCache = createCursor(null);
  function peekCacheFromPool() {
    var cacheResumedFromPreviousRender = resumedCache.current;
    return null !== cacheResumedFromPreviousRender ? cacheResumedFromPreviousRender : workInProgressRoot.pooledCache;
  }
  function pushTransition(offscreenWorkInProgress, prevCachePool) {
    null === prevCachePool ? push(resumedCache, resumedCache.current) : push(resumedCache, prevCachePool.pool);
  }
  function getSuspendedCache() {
    var cacheFromPool = peekCacheFromPool();
    return null === cacheFromPool ? null : { parent: CacheContext._currentValue, pool: cacheFromPool };
  }
  var SuspenseException = Error(formatProdErrorMessage(460)), SuspenseyCommitException = Error(formatProdErrorMessage(474)), SuspenseActionException = Error(formatProdErrorMessage(542)), noopSuspenseyCommitThenable = { then: function() {
  } };
  function isThenableResolved(thenable) {
    thenable = thenable.status;
    return "fulfilled" === thenable || "rejected" === thenable;
  }
  function trackUsedThenable(thenableState2, thenable, index2) {
    index2 = thenableState2[index2];
    void 0 === index2 ? thenableState2.push(thenable) : index2 !== thenable && (thenable.then(noop$1, noop$1), thenable = index2);
    switch (thenable.status) {
      case "fulfilled":
        return thenable.value;
      case "rejected":
        throw thenableState2 = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState2), thenableState2;
      default:
        if ("string" === typeof thenable.status) thenable.then(noop$1, noop$1);
        else {
          thenableState2 = workInProgressRoot;
          if (null !== thenableState2 && 100 < thenableState2.shellSuspendCounter)
            throw Error(formatProdErrorMessage(482));
          thenableState2 = thenable;
          thenableState2.status = "pending";
          thenableState2.then(
            function(fulfilledValue) {
              if ("pending" === thenable.status) {
                var fulfilledThenable = thenable;
                fulfilledThenable.status = "fulfilled";
                fulfilledThenable.value = fulfilledValue;
              }
            },
            function(error) {
              if ("pending" === thenable.status) {
                var rejectedThenable = thenable;
                rejectedThenable.status = "rejected";
                rejectedThenable.reason = error;
              }
            }
          );
        }
        switch (thenable.status) {
          case "fulfilled":
            return thenable.value;
          case "rejected":
            throw thenableState2 = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState2), thenableState2;
        }
        suspendedThenable = thenable;
        throw SuspenseException;
    }
  }
  function resolveLazy(lazyType) {
    try {
      var init = lazyType._init;
      return init(lazyType._payload);
    } catch (x) {
      if (null !== x && "object" === typeof x && "function" === typeof x.then)
        throw suspendedThenable = x, SuspenseException;
      throw x;
    }
  }
  var suspendedThenable = null;
  function getSuspendedThenable() {
    if (null === suspendedThenable) throw Error(formatProdErrorMessage(459));
    var thenable = suspendedThenable;
    suspendedThenable = null;
    return thenable;
  }
  function checkIfUseWrappedInAsyncCatch(rejectedReason) {
    if (rejectedReason === SuspenseException || rejectedReason === SuspenseActionException)
      throw Error(formatProdErrorMessage(483));
  }
  var thenableState$1 = null, thenableIndexCounter$1 = 0;
  function unwrapThenable(thenable) {
    var index2 = thenableIndexCounter$1;
    thenableIndexCounter$1 += 1;
    null === thenableState$1 && (thenableState$1 = []);
    return trackUsedThenable(thenableState$1, thenable, index2);
  }
  function coerceRef(workInProgress2, element) {
    element = element.props.ref;
    workInProgress2.ref = void 0 !== element ? element : null;
  }
  function throwOnInvalidObjectTypeImpl(returnFiber, newChild) {
    if (newChild.$$typeof === REACT_LEGACY_ELEMENT_TYPE)
      throw Error(formatProdErrorMessage(525));
    returnFiber = Object.prototype.toString.call(newChild);
    throw Error(
      formatProdErrorMessage(
        31,
        "[object Object]" === returnFiber ? "object with keys {" + Object.keys(newChild).join(", ") + "}" : returnFiber
      )
    );
  }
  function createChildReconciler(shouldTrackSideEffects) {
    function deleteChild(returnFiber, childToDelete) {
      if (shouldTrackSideEffects) {
        var deletions = returnFiber.deletions;
        null === deletions ? (returnFiber.deletions = [childToDelete], returnFiber.flags |= 16) : deletions.push(childToDelete);
      }
    }
    function deleteRemainingChildren(returnFiber, currentFirstChild) {
      if (!shouldTrackSideEffects) return null;
      for (; null !== currentFirstChild; )
        deleteChild(returnFiber, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
      return null;
    }
    function mapRemainingChildren(currentFirstChild) {
      for (var existingChildren = /* @__PURE__ */ new Map(); null !== currentFirstChild; )
        null !== currentFirstChild.key ? existingChildren.set(currentFirstChild.key, currentFirstChild) : existingChildren.set(currentFirstChild.index, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
      return existingChildren;
    }
    function useFiber(fiber, pendingProps) {
      fiber = createWorkInProgress(fiber, pendingProps);
      fiber.index = 0;
      fiber.sibling = null;
      return fiber;
    }
    function placeChild(newFiber, lastPlacedIndex, newIndex) {
      newFiber.index = newIndex;
      if (!shouldTrackSideEffects)
        return newFiber.flags |= 1048576, lastPlacedIndex;
      newIndex = newFiber.alternate;
      if (null !== newIndex)
        return newIndex = newIndex.index, newIndex < lastPlacedIndex ? (newFiber.flags |= 67108866, lastPlacedIndex) : newIndex;
      newFiber.flags |= 67108866;
      return lastPlacedIndex;
    }
    function placeSingleChild(newFiber) {
      shouldTrackSideEffects && null === newFiber.alternate && (newFiber.flags |= 67108866);
      return newFiber;
    }
    function updateTextNode(returnFiber, current, textContent, lanes) {
      if (null === current || 6 !== current.tag)
        return current = createFiberFromText(textContent, returnFiber.mode, lanes), current.return = returnFiber, current;
      current = useFiber(current, textContent);
      current.return = returnFiber;
      return current;
    }
    function updateElement(returnFiber, current, element, lanes) {
      var elementType = element.type;
      if (elementType === REACT_FRAGMENT_TYPE)
        return updateFragment(
          returnFiber,
          current,
          element.props.children,
          lanes,
          element.key
        );
      if (null !== current && (current.elementType === elementType || "object" === typeof elementType && null !== elementType && elementType.$$typeof === REACT_LAZY_TYPE && resolveLazy(elementType) === current.type))
        return current = useFiber(current, element.props), coerceRef(current, element), current.return = returnFiber, current;
      current = createFiberFromTypeAndProps(
        element.type,
        element.key,
        element.props,
        null,
        returnFiber.mode,
        lanes
      );
      coerceRef(current, element);
      current.return = returnFiber;
      return current;
    }
    function updatePortal(returnFiber, current, portal, lanes) {
      if (null === current || 4 !== current.tag || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation)
        return current = createFiberFromPortal(portal, returnFiber.mode, lanes), current.return = returnFiber, current;
      current = useFiber(current, portal.children || []);
      current.return = returnFiber;
      return current;
    }
    function updateFragment(returnFiber, current, fragment, lanes, key) {
      if (null === current || 7 !== current.tag)
        return current = createFiberFromFragment(
          fragment,
          returnFiber.mode,
          lanes,
          key
        ), current.return = returnFiber, current;
      current = useFiber(current, fragment);
      current.return = returnFiber;
      return current;
    }
    function createChild(returnFiber, newChild, lanes) {
      if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
        return newChild = createFiberFromText(
          "" + newChild,
          returnFiber.mode,
          lanes
        ), newChild.return = returnFiber, newChild;
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            return lanes = createFiberFromTypeAndProps(
              newChild.type,
              newChild.key,
              newChild.props,
              null,
              returnFiber.mode,
              lanes
            ), coerceRef(lanes, newChild), lanes.return = returnFiber, lanes;
          case REACT_PORTAL_TYPE:
            return newChild = createFiberFromPortal(
              newChild,
              returnFiber.mode,
              lanes
            ), newChild.return = returnFiber, newChild;
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), createChild(returnFiber, newChild, lanes);
        }
        if (isArrayImpl(newChild) || getIteratorFn(newChild))
          return newChild = createFiberFromFragment(
            newChild,
            returnFiber.mode,
            lanes,
            null
          ), newChild.return = returnFiber, newChild;
        if ("function" === typeof newChild.then)
          return createChild(returnFiber, unwrapThenable(newChild), lanes);
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return createChild(
            returnFiber,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return null;
    }
    function updateSlot(returnFiber, oldFiber, newChild, lanes) {
      var key = null !== oldFiber ? oldFiber.key : null;
      if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
        return null !== key ? null : updateTextNode(returnFiber, oldFiber, "" + newChild, lanes);
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            return newChild.key === key ? updateElement(returnFiber, oldFiber, newChild, lanes) : null;
          case REACT_PORTAL_TYPE:
            return newChild.key === key ? updatePortal(returnFiber, oldFiber, newChild, lanes) : null;
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), updateSlot(returnFiber, oldFiber, newChild, lanes);
        }
        if (isArrayImpl(newChild) || getIteratorFn(newChild))
          return null !== key ? null : updateFragment(returnFiber, oldFiber, newChild, lanes, null);
        if ("function" === typeof newChild.then)
          return updateSlot(
            returnFiber,
            oldFiber,
            unwrapThenable(newChild),
            lanes
          );
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return updateSlot(
            returnFiber,
            oldFiber,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return null;
    }
    function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
      if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
        return existingChildren = existingChildren.get(newIdx) || null, updateTextNode(returnFiber, existingChildren, "" + newChild, lanes);
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            return existingChildren = existingChildren.get(
              null === newChild.key ? newIdx : newChild.key
            ) || null, updateElement(returnFiber, existingChildren, newChild, lanes);
          case REACT_PORTAL_TYPE:
            return existingChildren = existingChildren.get(
              null === newChild.key ? newIdx : newChild.key
            ) || null, updatePortal(returnFiber, existingChildren, newChild, lanes);
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), updateFromMap(
              existingChildren,
              returnFiber,
              newIdx,
              newChild,
              lanes
            );
        }
        if (isArrayImpl(newChild) || getIteratorFn(newChild))
          return existingChildren = existingChildren.get(newIdx) || null, updateFragment(returnFiber, existingChildren, newChild, lanes, null);
        if ("function" === typeof newChild.then)
          return updateFromMap(
            existingChildren,
            returnFiber,
            newIdx,
            unwrapThenable(newChild),
            lanes
          );
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return updateFromMap(
            existingChildren,
            returnFiber,
            newIdx,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return null;
    }
    function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
      for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null; null !== oldFiber && newIdx < newChildren.length; newIdx++) {
        oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
        var newFiber = updateSlot(
          returnFiber,
          oldFiber,
          newChildren[newIdx],
          lanes
        );
        if (null === newFiber) {
          null === oldFiber && (oldFiber = nextOldFiber);
          break;
        }
        shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
        currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
        null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
        previousNewFiber = newFiber;
        oldFiber = nextOldFiber;
      }
      if (newIdx === newChildren.length)
        return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
      if (null === oldFiber) {
        for (; newIdx < newChildren.length; newIdx++)
          oldFiber = createChild(returnFiber, newChildren[newIdx], lanes), null !== oldFiber && (currentFirstChild = placeChild(
            oldFiber,
            currentFirstChild,
            newIdx
          ), null === previousNewFiber ? resultingFirstChild = oldFiber : previousNewFiber.sibling = oldFiber, previousNewFiber = oldFiber);
        isHydrating && pushTreeFork(returnFiber, newIdx);
        return resultingFirstChild;
      }
      for (oldFiber = mapRemainingChildren(oldFiber); newIdx < newChildren.length; newIdx++)
        nextOldFiber = updateFromMap(
          oldFiber,
          returnFiber,
          newIdx,
          newChildren[newIdx],
          lanes
        ), null !== nextOldFiber && (shouldTrackSideEffects && null !== nextOldFiber.alternate && oldFiber.delete(
          null === nextOldFiber.key ? newIdx : nextOldFiber.key
        ), currentFirstChild = placeChild(
          nextOldFiber,
          currentFirstChild,
          newIdx
        ), null === previousNewFiber ? resultingFirstChild = nextOldFiber : previousNewFiber.sibling = nextOldFiber, previousNewFiber = nextOldFiber);
      shouldTrackSideEffects && oldFiber.forEach(function(child) {
        return deleteChild(returnFiber, child);
      });
      isHydrating && pushTreeFork(returnFiber, newIdx);
      return resultingFirstChild;
    }
    function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildren, lanes) {
      if (null == newChildren) throw Error(formatProdErrorMessage(151));
      for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null, step = newChildren.next(); null !== oldFiber && !step.done; newIdx++, step = newChildren.next()) {
        oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
        var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
        if (null === newFiber) {
          null === oldFiber && (oldFiber = nextOldFiber);
          break;
        }
        shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
        currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
        null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
        previousNewFiber = newFiber;
        oldFiber = nextOldFiber;
      }
      if (step.done)
        return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
      if (null === oldFiber) {
        for (; !step.done; newIdx++, step = newChildren.next())
          step = createChild(returnFiber, step.value, lanes), null !== step && (currentFirstChild = placeChild(step, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = step : previousNewFiber.sibling = step, previousNewFiber = step);
        isHydrating && pushTreeFork(returnFiber, newIdx);
        return resultingFirstChild;
      }
      for (oldFiber = mapRemainingChildren(oldFiber); !step.done; newIdx++, step = newChildren.next())
        step = updateFromMap(oldFiber, returnFiber, newIdx, step.value, lanes), null !== step && (shouldTrackSideEffects && null !== step.alternate && oldFiber.delete(null === step.key ? newIdx : step.key), currentFirstChild = placeChild(step, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = step : previousNewFiber.sibling = step, previousNewFiber = step);
      shouldTrackSideEffects && oldFiber.forEach(function(child) {
        return deleteChild(returnFiber, child);
      });
      isHydrating && pushTreeFork(returnFiber, newIdx);
      return resultingFirstChild;
    }
    function reconcileChildFibersImpl(returnFiber, currentFirstChild, newChild, lanes) {
      "object" === typeof newChild && null !== newChild && newChild.type === REACT_FRAGMENT_TYPE && null === newChild.key && (newChild = newChild.props.children);
      if ("object" === typeof newChild && null !== newChild) {
        switch (newChild.$$typeof) {
          case REACT_ELEMENT_TYPE:
            a: {
              for (var key = newChild.key; null !== currentFirstChild; ) {
                if (currentFirstChild.key === key) {
                  key = newChild.type;
                  if (key === REACT_FRAGMENT_TYPE) {
                    if (7 === currentFirstChild.tag) {
                      deleteRemainingChildren(
                        returnFiber,
                        currentFirstChild.sibling
                      );
                      lanes = useFiber(
                        currentFirstChild,
                        newChild.props.children
                      );
                      lanes.return = returnFiber;
                      returnFiber = lanes;
                      break a;
                    }
                  } else if (currentFirstChild.elementType === key || "object" === typeof key && null !== key && key.$$typeof === REACT_LAZY_TYPE && resolveLazy(key) === currentFirstChild.type) {
                    deleteRemainingChildren(
                      returnFiber,
                      currentFirstChild.sibling
                    );
                    lanes = useFiber(currentFirstChild, newChild.props);
                    coerceRef(lanes, newChild);
                    lanes.return = returnFiber;
                    returnFiber = lanes;
                    break a;
                  }
                  deleteRemainingChildren(returnFiber, currentFirstChild);
                  break;
                } else deleteChild(returnFiber, currentFirstChild);
                currentFirstChild = currentFirstChild.sibling;
              }
              newChild.type === REACT_FRAGMENT_TYPE ? (lanes = createFiberFromFragment(
                newChild.props.children,
                returnFiber.mode,
                lanes,
                newChild.key
              ), lanes.return = returnFiber, returnFiber = lanes) : (lanes = createFiberFromTypeAndProps(
                newChild.type,
                newChild.key,
                newChild.props,
                null,
                returnFiber.mode,
                lanes
              ), coerceRef(lanes, newChild), lanes.return = returnFiber, returnFiber = lanes);
            }
            return placeSingleChild(returnFiber);
          case REACT_PORTAL_TYPE:
            a: {
              for (key = newChild.key; null !== currentFirstChild; ) {
                if (currentFirstChild.key === key)
                  if (4 === currentFirstChild.tag && currentFirstChild.stateNode.containerInfo === newChild.containerInfo && currentFirstChild.stateNode.implementation === newChild.implementation) {
                    deleteRemainingChildren(
                      returnFiber,
                      currentFirstChild.sibling
                    );
                    lanes = useFiber(currentFirstChild, newChild.children || []);
                    lanes.return = returnFiber;
                    returnFiber = lanes;
                    break a;
                  } else {
                    deleteRemainingChildren(returnFiber, currentFirstChild);
                    break;
                  }
                else deleteChild(returnFiber, currentFirstChild);
                currentFirstChild = currentFirstChild.sibling;
              }
              lanes = createFiberFromPortal(newChild, returnFiber.mode, lanes);
              lanes.return = returnFiber;
              returnFiber = lanes;
            }
            return placeSingleChild(returnFiber);
          case REACT_LAZY_TYPE:
            return newChild = resolveLazy(newChild), reconcileChildFibersImpl(
              returnFiber,
              currentFirstChild,
              newChild,
              lanes
            );
        }
        if (isArrayImpl(newChild))
          return reconcileChildrenArray(
            returnFiber,
            currentFirstChild,
            newChild,
            lanes
          );
        if (getIteratorFn(newChild)) {
          key = getIteratorFn(newChild);
          if ("function" !== typeof key) throw Error(formatProdErrorMessage(150));
          newChild = key.call(newChild);
          return reconcileChildrenIterator(
            returnFiber,
            currentFirstChild,
            newChild,
            lanes
          );
        }
        if ("function" === typeof newChild.then)
          return reconcileChildFibersImpl(
            returnFiber,
            currentFirstChild,
            unwrapThenable(newChild),
            lanes
          );
        if (newChild.$$typeof === REACT_CONTEXT_TYPE)
          return reconcileChildFibersImpl(
            returnFiber,
            currentFirstChild,
            readContextDuringReconciliation(returnFiber, newChild),
            lanes
          );
        throwOnInvalidObjectTypeImpl(returnFiber, newChild);
      }
      return "string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild ? (newChild = "" + newChild, null !== currentFirstChild && 6 === currentFirstChild.tag ? (deleteRemainingChildren(returnFiber, currentFirstChild.sibling), lanes = useFiber(currentFirstChild, newChild), lanes.return = returnFiber, returnFiber = lanes) : (deleteRemainingChildren(returnFiber, currentFirstChild), lanes = createFiberFromText(newChild, returnFiber.mode, lanes), lanes.return = returnFiber, returnFiber = lanes), placeSingleChild(returnFiber)) : deleteRemainingChildren(returnFiber, currentFirstChild);
    }
    return function(returnFiber, currentFirstChild, newChild, lanes) {
      try {
        thenableIndexCounter$1 = 0;
        var firstChildFiber = reconcileChildFibersImpl(
          returnFiber,
          currentFirstChild,
          newChild,
          lanes
        );
        thenableState$1 = null;
        return firstChildFiber;
      } catch (x) {
        if (x === SuspenseException || x === SuspenseActionException) throw x;
        var fiber = createFiberImplClass(29, x, null, returnFiber.mode);
        fiber.lanes = lanes;
        fiber.return = returnFiber;
        return fiber;
      } finally {
      }
    };
  }
  var reconcileChildFibers = createChildReconciler(true), mountChildFibers = createChildReconciler(false), hasForceUpdate = false;
  function initializeUpdateQueue(fiber) {
    fiber.updateQueue = {
      baseState: fiber.memoizedState,
      firstBaseUpdate: null,
      lastBaseUpdate: null,
      shared: { pending: null, lanes: 0, hiddenCallbacks: null },
      callbacks: null
    };
  }
  function cloneUpdateQueue(current, workInProgress2) {
    current = current.updateQueue;
    workInProgress2.updateQueue === current && (workInProgress2.updateQueue = {
      baseState: current.baseState,
      firstBaseUpdate: current.firstBaseUpdate,
      lastBaseUpdate: current.lastBaseUpdate,
      shared: current.shared,
      callbacks: null
    });
  }
  function createUpdate(lane) {
    return { lane, tag: 0, payload: null, callback: null, next: null };
  }
  function enqueueUpdate(fiber, update, lane) {
    var updateQueue = fiber.updateQueue;
    if (null === updateQueue) return null;
    updateQueue = updateQueue.shared;
    if (0 !== (executionContext & 2)) {
      var pending = updateQueue.pending;
      null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
      updateQueue.pending = update;
      update = getRootForUpdatedFiber(fiber);
      markUpdateLaneFromFiberToRoot(fiber, null, lane);
      return update;
    }
    enqueueUpdate$1(fiber, updateQueue, update, lane);
    return getRootForUpdatedFiber(fiber);
  }
  function entangleTransitions(root3, fiber, lane) {
    fiber = fiber.updateQueue;
    if (null !== fiber && (fiber = fiber.shared, 0 !== (lane & 4194048))) {
      var queueLanes = fiber.lanes;
      queueLanes &= root3.pendingLanes;
      lane |= queueLanes;
      fiber.lanes = lane;
      markRootEntangled(root3, lane);
    }
  }
  function enqueueCapturedUpdate(workInProgress2, capturedUpdate) {
    var queue = workInProgress2.updateQueue, current = workInProgress2.alternate;
    if (null !== current && (current = current.updateQueue, queue === current)) {
      var newFirst = null, newLast = null;
      queue = queue.firstBaseUpdate;
      if (null !== queue) {
        do {
          var clone = {
            lane: queue.lane,
            tag: queue.tag,
            payload: queue.payload,
            callback: null,
            next: null
          };
          null === newLast ? newFirst = newLast = clone : newLast = newLast.next = clone;
          queue = queue.next;
        } while (null !== queue);
        null === newLast ? newFirst = newLast = capturedUpdate : newLast = newLast.next = capturedUpdate;
      } else newFirst = newLast = capturedUpdate;
      queue = {
        baseState: current.baseState,
        firstBaseUpdate: newFirst,
        lastBaseUpdate: newLast,
        shared: current.shared,
        callbacks: current.callbacks
      };
      workInProgress2.updateQueue = queue;
      return;
    }
    workInProgress2 = queue.lastBaseUpdate;
    null === workInProgress2 ? queue.firstBaseUpdate = capturedUpdate : workInProgress2.next = capturedUpdate;
    queue.lastBaseUpdate = capturedUpdate;
  }
  var didReadFromEntangledAsyncAction = false;
  function suspendIfUpdateReadFromEntangledAsyncAction() {
    if (didReadFromEntangledAsyncAction) {
      var entangledActionThenable = currentEntangledActionThenable;
      if (null !== entangledActionThenable) throw entangledActionThenable;
    }
  }
  function processUpdateQueue(workInProgress$jscomp$0, props, instance$jscomp$0, renderLanes2) {
    didReadFromEntangledAsyncAction = false;
    var queue = workInProgress$jscomp$0.updateQueue;
    hasForceUpdate = false;
    var firstBaseUpdate = queue.firstBaseUpdate, lastBaseUpdate = queue.lastBaseUpdate, pendingQueue = queue.shared.pending;
    if (null !== pendingQueue) {
      queue.shared.pending = null;
      var lastPendingUpdate = pendingQueue, firstPendingUpdate = lastPendingUpdate.next;
      lastPendingUpdate.next = null;
      null === lastBaseUpdate ? firstBaseUpdate = firstPendingUpdate : lastBaseUpdate.next = firstPendingUpdate;
      lastBaseUpdate = lastPendingUpdate;
      var current = workInProgress$jscomp$0.alternate;
      null !== current && (current = current.updateQueue, pendingQueue = current.lastBaseUpdate, pendingQueue !== lastBaseUpdate && (null === pendingQueue ? current.firstBaseUpdate = firstPendingUpdate : pendingQueue.next = firstPendingUpdate, current.lastBaseUpdate = lastPendingUpdate));
    }
    if (null !== firstBaseUpdate) {
      var newState = queue.baseState;
      lastBaseUpdate = 0;
      current = firstPendingUpdate = lastPendingUpdate = null;
      pendingQueue = firstBaseUpdate;
      do {
        var updateLane = pendingQueue.lane & -536870913, isHiddenUpdate = updateLane !== pendingQueue.lane;
        if (isHiddenUpdate ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes2 & updateLane) === updateLane) {
          0 !== updateLane && updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction = true);
          null !== current && (current = current.next = {
            lane: 0,
            tag: pendingQueue.tag,
            payload: pendingQueue.payload,
            callback: null,
            next: null
          });
          a: {
            var workInProgress2 = workInProgress$jscomp$0, update = pendingQueue;
            updateLane = props;
            var instance = instance$jscomp$0;
            switch (update.tag) {
              case 1:
                workInProgress2 = update.payload;
                if ("function" === typeof workInProgress2) {
                  newState = workInProgress2.call(instance, newState, updateLane);
                  break a;
                }
                newState = workInProgress2;
                break a;
              case 3:
                workInProgress2.flags = workInProgress2.flags & -65537 | 128;
              case 0:
                workInProgress2 = update.payload;
                updateLane = "function" === typeof workInProgress2 ? workInProgress2.call(instance, newState, updateLane) : workInProgress2;
                if (null === updateLane || void 0 === updateLane) break a;
                newState = assign({}, newState, updateLane);
                break a;
              case 2:
                hasForceUpdate = true;
            }
          }
          updateLane = pendingQueue.callback;
          null !== updateLane && (workInProgress$jscomp$0.flags |= 64, isHiddenUpdate && (workInProgress$jscomp$0.flags |= 8192), isHiddenUpdate = queue.callbacks, null === isHiddenUpdate ? queue.callbacks = [updateLane] : isHiddenUpdate.push(updateLane));
        } else
          isHiddenUpdate = {
            lane: updateLane,
            tag: pendingQueue.tag,
            payload: pendingQueue.payload,
            callback: pendingQueue.callback,
            next: null
          }, null === current ? (firstPendingUpdate = current = isHiddenUpdate, lastPendingUpdate = newState) : current = current.next = isHiddenUpdate, lastBaseUpdate |= updateLane;
        pendingQueue = pendingQueue.next;
        if (null === pendingQueue)
          if (pendingQueue = queue.shared.pending, null === pendingQueue)
            break;
          else
            isHiddenUpdate = pendingQueue, pendingQueue = isHiddenUpdate.next, isHiddenUpdate.next = null, queue.lastBaseUpdate = isHiddenUpdate, queue.shared.pending = null;
      } while (1);
      null === current && (lastPendingUpdate = newState);
      queue.baseState = lastPendingUpdate;
      queue.firstBaseUpdate = firstPendingUpdate;
      queue.lastBaseUpdate = current;
      null === firstBaseUpdate && (queue.shared.lanes = 0);
      workInProgressRootSkippedLanes |= lastBaseUpdate;
      workInProgress$jscomp$0.lanes = lastBaseUpdate;
      workInProgress$jscomp$0.memoizedState = newState;
    }
  }
  function callCallback(callback, context) {
    if ("function" !== typeof callback)
      throw Error(formatProdErrorMessage(191, callback));
    callback.call(context);
  }
  function commitCallbacks(updateQueue, context) {
    var callbacks = updateQueue.callbacks;
    if (null !== callbacks)
      for (updateQueue.callbacks = null, updateQueue = 0; updateQueue < callbacks.length; updateQueue++)
        callCallback(callbacks[updateQueue], context);
  }
  var currentTreeHiddenStackCursor = createCursor(null), prevEntangledRenderLanesCursor = createCursor(0);
  function pushHiddenContext(fiber, context) {
    fiber = entangledRenderLanes;
    push(prevEntangledRenderLanesCursor, fiber);
    push(currentTreeHiddenStackCursor, context);
    entangledRenderLanes = fiber | context.baseLanes;
  }
  function reuseHiddenContextOnStack() {
    push(prevEntangledRenderLanesCursor, entangledRenderLanes);
    push(currentTreeHiddenStackCursor, currentTreeHiddenStackCursor.current);
  }
  function popHiddenContext() {
    entangledRenderLanes = prevEntangledRenderLanesCursor.current;
    pop(currentTreeHiddenStackCursor);
    pop(prevEntangledRenderLanesCursor);
  }
  var suspenseHandlerStackCursor = createCursor(null), shellBoundary = null;
  function pushPrimaryTreeSuspenseHandler(handler) {
    var current = handler.alternate;
    push(suspenseStackCursor, suspenseStackCursor.current & 1);
    push(suspenseHandlerStackCursor, handler);
    null === shellBoundary && (null === current || null !== currentTreeHiddenStackCursor.current ? shellBoundary = handler : null !== current.memoizedState && (shellBoundary = handler));
  }
  function pushDehydratedActivitySuspenseHandler(fiber) {
    push(suspenseStackCursor, suspenseStackCursor.current);
    push(suspenseHandlerStackCursor, fiber);
    null === shellBoundary && (shellBoundary = fiber);
  }
  function pushOffscreenSuspenseHandler(fiber) {
    22 === fiber.tag ? (push(suspenseStackCursor, suspenseStackCursor.current), push(suspenseHandlerStackCursor, fiber), null === shellBoundary && (shellBoundary = fiber)) : reuseSuspenseHandlerOnStack();
  }
  function reuseSuspenseHandlerOnStack() {
    push(suspenseStackCursor, suspenseStackCursor.current);
    push(suspenseHandlerStackCursor, suspenseHandlerStackCursor.current);
  }
  function popSuspenseHandler(fiber) {
    pop(suspenseHandlerStackCursor);
    shellBoundary === fiber && (shellBoundary = null);
    pop(suspenseStackCursor);
  }
  var suspenseStackCursor = createCursor(0);
  function findFirstSuspended(row) {
    for (var node = row; null !== node; ) {
      if (13 === node.tag) {
        var state = node.memoizedState;
        if (null !== state && (state = state.dehydrated, null === state || isSuspenseInstancePending(state) || isSuspenseInstanceFallback(state)))
          return node;
      } else if (19 === node.tag && ("forwards" === node.memoizedProps.revealOrder || "backwards" === node.memoizedProps.revealOrder || "unstable_legacy-backwards" === node.memoizedProps.revealOrder || "together" === node.memoizedProps.revealOrder)) {
        if (0 !== (node.flags & 128)) return node;
      } else if (null !== node.child) {
        node.child.return = node;
        node = node.child;
        continue;
      }
      if (node === row) break;
      for (; null === node.sibling; ) {
        if (null === node.return || node.return === row) return null;
        node = node.return;
      }
      node.sibling.return = node.return;
      node = node.sibling;
    }
    return null;
  }
  var renderLanes = 0, currentlyRenderingFiber = null, currentHook = null, workInProgressHook = null, didScheduleRenderPhaseUpdate = false, didScheduleRenderPhaseUpdateDuringThisPass = false, shouldDoubleInvokeUserFnsInHooksDEV = false, localIdCounter = 0, thenableIndexCounter = 0, thenableState = null, globalClientIdCounter = 0;
  function throwInvalidHookError() {
    throw Error(formatProdErrorMessage(321));
  }
  function areHookInputsEqual(nextDeps, prevDeps) {
    if (null === prevDeps) return false;
    for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++)
      if (!objectIs(nextDeps[i], prevDeps[i])) return false;
    return true;
  }
  function renderWithHooks(current, workInProgress2, Component, props, secondArg, nextRenderLanes) {
    renderLanes = nextRenderLanes;
    currentlyRenderingFiber = workInProgress2;
    workInProgress2.memoizedState = null;
    workInProgress2.updateQueue = null;
    workInProgress2.lanes = 0;
    ReactSharedInternals.H = null === current || null === current.memoizedState ? HooksDispatcherOnMount : HooksDispatcherOnUpdate;
    shouldDoubleInvokeUserFnsInHooksDEV = false;
    nextRenderLanes = Component(props, secondArg);
    shouldDoubleInvokeUserFnsInHooksDEV = false;
    didScheduleRenderPhaseUpdateDuringThisPass && (nextRenderLanes = renderWithHooksAgain(
      workInProgress2,
      Component,
      props,
      secondArg
    ));
    finishRenderingHooks(current);
    return nextRenderLanes;
  }
  function finishRenderingHooks(current) {
    ReactSharedInternals.H = ContextOnlyDispatcher;
    var didRenderTooFewHooks = null !== currentHook && null !== currentHook.next;
    renderLanes = 0;
    workInProgressHook = currentHook = currentlyRenderingFiber = null;
    didScheduleRenderPhaseUpdate = false;
    thenableIndexCounter = 0;
    thenableState = null;
    if (didRenderTooFewHooks) throw Error(formatProdErrorMessage(300));
    null === current || didReceiveUpdate || (current = current.dependencies, null !== current && checkIfContextChanged(current) && (didReceiveUpdate = true));
  }
  function renderWithHooksAgain(workInProgress2, Component, props, secondArg) {
    currentlyRenderingFiber = workInProgress2;
    var numberOfReRenders = 0;
    do {
      didScheduleRenderPhaseUpdateDuringThisPass && (thenableState = null);
      thenableIndexCounter = 0;
      didScheduleRenderPhaseUpdateDuringThisPass = false;
      if (25 <= numberOfReRenders) throw Error(formatProdErrorMessage(301));
      numberOfReRenders += 1;
      workInProgressHook = currentHook = null;
      if (null != workInProgress2.updateQueue) {
        var children = workInProgress2.updateQueue;
        children.lastEffect = null;
        children.events = null;
        children.stores = null;
        null != children.memoCache && (children.memoCache.index = 0);
      }
      ReactSharedInternals.H = HooksDispatcherOnRerender;
      children = Component(props, secondArg);
    } while (didScheduleRenderPhaseUpdateDuringThisPass);
    return children;
  }
  function TransitionAwareHostComponent() {
    var dispatcher = ReactSharedInternals.H, maybeThenable = dispatcher.useState()[0];
    maybeThenable = "function" === typeof maybeThenable.then ? useThenable(maybeThenable) : maybeThenable;
    dispatcher = dispatcher.useState()[0];
    (null !== currentHook ? currentHook.memoizedState : null) !== dispatcher && (currentlyRenderingFiber.flags |= 1024);
    return maybeThenable;
  }
  function checkDidRenderIdHook() {
    var didRenderIdHook = 0 !== localIdCounter;
    localIdCounter = 0;
    return didRenderIdHook;
  }
  function bailoutHooks(current, workInProgress2, lanes) {
    workInProgress2.updateQueue = current.updateQueue;
    workInProgress2.flags &= -2053;
    current.lanes &= ~lanes;
  }
  function resetHooksOnUnwind(workInProgress2) {
    if (didScheduleRenderPhaseUpdate) {
      for (workInProgress2 = workInProgress2.memoizedState; null !== workInProgress2; ) {
        var queue = workInProgress2.queue;
        null !== queue && (queue.pending = null);
        workInProgress2 = workInProgress2.next;
      }
      didScheduleRenderPhaseUpdate = false;
    }
    renderLanes = 0;
    workInProgressHook = currentHook = currentlyRenderingFiber = null;
    didScheduleRenderPhaseUpdateDuringThisPass = false;
    thenableIndexCounter = localIdCounter = 0;
    thenableState = null;
  }
  function mountWorkInProgressHook() {
    var hook = {
      memoizedState: null,
      baseState: null,
      baseQueue: null,
      queue: null,
      next: null
    };
    null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = hook : workInProgressHook = workInProgressHook.next = hook;
    return workInProgressHook;
  }
  function updateWorkInProgressHook() {
    if (null === currentHook) {
      var nextCurrentHook = currentlyRenderingFiber.alternate;
      nextCurrentHook = null !== nextCurrentHook ? nextCurrentHook.memoizedState : null;
    } else nextCurrentHook = currentHook.next;
    var nextWorkInProgressHook = null === workInProgressHook ? currentlyRenderingFiber.memoizedState : workInProgressHook.next;
    if (null !== nextWorkInProgressHook)
      workInProgressHook = nextWorkInProgressHook, currentHook = nextCurrentHook;
    else {
      if (null === nextCurrentHook) {
        if (null === currentlyRenderingFiber.alternate)
          throw Error(formatProdErrorMessage(467));
        throw Error(formatProdErrorMessage(310));
      }
      currentHook = nextCurrentHook;
      nextCurrentHook = {
        memoizedState: currentHook.memoizedState,
        baseState: currentHook.baseState,
        baseQueue: currentHook.baseQueue,
        queue: currentHook.queue,
        next: null
      };
      null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = nextCurrentHook : workInProgressHook = workInProgressHook.next = nextCurrentHook;
    }
    return workInProgressHook;
  }
  function createFunctionComponentUpdateQueue() {
    return { lastEffect: null, events: null, stores: null, memoCache: null };
  }
  function useThenable(thenable) {
    var index2 = thenableIndexCounter;
    thenableIndexCounter += 1;
    null === thenableState && (thenableState = []);
    thenable = trackUsedThenable(thenableState, thenable, index2);
    index2 = currentlyRenderingFiber;
    null === (null === workInProgressHook ? index2.memoizedState : workInProgressHook.next) && (index2 = index2.alternate, ReactSharedInternals.H = null === index2 || null === index2.memoizedState ? HooksDispatcherOnMount : HooksDispatcherOnUpdate);
    return thenable;
  }
  function use(usable) {
    if (null !== usable && "object" === typeof usable) {
      if ("function" === typeof usable.then) return useThenable(usable);
      if (usable.$$typeof === REACT_CONTEXT_TYPE) return readContext(usable);
    }
    throw Error(formatProdErrorMessage(438, String(usable)));
  }
  function useMemoCache(size) {
    var memoCache = null, updateQueue = currentlyRenderingFiber.updateQueue;
    null !== updateQueue && (memoCache = updateQueue.memoCache);
    if (null == memoCache) {
      var current = currentlyRenderingFiber.alternate;
      null !== current && (current = current.updateQueue, null !== current && (current = current.memoCache, null != current && (memoCache = {
        data: current.data.map(function(array) {
          return array.slice();
        }),
        index: 0
      })));
    }
    null == memoCache && (memoCache = { data: [], index: 0 });
    null === updateQueue && (updateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = updateQueue);
    updateQueue.memoCache = memoCache;
    updateQueue = memoCache.data[memoCache.index];
    if (void 0 === updateQueue)
      for (updateQueue = memoCache.data[memoCache.index] = Array(size), current = 0; current < size; current++)
        updateQueue[current] = REACT_MEMO_CACHE_SENTINEL;
    memoCache.index++;
    return updateQueue;
  }
  function basicStateReducer(state, action) {
    return "function" === typeof action ? action(state) : action;
  }
  function updateReducer(reducer) {
    var hook = updateWorkInProgressHook();
    return updateReducerImpl(hook, currentHook, reducer);
  }
  function updateReducerImpl(hook, current, reducer) {
    var queue = hook.queue;
    if (null === queue) throw Error(formatProdErrorMessage(311));
    queue.lastRenderedReducer = reducer;
    var baseQueue = hook.baseQueue, pendingQueue = queue.pending;
    if (null !== pendingQueue) {
      if (null !== baseQueue) {
        var baseFirst = baseQueue.next;
        baseQueue.next = pendingQueue.next;
        pendingQueue.next = baseFirst;
      }
      current.baseQueue = baseQueue = pendingQueue;
      queue.pending = null;
    }
    pendingQueue = hook.baseState;
    if (null === baseQueue) hook.memoizedState = pendingQueue;
    else {
      current = baseQueue.next;
      var newBaseQueueFirst = baseFirst = null, newBaseQueueLast = null, update = current, didReadFromEntangledAsyncAction$60 = false;
      do {
        var updateLane = update.lane & -536870913;
        if (updateLane !== update.lane ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes & updateLane) === updateLane) {
          var revertLane = update.revertLane;
          if (0 === revertLane)
            null !== newBaseQueueLast && (newBaseQueueLast = newBaseQueueLast.next = {
              lane: 0,
              revertLane: 0,
              gesture: null,
              action: update.action,
              hasEagerState: update.hasEagerState,
              eagerState: update.eagerState,
              next: null
            }), updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction$60 = true);
          else if ((renderLanes & revertLane) === revertLane) {
            update = update.next;
            revertLane === currentEntangledLane && (didReadFromEntangledAsyncAction$60 = true);
            continue;
          } else
            updateLane = {
              lane: 0,
              revertLane: update.revertLane,
              gesture: null,
              action: update.action,
              hasEagerState: update.hasEagerState,
              eagerState: update.eagerState,
              next: null
            }, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = updateLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = updateLane, currentlyRenderingFiber.lanes |= revertLane, workInProgressRootSkippedLanes |= revertLane;
          updateLane = update.action;
          shouldDoubleInvokeUserFnsInHooksDEV && reducer(pendingQueue, updateLane);
          pendingQueue = update.hasEagerState ? update.eagerState : reducer(pendingQueue, updateLane);
        } else
          revertLane = {
            lane: updateLane,
            revertLane: update.revertLane,
            gesture: update.gesture,
            action: update.action,
            hasEagerState: update.hasEagerState,
            eagerState: update.eagerState,
            next: null
          }, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = revertLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = revertLane, currentlyRenderingFiber.lanes |= updateLane, workInProgressRootSkippedLanes |= updateLane;
        update = update.next;
      } while (null !== update && update !== current);
      null === newBaseQueueLast ? baseFirst = pendingQueue : newBaseQueueLast.next = newBaseQueueFirst;
      if (!objectIs(pendingQueue, hook.memoizedState) && (didReceiveUpdate = true, didReadFromEntangledAsyncAction$60 && (reducer = currentEntangledActionThenable, null !== reducer)))
        throw reducer;
      hook.memoizedState = pendingQueue;
      hook.baseState = baseFirst;
      hook.baseQueue = newBaseQueueLast;
      queue.lastRenderedState = pendingQueue;
    }
    null === baseQueue && (queue.lanes = 0);
    return [hook.memoizedState, queue.dispatch];
  }
  function rerenderReducer(reducer) {
    var hook = updateWorkInProgressHook(), queue = hook.queue;
    if (null === queue) throw Error(formatProdErrorMessage(311));
    queue.lastRenderedReducer = reducer;
    var dispatch = queue.dispatch, lastRenderPhaseUpdate = queue.pending, newState = hook.memoizedState;
    if (null !== lastRenderPhaseUpdate) {
      queue.pending = null;
      var update = lastRenderPhaseUpdate = lastRenderPhaseUpdate.next;
      do
        newState = reducer(newState, update.action), update = update.next;
      while (update !== lastRenderPhaseUpdate);
      objectIs(newState, hook.memoizedState) || (didReceiveUpdate = true);
      hook.memoizedState = newState;
      null === hook.baseQueue && (hook.baseState = newState);
      queue.lastRenderedState = newState;
    }
    return [newState, dispatch];
  }
  function updateSyncExternalStore(subscribe, getSnapshot, getServerSnapshot) {
    var fiber = currentlyRenderingFiber, hook = updateWorkInProgressHook(), isHydrating$jscomp$0 = isHydrating;
    if (isHydrating$jscomp$0) {
      if (void 0 === getServerSnapshot) throw Error(formatProdErrorMessage(407));
      getServerSnapshot = getServerSnapshot();
    } else getServerSnapshot = getSnapshot();
    var snapshotChanged = !objectIs(
      (currentHook || hook).memoizedState,
      getServerSnapshot
    );
    snapshotChanged && (hook.memoizedState = getServerSnapshot, didReceiveUpdate = true);
    hook = hook.queue;
    updateEffect(subscribeToStore.bind(null, fiber, hook, subscribe), [
      subscribe
    ]);
    if (hook.getSnapshot !== getSnapshot || snapshotChanged || null !== workInProgressHook && workInProgressHook.memoizedState.tag & 1) {
      fiber.flags |= 2048;
      pushSimpleEffect(
        9,
        { destroy: void 0 },
        updateStoreInstance.bind(
          null,
          fiber,
          hook,
          getServerSnapshot,
          getSnapshot
        ),
        null
      );
      if (null === workInProgressRoot) throw Error(formatProdErrorMessage(349));
      isHydrating$jscomp$0 || 0 !== (renderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
    }
    return getServerSnapshot;
  }
  function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
    fiber.flags |= 16384;
    fiber = { getSnapshot, value: renderedSnapshot };
    getSnapshot = currentlyRenderingFiber.updateQueue;
    null === getSnapshot ? (getSnapshot = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = getSnapshot, getSnapshot.stores = [fiber]) : (renderedSnapshot = getSnapshot.stores, null === renderedSnapshot ? getSnapshot.stores = [fiber] : renderedSnapshot.push(fiber));
  }
  function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
    inst.value = nextSnapshot;
    inst.getSnapshot = getSnapshot;
    checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
  }
  function subscribeToStore(fiber, inst, subscribe) {
    return subscribe(function() {
      checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
    });
  }
  function checkIfSnapshotChanged(inst) {
    var latestGetSnapshot = inst.getSnapshot;
    inst = inst.value;
    try {
      var nextValue = latestGetSnapshot();
      return !objectIs(inst, nextValue);
    } catch (error) {
      return true;
    }
  }
  function forceStoreRerender(fiber) {
    var root3 = enqueueConcurrentRenderForLane(fiber, 2);
    null !== root3 && scheduleUpdateOnFiber(root3, fiber, 2);
  }
  function mountStateImpl(initialState) {
    var hook = mountWorkInProgressHook();
    if ("function" === typeof initialState) {
      var initialStateInitializer = initialState;
      initialState = initialStateInitializer();
      if (shouldDoubleInvokeUserFnsInHooksDEV) {
        setIsStrictModeForDevtools(true);
        try {
          initialStateInitializer();
        } finally {
          setIsStrictModeForDevtools(false);
        }
      }
    }
    hook.memoizedState = hook.baseState = initialState;
    hook.queue = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: basicStateReducer,
      lastRenderedState: initialState
    };
    return hook;
  }
  function updateOptimisticImpl(hook, current, passthrough, reducer) {
    hook.baseState = passthrough;
    return updateReducerImpl(
      hook,
      currentHook,
      "function" === typeof reducer ? reducer : basicStateReducer
    );
  }
  function dispatchActionState(fiber, actionQueue, setPendingState, setState, payload) {
    if (isRenderPhaseUpdate(fiber)) throw Error(formatProdErrorMessage(485));
    fiber = actionQueue.action;
    if (null !== fiber) {
      var actionNode = {
        payload,
        action: fiber,
        next: null,
        isTransition: true,
        status: "pending",
        value: null,
        reason: null,
        listeners: [],
        then: function(listener) {
          actionNode.listeners.push(listener);
        }
      };
      null !== ReactSharedInternals.T ? setPendingState(true) : actionNode.isTransition = false;
      setState(actionNode);
      setPendingState = actionQueue.pending;
      null === setPendingState ? (actionNode.next = actionQueue.pending = actionNode, runActionStateAction(actionQueue, actionNode)) : (actionNode.next = setPendingState.next, actionQueue.pending = setPendingState.next = actionNode);
    }
  }
  function runActionStateAction(actionQueue, node) {
    var action = node.action, payload = node.payload, prevState = actionQueue.state;
    if (node.isTransition) {
      var prevTransition = ReactSharedInternals.T, currentTransition = {};
      ReactSharedInternals.T = currentTransition;
      try {
        var returnValue = action(prevState, payload), onStartTransitionFinish = ReactSharedInternals.S;
        null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
        handleActionReturnValue(actionQueue, node, returnValue);
      } catch (error) {
        onActionError(actionQueue, node, error);
      } finally {
        null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
      }
    } else
      try {
        prevTransition = action(prevState, payload), handleActionReturnValue(actionQueue, node, prevTransition);
      } catch (error$66) {
        onActionError(actionQueue, node, error$66);
      }
  }
  function handleActionReturnValue(actionQueue, node, returnValue) {
    null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then ? returnValue.then(
      function(nextState) {
        onActionSuccess(actionQueue, node, nextState);
      },
      function(error) {
        return onActionError(actionQueue, node, error);
      }
    ) : onActionSuccess(actionQueue, node, returnValue);
  }
  function onActionSuccess(actionQueue, actionNode, nextState) {
    actionNode.status = "fulfilled";
    actionNode.value = nextState;
    notifyActionListeners(actionNode);
    actionQueue.state = nextState;
    actionNode = actionQueue.pending;
    null !== actionNode && (nextState = actionNode.next, nextState === actionNode ? actionQueue.pending = null : (nextState = nextState.next, actionNode.next = nextState, runActionStateAction(actionQueue, nextState)));
  }
  function onActionError(actionQueue, actionNode, error) {
    var last = actionQueue.pending;
    actionQueue.pending = null;
    if (null !== last) {
      last = last.next;
      do
        actionNode.status = "rejected", actionNode.reason = error, notifyActionListeners(actionNode), actionNode = actionNode.next;
      while (actionNode !== last);
    }
    actionQueue.action = null;
  }
  function notifyActionListeners(actionNode) {
    actionNode = actionNode.listeners;
    for (var i = 0; i < actionNode.length; i++) (0, actionNode[i])();
  }
  function actionStateReducer(oldState, newState) {
    return newState;
  }
  function mountActionState(action, initialStateProp) {
    if (isHydrating) {
      var ssrFormState = workInProgressRoot.formState;
      if (null !== ssrFormState) {
        a: {
          var JSCompiler_inline_result = currentlyRenderingFiber;
          if (isHydrating) {
            if (nextHydratableInstance) {
              b: {
                var JSCompiler_inline_result$jscomp$0 = nextHydratableInstance;
                for (var inRootOrSingleton = rootOrSingletonContext; 8 !== JSCompiler_inline_result$jscomp$0.nodeType; ) {
                  if (!inRootOrSingleton) {
                    JSCompiler_inline_result$jscomp$0 = null;
                    break b;
                  }
                  JSCompiler_inline_result$jscomp$0 = getNextHydratable(
                    JSCompiler_inline_result$jscomp$0.nextSibling
                  );
                  if (null === JSCompiler_inline_result$jscomp$0) {
                    JSCompiler_inline_result$jscomp$0 = null;
                    break b;
                  }
                }
                inRootOrSingleton = JSCompiler_inline_result$jscomp$0.data;
                JSCompiler_inline_result$jscomp$0 = "F!" === inRootOrSingleton || "F" === inRootOrSingleton ? JSCompiler_inline_result$jscomp$0 : null;
              }
              if (JSCompiler_inline_result$jscomp$0) {
                nextHydratableInstance = getNextHydratable(
                  JSCompiler_inline_result$jscomp$0.nextSibling
                );
                JSCompiler_inline_result = "F!" === JSCompiler_inline_result$jscomp$0.data;
                break a;
              }
            }
            throwOnHydrationMismatch(JSCompiler_inline_result);
          }
          JSCompiler_inline_result = false;
        }
        JSCompiler_inline_result && (initialStateProp = ssrFormState[0]);
      }
    }
    ssrFormState = mountWorkInProgressHook();
    ssrFormState.memoizedState = ssrFormState.baseState = initialStateProp;
    JSCompiler_inline_result = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: actionStateReducer,
      lastRenderedState: initialStateProp
    };
    ssrFormState.queue = JSCompiler_inline_result;
    ssrFormState = dispatchSetState.bind(
      null,
      currentlyRenderingFiber,
      JSCompiler_inline_result
    );
    JSCompiler_inline_result.dispatch = ssrFormState;
    JSCompiler_inline_result = mountStateImpl(false);
    inRootOrSingleton = dispatchOptimisticSetState.bind(
      null,
      currentlyRenderingFiber,
      false,
      JSCompiler_inline_result.queue
    );
    JSCompiler_inline_result = mountWorkInProgressHook();
    JSCompiler_inline_result$jscomp$0 = {
      state: initialStateProp,
      dispatch: null,
      action,
      pending: null
    };
    JSCompiler_inline_result.queue = JSCompiler_inline_result$jscomp$0;
    ssrFormState = dispatchActionState.bind(
      null,
      currentlyRenderingFiber,
      JSCompiler_inline_result$jscomp$0,
      inRootOrSingleton,
      ssrFormState
    );
    JSCompiler_inline_result$jscomp$0.dispatch = ssrFormState;
    JSCompiler_inline_result.memoizedState = action;
    return [initialStateProp, ssrFormState, false];
  }
  function updateActionState(action) {
    var stateHook = updateWorkInProgressHook();
    return updateActionStateImpl(stateHook, currentHook, action);
  }
  function updateActionStateImpl(stateHook, currentStateHook, action) {
    currentStateHook = updateReducerImpl(
      stateHook,
      currentStateHook,
      actionStateReducer
    )[0];
    stateHook = updateReducer(basicStateReducer)[0];
    if ("object" === typeof currentStateHook && null !== currentStateHook && "function" === typeof currentStateHook.then)
      try {
        var state = useThenable(currentStateHook);
      } catch (x) {
        if (x === SuspenseException) throw SuspenseActionException;
        throw x;
      }
    else state = currentStateHook;
    currentStateHook = updateWorkInProgressHook();
    var actionQueue = currentStateHook.queue, dispatch = actionQueue.dispatch;
    action !== currentStateHook.memoizedState && (currentlyRenderingFiber.flags |= 2048, pushSimpleEffect(
      9,
      { destroy: void 0 },
      actionStateActionEffect.bind(null, actionQueue, action),
      null
    ));
    return [state, dispatch, stateHook];
  }
  function actionStateActionEffect(actionQueue, action) {
    actionQueue.action = action;
  }
  function rerenderActionState(action) {
    var stateHook = updateWorkInProgressHook(), currentStateHook = currentHook;
    if (null !== currentStateHook)
      return updateActionStateImpl(stateHook, currentStateHook, action);
    updateWorkInProgressHook();
    stateHook = stateHook.memoizedState;
    currentStateHook = updateWorkInProgressHook();
    var dispatch = currentStateHook.queue.dispatch;
    currentStateHook.memoizedState = action;
    return [stateHook, dispatch, false];
  }
  function pushSimpleEffect(tag, inst, create, deps) {
    tag = { tag, create, deps, inst, next: null };
    inst = currentlyRenderingFiber.updateQueue;
    null === inst && (inst = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = inst);
    create = inst.lastEffect;
    null === create ? inst.lastEffect = tag.next = tag : (deps = create.next, create.next = tag, tag.next = deps, inst.lastEffect = tag);
    return tag;
  }
  function updateRef() {
    return updateWorkInProgressHook().memoizedState;
  }
  function mountEffectImpl(fiberFlags, hookFlags, create, deps) {
    var hook = mountWorkInProgressHook();
    currentlyRenderingFiber.flags |= fiberFlags;
    hook.memoizedState = pushSimpleEffect(
      1 | hookFlags,
      { destroy: void 0 },
      create,
      void 0 === deps ? null : deps
    );
  }
  function updateEffectImpl(fiberFlags, hookFlags, create, deps) {
    var hook = updateWorkInProgressHook();
    deps = void 0 === deps ? null : deps;
    var inst = hook.memoizedState.inst;
    null !== currentHook && null !== deps && areHookInputsEqual(deps, currentHook.memoizedState.deps) ? hook.memoizedState = pushSimpleEffect(hookFlags, inst, create, deps) : (currentlyRenderingFiber.flags |= fiberFlags, hook.memoizedState = pushSimpleEffect(
      1 | hookFlags,
      inst,
      create,
      deps
    ));
  }
  function mountEffect(create, deps) {
    mountEffectImpl(8390656, 8, create, deps);
  }
  function updateEffect(create, deps) {
    updateEffectImpl(2048, 8, create, deps);
  }
  function useEffectEventImpl(payload) {
    currentlyRenderingFiber.flags |= 4;
    var componentUpdateQueue = currentlyRenderingFiber.updateQueue;
    if (null === componentUpdateQueue)
      componentUpdateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = componentUpdateQueue, componentUpdateQueue.events = [payload];
    else {
      var events = componentUpdateQueue.events;
      null === events ? componentUpdateQueue.events = [payload] : events.push(payload);
    }
  }
  function updateEvent(callback) {
    var ref = updateWorkInProgressHook().memoizedState;
    useEffectEventImpl({ ref, nextImpl: callback });
    return function() {
      if (0 !== (executionContext & 2)) throw Error(formatProdErrorMessage(440));
      return ref.impl.apply(void 0, arguments);
    };
  }
  function updateInsertionEffect(create, deps) {
    return updateEffectImpl(4, 2, create, deps);
  }
  function updateLayoutEffect(create, deps) {
    return updateEffectImpl(4, 4, create, deps);
  }
  function imperativeHandleEffect(create, ref) {
    if ("function" === typeof ref) {
      create = create();
      var refCleanup = ref(create);
      return function() {
        "function" === typeof refCleanup ? refCleanup() : ref(null);
      };
    }
    if (null !== ref && void 0 !== ref)
      return create = create(), ref.current = create, function() {
        ref.current = null;
      };
  }
  function updateImperativeHandle(ref, create, deps) {
    deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
    updateEffectImpl(4, 4, imperativeHandleEffect.bind(null, create, ref), deps);
  }
  function mountDebugValue() {
  }
  function updateCallback(callback, deps) {
    var hook = updateWorkInProgressHook();
    deps = void 0 === deps ? null : deps;
    var prevState = hook.memoizedState;
    if (null !== deps && areHookInputsEqual(deps, prevState[1]))
      return prevState[0];
    hook.memoizedState = [callback, deps];
    return callback;
  }
  function updateMemo(nextCreate, deps) {
    var hook = updateWorkInProgressHook();
    deps = void 0 === deps ? null : deps;
    var prevState = hook.memoizedState;
    if (null !== deps && areHookInputsEqual(deps, prevState[1]))
      return prevState[0];
    prevState = nextCreate();
    if (shouldDoubleInvokeUserFnsInHooksDEV) {
      setIsStrictModeForDevtools(true);
      try {
        nextCreate();
      } finally {
        setIsStrictModeForDevtools(false);
      }
    }
    hook.memoizedState = [prevState, deps];
    return prevState;
  }
  function mountDeferredValueImpl(hook, value, initialValue) {
    if (void 0 === initialValue || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930))
      return hook.memoizedState = value;
    hook.memoizedState = initialValue;
    hook = requestDeferredLane();
    currentlyRenderingFiber.lanes |= hook;
    workInProgressRootSkippedLanes |= hook;
    return initialValue;
  }
  function updateDeferredValueImpl(hook, prevValue, value, initialValue) {
    if (objectIs(value, prevValue)) return value;
    if (null !== currentTreeHiddenStackCursor.current)
      return hook = mountDeferredValueImpl(hook, value, initialValue), objectIs(hook, prevValue) || (didReceiveUpdate = true), hook;
    if (0 === (renderLanes & 42) || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930))
      return didReceiveUpdate = true, hook.memoizedState = value;
    hook = requestDeferredLane();
    currentlyRenderingFiber.lanes |= hook;
    workInProgressRootSkippedLanes |= hook;
    return prevValue;
  }
  function startTransition(fiber, queue, pendingState, finishedState, callback) {
    var previousPriority = ReactDOMSharedInternals.p;
    ReactDOMSharedInternals.p = 0 !== previousPriority && 8 > previousPriority ? previousPriority : 8;
    var prevTransition = ReactSharedInternals.T, currentTransition = {};
    ReactSharedInternals.T = currentTransition;
    dispatchOptimisticSetState(fiber, false, queue, pendingState);
    try {
      var returnValue = callback(), onStartTransitionFinish = ReactSharedInternals.S;
      null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
      if (null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then) {
        var thenableForFinishedState = chainThenableValue(
          returnValue,
          finishedState
        );
        dispatchSetStateInternal(
          fiber,
          queue,
          thenableForFinishedState,
          requestUpdateLane(fiber)
        );
      } else
        dispatchSetStateInternal(
          fiber,
          queue,
          finishedState,
          requestUpdateLane(fiber)
        );
    } catch (error) {
      dispatchSetStateInternal(
        fiber,
        queue,
        { then: function() {
        }, status: "rejected", reason: error },
        requestUpdateLane()
      );
    } finally {
      ReactDOMSharedInternals.p = previousPriority, null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
    }
  }
  function noop() {
  }
  function startHostTransition(formFiber, pendingState, action, formData) {
    if (5 !== formFiber.tag) throw Error(formatProdErrorMessage(476));
    var queue = ensureFormComponentIsStateful(formFiber).queue;
    startTransition(
      formFiber,
      queue,
      pendingState,
      sharedNotPendingObject,
      null === action ? noop : function() {
        requestFormReset$1(formFiber);
        return action(formData);
      }
    );
  }
  function ensureFormComponentIsStateful(formFiber) {
    var existingStateHook = formFiber.memoizedState;
    if (null !== existingStateHook) return existingStateHook;
    existingStateHook = {
      memoizedState: sharedNotPendingObject,
      baseState: sharedNotPendingObject,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: basicStateReducer,
        lastRenderedState: sharedNotPendingObject
      },
      next: null
    };
    var initialResetState = {};
    existingStateHook.next = {
      memoizedState: initialResetState,
      baseState: initialResetState,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: basicStateReducer,
        lastRenderedState: initialResetState
      },
      next: null
    };
    formFiber.memoizedState = existingStateHook;
    formFiber = formFiber.alternate;
    null !== formFiber && (formFiber.memoizedState = existingStateHook);
    return existingStateHook;
  }
  function requestFormReset$1(formFiber) {
    var stateHook = ensureFormComponentIsStateful(formFiber);
    null === stateHook.next && (stateHook = formFiber.alternate.memoizedState);
    dispatchSetStateInternal(
      formFiber,
      stateHook.next.queue,
      {},
      requestUpdateLane()
    );
  }
  function useHostTransitionStatus() {
    return readContext(HostTransitionContext);
  }
  function updateId() {
    return updateWorkInProgressHook().memoizedState;
  }
  function updateRefresh() {
    return updateWorkInProgressHook().memoizedState;
  }
  function refreshCache(fiber) {
    for (var provider = fiber.return; null !== provider; ) {
      switch (provider.tag) {
        case 24:
        case 3:
          var lane = requestUpdateLane();
          fiber = createUpdate(lane);
          var root$69 = enqueueUpdate(provider, fiber, lane);
          null !== root$69 && (scheduleUpdateOnFiber(root$69, provider, lane), entangleTransitions(root$69, provider, lane));
          provider = { cache: createCache() };
          fiber.payload = provider;
          return;
      }
      provider = provider.return;
    }
  }
  function dispatchReducerAction(fiber, queue, action) {
    var lane = requestUpdateLane();
    action = {
      lane,
      revertLane: 0,
      gesture: null,
      action,
      hasEagerState: false,
      eagerState: null,
      next: null
    };
    isRenderPhaseUpdate(fiber) ? enqueueRenderPhaseUpdate(queue, action) : (action = enqueueConcurrentHookUpdate(fiber, queue, action, lane), null !== action && (scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane)));
  }
  function dispatchSetState(fiber, queue, action) {
    var lane = requestUpdateLane();
    dispatchSetStateInternal(fiber, queue, action, lane);
  }
  function dispatchSetStateInternal(fiber, queue, action, lane) {
    var update = {
      lane,
      revertLane: 0,
      gesture: null,
      action,
      hasEagerState: false,
      eagerState: null,
      next: null
    };
    if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
    else {
      var alternate = fiber.alternate;
      if (0 === fiber.lanes && (null === alternate || 0 === alternate.lanes) && (alternate = queue.lastRenderedReducer, null !== alternate))
        try {
          var currentState = queue.lastRenderedState, eagerState = alternate(currentState, action);
          update.hasEagerState = true;
          update.eagerState = eagerState;
          if (objectIs(eagerState, currentState))
            return enqueueUpdate$1(fiber, queue, update, 0), null === workInProgressRoot && finishQueueingConcurrentUpdates(), false;
        } catch (error) {
        } finally {
        }
      action = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
      if (null !== action)
        return scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane), true;
    }
    return false;
  }
  function dispatchOptimisticSetState(fiber, throwIfDuringRender, queue, action) {
    action = {
      lane: 2,
      revertLane: requestTransitionLane(),
      gesture: null,
      action,
      hasEagerState: false,
      eagerState: null,
      next: null
    };
    if (isRenderPhaseUpdate(fiber)) {
      if (throwIfDuringRender) throw Error(formatProdErrorMessage(479));
    } else
      throwIfDuringRender = enqueueConcurrentHookUpdate(
        fiber,
        queue,
        action,
        2
      ), null !== throwIfDuringRender && scheduleUpdateOnFiber(throwIfDuringRender, fiber, 2);
  }
  function isRenderPhaseUpdate(fiber) {
    var alternate = fiber.alternate;
    return fiber === currentlyRenderingFiber || null !== alternate && alternate === currentlyRenderingFiber;
  }
  function enqueueRenderPhaseUpdate(queue, update) {
    didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = true;
    var pending = queue.pending;
    null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
    queue.pending = update;
  }
  function entangleTransitionUpdate(root3, queue, lane) {
    if (0 !== (lane & 4194048)) {
      var queueLanes = queue.lanes;
      queueLanes &= root3.pendingLanes;
      lane |= queueLanes;
      queue.lanes = lane;
      markRootEntangled(root3, lane);
    }
  }
  var ContextOnlyDispatcher = {
    readContext,
    use,
    useCallback: throwInvalidHookError,
    useContext: throwInvalidHookError,
    useEffect: throwInvalidHookError,
    useImperativeHandle: throwInvalidHookError,
    useLayoutEffect: throwInvalidHookError,
    useInsertionEffect: throwInvalidHookError,
    useMemo: throwInvalidHookError,
    useReducer: throwInvalidHookError,
    useRef: throwInvalidHookError,
    useState: throwInvalidHookError,
    useDebugValue: throwInvalidHookError,
    useDeferredValue: throwInvalidHookError,
    useTransition: throwInvalidHookError,
    useSyncExternalStore: throwInvalidHookError,
    useId: throwInvalidHookError,
    useHostTransitionStatus: throwInvalidHookError,
    useFormState: throwInvalidHookError,
    useActionState: throwInvalidHookError,
    useOptimistic: throwInvalidHookError,
    useMemoCache: throwInvalidHookError,
    useCacheRefresh: throwInvalidHookError
  };
  ContextOnlyDispatcher.useEffectEvent = throwInvalidHookError;
  var HooksDispatcherOnMount = {
    readContext,
    use,
    useCallback: function(callback, deps) {
      mountWorkInProgressHook().memoizedState = [
        callback,
        void 0 === deps ? null : deps
      ];
      return callback;
    },
    useContext: readContext,
    useEffect: mountEffect,
    useImperativeHandle: function(ref, create, deps) {
      deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
      mountEffectImpl(
        4194308,
        4,
        imperativeHandleEffect.bind(null, create, ref),
        deps
      );
    },
    useLayoutEffect: function(create, deps) {
      return mountEffectImpl(4194308, 4, create, deps);
    },
    useInsertionEffect: function(create, deps) {
      mountEffectImpl(4, 2, create, deps);
    },
    useMemo: function(nextCreate, deps) {
      var hook = mountWorkInProgressHook();
      deps = void 0 === deps ? null : deps;
      var nextValue = nextCreate();
      if (shouldDoubleInvokeUserFnsInHooksDEV) {
        setIsStrictModeForDevtools(true);
        try {
          nextCreate();
        } finally {
          setIsStrictModeForDevtools(false);
        }
      }
      hook.memoizedState = [nextValue, deps];
      return nextValue;
    },
    useReducer: function(reducer, initialArg, init) {
      var hook = mountWorkInProgressHook();
      if (void 0 !== init) {
        var initialState = init(initialArg);
        if (shouldDoubleInvokeUserFnsInHooksDEV) {
          setIsStrictModeForDevtools(true);
          try {
            init(initialArg);
          } finally {
            setIsStrictModeForDevtools(false);
          }
        }
      } else initialState = initialArg;
      hook.memoizedState = hook.baseState = initialState;
      reducer = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: reducer,
        lastRenderedState: initialState
      };
      hook.queue = reducer;
      reducer = reducer.dispatch = dispatchReducerAction.bind(
        null,
        currentlyRenderingFiber,
        reducer
      );
      return [hook.memoizedState, reducer];
    },
    useRef: function(initialValue) {
      var hook = mountWorkInProgressHook();
      initialValue = { current: initialValue };
      return hook.memoizedState = initialValue;
    },
    useState: function(initialState) {
      initialState = mountStateImpl(initialState);
      var queue = initialState.queue, dispatch = dispatchSetState.bind(null, currentlyRenderingFiber, queue);
      queue.dispatch = dispatch;
      return [initialState.memoizedState, dispatch];
    },
    useDebugValue: mountDebugValue,
    useDeferredValue: function(value, initialValue) {
      var hook = mountWorkInProgressHook();
      return mountDeferredValueImpl(hook, value, initialValue);
    },
    useTransition: function() {
      var stateHook = mountStateImpl(false);
      stateHook = startTransition.bind(
        null,
        currentlyRenderingFiber,
        stateHook.queue,
        true,
        false
      );
      mountWorkInProgressHook().memoizedState = stateHook;
      return [false, stateHook];
    },
    useSyncExternalStore: function(subscribe, getSnapshot, getServerSnapshot) {
      var fiber = currentlyRenderingFiber, hook = mountWorkInProgressHook();
      if (isHydrating) {
        if (void 0 === getServerSnapshot)
          throw Error(formatProdErrorMessage(407));
        getServerSnapshot = getServerSnapshot();
      } else {
        getServerSnapshot = getSnapshot();
        if (null === workInProgressRoot)
          throw Error(formatProdErrorMessage(349));
        0 !== (workInProgressRootRenderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
      }
      hook.memoizedState = getServerSnapshot;
      var inst = { value: getServerSnapshot, getSnapshot };
      hook.queue = inst;
      mountEffect(subscribeToStore.bind(null, fiber, inst, subscribe), [
        subscribe
      ]);
      fiber.flags |= 2048;
      pushSimpleEffect(
        9,
        { destroy: void 0 },
        updateStoreInstance.bind(
          null,
          fiber,
          inst,
          getServerSnapshot,
          getSnapshot
        ),
        null
      );
      return getServerSnapshot;
    },
    useId: function() {
      var hook = mountWorkInProgressHook(), identifierPrefix = workInProgressRoot.identifierPrefix;
      if (isHydrating) {
        var JSCompiler_inline_result = treeContextOverflow;
        var idWithLeadingBit = treeContextId;
        JSCompiler_inline_result = (idWithLeadingBit & ~(1 << 32 - clz32(idWithLeadingBit) - 1)).toString(32) + JSCompiler_inline_result;
        identifierPrefix = "_" + identifierPrefix + "R_" + JSCompiler_inline_result;
        JSCompiler_inline_result = localIdCounter++;
        0 < JSCompiler_inline_result && (identifierPrefix += "H" + JSCompiler_inline_result.toString(32));
        identifierPrefix += "_";
      } else
        JSCompiler_inline_result = globalClientIdCounter++, identifierPrefix = "_" + identifierPrefix + "r_" + JSCompiler_inline_result.toString(32) + "_";
      return hook.memoizedState = identifierPrefix;
    },
    useHostTransitionStatus,
    useFormState: mountActionState,
    useActionState: mountActionState,
    useOptimistic: function(passthrough) {
      var hook = mountWorkInProgressHook();
      hook.memoizedState = hook.baseState = passthrough;
      var queue = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: null,
        lastRenderedState: null
      };
      hook.queue = queue;
      hook = dispatchOptimisticSetState.bind(
        null,
        currentlyRenderingFiber,
        true,
        queue
      );
      queue.dispatch = hook;
      return [passthrough, hook];
    },
    useMemoCache,
    useCacheRefresh: function() {
      return mountWorkInProgressHook().memoizedState = refreshCache.bind(
        null,
        currentlyRenderingFiber
      );
    },
    useEffectEvent: function(callback) {
      var hook = mountWorkInProgressHook(), ref = { impl: callback };
      hook.memoizedState = ref;
      return function() {
        if (0 !== (executionContext & 2))
          throw Error(formatProdErrorMessage(440));
        return ref.impl.apply(void 0, arguments);
      };
    }
  }, HooksDispatcherOnUpdate = {
    readContext,
    use,
    useCallback: updateCallback,
    useContext: readContext,
    useEffect: updateEffect,
    useImperativeHandle: updateImperativeHandle,
    useInsertionEffect: updateInsertionEffect,
    useLayoutEffect: updateLayoutEffect,
    useMemo: updateMemo,
    useReducer: updateReducer,
    useRef: updateRef,
    useState: function() {
      return updateReducer(basicStateReducer);
    },
    useDebugValue: mountDebugValue,
    useDeferredValue: function(value, initialValue) {
      var hook = updateWorkInProgressHook();
      return updateDeferredValueImpl(
        hook,
        currentHook.memoizedState,
        value,
        initialValue
      );
    },
    useTransition: function() {
      var booleanOrThenable = updateReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
      return [
        "boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable),
        start
      ];
    },
    useSyncExternalStore: updateSyncExternalStore,
    useId: updateId,
    useHostTransitionStatus,
    useFormState: updateActionState,
    useActionState: updateActionState,
    useOptimistic: function(passthrough, reducer) {
      var hook = updateWorkInProgressHook();
      return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
    },
    useMemoCache,
    useCacheRefresh: updateRefresh
  };
  HooksDispatcherOnUpdate.useEffectEvent = updateEvent;
  var HooksDispatcherOnRerender = {
    readContext,
    use,
    useCallback: updateCallback,
    useContext: readContext,
    useEffect: updateEffect,
    useImperativeHandle: updateImperativeHandle,
    useInsertionEffect: updateInsertionEffect,
    useLayoutEffect: updateLayoutEffect,
    useMemo: updateMemo,
    useReducer: rerenderReducer,
    useRef: updateRef,
    useState: function() {
      return rerenderReducer(basicStateReducer);
    },
    useDebugValue: mountDebugValue,
    useDeferredValue: function(value, initialValue) {
      var hook = updateWorkInProgressHook();
      return null === currentHook ? mountDeferredValueImpl(hook, value, initialValue) : updateDeferredValueImpl(
        hook,
        currentHook.memoizedState,
        value,
        initialValue
      );
    },
    useTransition: function() {
      var booleanOrThenable = rerenderReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
      return [
        "boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable),
        start
      ];
    },
    useSyncExternalStore: updateSyncExternalStore,
    useId: updateId,
    useHostTransitionStatus,
    useFormState: rerenderActionState,
    useActionState: rerenderActionState,
    useOptimistic: function(passthrough, reducer) {
      var hook = updateWorkInProgressHook();
      if (null !== currentHook)
        return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
      hook.baseState = passthrough;
      return [passthrough, hook.queue.dispatch];
    },
    useMemoCache,
    useCacheRefresh: updateRefresh
  };
  HooksDispatcherOnRerender.useEffectEvent = updateEvent;
  function applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, nextProps) {
    ctor = workInProgress2.memoizedState;
    getDerivedStateFromProps = getDerivedStateFromProps(nextProps, ctor);
    getDerivedStateFromProps = null === getDerivedStateFromProps || void 0 === getDerivedStateFromProps ? ctor : assign({}, ctor, getDerivedStateFromProps);
    workInProgress2.memoizedState = getDerivedStateFromProps;
    0 === workInProgress2.lanes && (workInProgress2.updateQueue.baseState = getDerivedStateFromProps);
  }
  var classComponentUpdater = {
    enqueueSetState: function(inst, payload, callback) {
      inst = inst._reactInternals;
      var lane = requestUpdateLane(), update = createUpdate(lane);
      update.payload = payload;
      void 0 !== callback && null !== callback && (update.callback = callback);
      payload = enqueueUpdate(inst, update, lane);
      null !== payload && (scheduleUpdateOnFiber(payload, inst, lane), entangleTransitions(payload, inst, lane));
    },
    enqueueReplaceState: function(inst, payload, callback) {
      inst = inst._reactInternals;
      var lane = requestUpdateLane(), update = createUpdate(lane);
      update.tag = 1;
      update.payload = payload;
      void 0 !== callback && null !== callback && (update.callback = callback);
      payload = enqueueUpdate(inst, update, lane);
      null !== payload && (scheduleUpdateOnFiber(payload, inst, lane), entangleTransitions(payload, inst, lane));
    },
    enqueueForceUpdate: function(inst, callback) {
      inst = inst._reactInternals;
      var lane = requestUpdateLane(), update = createUpdate(lane);
      update.tag = 2;
      void 0 !== callback && null !== callback && (update.callback = callback);
      callback = enqueueUpdate(inst, update, lane);
      null !== callback && (scheduleUpdateOnFiber(callback, inst, lane), entangleTransitions(callback, inst, lane));
    }
  };
  function checkShouldComponentUpdate(workInProgress2, ctor, oldProps, newProps, oldState, newState, nextContext) {
    workInProgress2 = workInProgress2.stateNode;
    return "function" === typeof workInProgress2.shouldComponentUpdate ? workInProgress2.shouldComponentUpdate(newProps, newState, nextContext) : ctor.prototype && ctor.prototype.isPureReactComponent ? !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState) : true;
  }
  function callComponentWillReceiveProps(workInProgress2, instance, newProps, nextContext) {
    workInProgress2 = instance.state;
    "function" === typeof instance.componentWillReceiveProps && instance.componentWillReceiveProps(newProps, nextContext);
    "function" === typeof instance.UNSAFE_componentWillReceiveProps && instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
    instance.state !== workInProgress2 && classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
  }
  function resolveClassComponentProps(Component, baseProps) {
    var newProps = baseProps;
    if ("ref" in baseProps) {
      newProps = {};
      for (var propName in baseProps)
        "ref" !== propName && (newProps[propName] = baseProps[propName]);
    }
    if (Component = Component.defaultProps) {
      newProps === baseProps && (newProps = assign({}, newProps));
      for (var propName$73 in Component)
        void 0 === newProps[propName$73] && (newProps[propName$73] = Component[propName$73]);
    }
    return newProps;
  }
  function defaultOnUncaughtError(error) {
    reportGlobalError(error);
  }
  function defaultOnCaughtError(error) {
    console.error(error);
  }
  function defaultOnRecoverableError(error) {
    reportGlobalError(error);
  }
  function logUncaughtError(root3, errorInfo) {
    try {
      var onUncaughtError = root3.onUncaughtError;
      onUncaughtError(errorInfo.value, { componentStack: errorInfo.stack });
    } catch (e$74) {
      setTimeout(function() {
        throw e$74;
      });
    }
  }
  function logCaughtError(root3, boundary, errorInfo) {
    try {
      var onCaughtError = root3.onCaughtError;
      onCaughtError(errorInfo.value, {
        componentStack: errorInfo.stack,
        errorBoundary: 1 === boundary.tag ? boundary.stateNode : null
      });
    } catch (e$75) {
      setTimeout(function() {
        throw e$75;
      });
    }
  }
  function createRootErrorUpdate(root3, errorInfo, lane) {
    lane = createUpdate(lane);
    lane.tag = 3;
    lane.payload = { element: null };
    lane.callback = function() {
      logUncaughtError(root3, errorInfo);
    };
    return lane;
  }
  function createClassErrorUpdate(lane) {
    lane = createUpdate(lane);
    lane.tag = 3;
    return lane;
  }
  function initializeClassErrorUpdate(update, root3, fiber, errorInfo) {
    var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
    if ("function" === typeof getDerivedStateFromError) {
      var error = errorInfo.value;
      update.payload = function() {
        return getDerivedStateFromError(error);
      };
      update.callback = function() {
        logCaughtError(root3, fiber, errorInfo);
      };
    }
    var inst = fiber.stateNode;
    null !== inst && "function" === typeof inst.componentDidCatch && (update.callback = function() {
      logCaughtError(root3, fiber, errorInfo);
      "function" !== typeof getDerivedStateFromError && (null === legacyErrorBoundariesThatAlreadyFailed ? legacyErrorBoundariesThatAlreadyFailed = /* @__PURE__ */ new Set([this]) : legacyErrorBoundariesThatAlreadyFailed.add(this));
      var stack = errorInfo.stack;
      this.componentDidCatch(errorInfo.value, {
        componentStack: null !== stack ? stack : ""
      });
    });
  }
  function throwException(root3, returnFiber, sourceFiber, value, rootRenderLanes) {
    sourceFiber.flags |= 32768;
    if (null !== value && "object" === typeof value && "function" === typeof value.then) {
      returnFiber = sourceFiber.alternate;
      null !== returnFiber && propagateParentContextChanges(
        returnFiber,
        sourceFiber,
        rootRenderLanes,
        true
      );
      sourceFiber = suspenseHandlerStackCursor.current;
      if (null !== sourceFiber) {
        switch (sourceFiber.tag) {
          case 31:
          case 13:
            return null === shellBoundary ? renderDidSuspendDelayIfPossible() : null === sourceFiber.alternate && 0 === workInProgressRootExitStatus && (workInProgressRootExitStatus = 3), sourceFiber.flags &= -257, sourceFiber.flags |= 65536, sourceFiber.lanes = rootRenderLanes, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? sourceFiber.updateQueue = /* @__PURE__ */ new Set([value]) : returnFiber.add(value), attachPingListener(root3, value, rootRenderLanes)), false;
          case 22:
            return sourceFiber.flags |= 65536, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? (returnFiber = {
              transitions: null,
              markerInstances: null,
              retryQueue: /* @__PURE__ */ new Set([value])
            }, sourceFiber.updateQueue = returnFiber) : (sourceFiber = returnFiber.retryQueue, null === sourceFiber ? returnFiber.retryQueue = /* @__PURE__ */ new Set([value]) : sourceFiber.add(value)), attachPingListener(root3, value, rootRenderLanes)), false;
        }
        throw Error(formatProdErrorMessage(435, sourceFiber.tag));
      }
      attachPingListener(root3, value, rootRenderLanes);
      renderDidSuspendDelayIfPossible();
      return false;
    }
    if (isHydrating)
      return returnFiber = suspenseHandlerStackCursor.current, null !== returnFiber ? (0 === (returnFiber.flags & 65536) && (returnFiber.flags |= 256), returnFiber.flags |= 65536, returnFiber.lanes = rootRenderLanes, value !== HydrationMismatchException && (root3 = Error(formatProdErrorMessage(422), { cause: value }), queueHydrationError(createCapturedValueAtFiber(root3, sourceFiber)))) : (value !== HydrationMismatchException && (returnFiber = Error(formatProdErrorMessage(423), {
        cause: value
      }), queueHydrationError(
        createCapturedValueAtFiber(returnFiber, sourceFiber)
      )), root3 = root3.current.alternate, root3.flags |= 65536, rootRenderLanes &= -rootRenderLanes, root3.lanes |= rootRenderLanes, value = createCapturedValueAtFiber(value, sourceFiber), rootRenderLanes = createRootErrorUpdate(
        root3.stateNode,
        value,
        rootRenderLanes
      ), enqueueCapturedUpdate(root3, rootRenderLanes), 4 !== workInProgressRootExitStatus && (workInProgressRootExitStatus = 2)), false;
    var wrapperError = Error(formatProdErrorMessage(520), { cause: value });
    wrapperError = createCapturedValueAtFiber(wrapperError, sourceFiber);
    null === workInProgressRootConcurrentErrors ? workInProgressRootConcurrentErrors = [wrapperError] : workInProgressRootConcurrentErrors.push(wrapperError);
    4 !== workInProgressRootExitStatus && (workInProgressRootExitStatus = 2);
    if (null === returnFiber) return true;
    value = createCapturedValueAtFiber(value, sourceFiber);
    sourceFiber = returnFiber;
    do {
      switch (sourceFiber.tag) {
        case 3:
          return sourceFiber.flags |= 65536, root3 = rootRenderLanes & -rootRenderLanes, sourceFiber.lanes |= root3, root3 = createRootErrorUpdate(sourceFiber.stateNode, value, root3), enqueueCapturedUpdate(sourceFiber, root3), false;
        case 1:
          if (returnFiber = sourceFiber.type, wrapperError = sourceFiber.stateNode, 0 === (sourceFiber.flags & 128) && ("function" === typeof returnFiber.getDerivedStateFromError || null !== wrapperError && "function" === typeof wrapperError.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(wrapperError))))
            return sourceFiber.flags |= 65536, rootRenderLanes &= -rootRenderLanes, sourceFiber.lanes |= rootRenderLanes, rootRenderLanes = createClassErrorUpdate(rootRenderLanes), initializeClassErrorUpdate(
              rootRenderLanes,
              root3,
              sourceFiber,
              value
            ), enqueueCapturedUpdate(sourceFiber, rootRenderLanes), false;
      }
      sourceFiber = sourceFiber.return;
    } while (null !== sourceFiber);
    return false;
  }
  var SelectiveHydrationException = Error(formatProdErrorMessage(461)), didReceiveUpdate = false;
  function reconcileChildren(current, workInProgress2, nextChildren, renderLanes2) {
    workInProgress2.child = null === current ? mountChildFibers(workInProgress2, null, nextChildren, renderLanes2) : reconcileChildFibers(
      workInProgress2,
      current.child,
      nextChildren,
      renderLanes2
    );
  }
  function updateForwardRef(current, workInProgress2, Component, nextProps, renderLanes2) {
    Component = Component.render;
    var ref = workInProgress2.ref;
    if ("ref" in nextProps) {
      var propsWithoutRef = {};
      for (var key in nextProps)
        "ref" !== key && (propsWithoutRef[key] = nextProps[key]);
    } else propsWithoutRef = nextProps;
    prepareToReadContext(workInProgress2);
    nextProps = renderWithHooks(
      current,
      workInProgress2,
      Component,
      propsWithoutRef,
      ref,
      renderLanes2
    );
    key = checkDidRenderIdHook();
    if (null !== current && !didReceiveUpdate)
      return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    isHydrating && key && pushMaterializedTreeId(workInProgress2);
    workInProgress2.flags |= 1;
    reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
    return workInProgress2.child;
  }
  function updateMemoComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    if (null === current) {
      var type = Component.type;
      if ("function" === typeof type && !shouldConstruct(type) && void 0 === type.defaultProps && null === Component.compare)
        return workInProgress2.tag = 15, workInProgress2.type = type, updateSimpleMemoComponent(
          current,
          workInProgress2,
          type,
          nextProps,
          renderLanes2
        );
      current = createFiberFromTypeAndProps(
        Component.type,
        null,
        nextProps,
        workInProgress2,
        workInProgress2.mode,
        renderLanes2
      );
      current.ref = workInProgress2.ref;
      current.return = workInProgress2;
      return workInProgress2.child = current;
    }
    type = current.child;
    if (!checkScheduledUpdateOrContext(current, renderLanes2)) {
      var prevProps = type.memoizedProps;
      Component = Component.compare;
      Component = null !== Component ? Component : shallowEqual;
      if (Component(prevProps, nextProps) && current.ref === workInProgress2.ref)
        return bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    }
    workInProgress2.flags |= 1;
    current = createWorkInProgress(type, nextProps);
    current.ref = workInProgress2.ref;
    current.return = workInProgress2;
    return workInProgress2.child = current;
  }
  function updateSimpleMemoComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    if (null !== current) {
      var prevProps = current.memoizedProps;
      if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress2.ref)
        if (didReceiveUpdate = false, workInProgress2.pendingProps = nextProps = prevProps, checkScheduledUpdateOrContext(current, renderLanes2))
          0 !== (current.flags & 131072) && (didReceiveUpdate = true);
        else
          return workInProgress2.lanes = current.lanes, bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    }
    return updateFunctionComponent(
      current,
      workInProgress2,
      Component,
      nextProps,
      renderLanes2
    );
  }
  function updateOffscreenComponent(current, workInProgress2, renderLanes2, nextProps) {
    var nextChildren = nextProps.children, prevState = null !== current ? current.memoizedState : null;
    null === current && null === workInProgress2.stateNode && (workInProgress2.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    });
    if ("hidden" === nextProps.mode) {
      if (0 !== (workInProgress2.flags & 128)) {
        prevState = null !== prevState ? prevState.baseLanes | renderLanes2 : renderLanes2;
        if (null !== current) {
          nextProps = workInProgress2.child = current.child;
          for (nextChildren = 0; null !== nextProps; )
            nextChildren = nextChildren | nextProps.lanes | nextProps.childLanes, nextProps = nextProps.sibling;
          nextProps = nextChildren & ~prevState;
        } else nextProps = 0, workInProgress2.child = null;
        return deferHiddenOffscreenComponent(
          current,
          workInProgress2,
          prevState,
          renderLanes2,
          nextProps
        );
      }
      if (0 !== (renderLanes2 & 536870912))
        workInProgress2.memoizedState = { baseLanes: 0, cachePool: null }, null !== current && pushTransition(
          workInProgress2,
          null !== prevState ? prevState.cachePool : null
        ), null !== prevState ? pushHiddenContext(workInProgress2, prevState) : reuseHiddenContextOnStack(), pushOffscreenSuspenseHandler(workInProgress2);
      else
        return nextProps = workInProgress2.lanes = 536870912, deferHiddenOffscreenComponent(
          current,
          workInProgress2,
          null !== prevState ? prevState.baseLanes | renderLanes2 : renderLanes2,
          renderLanes2,
          nextProps
        );
    } else
      null !== prevState ? (pushTransition(workInProgress2, prevState.cachePool), pushHiddenContext(workInProgress2, prevState), reuseSuspenseHandlerOnStack(), workInProgress2.memoizedState = null) : (null !== current && pushTransition(workInProgress2, null), reuseHiddenContextOnStack(), reuseSuspenseHandlerOnStack());
    reconcileChildren(current, workInProgress2, nextChildren, renderLanes2);
    return workInProgress2.child;
  }
  function bailoutOffscreenComponent(current, workInProgress2) {
    null !== current && 22 === current.tag || null !== workInProgress2.stateNode || (workInProgress2.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    });
    return workInProgress2.sibling;
  }
  function deferHiddenOffscreenComponent(current, workInProgress2, nextBaseLanes, renderLanes2, remainingChildLanes) {
    var JSCompiler_inline_result = peekCacheFromPool();
    JSCompiler_inline_result = null === JSCompiler_inline_result ? null : { parent: CacheContext._currentValue, pool: JSCompiler_inline_result };
    workInProgress2.memoizedState = {
      baseLanes: nextBaseLanes,
      cachePool: JSCompiler_inline_result
    };
    null !== current && pushTransition(workInProgress2, null);
    reuseHiddenContextOnStack();
    pushOffscreenSuspenseHandler(workInProgress2);
    null !== current && propagateParentContextChanges(current, workInProgress2, renderLanes2, true);
    workInProgress2.childLanes = remainingChildLanes;
    return null;
  }
  function mountActivityChildren(workInProgress2, nextProps) {
    nextProps = mountWorkInProgressOffscreenFiber(
      { mode: nextProps.mode, children: nextProps.children },
      workInProgress2.mode
    );
    nextProps.ref = workInProgress2.ref;
    workInProgress2.child = nextProps;
    nextProps.return = workInProgress2;
    return nextProps;
  }
  function retryActivityComponentWithoutHydrating(current, workInProgress2, renderLanes2) {
    reconcileChildFibers(workInProgress2, current.child, null, renderLanes2);
    current = mountActivityChildren(workInProgress2, workInProgress2.pendingProps);
    current.flags |= 2;
    popSuspenseHandler(workInProgress2);
    workInProgress2.memoizedState = null;
    return current;
  }
  function updateActivityComponent(current, workInProgress2, renderLanes2) {
    var nextProps = workInProgress2.pendingProps, didSuspend = 0 !== (workInProgress2.flags & 128);
    workInProgress2.flags &= -129;
    if (null === current) {
      if (isHydrating) {
        if ("hidden" === nextProps.mode)
          return current = mountActivityChildren(workInProgress2, nextProps), workInProgress2.lanes = 536870912, bailoutOffscreenComponent(null, current);
        pushDehydratedActivitySuspenseHandler(workInProgress2);
        (current = nextHydratableInstance) ? (current = canHydrateHydrationBoundary(
          current,
          rootOrSingletonContext
        ), current = null !== current && "&" === current.data ? current : null, null !== current && (workInProgress2.memoizedState = {
          dehydrated: current,
          treeContext: null !== treeContextProvider ? { id: treeContextId, overflow: treeContextOverflow } : null,
          retryLane: 536870912,
          hydrationErrors: null
        }, renderLanes2 = createFiberFromDehydratedFragment(current), renderLanes2.return = workInProgress2, workInProgress2.child = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null)) : current = null;
        if (null === current) throw throwOnHydrationMismatch(workInProgress2);
        workInProgress2.lanes = 536870912;
        return null;
      }
      return mountActivityChildren(workInProgress2, nextProps);
    }
    var prevState = current.memoizedState;
    if (null !== prevState) {
      var dehydrated = prevState.dehydrated;
      pushDehydratedActivitySuspenseHandler(workInProgress2);
      if (didSuspend)
        if (workInProgress2.flags & 256)
          workInProgress2.flags &= -257, workInProgress2 = retryActivityComponentWithoutHydrating(
            current,
            workInProgress2,
            renderLanes2
          );
        else if (null !== workInProgress2.memoizedState)
          workInProgress2.child = current.child, workInProgress2.flags |= 128, workInProgress2 = null;
        else throw Error(formatProdErrorMessage(558));
      else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress2, renderLanes2, false), didSuspend = 0 !== (renderLanes2 & current.childLanes), didReceiveUpdate || didSuspend) {
        nextProps = workInProgressRoot;
        if (null !== nextProps && (dehydrated = getBumpedLaneForHydration(nextProps, renderLanes2), 0 !== dehydrated && dehydrated !== prevState.retryLane))
          throw prevState.retryLane = dehydrated, enqueueConcurrentRenderForLane(current, dehydrated), scheduleUpdateOnFiber(nextProps, current, dehydrated), SelectiveHydrationException;
        renderDidSuspendDelayIfPossible();
        workInProgress2 = retryActivityComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        );
      } else
        current = prevState.treeContext, nextHydratableInstance = getNextHydratable(dehydrated.nextSibling), hydrationParentFiber = workInProgress2, isHydrating = true, hydrationErrors = null, rootOrSingletonContext = false, null !== current && restoreSuspendedTreeContext(workInProgress2, current), workInProgress2 = mountActivityChildren(workInProgress2, nextProps), workInProgress2.flags |= 4096;
      return workInProgress2;
    }
    current = createWorkInProgress(current.child, {
      mode: nextProps.mode,
      children: nextProps.children
    });
    current.ref = workInProgress2.ref;
    workInProgress2.child = current;
    current.return = workInProgress2;
    return current;
  }
  function markRef(current, workInProgress2) {
    var ref = workInProgress2.ref;
    if (null === ref)
      null !== current && null !== current.ref && (workInProgress2.flags |= 4194816);
    else {
      if ("function" !== typeof ref && "object" !== typeof ref)
        throw Error(formatProdErrorMessage(284));
      if (null === current || current.ref !== ref)
        workInProgress2.flags |= 4194816;
    }
  }
  function updateFunctionComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    prepareToReadContext(workInProgress2);
    Component = renderWithHooks(
      current,
      workInProgress2,
      Component,
      nextProps,
      void 0,
      renderLanes2
    );
    nextProps = checkDidRenderIdHook();
    if (null !== current && !didReceiveUpdate)
      return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    isHydrating && nextProps && pushMaterializedTreeId(workInProgress2);
    workInProgress2.flags |= 1;
    reconcileChildren(current, workInProgress2, Component, renderLanes2);
    return workInProgress2.child;
  }
  function replayFunctionComponent(current, workInProgress2, nextProps, Component, secondArg, renderLanes2) {
    prepareToReadContext(workInProgress2);
    workInProgress2.updateQueue = null;
    nextProps = renderWithHooksAgain(
      workInProgress2,
      Component,
      nextProps,
      secondArg
    );
    finishRenderingHooks(current);
    Component = checkDidRenderIdHook();
    if (null !== current && !didReceiveUpdate)
      return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
    isHydrating && Component && pushMaterializedTreeId(workInProgress2);
    workInProgress2.flags |= 1;
    reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
    return workInProgress2.child;
  }
  function updateClassComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
    prepareToReadContext(workInProgress2);
    if (null === workInProgress2.stateNode) {
      var context = emptyContextObject, contextType = Component.contextType;
      "object" === typeof contextType && null !== contextType && (context = readContext(contextType));
      context = new Component(nextProps, context);
      workInProgress2.memoizedState = null !== context.state && void 0 !== context.state ? context.state : null;
      context.updater = classComponentUpdater;
      workInProgress2.stateNode = context;
      context._reactInternals = workInProgress2;
      context = workInProgress2.stateNode;
      context.props = nextProps;
      context.state = workInProgress2.memoizedState;
      context.refs = {};
      initializeUpdateQueue(workInProgress2);
      contextType = Component.contextType;
      context.context = "object" === typeof contextType && null !== contextType ? readContext(contextType) : emptyContextObject;
      context.state = workInProgress2.memoizedState;
      contextType = Component.getDerivedStateFromProps;
      "function" === typeof contextType && (applyDerivedStateFromProps(
        workInProgress2,
        Component,
        contextType,
        nextProps
      ), context.state = workInProgress2.memoizedState);
      "function" === typeof Component.getDerivedStateFromProps || "function" === typeof context.getSnapshotBeforeUpdate || "function" !== typeof context.UNSAFE_componentWillMount && "function" !== typeof context.componentWillMount || (contextType = context.state, "function" === typeof context.componentWillMount && context.componentWillMount(), "function" === typeof context.UNSAFE_componentWillMount && context.UNSAFE_componentWillMount(), contextType !== context.state && classComponentUpdater.enqueueReplaceState(context, context.state, null), processUpdateQueue(workInProgress2, nextProps, context, renderLanes2), suspendIfUpdateReadFromEntangledAsyncAction(), context.state = workInProgress2.memoizedState);
      "function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308);
      nextProps = true;
    } else if (null === current) {
      context = workInProgress2.stateNode;
      var unresolvedOldProps = workInProgress2.memoizedProps, oldProps = resolveClassComponentProps(Component, unresolvedOldProps);
      context.props = oldProps;
      var oldContext = context.context, contextType$jscomp$0 = Component.contextType;
      contextType = emptyContextObject;
      "object" === typeof contextType$jscomp$0 && null !== contextType$jscomp$0 && (contextType = readContext(contextType$jscomp$0));
      var getDerivedStateFromProps = Component.getDerivedStateFromProps;
      contextType$jscomp$0 = "function" === typeof getDerivedStateFromProps || "function" === typeof context.getSnapshotBeforeUpdate;
      unresolvedOldProps = workInProgress2.pendingProps !== unresolvedOldProps;
      contextType$jscomp$0 || "function" !== typeof context.UNSAFE_componentWillReceiveProps && "function" !== typeof context.componentWillReceiveProps || (unresolvedOldProps || oldContext !== contextType) && callComponentWillReceiveProps(
        workInProgress2,
        context,
        nextProps,
        contextType
      );
      hasForceUpdate = false;
      var oldState = workInProgress2.memoizedState;
      context.state = oldState;
      processUpdateQueue(workInProgress2, nextProps, context, renderLanes2);
      suspendIfUpdateReadFromEntangledAsyncAction();
      oldContext = workInProgress2.memoizedState;
      unresolvedOldProps || oldState !== oldContext || hasForceUpdate ? ("function" === typeof getDerivedStateFromProps && (applyDerivedStateFromProps(
        workInProgress2,
        Component,
        getDerivedStateFromProps,
        nextProps
      ), oldContext = workInProgress2.memoizedState), (oldProps = hasForceUpdate || checkShouldComponentUpdate(
        workInProgress2,
        Component,
        oldProps,
        nextProps,
        oldState,
        oldContext,
        contextType
      )) ? (contextType$jscomp$0 || "function" !== typeof context.UNSAFE_componentWillMount && "function" !== typeof context.componentWillMount || ("function" === typeof context.componentWillMount && context.componentWillMount(), "function" === typeof context.UNSAFE_componentWillMount && context.UNSAFE_componentWillMount()), "function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308)) : ("function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308), workInProgress2.memoizedProps = nextProps, workInProgress2.memoizedState = oldContext), context.props = nextProps, context.state = oldContext, context.context = contextType, nextProps = oldProps) : ("function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308), nextProps = false);
    } else {
      context = workInProgress2.stateNode;
      cloneUpdateQueue(current, workInProgress2);
      contextType = workInProgress2.memoizedProps;
      contextType$jscomp$0 = resolveClassComponentProps(Component, contextType);
      context.props = contextType$jscomp$0;
      getDerivedStateFromProps = workInProgress2.pendingProps;
      oldState = context.context;
      oldContext = Component.contextType;
      oldProps = emptyContextObject;
      "object" === typeof oldContext && null !== oldContext && (oldProps = readContext(oldContext));
      unresolvedOldProps = Component.getDerivedStateFromProps;
      (oldContext = "function" === typeof unresolvedOldProps || "function" === typeof context.getSnapshotBeforeUpdate) || "function" !== typeof context.UNSAFE_componentWillReceiveProps && "function" !== typeof context.componentWillReceiveProps || (contextType !== getDerivedStateFromProps || oldState !== oldProps) && callComponentWillReceiveProps(
        workInProgress2,
        context,
        nextProps,
        oldProps
      );
      hasForceUpdate = false;
      oldState = workInProgress2.memoizedState;
      context.state = oldState;
      processUpdateQueue(workInProgress2, nextProps, context, renderLanes2);
      suspendIfUpdateReadFromEntangledAsyncAction();
      var newState = workInProgress2.memoizedState;
      contextType !== getDerivedStateFromProps || oldState !== newState || hasForceUpdate || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies) ? ("function" === typeof unresolvedOldProps && (applyDerivedStateFromProps(
        workInProgress2,
        Component,
        unresolvedOldProps,
        nextProps
      ), newState = workInProgress2.memoizedState), (contextType$jscomp$0 = hasForceUpdate || checkShouldComponentUpdate(
        workInProgress2,
        Component,
        contextType$jscomp$0,
        nextProps,
        oldState,
        newState,
        oldProps
      ) || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies)) ? (oldContext || "function" !== typeof context.UNSAFE_componentWillUpdate && "function" !== typeof context.componentWillUpdate || ("function" === typeof context.componentWillUpdate && context.componentWillUpdate(nextProps, newState, oldProps), "function" === typeof context.UNSAFE_componentWillUpdate && context.UNSAFE_componentWillUpdate(
        nextProps,
        newState,
        oldProps
      )), "function" === typeof context.componentDidUpdate && (workInProgress2.flags |= 4), "function" === typeof context.getSnapshotBeforeUpdate && (workInProgress2.flags |= 1024)) : ("function" !== typeof context.componentDidUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 4), "function" !== typeof context.getSnapshotBeforeUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 1024), workInProgress2.memoizedProps = nextProps, workInProgress2.memoizedState = newState), context.props = nextProps, context.state = newState, context.context = oldProps, nextProps = contextType$jscomp$0) : ("function" !== typeof context.componentDidUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 4), "function" !== typeof context.getSnapshotBeforeUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 1024), nextProps = false);
    }
    context = nextProps;
    markRef(current, workInProgress2);
    nextProps = 0 !== (workInProgress2.flags & 128);
    context || nextProps ? (context = workInProgress2.stateNode, Component = nextProps && "function" !== typeof Component.getDerivedStateFromError ? null : context.render(), workInProgress2.flags |= 1, null !== current && nextProps ? (workInProgress2.child = reconcileChildFibers(
      workInProgress2,
      current.child,
      null,
      renderLanes2
    ), workInProgress2.child = reconcileChildFibers(
      workInProgress2,
      null,
      Component,
      renderLanes2
    )) : reconcileChildren(current, workInProgress2, Component, renderLanes2), workInProgress2.memoizedState = context.state, current = workInProgress2.child) : current = bailoutOnAlreadyFinishedWork(
      current,
      workInProgress2,
      renderLanes2
    );
    return current;
  }
  function mountHostRootWithoutHydrating(current, workInProgress2, nextChildren, renderLanes2) {
    resetHydrationState();
    workInProgress2.flags |= 256;
    reconcileChildren(current, workInProgress2, nextChildren, renderLanes2);
    return workInProgress2.child;
  }
  var SUSPENDED_MARKER = {
    dehydrated: null,
    treeContext: null,
    retryLane: 0,
    hydrationErrors: null
  };
  function mountSuspenseOffscreenState(renderLanes2) {
    return { baseLanes: renderLanes2, cachePool: getSuspendedCache() };
  }
  function getRemainingWorkInPrimaryTree(current, primaryTreeDidDefer, renderLanes2) {
    current = null !== current ? current.childLanes & ~renderLanes2 : 0;
    primaryTreeDidDefer && (current |= workInProgressDeferredLane);
    return current;
  }
  function updateSuspenseComponent(current, workInProgress2, renderLanes2) {
    var nextProps = workInProgress2.pendingProps, showFallback = false, didSuspend = 0 !== (workInProgress2.flags & 128), JSCompiler_temp;
    (JSCompiler_temp = didSuspend) || (JSCompiler_temp = null !== current && null === current.memoizedState ? false : 0 !== (suspenseStackCursor.current & 2));
    JSCompiler_temp && (showFallback = true, workInProgress2.flags &= -129);
    JSCompiler_temp = 0 !== (workInProgress2.flags & 32);
    workInProgress2.flags &= -33;
    if (null === current) {
      if (isHydrating) {
        showFallback ? pushPrimaryTreeSuspenseHandler(workInProgress2) : reuseSuspenseHandlerOnStack();
        (current = nextHydratableInstance) ? (current = canHydrateHydrationBoundary(
          current,
          rootOrSingletonContext
        ), current = null !== current && "&" !== current.data ? current : null, null !== current && (workInProgress2.memoizedState = {
          dehydrated: current,
          treeContext: null !== treeContextProvider ? { id: treeContextId, overflow: treeContextOverflow } : null,
          retryLane: 536870912,
          hydrationErrors: null
        }, renderLanes2 = createFiberFromDehydratedFragment(current), renderLanes2.return = workInProgress2, workInProgress2.child = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null)) : current = null;
        if (null === current) throw throwOnHydrationMismatch(workInProgress2);
        isSuspenseInstanceFallback(current) ? workInProgress2.lanes = 32 : workInProgress2.lanes = 536870912;
        return null;
      }
      var nextPrimaryChildren = nextProps.children;
      nextProps = nextProps.fallback;
      if (showFallback)
        return reuseSuspenseHandlerOnStack(), showFallback = workInProgress2.mode, nextPrimaryChildren = mountWorkInProgressOffscreenFiber(
          { mode: "hidden", children: nextPrimaryChildren },
          showFallback
        ), nextProps = createFiberFromFragment(
          nextProps,
          showFallback,
          renderLanes2,
          null
        ), nextPrimaryChildren.return = workInProgress2, nextProps.return = workInProgress2, nextPrimaryChildren.sibling = nextProps, workInProgress2.child = nextPrimaryChildren, nextProps = workInProgress2.child, nextProps.memoizedState = mountSuspenseOffscreenState(renderLanes2), nextProps.childLanes = getRemainingWorkInPrimaryTree(
          current,
          JSCompiler_temp,
          renderLanes2
        ), workInProgress2.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(null, nextProps);
      pushPrimaryTreeSuspenseHandler(workInProgress2);
      return mountSuspensePrimaryChildren(workInProgress2, nextPrimaryChildren);
    }
    var prevState = current.memoizedState;
    if (null !== prevState && (nextPrimaryChildren = prevState.dehydrated, null !== nextPrimaryChildren)) {
      if (didSuspend)
        workInProgress2.flags & 256 ? (pushPrimaryTreeSuspenseHandler(workInProgress2), workInProgress2.flags &= -257, workInProgress2 = retrySuspenseComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        )) : null !== workInProgress2.memoizedState ? (reuseSuspenseHandlerOnStack(), workInProgress2.child = current.child, workInProgress2.flags |= 128, workInProgress2 = null) : (reuseSuspenseHandlerOnStack(), nextPrimaryChildren = nextProps.fallback, showFallback = workInProgress2.mode, nextProps = mountWorkInProgressOffscreenFiber(
          { mode: "visible", children: nextProps.children },
          showFallback
        ), nextPrimaryChildren = createFiberFromFragment(
          nextPrimaryChildren,
          showFallback,
          renderLanes2,
          null
        ), nextPrimaryChildren.flags |= 2, nextProps.return = workInProgress2, nextPrimaryChildren.return = workInProgress2, nextProps.sibling = nextPrimaryChildren, workInProgress2.child = nextProps, reconcileChildFibers(
          workInProgress2,
          current.child,
          null,
          renderLanes2
        ), nextProps = workInProgress2.child, nextProps.memoizedState = mountSuspenseOffscreenState(renderLanes2), nextProps.childLanes = getRemainingWorkInPrimaryTree(
          current,
          JSCompiler_temp,
          renderLanes2
        ), workInProgress2.memoizedState = SUSPENDED_MARKER, workInProgress2 = bailoutOffscreenComponent(null, nextProps));
      else if (pushPrimaryTreeSuspenseHandler(workInProgress2), isSuspenseInstanceFallback(nextPrimaryChildren)) {
        JSCompiler_temp = nextPrimaryChildren.nextSibling && nextPrimaryChildren.nextSibling.dataset;
        if (JSCompiler_temp) var digest = JSCompiler_temp.dgst;
        JSCompiler_temp = digest;
        nextProps = Error(formatProdErrorMessage(419));
        nextProps.stack = "";
        nextProps.digest = JSCompiler_temp;
        queueHydrationError({ value: nextProps, source: null, stack: null });
        workInProgress2 = retrySuspenseComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        );
      } else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress2, renderLanes2, false), JSCompiler_temp = 0 !== (renderLanes2 & current.childLanes), didReceiveUpdate || JSCompiler_temp) {
        JSCompiler_temp = workInProgressRoot;
        if (null !== JSCompiler_temp && (nextProps = getBumpedLaneForHydration(JSCompiler_temp, renderLanes2), 0 !== nextProps && nextProps !== prevState.retryLane))
          throw prevState.retryLane = nextProps, enqueueConcurrentRenderForLane(current, nextProps), scheduleUpdateOnFiber(JSCompiler_temp, current, nextProps), SelectiveHydrationException;
        isSuspenseInstancePending(nextPrimaryChildren) || renderDidSuspendDelayIfPossible();
        workInProgress2 = retrySuspenseComponentWithoutHydrating(
          current,
          workInProgress2,
          renderLanes2
        );
      } else
        isSuspenseInstancePending(nextPrimaryChildren) ? (workInProgress2.flags |= 192, workInProgress2.child = current.child, workInProgress2 = null) : (current = prevState.treeContext, nextHydratableInstance = getNextHydratable(
          nextPrimaryChildren.nextSibling
        ), hydrationParentFiber = workInProgress2, isHydrating = true, hydrationErrors = null, rootOrSingletonContext = false, null !== current && restoreSuspendedTreeContext(workInProgress2, current), workInProgress2 = mountSuspensePrimaryChildren(
          workInProgress2,
          nextProps.children
        ), workInProgress2.flags |= 4096);
      return workInProgress2;
    }
    if (showFallback)
      return reuseSuspenseHandlerOnStack(), nextPrimaryChildren = nextProps.fallback, showFallback = workInProgress2.mode, prevState = current.child, digest = prevState.sibling, nextProps = createWorkInProgress(prevState, {
        mode: "hidden",
        children: nextProps.children
      }), nextProps.subtreeFlags = prevState.subtreeFlags & 65011712, null !== digest ? nextPrimaryChildren = createWorkInProgress(
        digest,
        nextPrimaryChildren
      ) : (nextPrimaryChildren = createFiberFromFragment(
        nextPrimaryChildren,
        showFallback,
        renderLanes2,
        null
      ), nextPrimaryChildren.flags |= 2), nextPrimaryChildren.return = workInProgress2, nextProps.return = workInProgress2, nextProps.sibling = nextPrimaryChildren, workInProgress2.child = nextProps, bailoutOffscreenComponent(null, nextProps), nextProps = workInProgress2.child, nextPrimaryChildren = current.child.memoizedState, null === nextPrimaryChildren ? nextPrimaryChildren = mountSuspenseOffscreenState(renderLanes2) : (showFallback = nextPrimaryChildren.cachePool, null !== showFallback ? (prevState = CacheContext._currentValue, showFallback = showFallback.parent !== prevState ? { parent: prevState, pool: prevState } : showFallback) : showFallback = getSuspendedCache(), nextPrimaryChildren = {
        baseLanes: nextPrimaryChildren.baseLanes | renderLanes2,
        cachePool: showFallback
      }), nextProps.memoizedState = nextPrimaryChildren, nextProps.childLanes = getRemainingWorkInPrimaryTree(
        current,
        JSCompiler_temp,
        renderLanes2
      ), workInProgress2.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(current.child, nextProps);
    pushPrimaryTreeSuspenseHandler(workInProgress2);
    renderLanes2 = current.child;
    current = renderLanes2.sibling;
    renderLanes2 = createWorkInProgress(renderLanes2, {
      mode: "visible",
      children: nextProps.children
    });
    renderLanes2.return = workInProgress2;
    renderLanes2.sibling = null;
    null !== current && (JSCompiler_temp = workInProgress2.deletions, null === JSCompiler_temp ? (workInProgress2.deletions = [current], workInProgress2.flags |= 16) : JSCompiler_temp.push(current));
    workInProgress2.child = renderLanes2;
    workInProgress2.memoizedState = null;
    return renderLanes2;
  }
  function mountSuspensePrimaryChildren(workInProgress2, primaryChildren) {
    primaryChildren = mountWorkInProgressOffscreenFiber(
      { mode: "visible", children: primaryChildren },
      workInProgress2.mode
    );
    primaryChildren.return = workInProgress2;
    return workInProgress2.child = primaryChildren;
  }
  function mountWorkInProgressOffscreenFiber(offscreenProps, mode) {
    offscreenProps = createFiberImplClass(22, offscreenProps, null, mode);
    offscreenProps.lanes = 0;
    return offscreenProps;
  }
  function retrySuspenseComponentWithoutHydrating(current, workInProgress2, renderLanes2) {
    reconcileChildFibers(workInProgress2, current.child, null, renderLanes2);
    current = mountSuspensePrimaryChildren(
      workInProgress2,
      workInProgress2.pendingProps.children
    );
    current.flags |= 2;
    workInProgress2.memoizedState = null;
    return current;
  }
  function scheduleSuspenseWorkOnFiber(fiber, renderLanes2, propagationRoot) {
    fiber.lanes |= renderLanes2;
    var alternate = fiber.alternate;
    null !== alternate && (alternate.lanes |= renderLanes2);
    scheduleContextWorkOnParentPath(fiber.return, renderLanes2, propagationRoot);
  }
  function initSuspenseListRenderState(workInProgress2, isBackwards, tail, lastContentRow, tailMode, treeForkCount2) {
    var renderState = workInProgress2.memoizedState;
    null === renderState ? workInProgress2.memoizedState = {
      isBackwards,
      rendering: null,
      renderingStartTime: 0,
      last: lastContentRow,
      tail,
      tailMode,
      treeForkCount: treeForkCount2
    } : (renderState.isBackwards = isBackwards, renderState.rendering = null, renderState.renderingStartTime = 0, renderState.last = lastContentRow, renderState.tail = tail, renderState.tailMode = tailMode, renderState.treeForkCount = treeForkCount2);
  }
  function updateSuspenseListComponent(current, workInProgress2, renderLanes2) {
    var nextProps = workInProgress2.pendingProps, revealOrder = nextProps.revealOrder, tailMode = nextProps.tail;
    nextProps = nextProps.children;
    var suspenseContext = suspenseStackCursor.current, shouldForceFallback = 0 !== (suspenseContext & 2);
    shouldForceFallback ? (suspenseContext = suspenseContext & 1 | 2, workInProgress2.flags |= 128) : suspenseContext &= 1;
    push(suspenseStackCursor, suspenseContext);
    reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
    nextProps = isHydrating ? treeForkCount : 0;
    if (!shouldForceFallback && null !== current && 0 !== (current.flags & 128))
      a: for (current = workInProgress2.child; null !== current; ) {
        if (13 === current.tag)
          null !== current.memoizedState && scheduleSuspenseWorkOnFiber(current, renderLanes2, workInProgress2);
        else if (19 === current.tag)
          scheduleSuspenseWorkOnFiber(current, renderLanes2, workInProgress2);
        else if (null !== current.child) {
          current.child.return = current;
          current = current.child;
          continue;
        }
        if (current === workInProgress2) break a;
        for (; null === current.sibling; ) {
          if (null === current.return || current.return === workInProgress2)
            break a;
          current = current.return;
        }
        current.sibling.return = current.return;
        current = current.sibling;
      }
    switch (revealOrder) {
      case "forwards":
        renderLanes2 = workInProgress2.child;
        for (revealOrder = null; null !== renderLanes2; )
          current = renderLanes2.alternate, null !== current && null === findFirstSuspended(current) && (revealOrder = renderLanes2), renderLanes2 = renderLanes2.sibling;
        renderLanes2 = revealOrder;
        null === renderLanes2 ? (revealOrder = workInProgress2.child, workInProgress2.child = null) : (revealOrder = renderLanes2.sibling, renderLanes2.sibling = null);
        initSuspenseListRenderState(
          workInProgress2,
          false,
          revealOrder,
          renderLanes2,
          tailMode,
          nextProps
        );
        break;
      case "backwards":
      case "unstable_legacy-backwards":
        renderLanes2 = null;
        revealOrder = workInProgress2.child;
        for (workInProgress2.child = null; null !== revealOrder; ) {
          current = revealOrder.alternate;
          if (null !== current && null === findFirstSuspended(current)) {
            workInProgress2.child = revealOrder;
            break;
          }
          current = revealOrder.sibling;
          revealOrder.sibling = renderLanes2;
          renderLanes2 = revealOrder;
          revealOrder = current;
        }
        initSuspenseListRenderState(
          workInProgress2,
          true,
          renderLanes2,
          null,
          tailMode,
          nextProps
        );
        break;
      case "together":
        initSuspenseListRenderState(
          workInProgress2,
          false,
          null,
          null,
          void 0,
          nextProps
        );
        break;
      default:
        workInProgress2.memoizedState = null;
    }
    return workInProgress2.child;
  }
  function bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2) {
    null !== current && (workInProgress2.dependencies = current.dependencies);
    workInProgressRootSkippedLanes |= workInProgress2.lanes;
    if (0 === (renderLanes2 & workInProgress2.childLanes))
      if (null !== current) {
        if (propagateParentContextChanges(
          current,
          workInProgress2,
          renderLanes2,
          false
        ), 0 === (renderLanes2 & workInProgress2.childLanes))
          return null;
      } else return null;
    if (null !== current && workInProgress2.child !== current.child)
      throw Error(formatProdErrorMessage(153));
    if (null !== workInProgress2.child) {
      current = workInProgress2.child;
      renderLanes2 = createWorkInProgress(current, current.pendingProps);
      workInProgress2.child = renderLanes2;
      for (renderLanes2.return = workInProgress2; null !== current.sibling; )
        current = current.sibling, renderLanes2 = renderLanes2.sibling = createWorkInProgress(current, current.pendingProps), renderLanes2.return = workInProgress2;
      renderLanes2.sibling = null;
    }
    return workInProgress2.child;
  }
  function checkScheduledUpdateOrContext(current, renderLanes2) {
    if (0 !== (current.lanes & renderLanes2)) return true;
    current = current.dependencies;
    return null !== current && checkIfContextChanged(current) ? true : false;
  }
  function attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress2, renderLanes2) {
    switch (workInProgress2.tag) {
      case 3:
        pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
        pushProvider(workInProgress2, CacheContext, current.memoizedState.cache);
        resetHydrationState();
        break;
      case 27:
      case 5:
        pushHostContext(workInProgress2);
        break;
      case 4:
        pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
        break;
      case 10:
        pushProvider(
          workInProgress2,
          workInProgress2.type,
          workInProgress2.memoizedProps.value
        );
        break;
      case 31:
        if (null !== workInProgress2.memoizedState)
          return workInProgress2.flags |= 128, pushDehydratedActivitySuspenseHandler(workInProgress2), null;
        break;
      case 13:
        var state$102 = workInProgress2.memoizedState;
        if (null !== state$102) {
          if (null !== state$102.dehydrated)
            return pushPrimaryTreeSuspenseHandler(workInProgress2), workInProgress2.flags |= 128, null;
          if (0 !== (renderLanes2 & workInProgress2.child.childLanes))
            return updateSuspenseComponent(current, workInProgress2, renderLanes2);
          pushPrimaryTreeSuspenseHandler(workInProgress2);
          current = bailoutOnAlreadyFinishedWork(
            current,
            workInProgress2,
            renderLanes2
          );
          return null !== current ? current.sibling : null;
        }
        pushPrimaryTreeSuspenseHandler(workInProgress2);
        break;
      case 19:
        var didSuspendBefore = 0 !== (current.flags & 128);
        state$102 = 0 !== (renderLanes2 & workInProgress2.childLanes);
        state$102 || (propagateParentContextChanges(
          current,
          workInProgress2,
          renderLanes2,
          false
        ), state$102 = 0 !== (renderLanes2 & workInProgress2.childLanes));
        if (didSuspendBefore) {
          if (state$102)
            return updateSuspenseListComponent(
              current,
              workInProgress2,
              renderLanes2
            );
          workInProgress2.flags |= 128;
        }
        didSuspendBefore = workInProgress2.memoizedState;
        null !== didSuspendBefore && (didSuspendBefore.rendering = null, didSuspendBefore.tail = null, didSuspendBefore.lastEffect = null);
        push(suspenseStackCursor, suspenseStackCursor.current);
        if (state$102) break;
        else return null;
      case 22:
        return workInProgress2.lanes = 0, updateOffscreenComponent(
          current,
          workInProgress2,
          renderLanes2,
          workInProgress2.pendingProps
        );
      case 24:
        pushProvider(workInProgress2, CacheContext, current.memoizedState.cache);
    }
    return bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
  }
  function beginWork(current, workInProgress2, renderLanes2) {
    if (null !== current)
      if (current.memoizedProps !== workInProgress2.pendingProps)
        didReceiveUpdate = true;
      else {
        if (!checkScheduledUpdateOrContext(current, renderLanes2) && 0 === (workInProgress2.flags & 128))
          return didReceiveUpdate = false, attemptEarlyBailoutIfNoScheduledUpdate(
            current,
            workInProgress2,
            renderLanes2
          );
        didReceiveUpdate = 0 !== (current.flags & 131072) ? true : false;
      }
    else
      didReceiveUpdate = false, isHydrating && 0 !== (workInProgress2.flags & 1048576) && pushTreeId(workInProgress2, treeForkCount, workInProgress2.index);
    workInProgress2.lanes = 0;
    switch (workInProgress2.tag) {
      case 16:
        a: {
          var props = workInProgress2.pendingProps;
          current = resolveLazy(workInProgress2.elementType);
          workInProgress2.type = current;
          if ("function" === typeof current)
            shouldConstruct(current) ? (props = resolveClassComponentProps(current, props), workInProgress2.tag = 1, workInProgress2 = updateClassComponent(
              null,
              workInProgress2,
              current,
              props,
              renderLanes2
            )) : (workInProgress2.tag = 0, workInProgress2 = updateFunctionComponent(
              null,
              workInProgress2,
              current,
              props,
              renderLanes2
            ));
          else {
            if (void 0 !== current && null !== current) {
              var $$typeof = current.$$typeof;
              if ($$typeof === REACT_FORWARD_REF_TYPE) {
                workInProgress2.tag = 11;
                workInProgress2 = updateForwardRef(
                  null,
                  workInProgress2,
                  current,
                  props,
                  renderLanes2
                );
                break a;
              } else if ($$typeof === REACT_MEMO_TYPE) {
                workInProgress2.tag = 14;
                workInProgress2 = updateMemoComponent(
                  null,
                  workInProgress2,
                  current,
                  props,
                  renderLanes2
                );
                break a;
              }
            }
            workInProgress2 = getComponentNameFromType(current) || current;
            throw Error(formatProdErrorMessage(306, workInProgress2, ""));
          }
        }
        return workInProgress2;
      case 0:
        return updateFunctionComponent(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 1:
        return props = workInProgress2.type, $$typeof = resolveClassComponentProps(
          props,
          workInProgress2.pendingProps
        ), updateClassComponent(
          current,
          workInProgress2,
          props,
          $$typeof,
          renderLanes2
        );
      case 3:
        a: {
          pushHostContainer(
            workInProgress2,
            workInProgress2.stateNode.containerInfo
          );
          if (null === current) throw Error(formatProdErrorMessage(387));
          props = workInProgress2.pendingProps;
          var prevState = workInProgress2.memoizedState;
          $$typeof = prevState.element;
          cloneUpdateQueue(current, workInProgress2);
          processUpdateQueue(workInProgress2, props, null, renderLanes2);
          var nextState = workInProgress2.memoizedState;
          props = nextState.cache;
          pushProvider(workInProgress2, CacheContext, props);
          props !== prevState.cache && propagateContextChanges(
            workInProgress2,
            [CacheContext],
            renderLanes2,
            true
          );
          suspendIfUpdateReadFromEntangledAsyncAction();
          props = nextState.element;
          if (prevState.isDehydrated)
            if (prevState = {
              element: props,
              isDehydrated: false,
              cache: nextState.cache
            }, workInProgress2.updateQueue.baseState = prevState, workInProgress2.memoizedState = prevState, workInProgress2.flags & 256) {
              workInProgress2 = mountHostRootWithoutHydrating(
                current,
                workInProgress2,
                props,
                renderLanes2
              );
              break a;
            } else if (props !== $$typeof) {
              $$typeof = createCapturedValueAtFiber(
                Error(formatProdErrorMessage(424)),
                workInProgress2
              );
              queueHydrationError($$typeof);
              workInProgress2 = mountHostRootWithoutHydrating(
                current,
                workInProgress2,
                props,
                renderLanes2
              );
              break a;
            } else {
              current = workInProgress2.stateNode.containerInfo;
              switch (current.nodeType) {
                case 9:
                  current = current.body;
                  break;
                default:
                  current = "HTML" === current.nodeName ? current.ownerDocument.body : current;
              }
              nextHydratableInstance = getNextHydratable(current.firstChild);
              hydrationParentFiber = workInProgress2;
              isHydrating = true;
              hydrationErrors = null;
              rootOrSingletonContext = true;
              renderLanes2 = mountChildFibers(
                workInProgress2,
                null,
                props,
                renderLanes2
              );
              for (workInProgress2.child = renderLanes2; renderLanes2; )
                renderLanes2.flags = renderLanes2.flags & -3 | 4096, renderLanes2 = renderLanes2.sibling;
            }
          else {
            resetHydrationState();
            if (props === $$typeof) {
              workInProgress2 = bailoutOnAlreadyFinishedWork(
                current,
                workInProgress2,
                renderLanes2
              );
              break a;
            }
            reconcileChildren(current, workInProgress2, props, renderLanes2);
          }
          workInProgress2 = workInProgress2.child;
        }
        return workInProgress2;
      case 26:
        return markRef(current, workInProgress2), null === current ? (renderLanes2 = getResource(
          workInProgress2.type,
          null,
          workInProgress2.pendingProps,
          null
        )) ? workInProgress2.memoizedState = renderLanes2 : isHydrating || (renderLanes2 = workInProgress2.type, current = workInProgress2.pendingProps, props = getOwnerDocumentFromRootContainer(
          rootInstanceStackCursor.current
        ).createElement(renderLanes2), props[internalInstanceKey] = workInProgress2, props[internalPropsKey] = current, setInitialProperties(props, renderLanes2, current), markNodeAsHoistable(props), workInProgress2.stateNode = props) : workInProgress2.memoizedState = getResource(
          workInProgress2.type,
          current.memoizedProps,
          workInProgress2.pendingProps,
          current.memoizedState
        ), null;
      case 27:
        return pushHostContext(workInProgress2), null === current && isHydrating && (props = workInProgress2.stateNode = resolveSingletonInstance(
          workInProgress2.type,
          workInProgress2.pendingProps,
          rootInstanceStackCursor.current
        ), hydrationParentFiber = workInProgress2, rootOrSingletonContext = true, $$typeof = nextHydratableInstance, isSingletonScope(workInProgress2.type) ? (previousHydratableOnEnteringScopedSingleton = $$typeof, nextHydratableInstance = getNextHydratable(props.firstChild)) : nextHydratableInstance = $$typeof), reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), markRef(current, workInProgress2), null === current && (workInProgress2.flags |= 4194304), workInProgress2.child;
      case 5:
        if (null === current && isHydrating) {
          if ($$typeof = props = nextHydratableInstance)
            props = canHydrateInstance(
              props,
              workInProgress2.type,
              workInProgress2.pendingProps,
              rootOrSingletonContext
            ), null !== props ? (workInProgress2.stateNode = props, hydrationParentFiber = workInProgress2, nextHydratableInstance = getNextHydratable(props.firstChild), rootOrSingletonContext = false, $$typeof = true) : $$typeof = false;
          $$typeof || throwOnHydrationMismatch(workInProgress2);
        }
        pushHostContext(workInProgress2);
        $$typeof = workInProgress2.type;
        prevState = workInProgress2.pendingProps;
        nextState = null !== current ? current.memoizedProps : null;
        props = prevState.children;
        shouldSetTextContent($$typeof, prevState) ? props = null : null !== nextState && shouldSetTextContent($$typeof, nextState) && (workInProgress2.flags |= 32);
        null !== workInProgress2.memoizedState && ($$typeof = renderWithHooks(
          current,
          workInProgress2,
          TransitionAwareHostComponent,
          null,
          null,
          renderLanes2
        ), HostTransitionContext._currentValue = $$typeof);
        markRef(current, workInProgress2);
        reconcileChildren(current, workInProgress2, props, renderLanes2);
        return workInProgress2.child;
      case 6:
        if (null === current && isHydrating) {
          if (current = renderLanes2 = nextHydratableInstance)
            renderLanes2 = canHydrateTextInstance(
              renderLanes2,
              workInProgress2.pendingProps,
              rootOrSingletonContext
            ), null !== renderLanes2 ? (workInProgress2.stateNode = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null, current = true) : current = false;
          current || throwOnHydrationMismatch(workInProgress2);
        }
        return null;
      case 13:
        return updateSuspenseComponent(current, workInProgress2, renderLanes2);
      case 4:
        return pushHostContainer(
          workInProgress2,
          workInProgress2.stateNode.containerInfo
        ), props = workInProgress2.pendingProps, null === current ? workInProgress2.child = reconcileChildFibers(
          workInProgress2,
          null,
          props,
          renderLanes2
        ) : reconcileChildren(current, workInProgress2, props, renderLanes2), workInProgress2.child;
      case 11:
        return updateForwardRef(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 7:
        return reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps,
          renderLanes2
        ), workInProgress2.child;
      case 8:
        return reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), workInProgress2.child;
      case 12:
        return reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), workInProgress2.child;
      case 10:
        return props = workInProgress2.pendingProps, pushProvider(workInProgress2, workInProgress2.type, props.value), reconcileChildren(current, workInProgress2, props.children, renderLanes2), workInProgress2.child;
      case 9:
        return $$typeof = workInProgress2.type._context, props = workInProgress2.pendingProps.children, prepareToReadContext(workInProgress2), $$typeof = readContext($$typeof), props = props($$typeof), workInProgress2.flags |= 1, reconcileChildren(current, workInProgress2, props, renderLanes2), workInProgress2.child;
      case 14:
        return updateMemoComponent(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 15:
        return updateSimpleMemoComponent(
          current,
          workInProgress2,
          workInProgress2.type,
          workInProgress2.pendingProps,
          renderLanes2
        );
      case 19:
        return updateSuspenseListComponent(current, workInProgress2, renderLanes2);
      case 31:
        return updateActivityComponent(current, workInProgress2, renderLanes2);
      case 22:
        return updateOffscreenComponent(
          current,
          workInProgress2,
          renderLanes2,
          workInProgress2.pendingProps
        );
      case 24:
        return prepareToReadContext(workInProgress2), props = readContext(CacheContext), null === current ? ($$typeof = peekCacheFromPool(), null === $$typeof && ($$typeof = workInProgressRoot, prevState = createCache(), $$typeof.pooledCache = prevState, prevState.refCount++, null !== prevState && ($$typeof.pooledCacheLanes |= renderLanes2), $$typeof = prevState), workInProgress2.memoizedState = { parent: props, cache: $$typeof }, initializeUpdateQueue(workInProgress2), pushProvider(workInProgress2, CacheContext, $$typeof)) : (0 !== (current.lanes & renderLanes2) && (cloneUpdateQueue(current, workInProgress2), processUpdateQueue(workInProgress2, null, null, renderLanes2), suspendIfUpdateReadFromEntangledAsyncAction()), $$typeof = current.memoizedState, prevState = workInProgress2.memoizedState, $$typeof.parent !== props ? ($$typeof = { parent: props, cache: props }, workInProgress2.memoizedState = $$typeof, 0 === workInProgress2.lanes && (workInProgress2.memoizedState = workInProgress2.updateQueue.baseState = $$typeof), pushProvider(workInProgress2, CacheContext, props)) : (props = prevState.cache, pushProvider(workInProgress2, CacheContext, props), props !== $$typeof.cache && propagateContextChanges(
          workInProgress2,
          [CacheContext],
          renderLanes2,
          true
        ))), reconcileChildren(
          current,
          workInProgress2,
          workInProgress2.pendingProps.children,
          renderLanes2
        ), workInProgress2.child;
      case 29:
        throw workInProgress2.pendingProps;
    }
    throw Error(formatProdErrorMessage(156, workInProgress2.tag));
  }
  function markUpdate(workInProgress2) {
    workInProgress2.flags |= 4;
  }
  function preloadInstanceAndSuspendIfNeeded(workInProgress2, type, oldProps, newProps, renderLanes2) {
    if (type = 0 !== (workInProgress2.mode & 32)) type = false;
    if (type) {
      if (workInProgress2.flags |= 16777216, (renderLanes2 & 335544128) === renderLanes2)
        if (workInProgress2.stateNode.complete) workInProgress2.flags |= 8192;
        else if (shouldRemainOnPreviousScreen()) workInProgress2.flags |= 8192;
        else
          throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
    } else workInProgress2.flags &= -16777217;
  }
  function preloadResourceAndSuspendIfNeeded(workInProgress2, resource) {
    if ("stylesheet" !== resource.type || 0 !== (resource.state.loading & 4))
      workInProgress2.flags &= -16777217;
    else if (workInProgress2.flags |= 16777216, !preloadResource(resource))
      if (shouldRemainOnPreviousScreen()) workInProgress2.flags |= 8192;
      else
        throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
  }
  function scheduleRetryEffect(workInProgress2, retryQueue) {
    null !== retryQueue && (workInProgress2.flags |= 4);
    workInProgress2.flags & 16384 && (retryQueue = 22 !== workInProgress2.tag ? claimNextRetryLane() : 536870912, workInProgress2.lanes |= retryQueue, workInProgressSuspendedRetryLanes |= retryQueue);
  }
  function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
    if (!isHydrating)
      switch (renderState.tailMode) {
        case "hidden":
          hasRenderedATailFallback = renderState.tail;
          for (var lastTailNode = null; null !== hasRenderedATailFallback; )
            null !== hasRenderedATailFallback.alternate && (lastTailNode = hasRenderedATailFallback), hasRenderedATailFallback = hasRenderedATailFallback.sibling;
          null === lastTailNode ? renderState.tail = null : lastTailNode.sibling = null;
          break;
        case "collapsed":
          lastTailNode = renderState.tail;
          for (var lastTailNode$106 = null; null !== lastTailNode; )
            null !== lastTailNode.alternate && (lastTailNode$106 = lastTailNode), lastTailNode = lastTailNode.sibling;
          null === lastTailNode$106 ? hasRenderedATailFallback || null === renderState.tail ? renderState.tail = null : renderState.tail.sibling = null : lastTailNode$106.sibling = null;
      }
  }
  function bubbleProperties(completedWork) {
    var didBailout = null !== completedWork.alternate && completedWork.alternate.child === completedWork.child, newChildLanes = 0, subtreeFlags = 0;
    if (didBailout)
      for (var child$107 = completedWork.child; null !== child$107; )
        newChildLanes |= child$107.lanes | child$107.childLanes, subtreeFlags |= child$107.subtreeFlags & 65011712, subtreeFlags |= child$107.flags & 65011712, child$107.return = completedWork, child$107 = child$107.sibling;
    else
      for (child$107 = completedWork.child; null !== child$107; )
        newChildLanes |= child$107.lanes | child$107.childLanes, subtreeFlags |= child$107.subtreeFlags, subtreeFlags |= child$107.flags, child$107.return = completedWork, child$107 = child$107.sibling;
    completedWork.subtreeFlags |= subtreeFlags;
    completedWork.childLanes = newChildLanes;
    return didBailout;
  }
  function completeWork(current, workInProgress2, renderLanes2) {
    var newProps = workInProgress2.pendingProps;
    popTreeContext(workInProgress2);
    switch (workInProgress2.tag) {
      case 16:
      case 15:
      case 0:
      case 11:
      case 7:
      case 8:
      case 12:
      case 9:
      case 14:
        return bubbleProperties(workInProgress2), null;
      case 1:
        return bubbleProperties(workInProgress2), null;
      case 3:
        renderLanes2 = workInProgress2.stateNode;
        newProps = null;
        null !== current && (newProps = current.memoizedState.cache);
        workInProgress2.memoizedState.cache !== newProps && (workInProgress2.flags |= 2048);
        popProvider(CacheContext);
        popHostContainer();
        renderLanes2.pendingContext && (renderLanes2.context = renderLanes2.pendingContext, renderLanes2.pendingContext = null);
        if (null === current || null === current.child)
          popHydrationState(workInProgress2) ? markUpdate(workInProgress2) : null === current || current.memoizedState.isDehydrated && 0 === (workInProgress2.flags & 256) || (workInProgress2.flags |= 1024, upgradeHydrationErrorsToRecoverable());
        bubbleProperties(workInProgress2);
        return null;
      case 26:
        var type = workInProgress2.type, nextResource = workInProgress2.memoizedState;
        null === current ? (markUpdate(workInProgress2), null !== nextResource ? (bubbleProperties(workInProgress2), preloadResourceAndSuspendIfNeeded(workInProgress2, nextResource)) : (bubbleProperties(workInProgress2), preloadInstanceAndSuspendIfNeeded(
          workInProgress2,
          type,
          null,
          newProps,
          renderLanes2
        ))) : nextResource ? nextResource !== current.memoizedState ? (markUpdate(workInProgress2), bubbleProperties(workInProgress2), preloadResourceAndSuspendIfNeeded(workInProgress2, nextResource)) : (bubbleProperties(workInProgress2), workInProgress2.flags &= -16777217) : (current = current.memoizedProps, current !== newProps && markUpdate(workInProgress2), bubbleProperties(workInProgress2), preloadInstanceAndSuspendIfNeeded(
          workInProgress2,
          type,
          current,
          newProps,
          renderLanes2
        ));
        return null;
      case 27:
        popHostContext(workInProgress2);
        renderLanes2 = rootInstanceStackCursor.current;
        type = workInProgress2.type;
        if (null !== current && null != workInProgress2.stateNode)
          current.memoizedProps !== newProps && markUpdate(workInProgress2);
        else {
          if (!newProps) {
            if (null === workInProgress2.stateNode)
              throw Error(formatProdErrorMessage(166));
            bubbleProperties(workInProgress2);
            return null;
          }
          current = contextStackCursor.current;
          popHydrationState(workInProgress2) ? prepareToHydrateHostInstance(workInProgress2) : (current = resolveSingletonInstance(type, newProps, renderLanes2), workInProgress2.stateNode = current, markUpdate(workInProgress2));
        }
        bubbleProperties(workInProgress2);
        return null;
      case 5:
        popHostContext(workInProgress2);
        type = workInProgress2.type;
        if (null !== current && null != workInProgress2.stateNode)
          current.memoizedProps !== newProps && markUpdate(workInProgress2);
        else {
          if (!newProps) {
            if (null === workInProgress2.stateNode)
              throw Error(formatProdErrorMessage(166));
            bubbleProperties(workInProgress2);
            return null;
          }
          nextResource = contextStackCursor.current;
          if (popHydrationState(workInProgress2))
            prepareToHydrateHostInstance(workInProgress2);
          else {
            var ownerDocument = getOwnerDocumentFromRootContainer(
              rootInstanceStackCursor.current
            );
            switch (nextResource) {
              case 1:
                nextResource = ownerDocument.createElementNS(
                  "http://www.w3.org/2000/svg",
                  type
                );
                break;
              case 2:
                nextResource = ownerDocument.createElementNS(
                  "http://www.w3.org/1998/Math/MathML",
                  type
                );
                break;
              default:
                switch (type) {
                  case "svg":
                    nextResource = ownerDocument.createElementNS(
                      "http://www.w3.org/2000/svg",
                      type
                    );
                    break;
                  case "math":
                    nextResource = ownerDocument.createElementNS(
                      "http://www.w3.org/1998/Math/MathML",
                      type
                    );
                    break;
                  case "script":
                    nextResource = ownerDocument.createElement("div");
                    nextResource.innerHTML = "<script><\/script>";
                    nextResource = nextResource.removeChild(
                      nextResource.firstChild
                    );
                    break;
                  case "select":
                    nextResource = "string" === typeof newProps.is ? ownerDocument.createElement("select", {
                      is: newProps.is
                    }) : ownerDocument.createElement("select");
                    newProps.multiple ? nextResource.multiple = true : newProps.size && (nextResource.size = newProps.size);
                    break;
                  default:
                    nextResource = "string" === typeof newProps.is ? ownerDocument.createElement(type, { is: newProps.is }) : ownerDocument.createElement(type);
                }
            }
            nextResource[internalInstanceKey] = workInProgress2;
            nextResource[internalPropsKey] = newProps;
            a: for (ownerDocument = workInProgress2.child; null !== ownerDocument; ) {
              if (5 === ownerDocument.tag || 6 === ownerDocument.tag)
                nextResource.appendChild(ownerDocument.stateNode);
              else if (4 !== ownerDocument.tag && 27 !== ownerDocument.tag && null !== ownerDocument.child) {
                ownerDocument.child.return = ownerDocument;
                ownerDocument = ownerDocument.child;
                continue;
              }
              if (ownerDocument === workInProgress2) break a;
              for (; null === ownerDocument.sibling; ) {
                if (null === ownerDocument.return || ownerDocument.return === workInProgress2)
                  break a;
                ownerDocument = ownerDocument.return;
              }
              ownerDocument.sibling.return = ownerDocument.return;
              ownerDocument = ownerDocument.sibling;
            }
            workInProgress2.stateNode = nextResource;
            a: switch (setInitialProperties(nextResource, type, newProps), type) {
              case "button":
              case "input":
              case "select":
              case "textarea":
                newProps = !!newProps.autoFocus;
                break a;
              case "img":
                newProps = true;
                break a;
              default:
                newProps = false;
            }
            newProps && markUpdate(workInProgress2);
          }
        }
        bubbleProperties(workInProgress2);
        preloadInstanceAndSuspendIfNeeded(
          workInProgress2,
          workInProgress2.type,
          null === current ? null : current.memoizedProps,
          workInProgress2.pendingProps,
          renderLanes2
        );
        return null;
      case 6:
        if (current && null != workInProgress2.stateNode)
          current.memoizedProps !== newProps && markUpdate(workInProgress2);
        else {
          if ("string" !== typeof newProps && null === workInProgress2.stateNode)
            throw Error(formatProdErrorMessage(166));
          current = rootInstanceStackCursor.current;
          if (popHydrationState(workInProgress2)) {
            current = workInProgress2.stateNode;
            renderLanes2 = workInProgress2.memoizedProps;
            newProps = null;
            type = hydrationParentFiber;
            if (null !== type)
              switch (type.tag) {
                case 27:
                case 5:
                  newProps = type.memoizedProps;
              }
            current[internalInstanceKey] = workInProgress2;
            current = current.nodeValue === renderLanes2 || null !== newProps && true === newProps.suppressHydrationWarning || checkForUnmatchedText(current.nodeValue, renderLanes2) ? true : false;
            current || throwOnHydrationMismatch(workInProgress2, true);
          } else
            current = getOwnerDocumentFromRootContainer(current).createTextNode(
              newProps
            ), current[internalInstanceKey] = workInProgress2, workInProgress2.stateNode = current;
        }
        bubbleProperties(workInProgress2);
        return null;
      case 31:
        renderLanes2 = workInProgress2.memoizedState;
        if (null === current || null !== current.memoizedState) {
          newProps = popHydrationState(workInProgress2);
          if (null !== renderLanes2) {
            if (null === current) {
              if (!newProps) throw Error(formatProdErrorMessage(318));
              current = workInProgress2.memoizedState;
              current = null !== current ? current.dehydrated : null;
              if (!current) throw Error(formatProdErrorMessage(557));
              current[internalInstanceKey] = workInProgress2;
            } else
              resetHydrationState(), 0 === (workInProgress2.flags & 128) && (workInProgress2.memoizedState = null), workInProgress2.flags |= 4;
            bubbleProperties(workInProgress2);
            current = false;
          } else
            renderLanes2 = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = renderLanes2), current = true;
          if (!current) {
            if (workInProgress2.flags & 256)
              return popSuspenseHandler(workInProgress2), workInProgress2;
            popSuspenseHandler(workInProgress2);
            return null;
          }
          if (0 !== (workInProgress2.flags & 128))
            throw Error(formatProdErrorMessage(558));
        }
        bubbleProperties(workInProgress2);
        return null;
      case 13:
        newProps = workInProgress2.memoizedState;
        if (null === current || null !== current.memoizedState && null !== current.memoizedState.dehydrated) {
          type = popHydrationState(workInProgress2);
          if (null !== newProps && null !== newProps.dehydrated) {
            if (null === current) {
              if (!type) throw Error(formatProdErrorMessage(318));
              type = workInProgress2.memoizedState;
              type = null !== type ? type.dehydrated : null;
              if (!type) throw Error(formatProdErrorMessage(317));
              type[internalInstanceKey] = workInProgress2;
            } else
              resetHydrationState(), 0 === (workInProgress2.flags & 128) && (workInProgress2.memoizedState = null), workInProgress2.flags |= 4;
            bubbleProperties(workInProgress2);
            type = false;
          } else
            type = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = type), type = true;
          if (!type) {
            if (workInProgress2.flags & 256)
              return popSuspenseHandler(workInProgress2), workInProgress2;
            popSuspenseHandler(workInProgress2);
            return null;
          }
        }
        popSuspenseHandler(workInProgress2);
        if (0 !== (workInProgress2.flags & 128))
          return workInProgress2.lanes = renderLanes2, workInProgress2;
        renderLanes2 = null !== newProps;
        current = null !== current && null !== current.memoizedState;
        renderLanes2 && (newProps = workInProgress2.child, type = null, null !== newProps.alternate && null !== newProps.alternate.memoizedState && null !== newProps.alternate.memoizedState.cachePool && (type = newProps.alternate.memoizedState.cachePool.pool), nextResource = null, null !== newProps.memoizedState && null !== newProps.memoizedState.cachePool && (nextResource = newProps.memoizedState.cachePool.pool), nextResource !== type && (newProps.flags |= 2048));
        renderLanes2 !== current && renderLanes2 && (workInProgress2.child.flags |= 8192);
        scheduleRetryEffect(workInProgress2, workInProgress2.updateQueue);
        bubbleProperties(workInProgress2);
        return null;
      case 4:
        return popHostContainer(), null === current && listenToAllSupportedEvents(workInProgress2.stateNode.containerInfo), bubbleProperties(workInProgress2), null;
      case 10:
        return popProvider(workInProgress2.type), bubbleProperties(workInProgress2), null;
      case 19:
        pop(suspenseStackCursor);
        newProps = workInProgress2.memoizedState;
        if (null === newProps) return bubbleProperties(workInProgress2), null;
        type = 0 !== (workInProgress2.flags & 128);
        nextResource = newProps.rendering;
        if (null === nextResource)
          if (type) cutOffTailIfNeeded(newProps, false);
          else {
            if (0 !== workInProgressRootExitStatus || null !== current && 0 !== (current.flags & 128))
              for (current = workInProgress2.child; null !== current; ) {
                nextResource = findFirstSuspended(current);
                if (null !== nextResource) {
                  workInProgress2.flags |= 128;
                  cutOffTailIfNeeded(newProps, false);
                  current = nextResource.updateQueue;
                  workInProgress2.updateQueue = current;
                  scheduleRetryEffect(workInProgress2, current);
                  workInProgress2.subtreeFlags = 0;
                  current = renderLanes2;
                  for (renderLanes2 = workInProgress2.child; null !== renderLanes2; )
                    resetWorkInProgress(renderLanes2, current), renderLanes2 = renderLanes2.sibling;
                  push(
                    suspenseStackCursor,
                    suspenseStackCursor.current & 1 | 2
                  );
                  isHydrating && pushTreeFork(workInProgress2, newProps.treeForkCount);
                  return workInProgress2.child;
                }
                current = current.sibling;
              }
            null !== newProps.tail && now() > workInProgressRootRenderTargetTime && (workInProgress2.flags |= 128, type = true, cutOffTailIfNeeded(newProps, false), workInProgress2.lanes = 4194304);
          }
        else {
          if (!type)
            if (current = findFirstSuspended(nextResource), null !== current) {
              if (workInProgress2.flags |= 128, type = true, current = current.updateQueue, workInProgress2.updateQueue = current, scheduleRetryEffect(workInProgress2, current), cutOffTailIfNeeded(newProps, true), null === newProps.tail && "hidden" === newProps.tailMode && !nextResource.alternate && !isHydrating)
                return bubbleProperties(workInProgress2), null;
            } else
              2 * now() - newProps.renderingStartTime > workInProgressRootRenderTargetTime && 536870912 !== renderLanes2 && (workInProgress2.flags |= 128, type = true, cutOffTailIfNeeded(newProps, false), workInProgress2.lanes = 4194304);
          newProps.isBackwards ? (nextResource.sibling = workInProgress2.child, workInProgress2.child = nextResource) : (current = newProps.last, null !== current ? current.sibling = nextResource : workInProgress2.child = nextResource, newProps.last = nextResource);
        }
        if (null !== newProps.tail)
          return current = newProps.tail, newProps.rendering = current, newProps.tail = current.sibling, newProps.renderingStartTime = now(), current.sibling = null, renderLanes2 = suspenseStackCursor.current, push(
            suspenseStackCursor,
            type ? renderLanes2 & 1 | 2 : renderLanes2 & 1
          ), isHydrating && pushTreeFork(workInProgress2, newProps.treeForkCount), current;
        bubbleProperties(workInProgress2);
        return null;
      case 22:
      case 23:
        return popSuspenseHandler(workInProgress2), popHiddenContext(), newProps = null !== workInProgress2.memoizedState, null !== current ? null !== current.memoizedState !== newProps && (workInProgress2.flags |= 8192) : newProps && (workInProgress2.flags |= 8192), newProps ? 0 !== (renderLanes2 & 536870912) && 0 === (workInProgress2.flags & 128) && (bubbleProperties(workInProgress2), workInProgress2.subtreeFlags & 6 && (workInProgress2.flags |= 8192)) : bubbleProperties(workInProgress2), renderLanes2 = workInProgress2.updateQueue, null !== renderLanes2 && scheduleRetryEffect(workInProgress2, renderLanes2.retryQueue), renderLanes2 = null, null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (renderLanes2 = current.memoizedState.cachePool.pool), newProps = null, null !== workInProgress2.memoizedState && null !== workInProgress2.memoizedState.cachePool && (newProps = workInProgress2.memoizedState.cachePool.pool), newProps !== renderLanes2 && (workInProgress2.flags |= 2048), null !== current && pop(resumedCache), null;
      case 24:
        return renderLanes2 = null, null !== current && (renderLanes2 = current.memoizedState.cache), workInProgress2.memoizedState.cache !== renderLanes2 && (workInProgress2.flags |= 2048), popProvider(CacheContext), bubbleProperties(workInProgress2), null;
      case 25:
        return null;
      case 30:
        return null;
    }
    throw Error(formatProdErrorMessage(156, workInProgress2.tag));
  }
  function unwindWork(current, workInProgress2) {
    popTreeContext(workInProgress2);
    switch (workInProgress2.tag) {
      case 1:
        return current = workInProgress2.flags, current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 3:
        return popProvider(CacheContext), popHostContainer(), current = workInProgress2.flags, 0 !== (current & 65536) && 0 === (current & 128) ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 26:
      case 27:
      case 5:
        return popHostContext(workInProgress2), null;
      case 31:
        if (null !== workInProgress2.memoizedState) {
          popSuspenseHandler(workInProgress2);
          if (null === workInProgress2.alternate)
            throw Error(formatProdErrorMessage(340));
          resetHydrationState();
        }
        current = workInProgress2.flags;
        return current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 13:
        popSuspenseHandler(workInProgress2);
        current = workInProgress2.memoizedState;
        if (null !== current && null !== current.dehydrated) {
          if (null === workInProgress2.alternate)
            throw Error(formatProdErrorMessage(340));
          resetHydrationState();
        }
        current = workInProgress2.flags;
        return current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 19:
        return pop(suspenseStackCursor), null;
      case 4:
        return popHostContainer(), null;
      case 10:
        return popProvider(workInProgress2.type), null;
      case 22:
      case 23:
        return popSuspenseHandler(workInProgress2), popHiddenContext(), null !== current && pop(resumedCache), current = workInProgress2.flags, current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
      case 24:
        return popProvider(CacheContext), null;
      case 25:
        return null;
      default:
        return null;
    }
  }
  function unwindInterruptedWork(current, interruptedWork) {
    popTreeContext(interruptedWork);
    switch (interruptedWork.tag) {
      case 3:
        popProvider(CacheContext);
        popHostContainer();
        break;
      case 26:
      case 27:
      case 5:
        popHostContext(interruptedWork);
        break;
      case 4:
        popHostContainer();
        break;
      case 31:
        null !== interruptedWork.memoizedState && popSuspenseHandler(interruptedWork);
        break;
      case 13:
        popSuspenseHandler(interruptedWork);
        break;
      case 19:
        pop(suspenseStackCursor);
        break;
      case 10:
        popProvider(interruptedWork.type);
        break;
      case 22:
      case 23:
        popSuspenseHandler(interruptedWork);
        popHiddenContext();
        null !== current && pop(resumedCache);
        break;
      case 24:
        popProvider(CacheContext);
    }
  }
  function commitHookEffectListMount(flags, finishedWork) {
    try {
      var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
      if (null !== lastEffect) {
        var firstEffect = lastEffect.next;
        updateQueue = firstEffect;
        do {
          if ((updateQueue.tag & flags) === flags) {
            lastEffect = void 0;
            var create = updateQueue.create, inst = updateQueue.inst;
            lastEffect = create();
            inst.destroy = lastEffect;
          }
          updateQueue = updateQueue.next;
        } while (updateQueue !== firstEffect);
      }
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor$jscomp$0) {
    try {
      var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
      if (null !== lastEffect) {
        var firstEffect = lastEffect.next;
        updateQueue = firstEffect;
        do {
          if ((updateQueue.tag & flags) === flags) {
            var inst = updateQueue.inst, destroy = inst.destroy;
            if (void 0 !== destroy) {
              inst.destroy = void 0;
              lastEffect = finishedWork;
              var nearestMountedAncestor = nearestMountedAncestor$jscomp$0, destroy_ = destroy;
              try {
                destroy_();
              } catch (error) {
                captureCommitPhaseError(
                  lastEffect,
                  nearestMountedAncestor,
                  error
                );
              }
            }
          }
          updateQueue = updateQueue.next;
        } while (updateQueue !== firstEffect);
      }
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function commitClassCallbacks(finishedWork) {
    var updateQueue = finishedWork.updateQueue;
    if (null !== updateQueue) {
      var instance = finishedWork.stateNode;
      try {
        commitCallbacks(updateQueue, instance);
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
    }
  }
  function safelyCallComponentWillUnmount(current, nearestMountedAncestor, instance) {
    instance.props = resolveClassComponentProps(
      current.type,
      current.memoizedProps
    );
    instance.state = current.memoizedState;
    try {
      instance.componentWillUnmount();
    } catch (error) {
      captureCommitPhaseError(current, nearestMountedAncestor, error);
    }
  }
  function safelyAttachRef(current, nearestMountedAncestor) {
    try {
      var ref = current.ref;
      if (null !== ref) {
        switch (current.tag) {
          case 26:
          case 27:
          case 5:
            var instanceToUse = current.stateNode;
            break;
          case 30:
            instanceToUse = current.stateNode;
            break;
          default:
            instanceToUse = current.stateNode;
        }
        "function" === typeof ref ? current.refCleanup = ref(instanceToUse) : ref.current = instanceToUse;
      }
    } catch (error) {
      captureCommitPhaseError(current, nearestMountedAncestor, error);
    }
  }
  function safelyDetachRef(current, nearestMountedAncestor) {
    var ref = current.ref, refCleanup = current.refCleanup;
    if (null !== ref)
      if ("function" === typeof refCleanup)
        try {
          refCleanup();
        } catch (error) {
          captureCommitPhaseError(current, nearestMountedAncestor, error);
        } finally {
          current.refCleanup = null, current = current.alternate, null != current && (current.refCleanup = null);
        }
      else if ("function" === typeof ref)
        try {
          ref(null);
        } catch (error$140) {
          captureCommitPhaseError(current, nearestMountedAncestor, error$140);
        }
      else ref.current = null;
  }
  function commitHostMount(finishedWork) {
    var type = finishedWork.type, props = finishedWork.memoizedProps, instance = finishedWork.stateNode;
    try {
      a: switch (type) {
        case "button":
        case "input":
        case "select":
        case "textarea":
          props.autoFocus && instance.focus();
          break a;
        case "img":
          props.src ? instance.src = props.src : props.srcSet && (instance.srcset = props.srcSet);
      }
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function commitHostUpdate(finishedWork, newProps, oldProps) {
    try {
      var domElement = finishedWork.stateNode;
      updateProperties(domElement, finishedWork.type, oldProps, newProps);
      domElement[internalPropsKey] = newProps;
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  function isHostParent(fiber) {
    return 5 === fiber.tag || 3 === fiber.tag || 26 === fiber.tag || 27 === fiber.tag && isSingletonScope(fiber.type) || 4 === fiber.tag;
  }
  function getHostSibling(fiber) {
    a: for (; ; ) {
      for (; null === fiber.sibling; ) {
        if (null === fiber.return || isHostParent(fiber.return)) return null;
        fiber = fiber.return;
      }
      fiber.sibling.return = fiber.return;
      for (fiber = fiber.sibling; 5 !== fiber.tag && 6 !== fiber.tag && 18 !== fiber.tag; ) {
        if (27 === fiber.tag && isSingletonScope(fiber.type)) continue a;
        if (fiber.flags & 2) continue a;
        if (null === fiber.child || 4 === fiber.tag) continue a;
        else fiber.child.return = fiber, fiber = fiber.child;
      }
      if (!(fiber.flags & 2)) return fiber.stateNode;
    }
  }
  function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
    var tag = node.tag;
    if (5 === tag || 6 === tag)
      node = node.stateNode, before ? (9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent).insertBefore(node, before) : (before = 9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent, before.appendChild(node), parent = parent._reactRootContainer, null !== parent && void 0 !== parent || null !== before.onclick || (before.onclick = noop$1));
    else if (4 !== tag && (27 === tag && isSingletonScope(node.type) && (parent = node.stateNode, before = null), node = node.child, null !== node))
      for (insertOrAppendPlacementNodeIntoContainer(node, before, parent), node = node.sibling; null !== node; )
        insertOrAppendPlacementNodeIntoContainer(node, before, parent), node = node.sibling;
  }
  function insertOrAppendPlacementNode(node, before, parent) {
    var tag = node.tag;
    if (5 === tag || 6 === tag)
      node = node.stateNode, before ? parent.insertBefore(node, before) : parent.appendChild(node);
    else if (4 !== tag && (27 === tag && isSingletonScope(node.type) && (parent = node.stateNode), node = node.child, null !== node))
      for (insertOrAppendPlacementNode(node, before, parent), node = node.sibling; null !== node; )
        insertOrAppendPlacementNode(node, before, parent), node = node.sibling;
  }
  function commitHostSingletonAcquisition(finishedWork) {
    var singleton = finishedWork.stateNode, props = finishedWork.memoizedProps;
    try {
      for (var type = finishedWork.type, attributes = singleton.attributes; attributes.length; )
        singleton.removeAttributeNode(attributes[0]);
      setInitialProperties(singleton, type, props);
      singleton[internalInstanceKey] = finishedWork;
      singleton[internalPropsKey] = props;
    } catch (error) {
      captureCommitPhaseError(finishedWork, finishedWork.return, error);
    }
  }
  var offscreenSubtreeIsHidden = false, offscreenSubtreeWasHidden = false, needsFormReset = false, PossiblyWeakSet = "function" === typeof WeakSet ? WeakSet : Set, nextEffect = null;
  function commitBeforeMutationEffects(root3, firstChild) {
    root3 = root3.containerInfo;
    eventsEnabled = _enabled;
    root3 = getActiveElementDeep(root3);
    if (hasSelectionCapabilities(root3)) {
      if ("selectionStart" in root3)
        var JSCompiler_temp = {
          start: root3.selectionStart,
          end: root3.selectionEnd
        };
      else
        a: {
          JSCompiler_temp = (JSCompiler_temp = root3.ownerDocument) && JSCompiler_temp.defaultView || window;
          var selection = JSCompiler_temp.getSelection && JSCompiler_temp.getSelection();
          if (selection && 0 !== selection.rangeCount) {
            JSCompiler_temp = selection.anchorNode;
            var anchorOffset = selection.anchorOffset, focusNode = selection.focusNode;
            selection = selection.focusOffset;
            try {
              JSCompiler_temp.nodeType, focusNode.nodeType;
            } catch (e$20) {
              JSCompiler_temp = null;
              break a;
            }
            var length = 0, start = -1, end = -1, indexWithinAnchor = 0, indexWithinFocus = 0, node = root3, parentNode = null;
            b: for (; ; ) {
              for (var next; ; ) {
                node !== JSCompiler_temp || 0 !== anchorOffset && 3 !== node.nodeType || (start = length + anchorOffset);
                node !== focusNode || 0 !== selection && 3 !== node.nodeType || (end = length + selection);
                3 === node.nodeType && (length += node.nodeValue.length);
                if (null === (next = node.firstChild)) break;
                parentNode = node;
                node = next;
              }
              for (; ; ) {
                if (node === root3) break b;
                parentNode === JSCompiler_temp && ++indexWithinAnchor === anchorOffset && (start = length);
                parentNode === focusNode && ++indexWithinFocus === selection && (end = length);
                if (null !== (next = node.nextSibling)) break;
                node = parentNode;
                parentNode = node.parentNode;
              }
              node = next;
            }
            JSCompiler_temp = -1 === start || -1 === end ? null : { start, end };
          } else JSCompiler_temp = null;
        }
      JSCompiler_temp = JSCompiler_temp || { start: 0, end: 0 };
    } else JSCompiler_temp = null;
    selectionInformation = { focusedElem: root3, selectionRange: JSCompiler_temp };
    _enabled = false;
    for (nextEffect = firstChild; null !== nextEffect; )
      if (firstChild = nextEffect, root3 = firstChild.child, 0 !== (firstChild.subtreeFlags & 1028) && null !== root3)
        root3.return = firstChild, nextEffect = root3;
      else
        for (; null !== nextEffect; ) {
          firstChild = nextEffect;
          focusNode = firstChild.alternate;
          root3 = firstChild.flags;
          switch (firstChild.tag) {
            case 0:
              if (0 !== (root3 & 4) && (root3 = firstChild.updateQueue, root3 = null !== root3 ? root3.events : null, null !== root3))
                for (JSCompiler_temp = 0; JSCompiler_temp < root3.length; JSCompiler_temp++)
                  anchorOffset = root3[JSCompiler_temp], anchorOffset.ref.impl = anchorOffset.nextImpl;
              break;
            case 11:
            case 15:
              break;
            case 1:
              if (0 !== (root3 & 1024) && null !== focusNode) {
                root3 = void 0;
                JSCompiler_temp = firstChild;
                anchorOffset = focusNode.memoizedProps;
                focusNode = focusNode.memoizedState;
                selection = JSCompiler_temp.stateNode;
                try {
                  var resolvedPrevProps = resolveClassComponentProps(
                    JSCompiler_temp.type,
                    anchorOffset
                  );
                  root3 = selection.getSnapshotBeforeUpdate(
                    resolvedPrevProps,
                    focusNode
                  );
                  selection.__reactInternalSnapshotBeforeUpdate = root3;
                } catch (error) {
                  captureCommitPhaseError(
                    JSCompiler_temp,
                    JSCompiler_temp.return,
                    error
                  );
                }
              }
              break;
            case 3:
              if (0 !== (root3 & 1024)) {
                if (root3 = firstChild.stateNode.containerInfo, JSCompiler_temp = root3.nodeType, 9 === JSCompiler_temp)
                  clearContainerSparingly(root3);
                else if (1 === JSCompiler_temp)
                  switch (root3.nodeName) {
                    case "HEAD":
                    case "HTML":
                    case "BODY":
                      clearContainerSparingly(root3);
                      break;
                    default:
                      root3.textContent = "";
                  }
              }
              break;
            case 5:
            case 26:
            case 27:
            case 6:
            case 4:
            case 17:
              break;
            default:
              if (0 !== (root3 & 1024)) throw Error(formatProdErrorMessage(163));
          }
          root3 = firstChild.sibling;
          if (null !== root3) {
            root3.return = firstChild.return;
            nextEffect = root3;
            break;
          }
          nextEffect = firstChild.return;
        }
  }
  function commitLayoutEffectOnFiber(finishedRoot, current, finishedWork) {
    var flags = finishedWork.flags;
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 15:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        flags & 4 && commitHookEffectListMount(5, finishedWork);
        break;
      case 1:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        if (flags & 4)
          if (finishedRoot = finishedWork.stateNode, null === current)
            try {
              finishedRoot.componentDidMount();
            } catch (error) {
              captureCommitPhaseError(finishedWork, finishedWork.return, error);
            }
          else {
            var prevProps = resolveClassComponentProps(
              finishedWork.type,
              current.memoizedProps
            );
            current = current.memoizedState;
            try {
              finishedRoot.componentDidUpdate(
                prevProps,
                current,
                finishedRoot.__reactInternalSnapshotBeforeUpdate
              );
            } catch (error$139) {
              captureCommitPhaseError(
                finishedWork,
                finishedWork.return,
                error$139
              );
            }
          }
        flags & 64 && commitClassCallbacks(finishedWork);
        flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
        break;
      case 3:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        if (flags & 64 && (finishedRoot = finishedWork.updateQueue, null !== finishedRoot)) {
          current = null;
          if (null !== finishedWork.child)
            switch (finishedWork.child.tag) {
              case 27:
              case 5:
                current = finishedWork.child.stateNode;
                break;
              case 1:
                current = finishedWork.child.stateNode;
            }
          try {
            commitCallbacks(finishedRoot, current);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
        break;
      case 27:
        null === current && flags & 4 && commitHostSingletonAcquisition(finishedWork);
      case 26:
      case 5:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        null === current && flags & 4 && commitHostMount(finishedWork);
        flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
        break;
      case 12:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        break;
      case 31:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
        break;
      case 13:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
        flags & 64 && (finishedRoot = finishedWork.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot && (finishedWork = retryDehydratedSuspenseBoundary.bind(
          null,
          finishedWork
        ), registerSuspenseInstanceRetry(finishedRoot, finishedWork))));
        break;
      case 22:
        flags = null !== finishedWork.memoizedState || offscreenSubtreeIsHidden;
        if (!flags) {
          current = null !== current && null !== current.memoizedState || offscreenSubtreeWasHidden;
          prevProps = offscreenSubtreeIsHidden;
          var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
          offscreenSubtreeIsHidden = flags;
          (offscreenSubtreeWasHidden = current) && !prevOffscreenSubtreeWasHidden ? recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            0 !== (finishedWork.subtreeFlags & 8772)
          ) : recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
          offscreenSubtreeIsHidden = prevProps;
          offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
        }
        break;
      case 30:
        break;
      default:
        recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
    }
  }
  function detachFiberAfterEffects(fiber) {
    var alternate = fiber.alternate;
    null !== alternate && (fiber.alternate = null, detachFiberAfterEffects(alternate));
    fiber.child = null;
    fiber.deletions = null;
    fiber.sibling = null;
    5 === fiber.tag && (alternate = fiber.stateNode, null !== alternate && detachDeletedInstance(alternate));
    fiber.stateNode = null;
    fiber.return = null;
    fiber.dependencies = null;
    fiber.memoizedProps = null;
    fiber.memoizedState = null;
    fiber.pendingProps = null;
    fiber.stateNode = null;
    fiber.updateQueue = null;
  }
  var hostParent = null, hostParentIsContainer = false;
  function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
    for (parent = parent.child; null !== parent; )
      commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, parent), parent = parent.sibling;
  }
  function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
    if (injectedHook && "function" === typeof injectedHook.onCommitFiberUnmount)
      try {
        injectedHook.onCommitFiberUnmount(rendererID, deletedFiber);
      } catch (err) {
      }
    switch (deletedFiber.tag) {
      case 26:
        offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        deletedFiber.memoizedState ? deletedFiber.memoizedState.count-- : deletedFiber.stateNode && (deletedFiber = deletedFiber.stateNode, deletedFiber.parentNode.removeChild(deletedFiber));
        break;
      case 27:
        offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
        var prevHostParent = hostParent, prevHostParentIsContainer = hostParentIsContainer;
        isSingletonScope(deletedFiber.type) && (hostParent = deletedFiber.stateNode, hostParentIsContainer = false);
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        releaseSingletonInstance(deletedFiber.stateNode);
        hostParent = prevHostParent;
        hostParentIsContainer = prevHostParentIsContainer;
        break;
      case 5:
        offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
      case 6:
        prevHostParent = hostParent;
        prevHostParentIsContainer = hostParentIsContainer;
        hostParent = null;
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        hostParent = prevHostParent;
        hostParentIsContainer = prevHostParentIsContainer;
        if (null !== hostParent)
          if (hostParentIsContainer)
            try {
              (9 === hostParent.nodeType ? hostParent.body : "HTML" === hostParent.nodeName ? hostParent.ownerDocument.body : hostParent).removeChild(deletedFiber.stateNode);
            } catch (error) {
              captureCommitPhaseError(
                deletedFiber,
                nearestMountedAncestor,
                error
              );
            }
          else
            try {
              hostParent.removeChild(deletedFiber.stateNode);
            } catch (error) {
              captureCommitPhaseError(
                deletedFiber,
                nearestMountedAncestor,
                error
              );
            }
        break;
      case 18:
        null !== hostParent && (hostParentIsContainer ? (finishedRoot = hostParent, clearHydrationBoundary(
          9 === finishedRoot.nodeType ? finishedRoot.body : "HTML" === finishedRoot.nodeName ? finishedRoot.ownerDocument.body : finishedRoot,
          deletedFiber.stateNode
        ), retryIfBlockedOn(finishedRoot)) : clearHydrationBoundary(hostParent, deletedFiber.stateNode));
        break;
      case 4:
        prevHostParent = hostParent;
        prevHostParentIsContainer = hostParentIsContainer;
        hostParent = deletedFiber.stateNode.containerInfo;
        hostParentIsContainer = true;
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        hostParent = prevHostParent;
        hostParentIsContainer = prevHostParentIsContainer;
        break;
      case 0:
      case 11:
      case 14:
      case 15:
        commitHookEffectListUnmount(2, deletedFiber, nearestMountedAncestor);
        offscreenSubtreeWasHidden || commitHookEffectListUnmount(4, deletedFiber, nearestMountedAncestor);
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        break;
      case 1:
        offscreenSubtreeWasHidden || (safelyDetachRef(deletedFiber, nearestMountedAncestor), prevHostParent = deletedFiber.stateNode, "function" === typeof prevHostParent.componentWillUnmount && safelyCallComponentWillUnmount(
          deletedFiber,
          nearestMountedAncestor,
          prevHostParent
        ));
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        break;
      case 21:
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        break;
      case 22:
        offscreenSubtreeWasHidden = (prevHostParent = offscreenSubtreeWasHidden) || null !== deletedFiber.memoizedState;
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
        offscreenSubtreeWasHidden = prevHostParent;
        break;
      default:
        recursivelyTraverseDeletionEffects(
          finishedRoot,
          nearestMountedAncestor,
          deletedFiber
        );
    }
  }
  function commitActivityHydrationCallbacks(finishedRoot, finishedWork) {
    if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot))) {
      finishedRoot = finishedRoot.dehydrated;
      try {
        retryIfBlockedOn(finishedRoot);
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
    }
  }
  function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
    if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot))))
      try {
        retryIfBlockedOn(finishedRoot);
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
  }
  function getRetryCache(finishedWork) {
    switch (finishedWork.tag) {
      case 31:
      case 13:
      case 19:
        var retryCache = finishedWork.stateNode;
        null === retryCache && (retryCache = finishedWork.stateNode = new PossiblyWeakSet());
        return retryCache;
      case 22:
        return finishedWork = finishedWork.stateNode, retryCache = finishedWork._retryCache, null === retryCache && (retryCache = finishedWork._retryCache = new PossiblyWeakSet()), retryCache;
      default:
        throw Error(formatProdErrorMessage(435, finishedWork.tag));
    }
  }
  function attachSuspenseRetryListeners(finishedWork, wakeables) {
    var retryCache = getRetryCache(finishedWork);
    wakeables.forEach(function(wakeable) {
      if (!retryCache.has(wakeable)) {
        retryCache.add(wakeable);
        var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
        wakeable.then(retry, retry);
      }
    });
  }
  function recursivelyTraverseMutationEffects(root$jscomp$0, parentFiber) {
    var deletions = parentFiber.deletions;
    if (null !== deletions)
      for (var i = 0; i < deletions.length; i++) {
        var childToDelete = deletions[i], root3 = root$jscomp$0, returnFiber = parentFiber, parent = returnFiber;
        a: for (; null !== parent; ) {
          switch (parent.tag) {
            case 27:
              if (isSingletonScope(parent.type)) {
                hostParent = parent.stateNode;
                hostParentIsContainer = false;
                break a;
              }
              break;
            case 5:
              hostParent = parent.stateNode;
              hostParentIsContainer = false;
              break a;
            case 3:
            case 4:
              hostParent = parent.stateNode.containerInfo;
              hostParentIsContainer = true;
              break a;
          }
          parent = parent.return;
        }
        if (null === hostParent) throw Error(formatProdErrorMessage(160));
        commitDeletionEffectsOnFiber(root3, returnFiber, childToDelete);
        hostParent = null;
        hostParentIsContainer = false;
        root3 = childToDelete.alternate;
        null !== root3 && (root3.return = null);
        childToDelete.return = null;
      }
    if (parentFiber.subtreeFlags & 13886)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitMutationEffectsOnFiber(parentFiber, root$jscomp$0), parentFiber = parentFiber.sibling;
  }
  var currentHoistableRoot = null;
  function commitMutationEffectsOnFiber(finishedWork, root3) {
    var current = finishedWork.alternate, flags = finishedWork.flags;
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 14:
      case 15:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 4 && (commitHookEffectListUnmount(3, finishedWork, finishedWork.return), commitHookEffectListMount(3, finishedWork), commitHookEffectListUnmount(5, finishedWork, finishedWork.return));
        break;
      case 1:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        flags & 64 && offscreenSubtreeIsHidden && (finishedWork = finishedWork.updateQueue, null !== finishedWork && (flags = finishedWork.callbacks, null !== flags && (current = finishedWork.shared.hiddenCallbacks, finishedWork.shared.hiddenCallbacks = null === current ? flags : current.concat(flags))));
        break;
      case 26:
        var hoistableRoot = currentHoistableRoot;
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        if (flags & 4) {
          var currentResource = null !== current ? current.memoizedState : null;
          flags = finishedWork.memoizedState;
          if (null === current)
            if (null === flags)
              if (null === finishedWork.stateNode) {
                a: {
                  flags = finishedWork.type;
                  current = finishedWork.memoizedProps;
                  hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
                  b: switch (flags) {
                    case "title":
                      currentResource = hoistableRoot.getElementsByTagName("title")[0];
                      if (!currentResource || currentResource[internalHoistableMarker] || currentResource[internalInstanceKey] || "http://www.w3.org/2000/svg" === currentResource.namespaceURI || currentResource.hasAttribute("itemprop"))
                        currentResource = hoistableRoot.createElement(flags), hoistableRoot.head.insertBefore(
                          currentResource,
                          hoistableRoot.querySelector("head > title")
                        );
                      setInitialProperties(currentResource, flags, current);
                      currentResource[internalInstanceKey] = finishedWork;
                      markNodeAsHoistable(currentResource);
                      flags = currentResource;
                      break a;
                    case "link":
                      var maybeNodes = getHydratableHoistableCache(
                        "link",
                        "href",
                        hoistableRoot
                      ).get(flags + (current.href || ""));
                      if (maybeNodes) {
                        for (var i = 0; i < maybeNodes.length; i++)
                          if (currentResource = maybeNodes[i], currentResource.getAttribute("href") === (null == current.href || "" === current.href ? null : current.href) && currentResource.getAttribute("rel") === (null == current.rel ? null : current.rel) && currentResource.getAttribute("title") === (null == current.title ? null : current.title) && currentResource.getAttribute("crossorigin") === (null == current.crossOrigin ? null : current.crossOrigin)) {
                            maybeNodes.splice(i, 1);
                            break b;
                          }
                      }
                      currentResource = hoistableRoot.createElement(flags);
                      setInitialProperties(currentResource, flags, current);
                      hoistableRoot.head.appendChild(currentResource);
                      break;
                    case "meta":
                      if (maybeNodes = getHydratableHoistableCache(
                        "meta",
                        "content",
                        hoistableRoot
                      ).get(flags + (current.content || ""))) {
                        for (i = 0; i < maybeNodes.length; i++)
                          if (currentResource = maybeNodes[i], currentResource.getAttribute("content") === (null == current.content ? null : "" + current.content) && currentResource.getAttribute("name") === (null == current.name ? null : current.name) && currentResource.getAttribute("property") === (null == current.property ? null : current.property) && currentResource.getAttribute("http-equiv") === (null == current.httpEquiv ? null : current.httpEquiv) && currentResource.getAttribute("charset") === (null == current.charSet ? null : current.charSet)) {
                            maybeNodes.splice(i, 1);
                            break b;
                          }
                      }
                      currentResource = hoistableRoot.createElement(flags);
                      setInitialProperties(currentResource, flags, current);
                      hoistableRoot.head.appendChild(currentResource);
                      break;
                    default:
                      throw Error(formatProdErrorMessage(468, flags));
                  }
                  currentResource[internalInstanceKey] = finishedWork;
                  markNodeAsHoistable(currentResource);
                  flags = currentResource;
                }
                finishedWork.stateNode = flags;
              } else
                mountHoistable(
                  hoistableRoot,
                  finishedWork.type,
                  finishedWork.stateNode
                );
            else
              finishedWork.stateNode = acquireResource(
                hoistableRoot,
                flags,
                finishedWork.memoizedProps
              );
          else
            currentResource !== flags ? (null === currentResource ? null !== current.stateNode && (current = current.stateNode, current.parentNode.removeChild(current)) : currentResource.count--, null === flags ? mountHoistable(
              hoistableRoot,
              finishedWork.type,
              finishedWork.stateNode
            ) : acquireResource(
              hoistableRoot,
              flags,
              finishedWork.memoizedProps
            )) : null === flags && null !== finishedWork.stateNode && commitHostUpdate(
              finishedWork,
              finishedWork.memoizedProps,
              current.memoizedProps
            );
        }
        break;
      case 27:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        null !== current && flags & 4 && commitHostUpdate(
          finishedWork,
          finishedWork.memoizedProps,
          current.memoizedProps
        );
        break;
      case 5:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
        if (finishedWork.flags & 32) {
          hoistableRoot = finishedWork.stateNode;
          try {
            setTextContent(hoistableRoot, "");
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
        flags & 4 && null != finishedWork.stateNode && (hoistableRoot = finishedWork.memoizedProps, commitHostUpdate(
          finishedWork,
          hoistableRoot,
          null !== current ? current.memoizedProps : hoistableRoot
        ));
        flags & 1024 && (needsFormReset = true);
        break;
      case 6:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        if (flags & 4) {
          if (null === finishedWork.stateNode)
            throw Error(formatProdErrorMessage(162));
          flags = finishedWork.memoizedProps;
          current = finishedWork.stateNode;
          try {
            current.nodeValue = flags;
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
        break;
      case 3:
        tagCaches = null;
        hoistableRoot = currentHoistableRoot;
        currentHoistableRoot = getHoistableRoot(root3.containerInfo);
        recursivelyTraverseMutationEffects(root3, finishedWork);
        currentHoistableRoot = hoistableRoot;
        commitReconciliationEffects(finishedWork);
        if (flags & 4 && null !== current && current.memoizedState.isDehydrated)
          try {
            retryIfBlockedOn(root3.containerInfo);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        needsFormReset && (needsFormReset = false, recursivelyResetForms(finishedWork));
        break;
      case 4:
        flags = currentHoistableRoot;
        currentHoistableRoot = getHoistableRoot(
          finishedWork.stateNode.containerInfo
        );
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        currentHoistableRoot = flags;
        break;
      case 12:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        break;
      case 31:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
        break;
      case 13:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        finishedWork.child.flags & 8192 && null !== finishedWork.memoizedState !== (null !== current && null !== current.memoizedState) && (globalMostRecentFallbackTime = now());
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
        break;
      case 22:
        hoistableRoot = null !== finishedWork.memoizedState;
        var wasHidden = null !== current && null !== current.memoizedState, prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden, prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
        offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden || hoistableRoot;
        offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || wasHidden;
        recursivelyTraverseMutationEffects(root3, finishedWork);
        offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
        offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
        commitReconciliationEffects(finishedWork);
        if (flags & 8192)
          a: for (root3 = finishedWork.stateNode, root3._visibility = hoistableRoot ? root3._visibility & -2 : root3._visibility | 1, hoistableRoot && (null === current || wasHidden || offscreenSubtreeIsHidden || offscreenSubtreeWasHidden || recursivelyTraverseDisappearLayoutEffects(finishedWork)), current = null, root3 = finishedWork; ; ) {
            if (5 === root3.tag || 26 === root3.tag) {
              if (null === current) {
                wasHidden = current = root3;
                try {
                  if (currentResource = wasHidden.stateNode, hoistableRoot)
                    maybeNodes = currentResource.style, "function" === typeof maybeNodes.setProperty ? maybeNodes.setProperty("display", "none", "important") : maybeNodes.display = "none";
                  else {
                    i = wasHidden.stateNode;
                    var styleProp = wasHidden.memoizedProps.style, display = void 0 !== styleProp && null !== styleProp && styleProp.hasOwnProperty("display") ? styleProp.display : null;
                    i.style.display = null == display || "boolean" === typeof display ? "" : ("" + display).trim();
                  }
                } catch (error) {
                  captureCommitPhaseError(wasHidden, wasHidden.return, error);
                }
              }
            } else if (6 === root3.tag) {
              if (null === current) {
                wasHidden = root3;
                try {
                  wasHidden.stateNode.nodeValue = hoistableRoot ? "" : wasHidden.memoizedProps;
                } catch (error) {
                  captureCommitPhaseError(wasHidden, wasHidden.return, error);
                }
              }
            } else if (18 === root3.tag) {
              if (null === current) {
                wasHidden = root3;
                try {
                  var instance = wasHidden.stateNode;
                  hoistableRoot ? hideOrUnhideDehydratedBoundary(instance, true) : hideOrUnhideDehydratedBoundary(wasHidden.stateNode, false);
                } catch (error) {
                  captureCommitPhaseError(wasHidden, wasHidden.return, error);
                }
              }
            } else if ((22 !== root3.tag && 23 !== root3.tag || null === root3.memoizedState || root3 === finishedWork) && null !== root3.child) {
              root3.child.return = root3;
              root3 = root3.child;
              continue;
            }
            if (root3 === finishedWork) break a;
            for (; null === root3.sibling; ) {
              if (null === root3.return || root3.return === finishedWork) break a;
              current === root3 && (current = null);
              root3 = root3.return;
            }
            current === root3 && (current = null);
            root3.sibling.return = root3.return;
            root3 = root3.sibling;
          }
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (current = flags.retryQueue, null !== current && (flags.retryQueue = null, attachSuspenseRetryListeners(finishedWork, current))));
        break;
      case 19:
        recursivelyTraverseMutationEffects(root3, finishedWork);
        commitReconciliationEffects(finishedWork);
        flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
        break;
      case 30:
        break;
      case 21:
        break;
      default:
        recursivelyTraverseMutationEffects(root3, finishedWork), commitReconciliationEffects(finishedWork);
    }
  }
  function commitReconciliationEffects(finishedWork) {
    var flags = finishedWork.flags;
    if (flags & 2) {
      try {
        for (var hostParentFiber, parentFiber = finishedWork.return; null !== parentFiber; ) {
          if (isHostParent(parentFiber)) {
            hostParentFiber = parentFiber;
            break;
          }
          parentFiber = parentFiber.return;
        }
        if (null == hostParentFiber) throw Error(formatProdErrorMessage(160));
        switch (hostParentFiber.tag) {
          case 27:
            var parent = hostParentFiber.stateNode, before = getHostSibling(finishedWork);
            insertOrAppendPlacementNode(finishedWork, before, parent);
            break;
          case 5:
            var parent$141 = hostParentFiber.stateNode;
            hostParentFiber.flags & 32 && (setTextContent(parent$141, ""), hostParentFiber.flags &= -33);
            var before$142 = getHostSibling(finishedWork);
            insertOrAppendPlacementNode(finishedWork, before$142, parent$141);
            break;
          case 3:
          case 4:
            var parent$143 = hostParentFiber.stateNode.containerInfo, before$144 = getHostSibling(finishedWork);
            insertOrAppendPlacementNodeIntoContainer(
              finishedWork,
              before$144,
              parent$143
            );
            break;
          default:
            throw Error(formatProdErrorMessage(161));
        }
      } catch (error) {
        captureCommitPhaseError(finishedWork, finishedWork.return, error);
      }
      finishedWork.flags &= -3;
    }
    flags & 4096 && (finishedWork.flags &= -4097);
  }
  function recursivelyResetForms(parentFiber) {
    if (parentFiber.subtreeFlags & 1024)
      for (parentFiber = parentFiber.child; null !== parentFiber; ) {
        var fiber = parentFiber;
        recursivelyResetForms(fiber);
        5 === fiber.tag && fiber.flags & 1024 && fiber.stateNode.reset();
        parentFiber = parentFiber.sibling;
      }
  }
  function recursivelyTraverseLayoutEffects(root3, parentFiber) {
    if (parentFiber.subtreeFlags & 8772)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitLayoutEffectOnFiber(root3, parentFiber.alternate, parentFiber), parentFiber = parentFiber.sibling;
  }
  function recursivelyTraverseDisappearLayoutEffects(parentFiber) {
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      var finishedWork = parentFiber;
      switch (finishedWork.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          commitHookEffectListUnmount(4, finishedWork, finishedWork.return);
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 1:
          safelyDetachRef(finishedWork, finishedWork.return);
          var instance = finishedWork.stateNode;
          "function" === typeof instance.componentWillUnmount && safelyCallComponentWillUnmount(
            finishedWork,
            finishedWork.return,
            instance
          );
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 27:
          releaseSingletonInstance(finishedWork.stateNode);
        case 26:
        case 5:
          safelyDetachRef(finishedWork, finishedWork.return);
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 22:
          null === finishedWork.memoizedState && recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        case 30:
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
          break;
        default:
          recursivelyTraverseDisappearLayoutEffects(finishedWork);
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function recursivelyTraverseReappearLayoutEffects(finishedRoot$jscomp$0, parentFiber, includeWorkInProgressEffects) {
    includeWorkInProgressEffects = includeWorkInProgressEffects && 0 !== (parentFiber.subtreeFlags & 8772);
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      var current = parentFiber.alternate, finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, flags = finishedWork.flags;
      switch (finishedWork.tag) {
        case 0:
        case 11:
        case 15:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          commitHookEffectListMount(4, finishedWork);
          break;
        case 1:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          current = finishedWork;
          finishedRoot = current.stateNode;
          if ("function" === typeof finishedRoot.componentDidMount)
            try {
              finishedRoot.componentDidMount();
            } catch (error) {
              captureCommitPhaseError(current, current.return, error);
            }
          current = finishedWork;
          finishedRoot = current.updateQueue;
          if (null !== finishedRoot) {
            var instance = current.stateNode;
            try {
              var hiddenCallbacks = finishedRoot.shared.hiddenCallbacks;
              if (null !== hiddenCallbacks)
                for (finishedRoot.shared.hiddenCallbacks = null, finishedRoot = 0; finishedRoot < hiddenCallbacks.length; finishedRoot++)
                  callCallback(hiddenCallbacks[finishedRoot], instance);
            } catch (error) {
              captureCommitPhaseError(current, current.return, error);
            }
          }
          includeWorkInProgressEffects && flags & 64 && commitClassCallbacks(finishedWork);
          safelyAttachRef(finishedWork, finishedWork.return);
          break;
        case 27:
          commitHostSingletonAcquisition(finishedWork);
        case 26:
        case 5:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && null === current && flags & 4 && commitHostMount(finishedWork);
          safelyAttachRef(finishedWork, finishedWork.return);
          break;
        case 12:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          break;
        case 31:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
          break;
        case 13:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
          break;
        case 22:
          null === finishedWork.memoizedState && recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
          safelyAttachRef(finishedWork, finishedWork.return);
          break;
        case 30:
          break;
        default:
          recursivelyTraverseReappearLayoutEffects(
            finishedRoot,
            finishedWork,
            includeWorkInProgressEffects
          );
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function commitOffscreenPassiveMountEffects(current, finishedWork) {
    var previousCache = null;
    null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (previousCache = current.memoizedState.cachePool.pool);
    current = null;
    null !== finishedWork.memoizedState && null !== finishedWork.memoizedState.cachePool && (current = finishedWork.memoizedState.cachePool.pool);
    current !== previousCache && (null != current && current.refCount++, null != previousCache && releaseCache(previousCache));
  }
  function commitCachePassiveMountEffect(current, finishedWork) {
    current = null;
    null !== finishedWork.alternate && (current = finishedWork.alternate.memoizedState.cache);
    finishedWork = finishedWork.memoizedState.cache;
    finishedWork !== current && (finishedWork.refCount++, null != current && releaseCache(current));
  }
  function recursivelyTraversePassiveMountEffects(root3, parentFiber, committedLanes, committedTransitions) {
    if (parentFiber.subtreeFlags & 10256)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitPassiveMountOnFiber(
          root3,
          parentFiber,
          committedLanes,
          committedTransitions
        ), parentFiber = parentFiber.sibling;
  }
  function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions) {
    var flags = finishedWork.flags;
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 15:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        flags & 2048 && commitHookEffectListMount(9, finishedWork);
        break;
      case 1:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        break;
      case 3:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        flags & 2048 && (finishedRoot = null, null !== finishedWork.alternate && (finishedRoot = finishedWork.alternate.memoizedState.cache), finishedWork = finishedWork.memoizedState.cache, finishedWork !== finishedRoot && (finishedWork.refCount++, null != finishedRoot && releaseCache(finishedRoot)));
        break;
      case 12:
        if (flags & 2048) {
          recursivelyTraversePassiveMountEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions
          );
          finishedRoot = finishedWork.stateNode;
          try {
            var _finishedWork$memoize2 = finishedWork.memoizedProps, id = _finishedWork$memoize2.id, onPostCommit = _finishedWork$memoize2.onPostCommit;
            "function" === typeof onPostCommit && onPostCommit(
              id,
              null === finishedWork.alternate ? "mount" : "update",
              finishedRoot.passiveEffectDuration,
              -0
            );
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        } else
          recursivelyTraversePassiveMountEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions
          );
        break;
      case 31:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        break;
      case 13:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        break;
      case 23:
        break;
      case 22:
        _finishedWork$memoize2 = finishedWork.stateNode;
        id = finishedWork.alternate;
        null !== finishedWork.memoizedState ? _finishedWork$memoize2._visibility & 2 ? recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        ) : recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork) : _finishedWork$memoize2._visibility & 2 ? recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        ) : (_finishedWork$memoize2._visibility |= 2, recursivelyTraverseReconnectPassiveEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions,
          0 !== (finishedWork.subtreeFlags & 10256) || false
        ));
        flags & 2048 && commitOffscreenPassiveMountEffects(id, finishedWork);
        break;
      case 24:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
        flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
        break;
      default:
        recursivelyTraversePassiveMountEffects(
          finishedRoot,
          finishedWork,
          committedLanes,
          committedTransitions
        );
    }
  }
  function recursivelyTraverseReconnectPassiveEffects(finishedRoot$jscomp$0, parentFiber, committedLanes$jscomp$0, committedTransitions$jscomp$0, includeWorkInProgressEffects) {
    includeWorkInProgressEffects = includeWorkInProgressEffects && (0 !== (parentFiber.subtreeFlags & 10256) || false);
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      var finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, committedLanes = committedLanes$jscomp$0, committedTransitions = committedTransitions$jscomp$0, flags = finishedWork.flags;
      switch (finishedWork.tag) {
        case 0:
        case 11:
        case 15:
          recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          );
          commitHookEffectListMount(8, finishedWork);
          break;
        case 23:
          break;
        case 22:
          var instance = finishedWork.stateNode;
          null !== finishedWork.memoizedState ? instance._visibility & 2 ? recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          ) : recursivelyTraverseAtomicPassiveEffects(
            finishedRoot,
            finishedWork
          ) : (instance._visibility |= 2, recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          ));
          includeWorkInProgressEffects && flags & 2048 && commitOffscreenPassiveMountEffects(
            finishedWork.alternate,
            finishedWork
          );
          break;
        case 24:
          recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          );
          includeWorkInProgressEffects && flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
          break;
        default:
          recursivelyTraverseReconnectPassiveEffects(
            finishedRoot,
            finishedWork,
            committedLanes,
            committedTransitions,
            includeWorkInProgressEffects
          );
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function recursivelyTraverseAtomicPassiveEffects(finishedRoot$jscomp$0, parentFiber) {
    if (parentFiber.subtreeFlags & 10256)
      for (parentFiber = parentFiber.child; null !== parentFiber; ) {
        var finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, flags = finishedWork.flags;
        switch (finishedWork.tag) {
          case 22:
            recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
            flags & 2048 && commitOffscreenPassiveMountEffects(
              finishedWork.alternate,
              finishedWork
            );
            break;
          case 24:
            recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
            flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
            break;
          default:
            recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
        }
        parentFiber = parentFiber.sibling;
      }
  }
  var suspenseyCommitFlag = 8192;
  function recursivelyAccumulateSuspenseyCommit(parentFiber, committedLanes, suspendedState) {
    if (parentFiber.subtreeFlags & suspenseyCommitFlag)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        accumulateSuspenseyCommitOnFiber(
          parentFiber,
          committedLanes,
          suspendedState
        ), parentFiber = parentFiber.sibling;
  }
  function accumulateSuspenseyCommitOnFiber(fiber, committedLanes, suspendedState) {
    switch (fiber.tag) {
      case 26:
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
        fiber.flags & suspenseyCommitFlag && null !== fiber.memoizedState && suspendResource(
          suspendedState,
          currentHoistableRoot,
          fiber.memoizedState,
          fiber.memoizedProps
        );
        break;
      case 5:
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
        break;
      case 3:
      case 4:
        var previousHoistableRoot = currentHoistableRoot;
        currentHoistableRoot = getHoistableRoot(fiber.stateNode.containerInfo);
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
        currentHoistableRoot = previousHoistableRoot;
        break;
      case 22:
        null === fiber.memoizedState && (previousHoistableRoot = fiber.alternate, null !== previousHoistableRoot && null !== previousHoistableRoot.memoizedState ? (previousHoistableRoot = suspenseyCommitFlag, suspenseyCommitFlag = 16777216, recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        ), suspenseyCommitFlag = previousHoistableRoot) : recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        ));
        break;
      default:
        recursivelyAccumulateSuspenseyCommit(
          fiber,
          committedLanes,
          suspendedState
        );
    }
  }
  function detachAlternateSiblings(parentFiber) {
    var previousFiber = parentFiber.alternate;
    if (null !== previousFiber && (parentFiber = previousFiber.child, null !== parentFiber)) {
      previousFiber.child = null;
      do
        previousFiber = parentFiber.sibling, parentFiber.sibling = null, parentFiber = previousFiber;
      while (null !== parentFiber);
    }
  }
  function recursivelyTraversePassiveUnmountEffects(parentFiber) {
    var deletions = parentFiber.deletions;
    if (0 !== (parentFiber.flags & 16)) {
      if (null !== deletions)
        for (var i = 0; i < deletions.length; i++) {
          var childToDelete = deletions[i];
          nextEffect = childToDelete;
          commitPassiveUnmountEffectsInsideOfDeletedTree_begin(
            childToDelete,
            parentFiber
          );
        }
      detachAlternateSiblings(parentFiber);
    }
    if (parentFiber.subtreeFlags & 10256)
      for (parentFiber = parentFiber.child; null !== parentFiber; )
        commitPassiveUnmountOnFiber(parentFiber), parentFiber = parentFiber.sibling;
  }
  function commitPassiveUnmountOnFiber(finishedWork) {
    switch (finishedWork.tag) {
      case 0:
      case 11:
      case 15:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
        finishedWork.flags & 2048 && commitHookEffectListUnmount(9, finishedWork, finishedWork.return);
        break;
      case 3:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
        break;
      case 12:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
        break;
      case 22:
        var instance = finishedWork.stateNode;
        null !== finishedWork.memoizedState && instance._visibility & 2 && (null === finishedWork.return || 13 !== finishedWork.return.tag) ? (instance._visibility &= -3, recursivelyTraverseDisconnectPassiveEffects(finishedWork)) : recursivelyTraversePassiveUnmountEffects(finishedWork);
        break;
      default:
        recursivelyTraversePassiveUnmountEffects(finishedWork);
    }
  }
  function recursivelyTraverseDisconnectPassiveEffects(parentFiber) {
    var deletions = parentFiber.deletions;
    if (0 !== (parentFiber.flags & 16)) {
      if (null !== deletions)
        for (var i = 0; i < deletions.length; i++) {
          var childToDelete = deletions[i];
          nextEffect = childToDelete;
          commitPassiveUnmountEffectsInsideOfDeletedTree_begin(
            childToDelete,
            parentFiber
          );
        }
      detachAlternateSiblings(parentFiber);
    }
    for (parentFiber = parentFiber.child; null !== parentFiber; ) {
      deletions = parentFiber;
      switch (deletions.tag) {
        case 0:
        case 11:
        case 15:
          commitHookEffectListUnmount(8, deletions, deletions.return);
          recursivelyTraverseDisconnectPassiveEffects(deletions);
          break;
        case 22:
          i = deletions.stateNode;
          i._visibility & 2 && (i._visibility &= -3, recursivelyTraverseDisconnectPassiveEffects(deletions));
          break;
        default:
          recursivelyTraverseDisconnectPassiveEffects(deletions);
      }
      parentFiber = parentFiber.sibling;
    }
  }
  function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor) {
    for (; null !== nextEffect; ) {
      var fiber = nextEffect;
      switch (fiber.tag) {
        case 0:
        case 11:
        case 15:
          commitHookEffectListUnmount(8, fiber, nearestMountedAncestor);
          break;
        case 23:
        case 22:
          if (null !== fiber.memoizedState && null !== fiber.memoizedState.cachePool) {
            var cache = fiber.memoizedState.cachePool.pool;
            null != cache && cache.refCount++;
          }
          break;
        case 24:
          releaseCache(fiber.memoizedState.cache);
      }
      cache = fiber.child;
      if (null !== cache) cache.return = fiber, nextEffect = cache;
      else
        a: for (fiber = deletedSubtreeRoot; null !== nextEffect; ) {
          cache = nextEffect;
          var sibling = cache.sibling, returnFiber = cache.return;
          detachFiberAfterEffects(cache);
          if (cache === fiber) {
            nextEffect = null;
            break a;
          }
          if (null !== sibling) {
            sibling.return = returnFiber;
            nextEffect = sibling;
            break a;
          }
          nextEffect = returnFiber;
        }
    }
  }
  var DefaultAsyncDispatcher = {
    getCacheForType: function(resourceType) {
      var cache = readContext(CacheContext), cacheForType = cache.data.get(resourceType);
      void 0 === cacheForType && (cacheForType = resourceType(), cache.data.set(resourceType, cacheForType));
      return cacheForType;
    },
    cacheSignal: function() {
      return readContext(CacheContext).controller.signal;
    }
  }, PossiblyWeakMap = "function" === typeof WeakMap ? WeakMap : Map, executionContext = 0, workInProgressRoot = null, workInProgress = null, workInProgressRootRenderLanes = 0, workInProgressSuspendedReason = 0, workInProgressThrownValue = null, workInProgressRootDidSkipSuspendedSiblings = false, workInProgressRootIsPrerendering = false, workInProgressRootDidAttachPingListener = false, entangledRenderLanes = 0, workInProgressRootExitStatus = 0, workInProgressRootSkippedLanes = 0, workInProgressRootInterleavedUpdatedLanes = 0, workInProgressRootPingedLanes = 0, workInProgressDeferredLane = 0, workInProgressSuspendedRetryLanes = 0, workInProgressRootConcurrentErrors = null, workInProgressRootRecoverableErrors = null, workInProgressRootDidIncludeRecursiveRenderUpdate = false, globalMostRecentFallbackTime = 0, globalMostRecentTransitionTime = 0, workInProgressRootRenderTargetTime = Infinity, workInProgressTransitions = null, legacyErrorBoundariesThatAlreadyFailed = null, pendingEffectsStatus = 0, pendingEffectsRoot = null, pendingFinishedWork = null, pendingEffectsLanes = 0, pendingEffectsRemainingLanes = 0, pendingPassiveTransitions = null, pendingRecoverableErrors = null, nestedUpdateCount = 0, rootWithNestedUpdates = null;
  function requestUpdateLane() {
    return 0 !== (executionContext & 2) && 0 !== workInProgressRootRenderLanes ? workInProgressRootRenderLanes & -workInProgressRootRenderLanes : null !== ReactSharedInternals.T ? requestTransitionLane() : resolveUpdatePriority();
  }
  function requestDeferredLane() {
    if (0 === workInProgressDeferredLane)
      if (0 === (workInProgressRootRenderLanes & 536870912) || isHydrating) {
        var lane = nextTransitionDeferredLane;
        nextTransitionDeferredLane <<= 1;
        0 === (nextTransitionDeferredLane & 3932160) && (nextTransitionDeferredLane = 262144);
        workInProgressDeferredLane = lane;
      } else workInProgressDeferredLane = 536870912;
    lane = suspenseHandlerStackCursor.current;
    null !== lane && (lane.flags |= 32);
    return workInProgressDeferredLane;
  }
  function scheduleUpdateOnFiber(root3, fiber, lane) {
    if (root3 === workInProgressRoot && (2 === workInProgressSuspendedReason || 9 === workInProgressSuspendedReason) || null !== root3.cancelPendingCommit)
      prepareFreshStack(root3, 0), markRootSuspended(
        root3,
        workInProgressRootRenderLanes,
        workInProgressDeferredLane,
        false
      );
    markRootUpdated$1(root3, lane);
    if (0 === (executionContext & 2) || root3 !== workInProgressRoot)
      root3 === workInProgressRoot && (0 === (executionContext & 2) && (workInProgressRootInterleavedUpdatedLanes |= lane), 4 === workInProgressRootExitStatus && markRootSuspended(
        root3,
        workInProgressRootRenderLanes,
        workInProgressDeferredLane,
        false
      )), ensureRootIsScheduled(root3);
  }
  function performWorkOnRoot(root$jscomp$0, lanes, forceSync) {
    if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(327));
    var shouldTimeSlice = !forceSync && 0 === (lanes & 127) && 0 === (lanes & root$jscomp$0.expiredLanes) || checkIfRootIsPrerendering(root$jscomp$0, lanes), exitStatus = shouldTimeSlice ? renderRootConcurrent(root$jscomp$0, lanes) : renderRootSync(root$jscomp$0, lanes, true), renderWasConcurrent = shouldTimeSlice;
    do {
      if (0 === exitStatus) {
        workInProgressRootIsPrerendering && !shouldTimeSlice && markRootSuspended(root$jscomp$0, lanes, 0, false);
        break;
      } else {
        forceSync = root$jscomp$0.current.alternate;
        if (renderWasConcurrent && !isRenderConsistentWithExternalStores(forceSync)) {
          exitStatus = renderRootSync(root$jscomp$0, lanes, false);
          renderWasConcurrent = false;
          continue;
        }
        if (2 === exitStatus) {
          renderWasConcurrent = lanes;
          if (root$jscomp$0.errorRecoveryDisabledLanes & renderWasConcurrent)
            var JSCompiler_inline_result = 0;
          else
            JSCompiler_inline_result = root$jscomp$0.pendingLanes & -536870913, JSCompiler_inline_result = 0 !== JSCompiler_inline_result ? JSCompiler_inline_result : JSCompiler_inline_result & 536870912 ? 536870912 : 0;
          if (0 !== JSCompiler_inline_result) {
            lanes = JSCompiler_inline_result;
            a: {
              var root3 = root$jscomp$0;
              exitStatus = workInProgressRootConcurrentErrors;
              var wasRootDehydrated = root3.current.memoizedState.isDehydrated;
              wasRootDehydrated && (prepareFreshStack(root3, JSCompiler_inline_result).flags |= 256);
              JSCompiler_inline_result = renderRootSync(
                root3,
                JSCompiler_inline_result,
                false
              );
              if (2 !== JSCompiler_inline_result) {
                if (workInProgressRootDidAttachPingListener && !wasRootDehydrated) {
                  root3.errorRecoveryDisabledLanes |= renderWasConcurrent;
                  workInProgressRootInterleavedUpdatedLanes |= renderWasConcurrent;
                  exitStatus = 4;
                  break a;
                }
                renderWasConcurrent = workInProgressRootRecoverableErrors;
                workInProgressRootRecoverableErrors = exitStatus;
                null !== renderWasConcurrent && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = renderWasConcurrent : workInProgressRootRecoverableErrors.push.apply(
                  workInProgressRootRecoverableErrors,
                  renderWasConcurrent
                ));
              }
              exitStatus = JSCompiler_inline_result;
            }
            renderWasConcurrent = false;
            if (2 !== exitStatus) continue;
          }
        }
        if (1 === exitStatus) {
          prepareFreshStack(root$jscomp$0, 0);
          markRootSuspended(root$jscomp$0, lanes, 0, true);
          break;
        }
        a: {
          shouldTimeSlice = root$jscomp$0;
          renderWasConcurrent = exitStatus;
          switch (renderWasConcurrent) {
            case 0:
            case 1:
              throw Error(formatProdErrorMessage(345));
            case 4:
              if ((lanes & 4194048) !== lanes) break;
            case 6:
              markRootSuspended(
                shouldTimeSlice,
                lanes,
                workInProgressDeferredLane,
                !workInProgressRootDidSkipSuspendedSiblings
              );
              break a;
            case 2:
              workInProgressRootRecoverableErrors = null;
              break;
            case 3:
            case 5:
              break;
            default:
              throw Error(formatProdErrorMessage(329));
          }
          if ((lanes & 62914560) === lanes && (exitStatus = globalMostRecentFallbackTime + 300 - now(), 10 < exitStatus)) {
            markRootSuspended(
              shouldTimeSlice,
              lanes,
              workInProgressDeferredLane,
              !workInProgressRootDidSkipSuspendedSiblings
            );
            if (0 !== getNextLanes(shouldTimeSlice, 0, true)) break a;
            pendingEffectsLanes = lanes;
            shouldTimeSlice.timeoutHandle = scheduleTimeout(
              commitRootWhenReady.bind(
                null,
                shouldTimeSlice,
                forceSync,
                workInProgressRootRecoverableErrors,
                workInProgressTransitions,
                workInProgressRootDidIncludeRecursiveRenderUpdate,
                lanes,
                workInProgressDeferredLane,
                workInProgressRootInterleavedUpdatedLanes,
                workInProgressSuspendedRetryLanes,
                workInProgressRootDidSkipSuspendedSiblings,
                renderWasConcurrent,
                "Throttled",
                -0,
                0
              ),
              exitStatus
            );
            break a;
          }
          commitRootWhenReady(
            shouldTimeSlice,
            forceSync,
            workInProgressRootRecoverableErrors,
            workInProgressTransitions,
            workInProgressRootDidIncludeRecursiveRenderUpdate,
            lanes,
            workInProgressDeferredLane,
            workInProgressRootInterleavedUpdatedLanes,
            workInProgressSuspendedRetryLanes,
            workInProgressRootDidSkipSuspendedSiblings,
            renderWasConcurrent,
            null,
            -0,
            0
          );
        }
      }
      break;
    } while (1);
    ensureRootIsScheduled(root$jscomp$0);
  }
  function commitRootWhenReady(root3, finishedWork, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, lanes, spawnedLane, updatedLanes, suspendedRetryLanes, didSkipSuspendedSiblings, exitStatus, suspendedCommitReason, completedRenderStartTime, completedRenderEndTime) {
    root3.timeoutHandle = -1;
    suspendedCommitReason = finishedWork.subtreeFlags;
    if (suspendedCommitReason & 8192 || 16785408 === (suspendedCommitReason & 16785408)) {
      suspendedCommitReason = {
        stylesheets: null,
        count: 0,
        imgCount: 0,
        imgBytes: 0,
        suspenseyImages: [],
        waitingForImages: true,
        waitingForViewTransition: false,
        unsuspend: noop$1
      };
      accumulateSuspenseyCommitOnFiber(
        finishedWork,
        lanes,
        suspendedCommitReason
      );
      var timeoutOffset = (lanes & 62914560) === lanes ? globalMostRecentFallbackTime - now() : (lanes & 4194048) === lanes ? globalMostRecentTransitionTime - now() : 0;
      timeoutOffset = waitForCommitToBeReady(
        suspendedCommitReason,
        timeoutOffset
      );
      if (null !== timeoutOffset) {
        pendingEffectsLanes = lanes;
        root3.cancelPendingCommit = timeoutOffset(
          commitRoot.bind(
            null,
            root3,
            finishedWork,
            lanes,
            recoverableErrors,
            transitions,
            didIncludeRenderPhaseUpdate,
            spawnedLane,
            updatedLanes,
            suspendedRetryLanes,
            exitStatus,
            suspendedCommitReason,
            null,
            completedRenderStartTime,
            completedRenderEndTime
          )
        );
        markRootSuspended(root3, lanes, spawnedLane, !didSkipSuspendedSiblings);
        return;
      }
    }
    commitRoot(
      root3,
      finishedWork,
      lanes,
      recoverableErrors,
      transitions,
      didIncludeRenderPhaseUpdate,
      spawnedLane,
      updatedLanes,
      suspendedRetryLanes
    );
  }
  function isRenderConsistentWithExternalStores(finishedWork) {
    for (var node = finishedWork; ; ) {
      var tag = node.tag;
      if ((0 === tag || 11 === tag || 15 === tag) && node.flags & 16384 && (tag = node.updateQueue, null !== tag && (tag = tag.stores, null !== tag)))
        for (var i = 0; i < tag.length; i++) {
          var check = tag[i], getSnapshot = check.getSnapshot;
          check = check.value;
          try {
            if (!objectIs(getSnapshot(), check)) return false;
          } catch (error) {
            return false;
          }
        }
      tag = node.child;
      if (node.subtreeFlags & 16384 && null !== tag)
        tag.return = node, node = tag;
      else {
        if (node === finishedWork) break;
        for (; null === node.sibling; ) {
          if (null === node.return || node.return === finishedWork) return true;
          node = node.return;
        }
        node.sibling.return = node.return;
        node = node.sibling;
      }
    }
    return true;
  }
  function markRootSuspended(root3, suspendedLanes, spawnedLane, didAttemptEntireTree) {
    suspendedLanes &= ~workInProgressRootPingedLanes;
    suspendedLanes &= ~workInProgressRootInterleavedUpdatedLanes;
    root3.suspendedLanes |= suspendedLanes;
    root3.pingedLanes &= ~suspendedLanes;
    didAttemptEntireTree && (root3.warmLanes |= suspendedLanes);
    didAttemptEntireTree = root3.expirationTimes;
    for (var lanes = suspendedLanes; 0 < lanes; ) {
      var index$6 = 31 - clz32(lanes), lane = 1 << index$6;
      didAttemptEntireTree[index$6] = -1;
      lanes &= ~lane;
    }
    0 !== spawnedLane && markSpawnedDeferredLane(root3, spawnedLane, suspendedLanes);
  }
  function flushSyncWork$1() {
    return 0 === (executionContext & 6) ? (flushSyncWorkAcrossRoots_impl(0), false) : true;
  }
  function resetWorkInProgressStack() {
    if (null !== workInProgress) {
      if (0 === workInProgressSuspendedReason)
        var interruptedWork = workInProgress.return;
      else
        interruptedWork = workInProgress, lastContextDependency = currentlyRenderingFiber$1 = null, resetHooksOnUnwind(interruptedWork), thenableState$1 = null, thenableIndexCounter$1 = 0, interruptedWork = workInProgress;
      for (; null !== interruptedWork; )
        unwindInterruptedWork(interruptedWork.alternate, interruptedWork), interruptedWork = interruptedWork.return;
      workInProgress = null;
    }
  }
  function prepareFreshStack(root3, lanes) {
    var timeoutHandle = root3.timeoutHandle;
    -1 !== timeoutHandle && (root3.timeoutHandle = -1, cancelTimeout(timeoutHandle));
    timeoutHandle = root3.cancelPendingCommit;
    null !== timeoutHandle && (root3.cancelPendingCommit = null, timeoutHandle());
    pendingEffectsLanes = 0;
    resetWorkInProgressStack();
    workInProgressRoot = root3;
    workInProgress = timeoutHandle = createWorkInProgress(root3.current, null);
    workInProgressRootRenderLanes = lanes;
    workInProgressSuspendedReason = 0;
    workInProgressThrownValue = null;
    workInProgressRootDidSkipSuspendedSiblings = false;
    workInProgressRootIsPrerendering = checkIfRootIsPrerendering(root3, lanes);
    workInProgressRootDidAttachPingListener = false;
    workInProgressSuspendedRetryLanes = workInProgressDeferredLane = workInProgressRootPingedLanes = workInProgressRootInterleavedUpdatedLanes = workInProgressRootSkippedLanes = workInProgressRootExitStatus = 0;
    workInProgressRootRecoverableErrors = workInProgressRootConcurrentErrors = null;
    workInProgressRootDidIncludeRecursiveRenderUpdate = false;
    0 !== (lanes & 8) && (lanes |= lanes & 32);
    var allEntangledLanes = root3.entangledLanes;
    if (0 !== allEntangledLanes)
      for (root3 = root3.entanglements, allEntangledLanes &= lanes; 0 < allEntangledLanes; ) {
        var index$4 = 31 - clz32(allEntangledLanes), lane = 1 << index$4;
        lanes |= root3[index$4];
        allEntangledLanes &= ~lane;
      }
    entangledRenderLanes = lanes;
    finishQueueingConcurrentUpdates();
    return timeoutHandle;
  }
  function handleThrow(root3, thrownValue) {
    currentlyRenderingFiber = null;
    ReactSharedInternals.H = ContextOnlyDispatcher;
    thrownValue === SuspenseException || thrownValue === SuspenseActionException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = 3) : thrownValue === SuspenseyCommitException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = 4) : workInProgressSuspendedReason = thrownValue === SelectiveHydrationException ? 8 : null !== thrownValue && "object" === typeof thrownValue && "function" === typeof thrownValue.then ? 6 : 1;
    workInProgressThrownValue = thrownValue;
    null === workInProgress && (workInProgressRootExitStatus = 1, logUncaughtError(
      root3,
      createCapturedValueAtFiber(thrownValue, root3.current)
    ));
  }
  function shouldRemainOnPreviousScreen() {
    var handler = suspenseHandlerStackCursor.current;
    return null === handler ? true : (workInProgressRootRenderLanes & 4194048) === workInProgressRootRenderLanes ? null === shellBoundary ? true : false : (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes || 0 !== (workInProgressRootRenderLanes & 536870912) ? handler === shellBoundary : false;
  }
  function pushDispatcher() {
    var prevDispatcher = ReactSharedInternals.H;
    ReactSharedInternals.H = ContextOnlyDispatcher;
    return null === prevDispatcher ? ContextOnlyDispatcher : prevDispatcher;
  }
  function pushAsyncDispatcher() {
    var prevAsyncDispatcher = ReactSharedInternals.A;
    ReactSharedInternals.A = DefaultAsyncDispatcher;
    return prevAsyncDispatcher;
  }
  function renderDidSuspendDelayIfPossible() {
    workInProgressRootExitStatus = 4;
    workInProgressRootDidSkipSuspendedSiblings || (workInProgressRootRenderLanes & 4194048) !== workInProgressRootRenderLanes && null !== suspenseHandlerStackCursor.current || (workInProgressRootIsPrerendering = true);
    0 === (workInProgressRootSkippedLanes & 134217727) && 0 === (workInProgressRootInterleavedUpdatedLanes & 134217727) || null === workInProgressRoot || markRootSuspended(
      workInProgressRoot,
      workInProgressRootRenderLanes,
      workInProgressDeferredLane,
      false
    );
  }
  function renderRootSync(root3, lanes, shouldYieldForPrerendering) {
    var prevExecutionContext = executionContext;
    executionContext |= 2;
    var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
    if (workInProgressRoot !== root3 || workInProgressRootRenderLanes !== lanes)
      workInProgressTransitions = null, prepareFreshStack(root3, lanes);
    lanes = false;
    var exitStatus = workInProgressRootExitStatus;
    a: do
      try {
        if (0 !== workInProgressSuspendedReason && null !== workInProgress) {
          var unitOfWork = workInProgress, thrownValue = workInProgressThrownValue;
          switch (workInProgressSuspendedReason) {
            case 8:
              resetWorkInProgressStack();
              exitStatus = 6;
              break a;
            case 3:
            case 2:
            case 9:
            case 6:
              null === suspenseHandlerStackCursor.current && (lanes = true);
              var reason = workInProgressSuspendedReason;
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, unitOfWork, thrownValue, reason);
              if (shouldYieldForPrerendering && workInProgressRootIsPrerendering) {
                exitStatus = 0;
                break a;
              }
              break;
            default:
              reason = workInProgressSuspendedReason, workInProgressSuspendedReason = 0, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root3, unitOfWork, thrownValue, reason);
          }
        }
        workLoopSync();
        exitStatus = workInProgressRootExitStatus;
        break;
      } catch (thrownValue$165) {
        handleThrow(root3, thrownValue$165);
      }
    while (1);
    lanes && root3.shellSuspendCounter++;
    lastContextDependency = currentlyRenderingFiber$1 = null;
    executionContext = prevExecutionContext;
    ReactSharedInternals.H = prevDispatcher;
    ReactSharedInternals.A = prevAsyncDispatcher;
    null === workInProgress && (workInProgressRoot = null, workInProgressRootRenderLanes = 0, finishQueueingConcurrentUpdates());
    return exitStatus;
  }
  function workLoopSync() {
    for (; null !== workInProgress; ) performUnitOfWork(workInProgress);
  }
  function renderRootConcurrent(root3, lanes) {
    var prevExecutionContext = executionContext;
    executionContext |= 2;
    var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
    workInProgressRoot !== root3 || workInProgressRootRenderLanes !== lanes ? (workInProgressTransitions = null, workInProgressRootRenderTargetTime = now() + 500, prepareFreshStack(root3, lanes)) : workInProgressRootIsPrerendering = checkIfRootIsPrerendering(
      root3,
      lanes
    );
    a: do
      try {
        if (0 !== workInProgressSuspendedReason && null !== workInProgress) {
          lanes = workInProgress;
          var thrownValue = workInProgressThrownValue;
          b: switch (workInProgressSuspendedReason) {
            case 1:
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, lanes, thrownValue, 1);
              break;
            case 2:
            case 9:
              if (isThenableResolved(thrownValue)) {
                workInProgressSuspendedReason = 0;
                workInProgressThrownValue = null;
                replaySuspendedUnitOfWork(lanes);
                break;
              }
              lanes = function() {
                2 !== workInProgressSuspendedReason && 9 !== workInProgressSuspendedReason || workInProgressRoot !== root3 || (workInProgressSuspendedReason = 7);
                ensureRootIsScheduled(root3);
              };
              thrownValue.then(lanes, lanes);
              break a;
            case 3:
              workInProgressSuspendedReason = 7;
              break a;
            case 4:
              workInProgressSuspendedReason = 5;
              break a;
            case 7:
              isThenableResolved(thrownValue) ? (workInProgressSuspendedReason = 0, workInProgressThrownValue = null, replaySuspendedUnitOfWork(lanes)) : (workInProgressSuspendedReason = 0, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root3, lanes, thrownValue, 7));
              break;
            case 5:
              var resource = null;
              switch (workInProgress.tag) {
                case 26:
                  resource = workInProgress.memoizedState;
                case 5:
                case 27:
                  var hostFiber = workInProgress;
                  if (resource ? preloadResource(resource) : hostFiber.stateNode.complete) {
                    workInProgressSuspendedReason = 0;
                    workInProgressThrownValue = null;
                    var sibling = hostFiber.sibling;
                    if (null !== sibling) workInProgress = sibling;
                    else {
                      var returnFiber = hostFiber.return;
                      null !== returnFiber ? (workInProgress = returnFiber, completeUnitOfWork(returnFiber)) : workInProgress = null;
                    }
                    break b;
                  }
              }
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, lanes, thrownValue, 5);
              break;
            case 6:
              workInProgressSuspendedReason = 0;
              workInProgressThrownValue = null;
              throwAndUnwindWorkLoop(root3, lanes, thrownValue, 6);
              break;
            case 8:
              resetWorkInProgressStack();
              workInProgressRootExitStatus = 6;
              break a;
            default:
              throw Error(formatProdErrorMessage(462));
          }
        }
        workLoopConcurrentByScheduler();
        break;
      } catch (thrownValue$167) {
        handleThrow(root3, thrownValue$167);
      }
    while (1);
    lastContextDependency = currentlyRenderingFiber$1 = null;
    ReactSharedInternals.H = prevDispatcher;
    ReactSharedInternals.A = prevAsyncDispatcher;
    executionContext = prevExecutionContext;
    if (null !== workInProgress) return 0;
    workInProgressRoot = null;
    workInProgressRootRenderLanes = 0;
    finishQueueingConcurrentUpdates();
    return workInProgressRootExitStatus;
  }
  function workLoopConcurrentByScheduler() {
    for (; null !== workInProgress && !shouldYield(); )
      performUnitOfWork(workInProgress);
  }
  function performUnitOfWork(unitOfWork) {
    var next = beginWork(unitOfWork.alternate, unitOfWork, entangledRenderLanes);
    unitOfWork.memoizedProps = unitOfWork.pendingProps;
    null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
  }
  function replaySuspendedUnitOfWork(unitOfWork) {
    var next = unitOfWork;
    var current = next.alternate;
    switch (next.tag) {
      case 15:
      case 0:
        next = replayFunctionComponent(
          current,
          next,
          next.pendingProps,
          next.type,
          void 0,
          workInProgressRootRenderLanes
        );
        break;
      case 11:
        next = replayFunctionComponent(
          current,
          next,
          next.pendingProps,
          next.type.render,
          next.ref,
          workInProgressRootRenderLanes
        );
        break;
      case 5:
        resetHooksOnUnwind(next);
      default:
        unwindInterruptedWork(current, next), next = workInProgress = resetWorkInProgress(next, entangledRenderLanes), next = beginWork(current, next, entangledRenderLanes);
    }
    unitOfWork.memoizedProps = unitOfWork.pendingProps;
    null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
  }
  function throwAndUnwindWorkLoop(root3, unitOfWork, thrownValue, suspendedReason) {
    lastContextDependency = currentlyRenderingFiber$1 = null;
    resetHooksOnUnwind(unitOfWork);
    thenableState$1 = null;
    thenableIndexCounter$1 = 0;
    var returnFiber = unitOfWork.return;
    try {
      if (throwException(
        root3,
        returnFiber,
        unitOfWork,
        thrownValue,
        workInProgressRootRenderLanes
      )) {
        workInProgressRootExitStatus = 1;
        logUncaughtError(
          root3,
          createCapturedValueAtFiber(thrownValue, root3.current)
        );
        workInProgress = null;
        return;
      }
    } catch (error) {
      if (null !== returnFiber) throw workInProgress = returnFiber, error;
      workInProgressRootExitStatus = 1;
      logUncaughtError(
        root3,
        createCapturedValueAtFiber(thrownValue, root3.current)
      );
      workInProgress = null;
      return;
    }
    if (unitOfWork.flags & 32768) {
      if (isHydrating || 1 === suspendedReason) root3 = true;
      else if (workInProgressRootIsPrerendering || 0 !== (workInProgressRootRenderLanes & 536870912))
        root3 = false;
      else if (workInProgressRootDidSkipSuspendedSiblings = root3 = true, 2 === suspendedReason || 9 === suspendedReason || 3 === suspendedReason || 6 === suspendedReason)
        suspendedReason = suspenseHandlerStackCursor.current, null !== suspendedReason && 13 === suspendedReason.tag && (suspendedReason.flags |= 16384);
      unwindUnitOfWork(unitOfWork, root3);
    } else completeUnitOfWork(unitOfWork);
  }
  function completeUnitOfWork(unitOfWork) {
    var completedWork = unitOfWork;
    do {
      if (0 !== (completedWork.flags & 32768)) {
        unwindUnitOfWork(
          completedWork,
          workInProgressRootDidSkipSuspendedSiblings
        );
        return;
      }
      unitOfWork = completedWork.return;
      var next = completeWork(
        completedWork.alternate,
        completedWork,
        entangledRenderLanes
      );
      if (null !== next) {
        workInProgress = next;
        return;
      }
      completedWork = completedWork.sibling;
      if (null !== completedWork) {
        workInProgress = completedWork;
        return;
      }
      workInProgress = completedWork = unitOfWork;
    } while (null !== completedWork);
    0 === workInProgressRootExitStatus && (workInProgressRootExitStatus = 5);
  }
  function unwindUnitOfWork(unitOfWork, skipSiblings) {
    do {
      var next = unwindWork(unitOfWork.alternate, unitOfWork);
      if (null !== next) {
        next.flags &= 32767;
        workInProgress = next;
        return;
      }
      next = unitOfWork.return;
      null !== next && (next.flags |= 32768, next.subtreeFlags = 0, next.deletions = null);
      if (!skipSiblings && (unitOfWork = unitOfWork.sibling, null !== unitOfWork)) {
        workInProgress = unitOfWork;
        return;
      }
      workInProgress = unitOfWork = next;
    } while (null !== unitOfWork);
    workInProgressRootExitStatus = 6;
    workInProgress = null;
  }
  function commitRoot(root3, finishedWork, lanes, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes) {
    root3.cancelPendingCommit = null;
    do
      flushPendingEffects();
    while (0 !== pendingEffectsStatus);
    if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(327));
    if (null !== finishedWork) {
      if (finishedWork === root3.current) throw Error(formatProdErrorMessage(177));
      didIncludeRenderPhaseUpdate = finishedWork.lanes | finishedWork.childLanes;
      didIncludeRenderPhaseUpdate |= concurrentlyUpdatedLanes;
      markRootFinished(
        root3,
        lanes,
        didIncludeRenderPhaseUpdate,
        spawnedLane,
        updatedLanes,
        suspendedRetryLanes
      );
      root3 === workInProgressRoot && (workInProgress = workInProgressRoot = null, workInProgressRootRenderLanes = 0);
      pendingFinishedWork = finishedWork;
      pendingEffectsRoot = root3;
      pendingEffectsLanes = lanes;
      pendingEffectsRemainingLanes = didIncludeRenderPhaseUpdate;
      pendingPassiveTransitions = transitions;
      pendingRecoverableErrors = recoverableErrors;
      0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? (root3.callbackNode = null, root3.callbackPriority = 0, scheduleCallback$1(NormalPriority$1, function() {
        flushPassiveEffects();
        return null;
      })) : (root3.callbackNode = null, root3.callbackPriority = 0);
      recoverableErrors = 0 !== (finishedWork.flags & 13878);
      if (0 !== (finishedWork.subtreeFlags & 13878) || recoverableErrors) {
        recoverableErrors = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        transitions = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        spawnedLane = executionContext;
        executionContext |= 4;
        try {
          commitBeforeMutationEffects(root3, finishedWork, lanes);
        } finally {
          executionContext = spawnedLane, ReactDOMSharedInternals.p = transitions, ReactSharedInternals.T = recoverableErrors;
        }
      }
      pendingEffectsStatus = 1;
      flushMutationEffects();
      flushLayoutEffects();
      flushSpawnedWork();
    }
  }
  function flushMutationEffects() {
    if (1 === pendingEffectsStatus) {
      pendingEffectsStatus = 0;
      var root3 = pendingEffectsRoot, finishedWork = pendingFinishedWork, rootMutationHasEffect = 0 !== (finishedWork.flags & 13878);
      if (0 !== (finishedWork.subtreeFlags & 13878) || rootMutationHasEffect) {
        rootMutationHasEffect = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        var previousPriority = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        var prevExecutionContext = executionContext;
        executionContext |= 4;
        try {
          commitMutationEffectsOnFiber(finishedWork, root3);
          var priorSelectionInformation = selectionInformation, curFocusedElem = getActiveElementDeep(root3.containerInfo), priorFocusedElem = priorSelectionInformation.focusedElem, priorSelectionRange = priorSelectionInformation.selectionRange;
          if (curFocusedElem !== priorFocusedElem && priorFocusedElem && priorFocusedElem.ownerDocument && containsNode(
            priorFocusedElem.ownerDocument.documentElement,
            priorFocusedElem
          )) {
            if (null !== priorSelectionRange && hasSelectionCapabilities(priorFocusedElem)) {
              var start = priorSelectionRange.start, end = priorSelectionRange.end;
              void 0 === end && (end = start);
              if ("selectionStart" in priorFocusedElem)
                priorFocusedElem.selectionStart = start, priorFocusedElem.selectionEnd = Math.min(
                  end,
                  priorFocusedElem.value.length
                );
              else {
                var doc = priorFocusedElem.ownerDocument || document, win = doc && doc.defaultView || window;
                if (win.getSelection) {
                  var selection = win.getSelection(), length = priorFocusedElem.textContent.length, start$jscomp$0 = Math.min(priorSelectionRange.start, length), end$jscomp$0 = void 0 === priorSelectionRange.end ? start$jscomp$0 : Math.min(priorSelectionRange.end, length);
                  !selection.extend && start$jscomp$0 > end$jscomp$0 && (curFocusedElem = end$jscomp$0, end$jscomp$0 = start$jscomp$0, start$jscomp$0 = curFocusedElem);
                  var startMarker = getNodeForCharacterOffset(
                    priorFocusedElem,
                    start$jscomp$0
                  ), endMarker = getNodeForCharacterOffset(
                    priorFocusedElem,
                    end$jscomp$0
                  );
                  if (startMarker && endMarker && (1 !== selection.rangeCount || selection.anchorNode !== startMarker.node || selection.anchorOffset !== startMarker.offset || selection.focusNode !== endMarker.node || selection.focusOffset !== endMarker.offset)) {
                    var range = doc.createRange();
                    range.setStart(startMarker.node, startMarker.offset);
                    selection.removeAllRanges();
                    start$jscomp$0 > end$jscomp$0 ? (selection.addRange(range), selection.extend(endMarker.node, endMarker.offset)) : (range.setEnd(endMarker.node, endMarker.offset), selection.addRange(range));
                  }
                }
              }
            }
            doc = [];
            for (selection = priorFocusedElem; selection = selection.parentNode; )
              1 === selection.nodeType && doc.push({
                element: selection,
                left: selection.scrollLeft,
                top: selection.scrollTop
              });
            "function" === typeof priorFocusedElem.focus && priorFocusedElem.focus();
            for (priorFocusedElem = 0; priorFocusedElem < doc.length; priorFocusedElem++) {
              var info = doc[priorFocusedElem];
              info.element.scrollLeft = info.left;
              info.element.scrollTop = info.top;
            }
          }
          _enabled = !!eventsEnabled;
          selectionInformation = eventsEnabled = null;
        } finally {
          executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootMutationHasEffect;
        }
      }
      root3.current = finishedWork;
      pendingEffectsStatus = 2;
    }
  }
  function flushLayoutEffects() {
    if (2 === pendingEffectsStatus) {
      pendingEffectsStatus = 0;
      var root3 = pendingEffectsRoot, finishedWork = pendingFinishedWork, rootHasLayoutEffect = 0 !== (finishedWork.flags & 8772);
      if (0 !== (finishedWork.subtreeFlags & 8772) || rootHasLayoutEffect) {
        rootHasLayoutEffect = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        var previousPriority = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        var prevExecutionContext = executionContext;
        executionContext |= 4;
        try {
          commitLayoutEffectOnFiber(root3, finishedWork.alternate, finishedWork);
        } finally {
          executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootHasLayoutEffect;
        }
      }
      pendingEffectsStatus = 3;
    }
  }
  function flushSpawnedWork() {
    if (4 === pendingEffectsStatus || 3 === pendingEffectsStatus) {
      pendingEffectsStatus = 0;
      requestPaint();
      var root3 = pendingEffectsRoot, finishedWork = pendingFinishedWork, lanes = pendingEffectsLanes, recoverableErrors = pendingRecoverableErrors;
      0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? pendingEffectsStatus = 5 : (pendingEffectsStatus = 0, pendingFinishedWork = pendingEffectsRoot = null, releaseRootPooledCache(root3, root3.pendingLanes));
      var remainingLanes = root3.pendingLanes;
      0 === remainingLanes && (legacyErrorBoundariesThatAlreadyFailed = null);
      lanesToEventPriority(lanes);
      finishedWork = finishedWork.stateNode;
      if (injectedHook && "function" === typeof injectedHook.onCommitFiberRoot)
        try {
          injectedHook.onCommitFiberRoot(
            rendererID,
            finishedWork,
            void 0,
            128 === (finishedWork.current.flags & 128)
          );
        } catch (err) {
        }
      if (null !== recoverableErrors) {
        finishedWork = ReactSharedInternals.T;
        remainingLanes = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 2;
        ReactSharedInternals.T = null;
        try {
          for (var onRecoverableError = root3.onRecoverableError, i = 0; i < recoverableErrors.length; i++) {
            var recoverableError = recoverableErrors[i];
            onRecoverableError(recoverableError.value, {
              componentStack: recoverableError.stack
            });
          }
        } finally {
          ReactSharedInternals.T = finishedWork, ReactDOMSharedInternals.p = remainingLanes;
        }
      }
      0 !== (pendingEffectsLanes & 3) && flushPendingEffects();
      ensureRootIsScheduled(root3);
      remainingLanes = root3.pendingLanes;
      0 !== (lanes & 261930) && 0 !== (remainingLanes & 42) ? root3 === rootWithNestedUpdates ? nestedUpdateCount++ : (nestedUpdateCount = 0, rootWithNestedUpdates = root3) : nestedUpdateCount = 0;
      flushSyncWorkAcrossRoots_impl(0);
    }
  }
  function releaseRootPooledCache(root3, remainingLanes) {
    0 === (root3.pooledCacheLanes &= remainingLanes) && (remainingLanes = root3.pooledCache, null != remainingLanes && (root3.pooledCache = null, releaseCache(remainingLanes)));
  }
  function flushPendingEffects() {
    flushMutationEffects();
    flushLayoutEffects();
    flushSpawnedWork();
    return flushPassiveEffects();
  }
  function flushPassiveEffects() {
    if (5 !== pendingEffectsStatus) return false;
    var root3 = pendingEffectsRoot, remainingLanes = pendingEffectsRemainingLanes;
    pendingEffectsRemainingLanes = 0;
    var renderPriority = lanesToEventPriority(pendingEffectsLanes), prevTransition = ReactSharedInternals.T, previousPriority = ReactDOMSharedInternals.p;
    try {
      ReactDOMSharedInternals.p = 32 > renderPriority ? 32 : renderPriority;
      ReactSharedInternals.T = null;
      renderPriority = pendingPassiveTransitions;
      pendingPassiveTransitions = null;
      var root$jscomp$0 = pendingEffectsRoot, lanes = pendingEffectsLanes;
      pendingEffectsStatus = 0;
      pendingFinishedWork = pendingEffectsRoot = null;
      pendingEffectsLanes = 0;
      if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(331));
      var prevExecutionContext = executionContext;
      executionContext |= 4;
      commitPassiveUnmountOnFiber(root$jscomp$0.current);
      commitPassiveMountOnFiber(
        root$jscomp$0,
        root$jscomp$0.current,
        lanes,
        renderPriority
      );
      executionContext = prevExecutionContext;
      flushSyncWorkAcrossRoots_impl(0, false);
      if (injectedHook && "function" === typeof injectedHook.onPostCommitFiberRoot)
        try {
          injectedHook.onPostCommitFiberRoot(rendererID, root$jscomp$0);
        } catch (err) {
        }
      return true;
    } finally {
      ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition, releaseRootPooledCache(root3, remainingLanes);
    }
  }
  function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
    sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
    sourceFiber = createRootErrorUpdate(rootFiber.stateNode, sourceFiber, 2);
    rootFiber = enqueueUpdate(rootFiber, sourceFiber, 2);
    null !== rootFiber && (markRootUpdated$1(rootFiber, 2), ensureRootIsScheduled(rootFiber));
  }
  function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error) {
    if (3 === sourceFiber.tag)
      captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error);
    else
      for (; null !== nearestMountedAncestor; ) {
        if (3 === nearestMountedAncestor.tag) {
          captureCommitPhaseErrorOnRoot(
            nearestMountedAncestor,
            sourceFiber,
            error
          );
          break;
        } else if (1 === nearestMountedAncestor.tag) {
          var instance = nearestMountedAncestor.stateNode;
          if ("function" === typeof nearestMountedAncestor.type.getDerivedStateFromError || "function" === typeof instance.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(instance))) {
            sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
            error = createClassErrorUpdate(2);
            instance = enqueueUpdate(nearestMountedAncestor, error, 2);
            null !== instance && (initializeClassErrorUpdate(
              error,
              instance,
              nearestMountedAncestor,
              sourceFiber
            ), markRootUpdated$1(instance, 2), ensureRootIsScheduled(instance));
            break;
          }
        }
        nearestMountedAncestor = nearestMountedAncestor.return;
      }
  }
  function attachPingListener(root3, wakeable, lanes) {
    var pingCache = root3.pingCache;
    if (null === pingCache) {
      pingCache = root3.pingCache = new PossiblyWeakMap();
      var threadIDs = /* @__PURE__ */ new Set();
      pingCache.set(wakeable, threadIDs);
    } else
      threadIDs = pingCache.get(wakeable), void 0 === threadIDs && (threadIDs = /* @__PURE__ */ new Set(), pingCache.set(wakeable, threadIDs));
    threadIDs.has(lanes) || (workInProgressRootDidAttachPingListener = true, threadIDs.add(lanes), root3 = pingSuspendedRoot.bind(null, root3, wakeable, lanes), wakeable.then(root3, root3));
  }
  function pingSuspendedRoot(root3, wakeable, pingedLanes) {
    var pingCache = root3.pingCache;
    null !== pingCache && pingCache.delete(wakeable);
    root3.pingedLanes |= root3.suspendedLanes & pingedLanes;
    root3.warmLanes &= ~pingedLanes;
    workInProgressRoot === root3 && (workInProgressRootRenderLanes & pingedLanes) === pingedLanes && (4 === workInProgressRootExitStatus || 3 === workInProgressRootExitStatus && (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes && 300 > now() - globalMostRecentFallbackTime ? 0 === (executionContext & 2) && prepareFreshStack(root3, 0) : workInProgressRootPingedLanes |= pingedLanes, workInProgressSuspendedRetryLanes === workInProgressRootRenderLanes && (workInProgressSuspendedRetryLanes = 0));
    ensureRootIsScheduled(root3);
  }
  function retryTimedOutBoundary(boundaryFiber, retryLane) {
    0 === retryLane && (retryLane = claimNextRetryLane());
    boundaryFiber = enqueueConcurrentRenderForLane(boundaryFiber, retryLane);
    null !== boundaryFiber && (markRootUpdated$1(boundaryFiber, retryLane), ensureRootIsScheduled(boundaryFiber));
  }
  function retryDehydratedSuspenseBoundary(boundaryFiber) {
    var suspenseState = boundaryFiber.memoizedState, retryLane = 0;
    null !== suspenseState && (retryLane = suspenseState.retryLane);
    retryTimedOutBoundary(boundaryFiber, retryLane);
  }
  function resolveRetryWakeable(boundaryFiber, wakeable) {
    var retryLane = 0;
    switch (boundaryFiber.tag) {
      case 31:
      case 13:
        var retryCache = boundaryFiber.stateNode;
        var suspenseState = boundaryFiber.memoizedState;
        null !== suspenseState && (retryLane = suspenseState.retryLane);
        break;
      case 19:
        retryCache = boundaryFiber.stateNode;
        break;
      case 22:
        retryCache = boundaryFiber.stateNode._retryCache;
        break;
      default:
        throw Error(formatProdErrorMessage(314));
    }
    null !== retryCache && retryCache.delete(wakeable);
    retryTimedOutBoundary(boundaryFiber, retryLane);
  }
  function scheduleCallback$1(priorityLevel, callback) {
    return scheduleCallback$3(priorityLevel, callback);
  }
  var firstScheduledRoot = null, lastScheduledRoot = null, didScheduleMicrotask = false, mightHavePendingSyncWork = false, isFlushingWork = false, currentEventTransitionLane = 0;
  function ensureRootIsScheduled(root3) {
    root3 !== lastScheduledRoot && null === root3.next && (null === lastScheduledRoot ? firstScheduledRoot = lastScheduledRoot = root3 : lastScheduledRoot = lastScheduledRoot.next = root3);
    mightHavePendingSyncWork = true;
    didScheduleMicrotask || (didScheduleMicrotask = true, scheduleImmediateRootScheduleTask());
  }
  function flushSyncWorkAcrossRoots_impl(syncTransitionLanes, onlyLegacy) {
    if (!isFlushingWork && mightHavePendingSyncWork) {
      isFlushingWork = true;
      do {
        var didPerformSomeWork = false;
        for (var root$170 = firstScheduledRoot; null !== root$170; ) {
          if (0 !== syncTransitionLanes) {
            var pendingLanes = root$170.pendingLanes;
            if (0 === pendingLanes) var JSCompiler_inline_result = 0;
            else {
              var suspendedLanes = root$170.suspendedLanes, pingedLanes = root$170.pingedLanes;
              JSCompiler_inline_result = (1 << 31 - clz32(42 | syncTransitionLanes) + 1) - 1;
              JSCompiler_inline_result &= pendingLanes & ~(suspendedLanes & ~pingedLanes);
              JSCompiler_inline_result = JSCompiler_inline_result & 201326741 ? JSCompiler_inline_result & 201326741 | 1 : JSCompiler_inline_result ? JSCompiler_inline_result | 2 : 0;
            }
            0 !== JSCompiler_inline_result && (didPerformSomeWork = true, performSyncWorkOnRoot(root$170, JSCompiler_inline_result));
          } else
            JSCompiler_inline_result = workInProgressRootRenderLanes, JSCompiler_inline_result = getNextLanes(
              root$170,
              root$170 === workInProgressRoot ? JSCompiler_inline_result : 0,
              null !== root$170.cancelPendingCommit || -1 !== root$170.timeoutHandle
            ), 0 === (JSCompiler_inline_result & 3) || checkIfRootIsPrerendering(root$170, JSCompiler_inline_result) || (didPerformSomeWork = true, performSyncWorkOnRoot(root$170, JSCompiler_inline_result));
          root$170 = root$170.next;
        }
      } while (didPerformSomeWork);
      isFlushingWork = false;
    }
  }
  function processRootScheduleInImmediateTask() {
    processRootScheduleInMicrotask();
  }
  function processRootScheduleInMicrotask() {
    mightHavePendingSyncWork = didScheduleMicrotask = false;
    var syncTransitionLanes = 0;
    0 !== currentEventTransitionLane && shouldAttemptEagerTransition() && (syncTransitionLanes = currentEventTransitionLane);
    for (var currentTime = now(), prev = null, root3 = firstScheduledRoot; null !== root3; ) {
      var next = root3.next, nextLanes = scheduleTaskForRootDuringMicrotask(root3, currentTime);
      if (0 === nextLanes)
        root3.next = null, null === prev ? firstScheduledRoot = next : prev.next = next, null === next && (lastScheduledRoot = prev);
      else if (prev = root3, 0 !== syncTransitionLanes || 0 !== (nextLanes & 3))
        mightHavePendingSyncWork = true;
      root3 = next;
    }
    0 !== pendingEffectsStatus && 5 !== pendingEffectsStatus || flushSyncWorkAcrossRoots_impl(syncTransitionLanes);
    0 !== currentEventTransitionLane && (currentEventTransitionLane = 0);
  }
  function scheduleTaskForRootDuringMicrotask(root3, currentTime) {
    for (var suspendedLanes = root3.suspendedLanes, pingedLanes = root3.pingedLanes, expirationTimes = root3.expirationTimes, lanes = root3.pendingLanes & -62914561; 0 < lanes; ) {
      var index$5 = 31 - clz32(lanes), lane = 1 << index$5, expirationTime = expirationTimes[index$5];
      if (-1 === expirationTime) {
        if (0 === (lane & suspendedLanes) || 0 !== (lane & pingedLanes))
          expirationTimes[index$5] = computeExpirationTime(lane, currentTime);
      } else expirationTime <= currentTime && (root3.expiredLanes |= lane);
      lanes &= ~lane;
    }
    currentTime = workInProgressRoot;
    suspendedLanes = workInProgressRootRenderLanes;
    suspendedLanes = getNextLanes(
      root3,
      root3 === currentTime ? suspendedLanes : 0,
      null !== root3.cancelPendingCommit || -1 !== root3.timeoutHandle
    );
    pingedLanes = root3.callbackNode;
    if (0 === suspendedLanes || root3 === currentTime && (2 === workInProgressSuspendedReason || 9 === workInProgressSuspendedReason) || null !== root3.cancelPendingCommit)
      return null !== pingedLanes && null !== pingedLanes && cancelCallback$1(pingedLanes), root3.callbackNode = null, root3.callbackPriority = 0;
    if (0 === (suspendedLanes & 3) || checkIfRootIsPrerendering(root3, suspendedLanes)) {
      currentTime = suspendedLanes & -suspendedLanes;
      if (currentTime === root3.callbackPriority) return currentTime;
      null !== pingedLanes && cancelCallback$1(pingedLanes);
      switch (lanesToEventPriority(suspendedLanes)) {
        case 2:
        case 8:
          suspendedLanes = UserBlockingPriority;
          break;
        case 32:
          suspendedLanes = NormalPriority$1;
          break;
        case 268435456:
          suspendedLanes = IdlePriority;
          break;
        default:
          suspendedLanes = NormalPriority$1;
      }
      pingedLanes = performWorkOnRootViaSchedulerTask.bind(null, root3);
      suspendedLanes = scheduleCallback$3(suspendedLanes, pingedLanes);
      root3.callbackPriority = currentTime;
      root3.callbackNode = suspendedLanes;
      return currentTime;
    }
    null !== pingedLanes && null !== pingedLanes && cancelCallback$1(pingedLanes);
    root3.callbackPriority = 2;
    root3.callbackNode = null;
    return 2;
  }
  function performWorkOnRootViaSchedulerTask(root3, didTimeout) {
    if (0 !== pendingEffectsStatus && 5 !== pendingEffectsStatus)
      return root3.callbackNode = null, root3.callbackPriority = 0, null;
    var originalCallbackNode = root3.callbackNode;
    if (flushPendingEffects() && root3.callbackNode !== originalCallbackNode)
      return null;
    var workInProgressRootRenderLanes$jscomp$0 = workInProgressRootRenderLanes;
    workInProgressRootRenderLanes$jscomp$0 = getNextLanes(
      root3,
      root3 === workInProgressRoot ? workInProgressRootRenderLanes$jscomp$0 : 0,
      null !== root3.cancelPendingCommit || -1 !== root3.timeoutHandle
    );
    if (0 === workInProgressRootRenderLanes$jscomp$0) return null;
    performWorkOnRoot(root3, workInProgressRootRenderLanes$jscomp$0, didTimeout);
    scheduleTaskForRootDuringMicrotask(root3, now());
    return null != root3.callbackNode && root3.callbackNode === originalCallbackNode ? performWorkOnRootViaSchedulerTask.bind(null, root3) : null;
  }
  function performSyncWorkOnRoot(root3, lanes) {
    if (flushPendingEffects()) return null;
    performWorkOnRoot(root3, lanes, true);
  }
  function scheduleImmediateRootScheduleTask() {
    scheduleMicrotask(function() {
      0 !== (executionContext & 6) ? scheduleCallback$3(
        ImmediatePriority,
        processRootScheduleInImmediateTask
      ) : processRootScheduleInMicrotask();
    });
  }
  function requestTransitionLane() {
    if (0 === currentEventTransitionLane) {
      var actionScopeLane = currentEntangledLane;
      0 === actionScopeLane && (actionScopeLane = nextTransitionUpdateLane, nextTransitionUpdateLane <<= 1, 0 === (nextTransitionUpdateLane & 261888) && (nextTransitionUpdateLane = 256));
      currentEventTransitionLane = actionScopeLane;
    }
    return currentEventTransitionLane;
  }
  function coerceFormActionProp(actionProp) {
    return null == actionProp || "symbol" === typeof actionProp || "boolean" === typeof actionProp ? null : "function" === typeof actionProp ? actionProp : sanitizeURL("" + actionProp);
  }
  function createFormDataWithSubmitter(form, submitter) {
    var temp = submitter.ownerDocument.createElement("input");
    temp.name = submitter.name;
    temp.value = submitter.value;
    form.id && temp.setAttribute("form", form.id);
    submitter.parentNode.insertBefore(temp, submitter);
    form = new FormData(form);
    temp.parentNode.removeChild(temp);
    return form;
  }
  function extractEvents$1(dispatchQueue, domEventName, maybeTargetInst, nativeEvent, nativeEventTarget) {
    if ("submit" === domEventName && maybeTargetInst && maybeTargetInst.stateNode === nativeEventTarget) {
      var action = coerceFormActionProp(
        (nativeEventTarget[internalPropsKey] || null).action
      ), submitter = nativeEvent.submitter;
      submitter && (domEventName = (domEventName = submitter[internalPropsKey] || null) ? coerceFormActionProp(domEventName.formAction) : submitter.getAttribute("formAction"), null !== domEventName && (action = domEventName, submitter = null));
      var event = new SyntheticEvent(
        "action",
        "action",
        null,
        nativeEvent,
        nativeEventTarget
      );
      dispatchQueue.push({
        event,
        listeners: [
          {
            instance: null,
            listener: function() {
              if (nativeEvent.defaultPrevented) {
                if (0 !== currentEventTransitionLane) {
                  var formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget);
                  startHostTransition(
                    maybeTargetInst,
                    {
                      pending: true,
                      data: formData,
                      method: nativeEventTarget.method,
                      action
                    },
                    null,
                    formData
                  );
                }
              } else
                "function" === typeof action && (event.preventDefault(), formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget), startHostTransition(
                  maybeTargetInst,
                  {
                    pending: true,
                    data: formData,
                    method: nativeEventTarget.method,
                    action
                  },
                  action,
                  formData
                ));
            },
            currentTarget: nativeEventTarget
          }
        ]
      });
    }
  }
  for (var i$jscomp$inline_1577 = 0; i$jscomp$inline_1577 < simpleEventPluginEvents.length; i$jscomp$inline_1577++) {
    var eventName$jscomp$inline_1578 = simpleEventPluginEvents[i$jscomp$inline_1577], domEventName$jscomp$inline_1579 = eventName$jscomp$inline_1578.toLowerCase(), capitalizedEvent$jscomp$inline_1580 = eventName$jscomp$inline_1578[0].toUpperCase() + eventName$jscomp$inline_1578.slice(1);
    registerSimpleEvent(
      domEventName$jscomp$inline_1579,
      "on" + capitalizedEvent$jscomp$inline_1580
    );
  }
  registerSimpleEvent(ANIMATION_END, "onAnimationEnd");
  registerSimpleEvent(ANIMATION_ITERATION, "onAnimationIteration");
  registerSimpleEvent(ANIMATION_START, "onAnimationStart");
  registerSimpleEvent("dblclick", "onDoubleClick");
  registerSimpleEvent("focusin", "onFocus");
  registerSimpleEvent("focusout", "onBlur");
  registerSimpleEvent(TRANSITION_RUN, "onTransitionRun");
  registerSimpleEvent(TRANSITION_START, "onTransitionStart");
  registerSimpleEvent(TRANSITION_CANCEL, "onTransitionCancel");
  registerSimpleEvent(TRANSITION_END, "onTransitionEnd");
  registerDirectEvent("onMouseEnter", ["mouseout", "mouseover"]);
  registerDirectEvent("onMouseLeave", ["mouseout", "mouseover"]);
  registerDirectEvent("onPointerEnter", ["pointerout", "pointerover"]);
  registerDirectEvent("onPointerLeave", ["pointerout", "pointerover"]);
  registerTwoPhaseEvent(
    "onChange",
    "change click focusin focusout input keydown keyup selectionchange".split(" ")
  );
  registerTwoPhaseEvent(
    "onSelect",
    "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(
      " "
    )
  );
  registerTwoPhaseEvent("onBeforeInput", [
    "compositionend",
    "keypress",
    "textInput",
    "paste"
  ]);
  registerTwoPhaseEvent(
    "onCompositionEnd",
    "compositionend focusout keydown keypress keyup mousedown".split(" ")
  );
  registerTwoPhaseEvent(
    "onCompositionStart",
    "compositionstart focusout keydown keypress keyup mousedown".split(" ")
  );
  registerTwoPhaseEvent(
    "onCompositionUpdate",
    "compositionupdate focusout keydown keypress keyup mousedown".split(" ")
  );
  var mediaEventTypes = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(
    " "
  ), nonDelegatedEvents = new Set(
    "beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(mediaEventTypes)
  );
  function processDispatchQueue(dispatchQueue, eventSystemFlags) {
    eventSystemFlags = 0 !== (eventSystemFlags & 4);
    for (var i = 0; i < dispatchQueue.length; i++) {
      var _dispatchQueue$i = dispatchQueue[i], event = _dispatchQueue$i.event;
      _dispatchQueue$i = _dispatchQueue$i.listeners;
      a: {
        var previousInstance = void 0;
        if (eventSystemFlags)
          for (var i$jscomp$0 = _dispatchQueue$i.length - 1; 0 <= i$jscomp$0; i$jscomp$0--) {
            var _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget;
            _dispatchListeners$i = _dispatchListeners$i.listener;
            if (instance !== previousInstance && event.isPropagationStopped())
              break a;
            previousInstance = _dispatchListeners$i;
            event.currentTarget = currentTarget;
            try {
              previousInstance(event);
            } catch (error) {
              reportGlobalError(error);
            }
            event.currentTarget = null;
            previousInstance = instance;
          }
        else
          for (i$jscomp$0 = 0; i$jscomp$0 < _dispatchQueue$i.length; i$jscomp$0++) {
            _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0];
            instance = _dispatchListeners$i.instance;
            currentTarget = _dispatchListeners$i.currentTarget;
            _dispatchListeners$i = _dispatchListeners$i.listener;
            if (instance !== previousInstance && event.isPropagationStopped())
              break a;
            previousInstance = _dispatchListeners$i;
            event.currentTarget = currentTarget;
            try {
              previousInstance(event);
            } catch (error) {
              reportGlobalError(error);
            }
            event.currentTarget = null;
            previousInstance = instance;
          }
      }
    }
  }
  function listenToNonDelegatedEvent(domEventName, targetElement) {
    var JSCompiler_inline_result = targetElement[internalEventHandlersKey];
    void 0 === JSCompiler_inline_result && (JSCompiler_inline_result = targetElement[internalEventHandlersKey] = /* @__PURE__ */ new Set());
    var listenerSetKey = domEventName + "__bubble";
    JSCompiler_inline_result.has(listenerSetKey) || (addTrappedEventListener(targetElement, domEventName, 2, false), JSCompiler_inline_result.add(listenerSetKey));
  }
  function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
    var eventSystemFlags = 0;
    isCapturePhaseListener && (eventSystemFlags |= 4);
    addTrappedEventListener(
      target,
      domEventName,
      eventSystemFlags,
      isCapturePhaseListener
    );
  }
  var listeningMarker = "_reactListening" + Math.random().toString(36).slice(2);
  function listenToAllSupportedEvents(rootContainerElement) {
    if (!rootContainerElement[listeningMarker]) {
      rootContainerElement[listeningMarker] = true;
      allNativeEvents.forEach(function(domEventName) {
        "selectionchange" !== domEventName && (nonDelegatedEvents.has(domEventName) || listenToNativeEvent(domEventName, false, rootContainerElement), listenToNativeEvent(domEventName, true, rootContainerElement));
      });
      var ownerDocument = 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
      null === ownerDocument || ownerDocument[listeningMarker] || (ownerDocument[listeningMarker] = true, listenToNativeEvent("selectionchange", false, ownerDocument));
    }
  }
  function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener) {
    switch (getEventPriority(domEventName)) {
      case 2:
        var listenerWrapper = dispatchDiscreteEvent;
        break;
      case 8:
        listenerWrapper = dispatchContinuousEvent;
        break;
      default:
        listenerWrapper = dispatchEvent;
    }
    eventSystemFlags = listenerWrapper.bind(
      null,
      domEventName,
      eventSystemFlags,
      targetContainer
    );
    listenerWrapper = void 0;
    !passiveBrowserEventsSupported || "touchstart" !== domEventName && "touchmove" !== domEventName && "wheel" !== domEventName || (listenerWrapper = true);
    isCapturePhaseListener ? void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
      capture: true,
      passive: listenerWrapper
    }) : targetContainer.addEventListener(domEventName, eventSystemFlags, true) : void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
      passive: listenerWrapper
    }) : targetContainer.addEventListener(domEventName, eventSystemFlags, false);
  }
  function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst$jscomp$0, targetContainer) {
    var ancestorInst = targetInst$jscomp$0;
    if (0 === (eventSystemFlags & 1) && 0 === (eventSystemFlags & 2) && null !== targetInst$jscomp$0)
      a: for (; ; ) {
        if (null === targetInst$jscomp$0) return;
        var nodeTag = targetInst$jscomp$0.tag;
        if (3 === nodeTag || 4 === nodeTag) {
          var container = targetInst$jscomp$0.stateNode.containerInfo;
          if (container === targetContainer) break;
          if (4 === nodeTag)
            for (nodeTag = targetInst$jscomp$0.return; null !== nodeTag; ) {
              var grandTag = nodeTag.tag;
              if ((3 === grandTag || 4 === grandTag) && nodeTag.stateNode.containerInfo === targetContainer)
                return;
              nodeTag = nodeTag.return;
            }
          for (; null !== container; ) {
            nodeTag = getClosestInstanceFromNode(container);
            if (null === nodeTag) return;
            grandTag = nodeTag.tag;
            if (5 === grandTag || 6 === grandTag || 26 === grandTag || 27 === grandTag) {
              targetInst$jscomp$0 = ancestorInst = nodeTag;
              continue a;
            }
            container = container.parentNode;
          }
        }
        targetInst$jscomp$0 = targetInst$jscomp$0.return;
      }
    batchedUpdates$1(function() {
      var targetInst = ancestorInst, nativeEventTarget = getEventTarget(nativeEvent), dispatchQueue = [];
      a: {
        var reactName = topLevelEventsToReactNames.get(domEventName);
        if (void 0 !== reactName) {
          var SyntheticEventCtor = SyntheticEvent, reactEventType = domEventName;
          switch (domEventName) {
            case "keypress":
              if (0 === getEventCharCode(nativeEvent)) break a;
            case "keydown":
            case "keyup":
              SyntheticEventCtor = SyntheticKeyboardEvent;
              break;
            case "focusin":
              reactEventType = "focus";
              SyntheticEventCtor = SyntheticFocusEvent;
              break;
            case "focusout":
              reactEventType = "blur";
              SyntheticEventCtor = SyntheticFocusEvent;
              break;
            case "beforeblur":
            case "afterblur":
              SyntheticEventCtor = SyntheticFocusEvent;
              break;
            case "click":
              if (2 === nativeEvent.button) break a;
            case "auxclick":
            case "dblclick":
            case "mousedown":
            case "mousemove":
            case "mouseup":
            case "mouseout":
            case "mouseover":
            case "contextmenu":
              SyntheticEventCtor = SyntheticMouseEvent;
              break;
            case "drag":
            case "dragend":
            case "dragenter":
            case "dragexit":
            case "dragleave":
            case "dragover":
            case "dragstart":
            case "drop":
              SyntheticEventCtor = SyntheticDragEvent;
              break;
            case "touchcancel":
            case "touchend":
            case "touchmove":
            case "touchstart":
              SyntheticEventCtor = SyntheticTouchEvent;
              break;
            case ANIMATION_END:
            case ANIMATION_ITERATION:
            case ANIMATION_START:
              SyntheticEventCtor = SyntheticAnimationEvent;
              break;
            case TRANSITION_END:
              SyntheticEventCtor = SyntheticTransitionEvent;
              break;
            case "scroll":
            case "scrollend":
              SyntheticEventCtor = SyntheticUIEvent;
              break;
            case "wheel":
              SyntheticEventCtor = SyntheticWheelEvent;
              break;
            case "copy":
            case "cut":
            case "paste":
              SyntheticEventCtor = SyntheticClipboardEvent;
              break;
            case "gotpointercapture":
            case "lostpointercapture":
            case "pointercancel":
            case "pointerdown":
            case "pointermove":
            case "pointerout":
            case "pointerover":
            case "pointerup":
              SyntheticEventCtor = SyntheticPointerEvent;
              break;
            case "toggle":
            case "beforetoggle":
              SyntheticEventCtor = SyntheticToggleEvent;
          }
          var inCapturePhase = 0 !== (eventSystemFlags & 4), accumulateTargetOnly = !inCapturePhase && ("scroll" === domEventName || "scrollend" === domEventName), reactEventName = inCapturePhase ? null !== reactName ? reactName + "Capture" : null : reactName;
          inCapturePhase = [];
          for (var instance = targetInst, lastHostComponent; null !== instance; ) {
            var _instance = instance;
            lastHostComponent = _instance.stateNode;
            _instance = _instance.tag;
            5 !== _instance && 26 !== _instance && 27 !== _instance || null === lastHostComponent || null === reactEventName || (_instance = getListener(instance, reactEventName), null != _instance && inCapturePhase.push(
              createDispatchListener(instance, _instance, lastHostComponent)
            ));
            if (accumulateTargetOnly) break;
            instance = instance.return;
          }
          0 < inCapturePhase.length && (reactName = new SyntheticEventCtor(
            reactName,
            reactEventType,
            null,
            nativeEvent,
            nativeEventTarget
          ), dispatchQueue.push({ event: reactName, listeners: inCapturePhase }));
        }
      }
      if (0 === (eventSystemFlags & 7)) {
        a: {
          reactName = "mouseover" === domEventName || "pointerover" === domEventName;
          SyntheticEventCtor = "mouseout" === domEventName || "pointerout" === domEventName;
          if (reactName && nativeEvent !== currentReplayingEvent && (reactEventType = nativeEvent.relatedTarget || nativeEvent.fromElement) && (getClosestInstanceFromNode(reactEventType) || reactEventType[internalContainerInstanceKey]))
            break a;
          if (SyntheticEventCtor || reactName) {
            reactName = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget : (reactName = nativeEventTarget.ownerDocument) ? reactName.defaultView || reactName.parentWindow : window;
            if (SyntheticEventCtor) {
              if (reactEventType = nativeEvent.relatedTarget || nativeEvent.toElement, SyntheticEventCtor = targetInst, reactEventType = reactEventType ? getClosestInstanceFromNode(reactEventType) : null, null !== reactEventType && (accumulateTargetOnly = getNearestMountedFiber(reactEventType), inCapturePhase = reactEventType.tag, reactEventType !== accumulateTargetOnly || 5 !== inCapturePhase && 27 !== inCapturePhase && 6 !== inCapturePhase))
                reactEventType = null;
            } else SyntheticEventCtor = null, reactEventType = targetInst;
            if (SyntheticEventCtor !== reactEventType) {
              inCapturePhase = SyntheticMouseEvent;
              _instance = "onMouseLeave";
              reactEventName = "onMouseEnter";
              instance = "mouse";
              if ("pointerout" === domEventName || "pointerover" === domEventName)
                inCapturePhase = SyntheticPointerEvent, _instance = "onPointerLeave", reactEventName = "onPointerEnter", instance = "pointer";
              accumulateTargetOnly = null == SyntheticEventCtor ? reactName : getNodeFromInstance(SyntheticEventCtor);
              lastHostComponent = null == reactEventType ? reactName : getNodeFromInstance(reactEventType);
              reactName = new inCapturePhase(
                _instance,
                instance + "leave",
                SyntheticEventCtor,
                nativeEvent,
                nativeEventTarget
              );
              reactName.target = accumulateTargetOnly;
              reactName.relatedTarget = lastHostComponent;
              _instance = null;
              getClosestInstanceFromNode(nativeEventTarget) === targetInst && (inCapturePhase = new inCapturePhase(
                reactEventName,
                instance + "enter",
                reactEventType,
                nativeEvent,
                nativeEventTarget
              ), inCapturePhase.target = lastHostComponent, inCapturePhase.relatedTarget = accumulateTargetOnly, _instance = inCapturePhase);
              accumulateTargetOnly = _instance;
              if (SyntheticEventCtor && reactEventType)
                b: {
                  inCapturePhase = getParent;
                  reactEventName = SyntheticEventCtor;
                  instance = reactEventType;
                  lastHostComponent = 0;
                  for (_instance = reactEventName; _instance; _instance = inCapturePhase(_instance))
                    lastHostComponent++;
                  _instance = 0;
                  for (var tempB = instance; tempB; tempB = inCapturePhase(tempB))
                    _instance++;
                  for (; 0 < lastHostComponent - _instance; )
                    reactEventName = inCapturePhase(reactEventName), lastHostComponent--;
                  for (; 0 < _instance - lastHostComponent; )
                    instance = inCapturePhase(instance), _instance--;
                  for (; lastHostComponent--; ) {
                    if (reactEventName === instance || null !== instance && reactEventName === instance.alternate) {
                      inCapturePhase = reactEventName;
                      break b;
                    }
                    reactEventName = inCapturePhase(reactEventName);
                    instance = inCapturePhase(instance);
                  }
                  inCapturePhase = null;
                }
              else inCapturePhase = null;
              null !== SyntheticEventCtor && accumulateEnterLeaveListenersForEvent(
                dispatchQueue,
                reactName,
                SyntheticEventCtor,
                inCapturePhase,
                false
              );
              null !== reactEventType && null !== accumulateTargetOnly && accumulateEnterLeaveListenersForEvent(
                dispatchQueue,
                accumulateTargetOnly,
                reactEventType,
                inCapturePhase,
                true
              );
            }
          }
        }
        a: {
          reactName = targetInst ? getNodeFromInstance(targetInst) : window;
          SyntheticEventCtor = reactName.nodeName && reactName.nodeName.toLowerCase();
          if ("select" === SyntheticEventCtor || "input" === SyntheticEventCtor && "file" === reactName.type)
            var getTargetInstFunc = getTargetInstForChangeEvent;
          else if (isTextInputElement(reactName))
            if (isInputEventSupported)
              getTargetInstFunc = getTargetInstForInputOrChangeEvent;
            else {
              getTargetInstFunc = getTargetInstForInputEventPolyfill;
              var handleEventFunc = handleEventsForInputEventPolyfill;
            }
          else
            SyntheticEventCtor = reactName.nodeName, !SyntheticEventCtor || "input" !== SyntheticEventCtor.toLowerCase() || "checkbox" !== reactName.type && "radio" !== reactName.type ? targetInst && isCustomElement(targetInst.elementType) && (getTargetInstFunc = getTargetInstForChangeEvent) : getTargetInstFunc = getTargetInstForClickEvent;
          if (getTargetInstFunc && (getTargetInstFunc = getTargetInstFunc(domEventName, targetInst))) {
            createAndAccumulateChangeEvent(
              dispatchQueue,
              getTargetInstFunc,
              nativeEvent,
              nativeEventTarget
            );
            break a;
          }
          handleEventFunc && handleEventFunc(domEventName, reactName, targetInst);
          "focusout" === domEventName && targetInst && "number" === reactName.type && null != targetInst.memoizedProps.value && setDefaultValue(reactName, "number", reactName.value);
        }
        handleEventFunc = targetInst ? getNodeFromInstance(targetInst) : window;
        switch (domEventName) {
          case "focusin":
            if (isTextInputElement(handleEventFunc) || "true" === handleEventFunc.contentEditable)
              activeElement = handleEventFunc, activeElementInst = targetInst, lastSelection = null;
            break;
          case "focusout":
            lastSelection = activeElementInst = activeElement = null;
            break;
          case "mousedown":
            mouseDown = true;
            break;
          case "contextmenu":
          case "mouseup":
          case "dragend":
            mouseDown = false;
            constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
            break;
          case "selectionchange":
            if (skipSelectionChangeEvent) break;
          case "keydown":
          case "keyup":
            constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
        }
        var fallbackData;
        if (canUseCompositionEvent)
          b: {
            switch (domEventName) {
              case "compositionstart":
                var eventType = "onCompositionStart";
                break b;
              case "compositionend":
                eventType = "onCompositionEnd";
                break b;
              case "compositionupdate":
                eventType = "onCompositionUpdate";
                break b;
            }
            eventType = void 0;
          }
        else
          isComposing ? isFallbackCompositionEnd(domEventName, nativeEvent) && (eventType = "onCompositionEnd") : "keydown" === domEventName && 229 === nativeEvent.keyCode && (eventType = "onCompositionStart");
        eventType && (useFallbackCompositionData && "ko" !== nativeEvent.locale && (isComposing || "onCompositionStart" !== eventType ? "onCompositionEnd" === eventType && isComposing && (fallbackData = getData()) : (root2 = nativeEventTarget, startText = "value" in root2 ? root2.value : root2.textContent, isComposing = true)), handleEventFunc = accumulateTwoPhaseListeners(targetInst, eventType), 0 < handleEventFunc.length && (eventType = new SyntheticCompositionEvent(
          eventType,
          domEventName,
          null,
          nativeEvent,
          nativeEventTarget
        ), dispatchQueue.push({ event: eventType, listeners: handleEventFunc }), fallbackData ? eventType.data = fallbackData : (fallbackData = getDataFromCustomEvent(nativeEvent), null !== fallbackData && (eventType.data = fallbackData))));
        if (fallbackData = canUseTextInputEvent ? getNativeBeforeInputChars(domEventName, nativeEvent) : getFallbackBeforeInputChars(domEventName, nativeEvent))
          eventType = accumulateTwoPhaseListeners(targetInst, "onBeforeInput"), 0 < eventType.length && (handleEventFunc = new SyntheticCompositionEvent(
            "onBeforeInput",
            "beforeinput",
            null,
            nativeEvent,
            nativeEventTarget
          ), dispatchQueue.push({
            event: handleEventFunc,
            listeners: eventType
          }), handleEventFunc.data = fallbackData);
        extractEvents$1(
          dispatchQueue,
          domEventName,
          targetInst,
          nativeEvent,
          nativeEventTarget
        );
      }
      processDispatchQueue(dispatchQueue, eventSystemFlags);
    });
  }
  function createDispatchListener(instance, listener, currentTarget) {
    return {
      instance,
      listener,
      currentTarget
    };
  }
  function accumulateTwoPhaseListeners(targetFiber, reactName) {
    for (var captureName = reactName + "Capture", listeners = []; null !== targetFiber; ) {
      var _instance2 = targetFiber, stateNode = _instance2.stateNode;
      _instance2 = _instance2.tag;
      5 !== _instance2 && 26 !== _instance2 && 27 !== _instance2 || null === stateNode || (_instance2 = getListener(targetFiber, captureName), null != _instance2 && listeners.unshift(
        createDispatchListener(targetFiber, _instance2, stateNode)
      ), _instance2 = getListener(targetFiber, reactName), null != _instance2 && listeners.push(
        createDispatchListener(targetFiber, _instance2, stateNode)
      ));
      if (3 === targetFiber.tag) return listeners;
      targetFiber = targetFiber.return;
    }
    return [];
  }
  function getParent(inst) {
    if (null === inst) return null;
    do
      inst = inst.return;
    while (inst && 5 !== inst.tag && 27 !== inst.tag);
    return inst ? inst : null;
  }
  function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
    for (var registrationName = event._reactName, listeners = []; null !== target && target !== common; ) {
      var _instance3 = target, alternate = _instance3.alternate, stateNode = _instance3.stateNode;
      _instance3 = _instance3.tag;
      if (null !== alternate && alternate === common) break;
      5 !== _instance3 && 26 !== _instance3 && 27 !== _instance3 || null === stateNode || (alternate = stateNode, inCapturePhase ? (stateNode = getListener(target, registrationName), null != stateNode && listeners.unshift(
        createDispatchListener(target, stateNode, alternate)
      )) : inCapturePhase || (stateNode = getListener(target, registrationName), null != stateNode && listeners.push(
        createDispatchListener(target, stateNode, alternate)
      )));
      target = target.return;
    }
    0 !== listeners.length && dispatchQueue.push({ event, listeners });
  }
  var NORMALIZE_NEWLINES_REGEX = /\r\n?/g, NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;
  function normalizeMarkupForTextOrAttribute(markup) {
    return ("string" === typeof markup ? markup : "" + markup).replace(NORMALIZE_NEWLINES_REGEX, "\n").replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, "");
  }
  function checkForUnmatchedText(serverText, clientText) {
    clientText = normalizeMarkupForTextOrAttribute(clientText);
    return normalizeMarkupForTextOrAttribute(serverText) === clientText ? true : false;
  }
  function setProp(domElement, tag, key, value, props, prevValue) {
    switch (key) {
      case "children":
        "string" === typeof value ? "body" === tag || "textarea" === tag && "" === value || setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && "body" !== tag && setTextContent(domElement, "" + value);
        break;
      case "className":
        setValueForKnownAttribute(domElement, "class", value);
        break;
      case "tabIndex":
        setValueForKnownAttribute(domElement, "tabindex", value);
        break;
      case "dir":
      case "role":
      case "viewBox":
      case "width":
      case "height":
        setValueForKnownAttribute(domElement, key, value);
        break;
      case "style":
        setValueForStyles(domElement, value, prevValue);
        break;
      case "data":
        if ("object" !== tag) {
          setValueForKnownAttribute(domElement, "data", value);
          break;
        }
      case "src":
      case "href":
        if ("" === value && ("a" !== tag || "href" !== key)) {
          domElement.removeAttribute(key);
          break;
        }
        if (null == value || "function" === typeof value || "symbol" === typeof value || "boolean" === typeof value) {
          domElement.removeAttribute(key);
          break;
        }
        value = sanitizeURL("" + value);
        domElement.setAttribute(key, value);
        break;
      case "action":
      case "formAction":
        if ("function" === typeof value) {
          domElement.setAttribute(
            key,
            "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')"
          );
          break;
        } else
          "function" === typeof prevValue && ("formAction" === key ? ("input" !== tag && setProp(domElement, tag, "name", props.name, props, null), setProp(
            domElement,
            tag,
            "formEncType",
            props.formEncType,
            props,
            null
          ), setProp(
            domElement,
            tag,
            "formMethod",
            props.formMethod,
            props,
            null
          ), setProp(
            domElement,
            tag,
            "formTarget",
            props.formTarget,
            props,
            null
          )) : (setProp(domElement, tag, "encType", props.encType, props, null), setProp(domElement, tag, "method", props.method, props, null), setProp(domElement, tag, "target", props.target, props, null)));
        if (null == value || "symbol" === typeof value || "boolean" === typeof value) {
          domElement.removeAttribute(key);
          break;
        }
        value = sanitizeURL("" + value);
        domElement.setAttribute(key, value);
        break;
      case "onClick":
        null != value && (domElement.onclick = noop$1);
        break;
      case "onScroll":
        null != value && listenToNonDelegatedEvent("scroll", domElement);
        break;
      case "onScrollEnd":
        null != value && listenToNonDelegatedEvent("scrollend", domElement);
        break;
      case "dangerouslySetInnerHTML":
        if (null != value) {
          if ("object" !== typeof value || !("__html" in value))
            throw Error(formatProdErrorMessage(61));
          key = value.__html;
          if (null != key) {
            if (null != props.children) throw Error(formatProdErrorMessage(60));
            domElement.innerHTML = key;
          }
        }
        break;
      case "multiple":
        domElement.multiple = value && "function" !== typeof value && "symbol" !== typeof value;
        break;
      case "muted":
        domElement.muted = value && "function" !== typeof value && "symbol" !== typeof value;
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "defaultValue":
      case "defaultChecked":
      case "innerHTML":
      case "ref":
        break;
      case "autoFocus":
        break;
      case "xlinkHref":
        if (null == value || "function" === typeof value || "boolean" === typeof value || "symbol" === typeof value) {
          domElement.removeAttribute("xlink:href");
          break;
        }
        key = sanitizeURL("" + value);
        domElement.setAttributeNS(
          "http://www.w3.org/1999/xlink",
          "xlink:href",
          key
        );
        break;
      case "contentEditable":
      case "spellCheck":
      case "draggable":
      case "value":
      case "autoReverse":
      case "externalResourcesRequired":
      case "focusable":
      case "preserveAlpha":
        null != value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "" + value) : domElement.removeAttribute(key);
        break;
      case "inert":
      case "allowFullScreen":
      case "async":
      case "autoPlay":
      case "controls":
      case "default":
      case "defer":
      case "disabled":
      case "disablePictureInPicture":
      case "disableRemotePlayback":
      case "formNoValidate":
      case "hidden":
      case "loop":
      case "noModule":
      case "noValidate":
      case "open":
      case "playsInline":
      case "readOnly":
      case "required":
      case "reversed":
      case "scoped":
      case "seamless":
      case "itemScope":
        value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "") : domElement.removeAttribute(key);
        break;
      case "capture":
      case "download":
        true === value ? domElement.setAttribute(key, "") : false !== value && null != value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, value) : domElement.removeAttribute(key);
        break;
      case "cols":
      case "rows":
      case "size":
      case "span":
        null != value && "function" !== typeof value && "symbol" !== typeof value && !isNaN(value) && 1 <= value ? domElement.setAttribute(key, value) : domElement.removeAttribute(key);
        break;
      case "rowSpan":
      case "start":
        null == value || "function" === typeof value || "symbol" === typeof value || isNaN(value) ? domElement.removeAttribute(key) : domElement.setAttribute(key, value);
        break;
      case "popover":
        listenToNonDelegatedEvent("beforetoggle", domElement);
        listenToNonDelegatedEvent("toggle", domElement);
        setValueForAttribute(domElement, "popover", value);
        break;
      case "xlinkActuate":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:actuate",
          value
        );
        break;
      case "xlinkArcrole":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:arcrole",
          value
        );
        break;
      case "xlinkRole":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:role",
          value
        );
        break;
      case "xlinkShow":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:show",
          value
        );
        break;
      case "xlinkTitle":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:title",
          value
        );
        break;
      case "xlinkType":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/1999/xlink",
          "xlink:type",
          value
        );
        break;
      case "xmlBase":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/XML/1998/namespace",
          "xml:base",
          value
        );
        break;
      case "xmlLang":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/XML/1998/namespace",
          "xml:lang",
          value
        );
        break;
      case "xmlSpace":
        setValueForNamespacedAttribute(
          domElement,
          "http://www.w3.org/XML/1998/namespace",
          "xml:space",
          value
        );
        break;
      case "is":
        setValueForAttribute(domElement, "is", value);
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        if (!(2 < key.length) || "o" !== key[0] && "O" !== key[0] || "n" !== key[1] && "N" !== key[1])
          key = aliases.get(key) || key, setValueForAttribute(domElement, key, value);
    }
  }
  function setPropOnCustomElement(domElement, tag, key, value, props, prevValue) {
    switch (key) {
      case "style":
        setValueForStyles(domElement, value, prevValue);
        break;
      case "dangerouslySetInnerHTML":
        if (null != value) {
          if ("object" !== typeof value || !("__html" in value))
            throw Error(formatProdErrorMessage(61));
          key = value.__html;
          if (null != key) {
            if (null != props.children) throw Error(formatProdErrorMessage(60));
            domElement.innerHTML = key;
          }
        }
        break;
      case "children":
        "string" === typeof value ? setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && setTextContent(domElement, "" + value);
        break;
      case "onScroll":
        null != value && listenToNonDelegatedEvent("scroll", domElement);
        break;
      case "onScrollEnd":
        null != value && listenToNonDelegatedEvent("scrollend", domElement);
        break;
      case "onClick":
        null != value && (domElement.onclick = noop$1);
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "innerHTML":
      case "ref":
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        if (!registrationNameDependencies.hasOwnProperty(key))
          a: {
            if ("o" === key[0] && "n" === key[1] && (props = key.endsWith("Capture"), tag = key.slice(2, props ? key.length - 7 : void 0), prevValue = domElement[internalPropsKey] || null, prevValue = null != prevValue ? prevValue[key] : null, "function" === typeof prevValue && domElement.removeEventListener(tag, prevValue, props), "function" === typeof value)) {
              "function" !== typeof prevValue && null !== prevValue && (key in domElement ? domElement[key] = null : domElement.hasAttribute(key) && domElement.removeAttribute(key));
              domElement.addEventListener(tag, value, props);
              break a;
            }
            key in domElement ? domElement[key] = value : true === value ? domElement.setAttribute(key, "") : setValueForAttribute(domElement, key, value);
          }
    }
  }
  function setInitialProperties(domElement, tag, props) {
    switch (tag) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "img":
        listenToNonDelegatedEvent("error", domElement);
        listenToNonDelegatedEvent("load", domElement);
        var hasSrc = false, hasSrcSet = false, propKey;
        for (propKey in props)
          if (props.hasOwnProperty(propKey)) {
            var propValue = props[propKey];
            if (null != propValue)
              switch (propKey) {
                case "src":
                  hasSrc = true;
                  break;
                case "srcSet":
                  hasSrcSet = true;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  throw Error(formatProdErrorMessage(137, tag));
                default:
                  setProp(domElement, tag, propKey, propValue, props, null);
              }
          }
        hasSrcSet && setProp(domElement, tag, "srcSet", props.srcSet, props, null);
        hasSrc && setProp(domElement, tag, "src", props.src, props, null);
        return;
      case "input":
        listenToNonDelegatedEvent("invalid", domElement);
        var defaultValue = propKey = propValue = hasSrcSet = null, checked = null, defaultChecked = null;
        for (hasSrc in props)
          if (props.hasOwnProperty(hasSrc)) {
            var propValue$184 = props[hasSrc];
            if (null != propValue$184)
              switch (hasSrc) {
                case "name":
                  hasSrcSet = propValue$184;
                  break;
                case "type":
                  propValue = propValue$184;
                  break;
                case "checked":
                  checked = propValue$184;
                  break;
                case "defaultChecked":
                  defaultChecked = propValue$184;
                  break;
                case "value":
                  propKey = propValue$184;
                  break;
                case "defaultValue":
                  defaultValue = propValue$184;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  if (null != propValue$184)
                    throw Error(formatProdErrorMessage(137, tag));
                  break;
                default:
                  setProp(domElement, tag, hasSrc, propValue$184, props, null);
              }
          }
        initInput(
          domElement,
          propKey,
          defaultValue,
          checked,
          defaultChecked,
          propValue,
          hasSrcSet,
          false
        );
        return;
      case "select":
        listenToNonDelegatedEvent("invalid", domElement);
        hasSrc = propValue = propKey = null;
        for (hasSrcSet in props)
          if (props.hasOwnProperty(hasSrcSet) && (defaultValue = props[hasSrcSet], null != defaultValue))
            switch (hasSrcSet) {
              case "value":
                propKey = defaultValue;
                break;
              case "defaultValue":
                propValue = defaultValue;
                break;
              case "multiple":
                hasSrc = defaultValue;
              default:
                setProp(domElement, tag, hasSrcSet, defaultValue, props, null);
            }
        tag = propKey;
        props = propValue;
        domElement.multiple = !!hasSrc;
        null != tag ? updateOptions(domElement, !!hasSrc, tag, false) : null != props && updateOptions(domElement, !!hasSrc, props, true);
        return;
      case "textarea":
        listenToNonDelegatedEvent("invalid", domElement);
        propKey = hasSrcSet = hasSrc = null;
        for (propValue in props)
          if (props.hasOwnProperty(propValue) && (defaultValue = props[propValue], null != defaultValue))
            switch (propValue) {
              case "value":
                hasSrc = defaultValue;
                break;
              case "defaultValue":
                hasSrcSet = defaultValue;
                break;
              case "children":
                propKey = defaultValue;
                break;
              case "dangerouslySetInnerHTML":
                if (null != defaultValue) throw Error(formatProdErrorMessage(91));
                break;
              default:
                setProp(domElement, tag, propValue, defaultValue, props, null);
            }
        initTextarea(domElement, hasSrc, hasSrcSet, propKey);
        return;
      case "option":
        for (checked in props)
          if (props.hasOwnProperty(checked) && (hasSrc = props[checked], null != hasSrc))
            switch (checked) {
              case "selected":
                domElement.selected = hasSrc && "function" !== typeof hasSrc && "symbol" !== typeof hasSrc;
                break;
              default:
                setProp(domElement, tag, checked, hasSrc, props, null);
            }
        return;
      case "dialog":
        listenToNonDelegatedEvent("beforetoggle", domElement);
        listenToNonDelegatedEvent("toggle", domElement);
        listenToNonDelegatedEvent("cancel", domElement);
        listenToNonDelegatedEvent("close", domElement);
        break;
      case "iframe":
      case "object":
        listenToNonDelegatedEvent("load", domElement);
        break;
      case "video":
      case "audio":
        for (hasSrc = 0; hasSrc < mediaEventTypes.length; hasSrc++)
          listenToNonDelegatedEvent(mediaEventTypes[hasSrc], domElement);
        break;
      case "image":
        listenToNonDelegatedEvent("error", domElement);
        listenToNonDelegatedEvent("load", domElement);
        break;
      case "details":
        listenToNonDelegatedEvent("toggle", domElement);
        break;
      case "embed":
      case "source":
      case "link":
        listenToNonDelegatedEvent("error", domElement), listenToNonDelegatedEvent("load", domElement);
      case "area":
      case "base":
      case "br":
      case "col":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "track":
      case "wbr":
      case "menuitem":
        for (defaultChecked in props)
          if (props.hasOwnProperty(defaultChecked) && (hasSrc = props[defaultChecked], null != hasSrc))
            switch (defaultChecked) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(formatProdErrorMessage(137, tag));
              default:
                setProp(domElement, tag, defaultChecked, hasSrc, props, null);
            }
        return;
      default:
        if (isCustomElement(tag)) {
          for (propValue$184 in props)
            props.hasOwnProperty(propValue$184) && (hasSrc = props[propValue$184], void 0 !== hasSrc && setPropOnCustomElement(
              domElement,
              tag,
              propValue$184,
              hasSrc,
              props,
              void 0
            ));
          return;
        }
    }
    for (defaultValue in props)
      props.hasOwnProperty(defaultValue) && (hasSrc = props[defaultValue], null != hasSrc && setProp(domElement, tag, defaultValue, hasSrc, props, null));
  }
  function updateProperties(domElement, tag, lastProps, nextProps) {
    switch (tag) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "input":
        var name = null, type = null, value = null, defaultValue = null, lastDefaultValue = null, checked = null, defaultChecked = null;
        for (propKey in lastProps) {
          var lastProp = lastProps[propKey];
          if (lastProps.hasOwnProperty(propKey) && null != lastProp)
            switch (propKey) {
              case "checked":
                break;
              case "value":
                break;
              case "defaultValue":
                lastDefaultValue = lastProp;
              default:
                nextProps.hasOwnProperty(propKey) || setProp(domElement, tag, propKey, null, nextProps, lastProp);
            }
        }
        for (var propKey$201 in nextProps) {
          var propKey = nextProps[propKey$201];
          lastProp = lastProps[propKey$201];
          if (nextProps.hasOwnProperty(propKey$201) && (null != propKey || null != lastProp))
            switch (propKey$201) {
              case "type":
                type = propKey;
                break;
              case "name":
                name = propKey;
                break;
              case "checked":
                checked = propKey;
                break;
              case "defaultChecked":
                defaultChecked = propKey;
                break;
              case "value":
                value = propKey;
                break;
              case "defaultValue":
                defaultValue = propKey;
                break;
              case "children":
              case "dangerouslySetInnerHTML":
                if (null != propKey)
                  throw Error(formatProdErrorMessage(137, tag));
                break;
              default:
                propKey !== lastProp && setProp(
                  domElement,
                  tag,
                  propKey$201,
                  propKey,
                  nextProps,
                  lastProp
                );
            }
        }
        updateInput(
          domElement,
          value,
          defaultValue,
          lastDefaultValue,
          checked,
          defaultChecked,
          type,
          name
        );
        return;
      case "select":
        propKey = value = defaultValue = propKey$201 = null;
        for (type in lastProps)
          if (lastDefaultValue = lastProps[type], lastProps.hasOwnProperty(type) && null != lastDefaultValue)
            switch (type) {
              case "value":
                break;
              case "multiple":
                propKey = lastDefaultValue;
              default:
                nextProps.hasOwnProperty(type) || setProp(
                  domElement,
                  tag,
                  type,
                  null,
                  nextProps,
                  lastDefaultValue
                );
            }
        for (name in nextProps)
          if (type = nextProps[name], lastDefaultValue = lastProps[name], nextProps.hasOwnProperty(name) && (null != type || null != lastDefaultValue))
            switch (name) {
              case "value":
                propKey$201 = type;
                break;
              case "defaultValue":
                defaultValue = type;
                break;
              case "multiple":
                value = type;
              default:
                type !== lastDefaultValue && setProp(
                  domElement,
                  tag,
                  name,
                  type,
                  nextProps,
                  lastDefaultValue
                );
            }
        tag = defaultValue;
        lastProps = value;
        nextProps = propKey;
        null != propKey$201 ? updateOptions(domElement, !!lastProps, propKey$201, false) : !!nextProps !== !!lastProps && (null != tag ? updateOptions(domElement, !!lastProps, tag, true) : updateOptions(domElement, !!lastProps, lastProps ? [] : "", false));
        return;
      case "textarea":
        propKey = propKey$201 = null;
        for (defaultValue in lastProps)
          if (name = lastProps[defaultValue], lastProps.hasOwnProperty(defaultValue) && null != name && !nextProps.hasOwnProperty(defaultValue))
            switch (defaultValue) {
              case "value":
                break;
              case "children":
                break;
              default:
                setProp(domElement, tag, defaultValue, null, nextProps, name);
            }
        for (value in nextProps)
          if (name = nextProps[value], type = lastProps[value], nextProps.hasOwnProperty(value) && (null != name || null != type))
            switch (value) {
              case "value":
                propKey$201 = name;
                break;
              case "defaultValue":
                propKey = name;
                break;
              case "children":
                break;
              case "dangerouslySetInnerHTML":
                if (null != name) throw Error(formatProdErrorMessage(91));
                break;
              default:
                name !== type && setProp(domElement, tag, value, name, nextProps, type);
            }
        updateTextarea(domElement, propKey$201, propKey);
        return;
      case "option":
        for (var propKey$217 in lastProps)
          if (propKey$201 = lastProps[propKey$217], lastProps.hasOwnProperty(propKey$217) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$217))
            switch (propKey$217) {
              case "selected":
                domElement.selected = false;
                break;
              default:
                setProp(
                  domElement,
                  tag,
                  propKey$217,
                  null,
                  nextProps,
                  propKey$201
                );
            }
        for (lastDefaultValue in nextProps)
          if (propKey$201 = nextProps[lastDefaultValue], propKey = lastProps[lastDefaultValue], nextProps.hasOwnProperty(lastDefaultValue) && propKey$201 !== propKey && (null != propKey$201 || null != propKey))
            switch (lastDefaultValue) {
              case "selected":
                domElement.selected = propKey$201 && "function" !== typeof propKey$201 && "symbol" !== typeof propKey$201;
                break;
              default:
                setProp(
                  domElement,
                  tag,
                  lastDefaultValue,
                  propKey$201,
                  nextProps,
                  propKey
                );
            }
        return;
      case "img":
      case "link":
      case "area":
      case "base":
      case "br":
      case "col":
      case "embed":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "source":
      case "track":
      case "wbr":
      case "menuitem":
        for (var propKey$222 in lastProps)
          propKey$201 = lastProps[propKey$222], lastProps.hasOwnProperty(propKey$222) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$222) && setProp(domElement, tag, propKey$222, null, nextProps, propKey$201);
        for (checked in nextProps)
          if (propKey$201 = nextProps[checked], propKey = lastProps[checked], nextProps.hasOwnProperty(checked) && propKey$201 !== propKey && (null != propKey$201 || null != propKey))
            switch (checked) {
              case "children":
              case "dangerouslySetInnerHTML":
                if (null != propKey$201)
                  throw Error(formatProdErrorMessage(137, tag));
                break;
              default:
                setProp(
                  domElement,
                  tag,
                  checked,
                  propKey$201,
                  nextProps,
                  propKey
                );
            }
        return;
      default:
        if (isCustomElement(tag)) {
          for (var propKey$227 in lastProps)
            propKey$201 = lastProps[propKey$227], lastProps.hasOwnProperty(propKey$227) && void 0 !== propKey$201 && !nextProps.hasOwnProperty(propKey$227) && setPropOnCustomElement(
              domElement,
              tag,
              propKey$227,
              void 0,
              nextProps,
              propKey$201
            );
          for (defaultChecked in nextProps)
            propKey$201 = nextProps[defaultChecked], propKey = lastProps[defaultChecked], !nextProps.hasOwnProperty(defaultChecked) || propKey$201 === propKey || void 0 === propKey$201 && void 0 === propKey || setPropOnCustomElement(
              domElement,
              tag,
              defaultChecked,
              propKey$201,
              nextProps,
              propKey
            );
          return;
        }
    }
    for (var propKey$232 in lastProps)
      propKey$201 = lastProps[propKey$232], lastProps.hasOwnProperty(propKey$232) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$232) && setProp(domElement, tag, propKey$232, null, nextProps, propKey$201);
    for (lastProp in nextProps)
      propKey$201 = nextProps[lastProp], propKey = lastProps[lastProp], !nextProps.hasOwnProperty(lastProp) || propKey$201 === propKey || null == propKey$201 && null == propKey || setProp(domElement, tag, lastProp, propKey$201, nextProps, propKey);
  }
  function isLikelyStaticResource(initiatorType) {
    switch (initiatorType) {
      case "css":
      case "script":
      case "font":
      case "img":
      case "image":
      case "input":
      case "link":
        return true;
      default:
        return false;
    }
  }
  function estimateBandwidth() {
    if ("function" === typeof performance.getEntriesByType) {
      for (var count = 0, bits = 0, resourceEntries = performance.getEntriesByType("resource"), i = 0; i < resourceEntries.length; i++) {
        var entry = resourceEntries[i], transferSize = entry.transferSize, initiatorType = entry.initiatorType, duration = entry.duration;
        if (transferSize && duration && isLikelyStaticResource(initiatorType)) {
          initiatorType = 0;
          duration = entry.responseEnd;
          for (i += 1; i < resourceEntries.length; i++) {
            var overlapEntry = resourceEntries[i], overlapStartTime = overlapEntry.startTime;
            if (overlapStartTime > duration) break;
            var overlapTransferSize = overlapEntry.transferSize, overlapInitiatorType = overlapEntry.initiatorType;
            overlapTransferSize && isLikelyStaticResource(overlapInitiatorType) && (overlapEntry = overlapEntry.responseEnd, initiatorType += overlapTransferSize * (overlapEntry < duration ? 1 : (duration - overlapStartTime) / (overlapEntry - overlapStartTime)));
          }
          --i;
          bits += 8 * (transferSize + initiatorType) / (entry.duration / 1e3);
          count++;
          if (10 < count) break;
        }
      }
      if (0 < count) return bits / count / 1e6;
    }
    return navigator.connection && (count = navigator.connection.downlink, "number" === typeof count) ? count : 5;
  }
  var eventsEnabled = null, selectionInformation = null;
  function getOwnerDocumentFromRootContainer(rootContainerElement) {
    return 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
  }
  function getOwnHostContext(namespaceURI) {
    switch (namespaceURI) {
      case "http://www.w3.org/2000/svg":
        return 1;
      case "http://www.w3.org/1998/Math/MathML":
        return 2;
      default:
        return 0;
    }
  }
  function getChildHostContextProd(parentNamespace, type) {
    if (0 === parentNamespace)
      switch (type) {
        case "svg":
          return 1;
        case "math":
          return 2;
        default:
          return 0;
      }
    return 1 === parentNamespace && "foreignObject" === type ? 0 : parentNamespace;
  }
  function shouldSetTextContent(type, props) {
    return "textarea" === type || "noscript" === type || "string" === typeof props.children || "number" === typeof props.children || "bigint" === typeof props.children || "object" === typeof props.dangerouslySetInnerHTML && null !== props.dangerouslySetInnerHTML && null != props.dangerouslySetInnerHTML.__html;
  }
  var currentPopstateTransitionEvent = null;
  function shouldAttemptEagerTransition() {
    var event = window.event;
    if (event && "popstate" === event.type) {
      if (event === currentPopstateTransitionEvent) return false;
      currentPopstateTransitionEvent = event;
      return true;
    }
    currentPopstateTransitionEvent = null;
    return false;
  }
  var scheduleTimeout = "function" === typeof setTimeout ? setTimeout : void 0, cancelTimeout = "function" === typeof clearTimeout ? clearTimeout : void 0, localPromise = "function" === typeof Promise ? Promise : void 0, scheduleMicrotask = "function" === typeof queueMicrotask ? queueMicrotask : "undefined" !== typeof localPromise ? function(callback) {
    return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
  } : scheduleTimeout;
  function handleErrorInNextTick(error) {
    setTimeout(function() {
      throw error;
    });
  }
  function isSingletonScope(type) {
    return "head" === type;
  }
  function clearHydrationBoundary(parentInstance, hydrationInstance) {
    var node = hydrationInstance, depth = 0;
    do {
      var nextNode = node.nextSibling;
      parentInstance.removeChild(node);
      if (nextNode && 8 === nextNode.nodeType)
        if (node = nextNode.data, "/$" === node || "/&" === node) {
          if (0 === depth) {
            parentInstance.removeChild(nextNode);
            retryIfBlockedOn(hydrationInstance);
            return;
          }
          depth--;
        } else if ("$" === node || "$?" === node || "$~" === node || "$!" === node || "&" === node)
          depth++;
        else if ("html" === node)
          releaseSingletonInstance(parentInstance.ownerDocument.documentElement);
        else if ("head" === node) {
          node = parentInstance.ownerDocument.head;
          releaseSingletonInstance(node);
          for (var node$jscomp$0 = node.firstChild; node$jscomp$0; ) {
            var nextNode$jscomp$0 = node$jscomp$0.nextSibling, nodeName = node$jscomp$0.nodeName;
            node$jscomp$0[internalHoistableMarker] || "SCRIPT" === nodeName || "STYLE" === nodeName || "LINK" === nodeName && "stylesheet" === node$jscomp$0.rel.toLowerCase() || node.removeChild(node$jscomp$0);
            node$jscomp$0 = nextNode$jscomp$0;
          }
        } else
          "body" === node && releaseSingletonInstance(parentInstance.ownerDocument.body);
      node = nextNode;
    } while (node);
    retryIfBlockedOn(hydrationInstance);
  }
  function hideOrUnhideDehydratedBoundary(suspenseInstance, isHidden) {
    var node = suspenseInstance;
    suspenseInstance = 0;
    do {
      var nextNode = node.nextSibling;
      1 === node.nodeType ? isHidden ? (node._stashedDisplay = node.style.display, node.style.display = "none") : (node.style.display = node._stashedDisplay || "", "" === node.getAttribute("style") && node.removeAttribute("style")) : 3 === node.nodeType && (isHidden ? (node._stashedText = node.nodeValue, node.nodeValue = "") : node.nodeValue = node._stashedText || "");
      if (nextNode && 8 === nextNode.nodeType)
        if (node = nextNode.data, "/$" === node)
          if (0 === suspenseInstance) break;
          else suspenseInstance--;
        else
          "$" !== node && "$?" !== node && "$~" !== node && "$!" !== node || suspenseInstance++;
      node = nextNode;
    } while (node);
  }
  function clearContainerSparingly(container) {
    var nextNode = container.firstChild;
    nextNode && 10 === nextNode.nodeType && (nextNode = nextNode.nextSibling);
    for (; nextNode; ) {
      var node = nextNode;
      nextNode = nextNode.nextSibling;
      switch (node.nodeName) {
        case "HTML":
        case "HEAD":
        case "BODY":
          clearContainerSparingly(node);
          detachDeletedInstance(node);
          continue;
        case "SCRIPT":
        case "STYLE":
          continue;
        case "LINK":
          if ("stylesheet" === node.rel.toLowerCase()) continue;
      }
      container.removeChild(node);
    }
  }
  function canHydrateInstance(instance, type, props, inRootOrSingleton) {
    for (; 1 === instance.nodeType; ) {
      var anyProps = props;
      if (instance.nodeName.toLowerCase() !== type.toLowerCase()) {
        if (!inRootOrSingleton && ("INPUT" !== instance.nodeName || "hidden" !== instance.type))
          break;
      } else if (!inRootOrSingleton)
        if ("input" === type && "hidden" === instance.type) {
          var name = null == anyProps.name ? null : "" + anyProps.name;
          if ("hidden" === anyProps.type && instance.getAttribute("name") === name)
            return instance;
        } else return instance;
      else if (!instance[internalHoistableMarker])
        switch (type) {
          case "meta":
            if (!instance.hasAttribute("itemprop")) break;
            return instance;
          case "link":
            name = instance.getAttribute("rel");
            if ("stylesheet" === name && instance.hasAttribute("data-precedence"))
              break;
            else if (name !== anyProps.rel || instance.getAttribute("href") !== (null == anyProps.href || "" === anyProps.href ? null : anyProps.href) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin) || instance.getAttribute("title") !== (null == anyProps.title ? null : anyProps.title))
              break;
            return instance;
          case "style":
            if (instance.hasAttribute("data-precedence")) break;
            return instance;
          case "script":
            name = instance.getAttribute("src");
            if ((name !== (null == anyProps.src ? null : anyProps.src) || instance.getAttribute("type") !== (null == anyProps.type ? null : anyProps.type) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin)) && name && instance.hasAttribute("async") && !instance.hasAttribute("itemprop"))
              break;
            return instance;
          default:
            return instance;
        }
      instance = getNextHydratable(instance.nextSibling);
      if (null === instance) break;
    }
    return null;
  }
  function canHydrateTextInstance(instance, text, inRootOrSingleton) {
    if ("" === text) return null;
    for (; 3 !== instance.nodeType; ) {
      if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton)
        return null;
      instance = getNextHydratable(instance.nextSibling);
      if (null === instance) return null;
    }
    return instance;
  }
  function canHydrateHydrationBoundary(instance, inRootOrSingleton) {
    for (; 8 !== instance.nodeType; ) {
      if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton)
        return null;
      instance = getNextHydratable(instance.nextSibling);
      if (null === instance) return null;
    }
    return instance;
  }
  function isSuspenseInstancePending(instance) {
    return "$?" === instance.data || "$~" === instance.data;
  }
  function isSuspenseInstanceFallback(instance) {
    return "$!" === instance.data || "$?" === instance.data && "loading" !== instance.ownerDocument.readyState;
  }
  function registerSuspenseInstanceRetry(instance, callback) {
    var ownerDocument = instance.ownerDocument;
    if ("$~" === instance.data) instance._reactRetry = callback;
    else if ("$?" !== instance.data || "loading" !== ownerDocument.readyState)
      callback();
    else {
      var listener = function() {
        callback();
        ownerDocument.removeEventListener("DOMContentLoaded", listener);
      };
      ownerDocument.addEventListener("DOMContentLoaded", listener);
      instance._reactRetry = listener;
    }
  }
  function getNextHydratable(node) {
    for (; null != node; node = node.nextSibling) {
      var nodeType = node.nodeType;
      if (1 === nodeType || 3 === nodeType) break;
      if (8 === nodeType) {
        nodeType = node.data;
        if ("$" === nodeType || "$!" === nodeType || "$?" === nodeType || "$~" === nodeType || "&" === nodeType || "F!" === nodeType || "F" === nodeType)
          break;
        if ("/$" === nodeType || "/&" === nodeType) return null;
      }
    }
    return node;
  }
  var previousHydratableOnEnteringScopedSingleton = null;
  function getNextHydratableInstanceAfterHydrationBoundary(hydrationInstance) {
    hydrationInstance = hydrationInstance.nextSibling;
    for (var depth = 0; hydrationInstance; ) {
      if (8 === hydrationInstance.nodeType) {
        var data = hydrationInstance.data;
        if ("/$" === data || "/&" === data) {
          if (0 === depth)
            return getNextHydratable(hydrationInstance.nextSibling);
          depth--;
        } else
          "$" !== data && "$!" !== data && "$?" !== data && "$~" !== data && "&" !== data || depth++;
      }
      hydrationInstance = hydrationInstance.nextSibling;
    }
    return null;
  }
  function getParentHydrationBoundary(targetInstance) {
    targetInstance = targetInstance.previousSibling;
    for (var depth = 0; targetInstance; ) {
      if (8 === targetInstance.nodeType) {
        var data = targetInstance.data;
        if ("$" === data || "$!" === data || "$?" === data || "$~" === data || "&" === data) {
          if (0 === depth) return targetInstance;
          depth--;
        } else "/$" !== data && "/&" !== data || depth++;
      }
      targetInstance = targetInstance.previousSibling;
    }
    return null;
  }
  function resolveSingletonInstance(type, props, rootContainerInstance) {
    props = getOwnerDocumentFromRootContainer(rootContainerInstance);
    switch (type) {
      case "html":
        type = props.documentElement;
        if (!type) throw Error(formatProdErrorMessage(452));
        return type;
      case "head":
        type = props.head;
        if (!type) throw Error(formatProdErrorMessage(453));
        return type;
      case "body":
        type = props.body;
        if (!type) throw Error(formatProdErrorMessage(454));
        return type;
      default:
        throw Error(formatProdErrorMessage(451));
    }
  }
  function releaseSingletonInstance(instance) {
    for (var attributes = instance.attributes; attributes.length; )
      instance.removeAttributeNode(attributes[0]);
    detachDeletedInstance(instance);
  }
  var preloadPropsMap = /* @__PURE__ */ new Map(), preconnectsSet = /* @__PURE__ */ new Set();
  function getHoistableRoot(container) {
    return "function" === typeof container.getRootNode ? container.getRootNode() : 9 === container.nodeType ? container : container.ownerDocument;
  }
  var previousDispatcher = ReactDOMSharedInternals.d;
  ReactDOMSharedInternals.d = {
    f: flushSyncWork,
    r: requestFormReset,
    D: prefetchDNS,
    C: preconnect,
    L: preload2,
    m: preloadModule,
    X: preinitScript,
    S: preinitStyle,
    M: preinitModuleScript
  };
  function flushSyncWork() {
    var previousWasRendering = previousDispatcher.f(), wasRendering = flushSyncWork$1();
    return previousWasRendering || wasRendering;
  }
  function requestFormReset(form) {
    var formInst = getInstanceFromNode(form);
    null !== formInst && 5 === formInst.tag && "form" === formInst.type ? requestFormReset$1(formInst) : previousDispatcher.r(form);
  }
  var globalDocument = "undefined" === typeof document ? null : document;
  function preconnectAs(rel, href, crossOrigin) {
    var ownerDocument = globalDocument;
    if (ownerDocument && "string" === typeof href && href) {
      var limitedEscapedHref = escapeSelectorAttributeValueInsideDoubleQuotes(href);
      limitedEscapedHref = 'link[rel="' + rel + '"][href="' + limitedEscapedHref + '"]';
      "string" === typeof crossOrigin && (limitedEscapedHref += '[crossorigin="' + crossOrigin + '"]');
      preconnectsSet.has(limitedEscapedHref) || (preconnectsSet.add(limitedEscapedHref), rel = { rel, crossOrigin, href }, null === ownerDocument.querySelector(limitedEscapedHref) && (href = ownerDocument.createElement("link"), setInitialProperties(href, "link", rel), markNodeAsHoistable(href), ownerDocument.head.appendChild(href)));
    }
  }
  function prefetchDNS(href) {
    previousDispatcher.D(href);
    preconnectAs("dns-prefetch", href, null);
  }
  function preconnect(href, crossOrigin) {
    previousDispatcher.C(href, crossOrigin);
    preconnectAs("preconnect", href, crossOrigin);
  }
  function preload2(href, as, options2) {
    previousDispatcher.L(href, as, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && href && as) {
      var preloadSelector = 'link[rel="preload"][as="' + escapeSelectorAttributeValueInsideDoubleQuotes(as) + '"]';
      "image" === as ? options2 && options2.imageSrcSet ? (preloadSelector += '[imagesrcset="' + escapeSelectorAttributeValueInsideDoubleQuotes(
        options2.imageSrcSet
      ) + '"]', "string" === typeof options2.imageSizes && (preloadSelector += '[imagesizes="' + escapeSelectorAttributeValueInsideDoubleQuotes(
        options2.imageSizes
      ) + '"]')) : preloadSelector += '[href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]' : preloadSelector += '[href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]';
      var key = preloadSelector;
      switch (as) {
        case "style":
          key = getStyleKey(href);
          break;
        case "script":
          key = getScriptKey(href);
      }
      preloadPropsMap.has(key) || (href = assign(
        {
          rel: "preload",
          href: "image" === as && options2 && options2.imageSrcSet ? void 0 : href,
          as
        },
        options2
      ), preloadPropsMap.set(key, href), null !== ownerDocument.querySelector(preloadSelector) || "style" === as && ownerDocument.querySelector(getStylesheetSelectorFromKey(key)) || "script" === as && ownerDocument.querySelector(getScriptSelectorFromKey(key)) || (as = ownerDocument.createElement("link"), setInitialProperties(as, "link", href), markNodeAsHoistable(as), ownerDocument.head.appendChild(as)));
    }
  }
  function preloadModule(href, options2) {
    previousDispatcher.m(href, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && href) {
      var as = options2 && "string" === typeof options2.as ? options2.as : "script", preloadSelector = 'link[rel="modulepreload"][as="' + escapeSelectorAttributeValueInsideDoubleQuotes(as) + '"][href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]', key = preloadSelector;
      switch (as) {
        case "audioworklet":
        case "paintworklet":
        case "serviceworker":
        case "sharedworker":
        case "worker":
        case "script":
          key = getScriptKey(href);
      }
      if (!preloadPropsMap.has(key) && (href = assign({ rel: "modulepreload", href }, options2), preloadPropsMap.set(key, href), null === ownerDocument.querySelector(preloadSelector))) {
        switch (as) {
          case "audioworklet":
          case "paintworklet":
          case "serviceworker":
          case "sharedworker":
          case "worker":
          case "script":
            if (ownerDocument.querySelector(getScriptSelectorFromKey(key)))
              return;
        }
        as = ownerDocument.createElement("link");
        setInitialProperties(as, "link", href);
        markNodeAsHoistable(as);
        ownerDocument.head.appendChild(as);
      }
    }
  }
  function preinitStyle(href, precedence, options2) {
    previousDispatcher.S(href, precedence, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && href) {
      var styles = getResourcesFromRoot(ownerDocument).hoistableStyles, key = getStyleKey(href);
      precedence = precedence || "default";
      var resource = styles.get(key);
      if (!resource) {
        var state = { loading: 0, preload: null };
        if (resource = ownerDocument.querySelector(
          getStylesheetSelectorFromKey(key)
        ))
          state.loading = 5;
        else {
          href = assign(
            { rel: "stylesheet", href, "data-precedence": precedence },
            options2
          );
          (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(href, options2);
          var link = resource = ownerDocument.createElement("link");
          markNodeAsHoistable(link);
          setInitialProperties(link, "link", href);
          link._p = new Promise(function(resolve, reject) {
            link.onload = resolve;
            link.onerror = reject;
          });
          link.addEventListener("load", function() {
            state.loading |= 1;
          });
          link.addEventListener("error", function() {
            state.loading |= 2;
          });
          state.loading |= 4;
          insertStylesheet(resource, precedence, ownerDocument);
        }
        resource = {
          type: "stylesheet",
          instance: resource,
          count: 1,
          state
        };
        styles.set(key, resource);
      }
    }
  }
  function preinitScript(src, options2) {
    previousDispatcher.X(src, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && src) {
      var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
      resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({ src, async: true }, options2), (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options2), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
        type: "script",
        instance: resource,
        count: 1,
        state: null
      }, scripts.set(key, resource));
    }
  }
  function preinitModuleScript(src, options2) {
    previousDispatcher.M(src, options2);
    var ownerDocument = globalDocument;
    if (ownerDocument && src) {
      var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
      resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({ src, async: true, type: "module" }, options2), (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options2), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
        type: "script",
        instance: resource,
        count: 1,
        state: null
      }, scripts.set(key, resource));
    }
  }
  function getResource(type, currentProps, pendingProps, currentResource) {
    var JSCompiler_inline_result = (JSCompiler_inline_result = rootInstanceStackCursor.current) ? getHoistableRoot(JSCompiler_inline_result) : null;
    if (!JSCompiler_inline_result) throw Error(formatProdErrorMessage(446));
    switch (type) {
      case "meta":
      case "title":
        return null;
      case "style":
        return "string" === typeof pendingProps.precedence && "string" === typeof pendingProps.href ? (currentProps = getStyleKey(pendingProps.href), pendingProps = getResourcesFromRoot(
          JSCompiler_inline_result
        ).hoistableStyles, currentResource = pendingProps.get(currentProps), currentResource || (currentResource = {
          type: "style",
          instance: null,
          count: 0,
          state: null
        }, pendingProps.set(currentProps, currentResource)), currentResource) : { type: "void", instance: null, count: 0, state: null };
      case "link":
        if ("stylesheet" === pendingProps.rel && "string" === typeof pendingProps.href && "string" === typeof pendingProps.precedence) {
          type = getStyleKey(pendingProps.href);
          var styles$243 = getResourcesFromRoot(
            JSCompiler_inline_result
          ).hoistableStyles, resource$244 = styles$243.get(type);
          resource$244 || (JSCompiler_inline_result = JSCompiler_inline_result.ownerDocument || JSCompiler_inline_result, resource$244 = {
            type: "stylesheet",
            instance: null,
            count: 0,
            state: { loading: 0, preload: null }
          }, styles$243.set(type, resource$244), (styles$243 = JSCompiler_inline_result.querySelector(
            getStylesheetSelectorFromKey(type)
          )) && !styles$243._p && (resource$244.instance = styles$243, resource$244.state.loading = 5), preloadPropsMap.has(type) || (pendingProps = {
            rel: "preload",
            as: "style",
            href: pendingProps.href,
            crossOrigin: pendingProps.crossOrigin,
            integrity: pendingProps.integrity,
            media: pendingProps.media,
            hrefLang: pendingProps.hrefLang,
            referrerPolicy: pendingProps.referrerPolicy
          }, preloadPropsMap.set(type, pendingProps), styles$243 || preloadStylesheet(
            JSCompiler_inline_result,
            type,
            pendingProps,
            resource$244.state
          )));
          if (currentProps && null === currentResource)
            throw Error(formatProdErrorMessage(528, ""));
          return resource$244;
        }
        if (currentProps && null !== currentResource)
          throw Error(formatProdErrorMessage(529, ""));
        return null;
      case "script":
        return currentProps = pendingProps.async, pendingProps = pendingProps.src, "string" === typeof pendingProps && currentProps && "function" !== typeof currentProps && "symbol" !== typeof currentProps ? (currentProps = getScriptKey(pendingProps), pendingProps = getResourcesFromRoot(
          JSCompiler_inline_result
        ).hoistableScripts, currentResource = pendingProps.get(currentProps), currentResource || (currentResource = {
          type: "script",
          instance: null,
          count: 0,
          state: null
        }, pendingProps.set(currentProps, currentResource)), currentResource) : { type: "void", instance: null, count: 0, state: null };
      default:
        throw Error(formatProdErrorMessage(444, type));
    }
  }
  function getStyleKey(href) {
    return 'href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"';
  }
  function getStylesheetSelectorFromKey(key) {
    return 'link[rel="stylesheet"][' + key + "]";
  }
  function stylesheetPropsFromRawProps(rawProps) {
    return assign({}, rawProps, {
      "data-precedence": rawProps.precedence,
      precedence: null
    });
  }
  function preloadStylesheet(ownerDocument, key, preloadProps, state) {
    ownerDocument.querySelector('link[rel="preload"][as="style"][' + key + "]") ? state.loading = 1 : (key = ownerDocument.createElement("link"), state.preload = key, key.addEventListener("load", function() {
      return state.loading |= 1;
    }), key.addEventListener("error", function() {
      return state.loading |= 2;
    }), setInitialProperties(key, "link", preloadProps), markNodeAsHoistable(key), ownerDocument.head.appendChild(key));
  }
  function getScriptKey(src) {
    return '[src="' + escapeSelectorAttributeValueInsideDoubleQuotes(src) + '"]';
  }
  function getScriptSelectorFromKey(key) {
    return "script[async]" + key;
  }
  function acquireResource(hoistableRoot, resource, props) {
    resource.count++;
    if (null === resource.instance)
      switch (resource.type) {
        case "style":
          var instance = hoistableRoot.querySelector(
            'style[data-href~="' + escapeSelectorAttributeValueInsideDoubleQuotes(props.href) + '"]'
          );
          if (instance)
            return resource.instance = instance, markNodeAsHoistable(instance), instance;
          var styleProps = assign({}, props, {
            "data-href": props.href,
            "data-precedence": props.precedence,
            href: null,
            precedence: null
          });
          instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
            "style"
          );
          markNodeAsHoistable(instance);
          setInitialProperties(instance, "style", styleProps);
          insertStylesheet(instance, props.precedence, hoistableRoot);
          return resource.instance = instance;
        case "stylesheet":
          styleProps = getStyleKey(props.href);
          var instance$249 = hoistableRoot.querySelector(
            getStylesheetSelectorFromKey(styleProps)
          );
          if (instance$249)
            return resource.state.loading |= 4, resource.instance = instance$249, markNodeAsHoistable(instance$249), instance$249;
          instance = stylesheetPropsFromRawProps(props);
          (styleProps = preloadPropsMap.get(styleProps)) && adoptPreloadPropsForStylesheet(instance, styleProps);
          instance$249 = (hoistableRoot.ownerDocument || hoistableRoot).createElement("link");
          markNodeAsHoistable(instance$249);
          var linkInstance = instance$249;
          linkInstance._p = new Promise(function(resolve, reject) {
            linkInstance.onload = resolve;
            linkInstance.onerror = reject;
          });
          setInitialProperties(instance$249, "link", instance);
          resource.state.loading |= 4;
          insertStylesheet(instance$249, props.precedence, hoistableRoot);
          return resource.instance = instance$249;
        case "script":
          instance$249 = getScriptKey(props.src);
          if (styleProps = hoistableRoot.querySelector(
            getScriptSelectorFromKey(instance$249)
          ))
            return resource.instance = styleProps, markNodeAsHoistable(styleProps), styleProps;
          instance = props;
          if (styleProps = preloadPropsMap.get(instance$249))
            instance = assign({}, props), adoptPreloadPropsForScript(instance, styleProps);
          hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
          styleProps = hoistableRoot.createElement("script");
          markNodeAsHoistable(styleProps);
          setInitialProperties(styleProps, "link", instance);
          hoistableRoot.head.appendChild(styleProps);
          return resource.instance = styleProps;
        case "void":
          return null;
        default:
          throw Error(formatProdErrorMessage(443, resource.type));
      }
    else
      "stylesheet" === resource.type && 0 === (resource.state.loading & 4) && (instance = resource.instance, resource.state.loading |= 4, insertStylesheet(instance, props.precedence, hoistableRoot));
    return resource.instance;
  }
  function insertStylesheet(instance, precedence, root3) {
    for (var nodes = root3.querySelectorAll(
      'link[rel="stylesheet"][data-precedence],style[data-precedence]'
    ), last = nodes.length ? nodes[nodes.length - 1] : null, prior = last, i = 0; i < nodes.length; i++) {
      var node = nodes[i];
      if (node.dataset.precedence === precedence) prior = node;
      else if (prior !== last) break;
    }
    prior ? prior.parentNode.insertBefore(instance, prior.nextSibling) : (precedence = 9 === root3.nodeType ? root3.head : root3, precedence.insertBefore(instance, precedence.firstChild));
  }
  function adoptPreloadPropsForStylesheet(stylesheetProps, preloadProps) {
    null == stylesheetProps.crossOrigin && (stylesheetProps.crossOrigin = preloadProps.crossOrigin);
    null == stylesheetProps.referrerPolicy && (stylesheetProps.referrerPolicy = preloadProps.referrerPolicy);
    null == stylesheetProps.title && (stylesheetProps.title = preloadProps.title);
  }
  function adoptPreloadPropsForScript(scriptProps, preloadProps) {
    null == scriptProps.crossOrigin && (scriptProps.crossOrigin = preloadProps.crossOrigin);
    null == scriptProps.referrerPolicy && (scriptProps.referrerPolicy = preloadProps.referrerPolicy);
    null == scriptProps.integrity && (scriptProps.integrity = preloadProps.integrity);
  }
  var tagCaches = null;
  function getHydratableHoistableCache(type, keyAttribute, ownerDocument) {
    if (null === tagCaches) {
      var cache = /* @__PURE__ */ new Map();
      var caches = tagCaches = /* @__PURE__ */ new Map();
      caches.set(ownerDocument, cache);
    } else
      caches = tagCaches, cache = caches.get(ownerDocument), cache || (cache = /* @__PURE__ */ new Map(), caches.set(ownerDocument, cache));
    if (cache.has(type)) return cache;
    cache.set(type, null);
    ownerDocument = ownerDocument.getElementsByTagName(type);
    for (caches = 0; caches < ownerDocument.length; caches++) {
      var node = ownerDocument[caches];
      if (!(node[internalHoistableMarker] || node[internalInstanceKey] || "link" === type && "stylesheet" === node.getAttribute("rel")) && "http://www.w3.org/2000/svg" !== node.namespaceURI) {
        var nodeKey = node.getAttribute(keyAttribute) || "";
        nodeKey = type + nodeKey;
        var existing = cache.get(nodeKey);
        existing ? existing.push(node) : cache.set(nodeKey, [node]);
      }
    }
    return cache;
  }
  function mountHoistable(hoistableRoot, type, instance) {
    hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
    hoistableRoot.head.insertBefore(
      instance,
      "title" === type ? hoistableRoot.querySelector("head > title") : null
    );
  }
  function isHostHoistableType(type, props, hostContext) {
    if (1 === hostContext || null != props.itemProp) return false;
    switch (type) {
      case "meta":
      case "title":
        return true;
      case "style":
        if ("string" !== typeof props.precedence || "string" !== typeof props.href || "" === props.href)
          break;
        return true;
      case "link":
        if ("string" !== typeof props.rel || "string" !== typeof props.href || "" === props.href || props.onLoad || props.onError)
          break;
        switch (props.rel) {
          case "stylesheet":
            return type = props.disabled, "string" === typeof props.precedence && null == type;
          default:
            return true;
        }
      case "script":
        if (props.async && "function" !== typeof props.async && "symbol" !== typeof props.async && !props.onLoad && !props.onError && props.src && "string" === typeof props.src)
          return true;
    }
    return false;
  }
  function preloadResource(resource) {
    return "stylesheet" === resource.type && 0 === (resource.state.loading & 3) ? false : true;
  }
  function suspendResource(state, hoistableRoot, resource, props) {
    if ("stylesheet" === resource.type && ("string" !== typeof props.media || false !== matchMedia(props.media).matches) && 0 === (resource.state.loading & 4)) {
      if (null === resource.instance) {
        var key = getStyleKey(props.href), instance = hoistableRoot.querySelector(
          getStylesheetSelectorFromKey(key)
        );
        if (instance) {
          hoistableRoot = instance._p;
          null !== hoistableRoot && "object" === typeof hoistableRoot && "function" === typeof hoistableRoot.then && (state.count++, state = onUnsuspend.bind(state), hoistableRoot.then(state, state));
          resource.state.loading |= 4;
          resource.instance = instance;
          markNodeAsHoistable(instance);
          return;
        }
        instance = hoistableRoot.ownerDocument || hoistableRoot;
        props = stylesheetPropsFromRawProps(props);
        (key = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(props, key);
        instance = instance.createElement("link");
        markNodeAsHoistable(instance);
        var linkInstance = instance;
        linkInstance._p = new Promise(function(resolve, reject) {
          linkInstance.onload = resolve;
          linkInstance.onerror = reject;
        });
        setInitialProperties(instance, "link", props);
        resource.instance = instance;
      }
      null === state.stylesheets && (state.stylesheets = /* @__PURE__ */ new Map());
      state.stylesheets.set(resource, hoistableRoot);
      (hoistableRoot = resource.state.preload) && 0 === (resource.state.loading & 3) && (state.count++, resource = onUnsuspend.bind(state), hoistableRoot.addEventListener("load", resource), hoistableRoot.addEventListener("error", resource));
    }
  }
  var estimatedBytesWithinLimit = 0;
  function waitForCommitToBeReady(state, timeoutOffset) {
    state.stylesheets && 0 === state.count && insertSuspendedStylesheets(state, state.stylesheets);
    return 0 < state.count || 0 < state.imgCount ? function(commit) {
      var stylesheetTimer = setTimeout(function() {
        state.stylesheets && insertSuspendedStylesheets(state, state.stylesheets);
        if (state.unsuspend) {
          var unsuspend = state.unsuspend;
          state.unsuspend = null;
          unsuspend();
        }
      }, 6e4 + timeoutOffset);
      0 < state.imgBytes && 0 === estimatedBytesWithinLimit && (estimatedBytesWithinLimit = 62500 * estimateBandwidth());
      var imgTimer = setTimeout(
        function() {
          state.waitingForImages = false;
          if (0 === state.count && (state.stylesheets && insertSuspendedStylesheets(state, state.stylesheets), state.unsuspend)) {
            var unsuspend = state.unsuspend;
            state.unsuspend = null;
            unsuspend();
          }
        },
        (state.imgBytes > estimatedBytesWithinLimit ? 50 : 800) + timeoutOffset
      );
      state.unsuspend = commit;
      return function() {
        state.unsuspend = null;
        clearTimeout(stylesheetTimer);
        clearTimeout(imgTimer);
      };
    } : null;
  }
  function onUnsuspend() {
    this.count--;
    if (0 === this.count && (0 === this.imgCount || !this.waitingForImages)) {
      if (this.stylesheets) insertSuspendedStylesheets(this, this.stylesheets);
      else if (this.unsuspend) {
        var unsuspend = this.unsuspend;
        this.unsuspend = null;
        unsuspend();
      }
    }
  }
  var precedencesByRoot = null;
  function insertSuspendedStylesheets(state, resources) {
    state.stylesheets = null;
    null !== state.unsuspend && (state.count++, precedencesByRoot = /* @__PURE__ */ new Map(), resources.forEach(insertStylesheetIntoRoot, state), precedencesByRoot = null, onUnsuspend.call(state));
  }
  function insertStylesheetIntoRoot(root3, resource) {
    if (!(resource.state.loading & 4)) {
      var precedences = precedencesByRoot.get(root3);
      if (precedences) var last = precedences.get(null);
      else {
        precedences = /* @__PURE__ */ new Map();
        precedencesByRoot.set(root3, precedences);
        for (var nodes = root3.querySelectorAll(
          "link[data-precedence],style[data-precedence]"
        ), i = 0; i < nodes.length; i++) {
          var node = nodes[i];
          if ("LINK" === node.nodeName || "not all" !== node.getAttribute("media"))
            precedences.set(node.dataset.precedence, node), last = node;
        }
        last && precedences.set(null, last);
      }
      nodes = resource.instance;
      node = nodes.getAttribute("data-precedence");
      i = precedences.get(node) || last;
      i === last && precedences.set(null, nodes);
      precedences.set(node, nodes);
      this.count++;
      last = onUnsuspend.bind(this);
      nodes.addEventListener("load", last);
      nodes.addEventListener("error", last);
      i ? i.parentNode.insertBefore(nodes, i.nextSibling) : (root3 = 9 === root3.nodeType ? root3.head : root3, root3.insertBefore(nodes, root3.firstChild));
      resource.state.loading |= 4;
    }
  }
  var HostTransitionContext = {
    $$typeof: REACT_CONTEXT_TYPE,
    Provider: null,
    Consumer: null,
    _currentValue: sharedNotPendingObject,
    _currentValue2: sharedNotPendingObject,
    _threadCount: 0
  };
  function FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator, formState) {
    this.tag = 1;
    this.containerInfo = containerInfo;
    this.pingCache = this.current = this.pendingChildren = null;
    this.timeoutHandle = -1;
    this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null;
    this.callbackPriority = 0;
    this.expirationTimes = createLaneMap(-1);
    this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0;
    this.entanglements = createLaneMap(0);
    this.hiddenUpdates = createLaneMap(null);
    this.identifierPrefix = identifierPrefix;
    this.onUncaughtError = onUncaughtError;
    this.onCaughtError = onCaughtError;
    this.onRecoverableError = onRecoverableError;
    this.pooledCache = null;
    this.pooledCacheLanes = 0;
    this.formState = formState;
    this.incompleteTransitions = /* @__PURE__ */ new Map();
  }
  function createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, identifierPrefix, formState, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator) {
    containerInfo = new FiberRootNode(
      containerInfo,
      tag,
      hydrate,
      identifierPrefix,
      onUncaughtError,
      onCaughtError,
      onRecoverableError,
      onDefaultTransitionIndicator,
      formState
    );
    tag = 1;
    true === isStrictMode && (tag |= 24);
    isStrictMode = createFiberImplClass(3, null, null, tag);
    containerInfo.current = isStrictMode;
    isStrictMode.stateNode = containerInfo;
    tag = createCache();
    tag.refCount++;
    containerInfo.pooledCache = tag;
    tag.refCount++;
    isStrictMode.memoizedState = {
      element: initialChildren,
      isDehydrated: hydrate,
      cache: tag
    };
    initializeUpdateQueue(isStrictMode);
    return containerInfo;
  }
  function getContextForSubtree(parentComponent) {
    if (!parentComponent) return emptyContextObject;
    parentComponent = emptyContextObject;
    return parentComponent;
  }
  function updateContainerImpl(rootFiber, lane, element, container, parentComponent, callback) {
    parentComponent = getContextForSubtree(parentComponent);
    null === container.context ? container.context = parentComponent : container.pendingContext = parentComponent;
    container = createUpdate(lane);
    container.payload = { element };
    callback = void 0 === callback ? null : callback;
    null !== callback && (container.callback = callback);
    element = enqueueUpdate(rootFiber, container, lane);
    null !== element && (scheduleUpdateOnFiber(element, rootFiber, lane), entangleTransitions(element, rootFiber, lane));
  }
  function markRetryLaneImpl(fiber, retryLane) {
    fiber = fiber.memoizedState;
    if (null !== fiber && null !== fiber.dehydrated) {
      var a = fiber.retryLane;
      fiber.retryLane = 0 !== a && a < retryLane ? a : retryLane;
    }
  }
  function markRetryLaneIfNotHydrated(fiber, retryLane) {
    markRetryLaneImpl(fiber, retryLane);
    (fiber = fiber.alternate) && markRetryLaneImpl(fiber, retryLane);
  }
  function attemptContinuousHydration(fiber) {
    if (13 === fiber.tag || 31 === fiber.tag) {
      var root3 = enqueueConcurrentRenderForLane(fiber, 67108864);
      null !== root3 && scheduleUpdateOnFiber(root3, fiber, 67108864);
      markRetryLaneIfNotHydrated(fiber, 67108864);
    }
  }
  function attemptHydrationAtCurrentPriority(fiber) {
    if (13 === fiber.tag || 31 === fiber.tag) {
      var lane = requestUpdateLane();
      lane = getBumpedLaneForHydrationByLane(lane);
      var root3 = enqueueConcurrentRenderForLane(fiber, lane);
      null !== root3 && scheduleUpdateOnFiber(root3, fiber, lane);
      markRetryLaneIfNotHydrated(fiber, lane);
    }
  }
  var _enabled = true;
  function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
    var prevTransition = ReactSharedInternals.T;
    ReactSharedInternals.T = null;
    var previousPriority = ReactDOMSharedInternals.p;
    try {
      ReactDOMSharedInternals.p = 2, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
    } finally {
      ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
    }
  }
  function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
    var prevTransition = ReactSharedInternals.T;
    ReactSharedInternals.T = null;
    var previousPriority = ReactDOMSharedInternals.p;
    try {
      ReactDOMSharedInternals.p = 8, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
    } finally {
      ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
    }
  }
  function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
    if (_enabled) {
      var blockedOn = findInstanceBlockingEvent(nativeEvent);
      if (null === blockedOn)
        dispatchEventForPluginEventSystem(
          domEventName,
          eventSystemFlags,
          nativeEvent,
          return_targetInst,
          targetContainer
        ), clearIfContinuousEvent(domEventName, nativeEvent);
      else if (queueIfContinuousEvent(
        blockedOn,
        domEventName,
        eventSystemFlags,
        targetContainer,
        nativeEvent
      ))
        nativeEvent.stopPropagation();
      else if (clearIfContinuousEvent(domEventName, nativeEvent), eventSystemFlags & 4 && -1 < discreteReplayableEvents.indexOf(domEventName)) {
        for (; null !== blockedOn; ) {
          var fiber = getInstanceFromNode(blockedOn);
          if (null !== fiber)
            switch (fiber.tag) {
              case 3:
                fiber = fiber.stateNode;
                if (fiber.current.memoizedState.isDehydrated) {
                  var lanes = getHighestPriorityLanes(fiber.pendingLanes);
                  if (0 !== lanes) {
                    var root3 = fiber;
                    root3.pendingLanes |= 2;
                    for (root3.entangledLanes |= 2; lanes; ) {
                      var lane = 1 << 31 - clz32(lanes);
                      root3.entanglements[1] |= lane;
                      lanes &= ~lane;
                    }
                    ensureRootIsScheduled(fiber);
                    0 === (executionContext & 6) && (workInProgressRootRenderTargetTime = now() + 500, flushSyncWorkAcrossRoots_impl(0));
                  }
                }
                break;
              case 31:
              case 13:
                root3 = enqueueConcurrentRenderForLane(fiber, 2), null !== root3 && scheduleUpdateOnFiber(root3, fiber, 2), flushSyncWork$1(), markRetryLaneIfNotHydrated(fiber, 2);
            }
          fiber = findInstanceBlockingEvent(nativeEvent);
          null === fiber && dispatchEventForPluginEventSystem(
            domEventName,
            eventSystemFlags,
            nativeEvent,
            return_targetInst,
            targetContainer
          );
          if (fiber === blockedOn) break;
          blockedOn = fiber;
        }
        null !== blockedOn && nativeEvent.stopPropagation();
      } else
        dispatchEventForPluginEventSystem(
          domEventName,
          eventSystemFlags,
          nativeEvent,
          null,
          targetContainer
        );
    }
  }
  function findInstanceBlockingEvent(nativeEvent) {
    nativeEvent = getEventTarget(nativeEvent);
    return findInstanceBlockingTarget(nativeEvent);
  }
  var return_targetInst = null;
  function findInstanceBlockingTarget(targetNode) {
    return_targetInst = null;
    targetNode = getClosestInstanceFromNode(targetNode);
    if (null !== targetNode) {
      var nearestMounted = getNearestMountedFiber(targetNode);
      if (null === nearestMounted) targetNode = null;
      else {
        var tag = nearestMounted.tag;
        if (13 === tag) {
          targetNode = getSuspenseInstanceFromFiber(nearestMounted);
          if (null !== targetNode) return targetNode;
          targetNode = null;
        } else if (31 === tag) {
          targetNode = getActivityInstanceFromFiber(nearestMounted);
          if (null !== targetNode) return targetNode;
          targetNode = null;
        } else if (3 === tag) {
          if (nearestMounted.stateNode.current.memoizedState.isDehydrated)
            return 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
          targetNode = null;
        } else nearestMounted !== targetNode && (targetNode = null);
      }
    }
    return_targetInst = targetNode;
    return null;
  }
  function getEventPriority(domEventName) {
    switch (domEventName) {
      case "beforetoggle":
      case "cancel":
      case "click":
      case "close":
      case "contextmenu":
      case "copy":
      case "cut":
      case "auxclick":
      case "dblclick":
      case "dragend":
      case "dragstart":
      case "drop":
      case "focusin":
      case "focusout":
      case "input":
      case "invalid":
      case "keydown":
      case "keypress":
      case "keyup":
      case "mousedown":
      case "mouseup":
      case "paste":
      case "pause":
      case "play":
      case "pointercancel":
      case "pointerdown":
      case "pointerup":
      case "ratechange":
      case "reset":
      case "resize":
      case "seeked":
      case "submit":
      case "toggle":
      case "touchcancel":
      case "touchend":
      case "touchstart":
      case "volumechange":
      case "change":
      case "selectionchange":
      case "textInput":
      case "compositionstart":
      case "compositionend":
      case "compositionupdate":
      case "beforeblur":
      case "afterblur":
      case "beforeinput":
      case "blur":
      case "fullscreenchange":
      case "focus":
      case "hashchange":
      case "popstate":
      case "select":
      case "selectstart":
        return 2;
      case "drag":
      case "dragenter":
      case "dragexit":
      case "dragleave":
      case "dragover":
      case "mousemove":
      case "mouseout":
      case "mouseover":
      case "pointermove":
      case "pointerout":
      case "pointerover":
      case "scroll":
      case "touchmove":
      case "wheel":
      case "mouseenter":
      case "mouseleave":
      case "pointerenter":
      case "pointerleave":
        return 8;
      case "message":
        switch (getCurrentPriorityLevel()) {
          case ImmediatePriority:
            return 2;
          case UserBlockingPriority:
            return 8;
          case NormalPriority$1:
          case LowPriority:
            return 32;
          case IdlePriority:
            return 268435456;
          default:
            return 32;
        }
      default:
        return 32;
    }
  }
  var hasScheduledReplayAttempt = false, queuedFocus = null, queuedDrag = null, queuedMouse = null, queuedPointers = /* @__PURE__ */ new Map(), queuedPointerCaptures = /* @__PURE__ */ new Map(), queuedExplicitHydrationTargets = [], discreteReplayableEvents = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(
    " "
  );
  function clearIfContinuousEvent(domEventName, nativeEvent) {
    switch (domEventName) {
      case "focusin":
      case "focusout":
        queuedFocus = null;
        break;
      case "dragenter":
      case "dragleave":
        queuedDrag = null;
        break;
      case "mouseover":
      case "mouseout":
        queuedMouse = null;
        break;
      case "pointerover":
      case "pointerout":
        queuedPointers.delete(nativeEvent.pointerId);
        break;
      case "gotpointercapture":
      case "lostpointercapture":
        queuedPointerCaptures.delete(nativeEvent.pointerId);
    }
  }
  function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
    if (null === existingQueuedEvent || existingQueuedEvent.nativeEvent !== nativeEvent)
      return existingQueuedEvent = {
        blockedOn,
        domEventName,
        eventSystemFlags,
        nativeEvent,
        targetContainers: [targetContainer]
      }, null !== blockedOn && (blockedOn = getInstanceFromNode(blockedOn), null !== blockedOn && attemptContinuousHydration(blockedOn)), existingQueuedEvent;
    existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
    blockedOn = existingQueuedEvent.targetContainers;
    null !== targetContainer && -1 === blockedOn.indexOf(targetContainer) && blockedOn.push(targetContainer);
    return existingQueuedEvent;
  }
  function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
    switch (domEventName) {
      case "focusin":
        return queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(
          queuedFocus,
          blockedOn,
          domEventName,
          eventSystemFlags,
          targetContainer,
          nativeEvent
        ), true;
      case "dragenter":
        return queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(
          queuedDrag,
          blockedOn,
          domEventName,
          eventSystemFlags,
          targetContainer,
          nativeEvent
        ), true;
      case "mouseover":
        return queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(
          queuedMouse,
          blockedOn,
          domEventName,
          eventSystemFlags,
          targetContainer,
          nativeEvent
        ), true;
      case "pointerover":
        var pointerId = nativeEvent.pointerId;
        queuedPointers.set(
          pointerId,
          accumulateOrCreateContinuousQueuedReplayableEvent(
            queuedPointers.get(pointerId) || null,
            blockedOn,
            domEventName,
            eventSystemFlags,
            targetContainer,
            nativeEvent
          )
        );
        return true;
      case "gotpointercapture":
        return pointerId = nativeEvent.pointerId, queuedPointerCaptures.set(
          pointerId,
          accumulateOrCreateContinuousQueuedReplayableEvent(
            queuedPointerCaptures.get(pointerId) || null,
            blockedOn,
            domEventName,
            eventSystemFlags,
            targetContainer,
            nativeEvent
          )
        ), true;
    }
    return false;
  }
  function attemptExplicitHydrationTarget(queuedTarget) {
    var targetInst = getClosestInstanceFromNode(queuedTarget.target);
    if (null !== targetInst) {
      var nearestMounted = getNearestMountedFiber(targetInst);
      if (null !== nearestMounted) {
        if (targetInst = nearestMounted.tag, 13 === targetInst) {
          if (targetInst = getSuspenseInstanceFromFiber(nearestMounted), null !== targetInst) {
            queuedTarget.blockedOn = targetInst;
            runWithPriority(queuedTarget.priority, function() {
              attemptHydrationAtCurrentPriority(nearestMounted);
            });
            return;
          }
        } else if (31 === targetInst) {
          if (targetInst = getActivityInstanceFromFiber(nearestMounted), null !== targetInst) {
            queuedTarget.blockedOn = targetInst;
            runWithPriority(queuedTarget.priority, function() {
              attemptHydrationAtCurrentPriority(nearestMounted);
            });
            return;
          }
        } else if (3 === targetInst && nearestMounted.stateNode.current.memoizedState.isDehydrated) {
          queuedTarget.blockedOn = 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
          return;
        }
      }
    }
    queuedTarget.blockedOn = null;
  }
  function attemptReplayContinuousQueuedEvent(queuedEvent) {
    if (null !== queuedEvent.blockedOn) return false;
    for (var targetContainers = queuedEvent.targetContainers; 0 < targetContainers.length; ) {
      var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.nativeEvent);
      if (null === nextBlockedOn) {
        nextBlockedOn = queuedEvent.nativeEvent;
        var nativeEventClone = new nextBlockedOn.constructor(
          nextBlockedOn.type,
          nextBlockedOn
        );
        currentReplayingEvent = nativeEventClone;
        nextBlockedOn.target.dispatchEvent(nativeEventClone);
        currentReplayingEvent = null;
      } else
        return targetContainers = getInstanceFromNode(nextBlockedOn), null !== targetContainers && attemptContinuousHydration(targetContainers), queuedEvent.blockedOn = nextBlockedOn, false;
      targetContainers.shift();
    }
    return true;
  }
  function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
    attemptReplayContinuousQueuedEvent(queuedEvent) && map.delete(key);
  }
  function replayUnblockedEvents() {
    hasScheduledReplayAttempt = false;
    null !== queuedFocus && attemptReplayContinuousQueuedEvent(queuedFocus) && (queuedFocus = null);
    null !== queuedDrag && attemptReplayContinuousQueuedEvent(queuedDrag) && (queuedDrag = null);
    null !== queuedMouse && attemptReplayContinuousQueuedEvent(queuedMouse) && (queuedMouse = null);
    queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
    queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
  }
  function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
    queuedEvent.blockedOn === unblocked && (queuedEvent.blockedOn = null, hasScheduledReplayAttempt || (hasScheduledReplayAttempt = true, Scheduler.unstable_scheduleCallback(
      Scheduler.unstable_NormalPriority,
      replayUnblockedEvents
    )));
  }
  var lastScheduledReplayQueue = null;
  function scheduleReplayQueueIfNeeded(formReplayingQueue) {
    lastScheduledReplayQueue !== formReplayingQueue && (lastScheduledReplayQueue = formReplayingQueue, Scheduler.unstable_scheduleCallback(
      Scheduler.unstable_NormalPriority,
      function() {
        lastScheduledReplayQueue === formReplayingQueue && (lastScheduledReplayQueue = null);
        for (var i = 0; i < formReplayingQueue.length; i += 3) {
          var form = formReplayingQueue[i], submitterOrAction = formReplayingQueue[i + 1], formData = formReplayingQueue[i + 2];
          if ("function" !== typeof submitterOrAction)
            if (null === findInstanceBlockingTarget(submitterOrAction || form))
              continue;
            else break;
          var formInst = getInstanceFromNode(form);
          null !== formInst && (formReplayingQueue.splice(i, 3), i -= 3, startHostTransition(
            formInst,
            {
              pending: true,
              data: formData,
              method: form.method,
              action: submitterOrAction
            },
            submitterOrAction,
            formData
          ));
        }
      }
    ));
  }
  function retryIfBlockedOn(unblocked) {
    function unblock(queuedEvent) {
      return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
    }
    null !== queuedFocus && scheduleCallbackIfUnblocked(queuedFocus, unblocked);
    null !== queuedDrag && scheduleCallbackIfUnblocked(queuedDrag, unblocked);
    null !== queuedMouse && scheduleCallbackIfUnblocked(queuedMouse, unblocked);
    queuedPointers.forEach(unblock);
    queuedPointerCaptures.forEach(unblock);
    for (var i = 0; i < queuedExplicitHydrationTargets.length; i++) {
      var queuedTarget = queuedExplicitHydrationTargets[i];
      queuedTarget.blockedOn === unblocked && (queuedTarget.blockedOn = null);
    }
    for (; 0 < queuedExplicitHydrationTargets.length && (i = queuedExplicitHydrationTargets[0], null === i.blockedOn); )
      attemptExplicitHydrationTarget(i), null === i.blockedOn && queuedExplicitHydrationTargets.shift();
    i = (unblocked.ownerDocument || unblocked).$$reactFormReplay;
    if (null != i)
      for (queuedTarget = 0; queuedTarget < i.length; queuedTarget += 3) {
        var form = i[queuedTarget], submitterOrAction = i[queuedTarget + 1], formProps = form[internalPropsKey] || null;
        if ("function" === typeof submitterOrAction)
          formProps || scheduleReplayQueueIfNeeded(i);
        else if (formProps) {
          var action = null;
          if (submitterOrAction && submitterOrAction.hasAttribute("formAction"))
            if (form = submitterOrAction, formProps = submitterOrAction[internalPropsKey] || null)
              action = formProps.formAction;
            else {
              if (null !== findInstanceBlockingTarget(form)) continue;
            }
          else action = formProps.action;
          "function" === typeof action ? i[queuedTarget + 1] = action : (i.splice(queuedTarget, 3), queuedTarget -= 3);
          scheduleReplayQueueIfNeeded(i);
        }
      }
  }
  function defaultOnDefaultTransitionIndicator() {
    function handleNavigate(event) {
      event.canIntercept && "react-transition" === event.info && event.intercept({
        handler: function() {
          return new Promise(function(resolve) {
            return pendingResolve = resolve;
          });
        },
        focusReset: "manual",
        scroll: "manual"
      });
    }
    function handleNavigateComplete() {
      null !== pendingResolve && (pendingResolve(), pendingResolve = null);
      isCancelled || setTimeout(startFakeNavigation, 20);
    }
    function startFakeNavigation() {
      if (!isCancelled && !navigation.transition) {
        var currentEntry = navigation.currentEntry;
        currentEntry && null != currentEntry.url && navigation.navigate(currentEntry.url, {
          state: currentEntry.getState(),
          info: "react-transition",
          history: "replace"
        });
      }
    }
    if ("object" === typeof navigation) {
      var isCancelled = false, pendingResolve = null;
      navigation.addEventListener("navigate", handleNavigate);
      navigation.addEventListener("navigatesuccess", handleNavigateComplete);
      navigation.addEventListener("navigateerror", handleNavigateComplete);
      setTimeout(startFakeNavigation, 100);
      return function() {
        isCancelled = true;
        navigation.removeEventListener("navigate", handleNavigate);
        navigation.removeEventListener("navigatesuccess", handleNavigateComplete);
        navigation.removeEventListener("navigateerror", handleNavigateComplete);
        null !== pendingResolve && (pendingResolve(), pendingResolve = null);
      };
    }
  }
  function ReactDOMRoot(internalRoot) {
    this._internalRoot = internalRoot;
  }
  ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
    var root3 = this._internalRoot;
    if (null === root3) throw Error(formatProdErrorMessage(409));
    var current = root3.current, lane = requestUpdateLane();
    updateContainerImpl(current, lane, children, root3, null, null);
  };
  ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
    var root3 = this._internalRoot;
    if (null !== root3) {
      this._internalRoot = null;
      var container = root3.containerInfo;
      updateContainerImpl(root3.current, 2, null, root3, null, null);
      flushSyncWork$1();
      container[internalContainerInstanceKey] = null;
    }
  };
  function ReactDOMHydrationRoot(internalRoot) {
    this._internalRoot = internalRoot;
  }
  ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = function(target) {
    if (target) {
      var updatePriority = resolveUpdatePriority();
      target = { blockedOn: null, target, priority: updatePriority };
      for (var i = 0; i < queuedExplicitHydrationTargets.length && 0 !== updatePriority && updatePriority < queuedExplicitHydrationTargets[i].priority; i++) ;
      queuedExplicitHydrationTargets.splice(i, 0, target);
      0 === i && attemptExplicitHydrationTarget(target);
    }
  };
  var isomorphicReactPackageVersion$jscomp$inline_1840 = React.version;
  if ("19.2.7" !== isomorphicReactPackageVersion$jscomp$inline_1840)
    throw Error(
      formatProdErrorMessage(
        527,
        isomorphicReactPackageVersion$jscomp$inline_1840,
        "19.2.7"
      )
    );
  ReactDOMSharedInternals.findDOMNode = function(componentOrElement) {
    var fiber = componentOrElement._reactInternals;
    if (void 0 === fiber) {
      if ("function" === typeof componentOrElement.render)
        throw Error(formatProdErrorMessage(188));
      componentOrElement = Object.keys(componentOrElement).join(",");
      throw Error(formatProdErrorMessage(268, componentOrElement));
    }
    componentOrElement = findCurrentFiberUsingSlowPath(fiber);
    componentOrElement = null !== componentOrElement ? findCurrentHostFiberImpl(componentOrElement) : null;
    componentOrElement = null === componentOrElement ? null : componentOrElement.stateNode;
    return componentOrElement;
  };
  var internals$jscomp$inline_2347 = {
    bundleType: 0,
    version: "19.2.7",
    rendererPackageName: "react-dom",
    currentDispatcherRef: ReactSharedInternals,
    reconcilerVersion: "19.2.7"
  };
  if ("undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__) {
    var hook$jscomp$inline_2348 = __REACT_DEVTOOLS_GLOBAL_HOOK__;
    if (!hook$jscomp$inline_2348.isDisabled && hook$jscomp$inline_2348.supportsFiber)
      try {
        rendererID = hook$jscomp$inline_2348.inject(
          internals$jscomp$inline_2347
        ), injectedHook = hook$jscomp$inline_2348;
      } catch (err) {
      }
  }
  reactDomClient_production.createRoot = function(container, options2) {
    if (!isValidContainer(container)) throw Error(formatProdErrorMessage(299));
    var isStrictMode = false, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError;
    null !== options2 && void 0 !== options2 && (true === options2.unstable_strictMode && (isStrictMode = true), void 0 !== options2.identifierPrefix && (identifierPrefix = options2.identifierPrefix), void 0 !== options2.onUncaughtError && (onUncaughtError = options2.onUncaughtError), void 0 !== options2.onCaughtError && (onCaughtError = options2.onCaughtError), void 0 !== options2.onRecoverableError && (onRecoverableError = options2.onRecoverableError));
    options2 = createFiberRoot(
      container,
      1,
      false,
      null,
      null,
      isStrictMode,
      identifierPrefix,
      null,
      onUncaughtError,
      onCaughtError,
      onRecoverableError,
      defaultOnDefaultTransitionIndicator
    );
    container[internalContainerInstanceKey] = options2.current;
    listenToAllSupportedEvents(container);
    return new ReactDOMRoot(options2);
  };
  reactDomClient_production.hydrateRoot = function(container, initialChildren, options2) {
    if (!isValidContainer(container)) throw Error(formatProdErrorMessage(299));
    var isStrictMode = false, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError, formState = null;
    null !== options2 && void 0 !== options2 && (true === options2.unstable_strictMode && (isStrictMode = true), void 0 !== options2.identifierPrefix && (identifierPrefix = options2.identifierPrefix), void 0 !== options2.onUncaughtError && (onUncaughtError = options2.onUncaughtError), void 0 !== options2.onCaughtError && (onCaughtError = options2.onCaughtError), void 0 !== options2.onRecoverableError && (onRecoverableError = options2.onRecoverableError), void 0 !== options2.formState && (formState = options2.formState));
    initialChildren = createFiberRoot(
      container,
      1,
      true,
      initialChildren,
      null != options2 ? options2 : null,
      isStrictMode,
      identifierPrefix,
      formState,
      onUncaughtError,
      onCaughtError,
      onRecoverableError,
      defaultOnDefaultTransitionIndicator
    );
    initialChildren.context = getContextForSubtree(null);
    options2 = initialChildren.current;
    isStrictMode = requestUpdateLane();
    isStrictMode = getBumpedLaneForHydrationByLane(isStrictMode);
    identifierPrefix = createUpdate(isStrictMode);
    identifierPrefix.callback = null;
    enqueueUpdate(options2, identifierPrefix, isStrictMode);
    options2 = isStrictMode;
    initialChildren.current.lanes = options2;
    markRootUpdated$1(initialChildren, options2);
    ensureRootIsScheduled(initialChildren);
    container[internalContainerInstanceKey] = initialChildren.current;
    listenToAllSupportedEvents(container);
    return new ReactDOMHydrationRoot(initialChildren);
  };
  reactDomClient_production.version = "19.2.7";
  return reactDomClient_production;
}
var hasRequiredClient;
function requireClient() {
  if (hasRequiredClient) return client.exports;
  hasRequiredClient = 1;
  function checkDCE() {
    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
      return;
    }
    try {
      __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
    } catch (err) {
      console.error(err);
    }
  }
  {
    checkDCE();
    client.exports = requireReactDomClient_production();
  }
  return client.exports;
}
var clientExports = requireClient();
const scriptRel = /* @__PURE__ */ (function detectScriptRel() {
  const relList = typeof document !== "undefined" && document.createElement("link").relList;
  return relList && relList.supports && relList.supports("modulepreload") ? "modulepreload" : "preload";
})();
const assetsURL = function(dep, importerUrl) {
  return new URL(dep, importerUrl).href;
};
const seen = {};
const __vitePreload = function preload(baseModule, deps, importerUrl) {
  let promise = Promise.resolve();
  if (deps && deps.length > 0) {
    let allSettled = function(promises$2) {
      return Promise.all(promises$2.map((p) => Promise.resolve(p).then((value$1) => ({
        status: "fulfilled",
        value: value$1
      }), (reason) => ({
        status: "rejected",
        reason
      }))));
    };
    const links = document.getElementsByTagName("link");
    const cspNonceMeta = document.querySelector("meta[property=csp-nonce]");
    const cspNonce = cspNonceMeta?.nonce || cspNonceMeta?.getAttribute("nonce");
    promise = allSettled(deps.map((dep) => {
      dep = assetsURL(dep, importerUrl);
      if (dep in seen) return;
      seen[dep] = true;
      const isCss = dep.endsWith(".css");
      const cssSelector = isCss ? '[rel="stylesheet"]' : "";
      if (!!importerUrl) for (let i$1 = links.length - 1; i$1 >= 0; i$1--) {
        const link$1 = links[i$1];
        if (link$1.href === dep && (!isCss || link$1.rel === "stylesheet")) return;
      }
      else if (document.querySelector(`link[href="${dep}"]${cssSelector}`)) return;
      const link = document.createElement("link");
      link.rel = isCss ? "stylesheet" : scriptRel;
      if (!isCss) link.as = "script";
      link.crossOrigin = "";
      link.href = dep;
      if (cspNonce) link.setAttribute("nonce", cspNonce);
      document.head.appendChild(link);
      if (isCss) return new Promise((res, rej) => {
        link.addEventListener("load", res);
        link.addEventListener("error", () => rej(/* @__PURE__ */ new Error(`Unable to preload CSS for ${dep}`)));
      });
    }));
  }
  function handlePreloadError(err$2) {
    const e$1 = new Event("vite:preloadError", { cancelable: true });
    e$1.payload = err$2;
    window.dispatchEvent(e$1);
    if (!e$1.defaultPrevented) throw err$2;
  }
  return promise.then((res) => {
    for (const item of res || []) {
      if (item.status !== "rejected") continue;
      handlePreloadError(item.reason);
    }
    return baseModule().catch(handlePreloadError);
  });
};
const Spinner = Loading;
function Loading({ size = "md", label = "正在加载" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: `ui-loading ui-loading--${size}`, role: "status", "aria-label": label || "正在加载", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-loading__spinner", "aria-hidden": "true" }),
    label !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }) : null
  ] });
}
function Button({
  children,
  variant = "secondary",
  size = "md",
  loading = false,
  icon,
  iconPosition = "left",
  fullWidth = false,
  visibility = "visible",
  disabled,
  className = "",
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "button",
    {
      className: `ui-button ui-button--${variant} ui-control--${size}${fullWidth ? " ui-button--full" : ""}${visibility === "hidden" ? " ui-button--hidden" : ""} ${className}`.trim(),
      type: "button",
      disabled: disabled === true || loading,
      "aria-busy": loading,
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: `ui-button__content${loading ? " ui-button__content--loading" : ""}`, children: [
          icon !== void 0 && iconPosition === "left" ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-button__icon", children: icon }) : null,
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children }),
          icon !== void 0 && iconPosition === "right" ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-button__icon", children: icon }) : null
        ] }),
        loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-button__spinner", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { size: "sm", label: "" }) }) : null
      ]
    }
  );
}
function IconButton({ label, tooltip, size = "md", loading = false, disabled, variant = "default", className = "", children, ...props }) {
  const variantClass = variant === "promptVoice" ? "prompt-voice-button" : "";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "button",
    {
      className: `ui-icon-button ui-control--${size} ${variantClass} ${className}`.trim(),
      type: "button",
      "aria-label": label,
      title: tooltip ?? label,
      disabled: disabled === true || loading,
      "aria-busy": loading,
      ...props,
      children: loading ? /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { size: "sm", label: "" }) : children
    }
  );
}
function Pressable({ children, appearance = "plain", selected, type = "button", ...props }) {
  const selectionState = selected === void 0 ? {} : props.role === "tab" ? { "aria-selected": selected } : { "aria-pressed": selected };
  return /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type, className: `ui-pressable ui-pressable--${appearance}`, "data-selected": selected || void 0, ...selectionState, ...props, children });
}
const PATHS = {
  play: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m8 5 11 7-11 7V5Z" }),
  pause: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "6", y: "5", width: "4", height: "14", rx: "1" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "14", y: "5", width: "4", height: "14", rx: "1" })
  ] }),
  stop: /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "6", y: "6", width: "12", height: "12", rx: "1.5" }),
  layers: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m12 3 9 5-9 5-9-5 9-5Z" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m3 12 9 5 9-5" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m3 16 9 5 9-5" })
  ] }),
  image: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "3", y: "4", width: "18", height: "16", rx: "2" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "8.5", cy: "9", r: "1.5" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m4 17 5-5 4 4 2-2 5 4" })
  ] }),
  clock: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "12", cy: "12", r: "9" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M12 7v5l3 2" })
  ] }),
  folder: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M3 7a2 2 0 0 1 2-2h5l2 2h7a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7Z" }),
  gauge: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M4 17a8 8 0 1 1 16 0" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m12 14 4-5" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M6 17h12" })
  ] }),
  user: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "12", cy: "8", r: "4" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M4 21a8 8 0 0 1 16 0" })
  ] }),
  repeat: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m17 2 4 4-4 4" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M3 11V9a3 3 0 0 1 3-3h15" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m7 22-4-4 4-4" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M21 13v2a3 3 0 0 1-3 3H3" })
  ] }),
  sparkles: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m12 3 1.4 4.1L17.5 8.5l-4.1 1.4L12 14l-1.4-4.1-4.1-1.4 4.1-1.4L12 3Z" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m19 14 .8 2.2L22 17l-2.2.8L19 20l-.8-2.2L16 17l2.2-.8L19 14Z" })
  ] }),
  heart: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M20.8 5.7a5.5 5.5 0 0 0-7.8 0L12 6.8l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 22l8.8-8.5a5.5 5.5 0 0 0 0-7.8Z" }),
  trash: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M4 7h16" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M9 7V4h6v3" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "m6 7 1 14h10l1-14" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M10 11v6M14 11v6" })
  ] }),
  activity: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M3 12h4l2-6 4 12 2-6h6" }),
  grid: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "3", y: "3", width: "7", height: "7", rx: "1" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "14", y: "3", width: "7", height: "7", rx: "1" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "3", y: "14", width: "7", height: "7", rx: "1" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "14", y: "14", width: "7", height: "7", rx: "1" })
  ] }),
  message: /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M21 14a4 4 0 0 1-4 4H8l-5 3V7a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4v7Z" }),
  palette: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M12 3a9 9 0 0 0 0 18h1.5a2 2 0 0 0 0-4H12a2 2 0 0 1 0-4h5a4 4 0 0 0 4-4 9 9 0 0 0-9-6Z" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "7.5", cy: "10", r: ".7" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "9", cy: "6.5", r: ".7" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "14", cy: "6", r: ".7" })
  ] }),
  settings: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("circle", { cx: "12", cy: "12", r: "3" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-2.8 2.8-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6v.2h-4V21a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1L4.2 17l.1-.1a1.7 1.7 0 0 0 .3-1.9A1.7 1.7 0 0 0 3 14H2.8v-4H3a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9L4.2 7 7 4.2l.1.1a1.7 1.7 0 0 0 1.9.3A1.7 1.7 0 0 0 10 3V2.8h4V3a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1L19.8 7l-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.2v4H21a1.7 1.7 0 0 0-1.6 1Z" })
  ] }),
  microphone: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: "9", y: "3", width: "6", height: "12", rx: "3" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("path", { d: "M5.5 11a6.5 6.5 0 0 0 13 0M12 17.5V21M9 21h6" })
  ] })
};
function UiIcon({ name, ...props }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { className: "ui-icon", viewBox: "0 0 24 24", "aria-hidden": "true", fill: "none", stroke: "currentColor", strokeWidth: "1.8", strokeLinecap: "round", strokeLinejoin: "round", ...props, children: PATHS[name] });
}
function Avatar({ source, fallback, size = "md" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `ui-avatar ui-avatar--${size}`, children: source !== void 0 && source !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx("img", { src: source, alt: "" }) : fallback.slice(0, 1) });
}
function ColorPalette({ colors }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-color-palette", "aria-label": "主题色预览", children: colors.map((color, index) => /* @__PURE__ */ jsxRuntimeExports.jsx(
    "i",
    {
      className: "ui-color-palette__swatch",
      style: { "--ui-swatch-color": color },
      "aria-hidden": "true"
    },
    `${color}-${index}`
  )) });
}
function Accordion({ title, children, defaultOpen = false, disabled = false }) {
  const [open, setOpen] = reactExports.useState(defaultOpen);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-accordion", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { type: "button", "aria-expanded": open, disabled, onClick: () => setOpen((value) => !value), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: title }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", children: "⌄" })
    ] }),
    open ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-accordion__content", children }) : null
  ] });
}
function Card({ children, header, footer, padding = "default", selected = false, disabled = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: `ui-card ui-card--${padding}`, "aria-disabled": disabled, "data-selected": selected || void 0, children: [
    header !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("header", { children: header }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-card__body", children }),
    footer !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("footer", { children: footer }) : null
  ] });
}
function EmptyState({ title, description, action }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-state", "aria-label": title, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-state__icon", "aria-hidden": "true", children: "◇" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: title }),
    description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null,
    action
  ] });
}
function DataTable({ label, columns, rows, rowKey, density = "default", loading = false, emptyTitle = "暂无数据" }) {
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-data-table__state", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { label: "正在加载表格" }) });
  if (rows.length === 0) return /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: emptyTitle });
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-data-table__scroll", tabIndex: 0, role: "region", "aria-label": label, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: `ui-data-table ui-data-table--${density}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("tr", { children: columns.map((column) => /* @__PURE__ */ jsxRuntimeExports.jsx("th", { scope: "col", style: column.width !== void 0 ? { width: column.width } : void 0, "data-align": column.align, children: column.header }, column.key)) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: rows.map((row) => /* @__PURE__ */ jsxRuntimeExports.jsx("tr", { children: columns.map((column) => /* @__PURE__ */ jsxRuntimeExports.jsx("td", { "data-align": column.align, children: column.render(row) }, column.key)) }, rowKey(row))) })
  ] }) });
}
function MarketChart({ candles, label }) {
  const data = candles.map((item) => ({ time: item.openTime, open: Number(item.open), high: Number(item.high), low: Number(item.low), close: Number(item.close) })).filter((item) => [item.open, item.high, item.low, item.close].every(Number.isFinite)).slice(-80);
  if (data.length === 0) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-market-chart ui-market-chart--empty", children: "暂无 K 线数据" });
  const high = Math.max(...data.map((item) => item.high));
  const low = Math.min(...data.map((item) => item.low));
  const range = Math.max(high - low, 1);
  const y = (value) => 12 + (high - value) / range * 276;
  const step = 960 / data.length;
  const bodyWidth = Math.max(2, step * 0.62);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-market-chart", role: "img", "aria-label": label, children: /* @__PURE__ */ jsxRuntimeExports.jsx("svg", { viewBox: "0 0 960 300", preserveAspectRatio: "none", children: data.map((item, index) => {
    const x = index * step + step / 2;
    const rising = item.close >= item.open;
    const color = rising ? "var(--color-danger)" : "var(--color-success)";
    const bodyTop = y(Math.max(item.open, item.close));
    const bodyHeight = Math.max(2, Math.abs(y(item.open) - y(item.close)));
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("g", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("line", { x1: x, x2: x, y1: y(item.high), y2: y(item.low), stroke: color }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("rect", { x: x - bodyWidth / 2, y: bodyTop, width: bodyWidth, height: bodyHeight, fill: color })
    ] }, `${item.time}-${index}`);
  }) }) });
}
function Tag({ children, selected = false, disabled = false, onRemove }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ui-tag", "aria-disabled": disabled, "data-selected": selected || void 0, children: [
    children,
    onRemove !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "移除", size: "sm", disabled, onClick: onRemove, children: "×" }) : null
  ] });
}
function Alert({ tone = "info", title, children, action }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `ui-alert ui-alert--${tone}`, role: tone === "error" ? "alert" : "status", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      title !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: title }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children })
    ] }),
    action
  ] });
}
function Badge({ children, tone = "neutral" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `ui-badge ui-badge--${tone}`, children });
}
function Progress({ value, max = 100, label }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "ui-progress", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("progress", { value, max })
  ] });
}
function CircularProgress({ value, max = 100, label }) {
  const percent = value === void 0 ? void 0 : Math.max(0, Math.min(100, value / max * 100));
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-circular-progress", role: "progressbar", "aria-label": label, "aria-valuenow": value, "aria-valuemax": max, "data-indeterminate": value === void 0 || void 0, style: percent === void 0 ? void 0 : { "--ui-progress": `${percent * 3.6}deg` } });
}
function ErrorState({ title = "出现错误", message: message2, onRetry }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-state ui-state--error", role: "alert", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-state__icon", "aria-hidden": "true", children: "!" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: title }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: message2 }),
    onRetry !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: onRetry, children: "重试" }) : null
  ] });
}
class ErrorBoundary extends reactExports.Component {
  state = { error: null };
  static getDerivedStateFromError(error) {
    return { error };
  }
  componentDidCatch(error, info) {
    console.error("Renderer error boundary", { name: error.name, message: error.message, componentStack: info.componentStack });
  }
  render() {
    if (this.state.error !== null) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "page-shell page-shell--compact", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ErrorState,
        {
          title: "页面无法继续显示",
          message: this.state.error.message,
          onRetry: () => this.setState({ error: null })
        }
      ) });
    }
    return this.props.children;
  }
}
function Skeleton({ lines = 3 }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-skeleton", "aria-hidden": "true", children: Array.from({ length: Math.max(1, lines) }, (_, index) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}, index)) });
}
function OfflineState({ onRetry }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ErrorState, { title: "当前离线", message: "无法连接到所需服务，请检查连接后重试。", ...onRetry === void 0 ? {} : { onRetry } });
}
var reactDomExports = requireReactDom();
const PORTAL_ROOT_ID = "aimaid-ui-portal-root";
function getPortalRoot() {
  const existing = document.getElementById(PORTAL_ROOT_ID);
  if (existing !== null) return existing;
  const root2 = document.createElement("div");
  root2.id = PORTAL_ROOT_ID;
  root2.dataset.uiPortal = "true";
  document.body.append(root2);
  return root2;
}
const ToastContext = reactExports.createContext(null);
function ToastProvider({ children }) {
  const [items, setItems] = reactExports.useState([]);
  const show = reactExports.useCallback((message2, tone = "info") => {
    const id = crypto.randomUUID();
    setItems((current) => {
      const duplicate = current.find((item) => item.message === message2 && item.tone === tone);
      return duplicate === void 0 ? [...current, { id, message: message2, tone }].slice(-3) : current;
    });
    if (tone !== "error") setTimeout(() => setItems((current) => current.filter((item) => item.id !== id)), 3e3);
  }, []);
  const value = reactExports.useMemo(() => ({ show }), [show]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ToastContext.Provider, { value, children: [
    children,
    reactDomExports.createPortal(/* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-toast-region", "aria-live": "polite", "aria-relevant": "additions", children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `ui-toast ui-toast--${item.tone}`, role: item.tone === "error" ? "alert" : "status", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: item.message }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "关闭通知", size: "sm", onClick: () => setItems((current) => current.filter((entry) => entry.id !== item.id)), children: "×" })
    ] }, item.id)) }), getPortalRoot())
  ] });
}
function useToast() {
  const context = reactExports.useContext(ToastContext);
  if (context === null) throw new Error("useToast 必须在 ToastProvider 内使用");
  return context;
}
function UnauthorizedState({ onAuthorize }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ErrorState, { title: "需要授权", message: "当前操作需要明确授权后才能继续。", ...onAuthorize === void 0 ? {} : { onRetry: onAuthorize } });
}
function Checkbox({ label, description, id, ...props }) {
  const generatedId = reactExports.useId();
  const inputId = id ?? generatedId;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "ui-check", htmlFor: inputId, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { id: inputId, type: "checkbox", ...props }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-check__mark", "aria-hidden": "true", children: "✓" }),
    label !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: label }),
      description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: description }) : null
    ] }) : null
  ] });
}
function FilePicker({ label, maxBytes, onFiles, multiple, accept, ...props }) {
  const id = reactExports.useId();
  const ref = reactExports.useRef(null);
  const [error, setError] = reactExports.useState();
  const receive = (files) => {
    const oversized = maxBytes === void 0 ? void 0 : files.find((file) => file.size > maxBytes);
    if (oversized !== void 0) {
      setError(`${oversized.name} 超过大小限制。`);
      return;
    }
    setError(void 0);
    onFiles(files);
  };
  const drop = (event) => {
    event.preventDefault();
    receive(Array.from(event.dataTransfer.files));
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-file-picker", onDragOver: (event) => event.preventDefault(), onDrop: drop, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { ...props, ref, id, className: "ui-visually-hidden", type: "file", multiple, accept, onChange: (event) => receive(Array.from(event.target.files ?? [])) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "拖放文件到这里，或使用系统文件选择器。" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => ref.current?.click(), children: "选择文件" }),
    error !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "error", children: error }) : null
  ] });
}
function Combobox({ label, value = "", options, placeholder, loading = false, onChange }) {
  const [query, setQuery] = reactExports.useState("");
  const [open, setOpen] = reactExports.useState(false);
  const [active, setActive] = reactExports.useState(0);
  const filtered = reactExports.useMemo(() => options.filter((option) => option.label.toLowerCase().includes(query.toLowerCase())), [options, query]);
  const choose = (option) => {
    if (option.disabled) return;
    onChange(option.value);
    setQuery(option.label);
    setOpen(false);
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-combobox", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-field__label", children: label }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "ui-input", role: "combobox", "aria-expanded": open, "aria-controls": "combobox-options", "aria-autocomplete": "list", placeholder, value: query || options.find((item) => item.value === value)?.label || "", onFocus: () => setOpen(true), onChange: (event) => {
        setQuery(event.target.value);
        setOpen(true);
        setActive(0);
      }, onKeyDown: (event) => {
        if (event.key === "ArrowDown") {
          event.preventDefault();
          setActive((index) => Math.min(index + 1, filtered.length - 1));
        } else if (event.key === "ArrowUp") {
          event.preventDefault();
          setActive((index) => Math.max(0, index - 1));
        } else if (event.key === "Enter" && filtered[active] !== void 0) {
          event.preventDefault();
          choose(filtered[active]);
        } else if (event.key === "Escape") setOpen(false);
      } })
    ] }),
    open ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { id: "combobox-options", className: "ui-combobox__options", role: "listbox", "aria-label": label, children: loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "正在加载…" }) : filtered.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "无匹配结果" }) : filtered.map((option, index) => /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", role: "option", "aria-selected": option.value === value, "data-active": index === active || void 0, disabled: option.disabled, onMouseDown: (event) => event.preventDefault(), onClick: () => choose(option), children: option.label }, option.value)) }) : null
  ] });
}
function FormField$1({ label, children, required = false, description, error, htmlFor, orientation = "vertical" }) {
  const descriptionId = reactExports.useId();
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `ui-form-field ui-form-field--${orientation}`, "aria-describedby": description !== void 0 || error !== void 0 ? descriptionId : void 0, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "ui-form-field__label", htmlFor, children: [
      label,
      required ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", children: " *" }) : null
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-form-field__control", children }),
    description !== void 0 || error !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { id: descriptionId, className: error !== void 0 ? "ui-form-field__error" : "ui-form-field__description", children: error ?? description }) : null
  ] });
}
const Input = reactExports.forwardRef(function Input2({ label, error, hint, id, className = "", type, ...props }, ref) {
  const generatedId = reactExports.useId();
  const inputId = id ?? generatedId;
  const descriptionId = `${inputId}-description`;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `ui-field ${className}`.trim(), htmlFor: inputId, children: [
    label !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-field__label", children: label }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        ref,
        id: inputId,
        className: "ui-input",
        type,
        "aria-invalid": error !== void 0,
        "aria-describedby": error !== void 0 || hint !== void 0 ? descriptionId : void 0,
        ...props
      }
    ),
    error !== void 0 || hint !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { id: descriptionId, className: error !== void 0 ? "ui-field__error" : "ui-field__hint", children: error ?? hint }) : null
  ] });
});
const Range = reactExports.forwardRef(function Range2({ label, valueLabel, id, className = "", ...props }, ref) {
  const generatedId = reactExports.useId();
  const inputId = id ?? generatedId;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `ui-range ${className}`.trim(), htmlFor: inputId, children: [
    label !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ui-range__label", children: [
      label,
      valueLabel !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: valueLabel }) : null
    ] }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { ref, id: inputId, className: "ui-range__control", type: "range", ...props })
  ] });
});
function RadioGroup({ label, value, options, disabled = false, onChange }) {
  const name = reactExports.useId();
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("fieldset", { className: "ui-radio-group", disabled, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("legend", { children: label }),
    options.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "ui-radio", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "radio", name, value: option.value, checked: value === option.value, disabled: option.disabled, onChange: () => onChange(option.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-radio__mark" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: option.label }),
        option.description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: option.description }) : null
      ] })
    ] }, option.value))
  ] });
}
const RichTextEditor = reactExports.forwardRef(function RichTextEditor2({ value, label, disabled = false, onChange, onPasteFiles }, forwardedRef) {
  const ref = reactExports.useRef(null);
  reactExports.useImperativeHandle(forwardedRef, () => ({
    focus: () => ref.current?.focus(),
    insertHtml: (html) => {
      ref.current?.focus();
      document.execCommand("insertHTML", false, html);
      if (ref.current !== null) onChange(ref.current.innerHTML, ref.current.innerText);
    }
  }), [onChange]);
  reactExports.useEffect(() => {
    if (ref.current !== null && ref.current.innerHTML !== value) ref.current.innerHTML = value;
  }, [value]);
  const paste = (event) => {
    const files = Array.from(event.clipboardData.files);
    if (files.length > 0 && onPasteFiles !== void 0) {
      event.preventDefault();
      onPasteFiles(files);
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "div",
    {
      ref,
      className: "ui-rich-editor",
      role: "textbox",
      "aria-label": label,
      "aria-multiline": "true",
      "aria-disabled": disabled,
      contentEditable: !disabled,
      suppressContentEditableWarning: true,
      onInput: (event) => onChange(event.currentTarget.innerHTML, event.currentTarget.innerText),
      onPaste: paste
    }
  );
});
const Select = reactExports.forwardRef(function Select2({ label, error, options, id, ...props }, ref) {
  const generatedId = reactExports.useId();
  const inputId = id ?? generatedId;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "ui-field", htmlFor: inputId, children: [
    label !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-field__label", children: label }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("select", { ref, id: inputId, className: "ui-select", "aria-invalid": error !== void 0, ...props, children: options.map((option) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: option.value, disabled: option.disabled, children: option.label }, option.value)) }),
    error !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-field__error", children: error }) : null
  ] });
});
function SearchBox({ value, onChange, onSearch, loading = false, debounceMs = 300, ...props }) {
  const [draft, setDraft] = reactExports.useState(value);
  reactExports.useEffect(() => setDraft(value), [value]);
  reactExports.useEffect(() => {
    if (draft === value) return;
    const timer = window.setTimeout(() => onChange(draft), debounceMs);
    return () => window.clearTimeout(timer);
  }, [debounceMs, draft, onChange, value]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-search-box", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-search-box__icon", "aria-hidden": "true", children: "⌕" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        ...props,
        className: "ui-input",
        type: "search",
        value: draft,
        onChange: (event) => setDraft(event.target.value),
        onKeyDown: (event) => {
          if (event.key === "Enter") onSearch?.(draft);
          if (event.key === "Escape") {
            setDraft("");
            onChange("");
          }
          props.onKeyDown?.(event);
        }
      }
    ),
    loading ? /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { size: "sm", label: "搜索中" }) : draft.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "清空搜索", size: "sm", onClick: () => {
      setDraft("");
      onChange("");
    }, children: "×" }) : null
  ] });
}
function SegmentedControl({ label, value, options, onChange }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-segmented", role: "radiogroup", "aria-label": label, children: options.map(
    (option) => /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", role: "radio", "aria-checked": option.value === value, disabled: option.disabled, onClick: () => onChange(option.value), children: option.label }, option.value)
  ) });
}
function SearchInput(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { type: "search", ...props });
}
function NumberInput(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { type: "number", ...props });
}
function DateInput(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { type: "date", ...props });
}
function DateTimeInput(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { type: "datetime-local", ...props });
}
function Switch({ label, id, ...props }) {
  const generatedId = reactExports.useId();
  const inputId = id ?? generatedId;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "ui-switch", htmlFor: inputId, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { id: inputId, type: "checkbox", role: "switch", ...props }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-switch__track", "aria-hidden": "true", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label })
  ] });
}
const Textarea = reactExports.forwardRef(function Textarea2({ label, error, hint, id, className = "", ...props }, ref) {
  const generatedId = reactExports.useId();
  const inputId = id ?? generatedId;
  const descriptionId = `${inputId}-description`;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: `ui-field ${className}`.trim(), htmlFor: inputId, children: [
    label !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-field__label", children: label }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "textarea",
      {
        ref,
        id: inputId,
        className: "ui-textarea",
        "aria-invalid": error !== void 0,
        "aria-describedby": error !== void 0 || hint !== void 0 ? descriptionId : void 0,
        ...props
      }
    ),
    error !== void 0 || hint !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { id: descriptionId, className: error !== void 0 ? "ui-field__error" : "ui-field__hint", children: error ?? hint }) : null
  ] });
});
function PageToolbar({ lead, actions }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-page-toolbar", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-page-toolbar__lead", children: lead }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-page-toolbar__actions", children: actions })
  ] });
}
function Surface({ children, className = "", variant, scroll = false, ...props }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("section", { className: `ui-surface${scroll ? " ui-surface--scroll" : ""}${variant === void 0 ? "" : ` ${variant}`} ${className}`.trim(), ...props, children });
}
function SurfaceHeader({ title, meta }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "ui-surface__header", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: title }),
    meta !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: meta }) : null
  ] });
}
function ActionGroup({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-action-group", children });
}
function DetailList({ title, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-detail-list", children: [
    title !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: title }) : null,
    children
  ] });
}
function DetailRow({ label, value, wrap = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `ui-detail-row${wrap ? " ui-detail-row--wrap" : ""}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: value })
  ] });
}
function SettingsSection({ title, description, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-settings-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: title }),
      description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children })
  ] });
}
function SettingRow({ title, description, control }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-setting-row", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: title }),
      description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: control })
  ] });
}
function LayoutSlot({ as = "div", variant, children, ...props }) {
  const Component = as;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, { className: variant, ...props, children });
}
function Page({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-page", children });
}
function PageContent({ children, scroll = true }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: `ui-page-content${scroll ? " ui-page-content--scroll" : ""}`, children });
}
function Stack({ children, gap = "md", align = "stretch" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-stack ui-gap--${gap} ui-align--${align}`, children });
}
function Inline({ children, gap = "sm", align = "center", wrap = true, justify = "start" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-inline ui-gap--${gap} ui-align--${align} ui-justify--${justify}${wrap ? " ui-inline--wrap" : ""}`, children });
}
function Grid({ children, columns = "auto", gap = "md" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-grid ui-grid--${columns} ui-gap--${gap}`, children });
}
function Divider() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("hr", { className: "ui-divider" });
}
function Heading({ level = 2, children }) {
  if (level === 1) return /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "ui-heading ui-heading--1", children });
  if (level === 3) return /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "ui-heading ui-heading--3", children });
  if (level === 4) return /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "ui-heading ui-heading--4", children });
  return /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "ui-heading ui-heading--2", children });
}
function Text({ children, tone = "default", size = "md", as = "span", wrap = false }) {
  const className = `ui-text ui-text--${tone} ui-text--${size}${wrap ? " ui-text--wrap" : ""}`;
  return as === "p" ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className, children }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className, children });
}
function Strong({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { className: "ui-strong", children });
}
function KeyboardKey({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("kbd", { className: "ui-keyboard-key", children });
}
const MediaImage = reactExports.forwardRef(function MediaImage2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("img", { ref, className: "ui-media-image", ...props });
});
reactExports.forwardRef(function MediaCanvas2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("canvas", { ref, className: "ui-media-canvas", ...props });
});
const TransparentStage = reactExports.forwardRef(function TransparentStage2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { ref, className: "ui-transparent-stage", ...props });
});
const TransparentCanvas = reactExports.forwardRef(function TransparentCanvas2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("canvas", { ref, className: "ui-transparent-canvas", ...props });
});
const PetAudioContourCanvas = reactExports.forwardRef(function PetAudioContourCanvas2({ geometry = "content", ...props }, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("canvas", { ref, className: `ui-pet-audio-contour ui-pet-audio-contour--${geometry}`, ...props });
});
const PetPanelSurface = reactExports.forwardRef(function PetPanelSurface2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref, className: "ui-pet-panel", ...props });
});
const PetItemSurface = reactExports.forwardRef(function PetItemSurface2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref, className: "ui-pet-item", ...props });
});
function VisualRegion({ children, ratio = "auto", fit = "contain" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-visual-region ui-visual-region--${ratio} ui-visual-region--${fit}`, children });
}
function ScrollArea({ children, maxHeight = "md", ...props }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-scroll-area ui-scroll-area--${maxHeight}`, ...props, children });
}
const TrayMenuSurface = reactExports.forwardRef(function TrayMenuSurface2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { ref, className: "tray-menu-shell", ...props });
});
class BridgeCapabilityError extends Error {
  constructor(capability) {
    super(`当前窗口不具备 ${capability} 能力`);
    this.name = "BridgeCapabilityError";
  }
}
const bridge = Object.freeze({
  app: Object.freeze({
    get version() {
      return window.aimaid.appVersion;
    }
  }),
  window: Object.freeze({
    get kind() {
      return window.aimaid.windowKind;
    },
    open: (target) => requireCapability(window.aimaid.window.open, "window.open")(target),
    show: () => requireCapability(window.aimaid.window.show, "window.show")(),
    hide: () => requireCapability(window.aimaid.window.hide, "window.hide")(),
    close: () => requireCapability(window.aimaid.window.close, "window.close")(),
    quit: () => requireCapability(window.aimaid.window.quit, "window.quit")(),
    focus: () => requireCapability(window.aimaid.window.focus, "window.focus")(),
    minimize: () => requireCapability(window.aimaid.window.minimize, "window.minimize")(),
    toggleMaximize: () => requireCapability(window.aimaid.window.toggleMaximize, "window.toggleMaximize")()
  }),
  core: Object.freeze({
    invoke: (request, timeoutMs, requestId) => requireCapability(window.aimaid.core.invoke, "core.invoke")(request, timeoutMs, requestId),
    status: () => requireCapability(window.aimaid.core.status, "core.status")(),
    restart: () => requireCapability(window.aimaid.core.restart, "core.restart")(),
    cancel: (requestId) => requireCapability(window.aimaid.core.cancel, "core.cancel")(requestId)
  }),
  agentConfirmation: Object.freeze({
    get: () => requireCapability(window.aimaid.agentConfirmation?.get, "agent.confirmation.get")(),
    resolve: (requestId, approved) => requireCapability(window.aimaid.agentConfirmation?.resolve, "agent.confirmation.resolve")(requestId, approved)
  }),
  systemSettings: Object.freeze({
    get: () => requireCapability(window.aimaid.systemSettings?.get, "system.settings.get")(),
    setAutoStart: (enabled) => requireCapability(window.aimaid.systemSettings?.setAutoStart, "system.settings.setAutoStart")(enabled),
    setHotkey: (action, gesture) => requireCapability(window.aimaid.systemSettings?.setHotkey, "system.settings.setHotkey")(action, gesture),
    setBubbleStyle: (style) => requireCapability(window.aimaid.systemSettings?.setBubbleStyle, "system.settings.setBubbleStyle")(style)
  }),
  events: Object.freeze({
    subscribe: (types, listener) => requireCapability(window.aimaid.core.subscribe, "events.subscribe")(types, listener)
  }),
  pet: Object.freeze({
    ready: () => requireCapability(window.aimaid.pet?.ready, "pet.ready")(),
    getAssetManifest: (modelId) => requireCapability(window.aimaid.pet?.getAssetManifest, "pet.getAssetManifest")(modelId),
    setIgnoreMouseEvents: (ignore) => requireCapability(window.aimaid.pet?.setIgnoreMouseEvents, "pet.setIgnoreMouseEvents")(ignore),
    dragStart: () => requireCapability(window.aimaid.pet?.dragStart, "pet.dragStart")(),
    dragMove: () => requireCapability(window.aimaid.pet?.dragMove, "pet.dragMove")(),
    dragEnd: () => requireCapability(window.aimaid.pet?.dragEnd, "pet.dragEnd")(),
    updateWindow: (update) => requireCapability(window.aimaid.pet?.updateWindow, "pet.updateWindow")(update),
    reportMetrics: (metrics) => requireCapability(window.aimaid.pet?.reportMetrics, "pet.reportMetrics")(metrics),
    runtimeStatus: () => requireCapability(window.aimaid.pet?.runtimeStatus, "pet.runtime.get")(),
    publishLipSync: (frame) => requireCapability(window.aimaid.pet?.publishLipSync, "pet.publishLipSync")(frame),
    onLipSync: (listener) => requireCapability(window.aimaid.pet?.onLipSync, "pet.onLipSync")(listener),
    presentation: Object.freeze({
      get: () => requireCapability(window.aimaid.pet?.presentation.get, "pet.presentation.get")(),
      execute: (action) => requireCapability(window.aimaid.pet?.presentation.execute, "pet.presentation.execute")(action)
    }),
    onLifecycle: (listener) => requireCapability(window.aimaid.pet?.onLifecycle, "pet.onLifecycle")(listener)
  }),
  dialog: Object.freeze({
    openFile: (filters, multiSelect = false) => requireCapability(window.aimaid.dialog?.openFile, "dialog.openFile")(filters, multiSelect),
    openDirectory: () => requireCapability(window.aimaid.dialog?.openDirectory, "dialog.openDirectory")(),
    saveFile: (defaultPath, filters) => requireCapability(window.aimaid.dialog?.saveFile, "dialog.saveFile")(defaultPath, filters)
  }),
  shell: Object.freeze({
    showItemInFolder: (filePath) => requireCapability(window.aimaid.shell?.showItemInFolder, "shell.showItemInFolder")(filePath),
    openExternal: (url) => requireCapability(window.aimaid.shell?.openExternal, "shell.openExternal")(url)
  }),
  media: Object.freeze({
    registerLocalFile: (filePath) => requireCapability(window.aimaid.media?.registerLocalFile, "media.registerLocalFile")(filePath)
  }),
  notebook: Object.freeze({
    importFile: (filePath) => requireCapability(window.aimaid.notebook?.importFile, "notebook.importFile")(filePath),
    importData: (name, dataUrl) => requireCapability(window.aimaid.notebook?.importData, "notebook.importData")(name, dataUrl),
    imageAction: (action, path) => requireCapability(window.aimaid.notebook?.imageAction, "notebook.imageAction")(action, path)
  }),
  speech: Object.freeze({
    importAudioData: (dataUrl) => requireCapability(window.aimaid.speech?.importAudioData, "speech.audio.importData")(dataUrl)
  }),
  tray: Object.freeze({
    action: (action) => requireCapability(window.aimaid.tray?.action, "tray.action")(action),
    resize: (height) => requireCapability(window.aimaid.tray?.resize, "tray.resize")(height)
  }),
  douyin: Object.freeze({
    saveSession: () => requireCapability(window.aimaid.douyin?.saveSession, "douyin.session.save")(),
    inspectSession: () => requireCapability(window.aimaid.douyin?.inspectSession, "douyin.session.inspect")(),
    clearSession: () => requireCapability(window.aimaid.douyin?.clearSession, "douyin.session.clear")()
  })
});
function requireCapability(capability, name) {
  if (capability === void 0) throw new BridgeCapabilityError(name);
  return capability;
}
function WindowTitleBar({ title, subtitle, onBack, onClose, tools }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "ui-titlebar", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-titlebar__identity", children: [
      onBack !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "返回", size: "sm", onClick: onBack, children: "←" }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-titlebar__mark", "aria-hidden": "true", children: "A" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ui-titlebar__text", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: title }),
        subtitle !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: subtitle }) : null
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-titlebar__spacer" }),
    tools !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-titlebar__tools", children: tools }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-titlebar__controls", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "最小化", size: "sm", onClick: () => void bridge.window.minimize(), children: "—" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "最大化或恢复", size: "sm", onClick: () => void bridge.window.toggleMaximize(), children: "□" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "关闭", size: "sm", className: "ui-titlebar__close", onClick: () => void (onClose?.() ?? bridge.window.close()), children: "×" })
    ] })
  ] });
}
const Container = reactExports.forwardRef(function Container2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref, className: "ui-container", ...props });
});
const MainRegion = reactExports.forwardRef(function MainRegion2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { ref, className: "ui-main-region", ...props });
});
const Section = reactExports.forwardRef(function Section2({ variant, ...props }, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("section", { ref, className: "ui-section", "data-variant": variant, ...props });
});
const Article = reactExports.forwardRef(function Article2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("article", { ref, className: "ui-article", ...props });
});
reactExports.forwardRef(function Aside2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("aside", { ref, className: "ui-aside", ...props });
});
const Header = reactExports.forwardRef(function Header2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("header", { ref, className: "ui-header", ...props });
});
reactExports.forwardRef(function Footer2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("footer", { ref, className: "ui-footer", ...props });
});
reactExports.forwardRef(function Navigation2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("nav", { ref, className: "ui-navigation", ...props });
});
const InlineText = reactExports.forwardRef(function InlineText2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { ref, className: "ui-inline-text", ...props });
});
const SmallText = reactExports.forwardRef(function SmallText2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("small", { ref, className: "ui-small-text", ...props });
});
const Paragraph = reactExports.forwardRef(function Paragraph2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("p", { ref, className: "ui-paragraph", ...props });
});
reactExports.forwardRef(function Title12(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { ref, className: "ui-title ui-title--1", ...props });
});
const Title2 = reactExports.forwardRef(function Title22(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { ref, className: "ui-title ui-title--2", ...props });
});
const Title3 = reactExports.forwardRef(function Title32(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { ref, className: "ui-title ui-title--3", ...props });
});
const Title4 = reactExports.forwardRef(function Title42(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { ref, className: "ui-title ui-title--4", ...props });
});
const Emphasis = reactExports.forwardRef(function Emphasis2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("i", { ref, className: "ui-emphasis", ...props });
});
const DescriptionList = reactExports.forwardRef(function DescriptionList2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("dl", { ref, className: "ui-description-list", ...props });
});
const DescriptionTerm = reactExports.forwardRef(function DescriptionTerm2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("dt", { ref, className: "ui-description-term", ...props });
});
const DescriptionValue = reactExports.forwardRef(function DescriptionValue2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("dd", { ref, className: "ui-description-value", ...props });
});
const TimeValue = reactExports.forwardRef(function TimeValue2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("time", { ref, className: "ui-time", ...props });
});
const CodeBlock = reactExports.forwardRef(function CodeBlock2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { ref, className: "ui-code ui-code--block", ...props });
});
const FormLabel = reactExports.forwardRef(function FormLabel2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("label", { ref, className: "ui-form-label", ...props });
});
const Meter = reactExports.forwardRef(function Meter2(props, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("progress", { ref, className: "ui-meter", ...props });
});
function LineBreak() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("br", {});
}
function StatusDot({ state }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `status-dot status-dot--${state}`, "aria-hidden": "true" });
}
function StatusHero({ eyebrow, value, detail, actions, avatar, compact: compact2 = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `status-hero${compact2 ? " status-hero--compact" : ""}`, children: [
    avatar,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "status-hero__lead", children: [
      eyebrow !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "status-hero__eyebrow", children: eyebrow }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { className: "ui-strong", children: value }),
      detail !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { className: "ui-small-text", children: detail }) : null
    ] }),
    actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "status-hero__trailing", children: actions }) : null
  ] });
}
function StatusPanelGrid({ children, variant = "main" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `status-grid-3 status-grid-3--${variant}`, children });
}
function StatusList({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("dl", { className: "status-kv", children });
}
function StatusRow({ label, value, state, detail, meterPercent, variant }) {
  const hasMeter = meterPercent !== void 0;
  const classes = [
    "status-kv__row",
    hasMeter ? "status-kv__row--meter" : "",
    variant === "model" ? "status-kv__row--model" : ""
  ].filter(Boolean).join(" ");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: classes, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("dt", { children: [
      state !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { state }) : null,
      label
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("dd", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { className: "ui-strong", children: value }),
      detail !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { className: "ui-small-text", children: detail }) : null,
      hasMeter ? /* @__PURE__ */ jsxRuntimeExports.jsx(Meter, { value: Math.max(0, Math.min(100, meterPercent)), max: 100 }) : null
    ] })
  ] });
}
function ProductPage({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-page", children });
}
function ProductWorkspace({ children, layout = "single", ...props }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: `ui-product-workspace ui-product-workspace--${layout}`, ...props, children });
}
function ProductSidebar({ title, description, actions, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "ui-product-sidebar", children: [
    title !== void 0 || description !== void 0 || actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "ui-product-sidebar__header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        title !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: title }) : null,
        description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null
      ] }),
      actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-sidebar__actions", children: actions }) : null
    ] }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-sidebar__body", children })
  ] });
}
function ProductPanel({ title, description, actions, footer, children, scroll = false, emphasis = false, wide = false }) {
  const classes = ["ui-product-panel", scroll ? "ui-product-panel--scroll" : "", emphasis ? "ui-product-panel--emphasis" : "", wide ? "ui-product-panel--wide" : ""].filter(Boolean).join(" ");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: classes, children: [
    title !== void 0 || description !== void 0 || actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "ui-product-panel__header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        title !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: title }) : null,
        description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null
      ] }),
      actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-panel__actions", children: actions }) : null
    ] }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-panel__body", children }),
    footer !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("footer", { className: "ui-product-panel__footer", children: footer }) : null
  ] });
}
function ProductToolbar({ lead, actions, layout = "inline" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: `ui-product-toolbar ui-product-toolbar--${layout}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-toolbar__lead", children: lead }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-toolbar__actions", children: actions })
  ] });
}
function ProductTabNavigation({ tabs, actions, label }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("nav", { className: "remote-video-navigation", "aria-label": label, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "remote-video-tabs", role: "tablist", "aria-label": label, children: tabs }),
    actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "remote-video-navigation__actions", children: actions }) : null
  ] });
}
function ProductStatusBar({ children, actions }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("footer", { className: "ui-product-status", role: "status", "aria-live": "polite", "aria-atomic": "true", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children }),
    actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-status__actions", children: actions }) : null
  ] });
}
function ProductGrid({ children, density = "comfortable" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-product-grid ui-product-grid--${density}`, children });
}
function ProductHero({ eyebrow, value, detail, actions }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-product-hero", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-product-hero__content", children: [
      eyebrow !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: eyebrow }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: value }),
      detail !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: detail }) : null
    ] }),
    actions !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-product-hero__actions", children: actions }) : null
  ] });
}
function ProductMetric({ label, value, detail }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-product-metric", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: value }),
    detail !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: detail }) : null
  ] });
}
function ProductList({ children, compact: compact2 = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `ui-product-list${compact2 ? " ui-product-list--compact" : ""}`, children });
}
function ShowcasePage({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-showcase", children });
}
function ShowcaseContent({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "ui-showcase__content", children });
}
function ShowcaseIntro({ title, description, meta }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-showcase__intro", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "GLOBAL UI FOUNDATION" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { children: title }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description })
    ] }),
    meta !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("aside", { children: meta }) : null
  ] });
}
function ShowcaseSection({ title, description, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-showcase__section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("header", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: title }),
      description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-showcase__section-body", children })
  ] });
}
function ShowcaseRow({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-showcase__row", children });
}
function ShowcaseFormGrid({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-showcase__form-grid", children });
}
function ShowcaseStateGrid({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-showcase__state-grid", children });
}
function ShowcaseIconGrid({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-showcase__icon-grid", children });
}
function ShowcaseIcon({ icon, label }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-showcase__icon", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: icon }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: label })
  ] });
}
const AudioPlayer = reactExports.forwardRef(function AudioPlayer2({ source, controls = true, ...props }, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("audio", { ref, className: "ui-audio-player", src: source, controls, ...props });
});
const VideoPlayer = reactExports.forwardRef(function VideoPlayer2({ source, controls = true, ...props }, ref) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("video", { ref, className: "ui-video-player", src: source, controls, ...props });
});
const ExternalWebview = reactExports.forwardRef(function ExternalWebview2({ source, partition, label }, ref) {
  return reactExports.createElement("webview", { ref, src: source, partition, className: "ui-external-webview", "aria-label": label });
});
function TrayMusicPlayer({ title, singer, paused, onTogglePause, onStop }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "ui-tray-music-player", "aria-label": "音乐播放器", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-tray-music-player__copy", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "sm", wrap: true, children: title }),
      singer === "" ? null : /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "secondary", wrap: true, children: singer })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ui-tray-music-player__controls", role: "group", "aria-label": "播放控制", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        IconButton,
        {
          label: paused ? "继续播放" : "暂停",
          size: "sm",
          className: "ui-tray-music-player__control ui-tray-music-player__control--primary",
          onClick: onTogglePause,
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: paused ? "play" : "pause" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        IconButton,
        {
          label: "停止",
          size: "sm",
          className: "ui-tray-music-player__control",
          onClick: onStop,
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: "stop" })
        }
      )
    ] })
  ] });
}
function Tabs({ items, value, onChange, label }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-tabs", role: "tablist", "aria-label": label, children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(
    "button",
    {
      type: "button",
      role: "tab",
      className: "ui-tabs__tab",
      "aria-selected": value === item.id,
      disabled: item.disabled,
      onClick: () => onChange(item.id),
      children: item.label
    },
    item.id
  )) });
}
function Pagination({ page, pageSize, total, onPageChange, onPageSizeChange, pageSizeOptions = [10, 20, 50], loading = false }) {
  const pages = Math.max(1, Math.ceil(total / pageSize));
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("nav", { className: "ui-pagination", "aria-label": "分页", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
      "共 ",
      total,
      " 项"
    ] }),
    onPageSizeChange !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { "aria-label": "每页数量", value: String(pageSize), options: pageSizeOptions.map((value) => ({ value: String(value), label: `${value} / 页` })), onChange: (event) => onPageSizeChange(Number(event.target.value)) }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "上一页", size: "sm", disabled: loading || page <= 1, onClick: () => onPageChange(page - 1), children: "‹" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
      page,
      " / ",
      pages
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "下一页", size: "sm", disabled: loading || page >= pages, onClick: () => onPageChange(page + 1), children: "›" })
  ] });
}
function Tree({ label, nodes, selectedId, onSelect }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-tree", role: "tree", "aria-label": label, children: render(nodes, 1, selectedId, onSelect) });
}
function render(nodes, level, selectedId, onSelect) {
  return nodes.map((node) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { role: "treeitem", "aria-level": level, "aria-selected": node.id === selectedId, "aria-disabled": node.disabled, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { type: "button", disabled: node.disabled, onClick: () => onSelect(node.id), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { "aria-hidden": "true", children: node.children === void 0 ? "·" : "⌄" }),
      node.label
    ] }),
    node.children !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "group", children: render(node.children, level + 1, selectedId, onSelect) }) : null
  ] }, node.id));
}
function ListBox({ label, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-listbox", role: "listbox", "aria-label": label, children });
}
function ListBoxItem({ selected = false, disabled = false, leading, title, description, badge, onSelect }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { className: "ui-listbox__item", type: "button", role: "option", "aria-selected": selected, disabled, onClick: onSelect, children: [
    leading !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-listbox__leading", children: leading }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ui-listbox__content", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: title }),
      description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("small", { children: description }) : null
    ] }),
    badge !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-listbox__badge", children: badge }) : null
  ] });
}
function ContextMenu({ label, items, children }) {
  const [position, setPosition] = reactExports.useState(null);
  reactExports.useEffect(() => {
    if (position === null) return;
    const close = () => setPosition(null);
    document.addEventListener("pointerdown", close);
    document.addEventListener("scroll", close, true);
    return () => {
      document.removeEventListener("pointerdown", close);
      document.removeEventListener("scroll", close, true);
    };
  }, [position]);
  function open(event) {
    event.preventDefault();
    setPosition({ x: event.clientX, y: event.clientY });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { onContextMenu: open, children: [
    children,
    position !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-context-menu", role: "menu", "aria-label": label, style: { left: position.x, top: position.y }, children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        className: item.danger === true ? "ui-menu__item ui-menu__item--danger" : "ui-menu__item",
        type: "button",
        role: "menuitem",
        disabled: item.disabled,
        onClick: () => {
          item.onSelect();
          setPosition(null);
        },
        children: item.label
      },
      item.id
    )) }) : null
  ] });
}
function ContextMenuSurface({ label, items, position, footer, onClose }) {
  const menuRef = reactExports.useRef(null);
  const [style, setStyle] = reactExports.useState({ left: position.x, top: position.y, visibility: "hidden" });
  reactExports.useLayoutEffect(() => {
    const menu = menuRef.current;
    if (menu === null) return;
    const margin = 8;
    const rect = menu.getBoundingClientRect();
    const left = position.x + rect.width <= window.innerWidth - margin ? position.x : position.x - rect.width;
    const top = position.y + rect.height <= window.innerHeight - margin ? position.y : position.y - rect.height;
    setStyle({
      left: Math.max(margin, Math.min(left, window.innerWidth - rect.width - margin)),
      top: Math.max(margin, Math.min(top, window.innerHeight - rect.height - margin))
    });
  }, [items.length, position.x, position.y]);
  reactExports.useLayoutEffect(() => {
    const closeOnEscape = (event) => {
      if (event.key === "Escape") onClose();
    };
    window.addEventListener("keydown", closeOnEscape);
    return () => window.removeEventListener("keydown", closeOnEscape);
  }, [onClose]);
  return reactDomExports.createPortal(/* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-context-menu-layer", "data-pet-interactive": true, onPointerDown: onClose, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { ref: menuRef, className: "ui-context-menu ui-context-menu--rich", style, role: "menu", "aria-label": label, onPointerDown: (event) => event.stopPropagation(), children: [
    items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: item.separated === true ? "ui-menu__group-start" : void 0, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        className: `ui-menu__item${item.danger === true ? " ui-menu__item--danger" : ""}`,
        type: "button",
        role: "menuitem",
        disabled: item.disabled,
        onClick: () => {
          item.onSelect();
          onClose();
        },
        children: [
          item.icon !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-menu__icon", children: item.icon }) : null,
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-menu__label", children: item.label })
        ]
      }
    ) }, item.id)),
    footer !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("footer", { className: "ui-context-menu__footer", children: footer }) : null
  ] }) }), document.body);
}
function Dialog({ open, title, description, children, footer, onClose, size = "md", closeOnBackdrop = false }) {
  const ref = reactExports.useRef(null);
  const titleId = reactExports.useId();
  reactExports.useEffect(() => {
    const dialog = ref.current;
    if (dialog === null) return;
    if (open && !dialog.open) dialog.showModal();
    if (!open && dialog.open) dialog.close();
  }, [open]);
  if (!open) return null;
  return reactDomExports.createPortal(
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "dialog",
      {
        ref,
        className: `ui-dialog ui-dialog--${size}`,
        "aria-labelledby": titleId,
        onClick: (event) => {
          if (closeOnBackdrop && event.target === event.currentTarget) onClose();
        },
        onCancel: (event) => {
          event.preventDefault();
          onClose();
        },
        onClose,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "ui-overlay__header", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { id: titleId, children: title }),
              description !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: description }) : null
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "关闭对话框", size: "sm", onClick: onClose, children: "×" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-overlay__body", children }),
          footer !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("footer", { className: "ui-overlay__footer", children: footer }) : null
        ]
      }
    ),
    getPortalRoot()
  );
}
function ConfirmDialog$1({ open, title, description, confirmText = "确定", cancelText = "取消", confirmVariant = "primary", loading = false, onConfirm, onCancel }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open, size: "sm", title, description, onClose: onCancel, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: loading, onClick: onCancel, children: cancelText }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: confirmVariant, loading, onClick: onConfirm, children: confirmText })
  ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-visually-hidden", children: "请确认此操作" }) });
}
function Drawer({ open, title, children, side = "right", onClose }) {
  const dialogRef = reactExports.useRef(null);
  const titleId = reactExports.useId();
  reactExports.useEffect(() => {
    if (!open) return;
    const previouslyFocused = document.activeElement instanceof HTMLElement ? document.activeElement : null;
    const dialog = dialogRef.current;
    const focusable = () => dialog === null ? [] : Array.from(dialog.querySelectorAll('button:not([disabled]), input:not([disabled]), textarea:not([disabled]), select:not([disabled]), a[href], [tabindex]:not([tabindex="-1"])'));
    focusable()[0]?.focus();
    const onKeyDown = (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        onClose();
        return;
      }
      if (event.key !== "Tab") return;
      const candidates = focusable();
      if (candidates.length === 0) {
        event.preventDefault();
        dialog?.focus();
        return;
      }
      const first = candidates[0];
      const last = candidates[candidates.length - 1];
      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    };
    document.addEventListener("keydown", onKeyDown);
    return () => {
      document.removeEventListener("keydown", onKeyDown);
      previouslyFocused?.focus();
    };
  }, [open, onClose]);
  if (!open) return null;
  return reactDomExports.createPortal(
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-drawer-layer", role: "presentation", onMouseDown: (event) => {
      if (event.target === event.currentTarget) onClose();
    }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { ref: dialogRef, className: `ui-drawer ui-drawer--${side}`, role: "dialog", "aria-modal": "true", "aria-labelledby": titleId, tabIndex: -1, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "ui-overlay__header", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { id: titleId, children: title }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "关闭抽屉", size: "sm", onClick: onClose, children: "×" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ui-overlay__body", children })
    ] }) }),
    getPortalRoot()
  );
}
function Menu({ open, label, items, onClose, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ui-menu-anchor", children: [
    children,
    open ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-menu", role: "menu", "aria-label": label, children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        className: item.danger === true ? "ui-menu__item ui-menu__item--danger" : "ui-menu__item",
        type: "button",
        role: "menuitem",
        disabled: item.disabled,
        onClick: () => {
          item.onSelect();
          onClose();
        },
        children: item.label
      },
      item.id
    )) }) : null
  ] });
}
function Popover({ open, anchor, children, placement = "bottom-end" }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "ui-popover-anchor", children: [
    anchor,
    open ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: `ui-popover ui-popover--${placement}`, role: "dialog", children }) : null
  ] });
}
function Tooltip({ content: content2, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ui-tooltip", "data-tooltip": content2, children });
}
async function loadCharacters() {
  const [characters, settings] = await Promise.all([
    bridge.core.invoke({ type: "character.list", payload: {} }),
    bridge.core.invoke({ type: "settings.get", payload: { keys: ["voice_current_role_id"] } })
  ]);
  if (!characters.success) throw new Error(characters.error?.message ?? "语音角色读取失败。");
  if (!settings.success) throw new Error(settings.error?.message ?? "当前语音角色读取失败。");
  const payload = settings.payload;
  const current = payload?.settings?.find((item) => item.key === "voice_current_role_id")?.value ?? "";
  return { items: Array.isArray(characters.payload) ? characters.payload : [], currentRoleId: current };
}
async function setCurrentCharacter(roleId) {
  const response = await bridge.core.invoke({ type: "character.set_current", payload: { roleId } });
  if (!response.success) throw new Error(response.error?.message ?? "语音角色切换失败。");
}
async function deleteCharacter(roleId) {
  const response = await bridge.core.invoke({ type: "character.delete", payload: { roleId } });
  if (!response.success) throw new Error(response.error?.message ?? "语音角色删除失败。");
}
const PET_BUBBLE_STORAGE_KEY = "aimaid.pet-bubble";
const PET_BUBBLE_HOLD_STORAGE_KEY = "aimaid.bubble-hold";
const PRIORITIES = {
  error: 100,
  reminder: 90,
  speech: 80,
  conversation: 75,
  feedback: 60,
  processing: 40,
  status: 20
};
const PENDING_LIFETIMES_MS = {
  error: 3e4,
  reminder: 12e4,
  speech: 6e4,
  conversation: 6e4,
  feedback: 15e3,
  processing: 1e4,
  status: 8e3
};
function petBubblePriority(kind) {
  return PRIORITIES[kind];
}
function createPetBubbleMessage(payload) {
  return { ...payload, expiresAt: payload.createdAt + PENDING_LIFETIMES_MS[payload.kind] };
}
function canInterruptPetBubble(current, incoming, speechHeld) {
  if (incoming.kind === "error") return true;
  if (speechHeld) return incoming.kind === "speech";
  return petBubblePriority(incoming.kind) >= petBubblePriority(current.kind);
}
function selectNextPetBubble(pending, now) {
  const active = pending.filter((item) => item.expiresAt > now);
  if (active.length === 0) return { next: null, remaining: [] };
  const ordered = [...active].sort((left, right) => petBubblePriority(right.kind) - petBubblePriority(left.kind) || left.createdAt - right.createdAt);
  const [next, ...remaining] = ordered;
  return { next: next ?? null, remaining };
}
function isPetBubbleKind(value) {
  return typeof value === "string" && value in PRIORITIES;
}
function publishPetBubble(text, kind, actionTag) {
  const payload = {
    text,
    kind,
    nonce: crypto.randomUUID(),
    createdAt: Date.now(),
    ...actionTag === void 0 ? {} : { actionTag }
  };
  localStorage.setItem(PET_BUBBLE_STORAGE_KEY, JSON.stringify(payload));
  window.dispatchEvent(new CustomEvent("aimaid:pet-bubble", { detail: payload }));
}
const ANALYSIS_SCALE = Math.sqrt(20);
function computeLipSyncLevel(samples) {
  if (samples.length === 0) return 0;
  let sumOfSquares = 0;
  for (const sample of samples) sumOfSquares += sample * sample;
  const analyzed = Number((Math.sqrt(sumOfSquares / samples.length) * ANALYSIS_SCALE).toFixed(1));
  return analyzed > 0 ? Math.min(1, Math.max(0.4, analyzed * 1.2)) : 0;
}
function publishAudioLipSync(source, analyser) {
  const samples = new Float32Array(analyser.fftSize);
  let animationFrame = 0;
  let stopped = false;
  const sample = () => {
    if (stopped) return;
    analyser.getFloatTimeDomainData(samples);
    bridge.pet.publishLipSync({
      source,
      level: computeLipSyncLevel(samples),
      active: true,
      timestamp: Date.now()
    });
    animationFrame = requestAnimationFrame(sample);
  };
  sample();
  return () => {
    if (stopped) return;
    stopped = true;
    cancelAnimationFrame(animationFrame);
    bridge.pet.publishLipSync({ source, level: 0, active: false, timestamp: Date.now() });
  };
}
function paginatePetBubble(text, pageSize = 78) {
  const normalized = text.trim();
  if (normalized.length === 0) return [];
  const pages = [];
  for (let offset = 0; offset < normalized.length; offset += pageSize) pages.push(normalized.slice(offset, offset + pageSize));
  return pages;
}
let activeAudio = null;
let activeAudioContext = null;
let stopActiveLipSync = null;
let audioSubscription = null;
async function synthesizeAndPlayPages(text, voiceId, onPageStarted) {
  const pages = paginatePetBubble(text);
  if (pages.length === 0) return [];
  const paths = [];
  for (const page of pages) paths.push(await synthesizeSpeech(page, voiceId));
  for (const [index, path] of paths.entries()) {
    const audio = await playLocalAudio(path, () => onPageStarted(pages[index], index));
    if (audio === null) break;
    await waitForAudioEnd(audio);
  }
  return paths;
}
async function synthesizeSpeech(text, voiceId) {
  const response = await bridge.core.invoke({
    type: "tts.speak",
    payload: { text, ...voiceId === void 0 || voiceId.trim() === "" ? {} : { voiceId } }
  }, 12e4);
  if (!response.success || typeof response.payload !== "string" || response.payload.trim() === "")
    throw new Error(response.error?.message ?? "语音合成失败。");
  return response.payload;
}
async function playLocalAudio(filePath, onPlaybackStarted) {
  ensureAudioSubscription();
  const master = await loadMasterAudio$1();
  if (master.muted || master.volume <= 0) {
    stopAudioPlayback();
    return null;
  }
  const registered = await bridge.media.registerLocalFile(filePath);
  if (!registered.success || registered.payload?.url === void 0)
    throw new Error(registered.error?.message ?? "语音文件读取失败。");
  stopAudioPlayback();
  const audio = new Audio(registered.payload.url);
  audio.volume = master.volume / 100;
  const context = new AudioContext();
  const source = context.createMediaElementSource(audio);
  const analyser = context.createAnalyser();
  analyser.fftSize = 256;
  analyser.minDecibels = -90;
  analyser.maxDecibels = -10;
  analyser.smoothingTimeConstant = 0.85;
  source.connect(analyser);
  analyser.connect(context.destination);
  activeAudio = audio;
  activeAudioContext = context;
  audio.addEventListener("ended", () => {
    releaseAudio(audio);
  }, { once: true });
  audio.addEventListener("error", () => releaseAudio(audio), { once: true });
  try {
    await context.resume();
    await audio.play();
  } catch (error) {
    releaseAudio(audio);
    throw error;
  }
  if (activeAudio !== audio) return null;
  stopActiveLipSync = publishAudioLipSync("tts", analyser);
  reportPlayback(true);
  onPlaybackStarted?.();
  return audio;
}
async function playCachedAudio(filePath) {
  if (activeAudio !== null) return false;
  await playLocalAudio(filePath);
  return activeAudio !== null;
}
async function playLocalAudioPaths(paths, onPlaybackStarted) {
  let played = 0;
  for (const [index, path] of paths.entries()) {
    try {
      const audio = await playLocalAudio(path, () => onPlaybackStarted?.(index));
      if (audio === null) continue;
      played += 1;
      await waitForAudioEnd(audio);
    } catch {
    }
  }
  return played;
}
function waitForAudioEnd(audio) {
  return new Promise((resolve, reject) => {
    audio.addEventListener("ended", () => resolve(), { once: true });
    audio.addEventListener("error", () => reject(new Error("语音文件播放失败。")), { once: true });
  });
}
function stopAudioPlayback() {
  if (activeAudio !== null) {
    const audio = activeAudio;
    audio.pause();
    releaseAudio(audio);
  }
}
function releaseAudio(audio) {
  if (activeAudio !== audio) return;
  activeAudio = null;
  stopActiveLipSync?.();
  stopActiveLipSync = null;
  const context = activeAudioContext;
  activeAudioContext = null;
  void context?.close().catch((error) => console.error("[TTSPlayback] audio context close failed", error));
  reportPlayback(false);
}
function reportPlayback(playing) {
  reportBubbleHold(playing);
  void bridge.core.invoke({ type: "tts.playback.set", payload: { playing } });
}
function reportBubbleHold(held) {
  const detail = { held, nonce: crypto.randomUUID() };
  localStorage.setItem("aimaid.bubble-hold", JSON.stringify(detail));
  window.dispatchEvent(new CustomEvent("aimaid:bubble-hold", { detail }));
}
function ensureAudioSubscription() {
  if (audioSubscription !== null) return;
  audioSubscription = bridge.events.subscribe(["settings.changed"], (event) => {
    const payload = isRecord$3(event.payload) ? event.payload.data : null;
    if (!isRecord$3(payload) || !Array.isArray(payload.keys) || !payload.keys.some((key) => key === "master_audio_muted" || key === "master_audio_volume")) return;
    void loadMasterAudio$1().then((master) => {
      if (master.muted || master.volume <= 0) stopAudioPlayback();
      else if (activeAudio !== null) activeAudio.volume = master.volume / 100;
    }).catch(() => stopAudioPlayback());
  });
}
async function loadMasterAudio$1() {
  const response = await bridge.core.invoke({ type: "settings.get", payload: { keys: ["master_audio_muted", "master_audio_volume"] } });
  if (!response.success) throw new Error(response.error?.message ?? "主音量设置读取失败。");
  const payload = response.payload;
  const settings = new Map(payload?.settings?.map((item) => [item.key, item.value]));
  const parsed = Number.parseInt(settings.get("master_audio_volume") ?? "100", 10);
  const volume = Number.isFinite(parsed) ? Math.min(100, Math.max(0, parsed)) : 100;
  return { muted: settings.get("master_audio_muted")?.toLowerCase() === "true" || volume <= 0, volume };
}
function isRecord$3(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
async function attachAudioMetadata(messageId, audioPaths, metadata = {}) {
  if (!Number.isSafeInteger(messageId) || messageId <= 0 || audioPaths.length === 0) return;
  const response = await bridge.core.invoke({
    type: "chat.update_metadata",
    payload: { messageId, metadataJson: JSON.stringify({ ...metadata, audioPaths }) }
  });
  if (!response.success) throw new Error(response.error?.message ?? "语音记录保存失败。");
}
async function runAgentConversation$1(content2, options, invokeCore) {
  const maxSteps = 4;
  let conversationId = options.conversationId;
  let toolResultJson;
  let suppressSpeech = false;
  for (let step = 1; step <= maxSteps; step += 1) {
    const response = await invokeCore({ type: "agent.decide", payload: {
      content: content2,
      ...conversationId === void 0 ? {} : { conversationId },
      ...options.characterId === void 0 ? {} : { characterId: options.characterId },
      saveUserMessage: step === 1,
      ...toolResultJson === void 0 ? {} : { toolResultJson },
      toolStep: step,
      maxSteps,
      source: options.source,
      continueConversation: options.continueConversation ?? false
    } }, 12e4);
    if (!response.success) throw new Error(response.error?.message ?? "Agent 决策失败。");
    const decision = response.payload;
    if (decision === null || decision.conversationId === "") throw new Error("Agent 返回了无效决策。");
    conversationId = decision.conversationId;
    if (["final_response", "final_answer", "ask_user", "ask_clarify", "reject"].includes(decision.type)) {
      return {
        conversationId,
        messageId: decision.messageId,
        content: decision.message,
        voiceStyle: decision.voiceStyle || "normal",
        suppressSpeech
      };
    }
    let capability = decision.capability;
    let argsJson = decision.argsJson || "{}";
    if (decision.type === "reminder_create") {
      capability = "reminder.create";
      argsJson = JSON.stringify({ timeText: decision.timeText, content: decision.content || content2, repeat: decision.repeat || "none" });
    } else if (decision.type !== "tool_call") {
      throw new Error(`Agent 返回了不支持的决策类型：${decision.type}`);
    }
    if (capability === "") throw new Error("Agent 没有返回要执行的能力。");
    const tool = await invokeCore({ type: "agent.execute", payload: { conversationId, capabilityName: capability, argsJson } }, 12e4);
    if (!tool.success) throw new Error(tool.error?.message ?? `Agent 能力执行失败：${capability}`);
    const toolPayload = tool.payload;
    suppressSpeech ||= toolPayload?.resultPolicy === "silent";
    toolResultJson = JSON.stringify({ type: "tool_result", capabilityName: capability, result: tool.payload });
  }
  throw new Error("Agent 已达到最大执行步数。");
}
async function runAgentConversation(content2, options) {
  return runAgentConversation$1(content2, options, bridge.core.invoke);
}
function PromptPage() {
  const input = reactExports.useRef(null);
  const recorder = reactExports.useRef(void 0);
  const recordingStream = reactExports.useRef(void 0);
  const chunks = reactExports.useRef([]);
  const discardRecording = reactExports.useRef(false);
  const [text, setText] = reactExports.useState("");
  const [recording, setRecording] = reactExports.useState(false);
  const [transcribing, setTranscribing] = reactExports.useState(false);
  reactExports.useEffect(() => {
    document.body.classList.add("prompt-surface");
    const focus = () => {
      setText("");
      requestAnimationFrame(() => input.current?.focus());
    };
    window.addEventListener("focus", focus);
    focus();
    return () => {
      discardRecording.current = true;
      if (recorder.current?.state === "recording")
        recorder.current.stop();
      recordingStream.current?.getTracks().forEach((track) => track.stop());
      document.body.classList.remove("prompt-surface");
      window.removeEventListener("focus", focus);
    };
  }, []);
  async function transcribe(audio) {
    setTranscribing(true);
    publishPetBubble("正在识别语音…", "processing", "think");
    try {
      const imported = await bridge.speech.importAudioData(await blobToDataUrl$1(audio));
      if (!imported.success || imported.payload === null)
        throw new Error(imported.error?.message ?? "录音保存失败。");
      const response = await bridge.core.invoke({ type: "asr.transcribe", payload: {
        audioPath: imported.payload.path,
        language: "zh",
        requestId: `aimaid_${crypto.randomUUID().replaceAll("-", "")}`
      } }, 12e4);
      if (!response.success || typeof response.payload !== "string")
        throw new Error(response.error?.message ?? "语音识别失败。");
      const recognized = response.payload.trim();
      if (recognized === "")
        throw new Error("语音识别没有返回文字。");
      setText(recognized);
      requestAnimationFrame(() => {
        input.current?.focus();
        input.current?.setSelectionRange(recognized.length, recognized.length);
      });
      publishPetBubble("语音已转成文字，请确认后发送。", "feedback");
    } catch (reason) {
      publishPetBubble(reason instanceof Error ? reason.message : String(reason), "error", "error");
    } finally {
      setTranscribing(false);
    }
  }
  async function startRecording() {
    if (recording || transcribing)
      return;
    if (navigator.mediaDevices?.getUserMedia === void 0 || typeof MediaRecorder === "undefined") {
      publishPetBubble("当前系统不支持麦克风录音。", "error", "error");
      return;
    }
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true }, video: false });
      const mimeType = MediaRecorder.isTypeSupported("audio/webm;codecs=opus") ? "audio/webm;codecs=opus" : "audio/webm";
      const next = new MediaRecorder(stream, { mimeType });
      chunks.current = [];
      discardRecording.current = false;
      recordingStream.current = stream;
      recorder.current = next;
      next.ondataavailable = (event) => {
        if (event.data.size > 0)
          chunks.current.push(event.data);
      };
      next.onerror = () => publishPetBubble("麦克风录音失败，请检查系统权限。", "error", "error");
      next.onstop = () => {
        stream.getTracks().forEach((track) => track.stop());
        recordingStream.current = void 0;
        recorder.current = void 0;
        setRecording(false);
        if (discardRecording.current) {
          chunks.current = [];
          return;
        }
        const audio = new Blob(chunks.current, { type: next.mimeType });
        chunks.current = [];
        if (audio.size === 0) {
          publishPetBubble("没有录到声音，请重试。", "error", "error");
          return;
        }
        void transcribe(audio);
      };
      next.start(500);
      setRecording(true);
      publishPetBubble("正在听主人说话…", "status", "listen");
    } catch (reason) {
      recordingStream.current?.getTracks().forEach((track) => track.stop());
      publishPetBubble(reason instanceof Error ? reason.message : "无法访问麦克风，请检查系统权限。", "error", "error");
    }
  }
  function stopRecording(discard = false) {
    discardRecording.current = discard;
    if (recorder.current?.state === "recording")
      recorder.current.stop();
  }
  async function submit(continueConversation, ttsPreviewOnly, promptText = text) {
    const prompt = promptText.trim();
    if (prompt === "")
      return;
    await bridge.window.hide();
    if (ttsPreviewOnly) {
      publishPetBubble("正在试听语音.....^_^", "processing");
      try {
        const voiceId = await currentVoiceId();
        await synthesizeAndPlayPages(prompt, voiceId, (page) => publishPetBubble(page, "speech", "speak"));
      } catch (reason) {
        publishPetBubble(reason instanceof Error ? reason.message : String(reason), "error", "error");
      }
      return;
    }
    try {
      if (prompt.startsWith("-")) {
        publishPetBubble(await runChatCommand(prompt), "feedback");
        return;
      }
      const reminder = parseReminder(prompt);
      if (reminder.handled) {
        if (reminder.value === null) {
          publishPetBubble("我没听清提醒时间。可以说：10分钟后提醒我喝水，或 明天 23:30 提醒我休息。", "feedback");
          return;
        }
        const response = await bridge.core.invoke({ type: "reminder.save", payload: {
          reminderId: null,
          title: reminder.value.title,
          message: `提醒：${reminder.value.title}`,
          dueAt: reminder.value.dueAt.toISOString(),
          repeat: reminder.value.repeat,
          enabled: true,
          allowTts: true
        } });
        if (!response.success)
          throw new Error(response.error?.message ?? "提醒创建失败。");
        publishPetBubble(`已创建提醒：${reminder.value.title}
时间：${formatMinute(reminder.value.dueAt)}`, "feedback");
        return;
      }
      publishPetBubble("女仆正在跑腿通知.....^_^", "processing", "think");
      const character = await currentCharacter();
      const payload = await runAgentConversation(prompt, {
        ...character === void 0 ? {} : { characterId: character.roleId },
        continueConversation,
        source: "normal_chat"
      });
      const content2 = payload.content.trim() || "Agent 返回了空回复。";
      if (!payload.suppressSpeech && payload.messageId > 0 && await realtimeTtsEnabled()) {
        const voiceId = character?.preferredVoiceId || void 0;
        const audioPaths = await synthesizeAndPlayPages(
          content2,
          voiceId,
          (page) => publishPetBubble(page, "speech", actionTagForVoiceStyle$1(payload.voiceStyle))
        );
        await attachAudioMetadata(payload.messageId, audioPaths, { voiceId: voiceId ?? "", source: "prompt" });
      } else {
        publishPetBubble(content2, "feedback", actionTagForVoiceStyle$1(payload.voiceStyle));
      }
    } catch (reason) {
      publishPetBubble(reason instanceof Error ? reason.message : String(reason), "error", "error");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(MainRegion, { onMouseDown: (event) => {
    if (event.target === event.currentTarget)
      void bridge.window.hide();
  }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Section, { variant: "prompt", "aria-label": "快捷输入", onMouseDown: (event) => event.stopPropagation(), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { wrap: false, align: "center", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { ref: input, "aria-label": "快捷输入内容", value: text, rows: 2, onChange: (event) => setText(event.target.value), onKeyDownCapture: (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        stopRecording(true);
        void bridge.window.hide();
        return;
      }
      if (event.key !== "Enter")
        return;
      event.preventDefault();
      event.stopPropagation();
      const ttsPreviewOnly = event.shiftKey || event.getModifierState("Shift");
      void submit(!ttsPreviewOnly && event.ctrlKey, ttsPreviewOnly);
    } }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      IconButton,
      {
        variant: "promptVoice",
        size: "sm",
        label: recording ? "结束录音" : transcribing ? "正在识别语音" : "语音输入",
        tooltip: recording ? "结束录音并转文字" : "录音转文字",
        loading: transcribing,
        "aria-pressed": recording,
        onClick: () => recording ? stopRecording() : void startRecording(),
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: recording ? "stop" : "microphone" })
      }
    )
  ] }) }) });
}
async function currentVoiceId() {
  return (await currentCharacter())?.preferredVoiceId || void 0;
}
async function currentCharacter() {
  const characters = await loadCharacters();
  return characters.items.find((item) => item.roleId === characters.currentRoleId) ?? characters.items.find((item) => item.isEnabled) ?? characters.items[0];
}
async function realtimeTtsEnabled() {
  const response = await bridge.core.invoke({ type: "settings.get", payload: { keys: ["realtime_tts_enabled"] } });
  if (!response.success)
    throw new Error(response.error?.message ?? "实时 TTS 设置读取失败。");
  const payload = response.payload;
  const value = payload?.settings?.find((item) => item.key === "realtime_tts_enabled")?.value;
  return value === void 0 || value.toLowerCase() !== "false";
}
async function runChatCommand(commandText) {
  const list = await bridge.core.invoke({ type: "script.list", payload: {} });
  if (!list.success)
    throw new Error(list.error?.message ?? "快捷脚本读取失败。");
  const items = Array.isArray(list.payload) ? list.payload : [];
  const launcher = items.find((item) => item.enabled && item.commandText === commandText);
  if (launcher === void 0)
    return `没有配置聊天命令：${commandText}`;
  const response = await bridge.core.invoke({ type: "script.run", payload: { launcherId: launcher.launcherId } });
  return response.success && typeof response.payload === "string" ? response.payload : response.error?.message ?? `启动失败：${launcher.displayName}`;
}
function parseReminder(input) {
  if (!["提醒", "闹钟", "定时", "叫我", "喊我", "记得"].some((word) => input.toLocaleLowerCase().includes(word)))
    return { handled: false, value: null };
  const now = /* @__PURE__ */ new Date();
  const minute = input.match(/(\d+)\s*(分钟|分)/);
  if (minute?.[1] !== void 0)
    return reminderValue(input, new Date(now.getTime() + Math.max(1, Number(minute[1])) * 6e4), "none");
  const hour = input.match(/(\d+)\s*(小时|钟头)/);
  if (hour?.[1] !== void 0)
    return reminderValue(input, new Date(now.getTime() + Math.max(1, Number(hour[1])) * 36e5), "none");
  const daily = input.match(/每天\s*(\d{1,2})(?:[:：点]\s*(\d{1,2}))?/);
  if (daily?.[1] !== void 0) {
    const dueAt = atTime(now, Number(daily[1]), Number(daily[2] ?? 0));
    if (dueAt <= now)
      dueAt.setDate(dueAt.getDate() + 1);
    return reminderValue(input, dueAt, "daily");
  }
  const tomorrow = input.match(/明天\s*(\d{1,2})(?:[:：点]\s*(\d{1,2}))?/);
  if (tomorrow?.[1] !== void 0) {
    const dueAt = atTime(now, Number(tomorrow[1]), Number(tomorrow[2] ?? 0));
    dueAt.setDate(dueAt.getDate() + 1);
    return reminderValue(input, dueAt, "none");
  }
  return { handled: true, value: null };
}
function reminderValue(input, dueAt, repeat) {
  let title = input.replace(/\d+\s*(分钟|分|小时|钟头)/gi, "").replace(/每天\s*\d{1,2}(?:[:：点]\s*\d{0,2})?/gi, "").replace(/明天\s*\d{1,2}(?:[:：点]\s*\d{0,2})?/gi, "");
  for (const word of ["提醒我", "提醒", "闹钟", "定时", "叫我", "喊我", "记得"])
    title = title.replaceAll(word, "");
  title = title.replace(/^[ ，,。.：:]+|[ ，,。.：:]+$/g, "") || "该处理提醒事项了";
  return { handled: true, value: { dueAt, repeat, title } };
}
function atTime(now, hour, minute) {
  return new Date(now.getFullYear(), now.getMonth(), now.getDate(), Math.min(23, Math.max(0, hour)), Math.min(59, Math.max(0, minute)), 0);
}
function formatMinute(value) {
  const pad2 = (number) => String(number).padStart(2, "0");
  return `${value.getFullYear()}-${pad2(value.getMonth() + 1)}-${pad2(value.getDate())} ${pad2(value.getHours())}:${pad2(value.getMinutes())}`;
}
function actionTagForVoiceStyle$1(voiceStyle) {
  const normalized = voiceStyle.trim().toLowerCase();
  if (normalized === "lively")
    return "happy";
  if (normalized === "close")
    return "shy";
  if (normalized === "soft")
    return "smile";
  return "speak";
}
function blobToDataUrl$1(blob) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => typeof reader.result === "string" ? resolve(reader.result) : reject(new Error("录音读取失败。"));
    reader.onerror = () => reject(reader.error ?? new Error("录音读取失败。"));
    reader.readAsDataURL(blob);
  });
}
function CharacterPage() {
  const toast = useToast();
  const [items, setItems] = reactExports.useState(null);
  const [current, setCurrent] = reactExports.useState("");
  const [selected, setSelected] = reactExports.useState("");
  const [query, setQuery] = reactExports.useState("");
  const [error, setError] = reactExports.useState(null);
  const [busy, setBusy] = reactExports.useState(false);
  const [deleteConfirm, setDeleteConfirm] = reactExports.useState(false);
  const [avatarUrls, setAvatarUrls] = reactExports.useState({});
  const [roleVoices, setRoleVoices] = reactExports.useState([]);
  const [currentObjectKey, setCurrentObjectKey] = reactExports.useState("");
  const [boundRoleId, setBoundRoleId] = reactExports.useState("");
  const filtered = reactExports.useMemo(() => {
    const keyword = query.trim().toLocaleLowerCase();
    if (items === null || keyword === "") return items ?? [];
    return items.filter((item) => [item.name, item.roleId, item.voiceName, item.preferredVoiceId].some((value) => value.toLocaleLowerCase().includes(keyword)));
  }, [items, query]);
  const selectedItem = items?.find((item) => item.roleId === selected) ?? null;
  async function load() {
    try {
      const data = await loadCharacters();
      setItems(data.items);
      setCurrent(data.currentRoleId);
      setSelected((value) => data.items.some((item) => item.roleId === value) ? value : data.items.find((item) => item.roleId === data.currentRoleId)?.roleId ?? data.items[0]?.roleId ?? "");
      setError(null);
      const presentation = await bridge.pet.presentation.get();
      const snapshot = presentation.success ? presentation.payload : null;
      const objectKey = snapshot?.currentImage === null || snapshot?.currentImage === void 0 ? "" : `${snapshot.imageFolder.replace(/[\\/]+$/u, "")}\\${snapshot.currentImage.name}`;
      setCurrentObjectKey(objectKey);
      if (objectKey === "") setBoundRoleId("");
      else {
        const binding = await bridge.core.invoke({ type: "character.binding.get", payload: { targetKey: objectKey } });
        setBoundRoleId(binding.success && binding.payload !== null ? binding.payload.roleId : "");
      }
    } catch (reason) {
      setError(reason instanceof Error ? reason.message : String(reason));
    }
  }
  reactExports.useEffect(() => {
    void load();
  }, []);
  reactExports.useEffect(() => {
    if (items === null) return;
    void Promise.all(items.filter((item) => item.avatarPath !== "").map(async (item) => {
      const response = await bridge.media.registerLocalFile(item.avatarPath);
      return [item.roleId, response.success ? response.payload?.url ?? "" : ""];
    })).then((entries) => setAvatarUrls(Object.fromEntries(entries)));
  }, [items]);
  reactExports.useEffect(() => {
    if (selected === "") {
      setRoleVoices([]);
      return;
    }
    setRoleVoices([]);
    void bridge.core.invoke({ type: "character.voices", payload: { roleId: selected } }).then((response) => {
      if (!response.success || !Array.isArray(response.payload)) throw new Error(response.error?.message ?? "角色语音列表读取失败。");
      setRoleVoices(response.payload);
    }).catch((reason) => toast.show(reason instanceof Error ? reason.message : String(reason), "error"));
  }, [selected, toast]);
  reactExports.useEffect(() => {
    const reload = () => {
      void load();
    };
    window.addEventListener("focus", reload);
    return () => window.removeEventListener("focus", reload);
  }, []);
  async function choose() {
    if (selectedItem === null || selectedItem.roleId === current) return;
    try {
      setBusy(true);
      await setCurrentCharacter(selectedItem.roleId);
      setCurrent(selectedItem.roleId);
      toast.show(`已切换语音角色：${selectedItem.name}`, "success");
    } catch (reason) {
      toast.show(reason instanceof Error ? reason.message : String(reason), "error");
    } finally {
      setBusy(false);
    }
  }
  async function removeSelected() {
    if (selectedItem === null) return;
    try {
      setBusy(true);
      await deleteCharacter(selectedItem.roleId);
      setDeleteConfirm(false);
      await load();
      toast.show(`已删除语音角色：${selectedItem.name}`, "success");
    } catch (reason) {
      toast.show(reason instanceof Error ? reason.message : String(reason), "error");
    } finally {
      setBusy(false);
    }
  }
  async function bindSelected() {
    if (selectedItem === null) return;
    if (currentObjectKey === "") {
      toast.show("当前没有可绑定的对象。", "error");
      return;
    }
    setBusy(true);
    const response = await bridge.core.invoke({ type: "character.binding.set", payload: { targetKey: currentObjectKey, roleId: selectedItem.roleId } });
    setBusy(false);
    if (!response.success) {
      toast.show(response.error?.message ?? "角色绑定失败。", "error");
      return;
    }
    setBoundRoleId(selectedItem.roleId);
    toast.show(`已绑定语音角色：${selectedItem.name}`, "success");
  }
  async function unbindCurrent() {
    if (currentObjectKey === "") {
      toast.show("当前没有可绑定的对象。", "error");
      return;
    }
    setBusy(true);
    const response = await bridge.core.invoke({ type: "character.binding.clear", payload: { targetKey: currentObjectKey } });
    setBusy(false);
    if (!response.success) {
      toast.show(response.error?.message ?? "角色解绑失败。", "error");
      return;
    }
    setBoundRoleId("");
    toast.show("已解除当前对象的语音角色绑定。", "success");
  }
  async function openCurrentCard() {
    if (selectedItem === null) return;
    localStorage.setItem("aimaid.template-card-role", JSON.stringify(selectedItem));
    const response = await bridge.window.open("template-card");
    if (!response.success) toast.show(response.error?.message ?? "角色卡窗口打开失败。", "error");
  }
  async function openEditor(item) {
    if (item === null) localStorage.removeItem("aimaid.character-editor-role");
    else localStorage.setItem("aimaid.character-editor-role", JSON.stringify(item));
    const response = await bridge.window.open("character-editor");
    if (!response.success) toast.show(response.error?.message ?? "角色编辑窗口打开失败。", "error");
  }
  if (items === null && error === null) return /* @__PURE__ */ jsxRuntimeExports.jsx(Page, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { label: "正在加载语音角色…" }) }) });
  if (items === null) return /* @__PURE__ */ jsxRuntimeExports.jsx(ErrorState, { title: "语音角色加载失败", message: error ?? "", onRetry: () => void load() });
  const currentName = items.find((item) => item.roleId === current)?.name ?? "未选择";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "语音角色管理" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { scroll: false, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "character-page-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        PageToolbar,
        {
          lead: /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { tone: "secondary", children: "当前角色" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: currentName })
          ] }),
          actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(SearchInput, { "aria-label": "搜索角色", value: query, onChange: (event) => setQuery(event.target.value) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void openEditor(null), children: "新增" })
          ] })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "character-workspace", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Surface, { variant: "character-navigation", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(SurfaceHeader, { title: "角色列表", meta: `${filtered.length}/${items.length}` }),
          filtered.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: items.length === 0 ? "还没有角色" : "没有匹配的角色" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ListBox, { label: "角色列表", children: filtered.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            ListBoxItem,
            {
              selected: item.roleId === selected,
              leading: /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { source: avatarUrls[item.roleId] ?? "", fallback: item.name || item.roleId }),
              title: item.name || item.roleId,
              description: item.preferredVoiceId || item.voiceName || item.roleId,
              badge: item.roleId === current ? /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: "当前" }) : item.roleId === boundRoleId ? /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: "已绑定" }) : void 0,
              onSelect: () => setSelected(item.roleId)
            },
            item.roleId
          )) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Surface, { variant: "character-detail", children: selectedItem === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "请选择角色" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "header", variant: "character-summary", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(VisualRegion, { ratio: "square", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { source: avatarUrls[selectedItem.roleId] ?? "", fallback: selectedItem.name || "—", size: "preview" }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "xs", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Heading, { children: selectedItem.name || selectedItem.roleId }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { tone: "muted", children: [
                "角色 ID：",
                selectedItem.roleId
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: selectedItem.roleId === current ? "accent" : "neutral", children: selectedItem.roleId === current ? "当前角色" : "可切换" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: selectedItem.roleId === boundRoleId ? "已绑定当前对象" : "未绑定当前对象" })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(ActionGroup, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: busy, disabled: selectedItem.roleId === current || !selectedItem.isEnabled, onClick: () => void choose(), children: "设为当前" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void openEditor(selectedItem), children: "编辑" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void openCurrentCard(), children: "角色卡" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "character-detail-sections", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(DetailList, { title: "角色资料", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "角色名称", value: selectedItem?.name || "-" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "头像文件", value: fileName(selectedItem?.avatarPath) || "未配置", wrap: true }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "启用状态", value: selectedItem.isEnabled ? "已启用" : "已停用" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(DetailList, { title: "音色", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "默认音色", value: selectedItem.preferredVoiceId || selectedItem.voiceName || "-" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "可用音色", value: `${roleVoices.length} 个` })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(DetailList, { title: "角色卡状态", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "原角色卡", value: selectedItem.sourceCardJson ? "已配置" : "未配置" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "当前角色卡", value: formatTemplateStatus(selectedItem) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(DetailRow, { label: "生成时间", value: formatDate$3(selectedItem?.templateCardGeneratedAt) })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Surface, { variant: "character-binding", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(SurfaceHeader, { title: "当前对象绑定", meta: selectedItem.roleId === boundRoleId ? "已绑定" : "未绑定" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { as: "p", tone: "secondary", wrap: true, children: currentObjectKey === "" ? "当前没有可绑定对象。" : `当前对象：${fileName(currentObjectKey)}` }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(ActionGroup, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: currentObjectKey === "" || selectedItem.roleId === boundRoleId || busy, onClick: () => void bindSelected(), children: "绑定此角色" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: currentObjectKey === "" || boundRoleId === "" || busy, onClick: () => void unbindCurrent(), children: "解绑" })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "section", variant: "character-danger", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "sm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "危险操作" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { tone: "secondary", children: "删除会同时清除该角色的音色、角色卡和绑定。" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => setDeleteConfirm(true), children: "删除角色" })
            ] }) })
          ] })
        ] }) })
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: deleteConfirm, title: "删除语音角色", onClose: () => setDeleteConfirm(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: busy, onClick: () => setDeleteConfirm(false), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", loading: busy, onClick: () => void removeSelected(), children: "删除" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { as: "p", wrap: true, children: [
      "删除角色“",
      selectedItem?.name ?? "",
      "”？关联音色、角色卡和绑定也会被清除。"
    ] }) })
  ] });
}
function formatTemplateStatus(item) {
  if (item === null) return "-";
  if (["ready", "completed"].includes(item.templateCardGenerationStatus.toLowerCase()) && item.templateCardJson !== "") return "已生成";
  if (item.templateCardGenerationStatus.toLowerCase() === "generating") return "生成中";
  if (item.templateCardGenerationStatus.toLowerCase() === "failed") return "生成失败";
  return "尚未生成";
}
function fileName(value) {
  if (value === null || value === void 0) return "";
  return value.split(/[\\/]/u).filter(Boolean).at(-1) ?? "";
}
function formatDate$3(value) {
  if (value === null || value === void 0 || value === "") return "尚未生成";
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? "尚未生成" : date.toLocaleString("zh-CN");
}
function TemplateCardPage() {
  const initialRole = reactExports.useMemo(readRole$1, []);
  const [role, setRole] = reactExports.useState(initialRole);
  const [operation, setOperation] = reactExports.useState(null);
  const [error, setError] = reactExports.useState("");
  const generatedAt = formatDate$2(role?.templateCardGeneratedAt);
  const status = formatStatus(role?.templateCardGenerationStatus);
  const json = formatJson$1(role?.templateCardJson);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "当前角色卡" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { children: role === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "未找到角色" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "template-card-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "header", variant: "template-card-summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "xs", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: role.name || role.roleId }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: status === "已生成" ? "success" : status === "生成失败" ? "danger" : "neutral", children: status })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { tone: "secondary", children: [
            "角色 ID：",
            role.roleId,
            " · 生成时间：",
            generatedAt
          ] }),
          role.templateCardGenerationMessage ? /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { tone: "secondary", children: role.templateCardGenerationMessage }) : null
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
            "已迭代 ",
            role.templateCardIterationCount,
            " 次"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: operation === "重新生成", disabled: operation !== null, onClick: () => void generate(false), children: "重新生成" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { loading: operation === "继续迭代", disabled: !role.templateCardJson || operation !== null, onClick: () => void generate(true), children: "继续迭代" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Surface, { variant: "template-card-reader", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { "aria-label": "角色卡正文", readOnly: true, rows: 24, value: json || "当前角色卡尚未生成。" }) })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: error !== "", title: `${operation ?? "角色卡生成"}失败`, onClose: () => setError(""), footer: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => setError(""), children: "确定" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { as: "p", wrap: true, children: error }) })
  ] });
  async function generate(continueIteration) {
    if (role === null)
      return;
    const name = continueIteration ? "继续迭代" : "重新生成";
    setOperation(name);
    setError("");
    const response = await bridge.core.invoke({ type: "character.template.generate", payload: { roleId: role.roleId, continueIteration } }, 12e4);
    if (response.success && response.payload !== null) {
      const refreshed = response.payload;
      setRole(refreshed);
      localStorage.setItem("aimaid.template-card-role", JSON.stringify(refreshed));
    } else
      setError(response.error?.message ?? `未能读取${name}后的当前角色卡。`);
    setOperation(null);
  }
}
function readRole$1() {
  try {
    const value = localStorage.getItem("aimaid.template-card-role");
    return value === null ? null : JSON.parse(value);
  } catch {
    return null;
  }
}
function formatDate$2(value) {
  if (!value)
    return "尚未生成";
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? "尚未生成" : date.toLocaleString("zh-CN");
}
function formatStatus(value) {
  switch (value?.toLowerCase()) {
    case "ready":
    case "completed":
      return "已生成";
    case "generating":
      return "生成中";
    case "failed":
      return "生成失败";
    default:
      return "尚未生成";
  }
}
function formatJson$1(value) {
  if (!value)
    return "";
  try {
    return JSON.stringify(JSON.parse(value), null, 2);
  } catch {
    return value;
  }
}
function CharacterEditorPage() {
  const original = reactExports.useMemo(readRole, []);
  const [tab, setTab] = reactExports.useState("基础");
  const [roleId, setRoleId] = reactExports.useState(original?.roleId ?? "");
  const [name, setName] = reactExports.useState(original?.name ?? "");
  const [avatar, setAvatar] = reactExports.useState(original?.avatarPath ?? "");
  const [avatarUrl, setAvatarUrl] = reactExports.useState("");
  const [voiceAssets, setVoiceAssets] = reactExports.useState([]);
  const [voiceId, setVoiceId] = reactExports.useState(original?.preferredVoiceId ?? original?.voiceName ?? "");
  const [cardJson, setCardJson] = reactExports.useState(formatJson(original?.sourceCardJson ?? ""));
  const [error, setError] = reactExports.useState("");
  const [saving, setSaving] = reactExports.useState(false);
  const [voiceDialog, setVoiceDialog] = reactExports.useState(false);
  const [voiceFolder, setVoiceFolder] = reactExports.useState("");
  const [voiceStyle, setVoiceStyle] = reactExports.useState("normal");
  const [voiceDisplayName, setVoiceDisplayName] = reactExports.useState("");
  const [voiceDialogError, setVoiceDialogError] = reactExports.useState("");
  const editing = original !== null;
  reactExports.useEffect(() => {
    void loadVoiceAssets().then(setVoiceAssets).catch((reason) => setError(messageOf$7(reason)));
  }, []);
  reactExports.useEffect(() => {
    if (avatar === "") {
      setAvatarUrl("");
      return;
    }
    void bridge.media.registerLocalFile(avatar).then((response) => setAvatarUrl(response.success ? response.payload?.url ?? "" : ""));
  }, [avatar]);
  function parseCard() {
    try {
      const card = JSON.parse(cardJson);
      if (roleId.trim() === "" && typeof card.id === "string")
        setRoleId(card.id);
      if (name.trim() === "" && typeof card.name === "string")
        setName(card.name);
      setError("");
    } catch {
      setError("原角色卡 JSON 解析失败，请检查格式。");
    }
  }
  async function save2() {
    if (name.trim() === "") {
      setError("显示名称不能为空。");
      return;
    }
    if (voiceId.trim() === "") {
      setError("请选择一个角色音色。");
      return;
    }
    const id = roleId.trim() || name.trim();
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const character = {
      roleId: id,
      name: name.trim(),
      voiceName: original?.voiceName ?? voiceId.trim(),
      roleTitle: original?.roleTitle ?? name.trim(),
      cardPath: original?.cardPath ?? "",
      sourceCardJson: cardJson.trim(),
      templateCardJson: original?.templateCardJson ?? "",
      preferredVoiceId: voiceId.trim(),
      validationStatus: original?.validationStatus ?? "",
      isEnabled: original?.isEnabled ?? true,
      updatedAt: now,
      cardSummary: original?.cardSummary ?? "",
      cardSchemaVersion: original?.cardSchemaVersion ?? "",
      templateCardSourceHash: original?.templateCardSourceHash ?? "",
      templateCardGenerationStatus: original?.templateCardGenerationStatus ?? "",
      templateCardGenerationMessage: original?.templateCardGenerationMessage ?? "",
      templateCardGeneratedAt: original?.templateCardGeneratedAt ?? null,
      templateCardLastAttemptAt: original?.templateCardLastAttemptAt ?? null,
      templateCardIterationCount: original?.templateCardIterationCount ?? 0,
      validationMessage: original?.validationMessage ?? "",
      lastValidatedAt: original?.lastValidatedAt ?? null,
      avatarPath: avatar
    };
    setSaving(true);
    const response = await bridge.core.invoke({ type: "character.save", payload: { character } });
    setSaving(false);
    if (!response.success) {
      setError(response.error?.message ?? "角色保存失败。");
      return;
    }
    const voices = roleVoicesForSelection(voiceAssets, voiceId, id);
    const voiceResponse = await bridge.core.invoke({ type: "character.voices.set", payload: { roleId: id, voices } });
    if (!voiceResponse.success) {
      setError(voiceResponse.error?.message ?? "角色音色绑定失败。");
      return;
    }
    void bridge.window.close();
  }
  async function browseAvatar() {
    const response = await bridge.dialog.openFile([{ name: "图片文件", extensions: ["png", "jpg", "jpeg", "gif", "bmp", "webp"] }]);
    const file = readFirstPath(response.payload);
    if (file === null)
      return;
    const imported = await bridge.core.invoke({ type: "character.avatar.import", payload: { sourcePath: file } });
    if (!imported.success || typeof imported.payload !== "string") {
      setError(imported.error?.message ?? "头像复制失败。");
      return;
    }
    setAvatar(imported.payload);
  }
  async function browseVoiceFolder() {
    const response = await bridge.dialog.openDirectory();
    const file = response.success ? response.payload?.filePaths[0] : void 0;
    if (file === void 0)
      return;
    setVoiceFolder(file);
    if (voiceDisplayName.trim() === "")
      setVoiceDisplayName(buildVoiceId(file, voiceStyle));
    setVoiceDialogError("");
  }
  async function saveVoiceAsset() {
    if (voiceFolder.trim() === "") {
      setVoiceDialogError("请选择音色文件夹。");
      return;
    }
    if (buildVoiceId(voiceFolder, voiceStyle) === "") {
      setVoiceDialogError("无法生成 voiceId，请检查文件夹名称。");
      return;
    }
    const baseName = voiceFolder.trim().replace(/[\\/]+$/u, "").split(/[\\/]/u).at(-1) ?? "";
    const response = await bridge.core.invoke({ type: "character.voice_asset.add", payload: { baseName, displayName: voiceDisplayName.trim(), style: voiceStyle, sourceFolderPath: voiceFolder } });
    if (!response.success) {
      setVoiceDialogError(response.error?.message ?? "音色保存失败。");
      return;
    }
    const assets = await loadVoiceAssets();
    setVoiceAssets(assets);
    if (typeof response.payload === "object" && response.payload !== null && "voiceId" in response.payload && typeof response.payload.voiceId === "string")
      setVoiceId(response.payload.voiceId);
    setVoiceDialog(false);
    setVoiceDialogError("");
  }
  const generatedVoiceId = buildVoiceId(voiceFolder, voiceStyle);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: editing ? "编辑语音角色" : "新增语音角色", tools: /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void bridge.window.close(), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: saving, onClick: () => void save2(), children: editing ? "保存修改" : "创建角色" })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "character-editor-layout", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Surface, { variant: "character-editor-preview", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "md", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(VisualRegion, { ratio: "square", children: avatarUrl !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(MediaImage, { src: avatarUrl, alt: `${name || "角色"}头像` }) : /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "span", variant: "character-editor-placeholder", children: name.trim().slice(0, 1) || "—" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "xs", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: name.trim() || (editing ? "角色资料" : "新角色") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { tone: "muted", children: roleId.trim() || "尚未设置角色 ID" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(FormField$1, { label: "头像路径", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "头像路径", value: avatar, onChange: (event) => setAvatar(event.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void browseAvatar(), children: "浏览头像" })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Surface, { variant: "character-editor-form", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Tabs, { label: "角色编辑分类", value: tab, onChange: (value) => setTab(value), items: [{ id: "基础", label: "基础" }, { id: "音色", label: "音色" }, { id: "原角色卡", label: "角色卡" }] }),
        error !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "error", children: error }) : null,
        tab === "基础" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "lg", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "基础信息" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "角色 ID", value: roleId, onChange: (event) => setRoleId(event.target.value) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "显示名称", value: name, onChange: (event) => setName(event.target.value) })
        ] }) : null,
        tab === "音色" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "lg", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "音色配置" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "角色音色", value: voiceId, onChange: (event) => setVoiceId(event.target.value), options: [{ value: "", label: "请选择角色音色" }, ...voiceAssets.map((item) => ({ value: item.voiceId, label: `${item.displayName} · ${item.voiceId}` }))] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setVoiceDialog(true), children: "新建音色" })
        ] }) : null,
        tab === "原角色卡" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "md", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { justify: "between", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "原角色卡" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: parseCard, children: "解析角色卡" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { "aria-label": "原角色卡 JSON", value: cardJson, onChange: (event) => setCardJson(event.target.value), rows: 22 })
        ] }) : null
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: voiceDialog, title: "新建音色", onClose: () => setVoiceDialog(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setVoiceDialog(false), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void saveVoiceAsset(), children: "保存" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "md", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "sm", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "源文件夹", readOnly: true, value: voiceFolder }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void browseVoiceFolder(), children: "选择文件夹" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "音色类型", value: voiceStyle, onChange: (event) => setVoiceStyle(event.target.value), options: ["normal", "soft", "lively", "close"].map((value) => ({ value, label: value })) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "显示名称", value: voiceDisplayName, onChange: (event) => setVoiceDisplayName(event.target.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "将生成 voiceId", readOnly: true, value: generatedVoiceId }),
      voiceDialogError !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "error", children: voiceDialogError }) : null
    ] }) })
  ] });
}
async function loadVoiceAssets() {
  const response = await bridge.core.invoke({ type: "character.voice_assets", payload: {} });
  if (!response.success || !Array.isArray(response.payload))
    throw new Error(response.error?.message ?? "音色列表读取失败。");
  return response.payload;
}
function roleVoicesForSelection(assets, selectedVoiceId, roleId) {
  const base = selectedVoiceId.replace(/_(normal|soft|lively|close)$/iu, "");
  const matched = assets.filter((item) => item.voiceId.replace(/_(normal|soft|lively|close)$/iu, "") === base);
  const normal = matched.find((item) => item.voiceId.endsWith("_normal")) ?? assets.find((item) => item.voiceId === selectedVoiceId);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  return ["normal", "soft", "lively", "close"].map((style, index) => {
    const asset = matched.find((item) => item.voiceId.endsWith(`_${style}`)) ?? normal;
    return asset === void 0 ? null : { roleId, voiceId: asset.voiceId, style, isDefault: index === 0, isEnabled: true, updatedAt: now };
  }).filter((item) => item !== null);
}
function messageOf$7(reason) {
  return reason instanceof Error ? reason.message : String(reason);
}
function readRole() {
  try {
    const value = localStorage.getItem("aimaid.character-editor-role");
    return value === null ? null : JSON.parse(value);
  } catch {
    return null;
  }
}
function formatJson(value) {
  if (value === "")
    return "";
  try {
    return JSON.stringify(JSON.parse(value), null, 2);
  } catch {
    return value;
  }
}
function readFirstPath(value) {
  if (typeof value !== "object" || value === null || !("filePaths" in value) || !Array.isArray(value.filePaths))
    return null;
  return typeof value.filePaths[0] === "string" ? value.filePaths[0] : null;
}
function buildVoiceId(folder, style) {
  const part = folder.trim().replace(/[\\/]+$/u, "").split(/[\\/]/u).at(-1)?.toLowerCase().replace(/\s+/gu, "_").replace(/[^\p{L}\p{N}_-]/gu, "") ?? "";
  const base = part.replace(/_(normal|soft|lively|close)$/iu, "").replace(/^[_-]+|[_-]+$/gu, "");
  return base === "" ? "" : `${base}_${style}`;
}
async function listReminders() {
  const response = await bridge.core.invoke({ type: "reminder.list", payload: {} });
  if (!response.success) throw new Error(response.error?.message ?? "提醒读取失败。");
  return Array.isArray(response.payload) ? response.payload : [];
}
async function saveReminder(payload) {
  return invokeReminder({ type: "reminder.save", payload });
}
async function setReminderEnabled(reminderId, enabled) {
  return invokeReminder({ type: "reminder.set_enabled", payload: { reminderId, enabled } });
}
async function setReminderAllowTts(reminderId, allowTts) {
  return invokeReminder({ type: "reminder.set_allow_tts", payload: { reminderId, allowTts } });
}
async function deleteReminder(reminderId) {
  const response = await bridge.core.invoke({ type: "reminder.delete", payload: { reminderId } });
  if (!response.success) throw new Error(response.error?.message ?? "提醒删除失败。");
}
async function processDueReminders() {
  const response = await bridge.core.invoke({ type: "reminder.process_due", payload: { now: (/* @__PURE__ */ new Date()).toISOString() } });
  if (!response.success) throw new Error(response.error?.message ?? "提醒检查失败。");
}
async function invokeReminder(request) {
  const response = await bridge.core.invoke(request);
  if (!response.success || response.payload === null) throw new Error(response.error?.message ?? "提醒保存失败。");
  return response.payload;
}
function ReminderPage() {
  const toast = useToast();
  const [items, setItems] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [editing, setEditing] = reactExports.useState(null);
  const [deleting, setDeleting] = reactExports.useState(null);
  const [busyId, setBusyId] = reactExports.useState(null);
  async function load() {
    try {
      setError(null);
      setItems(await listReminders());
    } catch (reason) {
      setError(message(reason));
    }
  }
  reactExports.useEffect(() => {
    void load();
  }, []);
  async function mutate(id, operation) {
    try {
      setBusyId(id);
      await operation();
      await load();
    } catch (reason) {
      toast.show(message(reason), "error");
    } finally {
      setBusyId(null);
    }
  }
  const tools = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void load(), children: "刷新" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void mutate("check", processDueReminders), children: "检查" })
  ] });
  if (items === null)
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "提醒事项", tools }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { children: error === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { label: "正在读取提醒事项" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ErrorState, { title: "提醒事项读取失败", message: error, onRetry: () => void load() }) })
    ] });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "提醒事项", tools }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(PageContent, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "md", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(PageToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
            items.length,
            " 项提醒"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { tone: "secondary", children: [
            "启用 ",
            items.filter((item) => item.enabled).length,
            " 项"
          ] })
        ] }), actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => setEditing("new"), children: "新增提醒" }) }),
        items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "暂无提醒" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "reminder-list", children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Surface, { variant: "reminder-row", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "reminder-row__time", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: item.enabled ? "accent" : "neutral", children: item.enabled ? "已启用" : "已关闭" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: formatDate$1(item.nextDueAt ?? item.dueAt) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "xs", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.title }),
            item.message.trim() !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { as: "p", tone: "secondary", wrap: true, children: item.message }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { tone: "muted", children: "没有附加内容" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: item.repeat === "daily" ? "每天重复" : "仅一次" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: item.allowTts ? "TTS 播报" : "静默提醒" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { justify: "end", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "提醒", checked: item.enabled, disabled: busyId === item.reminderId, onChange: (event) => void mutate(item.reminderId, () => setReminderEnabled(item.reminderId, event.target.checked)) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "TTS", checked: item.allowTts, disabled: busyId === item.reminderId, onChange: (event) => void mutate(item.reminderId, () => setReminderAllowTts(item.reminderId, event.target.checked)) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => setEditing(item), children: "编辑" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", onClick: () => setDeleting(item), children: "删除" })
          ] })
        ] }, item.reminderId)) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ReminderEditor, { item: editing, close: () => setEditing(null), saved: () => {
        setEditing(null);
        void load();
      } }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: deleting !== null, title: "确认删除", description: deleting === null ? "" : `删除提醒 [${deleting.title}]？`, onClose: () => setDeleting(null), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDeleting(null), children: "取消" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => {
          const item = deleting;
          if (item === null)
            return;
          setDeleting(null);
          void mutate(item.reminderId, () => deleteReminder(item.reminderId));
        }, children: "删除" })
      ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { as: "p", wrap: true, children: "删除后无法在页面中恢复。" }) })
    ] })
  ] });
}
function ReminderEditor({ item, close, saved }) {
  const source = item === "new" || item === null ? null : item;
  const initialDue = reactExports.useMemo(() => toLocalInput(source?.nextDueAt ?? source?.dueAt ?? new Date(Date.now() + 6e5).toISOString()), [source]);
  const [title, setTitle] = reactExports.useState("");
  const [content2, setContent] = reactExports.useState("");
  const [dueDate, setDueDate] = reactExports.useState(initialDue.slice(0, 10));
  const [dueTime, setDueTime] = reactExports.useState(initialDue.slice(11, 16));
  const [repeat, setRepeat] = reactExports.useState("none");
  const [enabled, setEnabled] = reactExports.useState(true);
  const [allowTts, setAllowTts] = reactExports.useState(true);
  const [saving, setSaving] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const dueIsValid = /^\d{2}:\d{2}$/.test(dueTime) && !Number.isNaN((/* @__PURE__ */ new Date(`${dueDate}T${dueTime}:00`)).getTime());
  reactExports.useEffect(() => {
    setTitle(source?.title ?? "");
    setContent(source?.message ?? "");
    setDueDate(initialDue.slice(0, 10));
    setDueTime(initialDue.slice(11, 16));
    setRepeat(source?.repeat ?? "none");
    setEnabled(source?.enabled ?? true);
    setAllowTts(source?.allowTts ?? true);
    setError(null);
  }, [initialDue, source]);
  async function submit() {
    if (!/^\d{2}:\d{2}$/.test(dueTime)) {
      setError("时间格式请写成 HH:mm，例如 23:30。");
      return;
    }
    const date = /* @__PURE__ */ new Date(`${dueDate}T${dueTime}:00`);
    if (Number.isNaN(date.getTime())) {
      setError("请选择有效日期，并按 HH:mm 输入时间。");
      return;
    }
    if (repeat === "none" && date.getTime() <= Date.now()) {
      setError("一次性提醒必须选择未来时间。");
      return;
    }
    const cleanTitle = title.trim() === "" ? "提醒" : title.trim();
    const payload = { reminderId: source?.reminderId ?? null, title: cleanTitle, message: content2.trim() === "" ? cleanTitle : content2.trim(), dueAt: date.toISOString(), repeat, enabled, allowTts };
    try {
      setSaving(true);
      setError(null);
      await saveReminder(payload);
      saved();
    } catch (reason) {
      setError(message(reason));
    } finally {
      setSaving(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: item !== null, title: source === null ? "新增提醒" : "编辑提醒", onClose: close, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: close, children: "取消" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: saving, disabled: !dueIsValid, onClick: () => void submit(), children: "保存" })
  ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "lg", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "section", variant: "dialog-form-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "内容" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "标题", value: title, onChange: (event) => setTitle(event.target.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { label: "内容", value: content2, onChange: (event) => setContent(event.target.value) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "section", variant: "dialog-form-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "时间规则" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Grid, { columns: "two", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "日期", type: "date", value: dueDate, onChange: (event) => setDueDate(event.target.value) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "时间", type: "time", value: dueTime, ...error === null ? {} : { error }, onChange: (event) => {
          setDueTime(event.target.value);
          setError(null);
        } })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "重复规则", value: repeat, onChange: setRepeat, options: [{ value: "none", label: "一次" }, { value: "daily", label: "每天" }] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "section", variant: "dialog-form-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "播报行为" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "启用提醒", checked: enabled, onChange: (event) => setEnabled(event.target.checked) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "角色 TTS 播报", checked: allowTts, onChange: (event) => setAllowTts(event.target.checked) })
    ] })
  ] }) });
}
function formatDate$1(value) {
  return new Intl.DateTimeFormat("zh-CN", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value));
}
function toLocalInput(value) {
  const date = new Date(value);
  const offset = date.getTimezoneOffset() * 6e4;
  return new Date(date.getTime() - offset).toISOString().slice(0, 16);
}
function message(reason) {
  return reason instanceof Error ? reason.message : String(reason);
}
const STORAGE_KEY = "aimaid.appearance.current";
const CHANGE_EVENT = "aimaid-appearance-changed";
function saveAndApplyAppearance(configuration, colors) {
  localStorage.setItem(STORAGE_KEY, JSON.stringify({ configuration, colors }));
  applyAppearance(configuration, colors);
  window.dispatchEvent(new Event(CHANGE_EVENT));
}
function applyStoredAppearance() {
  const value = readStoredAppearance();
  if (value === null) return;
  if (!isSupportedPalette(value.colors)) {
    localStorage.removeItem(STORAGE_KEY);
    return;
  }
  applyAppearance(value.configuration, value.colors);
}
function subscribeAppearance(listener) {
  const storageListener = (event) => {
    if (event.key === STORAGE_KEY) listener();
  };
  window.addEventListener("storage", storageListener);
  window.addEventListener(CHANGE_EVENT, listener);
  return () => {
    window.removeEventListener("storage", storageListener);
    window.removeEventListener(CHANGE_EVENT, listener);
  };
}
function readStoredAppearance() {
  try {
    const value = JSON.parse(localStorage.getItem(STORAGE_KEY) ?? "null");
    return value !== null && value.configuration !== void 0 && Array.isArray(value.colors) && value.colors.length === 4 ? value : null;
  } catch {
    return null;
  }
}
function applyAppearance(configuration, colors) {
  const root2 = document.documentElement;
  const [canvas, surface, elevated, accent] = colors;
  const target = configuration.contentBrightness === "Soft" ? canvas : configuration.contentBrightness === "Clear" ? elevated : surface;
  const amount = configuration.contentBrightness === "Soft" ? 0.08 : configuration.contentBrightness === "Clear" ? 0.06 : 0;
  const content2 = amount === 0 ? surface : mix(surface, target, amount);
  root2.style.setProperty("--color-bg-canvas", canvas);
  root2.style.setProperty("--color-bg-surface", content2);
  root2.style.setProperty("--color-bg-elevated", elevated);
  root2.style.setProperty("--color-accent", accent);
  root2.style.setProperty("--color-focus", accent);
  root2.style.setProperty("--color-text-primary", "#222528");
  root2.style.setProperty("--color-text-secondary", "#565C62");
  root2.style.setProperty("--color-text-muted", "#767E86");
  root2.style.setProperty("--font-family-sans", configuration.fontFamily === "" ? 'Inter, "Segoe UI Variable", "Segoe UI", system-ui, sans-serif' : `"${configuration.fontFamily}", "Segoe UI", sans-serif`);
  root2.style.fontSize = `${configuration.fontScale * 100}%`;
  const radii = configuration.cornerRadiusStyle === "Small" ? [".25rem", ".375rem", ".5rem"] : configuration.cornerRadiusStyle === "Large" ? [".5rem", ".875rem", "1.25rem"] : [".375rem", ".625rem", ".875rem"];
  root2.style.setProperty("--radius-sm", radii[0]);
  root2.style.setProperty("--radius-md", radii[1]);
  root2.style.setProperty("--radius-lg", radii[2]);
  const spacing = configuration.density === "Compact" ? 0.85 : configuration.density === "Comfortable" ? 1.15 : 1;
  root2.style.setProperty("--appearance-density-scale", String(spacing));
  root2.style.setProperty("--space-2", `${0.5 * spacing}rem`);
  root2.style.setProperty("--space-3", `${0.75 * spacing}rem`);
  root2.style.setProperty("--space-4", `${1 * spacing}rem`);
  root2.style.setProperty("--space-5", `${1.25 * spacing}rem`);
  root2.style.setProperty("--space-6", `${1.5 * spacing}rem`);
  root2.style.setProperty("--duration-fast", configuration.animationsEnabled ? "120ms" : "0ms");
  root2.style.setProperty("--duration-normal", configuration.animationsEnabled ? "180ms" : "0ms");
  root2.style.setProperty("--duration-slow", configuration.animationsEnabled ? "280ms" : "0ms");
  root2.dataset.headerStyle = configuration.headerStyle;
  root2.style.colorScheme = "only light";
}
function isSupportedPalette(colors) {
  return luminance(colors[0]) >= 0.72 && luminance(colors[1]) >= 0.68 && luminance(colors[2]) >= 0.82;
}
function luminance(hex) {
  const [r, g, b] = channels(hex);
  return 0.2126 * (r / 255) + 0.7152 * (g / 255) + 0.0722 * (b / 255);
}
function mix(source, target, amount) {
  const [ar, ag, ab] = channels(source);
  const [br, bg, bb] = channels(target);
  return `#${[mixChannel(ar, br, amount), mixChannel(ag, bg, amount), mixChannel(ab, bb, amount)].join("")}`;
}
function mixChannel(source, target, amount) {
  return Math.round(source + (target - source) * amount).toString(16).padStart(2, "0");
}
function channels(hex) {
  const value = hex.replace("#", "");
  return [Number.parseInt(value.slice(0, 2), 16), Number.parseInt(value.slice(2, 4), 16), Number.parseInt(value.slice(4, 6), 16)];
}
const THEMES = [
  theme("harbor_blue", "港湾蓝", "清爽、沉静", "#EAF2F8", "#DDEAF4", "#FFFFFF", "#286F9D"),
  theme("cedar_green", "雪松绿", "自然、专注", "#EEF2EB", "#DFE9DC", "#FEFFFD", "#4C7650"),
  theme("amber_sand", "琥珀砂", "温暖、柔和", "#F7F0E3", "#ECE1CC", "#FFFDFC", "#A66A24"),
  theme("rose_clay", "玫瑰陶", "克制、雅致", "#F8ECEC", "#F0DCDD", "#FFFDFD", "#B34F5F"),
  theme("lavender_haze", "薰衣草雾", "轻盈、舒缓", "#F2EEF8", "#E5DCF1", "#FFFEFF", "#7655A6"),
  theme("sea_glass", "海盐青", "通透、平和", "#E8F3F1", "#D8E9E5", "#FCFFFE", "#287C72"),
  theme("river_stone", "河岸石", "中性、耐看", "#F0F0EC", "#E4E3DC", "#FEFEFC", "#5B6864"),
  theme("apricot_glow", "杏子光", "明快、亲和", "#F7EEE8", "#EEDDD1", "#FFFDFC", "#BB6444"),
  theme("iris_blue", "鸢尾蓝", "理性、优雅", "#EDF0F8", "#DDE3F2", "#FFFFFF", "#4F67A8")
];
const DEFAULT_CONFIGURATION = { themeId: "sea_glass", contentBrightness: "Standard", fontFamily: "", fontScale: 1, cornerRadiusStyle: "Medium", density: "Standard", headerStyle: "Subtle", animationsEnabled: true };
function AppearanceSettingsPage() {
  const toast = useToast();
  const [configuration, setConfiguration] = reactExports.useState(DEFAULT_CONFIGURATION);
  const revision = reactExports.useRef(0);
  reactExports.useEffect(() => {
    void bridge.core.invoke({ type: "appearance.get", payload: {} }).then((response) => {
      if (!response.success) throw new Error(response.error?.message ?? "外观设置读取失败。");
      const source = response.payload;
      const loaded = { ...source, themeId: normalizeThemeId(source.themeId) };
      setConfiguration(loaded);
      saveAndApplyAppearance(loaded, colorsFor(loaded.themeId));
      if (loaded.themeId !== source.themeId) {
        void bridge.core.invoke({ type: "appearance.save", payload: { configuration: loaded } }).then((migration) => {
          if (!migration.success) throw new Error(migration.error?.message ?? "旧版外观设置迁移失败。");
        }).catch((reason) => toast.show(messageOf$6(reason), "error"));
      }
    }).catch((reason) => toast.show(reason instanceof Error ? reason.message : String(reason), "error"));
  }, [toast]);
  function update(patch) {
    const previous = configuration;
    const next = { ...configuration, ...patch };
    const currentRevision = ++revision.current;
    setConfiguration(next);
    saveAndApplyAppearance(next, colorsFor(next.themeId));
    void bridge.core.invoke({ type: "appearance.save", payload: { configuration: next } }).then((response) => {
      if (!response.success) throw new Error(response.error?.message ?? "外观设置保存失败。");
    }).catch((reason) => {
      if (revision.current === currentRevision) {
        setConfiguration(previous);
        saveAndApplyAppearance(previous, colorsFor(previous.themeId));
      }
      toast.show(messageOf$6(reason), "error");
    });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "外观设置" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "appearance-content", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(SettingsSection, { title: "配色方案", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "theme-card-grid", children: THEMES.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(ThemeCard, { item, selected: configuration.themeId === item.id, select: () => update({ themeId: item.id }) }, item.id)) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "内容亮度", control: /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "内容亮度", value: configuration.contentBrightness, onChange: (contentBrightness) => update({ contentBrightness }), options: [{ value: "Soft", label: "柔和" }, { value: "Standard", label: "标准" }, { value: "Clear", label: "清晰" }] }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(SettingsSection, { title: "字体", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "字体方案", control: /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "字体方案", value: configuration.fontFamily, onChange: (fontFamily) => update({ fontFamily }), options: [{ value: "", label: "默认" }, { value: "Microsoft YaHei UI", label: "微软雅黑" }] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "字号缩放", control: /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "字号缩放", value: String(configuration.fontScale), onChange: (fontScale) => update({ fontScale: Number(fontScale) }), options: [{ value: "0.9", label: "90%" }, { value: "1", label: "100%" }, { value: "1.1", label: "110%" }, { value: "1.2", label: "120%" }] }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(SettingsSection, { title: "密度与圆角", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "圆角风格", control: /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "圆角风格", value: configuration.cornerRadiusStyle, onChange: (cornerRadiusStyle) => update({ cornerRadiusStyle }), options: [{ value: "Small", label: "小" }, { value: "Medium", label: "中" }, { value: "Large", label: "大" }] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "界面密度", control: /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "界面密度", value: configuration.density, onChange: (density) => update({ density }), options: [{ value: "Compact", label: "紧凑" }, { value: "Standard", label: "标准" }, { value: "Comfortable", label: "宽松" }] }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(SettingsSection, { title: "窗口顶部", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "顶部样式", control: /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "窗口顶部样式", value: configuration.headerStyle, onChange: (headerStyle) => update({ headerStyle }), options: [{ value: "None", label: "不显示" }, { value: "Subtle", label: "柔和" }, { value: "AccentStrip", label: "主题色顶栏" }, { value: "Filled", label: "主题色标题栏" }] }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(SettingsSection, { title: "动画", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title: "界面动画", control: /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: configuration.animationsEnabled ? "已开启" : "已关闭", checked: configuration.animationsEnabled, onChange: (event) => update({ animationsEnabled: event.target.checked }) }) }) })
    ] }) })
  ] });
}
function ThemeCard({ item, selected, select }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { appearance: "card", selected, "aria-current": selected ? "true" : void 0, onClick: select, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "span", variant: "theme-card__preview", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ColorPalette, { colors: item.colors }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "span", variant: "theme-card__meta", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "xs", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.name }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "muted", children: item.description })
      ] }),
      selected ? /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: "当前方案" }) : null
    ] })
  ] });
}
function theme(id, name, description, ...colors) {
  return { id, name, description, colors };
}
function colorsFor(themeId) {
  const selected = THEMES.find((item) => item.id === themeId);
  if (selected === void 0) throw new Error(`未知配色方案：${themeId}`);
  return selected.colors;
}
function normalizeThemeId(themeId) {
  const legacy = {
    fluent_fog: "harbor_blue",
    macos_mist: "harbor_blue",
    material_sage: "cedar_green",
    wechat_soft: "sea_glass",
    notion_paper: "amber_sand",
    slack_aubergine: "lavender_haze",
    neutral_soft: "river_stone",
    cream_paper: "amber_sand",
    cool_slate: "iris_blue",
    anime_sakura_cream: "rose_clay",
    anime_mint_soda: "sea_glass",
    anime_peach_cream: "apricot_glow",
    anime_sky_sailor: "harbor_blue",
    anime_candy_park: "lavender_haze",
    fluent_fog_light: "harbor_blue",
    macos_mist_light: "harbor_blue",
    material_sage_light: "cedar_green",
    wechat_soft_light: "sea_glass",
    notion_paper_light: "amber_sand",
    slack_aubergine_light: "lavender_haze",
    neutral_soft_light: "river_stone",
    cream_paper_light: "amber_sand",
    cool_slate_light: "iris_blue",
    fluent_graphite_dark: "river_stone",
    macos_ocean_dark: "harbor_blue",
    material_indigo_dark: "iris_blue",
    wechat_soft_dark: "sea_glass",
    vscode_modern_dark: "river_stone",
    jetbrains_darcula: "river_stone",
    oled_black_dark: "river_stone",
    anime_neon_nightingale: "lavender_haze"
  };
  const migrated = legacy[themeId];
  if (migrated !== void 0) return migrated;
  if (THEMES.some((item) => item.id === themeId)) return themeId;
  throw new Error(`未知配色方案：${themeId}`);
}
function messageOf$6(reason) {
  return reason instanceof Error ? reason.message : String(reason);
}
const SYMBOLS = ["BTC", "ETH", "SOL", "BNB", "DOGE", "XRP", "ADA", "AVAX", "LINK", "DOT", "LTC", "TRX", "SUI", "TON"];
const INTERVALS = ["5 分钟", "15 分钟", "1 小时", "4 小时", "1 天", "1 周"];
const EMA = ["7", "25", "90", "120", "200"];
function BitcoinPage() {
  const [symbols, setSymbols] = reactExports.useState(/* @__PURE__ */ new Set(["BTC"]));
  const [interval, setIntervalValue] = reactExports.useState("15 分钟");
  const [ema, setEma] = reactExports.useState(/* @__PURE__ */ new Set(["7", "25"]));
  const [dashboard, setDashboard] = reactExports.useState(false);
  const [catalog, setCatalog] = reactExports.useState([]);
  const [query, setQuery] = reactExports.useState("");
  const [snapshots, setSnapshots] = reactExports.useState({});
  const [snapshotErrors, setSnapshotErrors] = reactExports.useState({});
  const [status, setStatus] = reactExports.useState("正在读取 Core 行情数据…");
  const toggle = (source, value, update) => {
    const next = new Set(source);
    if (next.has(value)) next.delete(value);
    else next.add(value);
    update(next);
  };
  reactExports.useEffect(() => {
    void bridge.core.invoke({ type: "market.symbols", payload: {} }, 3e4).then((response) => {
      if (!response.success || !Array.isArray(response.payload)) {
        setStatus(response.error?.message ?? "币种目录读取失败。");
        return;
      }
      setCatalog(response.payload);
      setStatus(`已从 Core 读取 ${response.payload.length} 个 USDT 现货交易对。`);
    }).catch((error) => setStatus(messageOf$5(error, "币种目录读取失败。")));
  }, []);
  reactExports.useEffect(() => {
    for (const symbol of symbols) {
      setSnapshotErrors((current) => {
        const next = { ...current };
        delete next[symbol];
        return next;
      });
      void bridge.core.invoke({ type: "market.snapshot", payload: { symbol } }, 3e4).then((response) => {
        if (!response.success || response.payload === null) {
          const message2 = response.error?.message ?? `${symbol} 行情读取失败。`;
          setSnapshotErrors((current) => ({ ...current, [symbol]: message2 }));
          setStatus(message2);
          return;
        }
        setSnapshots((current) => ({ ...current, [symbol]: response.payload }));
      }).catch((error) => {
        const message2 = messageOf$5(error, `${symbol} 行情读取失败。`);
        setSnapshotErrors((current) => ({ ...current, [symbol]: message2 }));
        setStatus(message2);
      });
    }
  }, [symbols]);
  const matches = query.trim() === "" ? [] : catalog.filter((item) => item.symbol.includes(query.trim().toUpperCase()) || item.baseAsset.includes(query.trim().toUpperCase())).slice(0, 8);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "BTC 行情" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "dashboard", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: dashboard ? "secondary" : "primary", onClick: () => setDashboard(false), children: "编辑模式" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: dashboard ? "primary" : "secondary", onClick: () => setDashboard(true), children: "看板模式" })
      ] }), actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void bridge.window.open("crypto-chart"), children: "专业图表" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void bridge.window.open("crypto-provider"), children: "行情服务" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void bridge.window.open("crypto-events"), children: "事件中心" })
      ] }) }),
      !dashboard ? /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "看板配置", actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(SmallText, { children: [
        symbols.size,
        " 个资产"
      ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Selector, { title: "资产（可多选）", values: SYMBOLS, selected: symbols, select: (value) => toggle(symbols, value, setSymbols) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "搜索交易对", value: query, onChange: (event) => setQuery(event.target.value), onKeyDown: (event) => {
            if (event.key !== "Enter" || matches[0] === void 0) return;
            setSymbols((current) => new Set(current).add(matches[0].baseAsset));
            setQuery("");
          } }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: query.trim() === "" ? `${catalog.length} 个交易对` : matches.length === 0 ? "没有匹配交易对" : matches[0].symbol })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Selector, { title: "周期", values: INTERVALS, selected: /* @__PURE__ */ new Set([interval]), select: setIntervalValue }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Selector, { title: "EMA 参数", values: EMA, selected: ema, select: (value) => toggle(ema, value, setEma) })
      ] }) }) : null,
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "市场快照", description: `${[...symbols].join(" / ")} · ${interval}`, scroll: true, wide: dashboard, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductGrid, { density: "comfortable", children: [...symbols].map((symbol) => /* @__PURE__ */ jsxRuntimeExports.jsx(MarketCard, { symbol, snapshot: snapshots[symbol], error: snapshotErrors[symbol] }, symbol)) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: status })
    ] })
  ] });
}
function MarketCard({ symbol, snapshot, error }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Header, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { wrap: false, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(MediaImage, { src: `aimaid-asset://ui/market_icons/${symbol.toLowerCase()}.png`, alt: "" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: symbol }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: snapshot === void 0 ? "-- USDT" : `${formatNumber(snapshot.lastPrice)} USDT` })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: snapshot === void 0 ? "--%" : `${snapshot.priceChangePercent.toFixed(2)}%` }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(InlineText, { children: [
          "24h 最高 ",
          snapshot === void 0 ? "--" : formatNumber(snapshot.highPrice),
          " · 最低 ",
          snapshot === void 0 ? "--" : formatNumber(snapshot.lowPrice)
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void bridge.window.open("crypto-chart"), children: "专业图表" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "metrics", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Metric, { label: "成交额", value: snapshot === void 0 ? "--" : compact$1(snapshot.quoteVolume) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Metric, { label: "资金费率", value: snapshot?.fundingRate == null ? "--" : `${(snapshot.fundingRate * 100).toFixed(4)}%` }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Metric, { label: "持仓量 OI", value: snapshot?.openInterest == null ? "--" : compact$1(snapshot.openInterest) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Metric, { label: "盘口强弱", value: snapshot?.bidAskRatio == null ? "--" : snapshot.bidAskRatio.toFixed(2) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: error ?? (snapshot === void 0 ? "正在读取 Core 行情快照…" : `更新于 ${new Date(snapshot.updatedAt).toLocaleTimeString("zh-CN")}`) })
  ] });
}
function Metric({ label, value }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label, value });
}
function Selector({ title, values, selected, select }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: title }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Inline, { gap: "xs", wrap: true, children: values.map((value) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { selected: selected.has(value), onClick: () => select(value), children: value }, value)) })
  ] });
}
function formatNumber(value) {
  return value >= 1e3 ? value.toLocaleString("zh-CN", { maximumFractionDigits: 2 }) : value.toLocaleString("zh-CN", { maximumFractionDigits: 8 });
}
function compact$1(value) {
  const absolute = Math.abs(value);
  if (absolute >= 1e9) return `${(value / 1e9).toFixed(2)}B`;
  if (absolute >= 1e6) return `${(value / 1e6).toFixed(2)}M`;
  if (absolute >= 1e3) return `${(value / 1e3).toFixed(2)}K`;
  return value.toFixed(2);
}
function messageOf$5(error, fallback) {
  return error instanceof Error && error.message.trim() !== "" ? error.message : fallback;
}
function CryptoEventCenterPage() {
  const [events, setEvents] = reactExports.useState([]);
  const [status, setStatus] = reactExports.useState("0 条事件");
  const refresh = async () => {
    const response = await bridge.core.invoke({ type: "market.list", payload: { limit: 500 } });
    if (!response.success || !Array.isArray(response.payload)) {
      setStatus(response.error?.message ?? "市场事件读取失败。");
      return;
    }
    const loaded = response.payload.map(toMarketEvent);
    setEvents(loaded);
    setStatus(`${loaded.length} 条事件`);
  };
  reactExports.useEffect(() => {
    void refresh();
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "市场事件中心", tools: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void refresh(), children: "刷新" }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "CoreHost 运行期间自动记录 Binance 全市场强平事件。链上大额转账、交易所流入和服务器事件采集尚未接入。" }), actions: /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: status }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "实时事件流", scroll: true, children: events.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Section, { children: "暂无事件。保持应用运行即可持续接收全市场强平流；当前还没有服务器端链上事件源。" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ProductGrid, { density: "comfortable", children: events.map((event) => /* @__PURE__ */ jsxRuntimeExports.jsx(MarketEventCard, { event }, event.id)) }) })
    ] })
  ] });
}
function CryptoProviderSettingsPage() {
  const [enabled, setEnabled] = reactExports.useState(false);
  const [url, setUrl] = reactExports.useState("");
  const [timeout, setTimeoutValue] = reactExports.useState("8");
  const [status, setStatus] = reactExports.useState("当前：现货 REST / 合约 REST / 实时流 → Binance 直连 · AI Provider 尚未检测");
  const [lastHealthStatus, setLastHealthStatus] = reactExports.useState("未检测");
  const [lastHealthLatencyMs, setLastHealthLatencyMs] = reactExports.useState(null);
  const [lastCheckedAt, setLastCheckedAt] = reactExports.useState(null);
  reactExports.useEffect(() => {
    void loadCryptoProvider(setEnabled, setUrl, setTimeoutValue, setLastHealthStatus, setLastHealthLatencyMs, setLastCheckedAt, setStatus);
  }, []);
  const validate = (requireServiceUrl) => {
    const timeoutSeconds = Number(timeout);
    if (!/^\d+$/u.test(timeout.trim()) || timeoutSeconds < 1 || timeoutSeconds > 120) {
      setStatus("请求超时必须是 1 到 120 秒之间的整数。");
      return false;
    }
    if (requireServiceUrl) {
      try {
        const serviceUrl = new URL(url.trim());
        if (serviceUrl.protocol !== "http:" && serviceUrl.protocol !== "https:") throw new Error("unsupported protocol");
      } catch {
        setStatus("请输入有效的 HTTP 或 HTTPS AI Provider 地址。");
        return false;
      }
    }
    return true;
  };
  const configuration = () => ({ isEnabled: enabled, serviceUrl: url.trim(), timeoutSeconds: Number(timeout), lastHealthStatus, lastHealthLatencyMs, lastCheckedAt });
  const save2 = async () => {
    if (!validate(enabled))
      return;
    const response = await bridge.core.invoke({ type: "crypto_provider.save", payload: { configuration: configuration() } });
    setStatus(response.success ? "已保存；下次行情刷新立即使用新连接方式。" : `保存失败：${response.error?.message ?? "未知错误"}`);
  };
  const check = async () => {
    if (!validate(true))
      return;
    setStatus("正在检测 AI Provider…");
    const response = await bridge.core.invoke({ type: "crypto_provider.check", payload: { configuration: configuration() } }, 12e4);
    if (!response.success || !isRecord$2(response.payload)) {
      setStatus(`检测失败：${response.error?.message ?? "未知错误"}`);
      return;
    }
    const available = response.payload.available === true;
    const provider = typeof response.payload.provider === "string" ? response.payload.provider : "AI Provider";
    const latency = typeof response.payload.latencyMs === "number" ? response.payload.latencyMs : 0;
    setLastHealthStatus(available ? provider : "不可用");
    setLastHealthLatencyMs(latency);
    setLastCheckedAt((/* @__PURE__ */ new Date()).toISOString());
    setStatus(available ? `服务正常 · ${provider} · 上游 ${latency} ms` : "服务不可用");
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "加密行情服务" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ProductWorkspace, { layout: "center", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "连接设置", actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "现货 REST 使用 AI Provider", checked: enabled, onChange: (event) => setEnabled(event.target.checked) }), emphasis: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(FormLabel, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "连接方式" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: enabled ? "AI Provider 代理" : "Binance 直连" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "AI Provider 地址", value: url, onChange: (event) => setUrl(event.target.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "请求超时（秒）", value: timeout, onChange: (event) => setTimeoutValue(event.target.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "metrics", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "最近状态", value: lastHealthStatus }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "上游延迟", value: lastHealthLatencyMs === null ? "—" : `${lastHealthLatencyMs} ms` }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "检测时间", value: lastCheckedAt === null ? "尚未检测" : new Date(lastCheckedAt).toLocaleString("zh-CN") })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void check(), children: "检测服务" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void save2(), children: "保存" })
      ] }), children: status })
    ] }) }) })
  ] });
}
async function loadCryptoProvider(setEnabled, setUrl, setTimeoutValue, setHealth, setLatency, setChecked, setStatus) {
  const response = await bridge.core.invoke({ type: "crypto_provider.get", payload: {} });
  if (!response.success || !isCryptoProviderConfiguration(response.payload))
    return;
  const value = response.payload;
  setEnabled(value.isEnabled);
  setUrl(value.serviceUrl);
  setTimeoutValue(String(value.timeoutSeconds));
  setHealth(value.lastHealthStatus);
  setLatency(value.lastHealthLatencyMs);
  setChecked(value.lastCheckedAt);
  const route = value.isEnabled ? "当前：现货 REST → AI Provider；合约 REST / 实时流 → Binance 直连" : "当前：现货 REST / 合约 REST / 实时流 → Binance 直连";
  setStatus(value.lastCheckedAt === null ? `${route} · AI Provider 尚未检测` : `${route} · Provider ${value.lastHealthStatus} · ${value.lastHealthLatencyMs ?? "-"} ms · ${new Date(value.lastCheckedAt).toLocaleString("zh-CN", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" })}`);
}
function isCryptoProviderConfiguration(value) {
  return isRecord$2(value) && typeof value.isEnabled === "boolean" && typeof value.serviceUrl === "string" && typeof value.timeoutSeconds === "number";
}
function MarketEventCard({ event }) {
  const side = event.side === "SELL" ? "多单强平" : "空单强平";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Article, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Header, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
        event.symbol,
        " · ",
        side
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(TimeValue, { children: new Date(event.occurredAt).toLocaleString("zh-CN", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", second: "2-digit" }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "数量 ",
      formatAmount(event.amount),
      " · 成交价 ",
      event.price.toLocaleString("zh-CN", { minimumFractionDigits: 2, maximumFractionDigits: 2 }),
      " · 名义价值 ",
      compact(event.amount * event.price),
      " USDT"
    ] })
  ] });
}
function formatAmount(value) {
  return value.toFixed(4).replace(/\.?0+$/u, "");
}
function compact(value) {
  const absolute = Math.abs(value);
  if (absolute >= 1e9)
    return `${(value / 1e9).toFixed(2)}B`;
  if (absolute >= 1e6)
    return `${(value / 1e6).toFixed(2)}M`;
  if (absolute >= 1e3)
    return `${(value / 1e3).toFixed(2)}K`;
  return value.toFixed(2).replace(/\.?0+$/u, "");
}
function CryptoChartPage() {
  const [status, setStatus] = reactExports.useState("正在读取行情快照…");
  const [candles, setCandles] = reactExports.useState([]);
  reactExports.useEffect(() => {
    void bridge.core.invoke({ type: "market.chart_snapshot", payload: { symbol: "BTC", interval: "15m", emaPeriods: [7, 25] } }, 3e4).then((response) => {
      if (!response.success || response.payload === null) {
        setStatus(response.error?.message ?? "行情快照读取失败。");
        return;
      }
      const snapshot = response.payload;
      if (!Array.isArray(snapshot.candles) || snapshot.candles.length === 0) {
        setStatus("K 线快照为空。");
        return;
      }
      setCandles(snapshot.candles);
      setStatus("BTCUSDT · 15 分钟 · Core 实时快照");
    });
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "BTC / USDT" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "BTC / USDT", scroll: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(MarketChart, { candles, label: "BTC / USDT 15 分钟 K 线图" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: status })
    ] })
  ] });
}
function toMarketEvent(value) {
  let side = value.eventType.toLowerCase().includes("short") ? "BUY" : "SELL";
  try {
    const payload = JSON.parse(value.payloadJson);
    const payloadSide = payload.side ?? payload.o?.S ?? value.address;
    if (payloadSide === "BUY" || payloadSide === "SELL") side = payloadSide;
  } catch {
  }
  return { id: value.eventId, symbol: value.symbol, side, occurredAt: value.occurredAt, amount: value.amount ?? 0, price: value.price ?? 0 };
}
function isRecord$2(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function NotebookPage() {
  const toast = useToast();
  const editor = reactExports.useRef(null);
  const [notes, setNotes] = reactExports.useState([]);
  const [selectedId, setSelectedId] = reactExports.useState();
  const [query, setQuery] = reactExports.useState("");
  const [draft, setDraft] = reactExports.useState();
  const [dirty, setDirty] = reactExports.useState(false);
  const [deleting, setDeleting] = reactExports.useState(false);
  const draftRef = reactExports.useRef(void 0);
  const dirtyRef = reactExports.useRef(false);
  const revisionRef = reactExports.useRef(0);
  const saveQueue = reactExports.useRef(Promise.resolve());
  const load = reactExports.useCallback(async (preferredId) => {
    const response = await bridge.core.invoke({ type: "notebook.list", payload: {} });
    if (!response.success) throw new Error(response.error?.message ?? "笔记读取失败。");
    const items = Array.isArray(response.payload) ? response.payload : [];
    setNotes(items);
    const id = preferredId ?? items[0]?.noteId;
    const next = items.find((item) => item.noteId === id);
    setSelectedId(id);
    setDraft(next);
    draftRef.current = next;
    setDirty(false);
    dirtyRef.current = false;
  }, []);
  reactExports.useEffect(() => {
    void load().catch((reason) => toast.show(String(reason), "error"));
  }, []);
  const persist = reactExports.useCallback(async (note, revision) => {
    const next = { ...note, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    const operation = saveQueue.current.then(async () => {
      const response = await bridge.core.invoke({ type: "notebook.save", payload: { note: next } });
      if (!response.success) throw new Error(response.error?.message ?? "笔记保存失败。");
      setNotes((current) => [...current.filter((item) => item.noteId !== next.noteId), next]);
      if (draftRef.current?.noteId === next.noteId && revisionRef.current === revision) {
        draftRef.current = next;
        setDraft(next);
        dirtyRef.current = false;
        setDirty(false);
      }
    });
    saveQueue.current = operation.catch(() => void 0);
    await operation;
    return next;
  }, []);
  const flushDraft = reactExports.useCallback(async () => {
    while (draftRef.current !== void 0 && dirtyRef.current) await persist(draftRef.current, revisionRef.current);
    await saveQueue.current;
  }, [persist]);
  reactExports.useEffect(() => {
    if (!dirty || draft === void 0) return;
    const timer = window.setTimeout(() => void flushDraft().catch((reason) => toast.show(String(reason), "error")), 600);
    return () => window.clearTimeout(timer);
  }, [dirty, draft, flushDraft, toast]);
  const create = async () => {
    await flushDraft();
    const previousNoteId = draftRef.current?.noteId;
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const note = { noteId: String(Date.now()), title: "", contentMarkdown: "<p><br></p>", contentPlainText: "", attachmentIds: [], isPinned: false, isDeleted: false, createdAt: now, updatedAt: now };
    revisionRef.current += 1;
    const saved = await persist(note, revisionRef.current);
    if (draftRef.current?.noteId === previousNoteId && dirtyRef.current) await flushDraft();
    setSelectedId(saved.noteId);
    setDraft(saved);
    draftRef.current = saved;
    setDirty(false);
    dirtyRef.current = false;
    editor.current?.focus();
  };
  const choose = async (note) => {
    await flushDraft();
    setSelectedId(note.noteId);
    setDraft(note);
    draftRef.current = note;
    setDirty(false);
    dirtyRef.current = false;
  };
  const patchDraft = (patch) => {
    const current = draftRef.current;
    if (current === void 0) return;
    const next = { ...current, ...patch };
    revisionRef.current += 1;
    draftRef.current = next;
    setDraft(next);
    dirtyRef.current = true;
    setDirty(true);
  };
  const importImages = async (files) => {
    for (const file of files) {
      const dataUrl = await readDataUrl(file);
      const response = await bridge.notebook.importData(file.name, dataUrl);
      const imported = response.payload;
      if (!response.success || imported === void 0 || imported === null) {
        toast.show(response.error?.message ?? "图片导入失败。", "error");
        continue;
      }
      editor.current?.insertHtml(`<img src="${escapeAttribute(imported.url)}" data-path="${escapeAttribute(imported.path)}" alt="${escapeAttribute(file.name)}">`);
      patchDraft({ attachmentIds: [...draft?.attachmentIds ?? [], imported.path] });
    }
  };
  const chooseImages = async () => {
    const response = await bridge.dialog.openFile([{ name: "图片文件", extensions: ["png", "jpg", "jpeg", "gif", "bmp", "webp"] }]);
    const paths = response.success ? readFilePaths(response.payload) : [];
    for (const path of paths) {
      const imported = await bridge.notebook.importFile(path);
      if (!imported.success || imported.payload === null) {
        toast.show(imported.error?.message ?? "图片导入失败。", "error");
        continue;
      }
      editor.current?.insertHtml(`<img src="${escapeAttribute(imported.payload.url)}" data-path="${escapeAttribute(imported.payload.path)}" alt="${escapeAttribute(imported.payload.name)}">`);
      patchDraft({ attachmentIds: [...draft?.attachmentIds ?? [], imported.payload.path] });
    }
  };
  const attachmentAction = async (action, path) => {
    const response = await bridge.notebook.imageAction(action, path);
    if (!response.success) toast.show(response.error?.message ?? "图片操作失败。", "error");
  };
  const removeAttachment = (path) => {
    if (draft === void 0) return;
    const document2 = new DOMParser().parseFromString(draft.contentMarkdown, "text/html");
    document2.querySelectorAll("img").forEach((image) => {
      if (image.getAttribute("data-path") === path) image.remove();
    });
    patchDraft({ contentMarkdown: document2.body.innerHTML, contentPlainText: document2.body.innerText, attachmentIds: draft.attachmentIds.filter((item) => item !== path) });
  };
  const deleteCurrent = async () => {
    if (draft === void 0) return;
    const response = await bridge.core.invoke({ type: "notebook.delete", payload: { noteId: draft.noteId } });
    if (!response.success) {
      toast.show(response.error?.message ?? "笔记删除失败。", "error");
      return;
    }
    setDeleting(false);
    setSelectedId(void 0);
    setDraft(void 0);
    draftRef.current = void 0;
    dirtyRef.current = false;
    setDirty(false);
    await load();
  };
  const filtered = notes.filter((note) => `${note.title} ${note.contentPlainText}`.toLowerCase().includes(query.toLowerCase())).sort((a, b) => Number(b.isPinned) - Number(a.isPinned) || b.updatedAt.localeCompare(a.updatedAt));
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "记事本", onClose: async () => {
      try {
        await flushDraft();
        await bridge.window.close();
      } catch (reason) {
        toast.show(`关闭前保存失败：${String(reason)}`, "error");
      }
    } }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(PageContent, { scroll: false, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "notebook-workspace", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Surface, { variant: "notebook-navigation", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "sm", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(SurfaceHeader, { title: "笔记", meta: /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { size: "xs", tone: "muted", children: [
              filtered.length,
              "/",
              notes.length
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(SearchBox, { "aria-label": "搜索笔记", value: query, onChange: setQuery }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void create(), children: "新建笔记" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "notebook-list", children: filtered.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: notes.length === 0 ? "暂无笔记" : "没有匹配的笔记" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ListBox, { label: "笔记列表", children: filtered.map((note) => /* @__PURE__ */ jsxRuntimeExports.jsx(ListBoxItem, { selected: note.noteId === selectedId, title: note.title || "无标题", description: `${note.contentPlainText || "暂无内容"} · ${new Date(note.updatedAt).toLocaleString("zh-CN")}`, badge: note.isPinned ? /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", children: "已置顶" }) : void 0, onSelect: () => void choose(note).catch((reason) => toast.show(String(reason), "error")) }, note.noteId)) }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Surface, { variant: "notebook-editor", children: draft === void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "选择或新建笔记" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "none", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "header", variant: "notebook-editor__header", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "笔记标题", value: draft.title, onChange: (event) => patchDraft({ title: event.target.value }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: dirty ? "secondary" : "muted", children: dirty ? "保存中…" : "已保存" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "notebook-toolbar", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void chooseImages(), children: "插入图片" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => patchDraft({ isPinned: !draft.isPinned }), children: draft.isPinned ? "取消置顶" : "置顶" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void navigator.clipboard.writeText(draft.contentPlainText), children: "复制正文" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void flushDraft().catch((reason) => toast.show(String(reason), "error")), children: "立即保存" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", onClick: () => setDeleting(true), children: "删除" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "notebook-editor__body", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "sm", tone: "secondary", children: "正文编辑区" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(RichTextEditor, { ref: editor, label: "笔记正文", value: draft.contentMarkdown, onChange: (html, text) => patchDraft({ contentMarkdown: html, contentPlainText: text }), onPasteFiles: (files) => void importImages(files) }),
            draft.attachmentIds.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "section", variant: "notebook-attachments", "aria-label": "图片附件", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "sm", tone: "secondary", children: "图片附件" }),
              draft.attachmentIds.map((path) => /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "notebook-attachment", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "muted", wrap: true, children: path }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void attachmentAction("copy", path), children: "复制" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void attachmentAction("openLocation", path), children: "打开位置" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void attachmentAction("saveAs", path), children: "另存为" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", onClick: () => removeAttachment(path), children: "移除" })
                ] })
              ] }, path))
            ] }) : null
          ] })
        ] }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog$1, { open: deleting, title: "删除笔记？", description: "删除后无法恢复。", confirmText: "删除", confirmVariant: "danger", onCancel: () => setDeleting(false), onConfirm: () => void deleteCurrent() })
    ] })
  ] });
}
function readDataUrl(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => {
      if (typeof reader.result === "string") resolve(reader.result);
      else reject(new Error("图片读取结果不是 Data URL。"));
    };
    reader.onerror = () => reject(reader.error ?? new Error("图片读取失败。"));
    reader.readAsDataURL(file);
  });
}
function escapeAttribute(value) {
  return value.replaceAll("&", "&amp;").replaceAll('"', "&quot;").replaceAll("<", "&lt;").replaceAll(">", "&gt;");
}
function readFilePaths(value) {
  return typeof value === "object" && value !== null && "filePaths" in value && Array.isArray(value.filePaths) ? value.filePaths.filter((item) => typeof item === "string") : [];
}
function VoiceConversationPage() {
  const toast = useToast();
  const [roles, setRoles] = reactExports.useState([]);
  const [roleId, setRoleId] = reactExports.useState("");
  const [conversations, setConversations] = reactExports.useState([]);
  const [conversationId, setConversationId] = reactExports.useState();
  const [messages, setMessages] = reactExports.useState([]);
  const [avatarUrls, setAvatarUrls] = reactExports.useState({});
  const [query, setQuery] = reactExports.useState("");
  const [text, setText] = reactExports.useState("");
  const [sending, setSending] = reactExports.useState(false);
  const [speech, setSpeech] = reactExports.useState(false);
  const [deleting, setDeleting] = reactExports.useState(false);
  const [recording, setRecording] = reactExports.useState(false);
  const [transcribing, setTranscribing] = reactExports.useState(false);
  const recorder = reactExports.useRef(void 0);
  const recordingStream = reactExports.useRef(void 0);
  const chunks = reactExports.useRef([]);
  const discardRecording = reactExports.useRef(false);
  const loadConversations = reactExports.useCallback(async (selectedRoleId, search = "") => {
    const response = await bridge.core.invoke({ type: "voice_conversation.list", payload: { roleId: selectedRoleId, ...search === "" ? {} : { search } } });
    if (!response.success) throw new Error(response.error?.message ?? "会话列表读取失败。");
    const items = Array.isArray(response.payload) ? response.payload : [];
    setConversations(items);
    const id = items.some((item) => item.conversationId === conversationId) ? conversationId : items[0]?.conversationId;
    setConversationId(id);
  }, [conversationId]);
  const loadMessages = reactExports.useCallback(async (id) => {
    if (id === void 0) {
      setMessages([]);
      return;
    }
    const response = await bridge.core.invoke({ type: "chat.history", payload: { conversationId: id, limit: 100 } });
    if (!response.success) throw new Error(response.error?.message ?? "历史消息读取失败。");
    const payload = response.payload;
    setMessages(payload?.messages ?? []);
  }, []);
  reactExports.useEffect(() => {
    void Promise.all([loadCharacters(), bridge.core.invoke({ type: "settings.get", payload: { keys: ["voice_conversation_center_speech"] } })]).then(async ([characters, setting]) => {
      setRoles(characters.items);
      const selected2 = characters.currentRoleId || characters.items[0]?.roleId || "";
      setRoleId(selected2);
      const payload = setting.payload;
      setSpeech(payload?.settings?.find((item) => item.key === "voice_conversation_center_speech")?.value.toLowerCase() === "true");
      if (selected2 !== "") await loadConversations(selected2);
    }).catch((reason) => toast.show(String(reason), "error"));
  }, []);
  reactExports.useEffect(() => {
    void Promise.all(roles.filter((item) => item.avatarPath !== "").map(async (item) => {
      const response = await bridge.media.registerLocalFile(item.avatarPath);
      if (!response.success) throw new Error(response.error?.message ?? `角色“${item.name}”头像读取失败。`);
      return [item.roleId, response.payload?.url ?? ""];
    })).then((entries) => setAvatarUrls(Object.fromEntries(entries))).catch((reason) => toast.show(reason instanceof Error ? reason.message : String(reason), "error"));
  }, [roles, toast]);
  reactExports.useEffect(() => {
    void loadMessages(conversationId).catch((reason) => toast.show(String(reason), "error"));
  }, [conversationId, loadMessages]);
  reactExports.useEffect(() => {
    const viewport = document.querySelector(".conversation-messages");
    if (viewport === null) return;
    const frame = requestAnimationFrame(() => {
      viewport.scrollTop = viewport.scrollHeight;
    });
    return () => cancelAnimationFrame(frame);
  }, [conversationId, messages]);
  reactExports.useEffect(() => () => {
    recorder.current?.stop();
    recordingStream.current?.getTracks().forEach((track) => track.stop());
  }, []);
  const selectRole = async (next) => {
    setRoleId(next);
    setConversationId(void 0);
    setMessages([]);
    await setCurrentCharacter(next);
    await loadConversations(next);
  };
  const openCharacterManager = async () => {
    const response = await bridge.window.open("characters");
    if (!response.success) toast.show(response.error?.message ?? "角色管理打开失败。", "error");
  };
  const createConversation = async (seed = "新对话") => {
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const conversation = { conversationId: `voice_chat_${crypto.randomUUID().replaceAll("-", "")}`, voiceRoleId: roleId, title: seed.slice(0, 24), preview: "", createdAt: now, updatedAt: now };
    const response = await bridge.core.invoke({ type: "voice_conversation.save", payload: { conversation } });
    if (!response.success) throw new Error(response.error?.message ?? "新建会话失败。");
    setConversations((current) => [conversation, ...current]);
    setConversationId(conversation.conversationId);
    setMessages([]);
    return conversation;
  };
  const send = async () => {
    const content2 = text.trim();
    if (content2 === "" || roleId === "" || sending) return;
    setText("");
    setSending(true);
    publishPetBubble("正在生成回复…", "processing", "think");
    try {
      const conversation = conversations.find((item) => item.conversationId === conversationId) ?? await createConversation(content2);
      const optimistic = { id: -Date.now(), conversationId: conversation.conversationId, role: "user", content: content2, characterId: roleId, modelName: "", source: "voice-conversation", metadataJson: "{}", createdAt: (/* @__PURE__ */ new Date()).toISOString() };
      setMessages((current) => [...current, optimistic]);
      const payload = await runAgentConversation(content2, { conversationId: conversation.conversationId, characterId: roleId, source: "voice_conversation_center" });
      const reply = payload.content.trim();
      await bridge.core.invoke({ type: "voice_conversation.save", payload: { conversation: { ...conversation, preview: reply, updatedAt: (/* @__PURE__ */ new Date()).toISOString() } } });
      await loadMessages(conversation.conversationId);
      await loadConversations(roleId, query);
      const role2 = roles.find((item) => item.roleId === roleId);
      if (speech && reply !== "" && role2?.preferredVoiceId !== void 0) {
        const paths = await synthesizeAndPlayPages(
          reply,
          role2.preferredVoiceId,
          (page) => publishPetBubble(page, "speech", actionTagForVoiceStyle(payload.voiceStyle))
        );
        if (payload.messageId > 0) await attachAudioMetadata(payload.messageId, paths, { voiceId: role2.preferredVoiceId, source: "voice_conversation_center" });
      } else if (reply !== "") {
        publishPetBubble(reply, "feedback", actionTagForVoiceStyle(payload.voiceStyle));
      }
    } catch (reason) {
      const message2 = reason instanceof Error ? reason.message : String(reason);
      publishPetBubble(message2, "error", "error");
      toast.show(message2, "error");
    } finally {
      setSending(false);
    }
  };
  const transcribe = async (audio) => {
    setTranscribing(true);
    publishPetBubble("正在识别语音…", "processing", "think");
    try {
      const dataUrl = await blobToDataUrl(audio);
      const imported = await bridge.speech.importAudioData(dataUrl);
      if (!imported.success || imported.payload === null) throw new Error(imported.error?.message ?? "录音保存失败。");
      const response = await bridge.core.invoke({ type: "asr.transcribe", payload: {
        audioPath: imported.payload.path,
        characterId: roleId,
        ...conversationId === void 0 ? {} : { sessionId: conversationId },
        language: "zh",
        requestId: `aimaid_${crypto.randomUUID().replaceAll("-", "")}`
      } }, 12e4);
      if (!response.success || typeof response.payload !== "string") throw new Error(response.error?.message ?? "语音识别失败。");
      const recognized = response.payload.trim();
      if (recognized === "") throw new Error("语音识别没有返回文字。");
      setText((current) => current.trim() === "" ? recognized : `${current.trimEnd()} ${recognized}`);
      publishPetBubble(`我听到：${recognized}`, "feedback", "listen");
      toast.show("语音已转成文字，请确认后发送。", "success");
    } finally {
      setTranscribing(false);
    }
  };
  const startRecording = async () => {
    if (roleId === "" || recording || transcribing) return;
    if (navigator.mediaDevices?.getUserMedia === void 0 || typeof MediaRecorder === "undefined") {
      publishPetBubble("当前系统不支持麦克风录音。", "error", "error");
      toast.show("当前系统不支持麦克风录音。", "error");
      return;
    }
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ audio: { echoCancellation: true, noiseSuppression: true }, video: false });
      const mimeType = MediaRecorder.isTypeSupported("audio/webm;codecs=opus") ? "audio/webm;codecs=opus" : "audio/webm";
      const next = new MediaRecorder(stream, { mimeType });
      chunks.current = [];
      discardRecording.current = false;
      recordingStream.current = stream;
      recorder.current = next;
      next.ondataavailable = (event) => {
        if (event.data.size > 0) chunks.current.push(event.data);
      };
      next.onerror = () => {
        publishPetBubble("麦克风录音失败，请检查系统权限。", "error", "error");
        toast.show("麦克风录音失败，请检查系统权限。", "error");
      };
      next.onstop = () => {
        stream.getTracks().forEach((track) => track.stop());
        recordingStream.current = void 0;
        recorder.current = void 0;
        setRecording(false);
        if (discardRecording.current) {
          chunks.current = [];
          return;
        }
        const audio = new Blob(chunks.current, { type: next.mimeType });
        chunks.current = [];
        if (audio.size === 0) {
          publishPetBubble("没有录到声音，请重试。", "error", "error");
          toast.show("没有录到声音，请重试。", "error");
          return;
        }
        void transcribe(audio).catch((reason) => {
          const message2 = reason instanceof Error ? reason.message : String(reason);
          publishPetBubble(message2, "error", "error");
          toast.show(message2, "error");
        });
      };
      next.start(500);
      setRecording(true);
      publishPetBubble("正在听主人说话…", "status", "listen");
    } catch (reason) {
      const message2 = reason instanceof Error ? reason.message : "无法访问麦克风，请检查系统权限。";
      recordingStream.current?.getTracks().forEach((track) => track.stop());
      publishPetBubble(message2, "error", "error");
      toast.show(message2, "error");
    }
  };
  const stopRecording = (discard = false) => {
    discardRecording.current = discard;
    if (recorder.current?.state === "recording") recorder.current.stop();
  };
  const saveConversation = async (conversation) => {
    const response = await bridge.core.invoke({ type: "voice_conversation.save", payload: { conversation } });
    if (!response.success) toast.show(response.error?.message ?? "会话标题保存失败。", "error");
  };
  const saveSpeechPreference = async (enabled) => {
    const response = await bridge.core.invoke({ type: "settings.save", payload: { values: { voice_conversation_center_speech: String(enabled) } } });
    if (!response.success) {
      setSpeech(!enabled);
      toast.show(response.error?.message ?? "语音播报设置保存失败。", "error");
    }
  };
  const deleteConversation = async () => {
    if (conversationId === void 0) return;
    const response = await bridge.core.invoke({ type: "voice_conversation.delete", payload: { conversationId } });
    if (!response.success) {
      toast.show(response.error?.message ?? "会话删除失败。", "error");
      return;
    }
    setDeleting(false);
    setConversationId(void 0);
    setMessages([]);
    await loadConversations(roleId, query);
  };
  const selected = conversations.find((item) => item.conversationId === conversationId);
  const role = roles.find((item) => item.roleId === roleId);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "角色对话中心" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(PageContent, { scroll: false, children: [
      roles.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "暂无可用角色", action: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void openCharacterManager(), children: "打开角色管理" }) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "conversation-workspace", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Surface, { variant: "conversation-navigation", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "md", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { "aria-label": "当前角色", value: roleId, options: roles.map((item) => ({ value: item.roleId, label: item.name })), onChange: (event) => void selectRole(event.target.value) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: roleId === "", onClick: () => void createConversation(), children: "新建会话" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(SearchBox, { "aria-label": "搜索会话", value: query, onChange: (value) => {
              setQuery(value);
              void loadConversations(roleId, value);
            } })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "conversation-list", children: conversations.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "暂无会话" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ListBox, { label: "会话列表", children: conversations.map((item) => {
            const conversationRole = roles.find((candidate) => candidate.roleId === item.voiceRoleId);
            return /* @__PURE__ */ jsxRuntimeExports.jsx(ListBoxItem, { selected: item.conversationId === conversationId, leading: /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { source: avatarUrls[item.voiceRoleId] ?? "", fallback: conversationRole?.name ?? item.voiceRoleId }), title: item.title, description: item.preview || void 0, onSelect: () => setConversationId(item.conversationId) }, item.conversationId);
          }) }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Surface, { variant: "conversation-detail", children: selected === void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "选择或新建会话" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "header", variant: "conversation-header", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { source: avatarUrls[roleId] ?? "", fallback: role?.name || "助手" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "xs", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "muted", children: role?.name ?? "助手" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "会话标题", value: selected.title, onChange: (event) => setConversations((current) => current.map((item) => item.conversationId === selected.conversationId ? { ...item, title: event.target.value } : item)), onBlur: () => void saveConversation(selected) })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "播报新回复", checked: speech, onChange: (event) => {
                const enabled = event.target.checked;
                setSpeech(enabled);
                if (!enabled) stopAudioPlayback();
                void saveSpeechPreference(enabled);
              } }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", size: "sm", onClick: () => setDeleting(true), children: "删除会话" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "conversation-messages", "aria-live": "polite", children: messages.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "还没有消息" }) : messages.map((message2) => {
            const fromUser = message2.role.toLowerCase() === "user";
            const messageRole = roles.find((item) => item.roleId === message2.characterId) ?? role;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "article", variant: fromUser ? "conversation-message conversation-message--user" : "conversation-message conversation-message--assistant", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { source: fromUser ? "" : avatarUrls[messageRole?.roleId ?? ""] ?? "", fallback: fromUser ? "你" : messageRole?.name ?? "助手" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Stack, { gap: "sm", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: fromUser ? "你" : messageRole?.name ?? "助手" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "muted", children: new Date(message2.createdAt).toLocaleString() })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { as: "p", wrap: true, children: message2.content })
              ] })
            ] }, message2.id);
          }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "footer", variant: "conversation-composer", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { "aria-label": "消息", value: text, onChange: (event) => setText(event.target.value), onKeyDown: (event) => {
              if (event.key === "Enter" && !event.shiftKey) {
                event.preventDefault();
                void send();
              }
            } }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { justify: "end", children: [
              recording || transcribing || sending ? /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "muted", children: recording ? "正在录音" : transcribing ? "正在识别语音…" : "正在生成回复…" }) : null,
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Inline, { children: [
                recording ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "ghost", onClick: () => stopRecording(true), children: "取消录音" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", onClick: () => stopRecording(), children: "停止并识别" })
                ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: transcribing || sending || roleId === "", loading: transcribing, onClick: () => void startRecording(), children: "语音转文字" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: sending, disabled: text.trim() === "" || recording || transcribing, onClick: () => void send(), children: "发送" })
              ] })
            ] })
          ] })
        ] }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog$1, { open: deleting, title: "删除会话？", description: "会话和历史消息将被删除。", confirmText: "删除", confirmVariant: "danger", onCancel: () => setDeleting(false), onConfirm: () => void deleteConversation() })
    ] })
  ] });
}
function blobToDataUrl(blob) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => typeof reader.result === "string" ? resolve(reader.result) : reject(new Error("录音编码失败。"));
    reader.onerror = () => reject(reader.error ?? new Error("录音编码失败。"));
    reader.readAsDataURL(blob);
  });
}
function actionTagForVoiceStyle(voiceStyle) {
  const normalized = voiceStyle.trim().toLowerCase();
  if (normalized === "lively") return "happy";
  if (normalized === "close") return "shy";
  if (normalized === "soft") return "smile";
  return "speak";
}
const FILTERS = ["清除筛选", "未播放", "播放中", "已看完", "收藏", "无标签", "文件丢失", "缩略图失败", "本地文件", "URL 视频", "mp4", "mkv", "mov", "avi"];
function VideoLibraryPage() {
  const clickTimer = reactExports.useRef(null);
  const pendingClickId = reactExports.useRef("");
  const [items, setItems] = reactExports.useState([]);
  const [albums, setAlbums] = reactExports.useState([]);
  const [tags, setTags] = reactExports.useState([]);
  const [ready, setReady] = reactExports.useState(false);
  const [status, setStatus] = reactExports.useState("正在读取视频库…");
  const [busy, setBusy] = reactExports.useState(false);
  const [view, setView] = reactExports.useState("全部视频");
  const [activeAlbumId, setActiveAlbumId] = reactExports.useState(null);
  const [query, setQuery] = reactExports.useState("");
  const [sort, setSort] = reactExports.useState("最近");
  const [selecting, setSelecting] = reactExports.useState(false);
  const [selectedIds, setSelectedIds] = reactExports.useState([]);
  const [dialog, setDialog] = reactExports.useState(null);
  const [dialogItemId, setDialogItemId] = reactExports.useState("");
  const [dialogValue, setDialogValue] = reactExports.useState("");
  const [dialogExtra, setDialogExtra] = reactExports.useState("");
  const [targetAlbumId, setTargetAlbumId] = reactExports.useState("");
  const [filter, setFilter] = reactExports.useState("全部");
  const [filterMenuOpen, setFilterMenuOpen] = reactExports.useState(false);
  const [moreMenuOpen, setMoreMenuOpen] = reactExports.useState(false);
  const [folderPath, setFolderPath] = reactExports.useState("");
  const [recursive, setRecursive] = reactExports.useState(false);
  const [pendingFilePaths, setPendingFilePaths] = reactExports.useState([]);
  const [selectedTag, setSelectedTag] = reactExports.useState("");
  const [sidebarMenu, setSidebarMenu] = reactExports.useState(null);
  const [videoMenu, setVideoMenu] = reactExports.useState(null);
  const [backgroundMenu, setBackgroundMenu] = reactExports.useState(null);
  const [dependencyText, setDependencyText] = reactExports.useState("");
  async function load(message2) {
    const response = await invokeVideo("video.list", {});
    if (!response.success) {
      showError(response.error?.message ?? "视频库读取失败");
      return;
    }
    const snapshot = readSnapshot(response.payload);
    setItems(snapshot.items);
    setAlbums(snapshot.albums);
    setTags(snapshot.tags);
    setSelectedIds((current) => current.filter((id) => snapshot.items.some((item) => item.videoId === id)));
    if (activeAlbumId !== null && !snapshot.albums.some((album) => album.albumId === activeAlbumId))
      setActiveAlbumId(null);
    setReady(true);
    setStatus(message2 ?? `已加载 ${snapshot.items.length} 个视频。`);
  }
  reactExports.useEffect(() => {
    void load();
  }, []);
  const visible = reactExports.useMemo(() => {
    const keyword = query.trim().toLocaleLowerCase();
    const filtered = items.filter((item) => {
      if (activeAlbumId !== null && item.albumId !== activeAlbumId)
        return false;
      if (activeAlbumId === null && view === "收藏" && !item.isFavorite)
        return false;
      if (activeAlbumId === null && view === "最近播放" && item.lastPlayedAt == null)
        return false;
      if (activeAlbumId === null && view === "未归档" && item.albumId != null)
        return false;
      if (filter === "未播放" && (item.lastPositionSeconds ?? 0) > 0)
        return false;
      if (filter === "播放中" && ((item.lastPositionSeconds ?? 0) <= 0 || item.isCompleted === true))
        return false;
      if (filter === "已看完" && item.isCompleted !== true)
        return false;
      if (filter === "收藏" && !item.isFavorite)
        return false;
      if (filter === "无标签" && item.tags.trim() !== "")
        return false;
      if (filter === "文件丢失" && (item.filePath === "" || item.sourceType !== "Missing"))
        return false;
      if (filter === "缩略图失败" && item.coverPath !== "")
        return false;
      if (filter === "本地文件" && item.filePath === "")
        return false;
      if (filter === "URL 视频" && item.filePath !== "")
        return false;
      if (["mp4", "mkv", "mov", "avi"].includes(filter) && !item.filePath.toLocaleLowerCase().endsWith(`.${filter}`))
        return false;
      const itemTags = splitTags(item.tags);
      if (selectedTag === "__none__" && itemTags.length > 0)
        return false;
      if (selectedTag !== "" && selectedTag !== "__all__" && selectedTag !== "__none__" && !itemTags.some((tag) => tag.localeCompare(selectedTag, "zh-CN", { sensitivity: "accent" }) === 0))
        return false;
      return keyword === "" || [item.title, item.filePath, item.tags, item.remark ?? ""].some((value) => value.toLocaleLowerCase().includes(keyword));
    });
    return [...filtered].sort((left, right) => sort === "文件名" ? (left.title || left.filePath).localeCompare(right.title || right.filePath, "zh-CN") : sort === "时长" ? (right.durationSeconds ?? 0) - (left.durationSeconds ?? 0) : sort === "大小" ? (right.fileSize ?? 0) - (left.fileSize ?? 0) : Date.parse(right.createdAt) - Date.parse(left.createdAt));
  }, [activeAlbumId, filter, items, query, selectedTag, sort, view]);
  reactExports.useEffect(() => () => {
    if (clickTimer.current !== null) clearTimeout(clickTimer.current);
  }, []);
  async function play(id, includeVisibleQueue = false) {
    const videoIds = includeVisibleQueue ? visible.map((item) => item.videoId) : [id];
    const response = await invokeVideo("video.play", { videoIds, startVideoId: id });
    if (!response.success) {
      showError(response.error?.message ?? "PotPlayer 启动失败。");
      return;
    }
    setStatus(includeVisibleQueue ? `已用 PotPlayer 播放当前列表（${videoIds.length} 项）。` : "已用 PotPlayer 播放当前视频。");
  }
  function invokeCard(id) {
    if (selecting) {
      setSelectedIds((current) => current.includes(id) ? current.filter((value) => value !== id) : [...current, id]);
      return;
    }
    if (clickTimer.current !== null && pendingClickId.current === id) {
      clearTimeout(clickTimer.current);
      clickTimer.current = null;
      pendingClickId.current = "";
      void play(id, true);
      return;
    }
    if (clickTimer.current !== null) {
      clearTimeout(clickTimer.current);
      void play(pendingClickId.current, false);
    }
    pendingClickId.current = id;
    clickTimer.current = setTimeout(() => {
      clickTimer.current = null;
      pendingClickId.current = "";
      void play(id, false);
    }, 500);
  }
  async function openSubtitles() {
    const response = await bridge.window.open("video-subtitles");
    if (!response.success)
      showError(response.error?.message ?? "字幕管理窗口打开失败。");
  }
  async function handleAction(action, id = "") {
    if (action === "refresh")
      await load();
    else if (action === "play")
      await play(id, false);
    else if (action === "select") {
      setSelecting(true);
      setSelectedIds((current) => current.includes(id) ? current : [...current, id]);
    } else if (action === "importFile")
      await chooseFiles();
    else if (action === "importFolder")
      setDialog("import-folder");
    else if (action === "newAlbum")
      openSimpleDialog("album-create");
    else if (action === "copyPath") {
      const item = items.find((entry) => entry.videoId === id);
      if (item !== void 0)
        await navigator.clipboard.writeText(item.filePath || item.originalUrl);
    } else if (action === "openLocation") {
      const item = items.find((entry) => entry.videoId === id);
      if (item?.filePath)
        await bridge.shell.showItemInFolder(item.filePath);
    } else if (action === "rename")
      openItemDialog("rename-video", id, items.find((entry) => entry.videoId === id)?.title ?? "");
    else if (action === "editTag")
      openItemDialog("edit-tags", id, items.find((entry) => entry.videoId === id)?.tags ?? "");
    else if (action === "moveToAlbum")
      openItemDialog("batch-move", id);
    else if (action === "removeFromAlbum")
      openItemDialog("remove-album-item", id);
    else if (action === "remove")
      openItemDialog("remove-record", id);
    else if (action === "deleteLocal")
      openItemDialog("delete-local", id);
    else if (action === "regenerateCover")
      await run("video.refresh_metadata", { videoIds: [id] }, "视频元数据已刷新。");
  }
  function openSimpleDialog(next, value = "", extra = "") {
    setDialogItemId("");
    setDialogValue(value);
    setDialogExtra(extra);
    if (next === "batch-move")
      setTargetAlbumId(activeAlbumId ?? "");
    setDialog(next);
  }
  function openItemDialog(next, id, value = "") {
    setDialogItemId(id ?? "");
    setDialogValue(value);
    setDialogExtra("");
    setTargetAlbumId("");
    setDialog(next);
  }
  function closeDialog() {
    if (!busy)
      setDialog(null);
  }
  function exitSelection() {
    setSelecting(false);
    setSelectedIds([]);
    setMoreMenuOpen(false);
  }
  function targetIds() {
    return dialogItemId !== "" ? [dialogItemId] : selectedIds;
  }
  function showError(message2) {
    setStatus(message2);
  }
  async function run(type, payload, successMessage, close = false) {
    setBusy(true);
    try {
      const response = await invokeVideo(type, payload);
      if (!response.success) {
        showError(response.error?.message ?? "操作失败。");
        return false;
      }
      if (close)
        setDialog(null);
      await load(successMessage);
      return true;
    } finally {
      setBusy(false);
    }
  }
  async function chooseFolder() {
    const response = await bridge.dialog.openDirectory();
    const path = response.success ? response.payload?.filePaths[0] : void 0;
    if (path !== void 0)
      setFolderPath(path);
  }
  async function chooseFiles() {
    const response = await bridge.dialog.openFile([{ name: "视频文件", extensions: ["mp4", "mkv", "mov", "avi", "wmv", "flv", "webm", "m4v", "ts", "m2ts"] }], true);
    const paths = response.success ? readPaths$2(response.payload) : [];
    if (paths.length === 0)
      return;
    setPendingFilePaths(paths);
    setTargetAlbumId(activeAlbumId ?? "");
    setDialog("import-target");
  }
  async function importFiles() {
    setBusy(true);
    try {
      let imported = 0;
      for (const filePath of pendingFilePaths) {
        const response = await invokeVideo("video.import_file", { filePath, albumId: targetAlbumId || null });
        if (!response.success) {
          showError(response.error?.message ?? `导入失败：${filePath}`);
          return;
        }
        imported += 1;
      }
      setPendingFilePaths([]);
      setDialog(null);
      await load(`导入完成，共添加 ${imported} 个视频。`);
    } finally {
      setBusy(false);
    }
  }
  async function importFolder() {
    setBusy(true);
    try {
      const response = await invokeVideo("video.import_folder", { folderPath, recursive, albumId: targetAlbumId || null });
      if (!response.success) {
        showError(response.error?.message ?? "文件夹导入失败。");
        return;
      }
      const count = readNumber(response.payload, "importedCount");
      setFolderPath("");
      setDialog(null);
      await load(`扫描完成，共添加 ${count} 个视频。`);
    } finally {
      setBusy(false);
    }
  }
  async function loadDependencies() {
    const response = await invokeVideo("video.dependencies", {});
    if (!response.success) {
      showError(response.error?.message ?? "视频依赖检测失败。");
      return;
    }
    const rows = readDependencies(response.payload);
    setDependencyText(rows.length === 0 ? "没有返回依赖检测结果。" : rows.map((item) => `${item.name}：${item.available ? item.path || "可用" : "不可用"}`).join("\n"));
    setDialog("dependencies");
  }
  const counts = {
    "全部视频": items.length,
    "最近播放": items.filter((item) => item.lastPlayedAt != null).length,
    "未归档": items.filter((item) => item.albumId == null).length,
    "收藏": items.filter((item) => item.isFavorite).length
  };
  const dialogItem = items.find((item) => item.videoId === dialogItemId);
  const dialogItemName = dialogItem?.title || dialogItem?.filePath.split(/[\\/]/).at(-1) || "当前视频";
  const currentTitle = activeAlbumId === null ? view : albums.find((album) => album.albumId === activeAlbumId)?.name ?? "专辑";
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: query.trim() === "" ? `${currentTitle} ${visible.length} 个视频` : `搜索结果 ${visible.length} 个视频` }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "media", "data-product": "video-library", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductSidebar, { title: "视频导航", description: `${items.length} 个视频 · ${albums.length} 个专辑`, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductList, { compact: true, children: ["全部视频", "最近播放", "未归档", "收藏"].map((name) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { "data-video-navigation-item": true, selected: activeAlbumId === null && selectedTag === "" && view === name, onClick: () => {
          setActiveAlbumId(null);
          setSelectedTag("");
          setView(name);
        }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: counts[name] })
        ] }, name)) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Header, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "专辑" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "ghost", onClick: () => openSimpleDialog("album-create"), children: "新建" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ProductList, { compact: true, children: albums.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "暂无专辑" }) : albums.map((album) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { "data-video-navigation-item": true, selected: activeAlbumId === album.albumId, onClick: () => {
            setActiveAlbumId(album.albumId);
            setSelectedTag("");
            setView("全部视频");
          }, onContextMenu: (event) => {
            event.preventDefault();
            setSidebarMenu({ kind: "album", id: album.albumId, x: event.clientX, y: event.clientY });
          }, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: album.name }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: items.filter((item) => item.albumId === album.albumId).length })
          ] }, album.albumId)) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Header, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "标签" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "ghost", onClick: () => openSimpleDialog("tag-create"), children: "新建" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductList, { compact: true, children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { "data-video-navigation-item": true, selected: selectedTag === "__all__", onClick: () => {
              setActiveAlbumId(null);
              setView("全部视频");
              setSelectedTag("__all__");
            }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "全部标签" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: items.length })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { "data-video-navigation-item": true, selected: selectedTag === "__none__", onClick: () => {
              setActiveAlbumId(null);
              setView("全部视频");
              setSelectedTag("__none__");
            }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "无标签" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: items.filter((item) => splitTags(item.tags).length === 0).length })
            ] }),
            tags.map((tag) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { "data-video-navigation-item": true, selected: selectedTag === tag, onClick: () => {
              setActiveAlbumId(null);
              setView("全部视频");
              setSelectedTag(tag);
            }, onContextMenu: (event) => {
              event.preventDefault();
              setSidebarMenu({ kind: "tag", id: tag, x: event.clientX, y: event.clientY });
            }, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: tag }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: items.filter((item) => splitTags(item.tags).some((value) => value.toLocaleLowerCase() === tag.toLocaleLowerCase())).length })
            ] }, tag))
          ] })
        ] }),
        sidebarMenu !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContextMenuSurface, { label: sidebarMenu.kind === "album" ? "专辑操作" : "标签操作", position: sidebarMenu, onClose: () => setSidebarMenu(null), items: sidebarMenu.kind === "album" ? [
          { id: "rename", label: "重命名专辑", onSelect: () => {
            const album = albums.find((item) => item.albumId === sidebarMenu.id);
            if (album !== void 0)
              openSimpleDialog("album-rename", album.name, album.albumId);
          } },
          { id: "delete", label: "删除专辑", danger: true, onSelect: () => {
            const album = albums.find((item) => item.albumId === sidebarMenu.id);
            if (album !== void 0)
              openSimpleDialog("album-delete", album.name, album.albumId);
          } }
        ] : [
          { id: "add", label: "添加标签", onSelect: () => openSimpleDialog("tag-create") },
          { id: "rename", label: "重命名标签", onSelect: () => openSimpleDialog("tag-rename", sidebarMenu.id, sidebarMenu.id) },
          { id: "delete", label: "删除标签", danger: true, onSelect: () => openSimpleDialog("tag-delete", sidebarMenu.id) }
        ] }) : null
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPanel, { footer: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: busy ? "正在处理…" : status }), scroll: true, emphasis: true, children: [
        !selecting ? /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "搜索视频", value: query, onChange: (event) => setQuery(event.target.value) }), actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          ["最近", "文件名", "时长", "大小"].map((name) => /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: sort === name ? "primary" : "ghost", onClick: () => setSort(name), children: name }, name)),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Menu, { open: filterMenuOpen, label: "视频筛选", onClose: () => setFilterMenuOpen(false), items: FILTERS.map((name) => ({ id: name, label: name, onSelect: () => setFilter(name === "清除筛选" ? "全部" : name) })), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => setFilterMenuOpen((value) => !value), children: filter === "全部" ? "筛选" : filter }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void openSubtitles(), children: "字幕" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void loadDependencies(), children: "依赖" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => setSelecting(true), children: "批量选择" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "primary", onClick: () => void chooseFiles(), children: "导入文件" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => {
            setTargetAlbumId(activeAlbumId ?? "");
            setDialog("import-folder");
          }, children: "导入文件夹" })
        ] }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
            "已选择 ",
            selectedIds.length,
            " 项"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => setSelectedIds(visible.map((item) => item.videoId)), children: "全选" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: exitSelection, children: "取消选择" })
        ] }), actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: selectedIds.length === 0, onClick: () => openSimpleDialog("batch-tags"), children: "添加标签" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: selectedIds.length === 0, onClick: () => openSimpleDialog("batch-move"), children: "移动到专辑" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: selectedIds.length === 0, onClick: () => openSimpleDialog("batch-remove-album"), children: "从专辑移除" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", disabled: selectedIds.length === 0, onClick: () => openSimpleDialog("batch-remove"), children: "删除记录" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Menu, { open: moreMenuOpen, label: "更多操作", onClose: () => setMoreMenuOpen(false), items: [
            { id: "delete-local", label: "删除本地文件...", danger: true, onSelect: () => openSimpleDialog("batch-delete-local") },
            { id: "refresh", label: "重新生成缩略图", onSelect: () => {
              void run("video.refresh_metadata", { videoIds: selectedIds }, "视频元数据已刷新。");
            } },
            { id: "favorite", label: "切换收藏", onSelect: () => {
              void Promise.all(selectedIds.map((videoId) => invokeVideo("video.toggle_favorite", { videoId }))).then(() => load("收藏状态已更新。"));
            } },
            { id: "remark", label: "编辑备注", disabled: selectedIds.length !== 1, onSelect: () => {
              if (selectedIds.length === 1)
                openItemDialog("edit-remark", selectedIds[0], items.find((item) => item.videoId === selectedIds[0])?.remark ?? "");
            } },
            { id: "copy", label: "复制文件路径", onSelect: () => {
              void navigator.clipboard.writeText(items.filter((item) => selectedIds.includes(item.videoId)).map((item) => item.filePath).filter(Boolean).join("\n"));
            } }
          ], children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => setMoreMenuOpen((value) => !value), children: "更多" }) })
        ] }) }),
        !ready ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "正在读取视频库" }) : visible.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(
          EmptyState,
          {
            title: items.length === 0 ? "视频库还是空的" : "没有匹配的视频",
            action: items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void chooseFiles(), children: "导入第一个视频" }) : void 0
          }
        ) : /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "video-library-grid", onContextMenu: (event) => {
          if (event.target === event.currentTarget) {
            event.preventDefault();
            setBackgroundMenu({ x: event.clientX, y: event.clientY });
          }
        }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductGrid, { density: "cards", children: visible.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Pressable,
          {
            appearance: "card",
            selected: selectedIds.includes(item.videoId),
            "aria-label": item.title || item.filePath,
            onClick: () => invokeCard(item.videoId),
            onDoubleClick: (event) => event.preventDefault(),
            onKeyDown: (event) => {
              if (event.key === "Enter" && !selecting) {
                event.preventDefault();
                if (clickTimer.current !== null) clearTimeout(clickTimer.current);
                clickTimer.current = null;
                pendingClickId.current = "";
                void play(item.videoId, true);
              }
            },
            onContextMenu: (event) => {
              event.preventDefault();
              event.stopPropagation();
              setVideoMenu({ id: item.videoId, x: event.clientX, y: event.clientY });
            },
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "video-library-card__cover", children: [
                coverSource(item) !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(MediaImage, { src: coverSource(item), alt: "" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "VIDEO" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: formatDuration$1(item.durationSeconds ?? 0) })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "video-library-card__copy", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.title || item.filePath.split(/[\\/]/).at(-1) || "未命名视频" }),
                [item.tags, item.remark ?? ""].filter(Boolean).join(" · ") !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: [item.tags, item.remark ?? ""].filter(Boolean).join(" · ") }) : null,
                item.isFavorite ? /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: "收藏" }) : item.isCompleted ? /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "success", children: "已看完" }) : null
              ] })
            ]
          },
          item.videoId
        )) }) })
      ] })
    ] }),
    videoMenu !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContextMenuSurface, { label: "视频操作", position: videoMenu, onClose: () => setVideoMenu(null), items: [
      { id: "play", label: "播放", onSelect: () => {
        void handleAction("play", videoMenu.id);
      } },
      { id: "open", label: "打开所在位置", onSelect: () => {
        void handleAction("openLocation", videoMenu.id);
      } },
      { id: "copy", label: "复制文件路径", onSelect: () => {
        void handleAction("copyPath", videoMenu.id);
      } },
      { id: "tag", label: "添加标签…", separated: true, onSelect: () => {
        void handleAction("editTag", videoMenu.id);
      } },
      { id: "move", label: "移动到专辑…", onSelect: () => {
        void handleAction("moveToAlbum", videoMenu.id);
      } },
      { id: "unarchive", label: "从当前专辑移除", disabled: items.find((item) => item.videoId === videoMenu.id)?.albumId == null, onSelect: () => {
        void handleAction("removeFromAlbum", videoMenu.id);
      } },
      { id: "select", label: "选择", separated: true, onSelect: () => {
        void handleAction("select", videoMenu.id);
      } },
      { id: "cover", label: "重新生成缩略图", onSelect: () => {
        void handleAction("regenerateCover", videoMenu.id);
      } },
      { id: "rename", label: "重命名显示名", onSelect: () => {
        void handleAction("rename", videoMenu.id);
      } },
      { id: "remove", label: "删除库记录", separated: true, danger: true, onSelect: () => {
        void handleAction("remove", videoMenu.id);
      } },
      { id: "delete", label: "删除本地文件…", danger: true, disabled: !items.find((item) => item.videoId === videoMenu.id)?.filePath, onSelect: () => {
        void handleAction("deleteLocal", videoMenu.id);
      } }
    ] }) : null,
    backgroundMenu !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContextMenuSurface, { label: "视频库操作", position: backgroundMenu, onClose: () => setBackgroundMenu(null), items: [
      { id: "import-file", label: "导入文件", onSelect: () => {
        void chooseFiles();
      } },
      { id: "import-folder", label: "导入文件夹", onSelect: () => setDialog("import-folder") },
      { id: "new-album", label: "新建专辑", separated: true, onSelect: () => openSimpleDialog("album-create") },
      { id: "refresh", label: "刷新视频库", onSelect: () => {
        void load();
      } }
    ] }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx(TextDialog, { open: dialog === "album-create", title: "新建专辑", label: "请输入专辑名称：", value: dialogValue, setValue: setDialogValue, busy, close: closeDialog, confirm: () => void run("video.album.create", { name: dialogValue.trim(), description: "" }, "专辑已创建。", true) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TextDialog, { open: dialog === "album-rename", title: "重命名专辑", label: "请输入新的专辑名称：", value: dialogValue, setValue: setDialogValue, busy, close: closeDialog, confirm: () => void run("video.album.rename", { albumId: dialogExtra, name: dialogValue.trim() }, "专辑已重命名。", true) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "album-delete", title: "删除专辑", busy, close: closeDialog, confirm: () => void run("video.album.delete", { albumId: dialogExtra }, "专辑已删除，视频已移到“未归档”。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "删除专辑“",
      dialogValue,
      "”？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "专辑中的视频不会从本地磁盘删除，可在“未归档”中找到。"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TextDialog, { open: dialog === "tag-create", title: "添加标签", label: "输入标签名称：", value: dialogValue, setValue: setDialogValue, busy, close: closeDialog, confirm: () => void run("video.tag.create", { tag: dialogValue.trim() }, "标签已添加。", true) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TextDialog, { open: dialog === "tag-rename", title: "重命名标签", label: "请输入新的标签名称：", value: dialogValue, setValue: setDialogValue, busy, close: closeDialog, confirm: () => void run("video.tag.rename", { oldTag: dialogExtra, newTag: dialogValue.trim() }, "标签已重命名。", true) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "tag-delete", title: "删除标签", busy, close: closeDialog, confirm: () => void run("video.tag.delete", { tag: dialogValue }, "标签已删除。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "删除标签“",
      dialogValue,
      "”？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "视频文件不会被删除。"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: dialog === "import-target", title: "选择目标专辑", description: `即将导入 ${pendingFilePaths.length} 个文件。`, onClose: closeDialog, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: closeDialog, children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy || pendingFilePaths.length === 0, onClick: () => void importFiles(), children: "导入" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(AlbumSelector, { albums, value: targetAlbumId, setValue: setTargetAlbumId }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: dialog === "import-folder", title: "导入文件夹", onClose: closeDialog, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: closeDialog, children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy || folderPath === "", onClick: () => void importFolder(), children: "导入" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "文件夹路径", readOnly: true, value: folderPath }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void chooseFolder(), children: "浏览" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(AlbumSelector, { albums, value: targetAlbumId, setValue: setTargetAlbumId }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "包含子文件夹", checked: recursive, onChange: (event) => setRecursive(event.target.checked) })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TextDialog, { open: dialog === "rename-video", title: "重命名显示名", label: "请输入新的显示名：", value: dialogValue, setValue: setDialogValue, busy, close: closeDialog, confirm: () => void run("video.set_display_name", { videoId: dialogItemId, displayName: dialogValue.trim() }, "显示名已更新。", true) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(TextDialog, { open: dialog === "edit-remark", title: "编辑备注", label: "备注：", value: dialogValue, setValue: setDialogValue, busy, close: closeDialog, allowEmpty: true, confirm: () => void run("video.set_remark", { videoId: dialogItemId, remark: dialogValue.trim() }, "备注已更新。", true) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: dialog === "edit-tags" || dialog === "batch-tags", title: dialog === "batch-tags" ? "批量添加标签" : "添加标签", onClose: closeDialog, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: closeDialog, children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy, onClick: () => void run("video.tag.set", { videoIds: targetIds(), tags: normalizeTagText(dialogValue) }, "标签已更新。", true), children: "确定" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "标签", value: dialogValue, onChange: (event) => setDialogValue(event.target.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: tags.map((tag) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { onClick: () => setDialogValue(normalizeTagText([dialogValue, tag].filter(Boolean).join(","))), children: tag }, tag)) })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: dialog === "batch-move", title: "选择目标专辑", onClose: closeDialog, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: closeDialog, children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy || targetIds().length === 0, onClick: () => void run("video.album.move", { videoIds: targetIds(), albumId: targetAlbumId || null }, "视频已移动。", true), children: "确定" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(AlbumSelector, { albums, value: targetAlbumId, setValue: setTargetAlbumId }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "remove-record", title: "删除库记录", busy, close: closeDialog, confirm: () => void run("video.remove_records", { videoIds: [dialogItemId] }, "库记录已删除。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "删除“",
      dialogItemName,
      "”的库记录？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "本地视频文件不会被删除。"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "delete-local", title: "删除本地文件", busy, close: closeDialog, confirm: () => void run("video.delete_local_files", { videoIds: [dialogItemId] }, "本地文件已移入回收站，库记录已删除。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "确定要删除本地视频文件？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "该文件将被移入系统回收站，同时会从视频库移除。",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      dialogItemName
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "remove-album-item", title: "从专辑移除", busy, close: closeDialog, confirm: () => void run("video.album.move", { videoIds: [dialogItemId], albumId: null }, "视频已移到“未归档”。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "将“",
      dialogItemName,
      "”从当前专辑移除？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "视频不会从本地磁盘删除，可在“未归档”中找到。"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "batch-remove", title: "删除库记录", busy, close: closeDialog, confirm: () => void run("video.remove_records", { videoIds: selectedIds }, "库记录已删除。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "删除 ",
      selectedIds.length,
      " 个视频的库记录？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "本地视频文件不会被删除。"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "batch-remove-album", title: "从专辑移除", busy, close: closeDialog, confirm: () => void run("video.album.move", { videoIds: selectedIds, albumId: null }, "视频已移到“未归档”。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "将 ",
      selectedIds.length,
      " 个视频从当前专辑移除？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "视频不会从本地磁盘删除，可在“未归档”中找到。"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog, { open: dialog === "batch-delete-local", title: "删除本地文件", busy, close: closeDialog, confirm: () => void run("video.delete_local_files", { videoIds: selectedIds }, "本地文件已移入回收站，库记录已删除。", true), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "确定要删除本地视频文件？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "这些文件将被移入系统回收站，同时会从视频库移除。",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "文件数量：",
      selectedIds.length
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: dialog === "dependencies", title: "视频依赖检测", onClose: closeDialog, footer: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: closeDialog, children: "确定" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(CodeBlock, { children: dependencyText }) })
  ] });
}
function TextDialog({ open, title, label, value, setValue, busy, close, confirm, allowEmpty = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open, title, onClose: close, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: close, children: "取消" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy || !allowEmpty && value.trim() === "", onClick: confirm, children: "确定" })
  ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label, value, onChange: (event) => setValue(event.target.value), autoFocus: true }) });
}
function ConfirmDialog({ open, title, busy, close, confirm, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open, title, onClose: close, footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: close, children: "取消" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", disabled: busy, onClick: confirm, children: "删除" })
  ] }), children });
}
function AlbumSelector({ albums, value, setValue }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "目标专辑", value, options: [{ value: "", label: "未归档" }, ...albums.map((album) => ({ value: album.albumId, label: album.name }))], onChange: (event) => setValue(event.target.value) });
}
function coverSource(item) {
  if (item.coverPath === "")
    return "";
  if (/^https?:/i.test(item.coverPath))
    return item.coverPath;
  return `file:///${item.coverPath.replaceAll("\\", "/")}`;
}
function formatDuration$1(seconds) {
  const value = Math.max(0, Math.round(seconds));
  return `${Math.floor(value / 60).toString().padStart(2, "0")}:${(value % 60).toString().padStart(2, "0")}`;
}
function readPaths$2(value) {
  return isRecord$1(value) && Array.isArray(value.filePaths) ? value.filePaths.filter((item) => typeof item === "string") : [];
}
function splitTags(value) {
  return value.replaceAll("，", ",").split(",").map((tag) => tag.trim()).filter((tag, index, values) => tag !== "" && values.findIndex((item) => item.toLocaleLowerCase() === tag.toLocaleLowerCase()) === index);
}
function normalizeTagText(value) {
  return splitTags(value).join(",");
}
function readSnapshot(value) {
  if (!isRecord$1(value))
    return { items: [], albums: [], tags: [] };
  return { items: Array.isArray(value.items) ? value.items : [], albums: Array.isArray(value.albums) ? value.albums : [], tags: Array.isArray(value.tags) ? value.tags.filter((tag) => typeof tag === "string") : [] };
}
function readDependencies(value) {
  const rows = Array.isArray(value) ? value : isRecord$1(value) ? Object.entries(value).map(([name, item]) => isRecord$1(item) ? { name, ...item } : { name, path: typeof item === "string" ? item : "", available: Boolean(item) }) : [];
  return rows.filter(isRecord$1).map((item) => ({ name: typeof item.name === "string" ? item.name : "工具", path: typeof item.path === "string" ? item.path : typeof item.Path === "string" ? item.Path : "", available: typeof item.available === "boolean" ? item.available : item.Available === true }));
}
function readNumber(value, name) {
  return isRecord$1(value) && typeof value[name] === "number" ? value[name] : 0;
}
function isRecord$1(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
async function invokeVideo(type, payload) {
  return bridge.core.invoke({ type, payload });
}
const VIDEO_PROGRESS_INTERVAL_MS = 7e3;
const VIDEO_PROGRESS_RETRY_MS = 2500;
function restoredPlaybackPosition(lastPositionSeconds, durationSeconds, isCompleted) {
  const position = normalizeSeconds(lastPositionSeconds);
  const duration = normalizeSeconds(durationSeconds);
  if (position === 0 || duration === 0) return 0;
  if (isCompleted || duration - position <= Math.min(20, duration * 0.05)) return 0;
  return Math.min(position, duration);
}
class VideoProgressSession {
  constructor(persist, onStatus, now = Date.now) {
    this.persist = persist;
    this.onStatus = onStatus;
    this.now = now;
  }
  persist;
  onStatus;
  now;
  latest = null;
  lastAttemptAt = Number.NEGATIVE_INFINITY;
  inFlight = false;
  flushAfterFlight = false;
  retryTimer;
  disposed = false;
  update(positionSeconds, durationSeconds) {
    if (this.disposed) return;
    this.latest = normalizedSnapshot(positionSeconds, durationSeconds);
    if (this.now() - this.lastAttemptAt >= VIDEO_PROGRESS_INTERVAL_MS) void this.persistLatest();
  }
  flush(positionSeconds, durationSeconds) {
    if (positionSeconds !== void 0 && durationSeconds !== void 0) {
      this.latest = normalizedSnapshot(positionSeconds, durationSeconds);
    }
    this.clearRetry();
    if (this.inFlight) {
      this.flushAfterFlight = true;
      return;
    }
    if (this.latest !== null) void this.persistLatest(true);
  }
  dispose(positionSeconds, durationSeconds) {
    this.flush(positionSeconds, durationSeconds);
    this.disposed = true;
  }
  async persistLatest(force = false) {
    if (this.latest === null || this.inFlight || this.disposed && !force) return;
    const snapshot = this.latest;
    this.inFlight = true;
    this.lastAttemptAt = this.now();
    try {
      const response = await this.persist(snapshot);
      if (!response.success) {
        this.onStatus(response.error?.message === void 0 ? "播放进度保存失败，将自动重试。" : `播放进度保存失败：${response.error.message}，将自动重试。`);
        this.scheduleRetry();
        return;
      }
      if (this.latest === snapshot) this.latest = null;
      this.onStatus("");
    } catch (reason) {
      const message2 = reason instanceof Error ? reason.message : String(reason);
      this.onStatus(`播放进度保存失败：${message2}，将自动重试。`);
      this.scheduleRetry();
    } finally {
      this.inFlight = false;
      if (this.flushAfterFlight) {
        this.flushAfterFlight = false;
        void this.persistLatest(true);
      }
    }
  }
  scheduleRetry() {
    if (this.retryTimer !== void 0 || this.disposed) return;
    this.retryTimer = setTimeout(() => {
      this.retryTimer = void 0;
      void this.persistLatest(true);
    }, VIDEO_PROGRESS_RETRY_MS);
  }
  clearRetry() {
    if (this.retryTimer === void 0) return;
    clearTimeout(this.retryTimer);
    this.retryTimer = void 0;
  }
}
function normalizedSnapshot(positionSeconds, durationSeconds) {
  return { positionSeconds: normalizeSeconds(positionSeconds), durationSeconds: normalizeSeconds(durationSeconds) };
}
function normalizeSeconds(value) {
  return Number.isFinite(value) ? Math.max(0, Math.floor(value)) : 0;
}
function VideoPlayerPage() {
  const [item] = reactExports.useState(() => readPlayback());
  const [source, setSource] = reactExports.useState("");
  const [error, setError] = reactExports.useState("");
  const [progressStatus, setProgressStatus] = reactExports.useState("");
  const playerRef = reactExports.useRef(null);
  const progressSessionRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    void resolveSource(item).then((value) => {
      if (value === "") setError("这条视频记录没有可直接播放的媒体地址。");
      else setSource(value);
    }).catch((reason) => setError(reason instanceof Error ? reason.message : "本地视频读取失败。"));
  }, [item]);
  reactExports.useEffect(() => {
    if (item === null) return;
    const session = new VideoProgressSession(
      ({ positionSeconds, durationSeconds }) => bridge.core.invoke({ type: "video.update_progress", payload: { videoId: item.videoId, positionSeconds, durationSeconds } }),
      setProgressStatus
    );
    progressSessionRef.current = session;
    const flush = () => {
      const player = playerRef.current;
      if (player !== null) session.flush(player.currentTime, player.duration);
    };
    window.addEventListener("beforeunload", flush);
    window.addEventListener("pagehide", flush);
    return () => {
      window.removeEventListener("beforeunload", flush);
      window.removeEventListener("pagehide", flush);
      const player = playerRef.current;
      session.dispose(player?.currentTime, player?.duration);
      progressSessionRef.current = null;
    };
  }, [item]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: item?.title || item?.filePath.split(/[\\/]/).at(-1) || "视频播放" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: item?.title || "视频播放", description: item?.filePath || item?.originalUrl || "正在读取媒体地址", emphasis: true, scroll: true, children: error !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "error", title: "无法播放", children: error }) : source === "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "正在读取视频…" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(VideoPlayer, { ref: playerRef, source, autoPlay: true, onError: () => setError("当前视频格式或编码暂不受支持。"), onLoadedMetadata: (event) => {
        if (item === null) return;
        event.currentTarget.currentTime = restoredPlaybackPosition(item.lastPositionSeconds, event.currentTarget.duration, item.isCompleted);
      }, onTimeUpdate: (event) => progressSessionRef.current?.update(event.currentTarget.currentTime, event.currentTarget.duration), onPause: (event) => progressSessionRef.current?.flush(event.currentTarget.currentTime, event.currentTarget.duration), onEnded: (event) => progressSessionRef.current?.flush(event.currentTarget.currentTime, event.currentTarget.duration) }) }),
      progressStatus !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: progressStatus }) : null
    ] })
  ] });
}
function VideoSubtitlesPage() {
  const [items, setItems] = reactExports.useState([]);
  const [selectedPath, setSelectedPath] = reactExports.useState("");
  const [status, setStatus] = reactExports.useState("正在读取字幕资源…");
  const [deleteOpen, setDeleteOpen] = reactExports.useState(false);
  async function load(nextStatus) {
    const response = await bridge.core.invoke({ type: "subtitle.list", payload: {} });
    if (!response.success || !Array.isArray(response.payload)) {
      setStatus(response.error?.message ?? "字幕资源读取失败。");
      return;
    }
    const next = response.payload.filter(isSubtitleItem);
    setItems(next);
    if (!next.some((item) => item.path === selectedPath))
      setSelectedPath("");
    setStatus(nextStatus ?? (next.length === 0 ? "字幕资源已加载。" : `共 ${next.length} 个字幕文件。`));
  }
  reactExports.useEffect(() => {
    void load();
  }, []);
  async function add() {
    const response = await bridge.dialog.openFile([{ name: "字幕文件", extensions: ["srt", "ass", "ssa", "vtt"] }, { name: "所有文件", extensions: ["*"] }], true);
    const paths = response.success ? readPaths$1(response.payload) : [];
    if (paths.length === 0)
      return;
    for (const sourcePath of paths) {
      const result = await bridge.core.invoke({ type: "subtitle.import", payload: { sourcePath } });
      if (!result.success) {
        setStatus(result.error?.message ?? "字幕导入失败。");
        return;
      }
    }
    await load();
  }
  async function importFolder() {
    const response = await bridge.dialog.openDirectory();
    const folderPath = response.success ? response.payload?.filePaths[0] : void 0;
    if (folderPath === void 0)
      return;
    const result = await bridge.core.invoke({ type: "subtitle.import_folder", payload: { folderPath } });
    if (!result.success) {
      setStatus(result.error?.message ?? "字幕文件夹导入失败。");
      return;
    }
    const count = typeof result.payload === "number" ? result.payload : 0;
    await load(`递归导入完成，共添加 ${count} 个字幕。`);
  }
  async function remove() {
    if (selectedPath === "")
      return;
    const result = await bridge.core.invoke({ type: "subtitle.delete", payload: { path: selectedPath } });
    if (!result.success) {
      setStatus(result.error?.message ?? "字幕删除失败。");
      return;
    }
    setDeleteOpen(false);
    setSelectedPath("");
    await load();
  }
  const selected = items.find((item) => item.path === selectedPath);
  const tools = /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void add(), children: "添加字幕" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void importFolder(), children: "递归导入" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", disabled: selected === void 0, onClick: () => setDeleteOpen(true), children: "删除字幕" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void load(), children: "刷新" })
  ] });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "我的字幕", tools }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "字幕资源", actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
        items.length,
        " 个文件"
      ] }), scroll: true, children: items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "还没有字幕文件", action: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void add(), children: "添加第一个字幕" }) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ProductList, { children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { selected: selectedPath === item.path, onClick: () => setSelectedPath(item.path), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.name }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.path })
      ] }, item.path)) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: status })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: deleteOpen, title: "我的字幕", onClose: () => setDeleteOpen(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDeleteOpen(false), children: "否" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => void remove(), children: "是" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "删除字幕“",
      selected?.name ?? "",
      "”？"
    ] }) })
  ] });
}
function readPlayback() {
  try {
    const value = localStorage.getItem("aimaid.video-playback");
    return value === null ? null : JSON.parse(value);
  } catch {
    return null;
  }
}
async function resolveSource(item) {
  if (item === null)
    return "";
  if (item.filePath === "")
    return item.originalUrl;
  const response = await bridge.media.registerLocalFile(item.filePath);
  if (!response.success || response.payload === null)
    throw new Error(response.error?.message ?? "本地视频读取失败。");
  return response.payload.url;
}
function readPaths$1(value) {
  return typeof value === "object" && value !== null && "filePaths" in value && Array.isArray(value.filePaths) ? value.filePaths.filter((item) => typeof item === "string") : [];
}
function isSubtitleItem(value) {
  return typeof value === "object" && value !== null && "name" in value && typeof value.name === "string" && "path" in value && typeof value.path === "string";
}
const GROUPS = [
  { title: "常用", entries: [
    { title: "角色对话中心", target: "voice-conversation", icon: "message" },
    { title: "提醒事项", target: "reminders", icon: "clock" },
    { title: "记事本", target: "notebook", icon: "layers" }
  ] },
  { title: "媒体", entries: [
    { title: "视频库", target: "video", icon: "image" },
    { title: "远程视频中心", target: "remote-video", icon: "folder" }
  ] },
  { title: "工具", entries: [
    { title: "BTC", target: "bitcoin", icon: "activity" },
    { title: "计时", target: "timer", icon: "gauge" },
    { title: "密码库", target: "vault", icon: "settings" },
    { title: "快捷脚本", target: "scripts", icon: "sparkles" }
  ] }
];
const ENTRIES = GROUPS.flatMap(({ entries }) => entries);
function MainPage() {
  const { show } = useToast();
  const open = async (target) => {
    try {
      const opened = await bridge.window.open(target);
      if (!opened.success) {
        show(opened.error?.message ?? "窗口打开失败，请稍后重试。", "error");
        return;
      }
      const closed = await bridge.window.close();
      if (!closed.success) show(closed.error?.message ?? "工作台关闭失败。", "error");
    } catch (reason) {
      show(reason instanceof Error ? reason.message : "窗口打开失败，请稍后重试。", "error");
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "工作台" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "section", variant: "workbench-sections", "aria-label": "全部功能", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "header", variant: "workbench-group__header", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Heading, { level: 2, children: "全部功能" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "workbench-grid", children: ENTRIES.map((entry) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { appearance: "card", onClick: () => void open(entry.target), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "span", variant: "workbench-card__icon", children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: entry.icon }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "span", variant: "workbench-card__copy", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: entry.title }) })
      ] }, entry.target)) })
    ] }) })
  ] });
}
const field = (key, label, description, kind = "text") => ({ key, label, description, kind });
const USER_CONFIGURATION_GROUPS = [
  {
    name: "网络",
    fields: [
      field("App:Proxy:Address", "网络代理", "状态探测等 Core 网络请求使用的 HTTP/HTTPS 代理地址，例如 127.0.0.1:6324。")
    ]
  },
  {
    name: "TTS",
    fields: [
      field("App:Tts:Enabled", "启用 TTS", "是否启用语音合成服务。", "boolean"),
      field("App:Tts:Endpoint", "TTS 服务地址", "语音合成 HTTP 服务地址。", "url"),
      field("App:Tts:StartScriptPath", "TTS 启动脚本", "本地 TTS 服务启动脚本的完整路径。"),
      field("App:Tts:WorkingDirectory", "TTS 工作目录", "启动 TTS 服务时使用的工作目录。"),
      field("App:Tts:StartupTimeoutSeconds", "启动超时（秒）", "等待本地 TTS 服务健康的基础超时时间。", "integer"),
      field("App:Tts:RequestTimeoutSeconds", "请求超时（秒）", "单次语音合成请求的超时时间。", "integer"),
      field("App:Tts:VoiceId", "默认音色 ID", "没有角色级音色资产映射时发送给外部 TTS 服务的默认音色。")
    ]
  },
  {
    name: "语音识别",
    fields: [
      field("App:Asr:Enabled", "启用语音识别", "在角色对话中心使用 AIProvider 将录音转成文字。", "boolean"),
      field("App:Asr:Endpoint", "AIProvider 服务地址", "AIProvider 的 HTTP/HTTPS 根地址，不包含 /api/asr/transcriptions。", "url"),
      field("App:Asr:RequestTimeoutSeconds", "识别超时（秒）", "单次录音上传和识别的最长等待时间。", "integer")
    ]
  }
];
const HOTKEY_ACTIONS = [
  { action: "open-chat", label: "聊天输入", settingKey: "hotkey_open_chat", defaultGesture: "Ctrl+Shift+F", target: "chat" },
  { action: "open-workbench", label: "工作台", settingKey: "hotkey_open_workbench", defaultGesture: "", target: "main" },
  { action: "open-character-manager", label: "角色管理", settingKey: "hotkey_open_character_manager", defaultGesture: "", target: "characters" },
  { action: "open-notebook", label: "记事本", settingKey: "hotkey_open_notebook", defaultGesture: "Ctrl+Shift+R", target: "notebook" },
  { action: "open-status", label: "状态面板", settingKey: "hotkey_open_status", defaultGesture: "Ctrl+Shift+T", target: "status" },
  { action: "open-system-settings", label: "系统设置", settingKey: "hotkey_open_system_settings", defaultGesture: "Ctrl+Shift+G", target: "settings" },
  { action: "open-appearance-settings", label: "外观设置", settingKey: "hotkey_open_appearance_settings", defaultGesture: "", target: "appearance" },
  { action: "open-timer", label: "计时器", settingKey: "hotkey_open_timer", defaultGesture: "", target: "timer" },
  { action: "open-reminders", label: "提醒事项", settingKey: "hotkey_open_reminders", defaultGesture: "", target: "reminders" },
  { action: "open-vault", label: "密码库", settingKey: "hotkey_open_vault", defaultGesture: "Ctrl+Shift+P", target: "vault" },
  { action: "open-video-library", label: "视频库", settingKey: "hotkey_open_video_library", defaultGesture: "Ctrl+Shift+V", target: "video" },
  { action: "open-remote-video-center", label: "远程视频中心", settingKey: "hotkey_open_remote_video_center", defaultGesture: "Ctrl+Shift+Y", target: "remote-video" },
  { action: "open-voice-conversation-center", label: "角色对话中心", settingKey: "hotkey_open_voice_conversation_center", defaultGesture: "Ctrl+Shift+C", target: "voice-conversation" },
  { action: "open-command-manager", label: "快捷脚本", settingKey: "hotkey_open_command_manager", defaultGesture: "", target: "scripts" },
  { action: "open-bitcoin-market", label: "BTC 行情", settingKey: "hotkey_open_bitcoin_market", defaultGesture: "Ctrl+Shift+B", target: "bitcoin" },
  { action: "cycle-display-mode", label: "切换显示模式", settingKey: "hotkey_cycle_display_mode", defaultGesture: "Ctrl+Right" },
  { action: "cycle-display-mode-reverse", label: "反向切换显示模式", settingKey: "hotkey_cycle_display_mode_reverse", defaultGesture: "Ctrl+Left" },
  { action: "play-next", label: "播放下一个", settingKey: "hotkey_play_next", defaultGesture: "Ctrl+Down" },
  { action: "play-previous", label: "播放上一个", settingKey: "hotkey_play_previous", defaultGesture: "Ctrl+Up" }
];
const MUSIC_VISUALIZER_STYLE_KEY = "music_visualizer_style";
const MUSIC_VISUALIZER_STYLE_OPTIONS = [
  ["surround-bars", "环绕柱条"],
  ["surround-line", "环绕线条"],
  ["bottom-wave", "底部倒置柱状"],
  ["radial-bars", "背景径向柱状圆环"],
  ["circular-wave", "背景圆形波形线"],
  ["pulse-rings", "背景同心脉冲"]
];
function parseMusicVisualizerStyle(value) {
  return value === "surround-line" || value === "bottom-wave" || value === "radial-bars" || value === "circular-wave" || value === "pulse-rings" ? value : "surround-bars";
}
function isBackgroundMusicVisualizer(style) {
  return style === "radial-bars" || style === "circular-wave" || style === "pulse-rings";
}
function createBottomBarLayout(contour, sourceWidth) {
  const contourLeft = Math.min(...contour.points.map((point) => point.x));
  const contourRight = Math.max(...contour.points.map((point) => point.x));
  return {
    normalizedCenterX: (contourLeft + contourRight) / 2,
    spacing: Math.min(14, Math.max(1, sourceWidth))
  };
}
function bottomBarSlots(contour, sourceWidth, spacing) {
  const contourLeft = Math.min(...contour.points.map((point) => point.x));
  const contourRight = Math.max(...contour.points.map((point) => point.x));
  const visibleWidth = Math.max(1, (contourRight - contourLeft) * sourceWidth);
  const halfCount = Math.max(1, Math.floor(visibleWidth / (spacing * 2)));
  return Array.from({ length: halfCount * 2 + 1 }, (_, index) => index - halfCount);
}
function bottomBarIdentity(slot) {
  return slot >= 0 ? slot * 2 : -slot * 2 - 1;
}
function createRadialVisualizerLayout(contour, sourceWidth, sourceHeight) {
  const left = Math.min(...contour.points.map((point) => point.x));
  const right = Math.max(...contour.points.map((point) => point.x));
  const top = Math.min(...contour.points.map((point) => point.y));
  const bottom = Math.max(...contour.points.map((point) => point.y));
  const visibleWidth = (right - left) * sourceWidth;
  const visibleHeight = (bottom - top) * sourceHeight;
  return {
    normalizedCenterX: contour.center.x,
    normalizedCenterY: contour.center.y,
    radius: Math.min(260, Math.max(90, Math.max(visibleWidth, visibleHeight) * 0.32))
  };
}
const CATEGORIES = ["显示与窗口", "AI 模型", "AI 决策", "语音 / TTS", "缓存与性能", "快捷键", "网络与服务", "高级 / 诊断"];
const SEARCH_INDEX = {
  "显示与窗口": "语言 显示模式 Live2D 模型 轮播时间 图库目录 开机自启动 气泡主题 音浪 样式 环绕 柱条 线条 底部",
  "AI 模型": "模型列表 编辑 新增模型 业务链 映射 API 密钥 endpoint",
  "AI 决策": "主动决策 语音 勿扰 模式",
  "语音 / TTS": "实时 TTS 角色语音 播报",
  "缓存与性能": "语音缓存 周期 性能",
  "快捷键": "页面 功能 快捷键 录制 热键",
  "网络与服务": "网络 服务 行情 AI Provider 地址 超时 健康检查",
  "高级 / 诊断": "高级 诊断 用户配置 角色模板 Source Prompt 数据库"
};
const USER_CONFIGURATION_PREFIX = "user_config:";
const IMAGE_INTERVAL_OPTIONS = [
  { seconds: 5, label: "5 秒" },
  { seconds: 10, label: "10 秒" },
  { seconds: 20, label: "20 秒" },
  { seconds: 40, label: "40 秒" },
  { seconds: 60, label: "1 分钟" },
  { seconds: 180, label: "3 分钟" },
  { seconds: 300, label: "5 分钟" },
  { seconds: 600, label: "10 分钟" }
];
function SettingsPage() {
  const [category, setCategory] = reactExports.useState("显示与窗口");
  const [search, setSearch] = reactExports.useState("");
  const keyword = search.trim().toLocaleLowerCase();
  const matches = keyword === "" ? [] : CATEGORIES.filter((item) => `${item} ${SEARCH_INDEX[item]}`.toLocaleLowerCase().includes(keyword));
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "系统设置" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { scroll: false, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "settings-workspace", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "aside", variant: "settings-navigation", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(SearchBox, { "aria-label": "搜索设置", value: search, onChange: setSearch }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { as: "nav", variant: "settings-category-list", "aria-label": "设置分类", children: CATEGORIES.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { selected: category === item, onClick: () => setCategory(item), children: item }, item)) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "main", variant: "settings-content", children: [
        keyword !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "header", variant: "settings-content__header", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Title2, { children: "搜索结果" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
            "“",
            search.trim(),
            "”匹配 ",
            matches.length,
            " 个分类"
          ] })
        ] }) : null,
        keyword !== "" ? matches.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "没有匹配的设置" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "settings-search-results", children: matches.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { appearance: "card", onClick: () => {
          setCategory(item);
          setSearch("");
        }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item }) }, item)) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(SettingsCategory, { category, search })
      ] })
    ] }) })
  ] });
}
function SettingsCategory({ category, search }) {
  const content2 = reactExports.useMemo(() => {
    switch (category) {
      case "显示与窗口":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(DisplaySettings, {});
      case "AI 模型":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(ModelSettings, {});
      case "AI 决策":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(DecisionSettings, {});
      case "语音 / TTS":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(VoiceSettings, {});
      case "缓存与性能":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(CacheSettings, {});
      case "快捷键":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(HotkeySettings, {});
      case "网络与服务":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(NetworkSettings, {});
      case "高级 / 诊断":
        return /* @__PURE__ */ jsxRuntimeExports.jsx(AdvancedSettings, {});
    }
  }, [category]);
  if (search.trim() === "")
    return content2;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { "data-query": search, children: content2 });
}
function DisplaySettings() {
  const toast = useToast();
  const [presentation, setPresentation] = reactExports.useState(null);
  const [language, setLanguage] = reactExports.useState("zh-CN");
  const [bubbleStyle, setBubbleStyle] = reactExports.useState("");
  const [visualizerStyle, setVisualizerStyle] = reactExports.useState("surround-bars");
  const [platform, setPlatform] = reactExports.useState(null);
  reactExports.useEffect(() => {
    void Promise.all([
      bridge.pet.presentation.get(),
      loadSettings(["ui_language", "comic_bubble_style", MUSIC_VISUALIZER_STYLE_KEY]),
      bridge.systemSettings.get()
    ]).then(([presentationResponse, settings, platformResponse]) => {
      if (!presentationResponse.success || presentationResponse.payload === null)
        throw new Error(presentationResponse.error?.message ?? "显示设置读取失败。");
      if (!platformResponse.success || platformResponse.payload === null)
        throw new Error(platformResponse.error?.message ?? "系统平台设置读取失败。");
      setPresentation(presentationResponse.payload);
      setLanguage(settings.ui_language ?? "zh-CN");
      setBubbleStyle(settings.comic_bubble_style ?? "");
      setVisualizerStyle(parseMusicVisualizerStyle(settings[MUSIC_VISUALIZER_STYLE_KEY]));
      setPlatform(platformResponse.payload);
    }).catch((reason) => toast.show(messageOf$4(reason), "error"));
  }, []);
  async function setMode(mode) {
    let current = presentation;
    for (let step = 0; current !== null && current.mode !== mode && step < 3; step += 1) {
      const response = await bridge.pet.presentation.execute("cycle-mode");
      if (!response.success || response.payload === null) {
        toast.show(response.error?.message ?? "显示模式切换失败。", "error");
        return;
      }
      current = response.payload;
    }
    setPresentation(current);
  }
  async function setInterval(seconds) {
    let current = presentation;
    for (let step = 0; current !== null && current.imageIntervalSeconds !== seconds && step < 8; step += 1) {
      const response = await bridge.pet.presentation.execute("cycle-image-interval");
      if (!response.success || response.payload === null) {
        toast.show(response.error?.message ?? "轮播时间保存失败。", "error");
        return;
      }
      current = response.payload;
    }
    setPresentation(current);
  }
  async function setLive2dRole(role) {
    let current = presentation;
    if (current === null || !current.live2dRoles.includes(role))
      return;
    for (let step = 0; current.live2dRole !== role && step < current.live2dRoles.length; step += 1) {
      const response = await bridge.pet.presentation.execute("switch-live2d-role");
      if (!response.success || response.payload === null) {
        toast.show(response.error?.message ?? "Live2D 模型切换失败。", "error");
        return;
      }
      current = response.payload;
    }
    setPresentation(current);
  }
  async function chooseFolder() {
    const response = await bridge.pet.presentation.execute("choose-image-folder");
    if (response.success && response.payload !== null)
      setPresentation(response.payload);
    else
      toast.show(response.error?.message ?? "图库目录选择失败。", "error");
  }
  async function saveLanguage(value) {
    try {
      await saveSettings({ ui_language: value });
      setLanguage(value);
      document.documentElement.lang = value;
      toast.show("语言设置已保存；当前窗口的语言标记已立即更新，已打开页面会在重新打开后载入对应语言包。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    }
  }
  async function saveBubbleStyle(value) {
    try {
      const response = await bridge.systemSettings.setBubbleStyle(value);
      if (!response.success)
        throw new Error(response.error?.message ?? "气泡主题保存失败。");
      setBubbleStyle(value);
      toast.show("气泡主题已保存并立即应用。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    }
  }
  async function saveVisualizerStyle(value) {
    const next = parseMusicVisualizerStyle(value);
    try {
      await saveSettings({ [MUSIC_VISUALIZER_STYLE_KEY]: next });
      setVisualizerStyle(next);
      toast.show("音浪样式已保存并立即应用。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    }
  }
  async function setAutoStart(enabled) {
    const response = await bridge.systemSettings.setAutoStart(enabled);
    if (!response.success || response.payload === null) {
      toast.show(response.error?.message ?? "开机自启动更新失败。", "error");
      return;
    }
    setPlatform(response.payload);
    toast.show(enabled ? "开机自启动已开启。" : "开机自启动已关闭。", "success");
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "语言包", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "当前语言", value: language, onChange: (event) => void saveLanguage(event.target.value), options: [{ value: "zh-CN", label: "简体中文 · Chinese" }, { value: "en", label: "English · English" }, { value: "es", label: "Español · Spanish" }, { value: "ja", label: "日本語 · Japanese" }] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: `显示模式：${presentation === null ? "读取中" : displayModeLabel(presentation.mode)}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: [["image", "图片"], ["png-sequence", "PNG"], ["live2d", "Live2D"]].map(([mode, label]) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { selected: presentation?.mode === mode, onClick: () => void setMode(mode), children: label }, mode)) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "音乐音浪样式", description: "音浪作为独立覆盖层显示，不参与图片、PNG 序列或 Live2D 的缩放。", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ValueChoice, { values: [...MUSIC_VISUALIZER_STYLE_OPTIONS], selected: visualizerStyle, onSelect: (value) => void saveVisualizerStyle(value) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "Live2D 模型", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { disabled: presentation === null || presentation.live2dRoles.length < 2, value: presentation?.live2dRole ?? "", onChange: (event) => void setLive2dRole(event.target.value), options: (presentation?.live2dRoles ?? []).map((role) => ({ value: role, label: role })) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "轮播时间", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: IMAGE_INTERVAL_OPTIONS.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { selected: presentation?.imageIntervalSeconds === item.seconds, onClick: () => void setInterval(item.seconds), children: item.label }, item.seconds)) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "图库目录", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "图库目录", readOnly: true, value: presentation?.imageRoot ?? "" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void chooseFolder(), children: "浏览" })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "开机自启动", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { disabled: platform === null, label: "开机后自动启动女仆助手", checked: platform?.autoStartEnabled ?? false, onChange: (event) => void setAutoStart(event.target.checked) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: `气泡主题：${bubbleStyleLabel(bubbleStyle)}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ValueChoice, { values: [["", "自动（按内容）"], ["normal", "标准"], ["soft", "柔和"], ["lively", "活泼"], ["close", "亲密"]], selected: bubbleStyle, onSelect: (value) => void saveBubbleStyle(value) }) })
  ] });
}
function ModelSettings() {
  const toast = useToast();
  const [models, setModels] = reactExports.useState(null);
  const [business, setBusiness] = reactExports.useState(null);
  const [newKey, setNewKey] = reactExports.useState("");
  const [newType, setNewType] = reactExports.useState("local");
  const [busy, setBusy] = reactExports.useState(false);
  async function load() {
    const [modelResponse, businessResponse] = await Promise.all([
      bridge.core.invoke({ type: "model.list", payload: {} }),
      bridge.core.invoke({ type: "business_model.list", payload: {} })
    ]);
    if (!modelResponse.success || !Array.isArray(modelResponse.payload))
      throw new Error(modelResponse.error?.message ?? "模型配置读取失败。");
    if (!businessResponse.success || !Array.isArray(businessResponse.payload))
      throw new Error(businessResponse.error?.message ?? "业务模型读取失败。");
    setModels(modelResponse.payload);
    setBusiness(businessResponse.payload);
  }
  reactExports.useEffect(() => {
    void load().catch((reason) => toast.show(messageOf$4(reason), "error"));
  }, []);
  const updateModel = (key, patch) => setModels((items) => items?.map((item) => item.modelKey === key ? { ...item, ...patch } : item) ?? null);
  async function saveModels() {
    if (models === null)
      return;
    setBusy(true);
    try {
      const response = await bridge.core.invoke({ type: "model.save", payload: { configurations: models } });
      if (!response.success)
        throw new Error(response.error?.message ?? "模型配置保存失败。");
      toast.show("模型配置已保存；聊天与角色模板等业务会在下一次调用时读取新配置。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    } finally {
      setBusy(false);
    }
  }
  async function addModel() {
    setBusy(true);
    try {
      const response = await bridge.core.invoke({ type: "model.add", payload: { modelKey: newKey.trim(), modelType: newType } });
      if (!response.success)
        throw new Error(response.error?.message ?? "无法新增模型。");
      setNewKey("");
      await load();
      toast.show("模型配置已创建，并已进入全部业务模型选择。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    } finally {
      setBusy(false);
    }
  }
  async function saveBusiness() {
    if (business === null)
      return;
    setBusy(true);
    try {
      const response = await bridge.core.invoke({ type: "business_model.save", payload: { configurations: business } });
      if (!response.success)
        throw new Error(response.error?.message ?? "业务模型保存失败。");
      toast.show("6 条业务链的独立模型配置已保存，下一次对应业务调用立即生效。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    } finally {
      setBusy(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(SettingCard, { title: "模型配置", children: [
      models === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "正在读取模型配置…" }) : models.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Title4, { children: [
          "模型 · ",
          item.modelKey
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "类型", value: item.type, onChange: (event) => updateModel(item.modelKey, { type: event.target.value }), options: [{ value: "local", label: "local" }, { value: "api", label: "api" }] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "服务地址", value: item.endpoint, onChange: (event) => updateModel(item.modelKey, { endpoint: event.target.value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "模型名称", value: item.model, onChange: (event) => updateModel(item.modelKey, { model: event.target.value }) }),
        item.type === "api" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "API Key", type: "password", autoComplete: "new-password", value: item.apiKey, onChange: (event) => updateModel(item.modelKey, { apiKey: event.target.value }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "启用网页搜索", checked: item.enableWebSearch, onChange: (event) => updateModel(item.modelKey, { enableWebSearch: event.target.checked }) })
        ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "启用思考模式", checked: item.think, onChange: (event) => updateModel(item.modelKey, { think: event.target.checked }) })
      ] }, item.modelKey)),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: busy, disabled: models === null, onClick: () => void saveModels(), children: "保存模型配置" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "新增模型配置", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "模型标识", value: newKey, onChange: (event) => setNewKey(event.target.value) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: newType, onChange: (event) => setNewType(event.target.value), options: [{ value: "local", label: "本地模型" }, { value: "api", label: "API 模型" }] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: busy, onClick: () => void addModel(), children: "新增模型" })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(SettingCard, { title: "各业务链条模型", children: [
      business === null || models === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "正在读取业务模型…" }) : business.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(FormLabel, { children: [
          item.displayName,
          /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: item.businessKey })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: item.modelKey, onChange: (event) => setBusiness((rows) => rows?.map((row) => row.businessKey === item.businessKey ? { ...row, modelKey: event.target.value } : row) ?? null), options: models.map((model) => ({ value: model.modelKey, label: `${model.modelKey}: ${model.model}` })) })
      ] }, item.businessKey)),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: busy, disabled: business === null || models === null, onClick: () => void saveBusiness(), children: "保存全部业务模型" })
    ] })
  ] });
}
function DecisionSettings() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(BooleanRuntimeSetting, { settingKey: "ai_proactive_enabled", defaultValue: true, title: "AI 决策语音", label: "启用 AI 决策语音", success: (value) => value ? "AI 决策语音已开启。" : "AI 决策语音已关闭。" });
}
function VoiceSettings() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(BooleanRuntimeSetting, { settingKey: "realtime_tts_enabled", defaultValue: true, title: "实时 TTS", label: "实时生成并播放角色语音", success: (value) => value ? "实时 TTS 已开启。" : "实时 TTS 已关闭。" });
}
function CacheSettings() {
  const toast = useToast();
  const [hours, setHours] = reactExports.useState(1);
  reactExports.useEffect(() => {
    void loadSettings(["voice_cache_period_hours"]).then((values) => setHours(Number(values.voice_cache_period_hours ?? 1))).catch((reason) => toast.show(messageOf$4(reason), "error"));
  }, []);
  async function select(value) {
    try {
      await saveSettings({ voice_cache_period_hours: value });
      setHours(Number(value));
      toast.show(`语音缓存周期已切换：${value} 小时。`, "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: `语音缓存周期：${hours} 小时`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ValueChoice, { values: [1, 2, 4, 8, 16].map((value) => [String(value), `${value} 小时`]), selected: String(hours), onSelect: (value) => void select(value) }) });
}
function HotkeySettings() {
  const toast = useToast();
  const [selected, setSelected] = reactExports.useState(HOTKEY_ACTIONS[0].action);
  const [capturing, setCapturing] = reactExports.useState(false);
  const [platform, setPlatform] = reactExports.useState(null);
  reactExports.useEffect(() => {
    void bridge.systemSettings.get().then((response) => response.success && response.payload !== null ? setPlatform(response.payload) : toast.show(response.error?.message ?? "快捷键读取失败。", "error"));
  }, []);
  const current = platform?.hotkeys.find((item) => item.action === selected)?.gesture ?? "";
  const capture = async (event) => {
    if (!capturing)
      return;
    event.preventDefault();
    if (event.key === "Escape") {
      setCapturing(false);
      return;
    }
    if (["Control", "Alt", "Shift", "Meta"].includes(event.key))
      return;
    const parts = [];
    if (event.ctrlKey)
      parts.push("Ctrl");
    if (event.altKey)
      parts.push("Alt");
    if (event.shiftKey)
      parts.push("Shift");
    if (event.metaKey)
      parts.push("Win");
    parts.push(event.key.length === 1 ? event.key.toUpperCase() : event.key);
    setCapturing(false);
    const response = await bridge.systemSettings.setHotkey(selected, parts.join("+"));
    if (!response.success || response.payload === null) {
      toast.show(response.error?.message ?? "快捷键注册失败。", "error");
      return;
    }
    setPlatform(response.payload);
    toast.show("全局快捷键已保存并立即重新注册。", "success");
  };
  async function clear() {
    const response = await bridge.systemSettings.setHotkey(selected, "");
    if (!response.success || response.payload === null) {
      toast.show(response.error?.message ?? "快捷键禁用失败。", "error");
      return;
    }
    setPlatform(response.payload);
    toast.show("快捷键已禁用。", "success");
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "页面与功能快捷键", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { tabIndex: -1, onKeyDown: (event) => void capture(event), children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: selected, onChange: (event) => {
      setSelected(event.target.value);
      setCapturing(false);
    }, options: HOTKEY_ACTIONS.map((item) => ({ value: item.action, label: item.label })) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(KeyboardKey, { children: capturing ? "请按下按键组合…（Esc 取消）" : current || "（已禁用）" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: capturing, onClick: (event) => {
        setCapturing(true);
        event.currentTarget.closest(".hotkey-editor")?.focus();
      }, children: capturing ? "录制中…" : "重新录制" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: capturing || current === "", onClick: () => void clear(), children: "取消" })
    ] }),
    current !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "点击「重新录制」后按下新的组合键；「取消」可禁用该快捷键。" }) : null
  ] }) });
}
function NetworkSettings() {
  const toast = useToast();
  const [configuration, setConfiguration] = reactExports.useState(null);
  const [busy, setBusy] = reactExports.useState(false);
  reactExports.useEffect(() => {
    void bridge.core.invoke({ type: "crypto_provider.get", payload: {} }).then((response) => {
      if (!response.success || response.payload === null)
        throw new Error(response.error?.message ?? "行情服务设置读取失败。");
      setConfiguration(response.payload);
    }).catch((reason) => toast.show(messageOf$4(reason), "error"));
  }, []);
  async function execute(type) {
    if (configuration === null)
      return;
    setBusy(true);
    try {
      const response = await bridge.core.invoke({ type, payload: { configuration } });
      if (!response.success)
        throw new Error(response.error?.message ?? "行情服务操作失败。");
      if (type === "crypto_provider.check" && response.payload !== null && typeof response.payload === "object" && "configuration" in response.payload)
        setConfiguration(response.payload.configuration);
      toast.show(type === "crypto_provider.save" ? "行情服务设置已保存，下一次刷新立即生效。" : "行情服务健康检查完成。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    } finally {
      setBusy(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "加密行情 AI Provider", children: configuration === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "正在读取行情服务设置…" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "启用后 Spot REST 通过 AI Provider", checked: configuration.isEnabled, onChange: (event) => setConfiguration({ ...configuration, isEnabled: event.target.checked }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "服务地址", value: configuration.serviceUrl, onChange: (event) => setConfiguration({ ...configuration, serviceUrl: event.target.value }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "请求超时（秒）", value: String(configuration.timeoutSeconds), onChange: (event) => setConfiguration({ ...configuration, timeoutSeconds: Number(event.target.value) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "最近检查：",
      configuration.lastHealthStatus,
      configuration.lastHealthLatencyMs === null ? "" : ` · ${configuration.lastHealthLatencyMs} ms`
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { loading: busy, onClick: () => void execute("crypto_provider.check"), children: "健康检查" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { loading: busy, variant: "primary", onClick: () => void execute("crypto_provider.save"), children: "保存行情服务" })
    ] })
  ] }) });
}
function AdvancedSettings() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Accordion, { title: "勿扰模式", children: /* @__PURE__ */ jsxRuntimeExports.jsx(DisturbanceSetting, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Accordion, { title: "用户配置覆盖", children: /* @__PURE__ */ jsxRuntimeExports.jsx(UserConfigurationPanel, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Accordion, { title: "当前角色模板诊断", children: /* @__PURE__ */ jsxRuntimeExports.jsx(CharacterTemplateDiagnostics, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Accordion, { title: "LLM Source Prompt", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SourcePromptSettings, {}) })
  ] });
}
function CharacterTemplateDiagnostics() {
  const toast = useToast();
  const [role, setRole] = reactExports.useState(null);
  const [busy, setBusy] = reactExports.useState(null);
  async function load() {
    const [charactersResponse, settingsResponse] = await Promise.all([
      bridge.core.invoke({ type: "character.list", payload: {} }),
      bridge.core.invoke({ type: "settings.get", payload: { keys: ["voice_current_role_id"] } })
    ]);
    if (!charactersResponse.success || !Array.isArray(charactersResponse.payload))
      throw new Error(charactersResponse.error?.message ?? "角色列表读取失败。");
    if (!settingsResponse.success)
      throw new Error(settingsResponse.error?.message ?? "当前角色读取失败。");
    const settings = settingsResponse.payload;
    const currentRoleId = settings?.settings?.find((item) => item.key === "voice_current_role_id")?.value ?? "";
    const characters = charactersResponse.payload;
    setRole(characters.find((item) => item.roleId === currentRoleId) ?? characters.find((item) => item.isEnabled) ?? null);
  }
  reactExports.useEffect(() => {
    void load().catch((reason) => toast.show(messageOf$4(reason), "error"));
  }, []);
  async function generate(continueIteration) {
    if (role === null)
      return;
    setBusy(continueIteration ? "iterate" : "regenerate");
    try {
      const response = await bridge.core.invoke({ type: "character.template.generate", payload: { roleId: role.roleId, continueIteration } }, 12e4);
      if (!response.success || response.payload === null)
        throw new Error(response.error?.message ?? "角色模板生成失败。");
      setRole(response.payload);
      toast.show(continueIteration ? "当前角色模板已继续迭代。" : "当前角色模板已重新生成。", "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    } finally {
      setBusy(null);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "当前角色模板诊断", children: role === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "当前没有可诊断的启用角色。" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "角色：",
      role.name || role.roleId,
      " · 状态：",
      role.templateCardGenerationStatus || "尚未生成",
      " · 已迭代 ",
      role.templateCardIterationCount,
      " 次"
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "最近生成：",
      formatDiagnosticTime(role.templateCardGeneratedAt),
      role.templateCardGenerationMessage ? ` · ${role.templateCardGenerationMessage}` : ""
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { loading: busy === "regenerate", disabled: busy !== null, onClick: () => void generate(false), children: "重新生成" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { loading: busy === "iterate", disabled: busy !== null || role.templateCardJson === "", onClick: () => void generate(true), children: "继续迭代" })
    ] })
  ] }) });
}
function SourcePromptSettings() {
  const toast = useToast();
  const [items, setItems] = reactExports.useState(null);
  const [selected, setSelected] = reactExports.useState("");
  reactExports.useEffect(() => {
    void (async () => {
      const response = await bridge.core.invoke({ type: "source_prompt.list", payload: {} });
      if (!response.success || !Array.isArray(response.payload)) {
        toast.show(response.error?.message ?? "Source Prompt 读取失败。", "error");
        return;
      }
      const next = response.payload;
      setItems(next);
      setSelected(next[0]?.sourceKey ?? "");
    })();
  }, []);
  const current = items?.find((item) => item.sourceKey === selected);
  const change = (patch) => setItems((rows) => rows?.map((item) => item.sourceKey === selected ? { ...item, ...patch } : item) ?? null);
  async function save2() {
    if (current === void 0)
      return;
    const response = await bridge.core.invoke({ type: "source_prompt.save", payload: { prompt: current } });
    if (!response.success) {
      toast.show(response.error?.message ?? "Source Prompt 保存失败。", "error");
      return;
    }
    toast.show("当前 Source Prompt 已保存，下一次调用立即生效。", "success");
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: "LLM Source Prompt", children: items === null ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "正在读取 Source Prompt…" }) : items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "数据库中没有现役 Source Prompt。" }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: selected, onChange: (event) => setSelected(event.target.value), options: items.map((item) => ({ value: item.sourceKey, label: item.sourceKey })) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { label: "System Prompt", rows: 5, value: current?.systemPromptTemplate ?? "", onChange: (event) => change({ systemPromptTemplate: event.target.value }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { label: "User Prompt", rows: 5, value: current?.userPromptTemplate ?? "", onChange: (event) => change({ userPromptTemplate: event.target.value }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { label: "输出结构 JSON", rows: 5, value: current?.outputSchemaJson ?? "", onChange: (event) => change({ outputSchemaJson: event.target.value }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void save2(), children: "保存当前 Source Prompt" })
  ] }) });
}
function UserConfigurationPanel() {
  const toast = useToast();
  const fields = reactExports.useMemo(() => USER_CONFIGURATION_GROUPS.flatMap((group) => group.fields), []);
  const [values, setValues] = reactExports.useState({});
  const [dirty, setDirty] = reactExports.useState(() => /* @__PURE__ */ new Set());
  const [loading, setLoading] = reactExports.useState(true);
  const [saving, setSaving] = reactExports.useState(false);
  reactExports.useEffect(() => {
    let disposed = false;
    void (async () => {
      try {
        const loaded = {};
        for (let offset = 0; offset < fields.length; offset += 20) {
          const keys = fields.slice(offset, offset + 20).map((field2) => USER_CONFIGURATION_PREFIX + field2.key);
          const response = await bridge.core.invoke({ type: "settings.get", payload: { keys } });
          if (!response.success)
            throw new Error(response.error?.message ?? "用户配置读取失败。");
          const payload = response.payload;
          for (const item of payload?.settings ?? [])
            loaded[item.key.startsWith(USER_CONFIGURATION_PREFIX) ? item.key.slice(USER_CONFIGURATION_PREFIX.length) : item.key] = item.value;
        }
        if (!disposed)
          setValues(loaded);
      } catch (reason) {
        if (!disposed)
          toast.show(reason instanceof Error ? reason.message : String(reason), "error");
      } finally {
        if (!disposed)
          setLoading(false);
      }
    })();
    return () => {
      disposed = true;
    };
  }, [fields, toast]);
  function change(key, value) {
    setValues((current) => ({ ...current, [key]: value }));
    setDirty((current) => new Set(current).add(key));
  }
  async function save2() {
    if (dirty.size === 0) {
      toast.show("当前配置没有修改。", "info");
      return;
    }
    for (const key of dirty) {
      const field2 = fields.find((item) => item.key === key);
      const value = (values[key] ?? "").trim();
      if (field2?.kind === "integer" && !/^-?\d+$/.test(value)) {
        toast.show(`“${field2.label}”必须是整数。`, "error");
        return;
      }
      if (field2?.kind === "url" && value !== "") {
        try {
          const url = new URL(value);
          if (!["http:", "https:"].includes(url.protocol))
            throw new Error();
        } catch {
          toast.show(`“${field2.label}”必须是有效的 HTTP/HTTPS 地址。`, "error");
          return;
        }
      }
    }
    const changed = Object.fromEntries([...dirty].map((key) => [USER_CONFIGURATION_PREFIX + key, (values[key] ?? "").trim()]));
    try {
      setSaving(true);
      const response = await bridge.core.invoke({ type: "settings.save", payload: { values: changed } });
      if (!response.success)
        throw new Error(response.error?.message ?? "用户配置保存失败。");
      setDirty(/* @__PURE__ */ new Set());
      toast.show("用户配置已保存；TTS、代理和按次读取的消费者在下一次调用生效，进程启动型配置需重启应用。", "success");
    } catch (reason) {
      toast.show(reason instanceof Error ? reason.message : String(reason), "error");
    } finally {
      setSaving(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(SettingCard, { title: "用户配置覆盖", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: saving, disabled: loading || dirty.size === 0, onClick: () => void save2(), children: loading ? "正在读取…" : "保存当前配置" }),
    USER_CONFIGURATION_GROUPS.map((group) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Title4, { children: group.name }),
      group.fields.map((field2) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: field2.label }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: field2.kind === "boolean" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "开", disabled: loading, checked: (values[field2.key] ?? "false").toLocaleLowerCase() === "true", onChange: (event) => change(field2.key, String(event.target.checked)) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { title: field2.key, disabled: loading, value: values[field2.key] ?? "", onChange: (event) => change(field2.key, event.target.value) }) })
      ] }, field2.key))
    ] }, group.name))
  ] });
}
function BooleanRuntimeSetting({ settingKey, defaultValue, title, label, success }) {
  const toast = useToast();
  const [value, setValue] = reactExports.useState(defaultValue);
  const [loading, setLoading] = reactExports.useState(true);
  reactExports.useEffect(() => {
    void loadSettings([settingKey]).then((settings) => setValue(settings[settingKey] === void 0 ? defaultValue : settings[settingKey]?.toLocaleLowerCase() === "true")).catch((reason) => toast.show(messageOf$4(reason), "error")).finally(() => setLoading(false));
  }, [defaultValue, settingKey, toast]);
  async function change(next) {
    try {
      await saveSettings({ [settingKey]: String(next) });
      setValue(next);
      toast.show(success(next), "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { disabled: loading, label, checked: value, onChange: (event) => void change(event.target.checked) }) });
}
function DisturbanceSetting() {
  const toast = useToast();
  const [settings, setSettings] = reactExports.useState(null);
  reactExports.useEffect(() => {
    void bridge.core.invoke({ type: "disturbance_settings.get", payload: {} }).then((response) => {
      if (!response.success || response.payload === null)
        throw new Error(response.error?.message ?? "勿扰设置读取失败。");
      setSettings(response.payload);
    }).catch((reason) => toast.show(messageOf$4(reason), "error"));
  }, []);
  async function select(value) {
    if (settings === null)
      return;
    const next = { ...settings, mode: value, updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
    try {
      const response = await bridge.core.invoke({ type: "disturbance_settings.save", payload: { settings: next } });
      if (!response.success)
        throw new Error(response.error?.message ?? "勿扰设置保存失败。");
      setSettings(next);
      toast.show(`勿扰模式：${disturbanceLabel(value)}。主动决策消费者已立即切换。`, "success");
    } catch (reason) {
      toast.show(messageOf$4(reason), "error");
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingCard, { title: `勿扰模式：${disturbanceLabel(settings?.mode ?? "normal")}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ValueChoice, { values: [["normal", "正常"], ["quiet", "安静"], ["focus", "专注"], ["game", "游戏"], ["sleep", "睡眠"]], selected: settings?.mode ?? "normal", onSelect: (value) => void select(value) }) });
}
function SettingCard({ title, description, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingRow, { title, ...description === void 0 ? {} : { description }, control: /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children }) });
}
function ValueChoice({ values, selected, onSelect }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: values.map(([value, label]) => /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { selected: selected === value, onClick: () => onSelect(value), children: label }, value)) });
}
async function loadSettings(keys) {
  const response = await bridge.core.invoke({ type: "settings.get", payload: { keys } });
  if (!response.success)
    throw new Error(response.error?.message ?? "设置读取失败。");
  const payload = response.payload;
  return Object.fromEntries((payload?.settings ?? []).map((item) => [item.key, item.value]));
}
async function saveSettings(values) {
  const response = await bridge.core.invoke({ type: "settings.save", payload: { values } });
  if (!response.success)
    throw new Error(response.error?.message ?? "设置保存失败。");
}
function messageOf$4(reason) {
  return reason instanceof Error ? reason.message : String(reason);
}
function displayModeLabel(mode) {
  return mode === "image" ? "图片" : mode === "png-sequence" ? "PNG" : "Live2D";
}
function bubbleStyleLabel(value) {
  return value === "normal" ? "标准" : value === "soft" ? "柔和" : value === "lively" ? "活泼" : value === "close" ? "亲密" : "自动";
}
function disturbanceLabel(value) {
  return value === "quiet" ? "安静" : value === "focus" ? "专注" : value === "game" ? "游戏" : value === "sleep" ? "睡眠" : "正常";
}
function formatDiagnosticTime(value) {
  if (!value)
    return "尚未生成";
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? value : date.toLocaleString("zh-CN");
}
const DEFAULT_SETTINGS = { downloadRoot: "", cacheRoot: "", fileNameTemplate: "%(title)s [%(id)s].%(ext)s", defaultQualityPreference: "bestvideo+bestaudio/best", downloadThumbnail: true, downloadInfoJson: true, downloadSubtitles: true, overwriteExisting: false, autoImportToVideoLibrary: true, maxConcurrentDownloads: 2, ytDlpPath: "", ffmpegPath: "", potPlayerPath: "", updatedAt: "" };
function RemoteVideoPage() {
  const [tab, setTab] = reactExports.useState("解析结果");
  const [url, setUrl] = reactExports.useState("");
  const [rightPanel, setRightPanel] = reactExports.useState(null);
  const [results, setResults] = reactExports.useState([]);
  const [selectedIds, setSelectedIds] = reactExports.useState([]);
  const [downloads, setDownloads] = reactExports.useState([]);
  const [plays, setPlays] = reactExports.useState([]);
  const [settings, setSettings] = reactExports.useState(DEFAULT_SETTINGS);
  const [diagnostics, setDiagnostics] = reactExports.useState(null);
  const [diagnosticText, setDiagnosticText] = reactExports.useState("暂无诊断记录。");
  const [recordThumbnails, setRecordThumbnails] = reactExports.useState({});
  const [selectedResultId, setSelectedResultId] = reactExports.useState("");
  const [formatSelector, setFormatSelector] = reactExports.useState("");
  const [busy, setBusy] = reactExports.useState(false);
  const [status, setStatus] = reactExports.useState("就绪");
  const [deleteTaskId, setDeleteTaskId] = reactExports.useState("");
  const selectedResult = results.find((item) => item.itemId === selectedResultId) ?? results[0];
  async function invoke(type, payload = {}, timeout = 12e4) {
    const response = await bridge.core.invoke({ type, payload }, timeout);
    if (!response.success)
      throw new Error(response.error?.message ?? "Core 请求失败。");
    return response.payload;
  }
  async function refreshRecords() {
    const [downloadPayload, playPayload] = await Promise.all([
      invoke("remote_video.download.list", {}, 3e4),
      invoke("remote_video.play.list", {}, 3e4)
    ]);
    const downloadList = Array.isArray(downloadPayload) ? downloadPayload : [];
    const playList = Array.isArray(playPayload) ? playPayload : [];
    setDownloads(downloadList);
    setPlays(playList);
    void loadRecordThumbnails([...downloadList, ...playList].filter((r) => "itemId" in r && typeof r.itemId === "string" && r.itemId !== "").map((r) => "itemId" in r ? r.itemId : ""));
  }
  async function loadRecordThumbnails(ids) {
    if (ids.length === 0) return;
    const unique = [...new Set(ids.filter((id) => id !== "" && !recordThumbnails[id]))];
    if (unique.length === 0) return;
    await Promise.all(unique.map(async (itemId) => {
      try {
        const payload = await invoke("remote_video.thumbnail", { itemId }, 3e4);
        if (payload.mimeType.startsWith("image/") && payload.base64Data !== "") {
          const source = `data:${payload.mimeType};base64,${payload.base64Data}`;
          setRecordThumbnails((current) => ({ ...current, [itemId]: source }));
        }
      } catch {
      }
    }));
  }
  async function load() {
    try {
      const [settingsPayload, diagnosticPayload] = await Promise.all([
        invoke("remote_video.settings.get", {}, 3e4),
        invoke("remote_video.diagnostics", {}, 3e4)
      ]);
      if (settingsPayload !== null)
        setSettings(settingsPayload);
      if (diagnosticPayload !== null)
        applyDiagnostics(diagnosticPayload);
      await refreshRecords();
    } catch (error) {
      setStatus(messageOf$3(error));
    }
  }
  reactExports.useEffect(() => {
    void load();
  }, []);
  reactExports.useEffect(() => {
    if (!downloads.some((item) => item.status === "Queued" || item.status === "Running"))
      return;
    const timer = window.setInterval(() => {
      void refreshRecords().catch((error) => setStatus(messageOf$3(error)));
    }, 1e3);
    return () => window.clearInterval(timer);
  }, [downloads]);
  reactExports.useEffect(() => {
    setFormatSelector(selectedResult?.formats[0]?.selector ?? "");
  }, [selectedResult?.itemId]);
  async function resolve() {
    if (url.trim() === "") {
      setStatus("请先粘贴视频 URL、UP主主页、频道地址、播放列表或 UID。");
      return;
    }
    setBusy(true);
    setStatus("解析中…");
    try {
      const payload = await invoke("remote_video.resolve", { input: url });
      const items = Array.isArray(payload.items) ? payload.items : [];
      setResults(items);
      setSelectedIds([]);
      setSelectedResultId(items[0]?.itemId ?? "");
      setDiagnosticText(payload.diagnosticSummary || "解析完成。");
      setTab("解析结果");
      setStatus(`解析完成，已加载 ${items.length} 条。`);
      void loadThumbnails(items);
    } catch (error) {
      setStatus(messageOf$3(error));
      setDiagnosticText(messageOf$3(error));
    } finally {
      setBusy(false);
    }
  }
  async function loadThumbnails(items) {
    await Promise.all(items.map(async (item) => {
      try {
        const payload = await invoke("remote_video.thumbnail", { itemId: item.itemId }, 3e4);
        if (!payload.mimeType.startsWith("image/") || payload.base64Data === "") return;
        const source = `data:${payload.mimeType};base64,${payload.base64Data}`;
        setResults((current) => current.map((candidate) => candidate.itemId === item.itemId ? { ...candidate, thumbnailUrl: source } : candidate));
        setRecordThumbnails((current) => ({ ...current, [item.itemId]: source }));
      } catch {
      }
    }));
  }
  async function pasteFromClipboard() {
    try {
      const value = await navigator.clipboard.readText();
      setUrl(value);
      setStatus(value.trim() === "" ? "剪贴板中没有可解析的链接。" : "已从剪贴板粘贴链接。");
    } catch (error) {
      setStatus(`无法读取剪贴板：${messageOf$3(error)}`);
    }
  }
  async function play(item, mode) {
    setBusy(true);
    setStatus(mode === "direct" ? "正在解析播放地址…" : "正在缓存视频…");
    try {
      await invoke("remote_video.play", { itemId: item.itemId, formatSelector: item.itemId === selectedResult?.itemId ? formatSelector : "", mode });
      setStatus(mode === "direct" ? "PotPlayer 已启动" : "缓存播放文件已发送到 PotPlayer");
      await refreshRecords();
    } catch (error) {
      setStatus(messageOf$3(error));
      setDiagnosticText(messageOf$3(error));
    } finally {
      setBusy(false);
    }
  }
  async function startDownloads(ids) {
    if (ids.length === 0) {
      setStatus("请先选择要下载的视频。");
      return;
    }
    try {
      await invoke("remote_video.download.start", { itemIds: ids, formatSelector }, 3e4);
      setTab("下载记录");
      setStatus(`已添加 ${ids.length} 个下载任务。`);
      await refreshRecords();
    } catch (error) {
      setStatus(messageOf$3(error));
    }
  }
  async function saveSettings2() {
    try {
      const payload = await invoke("remote_video.settings.save", { settings }, 3e4);
      if (payload !== null)
        setSettings(payload);
      setStatus("下载设置已保存");
      setRightPanel(null);
    } catch (error) {
      setStatus(messageOf$3(error));
    }
  }
  async function refreshDiagnostics() {
    try {
      applyDiagnostics(await invoke("remote_video.diagnostics", {}, 3e4));
    } catch (error) {
      setDiagnosticText(messageOf$3(error));
    }
  }
  function applyDiagnostics(value) {
    setDiagnostics(value);
    setDiagnosticText([
      `检查时间：${formatDate(value.checkedAt)}`,
      `yt-dlp：${value.ytDlpExists ? "可用" : "不可用"} ${value.ytDlpPath}`,
      `FFmpeg：${value.ffmpegExists ? "可用" : "不可用"} ${value.ffmpegPath}`,
      `PotPlayer：${value.potPlayerExists ? "可用" : "不可用"} ${value.potPlayerPath}`,
      `下载目录：${value.downloadRootWritable ? "可写" : "不可写"} ${value.downloadRoot}`,
      `活动下载：${value.activeDownloads}`,
      `最近操作：${value.lastOperation || "无"} / ${value.lastStatus || "无"}`,
      value.lastMessage || ""
    ].filter(Boolean).join("\n"));
  }
  async function deleteDownload() {
    try {
      await invoke("remote_video.download.delete", { taskId: deleteTaskId }, 3e4);
      setDeleteTaskId("");
      await refreshRecords();
      setStatus("下载记录已删除");
    } catch (error) {
      setStatus(messageOf$3(error));
    }
  }
  async function copyText(value, successMessage) {
    try {
      await navigator.clipboard.writeText(value);
      setStatus(successMessage);
    } catch {
      setStatus("复制失败，请检查剪贴板权限");
    }
  }
  const tabs = [
    { label: "解析结果", count: results.length },
    { label: "下载记录", count: downloads.length },
    { label: "播放记录", count: plays.length }
  ];
  const onTabKeyDown = (event, current) => {
    if (!["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) return;
    event.preventDefault();
    const currentIndex = tabs.findIndex((item) => item.label === current);
    const nextIndex = event.key === "Home" ? 0 : event.key === "End" ? tabs.length - 1 : (currentIndex + (event.key === "ArrowRight" ? 1 : -1) + tabs.length) % tabs.length;
    const nextTab = tabs[nextIndex]?.label;
    if (nextTab === void 0) return;
    setTab(nextTab);
    event.currentTarget.parentElement?.querySelectorAll('[role="tab"]')[nextIndex]?.focus();
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "远程视频中心" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", "data-product": "remote-video", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { emphasis: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-hero", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-hero__composer", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { id: "remote-video-source", rows: 2, "aria-label": "远程视频地址", placeholder: "粘贴视频链接、作者主页、频道地址、播放列表或 UID", value: url, onChange: (event) => setUrl(event.target.value) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void pasteFromClipboard(), children: "粘贴" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: url.trim() === "", onClick: () => {
            setUrl("");
            setStatus("已清空输入。");
          }, children: "清空" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy || url.trim() === "", onClick: () => void resolve(), children: busy ? "解析中…" : "开始解析" })
        ] })
      ] }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductTabNavigation, { label: "远程视频内容", tabs: tabs.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { role: "tab", id: `remote-video-tab-${item.label}`, "aria-controls": "remote-video-tabpanel", selected: tab === item.label, tabIndex: tab === item.label ? 0 : -1, onKeyDown: (event) => onTabKeyDown(event, item.label), onClick: () => setTab(item.label), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.label }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: item.count })
      ] }, item.label)), actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => {
          setRightPanel("diagnostics");
          void refreshDiagnostics();
        }, children: "诊断" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void bridge.window.open("remote-site-config"), children: "站点配置" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setRightPanel("settings"), children: "下载设置" })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { scroll: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { id: "remote-video-tabpanel", role: "tabpanel", "aria-labelledby": `remote-video-tab-${tab}`, children: [
        tab === "解析结果" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ResultTab, { items: results, selected: selectedResult, selectedIds, formatSelector, busy, select: setSelectedResultId, selectIds: setSelectedIds, setFormatSelector, play, download: (ids) => void startDownloads(ids), copyText }) : null,
        tab === "下载记录" ? /* @__PURE__ */ jsxRuntimeExports.jsx(DownloadTab, { items: downloads, thumbnailFor: (id) => recordThumbnails[id] ?? results.find((item) => item.itemId === id)?.thumbnailUrl ?? "", refresh: () => void refreshRecords(), cancel: (id) => void invoke("remote_video.download.cancel", { taskId: id }, 3e4).then(refreshRecords).catch((error) => setStatus(messageOf$3(error))), play: (id) => void invoke("remote_video.download.play", { taskId: id }, 12e4).then(refreshRecords).catch((error) => setStatus(messageOf$3(error))), openLocation: (path) => void bridge.shell.showItemInFolder(path), remove: setDeleteTaskId }) : null,
        tab === "播放记录" ? /* @__PURE__ */ jsxRuntimeExports.jsx(PlayTab, { items: plays, thumbnailFor: (id) => recordThumbnails[id] ?? results.find((item) => item.itemId === id)?.thumbnailUrl ?? "", refresh: () => void refreshRecords(), replay: (id) => void invoke("remote_video.play.replay", { historyId: id }, 12e4).then(refreshRecords).catch((error) => setStatus(messageOf$3(error))) }) : null
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: status })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Drawer, { open: rightPanel === "settings", title: "下载设置", onClose: () => setRightPanel(null), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Title3, { children: "文件设置" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(PathInput, { label: "下载目录", value: settings.downloadRoot, browse: true, onChange: (value) => setSettings({ ...settings, downloadRoot: value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(PathInput, { label: "缓存目录", value: settings.cacheRoot, browse: true, onChange: (value) => setSettings({ ...settings, cacheRoot: value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "命名模板", value: settings.fileNameTemplate, onChange: (event) => setSettings({ ...settings, fileNameTemplate: event.target.value }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Title3, { children: "下载内容" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteToggle, { title: "保存封面", checked: settings.downloadThumbnail, onChange: (value) => setSettings({ ...settings, downloadThumbnail: value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteToggle, { title: "保存信息文件", checked: settings.downloadInfoJson, onChange: (value) => setSettings({ ...settings, downloadInfoJson: value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteToggle, { title: "下载字幕", checked: settings.downloadSubtitles, onChange: (value) => setSettings({ ...settings, downloadSubtitles: value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteToggle, { title: "覆盖同名文件", checked: settings.overwriteExisting, onChange: (value) => setSettings({ ...settings, overwriteExisting: value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteToggle, { title: "加入视频库", checked: settings.autoImportToVideoLibrary, onChange: (value) => setSettings({ ...settings, autoImportToVideoLibrary: value }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Title3, { children: "工具路径" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "yt-dlp", value: settings.ytDlpPath, onChange: (event) => setSettings({ ...settings, ytDlpPath: event.target.value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "FFmpeg", value: settings.ffmpegPath, onChange: (event) => setSettings({ ...settings, ffmpegPath: event.target.value }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "PotPlayer", value: settings.potPlayerPath, onChange: (event) => setSettings({ ...settings, potPlayerPath: event.target.value }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Title3, { children: "同时下载数" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "建议保持较小数量，避免站点限流。" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: String(settings.maxConcurrentDownloads), options: [1, 2, 3, 4].map((value) => ({ value: String(value), label: String(value) })), onChange: (event) => setSettings({ ...settings, maxConcurrentDownloads: Number(event.target.value) }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void saveSettings2(), children: "保存设置" })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Drawer, { open: rightPanel === "diagnostics", title: "诊断", onClose: () => setRightPanel(null), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Title3, { children: "解析与播放诊断" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "这里显示真实工具和下载目录状态；Cookie 和签名地址不会返回前端。" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void copyText(diagnosticText, "脱敏诊断已复制"), children: "复制脱敏诊断" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void refreshDiagnostics(), children: "刷新诊断" }),
        selectedResult !== void 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void bridge.shell.openExternal(selectedResult.originalUrl), children: "打开原网页" }) : null
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { rows: 20, readOnly: true, value: diagnosticText }),
      diagnostics !== null && diagnostics.activeDownloads > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
        "当前有 ",
        diagnostics.activeDownloads,
        " 个活动下载任务。"
      ] }) : null
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: deleteTaskId !== "", title: "删除下载记录", onClose: () => setDeleteTaskId(""), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDeleteTaskId(""), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => void deleteDownload(), children: "删除" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "确定删除这条下载记录？",
      /* @__PURE__ */ jsxRuntimeExports.jsx(LineBreak, {}),
      "已完成任务会按原有规则同步处理本地文件和视频库记录。"
    ] }) })
  ] });
}
function ResultTab({ items, selected, selectedIds, formatSelector, busy, select, selectIds, setFormatSelector, play, download, copyText }) {
  const formatOptions = selected?.formats.map((item) => ({ value: item.selector, label: item.displayName })) ?? [];
  if (items.length === 0)
    return /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "还没有解析结果", description: "先在上方粘贴链接，封面、标题和可用画质会显示在这里。", action: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => document.getElementById("remote-video-source")?.focus(), children: "输入链接" }) });
  if (items.length === 1 && selected !== void 0) return /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-feature", "data-remote-video-item": selected.itemId, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-feature__media", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteThumbnail, { source: selected.thumbnailUrl, alt: selected.title }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: selected.isLive ? "danger" : "neutral", children: selected.isLive ? "直播中" : durationText(selected.durationSeconds) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-feature__copy", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: selected.siteName || "远程视频" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Title2, { children: selected.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: selected.author || "未知作者" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(DescriptionList, { "data-remote-video-facts": true, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionTerm, { children: "发布时间" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionValue, { children: formatDate(selected.publishedAt) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionTerm, { children: "视频时长" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionValue, { children: durationText(selected.durationSeconds) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionTerm, { children: "视频 ID" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionValue, { children: selected.videoId || "未知" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionTerm, { children: "可用画质" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(DescriptionValue, { children: selected.formats.length > 0 ? `${selected.formats.length} 种` : "自动" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(FormLabel, { children: [
        "清晰度",
        /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: formatSelector, onChange: (event) => setFormatSelector(event.target.value), options: formatOptions.length > 0 ? formatOptions : [{ value: "", label: "自动｜最高画质" }] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-feature__actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: busy, onClick: () => void play(selected, "direct"), children: "播放" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: busy || selected.isLive, onClick: () => void play(selected, "cache"), children: "缓存后播放" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: selected.isLive, onClick: () => download([selected.itemId]), children: "下载" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void copyText(selected.originalUrl, "链接已复制"), children: "复制链接" })
      ] })
    ] })
  ] });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-toolbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(InlineText, { children: [
        "已解析 ",
        items.length,
        " 项 · 已选择 ",
        selectedIds.length,
        " 项"
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => selectIds(items.map((item) => item.itemId)), children: "全选" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => selectIds(items.filter((item) => item.downloadStatus !== "Downloaded").map((item) => item.itemId)), children: "选未下载" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => selectIds(items.filter((item) => !selectedIds.includes(item.itemId)).map((item) => item.itemId)), children: "反选" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "primary", disabled: selectedIds.length === 0, onClick: () => download(selectedIds), children: "下载所选" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-result-grid", role: "listbox", "aria-label": "解析结果", children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "article", variant: "remote-video-result-card", role: "option", "aria-selected": selected?.itemId === item.itemId, tabIndex: 0, onKeyDown: (event) => {
      if (event.target === event.currentTarget && (event.key === "Enter" || event.key === " ")) {
        event.preventDefault();
        select(item.itemId);
      }
    }, onClick: () => select(item.itemId), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Checkbox, { "aria-label": `选择 ${item.title}`, checked: selectedIds.includes(item.itemId), onClick: (event) => event.stopPropagation(), onChange: (event) => selectIds(event.target.checked ? [...selectedIds, item.itemId] : selectedIds.filter((id) => id !== item.itemId)) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-card__media", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteThumbnail, { source: item.thumbnailUrl, alt: item.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: durationText(item.durationSeconds) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-card__copy", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.author || "未知作者" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.siteName }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: item.downloadStatus === "Downloaded" || item.downloadStatus === "Completed" ? "success" : "neutral", children: downloadText(item.downloadStatus) })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-card__actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: (event) => {
          event.stopPropagation();
          void play(item, "direct");
        }, children: "播放" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: item.isLive, onClick: (event) => {
          event.stopPropagation();
          void play(item, "cache");
        }, children: "缓存" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: item.isLive, onClick: (event) => {
          event.stopPropagation();
          download([item.itemId]);
        }, children: "下载" })
      ] })
    ] }, item.itemId)) })
  ] });
}
function DownloadTab({ items, thumbnailFor, refresh, cancel, play, openLocation, remove }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-toolbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "包含进行中和已完成的任务" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: refresh, children: "刷新" })
    ] }),
    items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "暂无下载记录", description: "下载任务、速度与进度会显示在这里。" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-record-list", children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "article", variant: "remote-video-record-card", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-result-card__media", children: /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteThumbnail, { source: thumbnailFor(item.itemId), alt: item.title }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-record-card__main", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: downloadTone(item.status), children: downloadText(item.status) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-record-card__meta", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.author || "未知作者" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: fileSize(item.fileSize) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(TimeValue, { children: formatDate(item.createdAt) }),
          item.status === "Running" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(InlineText, { children: [
            item.speed,
            " · ",
            item.eta
          ] }) : null
        ] }),
        item.status === "Running" || item.status === "Queued" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Meter, { value: item.progress, max: 100 }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(InlineText, { children: [
            item.progress.toFixed(0),
            "%"
          ] })
        ] }) : null,
        item.errorMessage !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: item.errorMessage }) : null
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-record-card__actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", disabled: item.status !== "Running" && item.status !== "Queued", onClick: () => item.status === "Running" || item.status === "Queued" ? cancel(item.taskId) : void 0, visibility: item.status === "Running" || item.status === "Queued" ? "visible" : "hidden", children: "取消" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: item.status !== "Completed", onClick: () => item.status === "Completed" ? play(item.taskId) : void 0, visibility: item.status === "Completed" ? "visible" : "hidden", children: "播放" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", disabled: item.status !== "Completed", onClick: () => item.status === "Completed" ? openLocation(item.outputPath) : void 0, visibility: item.status === "Completed" ? "visible" : "hidden", children: "位置" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "danger", disabled: item.status === "Running" || item.status === "Queued", onClick: () => remove(item.taskId), children: "删除" })
      ] })
    ] }, item.taskId)) })
  ] });
}
function PlayTab({ items, thumbnailFor, refresh, replay }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-result-toolbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
        "最近共 ",
        items.length,
        " 条记录"
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: refresh, children: "刷新" })
    ] }),
    items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "暂无播放记录", description: "直接播放与缓存播放记录会显示在这里。" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-record-list", children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { as: "article", variant: "remote-video-record-card", onDoubleClick: () => replay(item.historyId), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-result-card__media", children: /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteThumbnail, { source: thumbnailFor(item.itemId ?? ""), alt: item.title }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-record-card__main", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.title }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "remote-video-record-card__meta", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.author || "未知作者" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.siteName })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: playActionText(item.action) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(TimeValue, { children: formatDate(item.playedAt) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutSlot, { variant: "remote-video-record-card__actions", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => replay(item.historyId), children: "再次播放" }) })
    ] }, item.historyId)) })
  ] });
}
function RemoteThumbnail({ source, alt }) {
  const [failed, setFailed] = reactExports.useState(false);
  reactExports.useEffect(() => setFailed(false), [source]);
  return source.trim() !== "" && !failed ? /* @__PURE__ */ jsxRuntimeExports.jsx(MediaImage, { src: source, alt, loading: "lazy", referrerPolicy: "no-referrer", onError: () => setFailed(true) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { "data-remote-video-thumbnail-empty": true, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: "image" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(SmallText, { children: "暂无封面" })
  ] });
}
function RemoteToggle({ title, checked, onChange }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: title, checked, onChange: (event) => onChange(event.target.checked) });
}
function PathInput({ label, value, onChange, browse }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label, value, onChange: (event) => onChange(event.target.value) }),
    browse === true ? /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void bridge.dialog.openDirectory().then((response) => {
      const path = response.payload?.filePaths[0];
      if (response.success && path !== void 0)
        onChange(path);
    }), children: "浏览" }) : null
  ] });
}
function messageOf$3(error) {
  return error instanceof Error ? error.message : String(error);
}
function formatDate(value) {
  if (!value)
    return "未知";
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? value : date.toLocaleString("zh-CN");
}
function durationText(seconds) {
  if (!Number.isFinite(seconds) || seconds <= 0)
    return "未知";
  const total = Math.round(seconds);
  const h = Math.floor(total / 3600);
  const m = Math.floor(total % 3600 / 60);
  const s = total % 60;
  return h > 0 ? `${h}:${String(m).padStart(2, "0")}:${String(s).padStart(2, "0")}` : `${m}:${String(s).padStart(2, "0")}`;
}
function fileSize(bytes) {
  if (!Number.isFinite(bytes) || bytes <= 0)
    return "未知大小";
  const units = ["B", "KB", "MB", "GB"];
  let value = bytes;
  let index = 0;
  while (value >= 1024 && index < units.length - 1) {
    value /= 1024;
    index += 1;
  }
  return `${value.toFixed(index === 0 ? 0 : 1)} ${units[index]}`;
}
function downloadText(status) {
  return { Queued: "排队中", Running: "下载中", Completed: "已下载", Downloaded: "已下载", Failed: "下载失败", Cancelled: "已取消" }[status] ?? status;
}
function downloadTone(status) {
  return status === "Completed" ? "success" : status === "Running" ? "info" : status === "Failed" ? "danger" : "neutral";
}
function playActionText(action) {
  return { CachePlay: "缓存播放", DirectStream: "直接播放", Downloaded: "本地播放", Replay: "再次播放" }[action] ?? action;
}
function RemoteSiteConfigPage() {
  const [cookieVisible, setCookieVisible] = reactExports.useState(false);
  const [enabled, setEnabled] = reactExports.useState(true);
  const [sessionStatus, setSessionStatus] = reactExports.useState("未检查");
  const [diagnosis, setDiagnosis] = reactExports.useState("");
  const [confirmClear, setConfirmClear] = reactExports.useState(false);
  const [items, setItems] = reactExports.useState([]);
  const [siteId, setSiteId] = reactExports.useState("");
  const [siteName, setSiteName] = reactExports.useState("");
  const [domainPattern, setDomainPattern] = reactExports.useState("");
  const [userAgent, setUserAgent] = reactExports.useState("");
  const [referer, setReferer] = reactExports.useState("");
  const [cookie, setCookie] = reactExports.useState("");
  const [cookieDirty, setCookieDirty] = reactExports.useState(false);
  const [hasCookie, setHasCookie] = reactExports.useState(false);
  const [notes, setNotes] = reactExports.useState("");
  const [message2, setMessage] = reactExports.useState("");
  const [confirmDelete, setConfirmDelete] = reactExports.useState(false);
  const canSave = siteName.trim() !== "" && domainPattern.trim() !== "";
  const openDouyin = () => {
    void bridge.window.open("douyin-login");
  };
  async function diagnose() {
    setSessionStatus("正在检查…");
    const response = await bridge.douyin.inspectSession();
    if (!response.success || response.payload === null) {
      setSessionStatus("检查失败");
      setDiagnosis(response.error?.message ?? "未知错误");
      return;
    }
    const value = response.payload;
    setSessionStatus(value.hasSession ? "已存在登录会话" : value.cookieCount > 0 ? "已有 Cookie，未检测到 sessionid" : "未登录");
    setDiagnosis(JSON.stringify(value, null, 2));
  }
  async function clearSession() {
    const response = await bridge.douyin.clearSession();
    setConfirmClear(false);
    if (!response.success) {
      setDiagnosis("清除失败：" + (response.error?.message ?? "未知错误"));
      return;
    }
    setSessionStatus("未登录");
    setDiagnosis("APP 抖音会话已清除。");
  }
  reactExports.useEffect(() => {
    void diagnose();
    void reloadSites();
  }, []);
  async function reloadSites(preferredId) {
    const response = await bridge.core.invoke({ type: "remote_site.list", payload: { enabledOnly: false } });
    const values = response.success && Array.isArray(response.payload) ? response.payload : [];
    setItems(values);
    const selected = values.find((item) => item.siteId === preferredId) ?? values[0];
    if (selected !== void 0)
      await selectSite(selected.siteId);
    else
      newSite();
  }
  async function selectSite(id) {
    const response = await bridge.core.invoke({ type: "remote_site.get", payload: { siteId: id } });
    if (!response.success || !isRemoteSiteDetail(response.payload))
      return;
    const detail = response.payload;
    let settings = {};
    try {
      settings = JSON.parse(detail.site.settingsJson);
    } catch {
      settings = {};
    }
    setSiteId(detail.site.siteId);
    setSiteName(detail.site.siteName);
    setDomainPattern(detail.site.domainPattern);
    setEnabled(detail.site.isEnabled);
    setUserAgent(settings.userAgent ?? "");
    setReferer(settings.referer ?? "");
    setNotes(settings.notes ?? "");
    setCookie("");
    setCookieDirty(false);
    setHasCookie(detail.site.hasProtectedCookie);
    setMessage("");
  }
  function newSite() {
    setSiteId("");
    setSiteName("");
    setDomainPattern("");
    setEnabled(true);
    setUserAgent("");
    setReferer("");
    setCookie("");
    setCookieDirty(false);
    setHasCookie(false);
    setNotes("");
    setMessage("");
  }
  async function saveSite() {
    if (siteName.trim() === "" || domainPattern.trim() === "") {
      setMessage("站点名称和域名匹配不能为空。");
      return;
    }
    const id = siteId || `site_${crypto.randomUUID().replaceAll("-", "")}`;
    const site = { siteId: id, siteName: siteName.trim(), domainPattern: domainPattern.trim(), adapterKey: "", qualityPreference: "", isEnabled: enabled, settingsJson: JSON.stringify({ userAgent, referer, notes }), updatedAt: (/* @__PURE__ */ new Date()).toISOString(), hasProtectedCookie: hasCookie };
    const response = await bridge.core.invoke({ type: "remote_site.save", payload: { site, plainCookie: cookieDirty ? cookie : null } });
    if (!response.success) {
      setMessage(response.error?.message ?? "保存失败。");
      return;
    }
    setHasCookie(cookieDirty ? cookie.trim() !== "" : hasCookie);
    setCookie("");
    setCookieDirty(false);
    await reloadSites(id);
    setMessage("已保存。");
  }
  async function deleteSite() {
    if (siteId === "") return;
    const response = await bridge.core.invoke({ type: "remote_site.delete", payload: { siteId } });
    if (!response.success) {
      setConfirmDelete(false);
      setMessage(response.error?.message ?? "删除失败，请重试。");
      return;
    }
    setConfirmDelete(false);
    await reloadSites();
    setMessage("站点配置已删除。");
  }
  const tools = /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: openDouyin, children: "抖音接入" });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "站点配置", tools }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "sidebar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductSidebar, { title: "站点列表", actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "primary", onClick: newSite, children: "新建" }), children: items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: "暂无站点配置" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ProductList, { children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { appearance: "navigation", selected: item.siteId === siteId, onClick: () => void selectSite(item.siteId), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.siteName }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.domainPattern })
      ] }, item.siteId)) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "配置详情", footer: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", disabled: siteId === "", onClick: () => setConfirmDelete(true), children: "删除" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: !canSave, onClick: () => void saveSite(), children: "保存配置" })
      ] }), children: message2 }), scroll: true, emphasis: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ConfigSection, { title: "基础匹配", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "站点名称", value: siteName, onChange: (event) => setSiteName(event.target.value) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "域名匹配", value: domainPattern, onChange: (event) => setDomainPattern(event.target.value) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "启用站点配置", checked: enabled, onChange: (event) => setEnabled(event.target.checked) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ConfigSection, { title: "请求身份", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "User-Agent", value: userAgent, onChange: (event) => setUserAgent(event.target.value) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "Referer", value: referer, onChange: (event) => setReferer(event.target.value) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ConfigSection, { title: "抖音会话", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
            "抖音会话状态：",
            sessionStatus
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: openDouyin, children: "登录/刷新会话" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void diagnose(), children: "诊断会话" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setConfirmClear(true), children: "清除 APP 会话" })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "诊断只显示 Cookie 数量和关键字段是否存在，不显示 Cookie 内容。" }),
          diagnosis !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(CodeBlock, { children: diagnosis }) : null
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ConfigSection, { title: "Cookie", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: cookieDirty ? cookie.trim() === "" ? "保存后会清除现有 Cookie。" : "已填写新 Cookie，保存后会替换现有内容。" : hasCookie ? "已安全保存 Cookie；出于安全考虑不会回显明文。" : "尚未保存 Cookie。" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "actions", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setCookieVisible((value) => !value), children: cookieVisible ? "隐藏输入" : "输入新 Cookie" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void navigator.clipboard.readText().then((value) => {
              setCookie(value);
              setCookieDirty(true);
              setCookieVisible(true);
            }).catch(() => setMessage("无法读取剪贴板，请手动输入 Cookie。")), children: "粘贴" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: !hasCookie && !cookieDirty, onClick: () => {
              setCookie("");
              setCookieDirty(true);
              setCookieVisible(false);
            }, children: "清除已保存 Cookie" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setMessage(!cookieDirty ? "Cookie 未修改，将保留现有内容。" : cookie.trim() === "" ? "保存后会清除 Cookie。" : "新 Cookie 已填写，保存后会加密替换。"), children: "校验" })
          ] }),
          cookieVisible ? /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { "aria-label": "Cookie", rows: 8, value: cookie, onChange: (event) => {
            setCookie(event.target.value);
            setCookieDirty(true);
          } }) : null
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ConfigSection, { title: "备注", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { "aria-label": "备注", rows: 4, value: notes, onChange: (event) => setNotes(event.target.value) }) })
      ] }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: confirmClear, title: "抖音会话", description: "清除女仆助手的抖音专用会话？这不会影响系统 Chrome 或 Edge 的登录状态。", onClose: () => setConfirmClear(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setConfirmClear(false), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => void clearSession(), children: "清除" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "只会删除 `persist:aimaid-douyin` 独立分区中的 Cookie 和站点存储。" }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: confirmDelete, title: "删除站点配置", description: `确认删除 ${siteName || "这个站点配置"}？`, onClose: () => setConfirmDelete(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setConfirmDelete(false), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => void deleteSite(), children: "删除" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "站点设置和安全存储的 Cookie 会一起删除。" }) })
  ] });
}
function isRemoteSiteDetail(value) {
  return typeof value === "object" && value !== null && "site" in value && typeof value.site === "object" && value.site !== null;
}
function ConfigSection({ title, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingsSection, { title, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children }) });
}
function ScriptsPage() {
  const [items, setItems] = reactExports.useState([]);
  const [launcherId, setLauncherId] = reactExports.useState("");
  const [command, setCommand] = reactExports.useState("");
  const [name, setName] = reactExports.useState("");
  const [exePath, setExePath] = reactExports.useState("");
  const [argumentsValue, setArgumentsValue] = reactExports.useState("");
  const [workingDirectory, setWorkingDirectory] = reactExports.useState("");
  const [enabled, setEnabled] = reactExports.useState(true);
  const [status, setStatus] = reactExports.useState("");
  const [warning, setWarning] = reactExports.useState("");
  const commandIsValid = command.trim().startsWith("-") && command.trim().length >= 2 && command.trim().length <= 32 && !/\s/u.test(command.trim());
  const canSave = commandIsValid && name.trim() !== "" && exePath.trim() !== "";
  reactExports.useEffect(() => {
    void reload();
  }, []);
  const reload = async (preferredId) => {
    const response = await bridge.core.invoke({ type: "script.list", payload: {} });
    if (!response.success || !Array.isArray(response.payload)) {
      setWarning(response.error?.message ?? "快捷脚本读取失败。");
      return;
    }
    const values = response.payload;
    setItems(values);
    const selected = values.find((item) => item.launcherId === preferredId) ?? values[0];
    if (selected !== void 0)
      selectItem(selected);
    else
      newItem();
  };
  const selectItem = (item) => {
    setLauncherId(item.launcherId);
    setCommand(item.commandText);
    setName(item.displayName);
    setExePath(item.exePath);
    setArgumentsValue(item.arguments);
    setWorkingDirectory(item.workingDirectory);
    setEnabled(item.enabled);
    setStatus(`最后修改：${new Date(item.updatedAt).toLocaleString("zh-CN")}`);
  };
  const newItem = () => {
    setLauncherId("");
    setCommand("");
    setName("");
    setExePath("");
    setArgumentsValue("");
    setWorkingDirectory("");
    setEnabled(true);
    setStatus("");
  };
  const chooseExecutable = async () => {
    const response = await bridge.dialog.openFile([
      { name: "程序或脚本", extensions: ["exe", "bat", "cmd", "ps1", "vbs", "js"] },
      { name: "所有文件", extensions: ["*"] }
    ]);
    if (!response.success) {
      setWarning(response.error?.message ?? "程序选择失败。");
      return;
    }
    const path = readPaths(response.payload)[0];
    if (path !== void 0)
      setExePath(path);
  };
  const chooseWorkingDirectory = async () => {
    const response = await bridge.dialog.openDirectory();
    if (!response.success) {
      setWarning(response.error?.message ?? "工作目录选择失败。");
      return;
    }
    const path = response.payload?.filePaths[0];
    if (path !== void 0)
      setWorkingDirectory(path);
  };
  const save2 = async () => {
    const trimmed = command.trim();
    if (!trimmed.startsWith("-") || trimmed.length < 2 || trimmed.length > 32 || /\s/u.test(trimmed)) {
      setWarning("聊天指令必须以“-”开头、不含空格，且不超过 32 个字符。");
      return;
    }
    if (name.trim() === "" || exePath.trim() === "") {
      setWarning("显示名称和程序或脚本路径不能为空。");
      return;
    }
    const response = await bridge.core.invoke({ type: "script.save", payload: { launcher: {
      launcherId,
      commandText: trimmed,
      displayName: name.trim(),
      exePath: exePath.trim(),
      arguments: argumentsValue,
      workingDirectory: workingDirectory.trim(),
      enabled,
      updatedAt: (/* @__PURE__ */ new Date()).toISOString()
    } } });
    if (!response.success || !isLauncher(response.payload)) {
      setWarning(response.error?.message ?? "保存失败。");
      return;
    }
    await reload(response.payload.launcherId);
    setStatus("已保存，聊天框可立即使用。");
  };
  const run = async () => {
    if (launcherId === "") {
      setStatus("请先保存，再运行测试。");
      return;
    }
    const response = await bridge.core.invoke({ type: "script.run", payload: { launcherId } });
    setStatus(response.success && typeof response.payload === "string" ? response.payload : response.error?.message ?? "启动失败。");
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "快捷脚本" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "sidebar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductSidebar, { title: "快捷指令", description: `${items.length} 条可用指令`, actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "primary", onClick: newItem, children: "新增" }), children: items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: "暂无快捷指令" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ProductList, { children: items.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { selected: item.launcherId === launcherId, onClick: () => selectItem(item), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.displayName }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: item.commandText })
      ] }, item.launcherId)) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPanel, { title: launcherId === "" ? "新建快捷指令" : name || command, actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: launcherId === "", onClick: () => void run(), children: "运行测试" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: !canSave, onClick: () => void save2(), children: "保存" })
      ] }), footer: status !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: status }) : void 0, scroll: true, emphasis: true, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(FormField, { label: "聊天指令", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "聊天指令", value: command, onChange: (event) => setCommand(event.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(FormField, { label: "显示名称", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "显示名称", value: name, onChange: (event) => setName(event.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(PathField, { label: "程序或脚本路径", value: exePath, setValue: setExePath, browse: () => void chooseExecutable() }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(FormField, { label: "启动参数", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "启动参数", value: argumentsValue, onChange: (event) => setArgumentsValue(event.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(PathField, { label: "工作目录", value: workingDirectory, setValue: setWorkingDirectory, browse: () => void chooseWorkingDirectory() }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "启用此快捷指令", checked: enabled, onChange: (event) => setEnabled(event.target.checked) })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: warning !== "", title: "快捷脚本", onClose: () => setWarning(""), footer: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => setWarning(""), children: "确定" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: warning }) })
  ] });
}
function FormField({ label, children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(FormLabel, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: label }),
    children
  ] });
}
function PathField({ label, value, setValue, browse }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(FormField, { label, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": label, value, onChange: (event) => setValue(event.target.value) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: browse, children: "浏览" })
  ] }) });
}
function readPaths(value) {
  return typeof value === "object" && value !== null && "filePaths" in value && Array.isArray(value.filePaths) ? value.filePaths.filter((item) => typeof item === "string") : [];
}
function isLauncher(value) {
  return typeof value === "object" && value !== null && "launcherId" in value && typeof value.launcherId === "string";
}
const EMPTY = { currentCharacter: null, avatarUrl: "", roleState: null, tts: null, models: { chat: "--", cache: "--", proactive: "--" }, latencies: { chatLatencyMs: null, cacheLatencyMs: null, proactiveLatencyMs: null } };
function StatusPage() {
  const [snapshot, setSnapshot] = reactExports.useState(EMPTY);
  const [resources, setResources] = reactExports.useState(null);
  const [network, setNetwork] = reactExports.useState([]);
  const [serverHealth, setServerHealth] = reactExports.useState(null);
  const [serverSummary, setServerSummary] = reactExports.useState(null);
  const [codexQuota, setCodexQuota] = reactExports.useState(null);
  const refreshMain = reactExports.useCallback(async () => {
    const [characterResult, voiceResult, ttsResult, modelResult, businessResult] = await Promise.allSettled([
      loadCharacters(),
      bridge.core.invoke({ type: "status.role", payload: {} }),
      bridge.core.invoke({ type: "status.tts", payload: {} }, 5e3),
      bridge.core.invoke({ type: "model.list", payload: {} }),
      bridge.core.invoke({ type: "business_model.list", payload: {} })
    ]);
    const currentCharacter2 = characterResult.status === "fulfilled" ? characterResult.value.items.find((item) => item.roleId === characterResult.value.currentRoleId) ?? null : null;
    const avatar = currentCharacter2?.avatarPath ? await bridge.media.registerLocalFile(currentCharacter2.avatarPath) : null;
    const roleResponse = voiceResult.status === "fulfilled" && voiceResult.value.success ? voiceResult.value.payload : null;
    const ttsResponse = ttsResult.status === "fulfilled" && ttsResult.value.success ? ttsResult.value.payload : null;
    const modelConfigurations = modelResult.status === "fulfilled" && modelResult.value.success && Array.isArray(modelResult.value.payload) ? modelResult.value.payload : [];
    const businessConfigurations = businessResult.status === "fulfilled" && businessResult.value.success && Array.isArray(businessResult.value.payload) ? businessResult.value.payload : [];
    const resolvedModels = resolveStatusModels(modelConfigurations, businessConfigurations);
    const latencyResult = await bridge.core.invoke({ type: "status.llm_latencies", payload: { chatModel: resolvedModels.chat === "--" ? "" : resolvedModels.chat, cacheModel: resolvedModels.cache === "--" ? "" : resolvedModels.cache, proactiveModel: resolvedModels.proactive === "--" ? "" : resolvedModels.proactive } });
    setSnapshot({
      currentCharacter: currentCharacter2,
      avatarUrl: avatar?.success ? avatar.payload?.url ?? "" : "",
      roleState: roleResponse,
      tts: ttsResponse,
      models: resolvedModels,
      latencies: latencyResult.success && latencyResult.payload !== null ? latencyResult.payload : { chatLatencyMs: null, cacheLatencyMs: null, proactiveLatencyMs: null }
    });
    const errors = [];
    if (characterResult.status === "rejected") errors.push(messageOf$2(characterResult.reason));
    if (voiceResult.status === "rejected") errors.push(messageOf$2(voiceResult.reason));
    else if (!voiceResult.value.success) errors.push(voiceResult.value.error?.message ?? "角色状态读取失败");
    if (ttsResult.status === "rejected") errors.push(messageOf$2(ttsResult.reason));
    else if (!ttsResult.value.success) errors.push(ttsResult.value.error?.message ?? "TTS 状态读取失败");
    if (modelResult.status === "rejected") errors.push(messageOf$2(modelResult.reason));
    else if (!modelResult.value.success) errors.push(modelResult.value.error?.message ?? "模型读取失败");
    if (businessResult.status === "rejected") errors.push(messageOf$2(businessResult.reason));
    else if (!businessResult.value.success) errors.push(businessResult.value.error?.message ?? "业务模型读取失败");
    if (!latencyResult.success) errors.push(latencyResult.error?.message ?? "延迟读取失败");
    if (errors.length > 0) throw new Error(Array.from(new Set(errors)).join("；"));
  }, []);
  const refreshQuota = reactExports.useCallback(async () => {
    const response = await bridge.core.invoke({ type: "status.codex_quota", payload: {} }, 15e3);
    if (!response.success || response.payload === null) throw new Error(response.error?.message ?? "Codex 额度读取失败");
    setCodexQuota(response.payload);
  }, []);
  const refreshHealth = reactExports.useCallback(async () => {
    const response = await bridge.core.invoke({ type: "status.server.health", payload: {} }, 25e3);
    if (!response.success || response.payload === null) throw new Error(response.error?.message ?? "服务器健康状态读取失败");
    setServerHealth(response.payload);
  }, []);
  const refreshSummary = reactExports.useCallback(async () => {
    const response = await bridge.core.invoke({ type: "status.server.summary", payload: {} }, 25e3);
    if (!response.success || response.payload === null) throw new Error(response.error?.message ?? "服务器摘要读取失败");
    setServerSummary(response.payload);
  }, []);
  const refreshResources = reactExports.useCallback(async () => {
    const response = await bridge.core.invoke({ type: "status.resources", payload: {} });
    if (!response.success || response.payload === null) throw new Error(response.error?.message ?? "系统资源读取失败");
    setResources(response.payload);
  }, []);
  const refreshNetwork = reactExports.useCallback(async () => {
    const response = await bridge.core.invoke({ type: "status.network", payload: {} });
    if (!response.success || !Array.isArray(response.payload)) throw new Error(response.error?.message ?? "网络状态读取失败");
    setNetwork(response.payload);
  }, []);
  useStatusPoll(refreshMain, 3e3);
  useStatusPoll(refreshQuota, 6e4);
  useStatusPoll(refreshHealth, 6e4);
  useStatusPoll(refreshSummary, 3e5);
  useStatusPoll(refreshResources, 200);
  useStatusPoll(refreshNetwork, 3e3);
  const character = snapshot.currentCharacter;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Page, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "状态面板" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PageContent, { scroll: false, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "dashboard", "data-product": "status", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusPanelGrid, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPanel, { title: "角色", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusHero, { compact: true, avatar: /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { source: snapshot.avatarUrl, fallback: character?.name || "—", size: "md" }), value: character?.name || "--", detail: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            "亲密等级 ",
            snapshot.roleState?.intimacyLevel ?? "--",
            " / 5 · ",
            snapshot.roleState?.intimacyLabel ?? "--"
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusList, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "语音", value: character?.voiceName || character?.preferredVoiceId || "未配置" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "心情", value: snapshot.roleState?.hasMaidState ? snapshot.roleState.maidMoodText : "--" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "好感", value: snapshot.roleState?.hasMaidState ? String(snapshot.roleState.maidFavorability) : "--" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "陪伴", value: snapshot.roleState?.hasMaidState ? snapshot.roleState.maidCompanionshipText : "--" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "互动", value: snapshot.roleState?.hasMaidState ? `${snapshot.roleState.maidInteractionCount} 次` : "--" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "缓存", value: snapshot.roleState === null || snapshot.roleState.voiceCacheTotal <= 0 ? "--" : `${snapshot.roleState.voiceCacheCompleted}/${snapshot.roleState.voiceCacheTotal}` }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "最近互动", value: snapshot.roleState?.hasMaidState ? snapshot.roleState.maidLastInteractionText : "--" })
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "核心服务", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusList, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            StatusRow,
            {
              label: "TTS",
              state: snapshot.tts === null ? "unknown" : snapshot.tts.online ? "online" : "offline",
              value: snapshot.tts === null ? "--" : snapshot.tts.online ? "正常" : "异常",
              detail: `${snapshot.tts?.lastLatencyMs ? `最近 ${formatLatency(snapshot.tts.lastLatencyMs)}` : "暂无记录"} · 处理中 ${snapshot.tts?.pendingSynthesisCount ?? "--"} · 待播放 ${snapshot.tts?.pendingPlaybackCount ?? "--"}`
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "对话", value: snapshot.models.chat, detail: formatModelLatency(snapshot.latencies.chatLatencyMs), variant: "model" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "缓存", value: snapshot.models.cache, detail: formatModelLatency(snapshot.latencies.cacheLatencyMs), variant: "model" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "主动", value: snapshot.models.proactive, detail: formatModelLatency(snapshot.latencies.proactiveLatencyMs), variant: "model" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "设备与网络", description: "本机资源 · TCP 443", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusList, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "CPU", value: resources === null ? "--" : `${resources.cpuPercent.toFixed(0)}%`, meterPercent: resources?.cpuPercent ?? 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "GPU", value: resources === null ? "--" : resources.gpuPercent === null ? "N/A" : `${resources.gpuPercent.toFixed(0)}%`, meterPercent: resources?.gpuPercent ?? 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "内存", value: resources === null ? "--" : `${resources.workingSetMb.toFixed(0)} MB` }),
          ["ChatGPT", "Google", "X", "抖音", "百度"].map((name) => {
            const probe = network.find((item) => item.name === name);
            return /* @__PURE__ */ jsxRuntimeExports.jsx(
              StatusRow,
              {
                label: name,
                state: probe === void 0 ? "unknown" : probe.success ? "online" : "offline",
                value: probe === void 0 ? "--" : probe.success ? `${probe.latencyMs ?? 0} ms` : "超时"
              },
              name
            );
          })
        ] }) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusPanelGrid, { variant: "summary", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ServerPanel, { title: "腾讯云", state: serverHealth === null ? "unknown" : serverHealth.tencentCloud ? "online" : "offline", summary: serverSummary?.tencentCloud ?? null }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ServerPanel, { title: "AWS", state: serverHealth === null ? "unknown" : serverHealth.aws ? "online" : "offline", summary: serverSummary?.aws ?? null }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(CodexQuotaPanel, { quota: codexQuota })
      ] })
    ] }) })
  ] });
}
function ServerPanel({ title, state, summary }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title, actions: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { state }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusList, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(ServerMetricRow, { label: "内存", metric: summary?.memory ?? null }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ServerMetricRow, { label: "磁盘", metric: summary?.disk ?? null }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ServerMetricRow, { label: "本期流量", metric: summary?.traffic ?? null })
  ] }) });
}
function ServerMetricRow({ label, metric }) {
  const percent = metric === null || metric.totalBytes <= 0 ? 0 : Math.max(0, Math.min(100, metric.usedBytes * 100 / metric.totalBytes));
  const text = metric === null || metric.totalBytes <= 0 ? "-- / --" : `${formatBytes(metric.usedBytes)} / ${formatBytes(metric.totalBytes)}`;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label, value: text, meterPercent: percent });
}
function CodexQuotaPanel({ quota }) {
  if (quota === null)
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "Codex 额度", actions: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { state: "unknown" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusList, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "状态", value: "未读取" }) }) });
  if (!quota.loggedIn)
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "Codex 额度", actions: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { state: "unknown" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusList, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "状态", value: "未登录" }) }) });
  const account = `${quota.account || "当前用户"}${quota.plan && quota.plan.toLowerCase() !== "unknown" ? ` · ${formatPlan(quota.plan)}` : ""}`;
  const remaining = [quota.primary?.remainingPercent, quota.secondary?.remainingPercent].filter((value) => value !== void 0);
  const lowest = remaining.length > 0 ? Math.min(...remaining) : -1;
  const state = lowest > 20 ? "online" : lowest >= 0 ? "offline" : "unknown";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "Codex 额度", description: quota.updatedAt || void 0, actions: /* @__PURE__ */ jsxRuntimeExports.jsx(StatusDot, { state }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(StatusList, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "账户", value: account }),
    quota.error ? /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "额度", value: quota.error }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      quota.primary !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: quota.primary.label, value: `剩余 ${Math.max(0, Math.min(100, quota.primary.remainingPercent)).toFixed(0)}%`, detail: quota.primary.resetsAt ? `重置于 ${quota.primary.resetsAt}` : void 0 }) : null,
      quota.secondary !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: quota.secondary.label, value: `剩余 ${Math.max(0, Math.min(100, quota.secondary.remainingPercent)).toFixed(0)}%`, detail: quota.secondary.resetsAt ? `重置于 ${quota.secondary.resetsAt}` : void 0 }) : null,
      quota.credits !== null && quota.credits !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(StatusRow, { label: "Credits", value: quota.credits === "unlimited" ? "Unlimited" : quota.credits }) : null
    ] })
  ] }) });
}
function resolveStatusModels(models, businesses) {
  const resolve = (businessKey) => {
    const mapping = businesses.find((item) => item.businessKey === businessKey && item.isEnabled);
    if (mapping === void 0)
      return "--";
    return models.find((item) => item.modelKey === mapping.modelKey)?.model || "--";
  };
  return { chat: resolve("chat_reply"), cache: resolve("lazy_voice_cache"), proactive: resolve("proactive_decision") };
}
function formatLatency(milliseconds) {
  return milliseconds >= 1e3 ? `${(milliseconds / 1e3).toFixed(1)}s` : `${milliseconds.toFixed(0)}ms`;
}
function formatModelLatency(milliseconds) {
  return milliseconds !== null && milliseconds > 0 ? `最近 ${formatLatency(milliseconds)}` : "暂无响应";
}
function formatBytes(bytes) {
  const units = ["B", "KB", "MB", "GB", "TB"];
  let scaled = Math.max(0, bytes);
  let index = 0;
  while (scaled >= 1024 && index < units.length - 1) {
    scaled /= 1024;
    index += 1;
  }
  return index === 0 ? `${scaled.toFixed(0)} ${units[index]}` : `${scaled.toFixed(2)} ${units[index]}`;
}
function formatPlan(plan) {
  return plan.length === 0 ? "" : `${plan[0].toUpperCase()}${plan.slice(1).toLowerCase()}`;
}
function useStatusPoll(task, intervalMs) {
  const taskRef = reactExports.useRef(task);
  taskRef.current = task;
  reactExports.useEffect(() => {
    let active = true;
    let timer;
    const run = async () => {
      if (!active) return;
      try {
        await taskRef.current();
      } catch (reason) {
        console.error("Status poll failed:", messageOf$2(reason));
      } finally {
        if (active) timer = window.setTimeout(() => void run(), intervalMs);
      }
    };
    void run();
    return () => {
      active = false;
      if (timer !== void 0) window.clearTimeout(timer);
    };
  }, [intervalMs]);
}
function messageOf$2(reason) {
  return reason instanceof Error ? reason.message : String(reason);
}
function calculateTimerClock(mode, startedAt, elapsedAtStart, remainingAtStart, now) {
  const delta = startedAt === null ? 0 : Math.max(0, Math.floor((now - startedAt) / 1e3));
  if (mode === "countup") return { elapsedSeconds: elapsedAtStart + delta, remainingSeconds: 0, completed: false };
  const remainingSeconds = Math.max(0, remainingAtStart - delta);
  return {
    elapsedSeconds: elapsedAtStart + Math.min(delta, remainingAtStart),
    remainingSeconds,
    completed: remainingSeconds === 0
  };
}
const QUICK_TIMES = [10, 15, 20, 25, 30];
const DIGIT_COLORS = [
  ["深色", "#101820"],
  ["白色", "#FFFFFF"],
  ["绿色", "#18A058"],
  ["黄色", "#F2B705"],
  ["红色", "#D64545"],
  ["蓝色", "#2474D8"]
];
function TimerPage() {
  const toast = useToast();
  const [mode, setMode] = reactExports.useState("idle");
  const [running, setRunning] = reactExports.useState(false);
  const [elapsedSeconds, setElapsedSeconds] = reactExports.useState(0);
  const [remainingSeconds, setRemainingSeconds] = reactExports.useState(0);
  const [recordsVisible, setRecordsVisible] = reactExports.useState(false);
  const [transparent, setTransparent] = reactExports.useState(false);
  const [digitColorIndex, setDigitColorIndex] = reactExports.useState(0);
  const [records, setRecords] = reactExports.useState([]);
  const startedAtRef = reactExports.useRef(null);
  const elapsedAtStartRef = reactExports.useRef(0);
  const remainingAtStartRef = reactExports.useRef(0);
  const completionAnnouncedRef = reactExports.useRef(false);
  reactExports.useEffect(() => {
    void loadRecords(setRecords).catch((reason) => toast.show(messageOf$1(reason), "error"));
  }, [toast]);
  reactExports.useEffect(() => {
    if (!running)
      return void 0;
    const update = () => {
      const startedAt = startedAtRef.current;
      if (startedAt === null) return;
      if (mode !== "idle") {
        const clock = calculateTimerClock(mode, startedAt, elapsedAtStartRef.current, remainingAtStartRef.current, Date.now());
        setRemainingSeconds(clock.remainingSeconds);
        setElapsedSeconds(clock.elapsedSeconds);
        if (clock.completed) {
          startedAtRef.current = null;
          setRunning(false);
          if (!completionAnnouncedRef.current) {
            completionAnnouncedRef.current = true;
            toast.show("倒计时结束。", "success");
            showSystemNotification("计时器", "倒计时结束。");
          }
        }
      }
    };
    update();
    const timer = window.setInterval(update, 250);
    return () => window.clearInterval(timer);
  }, [mode, running, toast]);
  const displaySeconds = mode === "countdown" ? remainingSeconds : elapsedSeconds;
  const timeText = formatClock(displaySeconds);
  const statusText = mode === "idle" ? "待命中" : mode === "countup" ? running ? "正计时中" : "已暂停" : running ? "倒计时中" : remainingSeconds === 0 ? "倒计时结束" : "已暂停";
  const digitColorName = (DIGIT_COLORS[digitColorIndex] ?? DIGIT_COLORS[0])[0];
  const startCountdown = (minutes) => {
    requestNotificationPermission();
    setMode("countdown");
    setElapsedSeconds(0);
    setRemainingSeconds(minutes * 60);
    elapsedAtStartRef.current = 0;
    remainingAtStartRef.current = minutes * 60;
    startedAtRef.current = Date.now();
    completionAnnouncedRef.current = false;
    setRunning(true);
  };
  const startCountUp = () => {
    setMode("countup");
    setElapsedSeconds(0);
    setRemainingSeconds(0);
    elapsedAtStartRef.current = 0;
    remainingAtStartRef.current = 0;
    startedAtRef.current = Date.now();
    completionAnnouncedRef.current = false;
    setRunning(true);
  };
  const toggleRunning = () => {
    if (mode === "idle") return;
    if (running) {
      const clock = calculateTimerClock(mode, startedAtRef.current, elapsedAtStartRef.current, remainingAtStartRef.current, Date.now());
      setElapsedSeconds(clock.elapsedSeconds);
      setRemainingSeconds(clock.remainingSeconds);
      elapsedAtStartRef.current = clock.elapsedSeconds;
      remainingAtStartRef.current = clock.remainingSeconds;
      startedAtRef.current = null;
      setRunning(false);
      return;
    }
    elapsedAtStartRef.current = elapsedSeconds;
    remainingAtStartRef.current = remainingSeconds;
    startedAtRef.current = Date.now();
    setRunning(true);
  };
  const reset = () => {
    setMode("idle");
    setRunning(false);
    setElapsedSeconds(0);
    setRemainingSeconds(0);
    elapsedAtStartRef.current = 0;
    remainingAtStartRef.current = 0;
    startedAtRef.current = null;
    completionAnnouncedRef.current = false;
  };
  const saveRecord = async () => {
    const durationSeconds = mode === "idle" ? 0 : calculateTimerClock(mode, running ? startedAtRef.current : null, elapsedAtStartRef.current, remainingAtStartRef.current, Date.now()).elapsedSeconds;
    const record = { id: `timer_${crypto.randomUUID().replaceAll("-", "")}`, savedAt: /* @__PURE__ */ new Date(), durationSeconds };
    const response = await bridge.core.invoke({ type: "timer_record.save", payload: { record: { recordId: record.id, savedAt: record.savedAt.toISOString(), durationSeconds: record.durationSeconds } } });
    if (!response.success) {
      toast.show(response.error?.message ?? "计时记录保存失败。", "error");
      return;
    }
    setRecords((current) => [...current, record]);
    toast.show("记录已保存。", "success");
  };
  if (transparent) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(MainRegion, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Pressable, { onClick: () => setTransparent(false), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: timeText }) }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "计时器" }),
    recordsVisible ? /* @__PURE__ */ jsxRuntimeExports.jsx(RecordsPage, { records, back: () => setRecordsVisible(false), remove: async (id) => {
      const response = await bridge.core.invoke({ type: "timer_record.delete", payload: { recordId: id } });
      if (!response.success) {
        toast.show(response.error?.message ?? "计时记录删除失败。", "error");
        return;
      }
      setRecords((current) => current.filter((item) => item.id !== id));
      toast.show("记录已删除。", "success");
    } }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", "data-product": "timer", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductHero, { eyebrow: statusText, value: timeText, detail: mode === "idle" ? void 0 : `本次已计时 ${formatDuration(elapsedSeconds)}`, actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: mode === "idle" || mode === "countdown" && remainingSeconds === 0, onClick: toggleRunning, children: running ? "暂停" : "继续计时" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: mode === "idle", onClick: reset, children: "重置" })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "快速开始", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "quick-actions", children: [
        QUICK_TIMES.map((minutes) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { onClick: () => startCountdown(minutes), children: [
          minutes,
          " 分钟"
        ] }, minutes)),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: startCountUp, children: "从 0 开始" })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "更多操作", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "actions", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: mode === "idle", onClick: () => void saveRecord(), children: "保存记录" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setRecordsVisible(true), children: "查看记录" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setTransparent(true), children: "透明模式" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDigitColorIndex((value) => (value + 1) % DIGIT_COLORS.length), children: digitColorName })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: mode === "countdown" && remainingSeconds === 0 ? "倒计时结束。" : `当前记录 ${records.length} 条 · 数字颜色 ${digitColorName}` })
    ] })
  ] });
}
function RecordsPage({ records, back, remove }) {
  const now = /* @__PURE__ */ new Date();
  const [year, setYear] = reactExports.useState(now.getFullYear());
  const [month, setMonth] = reactExports.useState(now.getMonth() + 1);
  const [contextMenu, setContextMenu] = reactExports.useState(null);
  const years = reactExports.useMemo(() => Array.from(/* @__PURE__ */ new Set([now.getFullYear(), ...records.map((item) => item.savedAt.getFullYear())])).sort((a, b) => b - a), [records, now]);
  const monthRecords = records.filter((item) => item.savedAt.getFullYear() === year && item.savedAt.getMonth() + 1 === month);
  const yearRecords = records.filter((item) => item.savedAt.getFullYear() === year);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", "data-product": "timer-records", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: back, children: "返回计时器" }), actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "年份", value: String(year), options: years.map((value) => ({ value: String(value), label: `${value} 年` })), onChange: (event) => setYear(Number(event.target.value)) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "月份", value: String(month), options: Array.from({ length: 12 }, (_, index) => ({ value: String(index + 1), label: `${index + 1} 月` })), onChange: (event) => setMonth(Number(event.target.value)) })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "metrics", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Stat, { scope: "当月", records: monthRecords }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Stat, { scope: "当年", records: yearRecords }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Stat, { scope: "历史", records })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: `${year} 年 ${month} 月`, description: `${monthRecords.length} 条计时记录`, scroll: true, children: monthRecords.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: "暂无计时记录" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ProductGrid, { density: "cards", children: monthRecords.map((record) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Article, { onContextMenu: (event) => {
      event.preventDefault();
      event.stopPropagation();
      setContextMenu({ id: record.id, x: event.clientX, y: event.clientY });
    }, title: "右键删除记录", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Strong, { children: [
        record.savedAt.getMonth() + 1,
        "月",
        record.savedAt.getDate(),
        "日"
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: formatDuration(record.durationSeconds) })
    ] }, record.id)) }) }),
    contextMenu !== null ? /* @__PURE__ */ jsxRuntimeExports.jsx(ContextMenuSurface, { label: "计时记录操作", position: contextMenu, onClose: () => setContextMenu(null), items: [
      { id: "delete", label: "删除记录", danger: true, onSelect: () => void remove(contextMenu.id) }
    ] }) : null
  ] });
}
function Stat({ scope, records }) {
  const durations = records.map((item) => item.durationSeconds);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: scope, value: `${records.length} 次`, detail: durations.length === 0 ? "暂无记录" : `最高 ${formatDuration(Math.max(...durations))} · 最低 ${formatDuration(Math.min(...durations))}` });
}
function formatClock(seconds) {
  const safe = Math.max(0, seconds);
  const hours = Math.floor(safe / 3600);
  const minutes = Math.floor(safe % 3600 / 60);
  const remaining = safe % 60;
  return hours >= 1 ? `${pad(hours)}:${pad(minutes)}:${pad(remaining)}` : `${pad(minutes)}:${pad(remaining)}`;
}
function formatDuration(seconds) {
  const hours = Math.floor(seconds / 3600);
  const minutes = Math.floor(seconds % 3600 / 60);
  const remaining = seconds % 60;
  if (hours >= 1)
    return `${hours}小时${minutes}分${remaining}秒`;
  if (minutes >= 1)
    return `${minutes}分${remaining}秒`;
  return `${remaining}秒`;
}
function pad(value) {
  return value.toString().padStart(2, "0");
}
async function loadRecords(setRecords) {
  const response = await bridge.core.invoke({ type: "timer_record.list", payload: {} });
  if (!response.success || !Array.isArray(response.payload))
    throw new Error(response.error?.message ?? "计时记录读取失败。");
  setRecords(response.payload.flatMap((value) => {
    if (!isTimerRecordDto(value)) return [];
    return [{ id: value.recordId, savedAt: new Date(value.savedAt), durationSeconds: value.durationSeconds }];
  }));
}
function messageOf$1(reason) {
  return reason instanceof Error ? reason.message : String(reason);
}
function requestNotificationPermission() {
  if (typeof Notification !== "undefined" && Notification.permission === "default") void Notification.requestPermission();
}
function showSystemNotification(title, body) {
  if (typeof Notification !== "undefined" && Notification.permission === "granted") new Notification(title, { body });
}
function isTimerRecordDto(value) {
  if (typeof value !== "object" || value === null) return false;
  const record = value;
  return typeof record.recordId === "string" && typeof record.savedAt === "string" && typeof record.durationSeconds === "number";
}
const TYPES = [
  { value: "All", label: "全部类型" },
  { value: "Login", label: "登录密码" },
  { value: "Wallet", label: "钱包" },
  { value: "ApiKey", label: "API Key" },
  { value: "Server", label: "服务器" }
];
const FIELDS = {
  Login: [["名称", false], ["账号", false], ["密码", true], ["网址", false], ["分类", false], ["备注", false]],
  Wallet: [["名称", false], ["链类型", false], ["钱包地址", false], ["私钥", true], ["助记词", true], ["备注", false]],
  ApiKey: [["名称", false], ["平台", false], ["API Key", true], ["Secret", true], ["备注", false]],
  Server: [["名称", false], ["地址", false], ["端口", false], ["账号", false], ["密码", true], ["备注", false]]
};
function VaultPage() {
  const [items, setItems] = reactExports.useState([]);
  const [currentId, setCurrentId] = reactExports.useState(null);
  const [createdAt, setCreatedAt] = reactExports.useState("");
  const [updatedAt, setUpdatedAt] = reactExports.useState("");
  const [type, setType] = reactExports.useState("Login");
  const [typeFilter, setTypeFilter] = reactExports.useState("All");
  const [query, setQuery] = reactExports.useState("");
  const [historyOpen, setHistoryOpen] = reactExports.useState(false);
  const [histories, setHistories] = reactExports.useState([]);
  const [selectedHistoryId, setSelectedHistoryId] = reactExports.useState(null);
  const [deleteOpen, setDeleteOpen] = reactExports.useState(false);
  const [exportStatus, setExportStatus] = reactExports.useState("");
  const [values, setValues] = reactExports.useState(createDraft);
  const [warning, setWarning] = reactExports.useState("");
  const [toast, setToast] = reactExports.useState("");
  const fields = reactExports.useMemo(() => FIELDS[type], [type]);
  async function load(selectId) {
    const response = await bridge.core.invoke({ type: "vault.list", payload: {} });
    if (!response.success || !Array.isArray(response.payload)) {
      setExportStatus(response.error?.message ?? "密码库读取失败。");
      return;
    }
    const next = response.payload.filter(isVaultItem);
    setItems(next);
    const targetId = selectId === null ? next[0]?.itemId : selectId ?? currentId ?? next[0]?.itemId;
    if (targetId !== void 0)
      await selectItem(targetId);
    else
      showDraft();
  }
  reactExports.useEffect(() => {
    void load();
  }, []);
  async function selectItem(itemId) {
    const response = await bridge.core.invoke({ type: "vault.secret.reveal", payload: { itemId } });
    if (!response.success || !isVaultDetail(response.payload)) {
      setExportStatus(response.error?.message ?? "密码库条目读取失败。");
      return;
    }
    const detail = response.payload;
    const metadata = parseRecord(detail.item.publicMetadataJson);
    const secrets = parseRecord(detail.secret ?? "");
    setCurrentId(detail.item.itemId);
    setCreatedAt(detail.item.createdAt);
    setUpdatedAt(detail.item.updatedAt);
    setType(isVaultType(detail.item.itemType) ? detail.item.itemType : "Login");
    setValues({
      名称: detail.item.name,
      分类: detail.item.category,
      账号: detail.item.account,
      网址: detail.item.url,
      平台: detail.item.platform,
      备注: metadata.Remark ?? "",
      链类型: metadata.ChainType ?? "",
      钱包地址: metadata.WalletAddress ?? "",
      地址: metadata.ServerAddress ?? "",
      端口: metadata.ServerPort ?? "",
      密码: secrets.Password ?? "",
      "API Key": secrets.ApiKey ?? "",
      Secret: secrets.Secret ?? "",
      私钥: secrets.PrivateKey ?? "",
      助记词: secrets.Mnemonic ?? ""
    });
  }
  function showDraft() {
    setCurrentId(null);
    setCreatedAt("");
    setUpdatedAt("");
    setType("Login");
    setValues(createDraft());
    setHistoryOpen(false);
  }
  function add() {
    showDraft();
    setWarning("");
    setExportStatus("");
  }
  async function save2() {
    if ((values["名称"] ?? "").trim() === "") {
      setWarning("名称不能为空。");
      return;
    }
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const itemId = currentId ?? crypto.randomUUID();
    const response = await bridge.core.invoke({ type: "vault.save", payload: { item: makeDto(itemId, type, values, createdAt || now, now), plainSecret: encodeSecrets(values) } });
    if (!response.success) {
      setExportStatus(response.error?.message ?? "密码库保存失败。");
      return;
    }
    await load(itemId);
  }
  async function remove() {
    if (currentId === null)
      return;
    const response = await bridge.core.invoke({ type: "vault.delete", payload: { itemId: currentId } });
    if (!response.success) {
      setExportStatus(response.error?.message ?? "删除条目失败。");
      return;
    }
    setDeleteOpen(false);
    setCurrentId(null);
    await load(null);
  }
  async function exportVault() {
    const stamp = (/* @__PURE__ */ new Date()).toISOString().replace(/[-:T]/gu, "").slice(0, 15);
    const response = await bridge.dialog.saveFile(`AI_Maid_Vault_${stamp}.7z`, [{ name: "7z 加密压缩包", extensions: ["7z"] }]);
    if (!response.success) {
      setExportStatus(response.error?.message ?? "导出位置选择失败。");
      return;
    }
    if (response.payload?.canceled || response.payload?.filePath === void 0)
      return;
    const exported = await bridge.core.invoke({ type: "vault.export", payload: { outputPath: response.payload.filePath } }, 12e4);
    setExportStatus(exported.success ? "导出完成。" : exported.error?.message ?? "导出失败。");
  }
  async function openHistory() {
    if (currentId === null)
      return;
    const response = await bridge.core.invoke({ type: "vault.history.list", payload: { itemId: currentId } });
    if (!response.success || !Array.isArray(response.payload)) {
      setExportStatus(response.error?.message ?? "历史记录读取失败。");
      return;
    }
    const next = response.payload.filter(isVaultHistory);
    setHistories(next);
    setSelectedHistoryId(next[0]?.historyId ?? null);
    setHistoryOpen(true);
  }
  async function restoreHistory() {
    if (selectedHistoryId === null || currentId === null)
      return;
    const response = await bridge.core.invoke({ type: "vault.history.restore", payload: { historyId: selectedHistoryId } });
    if (!response.success) {
      setExportStatus(response.error?.message ?? "恢复历史记录失败。");
      return;
    }
    setHistoryOpen(false);
    await load(currentId);
  }
  const copy = async (label) => {
    try {
      await navigator.clipboard.writeText(values[label] ?? "");
      setToast("已复制");
    } catch {
      setToast("复制失败");
    }
    window.setTimeout(() => setToast(""), 800);
  };
  const filtered = items.filter((item) => {
    if (typeFilter !== "All" && item.itemType !== typeFilter)
      return false;
    const text = query.trim().toLocaleLowerCase();
    if (text === "")
      return true;
    const metadata = parseRecord(item.publicMetadataJson);
    return [item.name, item.account, item.platform, item.url, item.category, metadata.Remark, metadata.WalletAddress, metadata.ServerAddress].some((value) => (value ?? "").toLocaleLowerCase().includes(text));
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "密码库", tools: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => void exportVault(), children: "导出" }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "sidebar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductSidebar, { title: "条目列表", description: `${filtered.length}/${items.length} 个条目`, actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", variant: "primary", onClick: add, children: "新增" }), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { layout: "stacked", lead: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": "搜索密码库", value: query, onChange: (event) => setQuery(event.target.value) }), actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { "aria-label": "条目类型", value: typeFilter, onChange: (event) => setTypeFilter(event.target.value), options: TYPES }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductList, { children: filtered.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: "没有匹配的条目" }) : filtered.map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { selected: currentId === item.itemId, onClick: () => void selectItem(item.itemId), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: item.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: listSubtitle(item) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(SmallText, { children: [
            item.category || "未分类",
            " · ",
            formatListTime(item.updatedAt),
            " ",
            /* @__PURE__ */ jsxRuntimeExports.jsx(Emphasis, { children: typeLabel(item.itemType) })
          ] })
        ] }, item.itemId)) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: currentId === null ? "新建密码库条目" : values["名称"], description: `${typeLabel(type)} · ${currentId === null ? "尚未保存" : `更新于 ${formatDetailTime(updatedAt)}`}`, actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: currentId === null, onClick: () => void openHistory(), children: "历史记录" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", disabled: currentId === null, onClick: () => setDeleteOpen(true), children: "删除" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => void save2(), children: "保存" })
      ] }), scroll: true, emphasis: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Section, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Title3, { children: "基础信息" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(FormLabel, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "条目类型" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { value: type, onChange: (event) => setType(event.target.value), options: TYPES.slice(1) })
        ] }),
        fields.map(([label, secret]) => {
          const multiline = label === "备注" || label === "助记词";
          const canCopy = secret || label === "账号" || label === "钱包地址" || label === "地址";
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(FormLabel, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: label }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(LayoutSlot, { variant: "vault-field-control", children: [
              multiline ? /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { "aria-label": label, rows: 4, value: values[label] ?? "", onChange: (event) => setValues((current) => ({ ...current, [label]: event.target.value })) }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { "aria-label": label, type: secret ? "password" : "text", value: values[label] ?? "", onChange: (event) => setValues((current) => ({ ...current, [label]: event.target.value })) }),
              canCopy ? /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: (values[label] ?? "") === "", onClick: () => void copy(label), children: "复制" }) : null
            ] })
          ] }, label);
        })
      ] }) }),
      exportStatus !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { children: exportStatus }) : null
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: historyOpen, title: "历史记录", onClose: () => setHistoryOpen(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: selectedHistoryId === null, onClick: () => void restoreHistory(), children: "恢复旧值" }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Header, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "字段" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "备注" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "时间" })
      ] }),
      histories.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "暂无历史记录" }) : histories.map((history) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Pressable, { selected: selectedHistoryId === history.historyId, onClick: () => setSelectedHistoryId(history.historyId), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: history.fieldName }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: history.changeRemark }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: formatDetailTime(history.createdAt) })
      ] }, history.historyId))
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: deleteOpen, title: "删除条目", onClose: () => setDeleteOpen(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDeleteOpen(false), children: "否" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => void remove(), children: "是" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Paragraph, { children: [
      "确认删除 [",
      values["名称"] ?? "",
      "]？"
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: warning !== "", title: "我的钱包", onClose: () => setWarning(""), footer: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => setWarning(""), children: "确定" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: warning }) }),
    toast !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: toast }) : null
  ] });
}
function createDraft() {
  return { 名称: "新条目", 分类: "常用" };
}
function typeLabel(type) {
  return TYPES.find((item) => item.value === type)?.label ?? "登录密码";
}
function isVaultType(value) {
  return value === "Login" || value === "Wallet" || value === "ApiKey" || value === "Server";
}
function isVaultItem(value) {
  return typeof value === "object" && value !== null && "itemId" in value && typeof value.itemId === "string" && "itemType" in value && typeof value.itemType === "string" && "name" in value && typeof value.name === "string" && "publicMetadataJson" in value && typeof value.publicMetadataJson === "string";
}
function isVaultDetail(value) {
  return typeof value === "object" && value !== null && "item" in value && isVaultItem(value.item) && "secret" in value && (value.secret === null || typeof value.secret === "string");
}
function isVaultHistory(value) {
  return typeof value === "object" && value !== null && "historyId" in value && typeof value.historyId === "string" && "itemId" in value && typeof value.itemId === "string" && "fieldName" in value && typeof value.fieldName === "string" && "changeRemark" in value && typeof value.changeRemark === "string" && "createdAt" in value && typeof value.createdAt === "string";
}
function parseRecord(value) {
  try {
    const parsed = JSON.parse(value);
    return typeof parsed === "object" && parsed !== null && !Array.isArray(parsed) ? Object.fromEntries(Object.entries(parsed).filter((entry) => typeof entry[1] === "string")) : {};
  } catch {
    return {};
  }
}
function makeDto(itemId, itemType, values, createdAt, updatedAt) {
  return { itemId, itemType, name: values["名称"] ?? "", category: values["分类"] ?? "常用", account: values["账号"] ?? "", url: values["网址"] ?? "", platform: values["平台"] ?? "", publicMetadataJson: JSON.stringify({ Remark: values["备注"] ?? "", ChainType: values["链类型"] ?? "", WalletAddress: values["钱包地址"] ?? "", ServerAddress: values["地址"] ?? "", ServerPort: values["端口"] ?? "" }), hasProtectedSecret: true, createdAt, updatedAt };
}
function encodeSecrets(values) {
  return JSON.stringify({ Password: values["密码"] ?? "", ApiKey: values["API Key"] ?? "", Secret: values.Secret ?? "", PrivateKey: values["私钥"] ?? "", Mnemonic: values["助记词"] ?? "" });
}
function listSubtitle(item) {
  const metadata = parseRecord(item.publicMetadataJson);
  const value = item.itemType === "Wallet" ? metadata.WalletAddress : item.itemType === "ApiKey" ? item.platform : item.itemType === "Server" ? metadata.ServerAddress : item.account;
  return value || "未填写账号或地址";
}
function formatListTime(value) {
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? "--" : `${(date.getMonth() + 1).toString().padStart(2, "0")}-${date.getDate().toString().padStart(2, "0")} ${date.getHours().toString().padStart(2, "0")}:${date.getMinutes().toString().padStart(2, "0")}`;
}
function formatDetailTime(value) {
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? "--" : `${date.getFullYear()}-${(date.getMonth() + 1).toString().padStart(2, "0")}-${date.getDate().toString().padStart(2, "0")} ${date.getHours().toString().padStart(2, "0")}:${date.getMinutes().toString().padStart(2, "0")}`;
}
function AgentConfirmPage() {
  const [request, setRequest] = reactExports.useState(null);
  const [busy, setBusy] = reactExports.useState(false);
  const [error, setError] = reactExports.useState("");
  const load = reactExports.useCallback(async () => {
    const response = await bridge.agentConfirmation.get();
    if (response.success) {
      setRequest(response.payload);
      setBusy(false);
      setError("");
    } else
      setError(response.error?.message ?? "确认请求读取失败。");
  }, []);
  reactExports.useEffect(() => {
    void load();
    window.addEventListener("focus", load);
    return () => window.removeEventListener("focus", load);
  }, [load]);
  async function finish(approved) {
    if (request === null || busy)
      return;
    setBusy(true);
    const response = await bridge.agentConfirmation.resolve(request.requestId, approved);
    if (!response.success || response.payload?.resolved !== true) {
      setError(response.error?.message ?? "确认请求已失效。");
      setBusy(false);
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "确认执行" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ProductWorkspace, { layout: "center", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: request?.capabilityName ?? "等待确认请求", description: request?.displayName ?? "正在读取需要确认的能力。", footer: /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: request === null || busy, onClick: () => void finish(false), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", loading: busy, disabled: request === null, onClick: () => void finish(true), children: "继续执行" })
    ] }), children: request === null ? "暂无待确认请求" : "等待你的确认" }), scroll: true, emphasis: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "warning", title: "请确认执行范围", children: "继续后将按下方参数执行该能力。" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "metrics", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "风险等级", value: request?.riskLevel || "—" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "执行器类型", value: request?.executorType || "—" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Field, { label: "说明", value: request?.summary ?? "" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: "执行参数" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { rows: 3, readOnly: true, value: request?.argsJson ?? "{}" })
      ] }),
      error !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "error", title: "无法继续", children: error }) : null
    ] }) }) })
  ] });
}
function Field({ label, value, accent = false }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: label }),
    accent ? /* @__PURE__ */ jsxRuntimeExports.jsx(Strong, { children: value || "—" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: value || "—" })
  ] });
}
function TrayMenuPage() {
  const safeAudio = { muted: true, volume: 0 };
  const confirmedAudio = reactExports.useRef(safeAudio);
  const surfaceRef = reactExports.useRef(null);
  const heightReported = reactExports.useRef(false);
  const [audio, setAudio] = reactExports.useState(safeAudio);
  const [music, setMusic] = reactExports.useState(null);
  const [error, setError] = reactExports.useState("");
  const [ready, setReady] = reactExports.useState(false);
  const [coreVersion, setCoreVersion] = reactExports.useState("");
  reactExports.useEffect(() => {
    void Promise.allSettled([loadMasterAudio(), loadCurrentMusic(), loadCoreVersion()]).then(([audioResult, musicResult, coreResult]) => {
      if (audioResult.status === "fulfilled") {
        confirmedAudio.current = audioResult.value;
        setAudio(audioResult.value);
      } else {
        setError(messageOf(audioResult.reason, "主音量设置读取失败。"));
      }
      if (musicResult.status === "fulfilled") setMusic(musicResult.value);
      else setError(messageOf(musicResult.reason, "当前音乐读取失败。"));
      if (coreResult.status === "fulfilled") setCoreVersion(coreResult.value);
      setReady(true);
    });
    return bridge.events.subscribe(
      ["music.playback.requested", "music.playback.state_changed", "music.playback.stopped"],
      (event) => updateMusicFromEvent(event, setMusic)
    );
  }, []);
  const save2 = async (next) => {
    const normalized = { muted: next.muted || next.volume <= 0, volume: next.volume };
    const response = await bridge.core.invoke({
      type: "settings.save",
      payload: { values: { master_audio_muted: String(normalized.muted), master_audio_volume: String(normalized.volume) } }
    });
    if (response.success) {
      confirmedAudio.current = normalized;
      setAudio(normalized);
      setError("");
    } else {
      setAudio(confirmedAudio.current);
      setError(response.error?.message ?? "主音量设置保存失败。");
    }
  };
  const controlMusic = async (action) => {
    const response = await bridge.core.invoke({
      type: action === "toggle-pause" ? "music.toggle_pause" : "music.stop",
      payload: {}
    });
    if (!response.success) {
      setError(response.error?.message ?? "音乐控制失败。");
      return;
    }
    setMusic(action === "stop" ? null : isPlaybackState(response.payload) ? response.payload : music);
    setError("");
  };
  const run = async (action) => {
    const response = await bridge.tray.action(action);
    if (!response.success) setError(response.error?.message ?? "托盘操作失败。");
  };
  const openShowcase = async () => {
    const response = await bridge.window.open("ui-showcase");
    if (!response.success) {
      setError(response.error?.message ?? "控件展示窗口打开失败。");
      return;
    }
    await bridge.window.close();
  };
  reactExports.useLayoutEffect(() => {
    const surface = surfaceRef.current;
    if (!ready || surface === null || heightReported.current) return;
    heightReported.current = true;
    const height = Math.ceil(surface.getBoundingClientRect().height);
    void bridge.tray.resize(height).then((response) => {
      if (!response.success) setError(response.error?.message ?? "托盘尺寸更新失败。");
    });
  }, [ready]);
  const hasMusic = music !== null && music.url !== "" && (music.isPlaying || music.isPaused);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(TrayMenuSurface, { ref: surfaceRef, onKeyDown: (event) => {
    if (event.key === "Escape") void bridge.window.close();
  }, tabIndex: -1, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => run("show"), children: "显示" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Divider, {}),
    hasMusic ? /* @__PURE__ */ jsxRuntimeExports.jsx(
      TrayMusicPlayer,
      {
        title: music.title,
        singer: music.singer,
        paused: music.isPaused,
        onTogglePause: () => void controlMusic("toggle-pause"),
        onStop: () => void controlMusic("stop")
      }
    ) : /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "muted", children: "当前未播放音乐" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Divider, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: audio.muted ? "primary" : "secondary", onClick: () => void save2({ ...audio, muted: !audio.muted }), children: audio.muted ? "声音：已静音" : "声音：正常" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      Range,
      {
        label: "主音量",
        valueLabel: `${audio.volume}%`,
        min: "0",
        max: "100",
        step: "1",
        value: audio.volume,
        onChange: (event) => setAudio({ muted: Number(event.target.value) <= 0, volume: Number(event.target.value) }),
        onPointerUp: (event) => {
          const volume = Number(event.currentTarget.value);
          void save2({ muted: volume <= 0, volume });
        },
        onKeyUp: (event) => {
          const volume = Number(event.currentTarget.value);
          void save2({ muted: volume <= 0, volume });
        }
      }
    ) }),
    error !== "" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Text, { size: "xs", tone: "danger", children: error }) : null,
    /* @__PURE__ */ jsxRuntimeExports.jsx(Divider, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => void openShowcase(), children: "控件展示" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Divider, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => run("reset-position"), children: "位置回归" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => run("hide"), children: "隐藏" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => run("quit"), children: "退出" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Divider, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Text, { size: "xs", tone: "muted", children: [
      "v",
      coreVersion || "..."
    ] })
  ] });
}
async function loadMasterAudio() {
  const response = await bridge.core.invoke({ type: "settings.get", payload: { keys: ["master_audio_muted", "master_audio_volume"] } });
  if (!response.success) throw new Error(response.error?.message ?? "主音量设置读取失败。");
  const payload = response.payload;
  const settings = new Map(payload?.settings?.map((item) => [item.key, item.value]));
  const volume = Math.min(100, Math.max(0, Number.parseInt(settings.get("master_audio_volume") ?? "100", 10) || 0));
  return { muted: settings.get("master_audio_muted")?.toLowerCase() === "true" || volume <= 0, volume };
}
async function loadCurrentMusic() {
  const response = await bridge.core.invoke({ type: "music.current", payload: {} });
  if (!response.success) throw new Error(response.error?.message ?? "当前音乐读取失败。");
  return isPlaybackState(response.payload) && response.payload.url !== "" ? response.payload : null;
}
async function loadCoreVersion() {
  const response = await bridge.core.status();
  if (!response.success) throw new Error(response.error?.message ?? "Core 状态读取失败。");
  const payload = response.payload;
  if (payload !== null && typeof payload === "object" && "coreVersion" in payload && typeof payload.coreVersion === "string") {
    return payload.coreVersion;
  }
  throw new Error("Core 版本号不存在。");
}
function updateMusicFromEvent(event, update) {
  if (event.type === "music.playback.stopped") {
    update(null);
    return;
  }
  const data = isRecord(event.payload) ? event.payload.data : null;
  if (isRecord(data) && isPlaybackState(data.playback)) update(data.playback);
}
function isPlaybackState(value) {
  return isRecord(value) && typeof value.url === "string" && typeof value.title === "string" && typeof value.singer === "string" && typeof value.isPlaying === "boolean" && typeof value.isPaused === "boolean";
}
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function messageOf(reason, fallback) {
  return reason instanceof Error && reason.message.trim() !== "" ? reason.message : fallback;
}
function DouyinLoginPage() {
  const [status, setStatus] = reactExports.useState("正在启动 App 专用浏览器…");
  const [ready, setReady] = reactExports.useState(false);
  const [saving, setSaving] = reactExports.useState(false);
  const webview = reactExports.useRef(null);
  reactExports.useEffect(() => {
    const node = webview.current;
    if (node === null)
      return;
    const started = () => {
      setReady(false);
      setStatus("正在打开抖音…");
    };
    const stopped = () => {
      setReady(true);
      setStatus("页面已打开。完成登录/验证并确认页面可用后保存会话。");
    };
    const failed = () => {
      setReady(false);
      setStatus("抖音页面加载失败，请检查网络后重试。");
    };
    node.addEventListener("did-start-loading", started);
    node.addEventListener("did-stop-loading", stopped);
    node.addEventListener("did-fail-load", failed);
    return () => {
      node.removeEventListener("did-start-loading", started);
      node.removeEventListener("did-stop-loading", stopped);
      node.removeEventListener("did-fail-load", failed);
    };
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductPage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "登录/刷新抖音会话" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductWorkspace, { layout: "single", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "抖音登录页面", scroll: true, emphasis: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalWebview, { ref: webview, source: "https://www.douyin.com/", partition: "persist:aimaid-douyin", label: "抖音登录页面" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: saving, onClick: () => void bridge.window.close(), children: "暂不保存" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", disabled: !ready || saving, onClick: () => void save(setStatus, setSaving), children: "保存会话并关闭" })
      ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: status }) })
    ] })
  ] });
}
async function save(setStatus, setSaving) {
  setSaving(true);
  setStatus("正在保存会话状态…");
  const response = await bridge.douyin.saveSession();
  if (!response.success) {
    setSaving(false);
    setStatus("保存会话失败：" + (response.error?.message ?? "未知错误"));
    return;
  }
  void bridge.window.close();
}
const ICONS = [
  "pause",
  "layers",
  "image",
  "clock",
  "folder",
  "gauge",
  "user",
  "repeat",
  "sparkles",
  "heart",
  "trash",
  "activity",
  "grid",
  "message",
  "palette",
  "settings"
];
function UiShowcasePage() {
  const toast = useToast();
  const [checked, setChecked] = reactExports.useState(true);
  const [enabled, setEnabled] = reactExports.useState(true);
  const [tab, setTab] = reactExports.useState("normal");
  const [menuOpen, setMenuOpen] = reactExports.useState(false);
  const [dialogOpen, setDialogOpen] = reactExports.useState(false);
  const [drawerOpen, setDrawerOpen] = reactExports.useState(false);
  const [range, setRange] = reactExports.useState(60);
  const [radio, setRadio] = reactExports.useState("standard");
  const [popoverOpen, setPopoverOpen] = reactExports.useState(false);
  const [listItem, setListItem] = reactExports.useState("one");
  const [confirmOpen, setConfirmOpen] = reactExports.useState(false);
  const [query, setQuery] = reactExports.useState("");
  const [segment, setSegment] = reactExports.useState("list");
  const [page, setPage] = reactExports.useState(1);
  const [combo, setCombo] = reactExports.useState("role-a");
  const [tree, setTree] = reactExports.useState("voice");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcasePage, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(WindowTitleBar, { title: "控件展示" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseContent, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(ShowcaseIntro, { title: "基础控件", meta: /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: "GLOBAL UI" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "按钮 Button", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", children: "主要按钮" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { children: "次要按钮" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", children: "幽灵按钮" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", children: "危险按钮" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", children: "小尺寸" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "md", children: "中尺寸" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "lg", children: "大尺寸" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { disabled: true, children: "禁用" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { loading: true, children: "加载中" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "图标与图标按钮 IconButton", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ShowcaseIconGrid, { children: ICONS.map((name) => /* @__PURE__ */ jsxRuntimeExports.jsx(ShowcaseIcon, { icon: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name }), label: name }, name)) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "小图标按钮", size: "sm", children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: "settings" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "中图标按钮", children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: "heart" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "大图标按钮", size: "lg", children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: "sparkles" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "禁用图标按钮", disabled: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(UiIcon, { name: "trash" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(IconButton, { label: "加载图标按钮", loading: true })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "表单控件 Form Controls", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseFormGrid, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "普通输入框" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "错误输入框", defaultValue: "错误内容", error: "这里显示校验错误" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "禁用输入框", value: "不可编辑", disabled: true, readOnly: true }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "下拉选择", defaultValue: "normal", options: [{ value: "normal", label: "普通选项" }, { value: "selected", label: "另一个选项" }, { value: "disabled", label: "禁用选项", disabled: true }] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "错误选择", error: "请选择有效项目", defaultValue: "", options: [{ value: "", label: "请选择" }, { value: "valid", label: "有效项目" }] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Select, { label: "禁用选择", disabled: true, defaultValue: "normal", options: [{ value: "normal", label: "不可操作" }] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { label: "多行文本", defaultValue: "这里展示多行文本输入框。" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Textarea, { label: "错误文本", defaultValue: "需要修改的内容", error: "内容格式不正确" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(SearchInput, { label: "搜索输入", value: "", onChange: () => void 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(NumberInput, { label: "数字输入", value: 12, onChange: () => void 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(DateInput, { label: "日期输入", value: "2026-07-22", onChange: () => void 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(DateTimeInput, { label: "日期时间", value: "2026-07-22T12:00", onChange: () => void 0 }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Range, { label: "滑动条", value: range, valueLabel: `${range}%`, min: 0, max: 100, onChange: (event) => setRange(Number(event.target.value)) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(RadioGroup, { label: "单选组", value: radio, onChange: setRadio, options: [{ value: "soft", label: "柔和", description: "较低对比度" }, { value: "standard", label: "标准" }, { value: "clear", label: "清晰", disabled: true }] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Checkbox, { label: "复选框", checked, onChange: (event) => setChecked(event.target.checked) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Checkbox, { label: "未选中" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Checkbox, { label: "禁用复选框", checked: true, disabled: true, readOnly: true }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "开关已开启", checked: enabled, onChange: (event) => setEnabled(event.target.checked) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "开关已关闭" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Switch, { label: "禁用开关", checked: true, disabled: true, readOnly: true })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "数据展示 Data Display", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: "默认" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: "强调" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "success", children: "成功" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "warning", children: "警告" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "danger", children: "危险" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tag, { children: "普通标签" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tag, { selected: true, children: "已选标签" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tag, { onRemove: () => void 0, children: "可移除标签" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { title: "信息提示", children: "页面内持续展示的重要信息。" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Alert, { tone: "warning", title: "需要注意", children: "此状态不会像 Toast 一样自动消失。" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Progress, { label: "任务进度", value: 68 }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Skeleton, { lines: 3 }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { fallback: "女", size: "sm" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { fallback: "仆" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { fallback: "长", size: "lg" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ColorPalette, { colors: ["var(--color-bg-canvas)", "var(--color-bg-elevated)", "var(--color-accent)", "var(--color-text-primary)"] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ListBox, { label: "列表示例", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(ListBoxItem, { selected: listItem === "one", leading: /* @__PURE__ */ jsxRuntimeExports.jsx(Avatar, { fallback: "A" }), title: "已选项目", badge: /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { tone: "accent", children: "当前" }), onSelect: () => setListItem("one") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ListBoxItem, { selected: listItem === "two", title: "普通项目", onSelect: () => setListItem("two") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ListBoxItem, { disabled: true, title: "禁用项目", onSelect: () => void 0 })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Card, { header: "卡片标题", footer: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", children: "卡片操作" }), children: "卡片正文使用统一边框、圆角和间距。" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Accordion, { title: "可折叠区域", defaultOpen: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "折叠内容遵循统一键盘与高度规则。" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(DataTable, { label: "示例数据表", rows: [{ id: "1", name: "角色资料", status: "正常" }, { id: "2", name: "语音记录", status: "处理中" }], rowKey: (row) => row.id, columns: [{ key: "name", header: "名称", render: (row) => row.name }, { key: "status", header: "状态", render: (row) => /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { children: row.status }) }] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Pagination, { page, pageSize: 10, total: 42, onPageChange: setPage })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "导航与反馈 Navigation / Feedback", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Tabs, { label: "控件状态", value: tab, onChange: setTab, items: [{ id: "normal", label: "普通" }, { id: "active", label: "选中" }, { id: "disabled", label: "禁用", disabled: true }] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { size: "sm", label: "小型加载" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { label: "中型加载" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Loading, { size: "lg", label: "大型加载" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Spinner, { size: "xl", label: "页面加载" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(CircularProgress, { value: 72, label: "圆形进度 72%" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "默认" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "正常" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(InlineText, { children: "失败" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseStateGrid, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(EmptyState, { title: "暂无内容", action: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", children: "创建内容" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ErrorState, { message: "操作未能完成，请检查后重试。", onRetry: () => toast.show("已触发重试示例。", "info") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(OfflineState, { onRetry: () => toast.show("已触发重新连接示例。", "info") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(UnauthorizedState, { onAuthorize: () => toast.show("已触发授权示例。", "info") })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ShowcaseSection, { title: "滚动区域 ScrollArea", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { maxHeight: "sm", children: Array.from({ length: 8 }, (_, index) => /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
        "滚动内容第 ",
        index + 1,
        " 行"
      ] }, index)) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "复合控件 Composites", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(SearchBox, { "aria-label": "统一搜索框", value: query, onChange: setQuery, onSearch: (value) => toast.show(`搜索：${value}`) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(SegmentedControl, { label: "布局方式", value: segment, onChange: setSegment, options: [{ value: "list", label: "列表" }, { value: "grid", label: "网格" }] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Combobox, { label: "角色搜索选择", value: combo, onChange: setCombo, options: [{ value: "role-a", label: "女仆 A" }, { value: "role-b", label: "女仆 B" }, { value: "role-c", label: "不可用角色", disabled: true }] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Tree, { label: "模型目录", selectedId: tree, onSelect: setTree, nodes: [{ id: "roles", label: "角色", children: [{ id: "voice", label: "语音角色" }, { id: "live2d", label: "Live2D 模型" }] }, { id: "media", label: "媒体库" }] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(FilePicker, { label: "文件选择", onFiles: (files) => toast.show(`已选择 ${files.length} 个文件`, "success") }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(AudioPlayer, { source: "", "aria-label": "音频播放器示例" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "工作区布局 Workspace Patterns", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductToolbar, { lead: /* @__PURE__ */ jsxRuntimeExports.jsx(SearchBox, { "aria-label": "工作区搜索", value: "", onChange: () => void 0 }), actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { children: "筛选" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", children: "新增" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductHero, { eyebrow: "正计时中", value: "25:00", detail: "主要数值保持唯一视觉焦点", actions: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", children: "暂停" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { children: "重置" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductPanel, { title: "数据概览", actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", children: "刷新" }), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(ProductGrid, { density: "metrics", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "当前状态", value: "正常" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "记录数量", value: "128" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ProductMetric, { label: "最近更新", value: "刚刚" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ProductStatusBar, { actions: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", children: "查看详情" }), children: "数据已同步" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseSection, { title: "浮层与通知 Overlays", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tooltip, { content: "这是基础 Tooltip 样式", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { children: "悬停提示" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Menu, { open: menuOpen, label: "示例菜单", onClose: () => setMenuOpen(false), items: [
            { id: "edit", label: "编辑", onSelect: () => toast.show("选择了编辑。") },
            { id: "disabled", label: "禁用项目", disabled: true, onSelect: () => void 0 },
            { id: "delete", label: "删除", danger: true, onSelect: () => toast.show("选择了危险操作。", "warning") }
          ], children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setMenuOpen((value) => !value), children: "展开菜单" }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDialogOpen(true), children: "打开对话框" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "danger", onClick: () => setConfirmOpen(true), children: "确认对话框" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDrawerOpen(true), children: "打开抽屉" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Popover, { open: popoverOpen, anchor: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setPopoverOpen((value) => !value), children: "切换 Popover" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "这是基础 Popover 内容。" }) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ContextMenu, { label: "示例右键菜单", items: [
          { id: "open", label: "打开", onSelect: () => toast.show("选择了打开。") },
          { id: "disabled", label: "禁用项目", disabled: true, onSelect: () => void 0 },
          { id: "remove", label: "删除", danger: true, onSelect: () => toast.show("选择了删除。", "warning") }
        ], children: /* @__PURE__ */ jsxRuntimeExports.jsx(Container, { children: "在这个区域点击右键，查看基础右键菜单" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(ShowcaseRow, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => toast.show("普通通知示例。"), children: "普通 Toast" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => toast.show("操作成功通知。", "success"), children: "成功 Toast" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => toast.show("请注意当前状态。", "warning"), children: "警告 Toast" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => toast.show("操作失败通知。", "error"), children: "错误 Toast" })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open: dialogOpen, title: "基础对话框", onClose: () => setDialogOpen(false), footer: /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setDialogOpen(false), children: "取消" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "primary", onClick: () => setDialogOpen(false), children: "确定" })
    ] }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "对话框内输入框" }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Drawer, { open: drawerOpen, title: "基础抽屉", onClose: () => setDrawerOpen(false), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Paragraph, { children: "这里展示抽屉的标题栏、内容区域和遮罩层。" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { label: "抽屉内输入框" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ConfirmDialog$1, { open: confirmOpen, title: "删除记录？", description: "删除后无法恢复。", confirmText: "删除", confirmVariant: "danger", onCancel: () => setConfirmOpen(false), onConfirm: () => {
      setConfirmOpen(false);
      toast.show("已确认删除示例。", "success");
    } })
  ] });
}
const PetPage = reactExports.lazy(() => __vitePreload(() => import("./PetPage-CSPLGTh0.js").then((n) => n.b), true ? [] : void 0, import.meta.url));
function App() {
  switch (bridge.window.kind) {
    case "main":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(MainPage, {});
    case "pet":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Suspense, { fallback: /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "pet-shell", children: "正在加载桌宠渲染入口…" }), children: /* @__PURE__ */ jsxRuntimeExports.jsx(PetPage, {}) });
    case "chat":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(PromptPage, {});
    case "settings":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(SettingsPage, {});
    case "reminders":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(ReminderPage, {});
    case "characters":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CharacterPage, {});
    case "template-card":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(TemplateCardPage, {});
    case "character-editor":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CharacterEditorPage, {});
    case "notebook":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(NotebookPage, {});
    case "video":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(VideoLibraryPage, {});
    case "video-player":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(VideoPlayerPage, {});
    case "video-subtitles":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(VideoSubtitlesPage, {});
    case "voice-conversation":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(VoiceConversationPage, {});
    case "status":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(StatusPage, {});
    case "appearance":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(AppearanceSettingsPage, {});
    case "bitcoin":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(BitcoinPage, {});
    case "crypto-events":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CryptoEventCenterPage, {});
    case "crypto-provider":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CryptoProviderSettingsPage, {});
    case "crypto-chart":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(CryptoChartPage, {});
    case "timer":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(TimerPage, {});
    case "remote-video":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteVideoPage, {});
    case "remote-site-config":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(RemoteSiteConfigPage, {});
    case "vault":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(VaultPage, {});
    case "scripts":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(ScriptsPage, {});
    case "agent-confirm":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(AgentConfirmPage, {});
    case "tray-menu":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(TrayMenuPage, {});
    case "douyin-login":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(DouyinLoginPage, {});
    case "ui-showcase":
      return /* @__PURE__ */ jsxRuntimeExports.jsx(UiShowcasePage, {});
  }
}
function GlobalUiRoot({ children }) {
  reactExports.useEffect(() => {
    const apply = () => applyStoredAppearance();
    apply();
    const unsubscribe = subscribeAppearance(apply);
    return unsubscribe;
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ToastProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "aimaid-ui-root", children }) });
}
const root = document.getElementById("root");
if (root === null) throw new Error("Renderer root element is missing");
const content = /* @__PURE__ */ jsxRuntimeExports.jsx(GlobalUiRoot, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(ErrorBoundary, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(App, {}) }) });
clientExports.createRoot(root).render(bridge.window.kind === "pet" ? content : /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.StrictMode, { children: content }));
export {
  Container as C,
  MUSIC_VISUALIZER_STYLE_KEY as M,
  Paragraph as P,
  TransparentStage as T,
  UiIcon as U,
  __vitePreload as _,
  PetAudioContourCanvas as a,
  bridge as b,
  createBottomBarLayout as c,
  createRadialVisualizerLayout as d,
  bottomBarSlots as e,
  bottomBarIdentity as f,
  canInterruptPetBubble as g,
  createPetBubbleMessage as h,
  isBackgroundMusicVisualizer as i,
  jsxRuntimeExports as j,
  PET_BUBBLE_STORAGE_KEY as k,
  PET_BUBBLE_HOLD_STORAGE_KEY as l,
  isPetBubbleKind as m,
  synthesizeAndPlayPages as n,
  playCachedAudio as o,
  publishAudioLipSync as p,
  PetPanelSurface as q,
  reactExports as r,
  selectNextPetBubble as s,
  PetItemSurface as t,
  playLocalAudioPaths as u,
  TransparentCanvas as v,
  ContextMenuSurface as w,
  parseMusicVisualizerStyle as x
};
