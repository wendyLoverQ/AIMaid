# UI 交互 TODO

这些事项需要未来 Electron/React UI 实现，但核心项目只提供 Command、Query、Event，不依赖 UI。

## 聊天

- `TODO(UI)` 订阅 `chat.delta` 增量渲染，并提供停止生成操作。
- `TODO(UI)` 明确展示模型错误、重试入口和会话切换。
- `TODO(UI)` TTS 播放、气泡展示和聊天文本是三个独立消费者，不能互相假定成功。

## 角色

- `TODO(UI)` 角色卡编辑前显示校验结果，保存后订阅 `character.changed`。
- `TODO(UI)` Electron 内部根据角色事件同步立绘、Live2D 模型与语音预览；C# 不通过管道控制渲染器。
- `TODO(UI)` 文件路径通过选择器产生，核心不弹文件窗口。

## 配置

- `TODO(UI)` 使用设置项、分组和开关呈现，不让普通用户编辑 JSON。
- `TODO(UI)` 保存后标明立即生效或重启生效；密钥只显示掩码。
- `TODO(UI)` 服务地址提供健康检查按钮和明确失败原因。

## 任务与下载

- `TODO(UI)` 订阅 `task.progress`，展示真实字节进度、速度和取消状态。
- `TODO(UI)` 取消后等待最终事件，不提前显示“已取消”。
- `TODO(UI)` 下载目录必须由用户配置或选择，不能依赖开发机器绝对路径。

## 文件与媒体

- `TODO(UI)` 覆盖、删除前显示精确路径并要求确认。
- `TODO(UI)` 打开外部媒体程序失败时展示可配置程序路径入口。
- `TODO(UI)` 文件选择、拖放与最近目录属于 UI 壳职责。

## ComfyUI

- `TODO(UI)` 显示排队、执行、预览、完成和节点错误，不能只显示旋转加载状态。
- `TODO(UI)` 工作流输入由 schema 生成控件，核心只接收最终 inputs。

## TTS / ASR

- `TODO(UI)` 提供录音权限、录音状态、停止和转写失败反馈。
- `TODO(UI)` TTS 播放队列、暂停和当前音频状态由后续事件契约补齐。

## 高风险操作

- `TODO(UI)` Agent、进程终止、文件删除、外部脚本等必须在 UI 中显示目标和风险并取得授权。
- `TODO(UI)` 不允许用静默兜底掩盖用户拒绝、权限不足或外部服务失败。
